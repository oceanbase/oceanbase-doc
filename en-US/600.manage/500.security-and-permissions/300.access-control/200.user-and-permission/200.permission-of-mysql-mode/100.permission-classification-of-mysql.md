|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type|MySQL Mode|

# Privilege types in MySQL mode

This topic describes the privilege types in OceanBase Database's MySQL mode.

## Privilege types

In MySQL mode, privileges are divided into three categories:

* Global privileges: the privileges to manage a tenant, such as modifying system settings and accessing all tables in the tenant.
* Database privileges: the privileges to manage all objects in a database, such as creating or deleting tables in the database and accessing those tables.
* Object privileges: the privileges to manage a specific object, such as accessing a specific table, view, or index.

The following table describes the privileges related to MySQL mode in OceanBase Database.

| Privilege category   |           Privilege         |                                         Description                                          |
|-----------|------------------------|---------------------------------------------------------------------------------------|
|  Object privileges  | CREATE                 | Determines whether the user can execute the `CREATE TABLE` statement.       |
|  Object privileges  | SELECT                 | Determines whether the user can query data from all or specific columns in a table.                                           |
|  Object privileges  | INSERT                 | Determines whether the user can insert data into all or specific columns in a table.                                        |
|  Object privileges  | UPDATE                 | Determines whether the user can update data in all or specific columns in a table.                                            |
|  Object privileges  | DELETE                 | Determines whether the user can delete existing data.                                                          |
|  Object privileges  | DROP                   | Determines whether the user can execute statements such as `DROP TABLE` and `DROP VIEW`. |
|  Object privileges  | INDEX                  | Determines whether the user can create and drop table indexes.                                                       |
|  Object privileges  | ALTER                  | Determines whether the user can execute statements such as `RENAME TABLE` and `ALTER TABLE`.|
|  Object privileges  | CREATE VIEW            | Determines whether the user can create views.                                                              |
|  Object privileges  | SHOW VIEW              | Determines whether the user can query views or understand how views execute.                                              |
|  Object privileges  | REFERENCES             | Determines whether the user can create foreign keys. To create a foreign key for a table, the user must have the REFERENCES privilege on the parent table.                    |
|  Object privileges  | TRIGGER                | Determines whether the user has the privilege to activate triggers. The user must have this privilege to create, view, or drop triggers.                   |
| Database privileges | SELECT                 | Determines whether the user can query data from tables.                                                          |
| Database privileges | INSERT                 | Determines whether the user can insert rows into tables.                                                        |
| Database privileges | UPDATE                 | Determines whether the user can modify existing data.                                                            |
| Database privileges | DELETE                 | Determines whether the user can delete existing data.                                                            |
| Database privileges | CREATE                 | Determines whether the user can execute statements such as `CREATE DATABASE`, `CREATE TABLE`, `CREATE SEQUENCE`, and `CREATE OUTLINE`. |
| Database privileges | DROP                   | Determines whether the user can execute statements such as `DROP DATABASE`, `DROP TABLE`, `DROP VIEW`, `DROP SEQUENCE`, and `DROP OUTLINE`.|
| Database privileges | INDEX                  | Determines whether the user can create and drop table indexes.                                                        |
| Database privileges | ALTER                  | Determines whether the user can execute statements such as `ALTER DATABASE`, `RENAME TABLE`, `ALTER TABLE`, `ALTER SEQUENCE`, and `ALTER OUTLINE`. |
| Database privileges | CREATE VIEW            | Determines whether the user can create views.                                                               |
| Database privileges | SHOW VIEW              | Determines whether the user can query views or understand how views execute.                                               |
| Database privileges | CREATE ROUTINE         | Determines whether the user can create procedures and functions.                                             |
| Database privileges | ALTER ROUTINE          | Determines whether the user can modify or drop procedures and functions.                                        |
| Database privileges | EXECUTE                | Determines whether the user can execute procedures and functions.                                              |
| Database privileges | REFERENCES             | Determines whether the user can create foreign keys.                |
| Database privileges | TRIGGER                | Determines whether the user has the privilege to activate triggers. The user must have this privilege to create, view, or drop triggers.                   |
|  Global privileges  | CREATE                 | Determines whether the user can execute statements such as `CREATE DATABASE`, `CREATE TABLEGROUP`, `CREATE TABLE`, `CREATE SEQUENCE`, and `CREATE OUTLINE`.        |
|  Global privileges  | ALTER                  | Determines whether the user can execute statements such as `ALTER DATABASE`, `ALTER TABLEGROUP`, `RENAME TABLE`, `ALTER TABLE`, `ALTER SEQUENCE`, and `ALTER OUTLINE`. |
|  Global privileges  | SELECT                 | Determines whether the user can query data from tables.                                                            |
|  Global privileges  | INSERT                 | Determines whether the user can insert data into tables.                                                          |
|  Global privileges  | UPDATE                 | Determines whether the user can modify existing data.                                                              |
|  Global privileges  | DELETE                 | Determines whether the user can delete existing data.                                                              |
|  Global privileges  | DROP                   | Determines whether the user can execute statements such as `DROP DATABASE`, `DROP TABLEGROUP`, `DROP TABLE`, `DROP VIEW`, `DROP SEQUENCE`, and `DROP OUTLINE`.  |
|  Global privileges  | INDEX                  | Determines whether the user can create and drop table indexes.                                                           |
|  Global privileges  | CREATE VIEW            | Determines whether the user can create views.                                                                  |
|  Global privileges  | SHOW VIEW              | Determines whether the user can query views or understand how views execute.                                                  |
|  Global privileges  | REFERENCES             | Determines whether the user can create foreign keys.                 |
|  Global privileges  | ALTER TENANT           | The privilege to modify tenant information.                                                                        |
|  Global privileges  | ALTER SYSTEM           | The privilege to execute the `ALTER SYSTEM` statement.                                                            |
|  Global privileges  | CREATE   RESOURCE POOL | The privilege to create, modify, and delete resource pools.                                                               |
|  Global privileges  | CREATE   RESOURCE UNIT | The privilege to create, modify, and delete resource units.                                                              |
|  Global privileges  | CREATE DATABASE LINK   | The privilege to create database links.                                                                       |
|  Global privileges  | DROP DATABASE LINK     | The privilege to drop database links.                                                                       |
|  Global privileges  | CREATE USER            | Determines whether the user can execute statements such as `CREATE USER`, `ALTER USER`, `DROP USER`, `CREATE ROLE`, and `DROP ROLE`.|
|  Global privileges  | CREATE ROUTINE         | Determines whether the user can create procedures and functions.                                                 |
|  Global privileges  | ALTER ROUTINE          | Determines whether the user can modify or drop procedures and functions.                                            |
|  Global privileges  | CREATE TABLESPACE      | Determines whether the user can execute statements such as `CREATE TABLESPACE`, `ALTER TABLESPACE`, and `DROP TABLESPACE`.      |
|  Global privileges  | CREATE ROLE            | Determines whether the user can execute the `CREATE ROLE` statement.|
|  Global privileges  | DROP ROLE              | Determines whether the user can execute the `DROP ROLE` statement.|
|  Global privileges  | EXECUTE                | Determines whether the user can execute procedures and functions.                                                  |
|  Global privileges  | PROCESS                | Determines whether the user can view the processes of other users by using the `SHOW PROCESSLIST` statement.                                |
|  Global privileges  | TRIGGER                | Determines whether the user has the privilege to activate triggers. The user must have this privilege to create, view, or drop triggers.                         |
|  Global privileges  | SHOW DB                | Determines whether the user can view the names of all databases on the server, including those that the user does not have sufficient access privileges to.                   |
|  Global privileges  | SHUTDOWN               | Determines whether the user can execute the `mysqladmin shutdown` statement. <main id="notice" type='explain'><h4>Note</h4><p>You can grant and revoke this privilege to a user using the <code>GRANT</code> and <code>REVOKE</code> statements, but the granted privilege does not take effect immediately. </p></main> |
|  Global privileges  | RELOAD                 | Determines whether the user can perform flush operations. <main id="notice" type='explain'><h4>Note</h4><p>You can grant and revoke this privilege to a user using the <code>GRANT</code> and <code>REVOKE</code> statements, but the granted privilege does not take effect immediately. </p></main>|
|  Global privileges  | FILE                   | Determines whether the user can execute the `SELECT INTO OUTFILE` and `LOAD DATA INFILE` statements.                       |
|  Global privileges  | SUPER                  | Determines whether the user can perform powerful administrative tasks, such as using the `KILL` statement to delete user processes, using the `SET GLOBAL` statement to modify global MySQL variables, and executing replication and log-related statements.|
|  Global privileges  | GRANT OPTION           | Allows the user to grant or revoke the same privileges that they have to other users. Generally, it is used in conjunction with other privileges, such as `SELECT`, `INSERT`, and `UPDATE`.|
|  Global privileges  | ALL PRIVILEGES         | Indicates that the user has all available privileges in the database except the `GRANT OPTION` privilege.|
|  Global privileges  | PROXY                  | Allows a user to connect to and operate on the database on behalf of another user. For example, user A can grant the PROXY privilege on user A to user B so that user B can connect to and operate on the database as user A, even without disclosing user A's password. This privilege is supported in V4.3.5 BP1 and later. |