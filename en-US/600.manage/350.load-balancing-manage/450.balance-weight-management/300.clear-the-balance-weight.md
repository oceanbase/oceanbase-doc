|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||

# Clear the partition weight

After setting the partition weight, you can clear the original partition weight based on business needs.

## Limitations

* You can manually clear the partition weights for user tenants only. You cannot manually clear the partition weights for system tenants.

* Clearing the table-level partition weights deletes the weights set for all partitions. If partition-level weights are set for the table, they will be retained.

## Clear partition weights in MySQL mode

1. Log in to the MySQL tenant of the cluster.

   The following example shows how to connect to the database. Please replace the actual environment parameters.

   ```shell
   obclient -h10.xx.xx.xx -P2883 -uroot@mysql001#obdemo -p***** -A
   ```

2. Query the partition weights of the table.

   ```shell
   obclient(root@mysql001)[oceanbase]> SELECT * FROM oceanbase.DBA_OB_OBJECT_BALANCE_WEIGHT;
   ```

   The query result is as follows:

   ```shell
   +----------+--------------+-----------------+--------+---------------+------------+----------------+-------------------+-----------------+-------------+---------------+-----------+
   | TABLE_ID | PARTITION_ID | SUBPARTITION_ID | WEIGHT | DATABASE_NAME | TABLE_NAME | PARTITION_NAME | SUBPARTITION_NAME | TABLEGROUP_NAME | DATABASE_ID | TABLEGROUP_ID | OBJECT_ID |
   +----------+--------------+-----------------+--------+---------------+------------+----------------+-------------------+-----------------+-------------+---------------+-----------+
   |   500002 |         NULL |            NULL |      1 | test          | tbl1_h     | NULL           | NULL              | NULL            |      500001 |          NULL |    500002 |
   |   500002 |       500003 |            NULL |      2 | test          | tbl1_h     | p0             | NULL              | NULL            |      500001 |          NULL |    500003 |
   +----------+--------------+-----------------+--------+---------------+------------+----------------+-------------------+-----------------+-------------+---------------+-----------+
   2 rows in set
   ```

3. Clear the partition weights of the table.

   * Clear the table-level partition weights

      Clear the partition weights of the `tbl1_h` table. The statement is as follows:

      ```shell
      obclient(root@mysql001)[test]> CALL DBMS_BALANCE.CLEAR_BALANCE_WEIGHT('test','tbl1_h');
      ```

   * Clear the partition-level weights of a partition

      Clear the partition weights of the `p0` partition in the `tbl1_h` table. The statement is as follows:

      ```shell
      obclient(root@mysql001)[test]> CALL DBMS_BALANCE.CLEAR_BALANCE_WEIGHT('test','tbl1_h','p0');
      ```

## Clear partition weights in Oracle mode

1. Log in to the Oracle tenant of the cluster.

   The following example shows how to connect to the database. Please replace the parameters with those in your actual environment.

   ```shell
   obclient -h10.xx.xx.xx -P2883 -usys@oracle001#obdemo -p***** -A
   ```

2. Query the partition weights of the table.

   ```shell
   obclient(SYS@oracle001)[SYS]> SELECT * FROM SYS.DBA_OB_OBJECT_BALANCE_WEIGHT;
   ```

   The query result is as follows:

   ```shell
   +----------+--------------+-----------------+--------+---------------+------------+----------------+-------------------+-----------------+-------------+---------------+-----------+
   | TABLE_ID | PARTITION_ID | SUBPARTITION_ID | WEIGHT | DATABASE_NAME | TABLE_NAME | PARTITION_NAME | SUBPARTITION_NAME | TABLEGROUP_NAME | DATABASE_ID | TABLEGROUP_ID | OBJECT_ID |
   +----------+--------------+-----------------+--------+---------------+------------+----------------+-------------------+-----------------+-------------+---------------+-----------+
   |   500002 |         NULL |            NULL |      1 | SYS           | TBL1_H     | NULL           | NULL              | NULL            |      201006 |          NULL |    500002 |
   |   500002 |       500003 |            NULL |      2 | SYS           | TBL1_H     | P0             | NULL              | NULL            |      201006 |          NULL |    500003 |
   +----------+--------------+-----------------+--------+---------------+------------+----------------+-------------------+-----------------+-------------+---------------+-----------+
   2 rows in set
   ```

3. Clear the partition weights of the table.

   * Clear the table-level weight

      The following example clears the partition weights of the `TBL1_H` table.

      ```shell
      obclient(SYS@oracle001)[SYS]> delimiter //
      ```

      ```shell
      obclient(SYS@oracle001)[SYS]> BEGIN
            DBMS_BALANCE.CLEAR_BALANCE_WEIGHT('SYS','TBL1_H');
            END;//
      ```

      ```shell
      obclient(SYS@oracle001)[SYS]> delimiter ;
      ```

   * Clear the partition-level weight

      The following example clears the partition-level weight of the `P0` partition in the `TBL1_H` table.

      ```shell
      obclient(SYS@oracle001)[SYS]> delimiter //
      ```

      ```shell
      obclient(SYS@oracle001)[SYS]> BEGIN
          DBMS_BALANCE.CLEAR_BALANCE_WEIGHT('SYS','TBL1_H','P0');
          END;//
      ```

      ```shell
      obclient(SYS@oracle001)[SYS]> delimiter ;
      ```

## Next steps

After clearing the partition weights, the partition distribution on the table may become unbalanced. If you want to achieve a balanced partition distribution as soon as possible, you can call the `DBMS_BALANCE.TRIGGER_PARTITION_BALANCE` procedure to manually trigger a partition balancing task. For more information on manually triggering a partition balancing task, see [Manually trigger a partition balancing task](../600.partition-balance-management/200.manually-trigger-a-partition-balancing-task.md).

Alternatively, you can wait for the scheduled partition balancing task to be triggered. For more information on scheduled partition balancing tasks, see [Configure a scheduled partition balancing task](../600.partition-balance-management/100.configure-a-scheduled-partition-balancing-task.md).