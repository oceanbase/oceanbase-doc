|description|This topic describes how to configure a database connection pool in Oracle mode and provides recommended parameter settings.|
|---|---|
|keywords|Connection pool, JDBC, Oracle mode, database connection|
|dir-name|Database connection pool configuration|
|dir-name-en|Database Connection Pool Configuration|
|tenant-type|Oracle Mode|

# Database connection pool configuration

Configuring a database connection pool is an important step to ensure efficient and stable connections between the system and the database. A well-configured connection pool can effectively manage the number of database connections, prevent connection exhaustion caused by high concurrency, and promptly clean up invalid connections through appropriate timeout settings, ensuring system performance.

This topic introduces the basic concepts of connection pools, comparisons of different connection pools, selection guidelines, recommended parameter settings for connection pools, and important JDBC parameters to help developers optimize database access.

## Basic concepts of connection pools

A database connection pool is a technology used to manage database connections. It establishes a connection pool between the application and the database, pre-creates a certain number of database connections, and stores these connections in memory. When the application needs to access the database, it can directly obtain a connection from the connection pool and return the connection to the connection pool after use, instead of creating a new connection every time.

The main advantages of connection pools include:

* **Improved performance**: Avoid the overhead of frequently creating and destroying connections.
* **Resource management**: Control the number of database connections to prevent connection exhaustion.
* **Connection reuse**: Improve connection utilization and reduce resource waste.
* **Connection monitoring**: Provide connection status monitoring and statistical information.

## Connection pool parameters

### Recommended connection pool settings

* Keep the minimum number of connections at two in the console under normal circumstances. Adjust based on business concurrency and transaction time.

* Set the idle connection timeout to 30 minutes.

  MySQL's default connection timeout is 8 hours, which can lead to dirty connections as the client cannot detect the disconnection. Connection pools can verify connection validity through mechanisms like heartbeats and testOnBorrow. If a connection is not used within this time, it will be disconnected.

## JDBC configuration parameters

The following table lists important JDBC parameters that must be set. These parameters can be configured in the ConnectionProperties of the connection pool or in the JdbcUrl. The table describes each parameter.

| Parameter           | Description                                                |
|----------------|-----------------------------------------------------|
| socketTimeout  | Defines the network socket timeout in milliseconds. A value of 0 indicates no timeout limit. You can also use the system variable `max_statement_time` to limit query time. Default value: 0 (standard configuration).|
| connectTimeout | The connection timeout in milliseconds. A value of 0 indicates no timeout limit. Default value: 30000. |

### JDBC configuration example

This section provides an example of JDBC configuration.

When using JDBC to connect to a database, you need to configure relevant parameters to achieve optimal database performance. Here are some recommended parameter settings.

Example JDBC connection:

```java
conn=jdbc:oceanbase://xxx.xxx.xxx.xxx:3306/test?rewriteBatchedStatements=TRUE&allowMultiQueries=TRUE&useLocalSessionState=TRUE&useUnicode=TRUE&characterEncoding=utf-8&socketTimeout=10000&connectTimeout=30000
```

The configuration parameters involved in this connection are as follows:

* `rewriteBatchedStatements`: Set this parameter to TRUE.

  * By default, the OceanBase Database JDBC driver ignores the executeBatch() statement and sends each SQL statement in a batch individually to the database, resulting in lower performance for batch inserts. To enable actual batch inserts, set this parameter to TRUE. This allows the driver to execute SQL statements in batches. Even if you use the addBatch method to combine multiple insert statements for the same table into a single insert statement with multiple values, the performance of batch inserts will be improved.

  * You must use the prepareStatement method to prepare each insert statement before calling addBatch. Otherwise, the statements cannot be executed in batches.

* `allowMultiQueries`: Set this parameter to TRUE.

  The JDBC driver allows application code to concatenate multiple SQL statements with semicolons (;) and send them as a single SQL statement to the server.

* `useLocalSessionState`: Set this parameter to TRUE to avoid frequent session variable queries to the OB database during transactions.

  The main session variables are autocommit, read_only, and transaction isolation.

* `socketTimeout`: The time in milliseconds that the socket waits for SQL results.

* `connectTimeout`: The time in milliseconds that the connection waits to be established.

* `useCursorFetch`: Set this parameter to `TRUE`.

  For queries involving large amounts of data, the database server creates a cursor and distributes data to the client based on the FetchSize. Setting this parameter to `TRUE` automatically sets `useServerPrepStmts=TRUE`.

* `useServerPrepStmts`: Specifies whether to use the PS protocol to send SQL statements to the database server.

  When set to `TRUE`, SQL statements are executed in two steps:

  * The SQL statement containing `?` is sent to the database server for preparation (`SQL_audit: request_type=5`).

  * The actual values are used to execute the SQL statement in the database (`SQL_audit: request_type=6`).

* `cachePrepStmts`: Specifies whether to enable the PS cache in the JDBC driver to cache prepared statements and avoid repeated preparation on both the client and server sides. `cachePrepStmts=TRUE` helps when `useServerPrepStmts=TRUE` and the same SQL statement is executed in batches repeatedly. Each batch execute includes preparation, and `cachePrepStmts=TRUE` avoids repeated preparation operations.

* `prepStmtCacheSQLLimit`: The maximum length of SQL statements that can be cached in the PS cache. Statements longer than this limit cannot be cached. prepStmtCacheSize: The maximum number of SQL statements that can be cached in the PS cache.

* `maxBatchTotalParamsNum`: The maximum number of parameters (i.e., the number of `?` placeholders) that a single SQL statement can support in batch operations. If the number of parameters exceeds the limit, the batch SQL statement will be split.