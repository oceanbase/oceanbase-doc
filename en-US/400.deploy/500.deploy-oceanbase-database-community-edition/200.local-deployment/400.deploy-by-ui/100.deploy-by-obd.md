# Deploy an OceanBase cluster through the GUI of obd

This topic describes how to deploy an OceanBase cluster in an x86-based CentOS Linux 7.9 system through the GUI of OceanBase Deployer (obd).

## Terms

* OceanBase Database

  An enterprise-level, native distributed database independently developed by the OceanBase team. For more information, see [OceanBase Database documentation](https://en.oceanbase.com/docs/oceanbase-database).

* ODP

  OceanBase Database Proxy (ODP), also known as OceanBase Proxy, is a dedicated proxy server for OceanBase Database. For more information, see [ODP documentation](https://en.oceanbase.com/docs/odp-en).

* OCP Express

  A web-based management tool for OceanBase Database 4.x, seamlessly integrated into the OceanBase Database cluster. It provides key performance monitoring and essential database management functionalities. For more information, see [OCP Express](../../../../700.reference/1400.platform-products/400.1.ocp-express/100.ocp-express.md).

* OBAgent

  A framework for data monitoring and collection in OceanBase Database. It supports both pushing and pulling modes for data collection in different scenarios.

* obconfigserver

  OceanBase Configserver (obconfigserver) provides metadata registration, storage, and query services for OceanBase Database. For more information, see [ob-configserver](https://github.com/oceanbase/oceanbase/tree/master/tools/ob-configserver).

## Prerequisites

Before you begin, make sure that the following conditions are met:

* Your server meets the software and hardware requirements. For more information, see [Software and hardware requirements](../../200.local-deployment/100.requirements-for-software-and-hardware.md).

* In a production environment, you need to perform environment and configuration checks. For specific operations, see [Check resources](../200.environment-and-configuration-checks/100.view-resources-optional.md).

* To deploy OceanBase Database only, make sure that at least 2 vCPUs, 8 GB of memory, and 19 GB of disk space are available. 

* To deploy OceanBase Database and all its components, make sure that at least 4 vCPUs, 10 GB of memory, and 25 GB of disk space are available. We recommend that more than 16 GB of memory be available. 

* To deploy OceanBase Cloud Platform (OCP) Express, make sure that the Java environment is installed and configured. At present, only Java Development Kit (JDK) 1.8 is supported. For more information, see **How do I configure the Java environment before I deploy OCP Express?** in [FAQ](https://en.oceanbase.com/docs/community-obd-en-10000000000860050). 

  <main id="notice" type='notice'>
    <h4>Notice</h4>
    <p>obd remotely performs installation and deployment by using the Secure Shell (SSH) protocol. Therefore, you must use SSH to verify whether the Java environment is available. For more information, see <b>Verify the Java environment</b>. </p>
  </main>

## Prepare the software

You can deploy OceanBase Database through the GUI of obd either online or offline. 

* Online deployment: Make sure that the server where obd resides can connect to the Internet. In this deployment mode, obd obtains the installation package from a remote image repository during deployment. Therefore, you do not need to deploy and configure the installation package in advance. 

* Offline deployment: The server where obd resides does not need to connect to the Internet during deployment. In this deployment mode, you must upload the installation package to the local image repository of obd in advance. If you choose offline deployment, we recommend that you download the all-in-one package of the desired version. 

You can select the appropriate method to prepare the software based on your specific circumstances.

### Online deployment

If you choose online deployment, run the following commands to install obd on the central control server: 

```shell
[admin@test001 ~]$ sudo yum install -y yum-utils
[admin@test001 ~]$ sudo yum-config-manager --add-repo https://mirrors.aliyun.com/oceanbase/OceanBase.repo
[admin@test001 ~]$ sudo yum install -y ob-deploy
[admin@test001 ~]$ source /etc/profile.d/obd.sh
```

### Offline deployment

If you choose offline deployment, run the following commands to download and install the all-in-one package. 

You can download the latest all-in-one package from [OceanBase Download Center](https://en.oceanbase.com/softwarecenter) and copy the package to the central control server.

```shell
[admin@test001 ~]$ tar -xzf oceanbase-all-in-one-*.tar.gz
[admin@test001 ~]$ cd oceanbase-all-in-one/bin/
[admin@test001 bin]$ ./install.sh
[admin@test001 bin]$ source ~/.oceanbase-all-in-one/bin/env.sh
```

## Procedure

 <main id="notice" type='explain'>
  <h4>Note</h4>
  <p>This article provides operational guidance using obd V3.0.0 as an example. The interface may vary with different versions of obd, so please refer to the actual interface. For more information on obd, see <a href="https://en.oceanbase.com/docs/obd-en">obd documentation</a>.</p>
</main>  

### Step 1: Start the GUI

1. Start to the GUI.

   Run the `obd web` command on the CLI to start the GUI, and click the generated URL to go to the GUI.

   ```shell
   [admin@test001 ~]$ obd web
   start obd WEB in 0.0.0.0:8680
   please open http://10.10.10.1:8680
   ```

   <main id="notice" type='explain'>
     <h4>Note</h4>
     <ul>
     <li>
     <p>The default port in the URL is 8680. You can use the <code>obd web -p &lt;PORT></code> command to specify a port. </p>
     </li>
     <li>
     <p>On Alibaba Cloud or other cloud environments, the program may fail to obtain a public IP address but return an intranet IP address. You must use a correct public IP address to access the GUI. </p>
     </li>
     <li>
     <p>The <code>obd web</code> command is bound to <code>0.0.0.0</code>. In the case of multi-NIC deployment, you can access the GUI through any accessible IP address. </p>
     </li>
     </ul>
   </main>

2. (Optional) To change the display language, click the **English** icon in the upper-right corner of the page and select a language as needed. 

   <!-- <img width="852" height="173.4" src="https://obbusiness-private.oss-cn-shanghai.aliyuncs.com/doc/img/obd/V2.2.0/zh-CN/2.quick-start/3.use-ui-deploy-oceanbase-01.png" alt="Chinese and English"> -->
   
3. Click on **<UI-TERM>Try Now</UI-TERM>** in the GUI. On the **<UI-TERM>Welcome to the OceanBase Deployment Wizard</UI-TERM>** page, select **<UI-TERM>OceanBase and supporting tools</UI-TERM>**, and click **<UI-TERM>OK</UI-TERM>** to enter the deployment page.

### Step 2: Configure the deployment information

On the **<UI-TERM>Deployment Configuration</UI-TERM>** page, you can configure the cluster name, deployment type, and components to deploy. 

   <!-- <img width="894.6" height="574.7" src="https://obbusiness-private.oss-cn-shanghai.aliyuncs.com/doc/img/obd/V2.2.0/zh-CN/2.quick-start/3.use-ui-deploy-oceanbase-02.png" alt="Deployment Configuration"> -->

| Parameter | Description |
|---------|--------|
| <UI-TERM>Deployment Configuration</UI-TERM> | The cluster name is a unique identifier for obd, with a default value of `myoceanbase`. You can customize the name, but it must not conflict with any existing deployments. The cluster name must begin with a letter, end with either a letter or a number, and can only include letters, numbers, and underscores. It must also be between 2 and 32 characters in length. To view the clusters managed by obd, you can use the `obd cluster list` command. |
|  <UI-TERM>**部署数据库**</UI-TERM> | You can select the OceanBase Database version from the drop-down list next to **<UI-TERM key="ocp-v2.Host.Detail.OCPAgent.Version">Version</UI-TERM>**. You can also click **<UI-TERM key="ocp-v2.Settings.Credential.LearnMore">Learn More</UI-TERM>** next to a component to learn more about the component. |
|  <UI-TERM>**负载类型**</UI-TERM> | You can select the load type of the OceanBase cluster from the drop-down list next to **<UI-TERM key="odc.NewSSODrawerButton.SSOForm.Type">Type</UI-TERM>**. You can switch load types to view descriptions of the types and learn about the scenarios where each type applies. |
| <UI-TERM>**Components**</UI-TERM> | By default, only the OBProxy component is selected. You can check or uncheck the components to enable or disable them. The version of each component is fixed to the latest version. Click **<UI-TERM key="ocp-v2.Settings.Credential.LearnMore">Learn More</UI-TERM>** next to a component to learn more about the component. <main id="notice" type='explain'><h4>Note</h4><p>When you check the OCP Express component, the OBAgent component is automatically checked. When you uncheck the OBAgent component, the OCP Express component is automatically unchecked.</p></main>  |

Then, click <UI-TERM>**Next Step**</UI-TERM> to go to the <UI-TERM>**Node Configuration**</UI-TERM> page. 

### Step 3: Configure the nodes

On the **Node Configuration** page, you can configure the database and component nodes, and specify the user information and software installation path. 

   <!-- <img width="858.9" height="667.1" src="https://obbusiness-private.oss-cn-shanghai.aliyuncs.com/doc/img/obd/V2.1.0/zh-CN/2.quick-start/3.use-ui-deploy-oceanbase-03.png" alt="Node Configuration"> -->

<table>
  <thead>
    <tr>
      <th>Module</th>
      <th>Parameter</th>
      <th>Description</th>
    </tr>
  </thead>
  <tr>
    <td rowspan="3">Database Node Configuration</td>
    <td>Zone Name</td>
    <td>Configure the zone name in OceanBase Database. The zone name can be customized, but it must begin with a letter, end with either a letter or a number, and only include letters, numbers, and underscores. Additionally, it must be between 2 and 32 characters long. For an OceanBase cluster with multiple zones, you must specify the zone that shares the same region as the primary zone. You can use the <b>+ Add Zone</b> button to add a zone or click the delete icon to remove one. </td>
  </tr>
  <tr>
    <td>OBServer Node</td>
    <td>Configure the IP address of an OBServer node in the OceanBase cluster. You can enter multiple IP addresses by clicking the Enter key after you input an IP address. <main id="notice" type='notice'><h4>Notice</h4><p>The IP address <code>127.0.0.1</code> cannot be used. </p></main></td>
  </tr>
  <tr>
    <td>RootServer Node</td>
    <td>Choose an IP address from the drop-down list of <b>OBServer Node</b> as the IP address of the RootServer node in the OceanBase cluster. For a multi-replica OceanBase cluster, you must specify a RootServer node for each zone. </td>
  </tr>
  <tr>
    <td rowspan="3">Component Node Configuration</td>
    <td>OCP Express Node</td>
    <td>You can select the IP address of an OBServer node from the drop-down list or enter a new IP address. You can select or enter only one IP address. <main id="notice" type='notice'><h4>Notice</h4><p>The IP address <code>127.0.0.1</code> cannot be used. </p></main></td>
  </tr>
  <tr>
    <td>OBProxy Node</td>
    <td>You can select multiple nodes by choosing OBServer node IP addresses from the drop-down list or entering new ones manually. To input multiple IP addresses, type an address and press the Enter key after each one. <main id="notice" type='notice'><h4>Notice</h4><p>The IP address <code>127.0.0.1</code> cannot be used.</p></main></td>
  </tr>
  <tr>
    <td>obconfigserver Node</td>
    <td>You can select multiple nodes by either choosing OBServer node IP addresses from the drop-down list or manually entering new ones. To input multiple IP addresses, type an address and press the Enter key after each one. If you select multiple nodes for obconfigserver, you must first configure load balancing. Then, enable the <b>More Configuration</b> option in the <b>Component Configuration</b> section on the <b>Cluster Configuration</b> page to set the <code>vip_address</code> and <code>vip_port</code>. <main id="notice" type='notice'><h4>Notice</h4><p>The IP address <code>127.0.0.1</code> cannot be used. </p></main></td>
  </tr>
  <tr>
    <td rowspan="3">Deployment User Configuration</td>
    <td>Username</td>
    <td>The username for deploying the OceanBase cluster. The default value is the user who starts the current process. You can customize the username. </td>
  </tr>
  <tr>
    <td>Password</td>
    <td>The login password of the user specified in <b>Username</b>. If password-free access is configured between nodes, you do not need to enter the password. </td>
  </tr>
  <tr>
    <td>SSH Port</td>
    <td>The default value is 22. You can customize the SSH port. </td>
  </tr>
  <tr>
    <td >Software Path Configuration</td>
    <td>Software Path</td>
    <td>The default location is the home directory of the deployment user, but you can customize the software path if needed. When you specify a custom path, a <code>myoceanbase</code> folder will be created in the chosen directory. </td>
  </tr>
</table>

Then, click **Next Step** to go to the **Cluster Configuration** page. 

### Step 4: Configure the cluster

On the **Cluster Configuration** page, configure related cluster information, including the password of the administrator user in the sys tenant (root@sys), data directory, log directory, and ports and parameters of OceanBase Database and its components. 

1. Configure the cluster.

   <!-- <img width="772.1" height="667.1" src="https://obbusiness-private.oss-cn-shanghai.aliyuncs.com/doc/img/obd/V2.1.0/zh-CN/2.quick-start/3.use-ui-deploy-oceanbase-04.png" alt="Cluster Configuration"> -->

   | Parameter  | Description |
   |-------|-----|
   | Mode | You can select **Maximize Utilization** or **Minimize Availability**. The **Maximum Utilization** mode maximizes resource utilization to ensure cluster performance and stability, and is recommended. In the **Minimum Availability** mode, minimum resources required for the normal running of the cluster are configured. For more information about the two modes, see [Mode configuration rules](https://en.oceanbase.com/docs/community-obd-en-10000000000904952). |
   | root@sys Password  | You can click **Random Generate** to let obd automatically create a random password, or you can set one manually. If setting a password manually, ensure it meets the following requirements: <ul><li>The password must be 8 to 32 characters long.</li><li>The password must include at least three of the following four character types: digits (0-9), uppercase letters (A-Z), lowercase letters (a-z), and special characters (<code>~!@#%^&*_-+=\|(){}[]:;,.?/</code>).</li></ul>Once the password is set, click **Copy Password** to save it for future use. |
   | Data Directory | By default, the data directory is configured to be the **<UI-TERM key="ocp-v2.src.component.AddArbitrationDrawer.SoftwarePath">Software Path</UI-TERM>** parameter on the **Node Configuration** page. You need to specify an absolute path that starts with `/`. You can customize the data directory, but make sure that the directory is empty.  |
   | Log Directory | By default, the log directory is configured to be the **<UI-TERM key="ocp-v2.src.component.AddArbitrationDrawer.SoftwarePath">Software Path</UI-TERM>** parameter on the **Node Configuration** page. You need to specify an absolute path that starts with `/`. You can customize the log directory, but make sure that the directory is empty.  |
   | SQL Port | The port number of the SQL service protocol. The default value is `2881`. Custom value is supported (only in the range of 1025 to 65535). Make sure that the specified port is not occupied. |
   | RPC Port | The port number of the remote access protocol. The default value is `2882`. Custom value is supported (only in the range of 1025 to 65535). Make sure that the specified port is not occupied. |
   | More Configurations | Click the **<UI-TERM key="ocp-v2.src.component.AddUnitSpecModal.MoreConfigurations">More Settings</UI-TERM>** button to view the corresponding cluster parameters. You can use the automatically assigned values to configure the parameters or customize each parameter. <main id="notice" type='explain'><h4>Note</h4><p>You can set the <code>scenario</code> parameter to the <b>More Configurations</b> section to specify the load type of the cluster. The default value of <code>scenario</code> is <code>htap</code>. </p></main> |

2. Configure the components.

   <!-- ![Component Configuration](https://obbusiness-private.oss-cn-shanghai.aliyuncs.com/doc/img/obd/V2.10.0/zh-CN/200.quick-start/300.use-ui-deploy-oceanbase-05.png) -->

   | Parameter   | Description   |
   |--------|--------|
   | OCP Express Administrator Password | The password of the OCP Express administrator account. You can click **Random Generate** to let obd automatically create a random password, or you can set one manually. If setting a password manually, ensure it meets the following requirements: <ul><li>The password must be 8 to 32 characters long.</li><li>The password contains at least two of the following four types of characters: digits (0\~9), uppercase letters (A\~Z), lowercase letters (a\~z), and special characters (<code>~!@#%^&*_-+=`\|(){}[]:;',.?/</code>)</li></ul> Once the password is set, click **Copy Password** to save it for future use.|
   | OBProxy SQL Port | The listening port of ODP. The default value is `2883`. Custom value is supported (only in the range of 1025 to 65535). Make sure that the specified port is not occupied. |
   | OBProxy Exporter Port | The Exporter port of OBProxy, which is used by Prometheus to pull the monitoring data of OBProxy. The default value is `2884`. Custom value is supported (only in the range of 1025 to 65535). Make sure that the specified port is not occupied. |
   | OBProxy RPC Port  | The RPC port of OBProxy. The default value is `2885`. Custom value is supported (only in the range of 1025 to 65535). Make sure that the specified port is not occupied. |
   | OBAgent Monitor Service Port | The port number of the OBAgent Monitor Service. The default value is `8088`. Custom value is supported (only in the range of 1025 to 65535). Make sure that the specified port is not occupied. |
   | OBAgent Management Service Port | The port number of the OBAgent Management Service. The default value is `8089`. Custom value is supported (only in the range of 1025 to 65535). Make sure that the specified port is not occupied. |
   | OCP Express Port | The HTTP port for accessing OCP Express. The default value is `8180`. Custom value is supported (only in the range of 1025 to 65535). Make sure that the specified port is not occupied. |
   | obconfigserver Port | The listening port of obconfigserver. The default value is `8080`. Custom value is supported (only in the range of 1025 to 65535). Make sure that the specified port is not occupied. |
   | More Configurations | Click the **<UI-TERM key="ocp-v2.src.component.AddUnitSpecModal.MoreConfigurations">More Settings</UI-TERM>** button to view the corresponding component parameters. You can use the automatically assigned values to configure the parameters or customize each parameter. <main id="notice" type='explain'><h4>Note</h4><p>If multiple obconfigserver nodes are configured on the <b>Node Configuration</b> page under <b>obconfigserver</b>, you need to click the <b>More Configurations</b> button and configure the <code>vip_address</code> and <code>vip_port</code> parameters. Otherwise, an error will occur during precheck. </p></main> |

After you configure all parameters, click **<UI-TERM key="oms-cloud.Performance.componentsAdd.FileParsingAddOSS.NextStep">Next</UI-TERM>** to go to the **<UI-TERM key="odc.component.CommonTaskDetailModal.TaskFlow.PreCheck">Pre-check</UI-TERM>** page.

### Step 5: Precheck

On the **<UI-TERM key="odc.component.CommonTaskDetailModal.TaskFlow.PreCheck">Pre-check</UI-TERM>** page, view all configuration information. If any issues are identified, click **<UI-TERM key="oms-cloud.Migration.New.PreviousStep">Previous</UI-TERM>** to modify the parameters; otherwise, click **<UI-TERM key="odc.component.CommonTaskDetailModal.TaskFlow.PreCheck">Pre-check</UI-TERM>** to perform the precheck.

If any errors occur during the precheck, you can click **<UI-TERM key="ocp-v2.page.Host.AutomaticRepair.1">Auto Repair</UI-TERM>** (for check items that can be automatically repaired) or click **More Solutions** to learn how to manually repair the errors. After you modify all errors, click **<UI-TERM key="ocp-v2.Detail.TakeOver.ReCheck">Recheck</UI-TERM>** to perform the precheck again.

<!-- <img width="751.2" height="407.4" src="https://obbusiness-private.oss-cn-shanghai.aliyuncs.com/doc/img/obd/V2.9.0/zh-CN/200.quick-start/300.use-ui-deploy-oceanbase-05.png" alt="Precheck"> -->

After the precheck is passed, click **Deploy** to deploy OceanBase Database.

### Step 6: Deploy

<!-- <img width="770.4" height="560.4" src="https://obbusiness-private.oss-cn-shanghai.aliyuncs.com/doc/img/obd/V2.9.0/zh-CN/200.quick-start/300.use-ui-deploy-oceanbase-06.png" alt="Deploy"> -->

<!-- where: -->

* After the deployment is successful, you can copy the connection string displayed on the page to connect to the corresponding component. In a CLI interface, the connection string for connecting to OceanBase Database components will connect to the OceanBase cluster as the root@sys user, and the connection string for connecting to OBProxy will connect to the ODP as the root@proxysys user.

* Click the connection string of the OCP Express component to go to the login page of OCP Express. You can use the account and password displayed on the deployment page to log in and change the password. Then you can manage the cluster using the GUI.

  <main id="notice" type='explain'>
    <h4>Note</h4>
    <p>On Alibaba Cloud or other cloud environments, the program may fail to obtain a public IP address but output an intranet IP address. You must use a correct public IP address to access the GUI. </p>
  </main>

* Click the expand icon next to a component on the **Deployment Report** page to view the deployment log of the component. After you click **<UI-TERM key="oms-cloud.CompatibilityReport.ObjectEvaluationOverview.tableDetail.ViewDetails">View Details</UI-TERM>** next to the component in the list, click the copy icon after the command to copy the command. Then you can paste the command in the console of the central control server to view the log file of the component.

* Click **Copy All** to copy all account and password information of the OceanBase cluster. After you click **<UI-TERM key="odc.components.PLPage.statusBar.Complete">Complete</UI-TERM>** , you will exit the deployment program.

<main id="notice" type='notice'>
  <h4>Notice</h4>
  <p> If you want to deploy multiple clusters, you must click <b>Complete</b> to end the current obd process before you deploy the next cluster. </p>
</main>

## Related operations

### Verify the Java environment

obd remotely executes a script to deploy OCP Express. Therefore, you must use SSH to verify the Java environment. You cannot directly run the `java -version` command on the server where OCP Express is deployed to verify the Java environment. 

<main id="notice" type='explain'>
  <h4>Note</h4>
  <p>Interactions between the client and server will initialize the environment variables, but the SSH access mode does not initialize environment variables. As a result, the system prompts that the Java command does not exist or the Java version is wrong when you use SSH. </p>
</main>

You can run the following command on any server that has established a network connection with the node where OCP Express resides. 

```shell
# ocp_express_node_username: the username of the node where OCP Express resides
# ocp_express_node_ip: the IP address of the node where OCP Express resides
[admin@test001 ~]$ ssh <ocp_express_node_username>@<ocp_express_node_ip> 'java -version'

# Command output
openjdk version "1.8.0_xxx" 
OpenJDK Runtime Environment (build 1.8.0_362-b08)
OpenJDK 64-Bit Server VM (build 25.362-b08, mixed mode)
```

If the required Java environment has been installed but failed the verification, you can resolve the issue in the following ways:

* Method 1: Specify the **java_bin** parameter in the **More Configurations** section.

   <!-- As shown in the following figure, set `java_bin` to the real path of Java, such as `/jdk8/bin/java`.  -->
   Set `java_bin` to the real path of Java, such as `/jdk8/bin/java`. 

<!-- <img width="860.3" height="641.2" src="https://obbusiness-private.oss-cn-shanghai.aliyuncs.com/doc/img/obd/V2.1.0/zh-CN/2.quick-start/3.use-ui-deploy-oceanbase-07.png" alt="More Configurations"> -->

* Method 2: Create a soft link from the executable file of Java to `/usr/bin/java`.

   ```shell
   [admin@test001 bin]$ pwd
   /jdk8/bin
   [admin@test001 bin]$ ln -s /jdk8/bin/java  /usr/bin/java
   ```

### Manage the cluster

You can run the following commands to manage a cluster deployed by using obd. For more information, see [Cluster commands](https://en.oceanbase.com/docs/community-obd-en-10000000000904961). 

```shell
# View the cluster list.
[admin@test001 ~]$ obd cluster list

# View the status of the myoceanbase cluster.
[admin@test001 ~]$ obd cluster display myoceanbase

# Stop the myoceanbase cluster in the running state.
[admin@test001 ~]$ obd cluster stop myoceanbase

# Destroy the myoceanbase cluster.
[admin@test001 ~]$ obd cluster destroy myoceanbase
```

### Deploy a component of the desired version

The all-in-one package is iterated based on OceanBase Database versions. If any component in the package has a later version, you can download the component of the latest version from [OceanBase Download Center](https://en.oceanbase.com/softwarecenter) and then upload it to the local image repository of obd. obd will automatically obtain the latest version from the local image repository during deployment. 

1. Go to the directory where the component installation package is located, run the following command to add it to the local image repository:

   ```shell
   [admin@test001 rpm]$ obd mirror clone *.rpm
   ```

2. View the list of installation packages in the local image repository:

   ```shell
   [admin@test001 rpm]$ obd mirror list local
   ```
