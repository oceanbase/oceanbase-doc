|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type|Oracle Mode|
|slug|materialized-views-resource-isolation-oracle-mode|

# Materialized view resource isolation

OceanBase Database supports using `DBMS_RESOURCE_MANAGER` to provide resource isolation for incremental refresh and MLOG purge of materialized views. You can set the maximum amount of resources that can be used by materialized views.

## Example

1. Create a resource plan named `mview_task`.

    ```sql
    CALL DBMS_RESOURCE_MANAGER.CREATE_PLAN('mview_task','Resource plan for materialized view processing');
    ```

    For more information about creating a resource plan, see [CREATE_PLAN](../../../../600.pl-reference/300.pl-oracle/1400.pl-system-package-oracle/13300.dbms-resource-manager-oracle/300.create-plan-oracle.md).

2. Create a consumer group named `mview_group`.

    ```sql
    CALL DBMS_RESOURCE_MANAGER.CREATE_CONSUMER_GROUP('mview_group','Materialized view processing group');
    ```

    For more information about creating a consumer group, see [CREATE_CONSUMER_GROUP](../../../../600.pl-reference/300.pl-oracle/1400.pl-system-package-oracle/13300.dbms-resource-manager-oracle/200.create-consumer-group-oracle.md).

3. Create a resource plan directive to limit CPU usage to 5%.

    ```sql
    CALL DBMS_RESOURCE_MANAGER.CREATE_PLAN_DIRECTIVE(
        PLAN => 'mview_task',
        GROUP_OR_SUBPLAN => 'mview_group',
        UTILIZATION_LIMIT => 5
        );
    ```

    For more information about creating a resource plan directive, see [CREATE_PLAN_DIRECTIVE](../../../../600.pl-reference/300.pl-oracle/1400.pl-system-package-oracle/13300.dbms-resource-manager-oracle/400.create-plan-directive-oracle.md).

4. Map the resource consumer group `mview_group` to `FUNCTION`.

    ```sql
    CALL DBMS_RESOURCE_MANAGER.SET_CONSUMER_GROUP_MAPPING(
        ATTRIBUTE => 'FUNCTION',
        VALUE => 'MVIEW',
        CONSUMER_GROUP => 'mview_group'
        );
    ```

    For more information about mapping sessions to resource groups, see [SET_CONSUMER_GROUP_MAPPING](../../../../600.pl-reference/300.pl-oracle/1400.pl-system-package-oracle/13300.dbms-resource-manager-oracle/800.set-consumer-group-mappingn-directive-oracle.md).

5. Activate the resource plan `mview_task`.

    ```sql
    SET GLOBAL resource_manager_plan = 'mview_task';
    ```

    For more information about activating a resource plan, see [resource_manager_plan](../../../../800.configuration-items-and-system-variables/200.system-variable/300.global-system-variable/11500.resource_manager_plan-global.md).

6. Update the CPU usage limit to 30%.

    ```sql
    CALL DBMS_RESOURCE_MANAGER.UPDATE_PLAN_DIRECTIVE(
        PLAN => 'mview_task',
        GROUP_OR_SUBPLAN => 'mview_group',
        NEW_UTILIZATION_LIMIT => 30
        );
    ```

    For more information about updating a resource plan directive, see [UPDATE_PLAN_DIRECTIVE](../../../../600.pl-reference/300.pl-oracle/1400.pl-system-package-oracle/13300.dbms-resource-manager-oracle/900.update-plan-directive-oracle.md).

## References

* [Materialized view log](200.materialized-views-log-of-oracle-mode.md)
* [Refresh materialized views](400.refresh-materialized-views-of-oracle-mode.md)
* [DBMS_RESOURCE_MANAGER overview](../../../../600.pl-reference/300.pl-oracle/1400.pl-system-package-oracle/13300.dbms-resource-manager-oracle/100.dbms-resource-manager-overview-oracle.md)
