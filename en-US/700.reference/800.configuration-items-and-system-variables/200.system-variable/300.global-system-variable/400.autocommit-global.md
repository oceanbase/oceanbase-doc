|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||

# autocommit

<main id="notice" type='explain'>
  <h4>Note</h4>
  <p>This variable is available starting with V1.4.</p>
</main>

## Description

`autocommit` is used to specify whether to automatically commit transactions.

## Privilege requirements

* Query variables

  * Global level

    The `sys` tenant and all user tenants can execute the `SHOW VARIABLES` statement or view `SYS.TENANT_VIRTUAL_GLOBAL_VARIABLE` (Oracle mode) and `information_schema.GLOBAL_VARIABLES` (MySQL mode) to query the values of global system variables.

  * Session level

    The `sys` tenant and all user tenants can execute the `SHOW VARIABLES` statement or view `SYS.TENANT_VIRTUAL_SESSION_VARIABLE` (Oracle mode) and `information_schema.SESSION_VARIABLES` (MySQL mode) to query the values of session system variables.

* Modify variables

  * Set the variable at the global level

    * The `sys` tenant can directly modify the values of global system variables.

    * MySQL user tenants must have the `SUPER` or `ALTER SYSTEM` privilege to modify the values of global system variables.

    * For V4.x, Oracle user tenants must have the `ALTER SYSTEM` privilege to modify the values of global system variables, starting from V4.2.0.

  * Set the variable at the session level

    The `sys` tenant and all user tenants can directly modify the values of session system variables in their own tenants.

## Attributes

| **Attribute**  |  **Description**  |
|-----------|---------------|
| Type    | bool                    |
| Default value     | 1                       |
| Value range    | <li> 1: automatically commit transactions   <li> 0: do not automatically commit transactions    |
| Scope    | <li> GLOBAL   <li> SESSION    |
| Modifiable | Yes. You can execute the `SET` statement to modify the value. |

## Examples

Set autocommit for transactions.

* Global level

  ```shell
  obclient> SET GLOBAL autocommit=1;
  ```

* Session level

  ```shell
  obclient> SET autocommit=1;
  ```

## References

* [Transaction management](../../../../300.develop/100.application-development-of-mysql-mode/600.transaction-in-develop-of-mysql-mode/100.transaction-management-overview-of-mysql-mode.md)
* [Start a transaction](../../../../300.develop/100.application-development-of-mysql-mode/600.transaction-in-develop-of-mysql-mode/200.start-a-transaction-of-mysql-mode.md)
* [Commit a transaction](../../../../300.develop/100.application-development-of-mysql-mode/600.transaction-in-develop-of-mysql-mode/400.submit-transaction-of-mysql-mode.md)