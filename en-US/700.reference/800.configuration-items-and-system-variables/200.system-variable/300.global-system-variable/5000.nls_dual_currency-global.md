|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||
|slug|nls_dual_currency-global|

# nls_dual_currency

<main id="notice" type='explain'>
  <h4>Note</h4>
  <p> This variable was introduced in V2.2.77. </p>
</main>

## Description

`nls_dual_currency` specifies the local currency symbol for the `U` number format element.

<main id="notice">
  <h4>Applicability</h4>
  <p>This variable applies only to the Oracle mode of OceanBase Database. </p>
</main>

## Privilege requirements

* Query variables

  * Global scope

    Tenants in the `sys` tenant and all user tenants can execute the `SHOW VARIABLES` statement or query the `SYS.TENANT_VIRTUAL_GLOBAL_VARIABLE` view (in Oracle mode) to view the values of global system variables.

  * Session scope

    Tenants in the `sys` tenant and all user tenants can execute the `SHOW VARIABLES` statement or query the `SYS.TENANT_VIRTUAL_SESSION_VARIABLE` view (in Oracle mode) to view the values of session system variables.

* Modify variables

    Tenants can neither modify the values of global nor session system variables in the `sys` tenant or user tenants.

## Attribute description

| **Attribute** | **Description** |
|---------|---------|
| Type | Varchar |
| Default value | $ |
| Applicable scope | <ul><li>GLOBAL</li><li>SESSION</li></ul> |
| Modifiable | No, the variable cannot be modified. |
