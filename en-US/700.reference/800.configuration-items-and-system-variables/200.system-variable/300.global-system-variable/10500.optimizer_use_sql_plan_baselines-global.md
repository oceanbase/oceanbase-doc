|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||
|machine-translation|en,ja-marker,ja-autoPR|
|slug|optimizer_use_sql_plan_baselines-global|

# optimizer_use_sql_plan_baselines

<main id="notice" type='explain'>
  <h4>Note</h4>
  <p>This variable is available starting with V2.1. </p>
</main>

## Description

`optimizer_use_sql_plan_baselines` specifies whether the optimizer uses plan baselines.

## Privilege requirements

* Query variables

  * Global level

    `sys` tenants and all user tenants can execute the `SHOW VARIABLES` statement or query the `SYS.TENANT_VIRTUAL_GLOBAL_VARIABLE` view (Oracle mode) or the `information_schema.GLOBAL_VARIABLES` view (MySQL mode) to view the value of a global system variable.

  * Session level

    `sys` tenants and all user tenants can execute the `SHOW VARIABLES` statement or query the `SYS.TENANT_VIRTUAL_SESSION_VARIABLE` view (Oracle mode) or the `information_schema.SESSION_VARIABLES` view (MySQL mode) to view the value of a session system variable.

* Modify variables

  * Global level

    * `sys` tenants can modify the value of a global system variable.

    * MySQL user tenants must have the `SUPER` or `ALTER SYSTEM` privilege to modify the value of a global system variable.

    * Oracle user tenants must have the `ALTER SYSTEM` privilege to modify the value of a global system variable.

  * Session level

    `sys` tenants and all user tenants can modify the value of a session system variable.

## Attributes

| **Attribute**  |               **Description**                |
|---------|-----------------------------------------------------------------------------|
| Type    | Bool                                |
| Default value     | 0                                   |
| Value range    | <li> 0: When a new plan is generated, the optimizer does not consider plans in the plan baseline. It generates and executes a new plan.   <li> 1: When a new plan is generated, the optimizer prioritizes using a plan from the plan baseline. If a new plan is different from the baseline plan, it is validated before being used.    |
| Scope    | <li> Global   <li> Session                |
| Modifiable  | Yes. You can execute the `SET` statement to modify the value.|

## Examples

Set the optimizer to not use plan baselines.

* Global level

  ```shell
  obclient> SET GLOBAL optimizer_use_sql_plan_baselines = 0;
  ```

* Session level

  ```shell
  obclient> SET optimizer_use_sql_plan_baselines = 0;
  ```
