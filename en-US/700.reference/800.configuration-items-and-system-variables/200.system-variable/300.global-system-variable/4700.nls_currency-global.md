| description ||
|---|---|
| keywords ||
| dir-name ||
| dir-name-en ||
| tenant-type ||
|slug|nls_currency-global|

# nls_currency

<main id="notice" type='explain'>
  <h4>Note</h4>
  <p>This variable was introduced in V2.2.77. </p>
</main>

## Description

`nls_currency` specifies the local currency symbol for the `L` number format element.

<main id="notice">
  <h4>Applicability</h4>
  <p>This variable applies only to the Oracle mode of OceanBase Database. </p>
</main>

## Privilege requirements

* Query variables

  * Global scope

    Tenants in the `sys` tenant and all user tenants can execute the `SHOW VARIABLES` statement or query the `SYS.TENANT_VIRTUAL_GLOBAL_VARIABLE` view (in Oracle mode) to view the values of global system variables.

  * Session scope

    Tenants in the `sys` tenant and all user tenants can execute the `SHOW VARIABLES` statement or query the `SYS.TENANT_VIRTUAL_SESSION_VARIABLE` view (in Oracle mode) to view the values of session system variables.

* Modify variables

  Tenants in the `sys` tenant and all user tenants cannot modify the values of global or session system variables in their own tenants.

## Attribute description

| **Attribute**  | **Description**  |
|---------|---------|
| Parameter type    | Varchar |
| Default value | $ |
| Effective scope    | <ul><li>GLOBAL</li><li>SESSION</li></ul> |
| Modifiable | No, this variable cannot be modified. |
