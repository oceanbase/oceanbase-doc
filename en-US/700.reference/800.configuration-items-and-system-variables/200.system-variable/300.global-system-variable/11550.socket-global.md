|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||
|machine-translation| en |

# socket

<main id="notice" type='explain'>
  <h4>Note</h4>
  <ul><li>For V4.3.x, this variable is available starting with V4.3.5 BP1. </li><li>For other V4.2.x versions, this variable is available starting with V4.2.5 BP1. For V4.2.1, this variable is available starting with V4.2.1 BP10. </li></ul>
</main>

## Description

`socket` specifies the name of the `Socket` file for the local client to connect to.

<main id="notice">
  <h4>Applicability</h4>
  <p>This variable is available only in OceanBase Database in MySQL-compatible mode. </p>
</main>

## Privilege requirements

* Query variables

  The `sys` tenant and all user tenants can execute the `SHOW VARIABLES` statement or query the `information_schema.GLOBAL_VARIABLES` view (MySQL-compatible mode) to view the values of Global system variables.

* Modify variables

  You cannot modify this variable.

## Attributes

| **Attribute**  |    **Description**  |
|-----------|---------------|
| Type    | Varchar              |
| Default value      | /run/sql.sock   |
| Scope    |  Global  |
| Modifiable  | No. This variable is read-only.       |
| Read-only    | Yes       |

## Examples

Query the name of the `socket` file for the local client to connect to.

```shell
obclient> SHOW VARIABLES LIKE 'socket';
```

The query result is as follows:

```shell
+---------------+------------------------------------+
| Variable_name | Value                              |
+---------------+------------------------------------+
| socket        | /home/admin/oceanbase/run/sql.sock |
+---------------+------------------------------------+
1 row in set (0.003 sec)
```

## References

[secure_file_priv](../300.global-system-variable/12000.secure_file_priv-global.md)