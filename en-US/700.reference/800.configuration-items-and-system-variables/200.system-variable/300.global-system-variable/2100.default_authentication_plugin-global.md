|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||
|machine-translation|en|
|slug|default_authentication_plugin-global|

# default_authentication_plugin

<main id="notice" type="explain">
  <h4>Note</h4>
  <p>This variable is available starting with V4.4.2 BP1.</p>
</main>

## Description

`default_authentication_plugin` specifies the default authentication plugin used for new users.

## Privilege requirements

* Query the variable

  The `sys` tenant and all user tenants can use the `SHOW VARIABLES` statement, the `SYS.TENANT_VIRTUAL_GLOBAL_VARIABLE` view in Oracle-compatible mode, or the `information_schema.GLOBAL_VARIABLES` view in MySQL-compatible mode to query the value of this Global system variable.

* Modify the variable

  * The `sys` tenant can directly modify the value of this Global system variable.

  * A MySQL user tenant must have the `SUPER` or `ALTER SYSTEM` privilege to modify the value of this Global system variable.

  * An Oracle user tenant must have the `ALTER SYSTEM` privilege to modify the value of this Global system variable.

## Attributes

| **Attribute** | **Description** |
|---------------|-----------------|
| Type | Varchar(64) |
| Default value | An empty string (`''`). Setting the variable to `DEFAULT` restores it to the empty string. |
| Value range | <ul><li>An empty string, which is equivalent to <code>mysql_native_password</code></li><li><code>ob_native_password</code>, which applies to Oracle-compatible mode and is equivalent to <code>mysql_native_password</code></li><li><code>mysql_native_password</code>, which applies to MySQL-compatible mode</li><li><code>caching_sha2_password</code></li></ul> |
| Scope | Global |
| Modifiable | Yes. You can use the `SET` statement to modify the value. |

<main id="notice" type="notice">
  <h4>Notice</h4>
  <p><code>caching_sha2_password</code> is supported starting with V4.4.2 BP2.</p>
</main>

## Examples

Set the default authentication plugin for new users to `caching_sha2_password`:

```shell
obclient> SET default_authentication_plugin = 'caching_sha2_password';
```
