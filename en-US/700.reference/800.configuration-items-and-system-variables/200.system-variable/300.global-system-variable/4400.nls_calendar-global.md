| description ||
|---|---|
| keywords ||
| dir-name ||
| dir-name-en ||
| tenant-type ||
|slug|nls_calendar-global|

# nls_calendar

<main id="notice" type='explain'>
  <h4>Notice</h4>
  <p>The nls_calendar variable is introduced in V2.2.77. </p>
</main>

## Description

`nls_calendar` specifies the calendar system used by the database.

<main id="notice">
  <h4>Applicability</h4>
  <p>The nls_calendar variable applies only to the Oracle mode of OceanBase Database. </p>
</main>

## Permissions

* Query variables

  * Global level

    Tenants of the `sys` tenant and all user tenants can execute the `SHOW VARIABLES` statement or view the `SYS.TENANT_VIRTUAL_GLOBAL_VARIABLE` view (in Oracle mode) to query the values of global system variables.

  * Session level

    Tenants of the `sys` tenant and all user tenants can execute the `SHOW VARIABLES` statement or view the `SYS.TENANT_VIRTUAL_SESSION_VARIABLE` view (in Oracle mode) to query the values of session system variables.

* Modify variables

  Tenants of the `sys` tenant and all user tenants cannot modify the values of global or session system variables of their own.

## Attribute

| **Attribute**  | **Description**  |
|---------|---------|
| Parameter type    | Varchar |
| Default value      | GREGORIAN |
| Effective scope    | <ul><li>GLOBAL</li><li>SESSION</li></ul> |
| Modifiable | No, the variable cannot be modified. |
