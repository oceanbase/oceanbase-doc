|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||

# ob_compatibility_mode

## Description

The `ob_compatibility_mode` variable specifies the compatibility mode of a tenant.

## Attributes

| **Attribute**  |                                                             **Description**                                                              |
|---------|---------------------------------------------------------------------------------------------------------------------------------|
| Type    | enum                                         |
| Default value     | MYSQL                                        |
| Valid values    | <li> MYSQL: indicates compatibility with MySQL   <li> ORACLE: indicates compatibility with Oracle    |
| Valid scope    | <li> Global   <li> Session                         |
| Modifiable    | This variable can be specified when a tenant is created. You cannot modify this variable by using an SQL statement after a tenant is created. |

## Examples

The following example shows how to create a tenant and set the `ob_compatibility_mode` variable to `MYSQL`:

```shell
CREATE TENANT IF NOT EXISTS t1 zone_list = ('zone1'), primary_zone = 'zone1', resource_pool_list = ('pool1') SET ob_compatibility_mode = 'MYSQL',lower_case_table_names = 0;
```