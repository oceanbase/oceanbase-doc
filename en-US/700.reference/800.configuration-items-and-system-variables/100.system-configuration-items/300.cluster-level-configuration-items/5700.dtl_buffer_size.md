|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||
|machine-translation| en |
|slug|dtl_buffer_size|

# dtl_buffer_size

<main id="notice" type='explain'>
  <h4>Note</h4>
  <p>For V2.2.50, this parameter is available starting with V2.2.50. </p>
</main>

## Description

`dtl_buffer_size` specifies the size of the buffer used by the SQL data transmission module.

## Privilege requirements

* Query the parameter

  The `sys` tenant and all user tenants can use the `SHOW PARAMETERS` statement or the `GV$OB_PARAMETERS` view to query this parameter.

* Modify the parameter

  Only the `sys` tenant can modify this parameter. User tenants cannot modify this parameter.

## Attributes

|      **Attribute**    |   **Description**  |
|------------------|-------------|
| Type             | Capacity        |
| Default value               | 64K. The default unit is MB. You must specify the unit when you configure this parameter.         |
| Value range             | [4K, 2 M] |
| Modifiable           | Yes. You can use the `ALTER SYSTEM SET` statement to modify the parameter.|
| Effective upon OBServer node restart | No. The setting takes effect immediately.        |

## Examples

Set the buffer size used by the SQL data transmission module to 64 KB.

```shell
obclient> ALTER SYSTEM SET dtl_buffer_size= '64K';
```

## References

[Parameter tuning for parallel queries](../../../../700.reference/1000.performance-tuning-guide/500.sql-optimization/300.distributed-execution-plan/900.optimize-the-parameters-for-parallel-query.md)