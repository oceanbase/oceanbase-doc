|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||
|machine-translation| en |
|slug|net_thread_count|

# net_thread_count

<main id="notice" type='explain'>
  <h4>Note</h4>
  <p>This parameter is available starting with V1.4. </p>
</main>

## Description

`net_thread_count` specifies the number of network I/O threads.

## Privilege requirements

* Query the parameter

  The `sys` tenant and all user tenants can query this parameter by using the `SHOW PARAMETERS` statement or the `GV$OB_PARAMETERS` view.

* Modify the parameter

  Only the `sys` tenant can modify this parameter. User tenants cannot modify this parameter.

## Attributes

|      **Attribute**    |   **Description**   |
|------------------|--------------|
| Type            | Int         |
| Default value              | 0          |
| Value range            | [0, 128] <main id="notice" type='explain'><h4>Note</h4><p>This parameter is available starting with V4.3.5 BP2. The value range is adjusted from [0, 64] to [0, 128].</p></main>|
| Modifiable          | Yes. You can use the `ALTER SYSTEM SET` statement to modify the parameter.|
| Effective upon OBServer node restart | No. The setting takes effect immediately.<main id="notice" type='explain'><h4>Note</h4><p>This parameter is available starting with V4.4.0. The effective mode is adjusted from "effective upon restart" to "effective immediately".</p></main>          |

## Considerations

* By default, the number of I/O threads for RPC and MySQL Libeasy is 1/8 of the number of CPU cores.

* If the value of `net_thread_count` is N (N > 0), the number of I/O threads for RPC and MySQL Libeasy is N.

## Examples

Set the number of network I/O threads to 6.

```shell
obclient> ALTER SYSTEM SET net_thread_count = 6;
```

## References

For more information about CPU-related parameters, see [CPU-related parameters](../../../../700.reference/1000.performance-tuning-guide/300.system-tuning/300.obproxy-parameter-optimization/100.cpu-parameters_obproxy.md).