|description||
|---|---|
| machine-translation ||
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||
|slug|strict_check_os_params|

# strict_check_os_params

<main id="notice" type='explain'>
<h4>Note</h4>
<p>For V4.3.x, this parameter is available starting with V4.3.0. For V4.2.x, this parameter is available starting with V4.2.2.</p>
</main>

## Description

The `strict_check_os_params` parameter specifies whether to enable the OS parameter check feature.

## Privilege requirements

* Query the parameter

  You can query the `strict_check_os_params` parameter by using the `SHOW PARAMETERS` statement or the `GV$OB_PARAMETERS` view in the `sys` tenant and all user tenants.

* Modify the parameter

  Only the `sys` tenant can modify the `strict_check_os_params` parameter. User tenants cannot modify this parameter.

## Attributes

| **Attribute** | **Description** |
| --- | --- |
| Type | Boolean |
| Default value | false |
| Value range |[true, false]<ul><li>When the value is true, an error is returned when an OS parameter is out of the specified range, and the OBServer node cannot be started. </li><li>When the value is false, a warning is returned when an OS parameter is out of the specified range, and the OBServer node can be started.  </li></ul>|
| Modifiable | Yes. You can use the `ALTER SYSTEM SET` statement to modify the parameter. |
| Effective upon OBServer node restart | No. The setting takes effect immediately. |

## Examples

Disable the OS parameter check feature.

```shell
obclient> ALTER SYSTEM SET strict_check_os_params=false;
```