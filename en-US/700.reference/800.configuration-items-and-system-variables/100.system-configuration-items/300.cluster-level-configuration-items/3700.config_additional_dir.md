|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||
|machine-translation|en,ja-marker,ja-autoPR|
|slug|config_additional_dir|

# config_additional_dir

<main id="notice" type='explain'>
  <h4>Note</h4>
  <p>For V2.2.50, this parameter is available starting with V2.2.50. </p>
</main>

## Description

`config_additional_dir` specifies multiple local directories for redundant storage of backup configuration files.

## Privilege requirements

* Query the parameter

  The `sys` tenant and all user tenants can query this parameter by using the `SHOW PARAMETERS` statement or the `GV$OB_PARAMETERS` view.

* Modify the parameter

  Only the `sys` tenant can modify this parameter. User tenants cannot modify this parameter.

## Attributes

|    **Attribute**      |     **Description**       |
|------------------|--------------------|
| Type           | String              |
| Default value             | etc2;etc3      |
| Modifiable          | Yes. You can use the `ALTER SYSTEM SET` statement to modify the parameter.|
| Effective upon OBServer node restart | No. The setting takes effect immediately.         |

## Examples

Set the local redundant storage backup directories to `etc2;etc3`.

```shell
obclient> ALTER SYSTEM SET config_additional_dir='/data/1/obdemo/etc2;/data/log1/obdemo/etc3';
```

## References

[Deploy a single-replica OceanBase cluster](../../../../400.deploy/300.deploy-oceanbase-enterprise-edition/400.deploy-through-the-command-line/200.deploy-the-oceanbase-cluster-command-line/300.deploy-single-replica-oceanbase-cluster.md)
