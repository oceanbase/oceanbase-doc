|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||
|machine-translation||

# recyclebin_object_expire_time

<main id="notice" type='explain'>
  <h4>Note</h4>
  <p>This parameter is available starting with V2.2.77. </p>
</main>

## Description

`recyclebin_object_expire_time` specifies the time after which schema objects in the recycle bin are automatically purged.

## Privilege requirements

* Query the parameter

  The `sys` tenant and all user tenants can query this parameter by using the `SHOW PARAMETERS` statement or the `GV$OB_PARAMETERS` view.

* Modify the parameter

  Only the `sys` tenant can modify this parameter. User tenants cannot modify this parameter.

## Attributes

|      **Attribute**      |    **Description**    |
|------------------|--------------|
| Type             | Time      |
| Default value               | 0s, which indicates that the automatic purge feature is disabled.        |
| Value range             | [0s, +∞) |
| Modifiable           | Yes. You can use the `ALTER SYSTEM SET` statement to modify the parameter.|
| Effective upon OBServer node restart | No. The setting takes effect immediately.     |

## Considerations

If the value is not 0s, schema objects that were moved to the recycle bin a specified time ago are purged.

## Examples

* Disable the automatic purge feature.

  ```shell
  obclient> ALTER SYSTEM SET recyclebin_object_expire_time = "0s";
  ```

* Enable the automatic purge feature and purge schema objects that were moved to the recycle bin 7 days ago.

  ```shell
  obclient> ALTER SYSTEM SET recyclebin_object_expire_time = "7d";
  ```

## References

[Clean up the recycle bin](../../../../600.manage/400.high-availability/500.recyclebin-management/500.purge-the-recyclebin.md)
