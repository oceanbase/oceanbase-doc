|description||
|---|---|
| machine-translation ||
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||
|slug|ssl_client_authentication|

# ssl_client_authentication

<main id="notice" type='explain'>
  <h4>Note</h4>
  <p>For V2.2.30, this parameter is available starting with V2.2.30. </p>
</main>

## Description

`ssl_client_authentication` specifies whether to enable the SSL connection feature.

## Privilege requirements

* Query the parameter

  The `sys` tenant and all user tenants can query the value of this parameter by executing the `SHOW PARAMETERS` statement or querying the `GV$OB_PARAMETERS` view.

* Modify the parameter

  Only the `sys` tenant can modify this parameter. User tenants cannot modify this parameter.

## Attributes

| **Attribute** | **Description** |
| -------- | ------- |
| Type             | Bool                       |
| Default value               | False                      |
| Value range             | <ul><li>True: enabled </li><li>False: disabled </li></ul>    |
| Modifiable           | Yes. You can use the `ALTER SYSTEM SET` statement to modify the parameter. |
| Effective upon OBServer node restart | No. The setting takes effect immediately. |

## Considerations

**After you set the `ssl_client_authentication` parameter, you must restart all OBServer nodes to make the setting take effect.**

## Examples

Disable the SSL connection feature.

```shell
obclient> ALTER SYSTEM SET ssl_client_authentication = False;
```

## References

[Data encryption for OBServer nodes](../../../../600.manage/500.security-and-permissions/500.data-encryption/200.data-transfer-encryption/100.data-transfer-encryption-overview.md)