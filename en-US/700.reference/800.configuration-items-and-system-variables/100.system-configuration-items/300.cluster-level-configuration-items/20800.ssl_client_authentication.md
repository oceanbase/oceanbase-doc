|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||
|machine-translation||

# ssl_client_authentication

<main id="notice" type='explain'>
  <h4>Note</h4>
  <p>This parameter is available starting with V2.2.50. </p>
</main>

## Description

`ssl_client_authentication` specifies whether to enable the SSL connection feature.

## Privilege requirements

* Query the parameter

  The `sys` tenant and all user tenants can query this parameter by using the `SHOW PARAMETERS` statement or the `GV$OB_PARAMETERS` view.

* Modify the parameter

  Only the `sys` tenant can modify this parameter. User tenants cannot modify this parameter.

## Attributes

| **Attribute** | **Description** |
| -------- | ------- |
| Type             | Bool                       |
| Default value               | False                      |
| Value range             | <li> False: Disable.   <li> True: Enable.    |
| Modifiable           | Yes. You can use the `ALTER SYSTEM SET` statement to modify the parameter. |
| Effective upon OBServer node restart | No. The setting takes effect immediately. |

## Considerations

**After you set the `ssl_client_authentication` parameter, you must restart all servers to make the setting take effect.**

## Examples

Disable the SSL connection feature.

```shell
obclient> ALTER SYSTEM SET ssl_client_authentication = False;
```

## References

[Data transmission encryption for OBServer nodes](../../../../600.manage/500.security-and-permissions/400.data-transfer-encryption/200.observer-in-transmission-encryption.md)
