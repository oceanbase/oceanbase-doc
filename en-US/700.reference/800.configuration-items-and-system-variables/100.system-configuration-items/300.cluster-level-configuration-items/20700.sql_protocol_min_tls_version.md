|description||
|---|---|
| machine-translation ||
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||
|slug|sql_protocol_min_tls_version|

# sql_protocol_min_tls_version

<main id="notice" type='explain'>
  <h4>Note</h4>
  <p>For V4.2.1, this parameter is available starting with V4.2.1.</p>
</main>

## Description

`sql_protocol_min_tls_version` specifies the minimum version of the SSL/TLS protocol used for SSL connections to the database.

## Privilege requirements

* Query the parameter

  The `sys` tenant and all user tenants can use the `SHOW PARAMETERS` statement or the `GV$OB_PARAMETERS` view to query this parameter.

* Modify the parameter

  Only the `sys` tenant can modify this parameter. User tenants cannot modify this parameter.

## Attributes

| **Attribute** | **Description** |
| --- | --- |
| Type             | String |
| Default value    | none |
| Value range      |<ul><li>none: No restriction is placed on the SSL/TLS version used for connections.  </li><li>TLSv1 / TLSv1.1 / TLSv1.2 / TLSv1.3: If you specify any of these versions, the specified version and higher versions are used for SSL encryption connections. </li></ul> |
| Modifiable       | Yes. You can use the `ALTER SYSTEM SET` statement to modify the parameter. |
| Effective upon OBServer node restart | No. The setting takes effect immediately. |

## Considerations

This parameter only controls the selection of SSL versions. To enable SSL, you must also set [ssl_client_authentication](20800.ssl_client_authentication.md).

## Examples

Set the minimum version of the SSL/TLS protocol used for SSL connections to NONE.

```shell
obclient> ALTER SYSTEM SET sql_protocol_min_tls_version = 'none';
```

## References

[Data transmission encryption](../../../../600.manage/500.security-and-permissions/500.data-encryption/200.data-transfer-encryption/100.data-transfer-encryption-overview.md)