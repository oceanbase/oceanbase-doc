|description||
|---|---|
| machine-translation ||
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||

# balancer_tolerance_percentage

<main id="notice" type='explain'>
  <h4>Note</h4>
  <p>This parameter is available starting with V1.4. </p>
</main>

## Description

`balancer_tolerance_percentage` specifies the tolerance of disk imbalance between log streams in a tenant for the partition balancing strategy. If the disk usage difference between any two log streams exceeds this percentage, disk-based partition balancing is triggered.

## Privilege requirements

* Query the parameter

  The `sys` tenant and all user tenants can query this parameter by using the `SHOW PARAMETERS` statement.

* Modify the parameter

  Only the `sys` tenant can modify this parameter. User tenants cannot modify this parameter.

## Attributes

|      **Attribute**    |  **Description** |
|------------------|-----------|
| Type            | Int        |
| Default value              | 10        |
| Value range            | [1, 100) |
| Modifiable          | Yes. You can use the `ALTER SYSTEM SET` statement to modify the parameter.    |
| Effective upon OBServer node restart | No. The setting takes effect immediately.    |

## Examples

Set the tolerance of disk imbalance between log streams in a tenant to 10.

```shell
obclient> ALTER SYSTEM SET balancer_tolerance_percentage=10;
```

## References

[Intra-tenant balancing](../../../100.oceanbase-database-concepts/500.distributed-database-objects/300.data-partitions-and-replicas/500.data-balancing/100.intra-tenant-balancing.md)