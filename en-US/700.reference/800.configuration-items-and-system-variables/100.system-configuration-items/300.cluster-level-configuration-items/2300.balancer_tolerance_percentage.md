|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||
|machine-translation| en |
|slug|balancer_tolerance_percentage|

# balancer_tolerance_percentage

<main id="notice" type='explain'>
  <h4>Note</h4>
  <p>This parameter is available starting with V1.4. </p>
</main>

## Description

`balancer_tolerance_percentage` specifies the tolerance for disk imbalance between log streams within a tenant during partition balancing. If the disk usage difference between any two log streams exceeds this percentage, disk-based partition balancing is triggered.

## Privilege requirements

* Query the parameter

  The `sys` tenant and all user tenants can use the `SHOW PARAMETERS` statement to query this parameter.

* Modify the parameter

  Only the `sys` tenant can modify this parameter. User tenants cannot modify this parameter.

## Attributes

|      **Attribute**    |  **Description** |
|------------------|-----------|
| Type            | Int        |
| Default value              | 10        |
| Value range            | [1, 100) |
| Modifiable          | Yes. You can use the `ALTER SYSTEM SET` statement to modify the parameter.    |
| Effective upon OBServer node restart | No. The setting takes effect immediately.    |

## Examples

Set the tolerance for disk imbalance between log streams within a tenant to 10.

```shell
obclient> ALTER SYSTEM SET balancer_tolerance_percentage=10;
```

## References

[Intra-tenant balancing](../../../../700.reference/100.oceanbase-database-concepts/500.distributed-database-objects/300.data-partitions-and-replicas/500.data-balancing/100.intra-tenant-balancing.md)