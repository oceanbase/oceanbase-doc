|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||
|machine-translation| en |
|slug|location_cache_refresh_min_interval|

# location_cache_refresh_min_interval

<main id="notice" type='explain'>
  <h4>Note</h4>
  <p>This parameter is available starting with V1.4. </p>
</main>

## Description

`location_cache_refresh_min_interval` specifies the minimum interval for location cache refresh requests to prevent excessive refresh requests that could cause system overload.

## Privilege requirements

* Query the parameter

  The `sys` tenant and all user tenants can query this parameter by using the `SHOW PARAMETERS` statement or the `GV$OB_PARAMETERS` view.

* Modify the parameter

  Only the `sys` tenant can modify this parameter. User tenants cannot modify this parameter.

## Attributes

| **Attribute** | **Description** |
| -------- | -------- |
| Type    | Time      |
| Default value      | 100ms     |
| Value range    | [0s, +∞) |
| Modifiable  | Yes. You can use the `ALTER SYSTEM SET` statement to modify the parameter. |
| Effective upon OBServer node restart | No. The setting takes effect immediately.  |

## Examples

Set the minimum interval for location cache refresh requests to 100 ms.

```shell
obclient> ALTER SYSTEM SET location_cache_refresh_min_interval ='100ms';
```