|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||
|machine-translation| en |

# min_observer_version

<main id="notice" type='explain'>
<h4>Note</h4>
<p>For V2.2.50, this parameter is available starting with V2.2.50.</p>
</main>

## Description

`min_observer_version` specifies the minimum version of the OBServer node application in the cluster.

## Privilege requirements

* Query the parameter

  The `sys` tenant and all user tenants can use the `SHOW PARAMETERS` statement or the `GV$OB_PARAMETERS` view to query this parameter.

* Modify the parameter

  Only the `sys` tenant can modify this parameter. User tenants cannot modify this parameter.

## Attributes

|      **Attribute**      | **Description**  |
|------------------|---------|
| Type             | Int     |
| Default value              | 4.4.1.0 |
| Modifiable          | Yes. You can use the `ALTER SYSTEM SET` statement to modify the parameter.|
| Effective upon OBServer node restart | No. The setting takes effect immediately.    |

## Examples

Set the minimum version of the OBServer node application in the cluster to 4.4.1.0.

```shell
obclient> ALTER SYSTEM SET min_observer_version='4.4.1.0';
```

## References

[Upgrade an OceanBase cluster](../../../../600.manage/100.cluster-management/300.common-cluster-operations/1500.oceanbase-version-upgrade/200.upgrade-oceanbase-enterprise-edition/300.start-upgrade.md)