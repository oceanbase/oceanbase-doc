|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||
|machine-translation|en,ja-marker,ja-autoPR|

# default_compress

<main id="notice" type='explain'>
  <h4>Note</h4>
  <p>This parameter is available starting with V2.1. </p>
</main>

## Description

`default_compress` specifies the default compression strategy when you create a table in Oracle mode.

<main id="notice">
  <h4>Applicability</h4>
  <p>This parameter is applicable only to OceanBase Database in Oracle mode. </p>
</main>

## Privilege requirements

* Query the parameter

  The `sys` tenant and all user tenants can query this parameter by using the `SHOW PARAMETERS` statement or the `GV$OB_PARAMETERS` view.

* Modify the parameter

  Only the `sys` tenant can modify this parameter. User tenants cannot modify this parameter.

## Attributes

|   **Attribute**    |  **Description**  |
|---------------|------------|
| Type             | String     |
| Default value               | archive                                                                  |
| Value range             | <ul><li>NOCOMPRESS: uses the Flat format without compression. </li><li>BASIC: uses the Flat format and <code>lz4_1.0</code> compression method. </li><li>OLTP: uses the Flat format and <code>zstd_1.3.8</code> compression method. </li><li>QUERY: uses the Encoding format and <code>lz4_1.0</code> compression method. </li> <li>ARCHIVE: uses the Encoding format and <code>zstd_1.3.8</code> compression method. </li></ul>     |
| Modifiable           | Yes. You can use the `ALTER SYSTEM SET` statement to modify the parameter. |
| Effective upon OBServer node restart | No. The setting takes effect immediately.   |

## Examples

Enable Debug Sync and set the timeout to 3 seconds.

```shell
obclient> ALTER SYSTEM SET default_compress= 'NOCOMPRESS';
```

## References

* Set the default compression algorithm for table data: [default_compress_func](5300.default_compress_func.md)
