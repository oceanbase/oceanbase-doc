|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||
|machine-translation||

# tenant_sql_login_thread_count

<main id="notice" type='explain'>
  <h4>Note</h4>
  <p>For V4.1.0, this parameter is available starting with V4.1.0. </p>
</main>

## Description

`tenant_sql_login_thread_count` specifies the number of threads for MySQL tenant login, that is, the number of `mysql_queue` threads.

## Privilege requirements

* Query the parameter

  The `sys` tenant and all user tenants can query this parameter by using the `SHOW PARAMETERS` statement or the `GV$OB_PARAMETERS` view.

* Modify the parameter

  The `sys` tenant and user tenants can modify this parameter.

## Attributes

|        Attribute      |  Description  |
|------------------|-------|
| Type             | Int |
| Default value               | 0, which indicates the value of `unit_min_cpu`. |
| Value range             | [0, 32] |
| Modifiable           | Yes. You can use the `ALTER SYSTEM SET` statement to modify the parameter.|
| Effective upon OBServer node restart | No. The setting takes effect immediately.  |

## Examples

Set the number of threads for MySQL tenant login to 0.

```shell
obclient> ALTER SYSTEM SET tenant_sql_login_thread_count= 0;
```

## References

Set the number of threads for processing SQL login requests: [sql_login_thread_count](../300.cluster-level-configuration-items/20500.sql_login_thread_count.md)
