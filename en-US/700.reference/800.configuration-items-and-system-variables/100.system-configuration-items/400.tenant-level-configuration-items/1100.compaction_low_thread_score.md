|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||
|machine-translation||

# compaction_low_thread_score

<main id="notice" type='explain'>
  <h4>Note</h4>
  <p>This parameter is available starting with V4.0.0. </p>
</main>

## Description

`compaction_low_thread_score` specifies the number of threads for low-priority major compactions. A low-priority major compaction is a major compaction on a major SSTable.

## Privilege requirements

* Query the parameter

  The `sys` tenant and all user tenants can query this parameter by using the `SHOW PARAMETERS` statement or the `GV$OB_PARAMETERS` view.

* Modify the parameter

  The `sys` tenant and user tenants can modify this parameter.

## Attributes

|      **Attribute**      |  **Description**  |
|------------------|----------|
| Type             | Int       |
| Default value               | 0, which indicates that the internal default value of 6 is used.        |
| Value range             | [0, 100] |
| Modifiable           | Yes. You can use the `ALTER SYSTEM SET` statement to modify the parameter. |
| Effective upon OBServer node restart | No. The setting takes effect immediately.      |

## Examples

Set the number of threads for low-priority major compactions to 6.

```shell
obclient> ALTER SYSTEM SET compaction_low_thread_score= 6;
```

## References

[I/O-related parameters](../../../../700.reference/1000.performance-tuning-guide/300.system-tuning/200.database-parameter-tuning/200.i-o-parameters.md)
