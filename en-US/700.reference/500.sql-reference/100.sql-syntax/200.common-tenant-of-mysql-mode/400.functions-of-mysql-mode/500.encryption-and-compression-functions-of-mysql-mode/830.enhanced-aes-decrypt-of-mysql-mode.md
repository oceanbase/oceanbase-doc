| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |

# ENHANCED_AES_DECRYPT

## Syntax

````sql
ENHANCED_AES_DECRYPT(cipher_text[, iv])
````

## Purpose

* This function is primarily used for column encryption, which can be enabled by using the `tde_method` parameter. For more information, see [tde_method](../../../../../800.configuration-items-and-system-variables/100.system-configuration-items/400.tenant-level-configuration-items/7800.tde_method.md).

* The required privilege for this function is `DECRYPT`.

* `cipher_text` is the ciphertext to be decrypted using AES.

* `[, iv]` is an optional parameter that represents the initialization vector.

* The input parameter `cipher_text` for the decryption function `ENHANCED_AES_DECRYPT` must be the result generated by the `ENHANCED_AES_ENCRYPT` function; otherwise, it will trigger the 4334 `OB_ERR_AES_DECRYPT` error.

## Examples

The `SELECT` statement uses the decryption function to return plaintext columns.

````sql
-- Return plaintext
SELECT ENHANCED_AES_DECRYPT(cipher_col) FROM tbl;
````

When comparing ciphertext with its corresponding plaintext, the result will return false. To compare, you need to either decrypt the ciphertext or encrypt the plaintext first.

````sql
-- After decrypting cipher_col, it is expected to match the corresponding plaintext data '12345'. However, indexes cannot be utilized.
SELECT ... FROM tbl WHERE ENHANCED_AES_DECRYPT(cipher_col) = '12345';
````

## References

* [ENHANCED_AES_ENCRYPT](820.enhanced-aes-encrypt-of-mysql-mode.md)

* [Privilege types in MySQL mode](../../../../../../600.manage/500.security-and-permissions/300.access-control/200.user-and-permission/200.permission-of-mysql-mode/100.permission-classification-of-mysql.md)