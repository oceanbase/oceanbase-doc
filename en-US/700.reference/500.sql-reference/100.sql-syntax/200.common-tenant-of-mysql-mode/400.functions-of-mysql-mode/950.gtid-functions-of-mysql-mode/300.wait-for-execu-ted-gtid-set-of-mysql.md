| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |

# WAIT_FOR_EXECUTED_GTID_SET()

## Purpose

`WAIT_FOR_EXECUTED_GTID_SET()` waits until the transaction corresponding to the specified global transaction ID (GTID) has been executed on the replica.

## Syntax

```sql
WAIT_FOR_EXECUTED_GTID_SET(gtid_set[, timeout])
```

<main id="notice" type='explain'>
  <h4>Note</h4>
  <p>Only the keyword syntax is supported. The functionality does not actually take effect. </p>
</main>