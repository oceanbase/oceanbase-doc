| Description   |                 |
|---------------|-----------------|
| machine-translation ||
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |
|slug|updatexml-function-mysql-mode|

# UPDATEXML

## Purpose

This function is used to update the content of a node specified by a path in an XML string.

## Syntax

```sql
UPDATEXML(xml_target, xpath_expr, new_xml)
```

## Parameters

<main id="notice" type='explain'>
  <h4>Note</h4>
  <p>Replace the part of <code>xml_target</code> specified by <code>xpath_expr</code> with <code>new_xml</code>. If <code>xpath_expr</code> does not match any result or matches multiple results, return the original result of <code>xml_target</code>.</p>
</main>

| **Field** | **Description** |
| -------- | -------- |
| xml_target | The specified XML string, which can be an XML document or fragment. |
| xpath_expr | The path (XPath) expression of the node to be updated. |
| new_xml | The new XML fragment to replace the node. |

## Return type

The return type is `LONGTEXT`.

## Examples

* Use the `UPDATEXML` function to replace the `</a>` node in the XML string with the `<x>carrot</x>` node and return the updated XML string. `<a/><b/>` represents a root node containing two nodes, `<a>` and `<b>`.

  ```shell
  obclient [test_db]> SELECT UPDATEXML('<a/><b/>', '/a', '<x>carrot</x>');
  ```

  The return result is as follows:

  ```shell
  +----------------------------------------------+
  | UPDATEXML('<a/><b/>', '/a', '<x>carrot</x>') |
  +----------------------------------------------+
  | <x>carrot</x><b></b>                         |
  +----------------------------------------------+
  1 row in set
  ```

* Use the `UPDATEXML` function to replace the `a` node under the `root` node in the XML string with the `<b>bbb</b>` node and return the updated XML string.

  ```shell
  obclient [test_db]> SELECT UPDATEXML('<root><a>aaa</a></root>', '/root/a', '<b>bbb</b>');
  ```

  The return result is as follows:

  ```shell
  +---------------------------------------------------------------+
  | UPDATEXML('<root><a>aaa</a></root>', '/root/a', '<b>bbb</b>') |
  +---------------------------------------------------------------+
  | <root><b>bbb</b></root>                                       |
  +---------------------------------------------------------------+
  1 row in set
  ```