| Description   |                 |
|---------------|-----------------|
| machine-translation ||
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |
|slug|decode_trace_id-mysql-mode|

# DECODE_TRACE_ID

## Syntax

```sql
DECODE_TRACE_ID()
```

## Purpose

The `DECODE_TRACE_ID()` function is used to parse the trace_id and return the IP address and port of the SQL request source server. You can also view the related information in the [GV$OB_SQL_AUDIT](../../../../../../../700.reference/700.system-views/400.system-view-of-mysql-mode/300.performance-view-of-mysql-mode/3500.gv-ob_sql_audit-of-mysql-mode.md) view.

## Examples

```sql
obclient [(none)]> SELECT DECODE_TRACE_ID(LAST_TRACE_ID());
```

The execution result is as follows:

```shell
+----------------------------------+
| DECODE_TRACE_ID(LAST_TRACE_ID()) |
+----------------------------------+
| xxx.xx.xxx.xx1:2882              |
+----------------------------------+
1 row in set
```