| Description   |                 |
|---------------|-----------------|
| machine-translation ||
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |
|slug|to_char-character-mysql-mode|

# TO_CHAR(character)

## Purpose

This function converts character data to a `VARCHAR` string.

<main id="notice" type='explain'>
  <h4>Note</h4>
  <p>This function is supported in OceanBase Database V4.3.5 BP1 and later. </p>
</main>

## Syntax

```sql
TO_CHAR(character)
```

## Parameters

`character`: the character data to be converted.

## Return type

* Returns `VARCHAR` data.
* Returns `NULL` if the value of `character` is `NULL`.

## Examples

```sql
SELECT TO_CHAR('Hello, World!'), TO_CHAR(NULL);
```

The return result is as follows:

```shell
+--------------------------+---------------+
| TO_CHAR('Hello, World!') | TO_CHAR(NULL) |
+--------------------------+---------------+
| Hello, World!            |          NULL |
+--------------------------+---------------+
1 row in set
```

## References

* [TO_CHAR(number)](1700.to_char_number-of-mysql-mode.md)
* [TO_CHAR(datetime)](1800.to_char_datetime-of-mysql-mode.md)