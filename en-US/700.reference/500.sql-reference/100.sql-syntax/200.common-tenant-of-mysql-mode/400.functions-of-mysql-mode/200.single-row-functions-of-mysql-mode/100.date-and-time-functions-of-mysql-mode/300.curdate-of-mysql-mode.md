| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |
|machine-translation||

# CURDATE

## Syntax

```javascript
CURDATE()
```

## Purpose

`CURDATE()` returns the current date without the time part.

## Examples

```javascript
obclient> SELECT CURDATE();
+------------+
| CURDATE() |
+------------+
| 2018-05-05 |
+------------+
1 row in set
```
