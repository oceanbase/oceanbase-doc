| Description   |                 |
|---------------|-----------------|
| machine-translation ||
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |
|slug|day-mysql-mode|

# DAY

## Syntax

```sql
DAY(date)
```

## Purpose

Returns the day of the month in the `date` value, ranging from 1 to 31. `DAY()` is an alias for `DAYOFMONTH()`.

## Examples

```sql
obclient> SELECT DAY('2008-12-31');
+--------------------------+
| DAY('2008-12-31') |
+--------------------------+
|                       31 |
+--------------------------+
```