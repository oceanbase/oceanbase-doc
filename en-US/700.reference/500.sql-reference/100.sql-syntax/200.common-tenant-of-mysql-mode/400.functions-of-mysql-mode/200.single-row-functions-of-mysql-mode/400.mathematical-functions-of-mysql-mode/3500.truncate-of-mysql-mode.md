| Description   |                 |
|---------------|-----------------|
| machine-translation ||
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |
|slug|truncate-mysql-mode|

# TRUNCATE

## Syntax

```sql
TRUNCATE(X,D)
```

## Purpose

Returns the result of `X` with `D` decimal places. If `D` is `0`, it returns the integer part of `X`. If `D` is negative, it sets the `D` digits to the left of the decimal point of `X` to `0`.

## Examples

```sql
obclient> SELECT TRUNCATE(3.27,1);
+------------------+
| TRUNCATE(3.27,1) |
+------------------+
|              3.2 |
+------------------+
1 row in set

obclient> SELECT TRUNCATE(3.27,0);
+------------------+
| TRUNCATE(3.27,0) |
+------------------+
|                3 |
+------------------+
1 row in set

obclient> SELECT TRUNCATE(3.27,-1);
+-------------------+
| TRUNCATE(3.27,-1) |
+-------------------+
|                 0 |
+-------------------+
1 row in set
```