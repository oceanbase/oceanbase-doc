| Description   |                 |
|---------------|-----------------|
| machine-translation ||
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |
|slug|bin-mysql-mode|

# BIN

## Syntax

```sql
bin(N)
```

## Purpose

Returns the binary representation of the number `N`. This function is equivalent to `CONV(N,10,2)`.

If `N` is `NULL`, it returns `NULL`.

## Examples

```sql
obclient> SELECT BIN(12);
+---------+
| BIN(12) |
+---------+
| 1100    |
+---------+
1 row in set
```