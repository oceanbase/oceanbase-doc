| Description   |                 |
|---------------|-----------------|
| machine-translation ||
| keywords      |                 |
| dir-name      | Other Hint        |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode     |
|slug|outline-hints-mysql-mode|

# Outline-related hints

| Hint Name                               | Description                            |
|---------------------------------------|----------------------------------|
| `BEGIN_OUTLINE_DATA`                  | The start marker for outline data.            |
| `END_OUTLINE_DATA`                    | The end marker for outline data.           |
| `QB_NAME`                             | Specifies a name for a query block, which can be referenced in other hints.|
| `OPTIMIZER_FEATURES_ENABLE`           | Specifies the optimizer version for query plan generation.             |

## OUTLINE DATA structure

**OUTLINE DATA** is used to identify hints during query optimization. A valid OUTLINE DATA structure is as follows:

```sql
SELECT /*+PARALLEL(2)*/ 1
FROM (SELECT c2 FROM t1 WHERE c1 = 1) v, t2
WHERE t2.c2 = v.c2;

/*+
  BEGIN_OUTLINE_DATA
  // Optimization hints
  LEADING(@"SEL$6D1A4751" ("TEST.T1"@"SEL$2" "TEST.T2"@"SEL$1"))
  USE_HASH(@"SEL$6D1A4751" "TEST.T2"@"SEL$1")
  PQ_DISTRIBUTE(@"SEL$6D1A4751" "TEST.T2"@"SEL$1" NONE BROADCAST)
  PX_JOIN_FILTER(@"SEL$6D1A4751" "TEST.T2"@"SEL$1")
  FULL(@"SEL$6D1A4751" "TEST.T1"@"SEL$2")
  FULL(@"SEL$6D1A4751" "TEST.T2"@"SEL$1")
  // Transformation hints
  MERGE(@"SEL$2" > "SEL$1")
  // Global hints
  OPTIMIZER_FEATURES_ENABLE('4.0.0.0')
  PARALLEL(2)
  END_OUTLINE_DATA
*/
```

The hints between `BEGIN_OUTLINE_DATA` and `END_OUTLINE_DATA` are recognized as OUTLINE DATA if they include the `OPTIMIZER_FEATURES_ENABLE` hint and specify a valid optimizer version.

## `OPTIMIZER_FEATURES_ENABLE` Hint

The `OPTIMIZER_FEATURES_ENABLE` hint specifies the optimizer version used for the current query.

### Syntax

```sql
/*+ OPTIMIZER_FEATURES_ENABLE ( version_number ) */
```

#### Parameters

- `version_number`: The optimizer version.

### Examples

```sql
-- Use the OPTIMIZER_FEATURES_ENABLE hint to specify that the optimizer should use features from version 4.0.0.0 when generating the execution plan.
SELECT /*+ OPTIMIZER_FEATURES_ENABLE('4.0.0.0') */ employee_id, last_name
FROM employees
WHERE last_name = 'Smith';
```

## `QB_NAME` Hint

The `QB_NAME` hint allows you to specify a name for a query block, which can then be referenced in other hints. This is particularly useful when you need to apply more precise control to specific parts of a query.

For more information, see [Use Query Block Name and QB_NAME Hint to Control Query Optimization](../150.query-block-name-qb-hint-of-mysql-mode.md)

### Syntax

```sql
/*+ QB_NAME(queryblock) */
```

#### Parameters

- `qb_name`: The name of the query block. It is case-insensitive, cannot contain special characters, and has a maximum length of 20 characters.

#### Considerations

The `QB_NAME` hint is ineffective in the following scenarios:

- Multiple valid `qb_name` values are specified at the same query level.
- The same `qb_name` value is specified at different query levels.

### Examples

```sql
-- Use the QB_NAME hint to name the query block 'qb' and the FULL hint to force a full table scan on the employees table.
SELECT /*+ QB_NAME(qb) FULL(@qb e) */ employee_id, last_name
FROM employees e
WHERE last_name = 'Smith';
```