| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |
|machine-translation||

# UNLOCK TABLES

## Purpose

This statement is used to release all table locks held by the current session.

<main id="notice" type='explain'>
  <h4>Note</h4>
  <p>For OceanBase Database V4.3.5, the <code>UNLOCK TABLES</code> statement is supported starting from V4.3.5 BP2.
</main>

## Limitations and considerations

`UNLOCK TABLES` does not automatically commit transactions.

## Syntax

```sql
UNLOCK {TABLE | TABLES};
```

## Parameters

|       **Parameter**       | **Description** |
|--------------------------|----------|
| UNLOCK {TABLE \| TABLES} | Releases all table locks held by the current session. `UNLOCK TABLE` is a synonym for `UNLOCK TABLES`.|

## Example

Release all table locks held by the current session.

```sql
UNLOCK TABLES;
```

## References

[LOCK TABLES](6000.lock-table-of-mysql-mode.md)
