| Description   |                 |
|---------------|-----------------|
| machine-translation ||
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |
|slug|delete-database-link-mysql-mode|

# DROP DATABASE LINK

## Purpose

This statement is used to drop a database link (i.e., `dblink`).

<main id="notice" type='explain'>
  <h4>Note</h4>
  <p>You must have the <code>DROP DATABASE LINK</code> system privilege to drop a database link. </p>
</main>

## Syntax

```sql
DROP DATABASE LINK [IF EXISTS] dblink_name
```

## Parameters

`dblink_name` specifies the name of the database link to be dropped.

## Examples

```sql
obclient> DROP DATABASE LINK IF EXISTS ob_dblink;
```