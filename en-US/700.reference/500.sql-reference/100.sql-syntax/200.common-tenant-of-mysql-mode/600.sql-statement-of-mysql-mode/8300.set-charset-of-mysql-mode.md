| Description   |                 |
|---------------|-----------------|
| description   | The `SET CHARSET` statement specifies the character set for the client and results. |
| machine-translation ||
| keywords      | SET CHARSET, set character set |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |
|slug|set-charset-mysql-mode|

# SET CHARSET

## Purpose

The `SET CHARSET` statement specifies the character set for the client and results.

## Syntax

```sql
SET {CHARSET | CHARACTER SET} {DEFAULT | charset_name}
```

## Parameters

|      Parameter      |                                                                                                                                            Description                                                                                                                                             |
|--------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| charset_name | Specifies the character set for the client and results. Valid values: <ul><li> `utf8`  </li> <li> `utf8mb4`   </li> <li> `gbk`   </li> <li> `utf16`   </li> <li> `gb18030`  </li></ul>  |
| DEFAULT      | Uses the system default character set.                                                                                                                                                                                                                                                                                |

## Examples

Set the client character set to `gbk`:

```sql
obclient> SET CHARSET 'gbk';
```

View character set-related variables:

```sql
obclient> SHOW VARIABLES LIKE '%char%';
```

The query result is as follows:

```sql
+--------------------------------------+---------+
| Variable_name                        | Value   |
+--------------------------------------+---------+
| character_set_client                 | gbk     |
| character_set_connection             | utf8mb4 |
| character_set_database               | utf8mb4 |
| character_set_filesystem             | binary  |
| character_set_results                | gbk     |
| character_set_server                 | utf8mb4 |
| character_set_system                 | utf8mb4 |
| validate_password_special_char_count | 0       |
+--------------------------------------+---------+
8 rows in set
```

Set the character set to the system default value:

```sql
obclient> SET CHARSET DEFAULT;
```