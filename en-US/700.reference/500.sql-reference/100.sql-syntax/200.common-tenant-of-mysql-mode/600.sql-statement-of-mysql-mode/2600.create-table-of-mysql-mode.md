| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |

# CREATE TABLE

## Description

This statement is used to create a new table in a database.

## Syntax

```sql
CREATE [hint_options] TABLE [IF NOT EXISTS] table_name
      (table_definition_list) [table_option_list] [partition_option] [table_column_group_option] [IGNORE | REPLACE] [AS] select;

CREATE TABLE [IF NOT EXISTS] table_name
      LIKE table_name;

table_definition_list:
    table_definition [, table_definition ...]

table_definition:
      column_definition_list
    | [CONSTRAINT [constraint_name]] PRIMARY KEY index_desc
    | [CONSTRAINT [constraint_name]] UNIQUE {INDEX | KEY}
            [index_name] index_desc
    | [CONSTRAINT [constraint_name]] FOREIGN KEY
            [index_name] index_desc
            REFERENCES reference_definition
            [match_action][opt_reference_option_list]
    | [FULLTEXT] {INDEX | KEY} [index_name] [index_type] (key_part,...) [WITH PARSER tokenizer_option] [PARSER_PROPERTIES[=](parser_properties_list)]
      [index_option_list] [index_column_group_option]
    | index_json_clause
    | [CONSTRAINT [constraint_name]] CHECK(expression) constranit_state

column_definition_list:
    column_definition [, column_definition ...]

column_definition:
     column_name data_type
         [DEFAULT const_value] [AUTO_INCREMENT]
         [NULL | NOT NULL] [[PRIMARY] KEY] [UNIQUE [KEY]] [COMMENT string_value] [SKIP_INDEX(skip_index_option_list)]
   | column_name data_type
         [GENERATED ALWAYS] AS (expr) [VIRTUAL | STORED]
         [opt_generated_column_attribute]

skip_index_option_list:
    skip_index_option [,skip_index_option ...]

skip_index_option:
    MIN_MAX
    | SUM

index_desc:
   (column_desc_list) [index_type] [index_option_list]

match_action:
   MATCH {SIMPLE | FULL | PARTIAL}

opt_reference_option_list:
   reference_option [,reference_option ...]

reference_option:
   ON {DELETE | UPDATE} {RESTRICT | CASCADE | SET NULL | NO ACTION | SET DEFAULT}

tokenizer_option:
    SPACE
    | NGRAM
    | BENG
    | IK

parser_properties_list:
    parser_properties, [parser_properties]

parser_properties:
    min_token_size = int_value
    | max_token_size = int_value
    | ngram_token_size = int_value
    | ik_mode = 'char_value'

key_part:
    {index_col_name [(length)] | (expr)} [ASC | DESC]

index_type:
    USING BTREE

index_option_list:
    index_option [ index_option ...]

index_option:
      [GLOBAL | LOCAL]
    | block_size
    | compression
    | STORING(column_name_list)
    | COMMENT string_value

table_option_list:
    table_option [ table_option ...]

table_option:
      [DEFAULT] {CHARSET | CHARACTER SET} [=] charset_name
    | [DEFAULT] COLLATE [=] collation_name
    | table_tablegroup
    | block_size
    | lob_inrow_threshold [=] num
    | compression
    | AUTO_INCREMENT [=] INT_VALUE
    | COMMENT string_value
    | ROW_FORMAT [=] REDUNDANT|COMPACT|DYNAMIC|COMPRESSED|DEFAULT
    | PCTFREE [=] num
    | parallel_clause
    | DUPLICATE_SCOPE [=] 'none|cluster'
    | TABLE_MODE [=] 'table_mode_value'
    | auto_increment_cache_size [=] INT_VALUE
    | READ {ONLY | WRITE}
    | ORGANIZATION [=] {INDEX | HEAP}
    | SEMISTRUCT_ENCODING_TYPE [=] 'encoding|'

parallel_clause:
    {NOPARALLEL | PARALLEL integer}

table_mode_value:
    NORMAL
    | QUEUING
    | MODERATE
    | SUPER
    | EXTREME

partition_option:
      PARTITION BY HASH(expression)
      [subpartition_option] PARTITIONS partition_count
    | PARTITION BY KEY([column_name_list])
      [subpartition_option] PARTITIONS partition_count
    | PARTITION BY RANGE {(expression) | COLUMNS (column_name_list)}
      [subpartition_option] (range_partition_list)
    | PARTITION BY LIST {(expression) | COLUMNS (column_name_list)}
      [subpartition_option] PARTITIONS partition_count
    | PARTITION BY RANGE [COLUMNS]([column_name_list]) [SIZE('size_value')] (range_partition_list)

subpartition_option:
      SUBPARTITION BY HASH(expression)
      SUBPARTITIONS subpartition_count
    | SUBPARTITION BY KEY(column_name_list)
      SUBPARTITIONS subpartition_count
    | SUBPARTITION BY RANGE {(expression) | COLUMNS (column_name_list)}
      (range_subpartition_list)
    | SUBPARTITION BY LIST(expression)

range_partition_list:
    range_partition [, range_partition ...]

range_partition:
    PARTITION partition_name
    VALUES LESS THAN {(expression_list) | MAXVALUE}

range_subpartition_list:
    range_subpartition [, range_subpartition ...]

range_subpartition:
    SUBPARTITION subpartition_name
    VALUES LESS THAN {(expression_list) | MAXVALUE}

expression_list:
    expression [, expression ...]

column_name_list:
    column_name [, column_name ...]

partition_name_list:
    partition_name [, partition_name ...]

partition_count | subpartition_count:
    INT_VALUE

table_column_group_option/index_column_group_option:
      WITH COLUMN GROUP(all columns)
    | WITH COLUMN GROUP(each column)
    | WITH COLUMN GROUP(all columns, each column)

index_json_clause:
    [UNIQUE] INDEX idx_json_name((CAST(json_column_name->'$.json_field_name' AS UNSIGNED ARRAY)))
    | INDEX idx_json_name(column_name, [column_name, ...] (CAST(json_column_name->'$.json_field_name' AS CHAR(n) ARRAY)))
```

## Parameters

|                        **Parameter**                        |        **Description**        |
|------------------------------------------------------|-----------------------------------|
| hint_options | Specifies hint options. You can manually specify the bypass import hint, namely, APPEND, DIRECT, or NO_DIRECT. The corresponding hint format is <code>/*+ [APPEND \| DIRECT(need_sort,max_error,load_type)] parallel(N) \|NO_DIRECT */</code>. For more information about importing data by using the CREATE TABLE AS SELECT statement, see the **Bypass import data by using the CREATE TABLE AS SELECT statement** section in [Full bypass import](../../../../../500.data-migration/1100.bypass-import/200.full-bypass-import.md). |
| IF NOT EXISTS | If you specify `IF NOT EXISTS`, an error will not be reported even if the to-be-created table already exists. If you do not specify it but the table already exists, an error will be returned. |
| IGNORE \| REPLACE | Specifies how to handle rows with duplicate unique key values when you use the `CREATE TABLE ... SELECT` statement. An error will be returned if you do not specify either <code>IGNORE</code> or <code>REPLACE</code>. <ul><li><code>IGNORE</code>: A conflict row whose unique key value conflicts with that in the new table will be ignored and will not be copied to the new table. In other words, the <code>IGNORE</code> option skips duplicate data and copies only rows that will not cause unique key conflicts.</li><li><code>REPLACE</code>: The row in the original new table with a unique key value that is duplicated will be replaced with the row containing the unique key value in the replicated data.</li></ul> |
| PRIMARY KEY                                          | Specifies the primary key for the table. If you do not specify it, a hidden primary key is used. OceanBase Database allows you to modify the primary key of a table or add a primary key to a table by using the `ALTER TABLE` statement. For more information, see [ALTER TABLE](1600.alter-table-of-mysql-mode.md). |
| FOREIGN KEY                                          | Specifies the foreign key for the table. If you do not specify the foreign key name, the name is created based on the table name, `OBFK`, and the creation time. (For example, the name of the foreign key created for the `t1` table at 00:00:00 on August 1, 2021, is `t1_OBFK_1627747200000000`. ) Foreign keys allow cross-reference to related data across tables. The result of an `UPDATE` or `DELETE` operation depends on the reference operation specified in the `ON UPDATE` and `ON DELETE` clauses:<ul><li>`CASCADE`: indicates that the corresponding row in the parent table is deleted or updated, and the system automatically deletes or updates the corresponding row in the child table.</li><li>`SET NULL`: indicates that the corresponding row in the parent table is deleted or updated, and the foreign key column in the child table is set to `NULL`.</li><li>`RESTRICT`: indicates that the corresponding operation on the parent table is rejected.</li><li>`NO ACTION`: indicates that the operation is delayed until the corresponding row in the child table is matched.</li></ul> In addition, the `SET DEFAULT` operation is supported. |
| FULLTEXT | Specifies whether to create a full-text index. For more information about how to create a full-text index, see the **Create a full-text index** section in [Create an index](../../../../300.database-object-management/100.manage-object-of-mysql-mode/500.manage-indexes-of-mysql-mode/200.create-an-index-of-mysql-mode.md). <main id="notice" type='notice'><h4>Notice</h4><p>At present, full-text indexes can only be created as partial indexes.</p></main> |
| WITH PARSER tokenizer_option                 | Specifies the parser for the full-text index. For more information, see [tokenizer_option](#tokenizer_option). |
| PARSER_PROPERTIES\[=\](parser_properties_list) | Specifies the properties of the parser. For more information, see [parser_properties](#parser_properties). <main id="notice" type='explain'><h4>Note</h4><p>The <code>PARSER_PROPERTIES</code> option for specifying parser properties is supported in OceanBase Database V4.3.5 and later, starting from V4.3.5 BP1.</p></main> |
| KEY \| INDEX                                         | Specifies a key or index for the table. If you do not specify the index name, the name of the first column that is referenced in the index definition is used as the index name. If there are duplicate index names, an underscore (_ ) and a number are appended to the index name. (For example, if an index is created based on the `c1` column and the index name is duplicated, the index name is changed to `c1_2`. ) You can use the `SHOW INDEX` statement to query the indexes on a table. |
| key_part        | Creates a function-based index. |
| index_col_name  | The name of the index column. ASC is supported but DESC is not. The default sort order is ASC. The sort order of an index is determined as follows: the first column specified in `index_col_name` is used to sort the data; for records with the same value in the first column, the next column specified in `index_col_name` is used to sort the data; this process continues. |
| expr            | A valid function-based index expression, which can be a boolean expression such as `c1=c1`. <main id="notice" type='notice'><h4>Notice</h4><p>Function-based indexes on generated columns are not supported in the current version of OceanBase Database. </p></main>              |
| ROW_FORMAT                                           | Specifies whether to enable the encoding storage format for the table. <ul><li> `redundant`: encoding storage format is not enabled.   <li> `compact`: encoding storage format is not enabled.</li>   <li> `dynamic`: encoding storage format is enabled.   <li> `compressed`: encoding storage format is enabled.</li>   <li> `default`: equivalent to `dynamic` mode.</li></ul>    |
| \[GENERATED ALWAYS\] AS (expr) \[VIRTUAL \| STORED\] | Creates a generated column. `expr` is an expression for calculating the column values. <ul><li> `VIRTUAL`: the column values are not stored. Instead, they are calculated after any `BEFORE` triggers fire when rows are read. Virtual columns do not occupy storage space.</li>   <li> `STORED`: the system evaluates and stores the column values when rows are inserted or updated. Stored columns require storage space and can be indexed.</li></ul>    |
| BLOCK_SIZE                                           | The size of microblocks in the table.  |
| lob_inrow_threshold                                  | The `INROW` threshold for `LOB` fields. When the data size of a `LOB` field exceeds this threshold, the excess part is stored in the LOB Meta table. The default value is determined by the `ob_default_lob_inrow_threshold` variable.|
| COMPRESSION                                          | The compression algorithm for the table. Valid values: <ul><li> `none`: no compression algorithm is used.   <li> `lz4_1.0`: the `lz4` compression algorithm is used.   <li> `zstd_1.0`: the `zstd` compression algorithm is used.   <li> `snappy_1.0`: the `snappy` compression algorithm is used. </ul>    |
| CHARSET \| CHARACTER SET                             | The default character set for columns in the table. For more information about supported character sets, see [Character sets](../100.basic-elements-of-mysql-mode/300.character-set-and-collation-of-mysql-mode/200.character-set-of-mysql-mode.md).    |
| COLLATE                                              | The default collation for columns in the table. For more information about supported collations, see [Collations](../100.basic-elements-of-mysql-mode/300.character-set-and-collation-of-mysql-mode/300.collation-of-mysql-mode.md).  |
| table_tablegroup                                     | The `tablegroup` to which the table belongs.   |
| AUTO_INCREMENT                                       | The initial value of the auto-increment column. OceanBase Database allows you to use auto-increment columns as the partitioning key.  |
| COMMENT                                              | The description of the table. The description is case-insensitive. |
| PCTFREE                                              | The percentage of space reserved for each macroblock.   |
| parallel_clause                                      | The parallelism of the table. <ul><li> `NOPARALLEL`: the parallelism is `1`. This is the default configuration. </li>  <li> `PARALLEL integer`: specifies the parallelism, where `integer` must be greater than or equal to `1`. </li></ul>    |
| DUPLICATE_SCOPE | The replication scope of the table. Valid values: <ul><li> `none`: indicates that the table is a normal table. This is the default value. </li><li> `cluster`: indicates that the table is a replicated table. The leader needs to replicate transactions to all full (F) and read-only (R) replicas in the current tenant. </li></ul>At present, OceanBase Database supports only replicated tables at the cluster level.|
| CHECK                                                | The CHECK constraint restricts the range of values in a column. <ul><li> If you define a `CHECK` constraint on a single column, you can write the column-level constraint to the column definition and specify a constraint name.</li>   <li> If you define a `CHECK` constraint on a table, the constraint is applied to multiple columns in the table and can precede the column definitions. When you drop a table, the `CHECK` constraints created on the table are also dropped.</li></ul> You can query the information about constraints in the following ways: <li> Use the `SHOW CREATE TABLE` statement. </li>  <li> Query the `information_schema.TABLE_CONSTRAINTS` view. </li>  <li> Query the `information_schema.CHECK_CONSTRAINTS` view.  </li> </ul>    |
| constraint_name                                      | The name of the constraint, which can contain up to 64 characters. <ul><li> Spaces are allowed at the beginning, end, and in the middle of the constraint name, but the name must be enclosed in `&quot;`.   <li> The `$` character is allowed in the constraint name.   <li> If the name of a `CHECK` constraint is a reserved word, it must be enclosed in `&quot;`, otherwise an error will be returned.   <li> The name of a `CHECK` constraint cannot be the same as that of another one in the same database.   </ul>    |
| expression                                           | The constraint expression. <ul><li> The `expression` parameter cannot be left empty.   <li> The result of `expression` cannot be of a non-boolean type.  <li> The `expression` parameter cannot contain a column that does not exist. </ul>   |
| table_column_group_option/index_column_group_option | The column storage option for a table or an index. The options are described as follows:<ul><li><code>WITH COLUMN GROUP(all columns, each column)</code>: specifies to create a rowstore table or index with redundant columnstore indexes.</li><li><code>WITH COLUMN GROUP(all columns)</code>: specifies to create a rowstore table or index.</li><li><code>WITH COLUMN GROUP(each column)</code>: specifies to create a columnstore table or index.</li></ul>|
| SKIP_INDEX | The Skip Index attribute of a column. The valid values are described as follows:<ul><li><code>MIN_MAX</code>: the most general aggregation data type of Skip Index. It stores the maximum value, minimum value, and null count of indexed columns in the index node granularity. This type of data can accelerate the pushdown of filters and the pushdown of `MIN/MAX` aggregation. </li><li> <code>SUM</code>: it is used to accelerate the pushdown of the `SUM` aggregation for numeric data. </li></ul>  <main id="notice" type='notice'><h4>Notice</h4><p><ul><li>Columns of the JSON or spatial data type are not allowed to have Skip Indexes created. </li><li>Generated columns are not allowed to have Skip Indexes created. </li></ul></p></main>|
| index_json_clause | The clause for creating multi-valued indexes. For more information about multi-valued indexes, see the **Create a multi-valued index** section in [Create an index](../../../../300.database-object-management/100.manage-object-of-mysql-mode/500.manage-indexes-of-mysql-mode/200.create-an-index-of-mysql-mode.md).<main id="notice" type='notice'><h4>Notice</h4><p>Multi-valued indexes are currently in the experimental stage. It is recommended that you do not use them in the production environment to avoid impacting the stability of the system.</p></main>|
| json_column_name  | The name of a column of the `JSON` data type in a table. |
| idx_json_name     | The name of the multi-valued index to be created. |
| json_field_name   | The name of the field on which an index is to be created in a JSON column. |
| TABLE_MODE        | The major compaction trigger threshold and strategy, which control the major compaction behavior after data is compacted. For more information about the parameter, see [table_mode_value](#table_mode_value). |
| auto_increment_cache_size | The number of cached auto-increment values. The default value is 0, which indicates that no configuration is made and the tenant-level parameter [auto_increment_cache_size](../../../../800.configuration-items-and-system-variables/200.system-variable/300.global-system-variable/100.auto_increment_cache_size-global.md) is used as the cache size for the auto-increment column. |
| READ {ONLY \| WRITE} | The read/write permission on the table. The options are described as follows:<ul><li>`READ ONLY`: the table can only be read and cannot be modified or deleted. <main id="notice" type='notice'><h4>Notice</h4><p>The <code>READ ONLY</code> parameter at the table level does not take effect for users with the <code>SUPER</code> privilege, but only for regular users.</p></main></li><li>`READ WRITE`: the default value, which indicates that the table can be read, modified, and deleted.</li></ul>|
| ORGANIZATION [=] {INDEX \| HEAP} | The organization mode of the table. The valid values are described as follows:<ul><li>`INDEX`: indicates an indexed-organized table.</li><li>`HEAP`: indicates a heap-organized table.</li></ul> If you do not specify the `ORGANIZATION` option, the value of the [default_table_organization](../../../../800.configuration-items-and-system-variables/100.system-configuration-items/400.tenant-level-configuration-items/2150.default_table_organization.md) parameter specifies the default organization mode.<main id="notice" type='explain'><h4>Note</h4><p>The <code>ORGANIZATION</code> option is supported in the <code>CREATE TABLE</code> statement in OceanBase Database V4.3.5 and later, starting from V4.3.5 BP1.</p></main>|
| PARTITION BY RANGE \[COLUMNS\]\(\[column_name_list\]\) \[SIZE\('size_value'\)\] | Specifies to create an automatically partitioned table. For more information, see the syntax for creating automatically partitioned tables in [Automatic partition splitting](../../../../300.database-object-management/100.manage-object-of-mysql-mode/300.manage-partitions-of-mysql-mode/250.partition-splitting-of-mysql-mode/300.automatic-partition-splitting-of-mysql-mode.md). |
| SEMISTRUCT_ENCODING_TYPE | The semi-structured data encoding type. The valid values are described as follows:<ul><li>`''`, which indicates that semi-structured data encoding is disabled. </li><li>`'encoding'`, which indicates that semi-structured data encoding is enabled.</li></ul>For more information about how to use semi-structured data encoding, see [Use semi-structured data encoding](../100.basic-elements-of-mysql-mode/100.data-type-of-mysql-mode/800.json-formatted-data-type-of-mysql-mode/200.create-a-json-value-of-mysql-mode.md).|

### table_mode_value

<main id="notice" type='explain'>
  <h4>Note</h4>
  <p>In the following <code>TABLE_MODE</code> modes, except for <code>NORMAL</code>, all other modes indicate <code>QUEUING</code> tables. A <code>QUEUING</code> table is the most basic type of table. The modes listed later (excluding the <code>NORMAL</code> mode) indicate tables that use a more aggressive merge strategy.</p>
</main>

* `NORMAL`: Normal. This is the default mode. In this mode, the probability that a major compaction is triggered after a minor compaction is extremely low.
* `QUEUING`: Queuing. In this mode, the probability that a major compaction is triggered after a minor compaction is low.
* `MODERATE`: Moderate. In this mode, the probability that a major compaction is triggered after a minor compaction is moderate.
* `SUPER`: Super. In this mode, the probability that a major compaction is triggered after a minor compaction is high.
* `EXTREME`: Extreme. In this mode, the probability that a major compaction is triggered after a minor compaction is relatively high.

For more information about major compactions, see [Adaptive compaction](../../../../200.system-management/500.manage-data-storage/200.merge-management/320.adaptive-compaction.md).

### tokenizer_option

* `SPACE`: The default value. Splits words based on spaces. You can specify the following parameters:

  |        Parameter      | Value range |
  |------------------|---------|
  | min_token_size   | [1, 16] |
  | max_token_size   | [10, 84]|

* `NGRAM`: Splits words based on N-Gram (Chinese). You can specify the following parameter:

  |        Parameter      | Value range |
  |------------------|---------|
  | ngram_token_size | [1, 10] |

* `BENG`: Splits words based on the Beng (Basic English) language. You can specify the following parameters:

  |        Parameter      | Value range |
  |------------------|---------|
  | min_token_size   | [1, 16] |
  | max_token_size   | [10, 84]|

* `IK`: Splits words based on the IK Chinese language. Only the `utf-8` character set is supported. You can specify the following parameter:

  |        Parameter      | Value range |
  |------------------|---------|
  | ik_mode          | <ul><li>`smart`</li><li>`max_word`</li></ul>|

  <main id="notice" type='explain'>
    <h4>Note</h4>
    <p>The <code>IK</code> tokenizer is supported in OceanBase Database V4.3.5 and later, starting from V4.3.5 BP1.</p>
  </main>

### parser_properties

* `min_token_size`: the minimum word length. The default value is 3, and the value range is 1 to 16.
* `max_token_size`: the maximum word length. The default value is 84, and the value range is 10 to 84.
* `ngram_token_size`: the word length for the `NGRAM` tokenizer. Only the `NGRAM` tokenizer is valid. The default value is 2, and the value range is 1 to 10.
* `ik_mode`: the tokenization mode for the `IK` tokenizer. Valid values:

  * `smart`: the default value. Uses dictionary words to improve tokenization accuracy. Prioritizes dictionary word boundaries, potentially reducing unnecessary expansions.
  * `max_word`: identifies dictionary-defined words without affecting maximum expansion. Even with defined dictionary words, `max_word` mode still attempts to split the text into more words.

**Here is an example:**

```sql
CREATE TABLE tbl1 (col1 VARCHAR(200), col2 TEXT,
    FULLTEXT INDEX fidx(col2) WITH PARSER ik PARSER_PROPERTIES=(ik_mode='max_word'));
```

## Examples

* Create a database table.

  ```sql
  obclient> CREATE TABLE tbl1 (c1 INT PRIMARY KEY, c2 VARCHAR(50));
  Query OK, 0 rows affected
  ```

* Create a table with indexes.

  ```sql
  obclient> CREATE TABLE tbl2 (c1 INT PRIMARY KEY, c2 INT, c3 INT, INDEX i1 (c2));
  Query OK, 0 rows affected
  ```

* Create a table named `tbl3` with a function index.

  ```shell
  obclient> CREATE TABLE tbl3 (c1 INT, c2 INT, INDEX i1 ((c1+1)), UNIQUE KEY ((c1+c2)));
  Query OK, 0 rows affected

  obclient> SHOW CREATE TABLE tbl3;
  +-------+----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
  | Table | Create Table                                                                                                                                                                                                                                                                                                                                                                     |
  +-------+----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
  | tbl3    | CREATE TABLE `tbl3` (
  `c1` int(11) DEFAULT NULL,
  `c2` int(11) DEFAULT NULL,
  UNIQUE KEY `functional_index` ((`c1` + `c2`)) BLOCK_SIZE 16384 LOCAL,
  KEY `i1` ((`c1` + 1)) BLOCK_SIZE 16384 LOCAL
  ) DEFAULT CHARSET = utf8mb4 ROW_FORMAT = DYNAMIC COMPRESSION = 'zstd_1.3.8' REPLICA_NUM = 2 BLOCK_SIZE = 16384 USE_BLOOM_FILTER = FALSE TABLET_SIZE = 134217728 PCTFREE = 0 |
  +-------+----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
  ```

* Create a hash-partitioned table with eight partitions.

  ```sql
  obclient> CREATE TABLE tbl4 (c1 INT PRIMARY KEY, c2 INT) PARTITION BY HASH(c1) PARTITIONS 8;
  Query OK, 0 rows affected
  ```

* Create a table that has a RANGE partition and a KEY subpartition.

  ```sql
  obclient> CREATE TABLE tbl5 (c1 INT, c2 INT, c3 INT) PARTITION BY RANGE(c1)
       SUBPARTITION BY KEY(c2, c3) SUBPARTITIONS 5
       (PARTITION p0 VALUES LESS THAN(0), PARTITION p1 VALUES LESS THAN(100));
  Query OK, 0 rows affected
  ```

* Create a table that has one column in the `gbk` character set and another column in the `utf8` character set.

  ```sql
  obclient> CREATE TABLE tbl6 (c1 VARCHAR(10),
                   c2 VARCHAR(10) CHARSET GBK COLLATE gbk_bin)
               DEFAULT CHARSET utf8 COLLATE utf8mb4_general_ci;
  Query OK, 0 rows affected
  ```

* Enable encoding, specify the `zstd` compression algorithm, and set the reserved space of macroblocks to 5%.

  ```sql
  obclient> CREATE TABLE tbl7 (c1 INT, c2 INT, c3 VARCHAR(64))
        COMPRESSION 'zstd_1.0'
        ROW_FORMAT DYNAMIC
        PCTFREE 5;
  Query OK, 0 rows affected
  ```

* Create a table named `tbl8` and set the parallelism to three.

  ```sql
  obclient> CREATE TABLE tbl8(c1 INT PRIMARY KEY, c2 INT) PARALLEL 3;
  Query OK, 0 rows affected
  ```

* Use an auto-increment column as the partitioning key.

  ```sql
  obclient> CREATE TABLE tbl9(inv_id BIGINT NOT NULL AUTO_INCREMENT,c1 BIGINT,
  PRIMARY KEY (inv_id) ) PARTITION BY HASH(inv_id) PARTITIONS 8;
  Query OK, 0 rows affected
  ```

* Specify the foreign key for table `ref_t2`. When a key value in the parent table is affected by an `UPDATE` operation and matches a row in a child table, the key value is set to NULL.

  ```shell
  obclient> CREATE TABLE ref_t1(c1 INT PRIMARY KEY, C2 INT);
  Query OK, 0 rows affected

  obclient> CREATE TABLE ref_t2(c1 INT PRIMARY KEY, C2 INT,FOREIGN KEY(c2) REFERENCES ref_t1(c1) ON UPDATE SET NULL);
  Query OK, 0 rows affected
  ```

* Create a table named `tbl10` with a `CHECK` constraint and view the constraint information.

  ```sql
  obclient> CREATE TABLE tbl10 (col1 INT, col2 INT, col3 INT, CONSTRAINT equal_check1 CHECK(col1 = col3 * 2));
  Query OK, 0 rows affected

  obclient> SHOW CREATE TABLE tbl10;
  +-------+------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
  | Table | Create Table                                                                                                                                                                                                                                                                                                                                               |
  +-------+------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
  | tbl10  | CREATE TABLE `tbl10` (
    `col1` int(11) DEFAULT NULL,
    `col2` int(11) DEFAULT NULL,
    `col3` int(11) DEFAULT NULL,
    CONSTRAINT `equal_check1` CHECK ((`col1` = (`col3` * 2)))
  ) DEFAULT CHARSET = utf8mb4 ROW_FORMAT = DYNAMIC COMPRESSION = 'zstd_1.3.8' REPLICA_NUM = 1 BLOCK_SIZE = 16384 USE_BLOOM_FILTER = FALSE TABLET_SIZE = 134217728 PCTFREE = 0 |
  +-------+------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
  1 row in set
  ```

* Create a table named `tbl11` based on the `tbl10` table and view the table information.

  ```sql
  obclient> CREATE TABLE tbl11 LIKE tbl10;
  Query OK, 0 rows affected

  obclient> SHOW CREATE TABLE tbl11;
  +-------+-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
  | Table | Create Table                                                                                                                                                                                                                                                                                                                                                                |
  +-------+-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
  | tbl11  | CREATE TABLE `tbl11` (
    `col1` int(11) DEFAULT NULL,
    `col2` int(11) DEFAULT NULL,
    `col3` int(11) DEFAULT NULL,
    CONSTRAINT `tbl11_OBCHECK_1650793233327894` CHECK ((`col1` = (`col3` * 2)))
  ) DEFAULT CHARSET = utf8mb4 ROW_FORMAT = DYNAMIC COMPRESSION = 'zstd_1.3.8' REPLICA_NUM = 1 BLOCK_SIZE = 16384 USE_BLOOM_FILTER = FALSE TABLET_SIZE = 134217728 PCTFREE = 0 |
  +-------+-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
  1 row in set
  ```

* Create a replicated table named `dup_t1` at the cluster level.

  1. Log in to the sys tenant and create a resource unit.

     ```shell
     obclient> CREATE RESOURCE UNIT IF NOT EXISTS 2c5g MAX_CPU 2, MEMORY_SIZE '5G';
     Query OK, 0 rows affected
     ```

  2. Create a resource pool with a unit number of 2.

     ```shell
     obclient> CREATE RESOURCE POOL tenant_pool UNIT = '2c5g', UNIT_NUM = 2, ZONE_LIST = ('z1', 'z2', 'z3');
     Query OK, 0 rows affected
     ```

  3. Create a user tenant named `obmysql` and specify the locality distribution.

     ```shell
     obclient> CREATE TENANT obmysql resource_pool_list=('tenant_pool'), LOCALITY = "F@z1, F@z2, R@z3", PRIMARY_ZONE = "z1" SET ob_tcp_invited_nodes='%';
     Query OK, 0 rows affected
     ```

  4. Log in to the `obmysql` tenant created in step 3 and create a replicated table.

     ```shell
     obclient> USE test;
     Database changed
     obclient> CREATE TABLE dup_t1(c1 INT) DUPLICATE_SCOPE = 'cluster';
     Query OK, 0 rows affected
     ```

  5. (Optional) View the broadcast log stream information. The replicated table is created in this log stream.

     ```shell
     obclient> SELECT * FROM oceanbase.DBA_OB_LS WHERE FLAG LIKE "%DUPLICATE%";
     +-------+--------+--------------+---------------+-------------+---------------------+----------+---------------------+---------------------+-----------+
     | LS_ID | STATUS | PRIMARY_ZONE | UNIT_GROUP_ID | LS_GROUP_ID | CREATE_SCN          | DROP_SCN | SYNC_SCN            | READABLE_SCN        | FLAG      |
     +-------+--------+--------------+---------------+-------------+---------------------+----------+---------------------+---------------------+-----------+
     |  1003 | NORMAL | z1;z2        |             0 |           0 | 1683267390195713284 |     NULL | 1683337744205408139 | 1683337744205408139 | DUPLICATE |
     +-------+--------+--------------+---------------+-------------+---------------------+----------+---------------------+---------------------+-----------+
     1 row in set

  6. (Optional) View the replica distribution of the replicated table in the sys tenant. The `REPLICA_TYPE` field indicates the replica type.

     ```shell
     obclient> SELECT * FROM oceanbase.CDB_OB_TABLE_LOCATIONS WHERE TABLE_NAME = "dup_t1";
     +-----------+---------------+------------+----------+------------+----------------+-------------------+------------+---------------+-----------+-------+------+----------------+----------+----------+--------------+-----------------+
     | TENANT_ID | DATABASE_NAME | TABLE_NAME | TABLE_ID | TABLE_TYPE | PARTITION_NAME | SUBPARTITION_NAME | INDEX_NAME | DATA_TABLE_ID | TABLET_ID | LS_ID | ZONE | SVR_IP         | SVR_PORT | ROLE     | REPLICA_TYPE | DUPLICATE_SCOPE |
     +-----------+---------------+------------+----------+------------+----------------+-------------------+------------+---------------+-----------+-------+------+----------------+----------+----------+--------------+-----------------+
     |      1002 | test          | dup_t1     |   500002 | USER TABLE | NULL           | NULL              | NULL       |          NULL |    200001 |  1003 | z1   | 11.xxx.xxx.xxx |    36125 | LEADER   | FULL         | CLUSTER         |
     |      1002 | test          | dup_t1     |   500002 | USER TABLE | NULL           | NULL              | NULL       |          NULL |    200001 |  1003 | z1   | 11.xxx.xxx.xxx  |    36124 | FOLLOWER | READONLY     | CLUSTER         |
     |      1002 | test          | dup_t1     |   500002 | USER TABLE | NULL           | NULL              | NULL       |          NULL |    200001 |  1003 | z2   | 11.xxx.xxx.xxx |    36127 | FOLLOWER | FULL         | CLUSTER         |
     |      1002 | test          | dup_t1     |   500002 | USER TABLE | NULL           | NULL              | NULL       |          NULL |    200001 |  1003 | z2   | 11.xxx.xxx.xxx |    36126 | FOLLOWER | READONLY     | CLUSTER         |
     |      1002 | test          | dup_t1     |   500002 | USER TABLE | NULL           | NULL              | NULL       |          NULL |    200001 |  1003 | z3   | 11.xxx.xxx.xxx |    36128 | FOLLOWER | READONLY     | CLUSTER         |
     |      1002 | test          | dup_t1     |   500002 | USER TABLE | NULL           | NULL              | NULL       |          NULL |    200001 |  1003 | z3   | 11.xxx.xxx.xxx  |    36129 | FOLLOWER | READONLY     | CLUSTER         |
     +-----------+---------------+------------+----------+------------+----------------+-------------------+------------+---------------+-----------+-------+------+----------------+----------+----------+--------------+-----------------+
     6 rows in set
     ```

  7. Insert data into and read data from the replicated table as you do with a normal table. For a read request, if a proxy is used, the read request may be routed to any OBServer node. If direct connection to an OBServer node is used, the read request is executed on the OBServer node that has a readable replica, provided that the replica is readable.

     ```shell
     obclient> INSERT INTO dup_t1 VALUES(1);
     Query OK, 1 row affected

     obclient> SELECT * FROM dup_t1;
     +------+
     | c1   |
     +------+
     |    1 |
     +------+
     1 row in set
     ```

* Create a columnstore table named `tbl1_cg`.

  ```sql
  CREATE TABLE tbl1_cg (col1 INT PRIMARY KEY, col2 VARCHAR(50)) WITH COLUMN GROUP(each column);
  ```

* Create a table with a columnstore index.

  ```sql
  CREATE TABLE tbl2_cg (col1 INT PRIMARY KEY, col2 INT, col3 INT, INDEX i1 (col2) WITH COLUMN GROUP(each column));
  ```

* Create a columnstore table with a columnstore index.

  ```sql
  CREATE TABLE tbl3_cg (col1 INT PRIMARY KEY, col2 INT, col3 INT, INDEX i1 (col2) WITH COLUMN GROUP(each column)) WITH COLUMN GROUP(each column);
  ```

* Specify the Skip Index attribute for a column when the table is created.

  ```sql
  CREATE TABLE test_skidx(
    col1 INT SKIP_INDEX(MIN_MAX, SUM),
    col2 FLOAT SKIP_INDEX(MIN_MAX),
    col3 VARCHAR(1024) SKIP_INDEX(MIN_MAX),
    col4 CHAR(10)
  );
  ```

* Create a table named `tbl1`. The table has an integer column named `col1`. The operation is executed with five parallelism. The data content of the new table `tbl1` comes from the query result of the `tbl2` table.

  ```sql
  CREATE /*+ parallel(5) */ TABLE tbl1 (col1 INT) AS SELECT col1 FROM tbl2;
  ```

## References

* [Create a table](../../../../300.database-object-management/100.manage-object-of-mysql-mode/200.manage-tables-of-mysql-mode/200.create-a-table-for-mysql-tenant-of-mysql-mode.md)
* [Modify a table](../../../../300.database-object-management/100.manage-object-of-mysql-mode/200.manage-tables-of-mysql-mode/600.change-table-of-mysql-mode.md)
