| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |
|machine-translation| en          |
|slug|drop-sensitive-rule-mysql-mode|

# DROP SENSITIVE RULE

## Purpose

This statement is used to drop a sensitive data protection rule.

## Privilege requirements

To execute the `DROP SENSITIVE RULE` statement, the current user must have the `CREATE SENSITIVE RULE` privilege. For more information about OceanBase Database privileges, see [Privilege classification in MySQL mode](../../../../../600.manage/500.security-and-permissions/300.access-control/200.user-and-permission/200.permission-of-mysql-mode/100.permission-classification-of-mysql.md).

## Syntax

```sql
DROP SENSITIVE RULE rule_name
```

## Parameters

The following table describes the parameters.

| Parameter | Data type | Default value | Description |
|--|--|--|--|
| rule_name | VARCHAR |  | The name of the sensitive data protection rule, which must be unique within the tenant. |

The following table describes the parameters.

* The rule takes effect immediately after it is dropped.

The following table describes the parameters.

* Only one rule can be dropped at a time.

## Examples

Here is an example:

```sql
-- Drop the sensitive data protection rule named r1.
DROP SENSITIVE RULE r1;
```

# References

* [CREATE SENSITIVE RULE](2470.create-sensitive-rule-of-mysql-mode.md)
* [ALTER SENSITIVE RULE](1350.alter-sensitive-rule-of-mysql-mode.md)