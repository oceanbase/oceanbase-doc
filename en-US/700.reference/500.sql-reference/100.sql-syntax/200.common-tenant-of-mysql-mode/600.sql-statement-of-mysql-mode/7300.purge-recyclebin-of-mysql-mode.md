| Description   |                 |
|---------------|-----------------|
| description   | The PURGE RECYCLEBIN statement is used to clear all database objects in the recycle bin. |
| keywords      |                 |
| keywords      | PURGE RECYCLEBIN, clear recycle bin |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |
|slug|purge-recyclebin-mysql-mode|
|machine-translation|                 |

# PURGE RECYCLEBIN

## Purpose

You can use this statement to purge the recycle bin.

  <main id="notice" type='notice'>
    <h4>Notice</h4>
    <p><code>PURGE RECYCLEBIN</code> clears all database objects from the recycle bin. You can execute this statement only as the root user. Proceed with caution. </p>
  </main>

## Syntax

```sql
PURGE RECYCLEBIN
```

## Examples

Purge the recycle bin:

```sql
obclient> PURGE RECYCLEBIN;
```
