| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |

# ALTER TABLE

## Description

This statement is used to modify the structure of an existing table, such as renaming the table and its attributes, adding columns, modifying the columns and their attributes, and dropping columns.

## Syntax

```sql
alter_table_stmt:
    ALTER TABLE table_name alter_table_action_list;

alter_table_action_list:
    alter_table_action [, alter_table_action ...]

alter_table_action:
      ADD [COLUMN] column_definition  
          [ opt_position_column ]
    | ADD [COLUMN] (column_definition_list)
    | ADD [CONSTRAINT [constraint_name]] UNIQUE {INDEX | KEY}
          [index_name] index_desc
    | ADD [CONSTRAINT [constraint_name]] FOREIGN KEY
          [index_name] index_desc
          REFERENCES reference_definition
          [match_action][opt_reference_option_list]
    | ADD PRIMARY KEY (column_name)
    | ADD CONSTRAINT [constraint_name] CHECK (expression)[[NOT] ENFORCED]
    | ADD PARTITION (range_partition_list)
    | ADD {INDEX | KEY} [index_name] [index_type] (key_part,...) [index_option_list][index_column_group_option]
    | ADD FULLTEXT [INDEX | KEY] [index_name](column_name, [column_name ...]) [WITH PARSER tokenizer_option] [PARSER_PROPERTIES[=](parser_properties_list)] [LOCAL]
    | ADD COLUMN GROUP([all columns, ]each column) [DELAYED]
    | ALTER [COLUMN] column_name {
          SET DEFAULT const_value
        | DROP DEFAULT
      }
    | ALTER {CHECK | CONSTRAINT} symbol [NOT] ENFORCED
    | ALTER INDEX index_name
          [VISIBLE | INVISIBLE]
    | CHANGE [COLUMN] column_name column_definition
    | DROP [COLUMN] column_name [, DROP COLUMN column_name ...]
    | DROP {PARTITION | SUBPARTITION} partition_name_list
    | DROP TABLEGROUP
    | DROP FOREIGN KEY fk_name
    | DROP PRIMARY KEY [, ADD PRIMARY KEY (column)]
    | DROP CHECK constraint_name
    | DROP COLUMN GROUP([all columns, ]each column)
    | MODIFY [COLUMN] column_definition
    | RENAME COLUMN old_col_name TO new_col_name
    | RENAME [TO] table_name
    | RENAME {INDEX | KEY} old_index_name TO new_index_name
    | [SET] table_option_list
    | TRUNCATE {PARTITION | SUBPARTITION} partition_name_list
    | EXCHANGE PARTITION partition_name WITH TABLE non_partition_table_name WITHOUT VALIDATION
    | REORGANIZE PARTITION partition_name INTO (split_range_partition_list)
    | PARTITION BY {RANGE [COLUMNS]([column_name_list]) [SIZE('size_value')] [range_partition_list]
                   | partition_option}
    | MODIFY PARTITION partition_name ADD SUBPARTITION (add_subpartition_option)
    | FORCE

column_definition_list:
    column_definition [, column_definition ...]

column_definition:
    column_name data_type
    [DEFAULT const_value] [AUTO_INCREMENT]
    [NULL | NOT NULL] [[PRIMARY] KEY] [UNIQUE [KEY]] [COMMENT string_value] [SKIP_INDEX(skip_index_option_list)] [ opt_position_column ]

skip_index_option_list:
    skip_index_option [,skip_index_option ...]

skip_index_option:
    MIN_MAX
    | SUM

opt_position_column:
    FIRST | BEFORE | AFTER column_name

index_desc:
   (column_desc_list) [index_type] [index_option_list]

match_action:
   MATCH {SIMPLE | FULL | PARTIAL}

opt_reference_option_list:
   reference_option [,reference_option ...]

reference_option:
   ON {DELETE | UPDATE} {RESTRICT | CASCADE | SET NULL | NO ACTION | SET DEFAULT}

column_desc_list:
    column_desc [, column_desc ...]

column_desc:
     column_name [(length)] [ASC | DESC]

key_part:
    {index_col_name [(length)] | (expr)} [ASC | DESC]

index_type:
    USING BTREE

index_option_list:
    index_option [ index_option ...]

index_option:
     [GLOBAL | LOCAL]
    | block_size
    | compression
    | STORING(column_name_list)
    | COMMENT string_value

index_column_group_option:
    WITH COLUMN GROUP([all columns, ]each column)

tokenizer_option:
    SPACE
    | NGRAM
    | BENG
    | IK
    | NGRAM2

parser_properties_list:
    parser_properties, [parser_properties]

parser_properties:
    min_token_size = int_value
    | max_token_size = int_value
    | ngram_token_size = int_value
    | ik_mode = 'char_value'
    | min_ngram_size = int_value
    | max_ngram_size = int_value

table_option_list:
    table_option [ table_option ...]

table_option:
      TABLEGROUP = tablegroup_name
    | block_size
    | lob_inrow_threshold [=] num
    | compression
    | AUTO_INCREMENT [=] INT_VALUE
    | COMMENT string_value
    | parallel_clause
    | TABLE_MODE [=] 'table_mode_value'
    | AUTO_INCREMENT_CACHE_SIZE [=] INT_VALUE
    | READ {ONLY | WRITE}
    | DUPLICATE_SCOPE= 'none | cluster'
    | enable_macro_block_bloom_filter [=] {True | False}
    | DYNAMIC_PARTITION_POLICY [=] (dynamic_partition_policy_list)
    | SEMISTRUCT_ENCODING_TYPE [=] 'encoding'

parallel_clause:
    {NOPARALLEL | PARALLEL integer}

table_mode_value:
    NORMAL
    | QUEUING
    | MODERATE
    | SUPER
    | EXTREME

dynamic_partition_policy_list:
    dynamic_partition_policy_option [, dynamic_partition_policy_option ...]

dynamic_partition_policy_option:
    ENABLE = {true | false}
    | PRECREATE_TIME = {'-1' | '0' | 'n {hour | day | week | month | year}'}
    | EXPIRE_TIME = {'-1' | '0' | 'n {hour | day | week | month | year}'}

partition_option:
      PARTITION BY HASH(expression)
      [subpartition_option] PARTITIONS partition_count
    | PARTITION BY KEY([column_name_list])
      [subpartition_option] PARTITIONS partition_count
    | PARTITION BY RANGE {(expression) | COLUMNS (column_name_list)}
      [subpartition_option] (range_partition_list)

subpartition_option:
      SUBPARTITION BY HASH(expression)
      SUBPARTITIONS subpartition_count
    | SUBPARTITION BY KEY(column_name_list)
      SUBPARTITIONS subpartition_count
    | SUBPARTITION BY RANGE {(expression) | COLUMNS (column_name_list)}
      (range_subpartition_list)

split_range_partition_list:
    PARTITION split_partition_name VALUES LESS THAN (value)
    [, PARTITION split_partition_name VALUES LESS THAN (value) ...]
     , PARTITION split_partition_name VALUES LESS THAN (source_value)

range_partition_list:
    range_partition [, range_partition ...]

range_partition:
    PARTITION partition_name
    VALUES LESS THAN {(expression_list) | MAXVALUE}

range_subpartition_list:
    range_subpartition [, range_subpartition ...]

range_subpartition:
    SUBPARTITION subpartition_name
    VALUES LESS THAN {(expression_list) | MAXVALUE}

expression_list:
    expression [, expression ...]

column_name_list:
    column_name [, column_name ...]

partition_name_list:
    partition_name [, partition_name ...]

partition_count | subpartition_count:
    INT_VALUE

add_subpartition_option:
    {SUBPARTITION subpartition_name VALUES LESS THAN range_partition_expr
        [, SUBPARTITION subpartition_name VALUES LESS THAN range_partition_expr] ...}
    | {SUBPARTITION subpartition_name VALUES IN list_partition_expr
        [, SUBPARTITION subpartition_name VALUES IN list_partition_expr] ...}
```

## Parameters

|                           **Parameter**                           |        **Description**             |
|------------------------------------------------------------|---------------------------------------------------------------------|
| ADD \[COLUMN\]                                             | Adds a column, including generated columns.     |
| \[FIRST  \| BEFORE \| AFTER column_name\]                  | Adds the new column as the first column of the table or before/after the `column_name` column. At present, OceanBase Database supports specifying the position of a column only in the `ADD COLUMN` syntax.     |
| CHANGE \[COLUMN\]                                          | Changes the name and definition of a column. It supports changing the length of specific character data types such as `VARCHAR`, `VARBINARY`, and `CHAR`.    |
| MODIFY \[COLUMN\]                                          | Modifies column attributes. |
| ALTER \[COLUMN\] {SET DEFAULT const_value \| DROP DEFAULT} | Modifies the default value of a column. |
| DROP \[COLUMN\]                                            | Drops a column.<main id="notice" type='notice'><h4>Notice</h4><p>You cannot drop a primary key column. </p></main>|
| ADD FOREIGN KEY                                            | Adds a foreign key. If no name is specified for the foreign key, it is named by using the table name, `OBFK`, and the creation time. For example, the name of a foreign key created for the `t1` table on August 1, 2021, at 00:00:00 is `t1_OBFK_1627747200000000`. The foreign key allows cross-referencing of data across tables. The result of an `UPDATE` or `DELETE` operation depends on the reference operation specified by the `ON UPDATE` and `ON DELETE` clauses:<ul><li>`CASCADE`: indicates that the corresponding row in the parent table is deleted or updated, and the system automatically deletes or updates the matching row in the child table. </li><li>`SET NULL`: indicates that the corresponding row in the parent table is deleted or updated, and the foreign key column in the child table is set to `NULL`. </li><li>`RESTRICT`: indicates that the deletion or update operation is rejected. </li><li>`NO ACTION`: indicates that the check is delayed. </li></ul> The `SET DEFAULT` operation is also supported. |
| ADD PRIMARY KEY                                            | Adds a primary key. You can specify one or more columns as the primary key. If multiple columns are specified, they constitute a composite primary key.               |
| ADD FULLTEXT \[INDEX \| KEY\]                              | Adds a full-text index. For more information, see the [Create an index](../../../../300.database-object-management/100.manage-object-of-mysql-mode/500.manage-indexes-of-mysql-mode/200.create-an-index-of-mysql-mode.md) topic, specifically the section about creating full-text indexes. |
| WITH PARSER tokenizer_option                 | Optional. Specifies the tokenizer of the full-text index. For more information, see [tokenizer_option](#tokenizer_option). |
| PARSER_PROPERTIES\[=\](parser_properties_list) | Optional. Specifies the properties of the tokenizer. For more information, see [parser_properties](#parser_properties). <main id="notice" type='explain'><h4>Note</h4><p>For OceanBase Database V4.3.5, the specification of tokenizer properties ( <code>PARSER_PROPERTIES</code> ) is supported from V4.3.5 BP1. </p></main>|
| ALTER INDEX                                                | Modifies the visibility of an index. If the index status is `INVISIBLE`, the SQL optimizer will not select this index.    |
| key_part        | Creates a (function-based) index. |
| index_col_name  | Specifies the name of the index column. ASC is supported after each column name, but DESC is not supported. By default, the ascending order is used. The index is created by first sorting the values of the first column specified in `index_col_name`; for records with the same value in the first column, the values in the next column are used for sorting, and so on. |
| expr            | Specifies a valid function-based index expression, which can be a boolean expression such as `c1=c1`. <br>**Note** In the current version, OceanBase Database does not allow the creation of function-based indexes on generated columns.             |
| ADD \[PARTITION\]                                          | Adds a partition to a partitioned table.  |
| DROP {PARTITION \| SUBPARTITION}                           | Drops a partition: <ul><li> `PARTITION`: drops a partition of RANGE or LIST type. All subpartitions in the specified partition are also dropped. The partition definition and data are dropped, and the indexes on the partition are maintained. </li>   <li> `SUBPARTITION`: drops a subpartition of `*-RANGE` or `*-LIST` type. The specified subpartition definition and data are dropped, and the indexes on the subpartition are maintained. </li></ul>    The names of multiple partitions to be dropped are separated by commas.<br> **Note**  When dropping a partition, make sure that no active transactions or queries are on the partition. Otherwise, the SQL statement may return an error or some exceptions.   |
| TRUNCATE {PARTITION \| SUBPARTITION}                       | Truncates partitions: <ul><li> `PARTITION`: truncates a partition of RANGE or LIST type. All data in the specified partition are truncated. If subpartitions exist in the specified partition, all data in the subpartitions are also truncated. The partition definition and data are dropped, and the indexes on the partition are maintained. </li>   <li> `SUBPARTITION`: truncates a subpartition of `*-RANGE` or `*-LIST` type. All data in the specified subpartition are truncated. The subpartition definition and data are dropped, and the indexes on the subpartition are maintained. </li> </ul>   The names of multiple partitions to be truncated are separated by commas.<br> **Note**  When truncating partitions, make sure that no active transactions or queries are on the partitions. Otherwise, the SQL statement may return an error or some exceptions. |
| RENAME COLUMN old_col_name TO new_col_name| Renames a column. Only the column name is changed, not the column definition.   <main id="notice" type='notice'><h4>Notice</h4> <p><ul><li>If the target name exists in the table, an error will be returned when the operation is executed. </li><li>If a cyclic renaming occurs, the statement can still be executed normally. For example, <code>ALTER TABLE t1 RENAME COLUMN a to b, RENAME COLUMN b to a;</code> </li><li>If an indexed or constrained column is renamed, the <code>RENAME COLUMN</code> statement can still be executed normally, and the index definition and constraint will be automatically updated. </li><li>Specifying both <code>RENAME COLUMN</code> and <code>ADD PARTITION</code> or <code>ALTER COLUMN</code> in the same <code>ALTER TABLE</code> statement is not supported. </li></ul></p></main>|
| RENAME \[TO\] table_name                                   | Renames a table. <main id="notice" type='notice'><h4>Notice</h4><p>During a <code>RENAME TABLE</code> operation, table locking and read/write protection are implemented, which increases the operation time. To avoid impacting other users performing DDL operations, we recommend that you do not perform <code>RENAME TABLE</code> operations in batches. </p></main>|
| RENAME {INDEX \| KEY}                                      | Renames an index or a key.   |
| DROP \[TABLEGROUP\]                                        | Drops a table group.  |
| DROP \[FOREIGN KEY\]                                       | Drops a foreign key.  |
| DROP \[PRIMARY KEY\]                                       | Drops a primary key.<main id="notice" type='explain'><h4>Note</h4><p>In MySQL mode, the following cases are not allowed to drop a primary key:<ul><li>The table is a parent table that contains foreign key information. </li> <li>The table is a child table, but the primary key column contains a referenced foreign key column. </li> </ul> </p>|
| \[SET\] table_option                                       | Sets table-level attributes. The following parameters are supported: <ul><li> `REPLICA_NUM`: specifies the number of replicas of the table. This parameter is temporarily not supported. </li>   <li> `tablegroup_name`: specifies the name of the table group to which the table belongs. </li>   <li> `BLOCK_SIZE`: specifies the size of microblocks in the table. The default value is `16384`, which is 16 KB. The value range is \[1024,1048576\]. </li>  <li> `lob_inrow_threshold`: specifies the threshold for storing LOB data in-row. When the size of LOB data exceeds this threshold, the data is stored out-of-row in the LOB meta table. The default value is 4 KB. </li>   <li> `COMPRESSION`: specifies the compression mode of the table. The default value is `None`, which specifies not to compress the table. </li>   <li> `AUTO_INCREMENT`: specifies the next value of the auto-increment column. The value can be increased or decreased.<main id="notice" type='notice'><h4>Notice</h4><p>When you decrease the value of the auto-increment column for a table that contains data, the new value of the <code>AUTO_INCREMENT</code> column is automatically adjusted to the next value of the maximum value in the auto-increment column. For example, if the current maximum value in the auto-increment column is 5 and the current value of the <code>AUTO_INCREMENT</code> column is 8, and if you set the value of the <code>AUTO_INCREMENT</code> column to any value between 0 and 6, the actual value of the <code>AUTO_INCREMENT</code> column after the statement is executed will be adjusted to 6. </p></main></li>   <li> `comment`: specifies the comment of the table. </li>   <li> `PROGRESSIVE_MERGE_NUM`: specifies the number of progressive merge steps. The value range is \[0,100\]. </li>   <li> `parallel_clause`: specifies the parallelism at the table level. </li> <li> `NOPARALLEL`: specifies a parallelism of `1`. This is the default configuration. </li>   <li> `PARALLEL integer`: specifies the parallelism, where `integer` must be greater than or equal to `1`. </li> </ul>     |
| CHECK                                                      | Modifies the `CHECK` constraint. The following operations are supported: <ul><li> Adds a new `CHECK` constraint. </li>   <li> Drops the `CHECK` constraint with the name of `constraint_name`. </li>  </ul>  |
| \[NOT\] ENFORCED                                           | Specifies whether to enforce the `CHECK` constraint with the name of `constraint_name`. <ul><li> If this parameter is omitted or set to `ENFORCED`, the constraint is created and enforced, which is the default state. </li>   <li> If set to `NOT ENFORCED`, the constraint is created but not enforced. </li> </ul>   |
| ADD COLUMN GROUP([all columns, ]each column) [DELAYED] | Changes a table to an in-column-store table. The syntax is described as follows:<ul><li><code>ADD COLUMN GROUP(all columns, each column)</code>: changes a table to a rowstore-columnstore redundant table. </li><li><code>ADD COLUMN GROUP(each column)</code>: changes a table to an in-column-store table. </li><li>`DELAYED`: optional. Specifies to asynchronously execute the command for converting a rowstore table into an in-column-store table. After the command is executed, the storage format specified in the table definition is modified. However, the actual conversion from rowstore to columnstore is performed when a major compaction is executed. This operation does not block current DML operations and is considered online DDL. By default, if you do not specify the `DELAYED` clause, offline DDL is performed, and the rowstore table is synchronously converted into an in-column-store table. <main id="notice" type='notice'><h4>Notice</h4><p><ul><li>The <code>DELAYED</code> clause can be specified only when you change a rowstore table into an in-column-store table (<code>each column</code>) or a rowstore-columnstore redundant table (<code>all columns, each column</code>). </li><li>After the <code>DELAYED</code> command for converting a rowstore table into an in-column-store table is executed, the query performance may not meet the expectation before the baseline data is compacted. </li></ul></p></main></li></ul>|
| DROP COLUMN GROUP([all columns, ]each column) | Removes the storage format of a table. The syntax is described as follows:<ul><li><code>DROP COLUMN GROUP(all columns, each column)</code>: removes the rowstore-columnstore redundant storage format from a table. </li><li><code>DROP COLUMN GROUP(all columns)</code>: removes the rowstore storage format from a table. </li><li><code>DROP COLUMN GROUP(each column)</code>: removes the columnstore storage format from a table. </li></ul>|
| index_column_group_option | Specifies index options. The syntax is described as follows:<ul><li><code>WITH COLUMN GROUP(all columns, each column)</code>: specifies to add a rowstore-columnstore redundant index. </li><li><code>WITH COLUMN GROUP(all columns)</code>: specifies to add a rowstore index. </li><li><code>WITH COLUMN GROUP(each column)</code>: specifies to add a columnstore index. </li></ul>|
| SKIP_INDEX | Modifies the skip index attribute of a column. Valid values are:<ul><li><code>MIN_MAX</code>: the most general aggregation data type of skip index. It stores the maximum values, minimum values, and null counts of indexed columns at the index node granularity. This type of data can accelerate filter pushdown and `MIN/MAX` aggregation pushdown. </li><li> <code>SUM</code>: used to accelerate the pushdown of `SUM` aggregation for numeric data types. </li></ul>  <main id="notice" type='notice'><h4>Notice</h4><p><ul><li>The skip index cannot be created for JSON and spatial data types. </li><li>Generated columns cannot have the skip index attribute. </li></ul></p></main>|
| EXCHANGE PARTITION partition_name WITH TABLE non_partition_table_name | Specifies a partition exchange. In this statement, `partition_name` indicates the name of a partition in a partitioned table, and `non_partition_table_name` indicates the name of a non-partitioned table. For more information about partition exchange, see [Partition exchange](../../../../300.database-object-management/100.manage-object-of-mysql-mode/300.manage-partitions-of-mysql-mode/1000.exchange-partition-of-mysql-mode.md).|
| TABLE_MODE | Optional. Specifies the major compaction trigger threshold and strategy, that is, the behavior of a table after minor compaction. For more information about the values, see [table_mode_value](#table_mode_value).|
| AUTO_INCREMENT_CACHE_SIZE | Specifies the number of auto-increment values in the cache. The default value is 0, which indicates that the system uses the tenant-level parameter [auto_increment_cache_size](../../../../800.configuration-items-and-system-variables/200.system-variable/300.global-system-variable/100.auto_increment_cache_size-global.md) as the cache size for the auto-increment column. |
| READ {ONLY \| WRITE} | Specifies the read/write permissions of a table. The specific meanings are as follows:<ul><li>`READ ONLY`: indicates that the table can only be read and cannot be modified or dropped. <main id="notice" type='notice'><h4>Notice</h4><p>The table-level <code>READ ONLY</code> parameter does not take effect for users with the <code>SUPER</code> privilege, but only for regular users. </p></main></li><li>`READ WRITE`: the default value, indicating that the table can be read, modified, and dropped. </li></ul>|
| DUPLICATE_SCOPE | Changes the replication scope of a table.<ul><li>`none`: converts the table to a normal table. </li><li>`cluster`: converts the table to a replicated table. </li></ul>For more information about how to change the replication scope of a table, see [Change the replication scope of a table](../../../../300.database-object-management/100.manage-object-of-mysql-mode/200.manage-tables-of-mysql-mode/650.change-duplicate-scope-of-mysql-mode.md).|
| enable_macro_block_bloom_filter [=] {True \| False} | Specifies whether to persist macro block-level bloom filters. The values are as follows:<ul><li><code>True</code>: Enables persistence of macro block-level bloom filters.</li></ul>  <ul><li><code>False</code>: Disables persistence of macro block-level bloom filters.</li></ul> <main id="notice" type='explain'><h4>Note</h4><p>In OceanBase Database V4.3.5, the <code>CREATE TABLE</code> statement supports the <code>enable_macro_block_bloom_filter</code> option starting from V4.3.5 BP2.</p></main>|  
| DYNAMIC_PARTITION_POLICY \[=\] \(dynamic_partition_policy_list\) | Modifies the dynamic partition management properties of a table. `dynamic_partition_policy_list` is a configurable parameter list for dynamic partition policies, with parameters separated by commas. For more information, see [dynamic_partition_policy_option](#dynamic_partition_policy_option) below. <main id="notice" type='explain'><h4>Note</h4><p>For OceanBase Database V4.3.5, specifying dynamic partition management properties for a table is supported starting from V4.3.5 BP2.</p></main>|
| REORGANIZE PARTITION | Manually splits a partition. The parameters are described as follows:<ul><li>`partition_name`: the name of the partition to be split. </li><li>`split_partition_name`: the name of the new partition after splitting. </li><li>`value`: the boundary value of the partition split, which indicates the range. The range of `value` for defining a partition split must be the same as that of the source partition and in ascending order. </li><li>`source_value`: the original boundary value of the partition to be split, which indicates the range. It is the definition of the last partition split, and its `value` is the same as that of the source partition. </li></ul>For more information about how to manually split a partition, see [Manually split a partition](../../../../300.database-object-management/100.manage-object-of-mysql-mode/300.manage-partitions-of-mysql-mode/250.partition-splitting-of-mysql-mode/200.manual-partition-splitting-of-mysql-mode.md). <main id="notice" type='notice'><h4>Notice</h4><p>In MySQL mode of OceanBase Database, the current version supports splitting one partition into multiple partitions, but does not support splitting multiple partitions into multiple partitions. </p></main> |
| PARTITION BY | Modifies the automatic partitioning attribute of a table or the partitioning rule. The syntax is described as follows:<ul><li> `partition_option`: the option to modify the partitioning rule. For more information, see [Modify the partitioning rule](../../../../300.database-object-management/100.manage-object-of-mysql-mode/300.manage-partitions-of-mysql-mode/300.modify-partition-rules-of-mysql-mode.md). </li><li> `RANGE [COLUMNS]([column_name_list]) [SIZE('size_value')]`: modifies the automatic partitioning attribute of a table. For more information, see the syntax for modifying the automatic partitioning attribute in [Automatic partition splitting](../../../../300.database-object-management/100.manage-object-of-mysql-mode/300.manage-partitions-of-mysql-mode/250.partition-splitting-of-mysql-mode/300.automatic-partition-splitting-of-mysql-mode.md). </li></ul>|
| MODIFY PARTITION partition_name ADD SUBPARTITION (add_subpartition_option) | Adds a subpartition. <main id="notice" type='notice'><h4>Notice</h4><p><ul><li>Subpartitions of HASH or KEY type cannot be added. </li><li>For OceanBase Database V4.3.5, the addition of subpartitions is supported from V4.3.5 BP1. </li></ul></p></main>|
| add_subpartition_option | The definition of the subpartition to be added. |
| range_partition_expr    | The expression for RANGE or RANGE COLUMNS partitioning.|
| list_partition_expr     | The expression for LIST or LIST COLUMNS partitioning.|
| FORCE | Clears obsolete columns. <main id="notice" type='explain'><h4>Note</h4><p><ul><li>Starting from OceanBase Database V4.3.5 BP2, clearing obsolete columns is supported.</li><li>When certain columns are deleted, they may still occupy physical storage space even if they are no longer in use. To remove these obsolete columns and reclaim the associated space, you need to clear the obsolete columns.</li></ul></p></main>|
| SEMISTRUCT_ENCODING_TYPE | Optional. Specifies the semi-structured data encoding type. Valid values are:<ul><li>`''`: specifies not to enable semi-structured data encoding. </li><li>`'encoding'`: specifies to enable semi-structured data encoding. </li></ul>For more information about how to use semi-structured data encoding, see [Use semi-structured data encoding](../100.basic-elements-of-mysql-mode/100.data-type-of-mysql-mode/800.json-formatted-data-type-of-mysql-mode/200.create-a-json-value-of-mysql-mode.md).|

### table_mode_value

<main id="notice" type='explain'>
  <h4>Note</h4>
  <p>For all <code>TABLE_MODE</code> modes except <code>NORMAL</code>, they represent <code>QUEUING</code> tables. <code>QUEUING</code> tables are the most fundamental type, and the subsequent modes (excluding the NORMAL mode) represent more aggressive merge strategies.</p>
</main>

* `NORMAL`: Normal. This is the default value. In this mode, the probability of triggering a major compaction after a minor compaction is very low.
* `QUEUING`: Queuing. In this mode, the probability of triggering a major compaction after a minor compaction is low.
* `MODERATE`: Moderate. In this mode, the probability of triggering a major compaction after a minor compaction is moderate.
* `SUPER`: Super. In this mode, the probability of triggering a major compaction after a minor compaction is high.
* `EXTREME`: Extreme. In this mode, the probability of triggering a major compaction after a minor compaction is high.

For more information about major compactions, see [Adaptive compaction](../../../../200.system-management/500.manage-data-storage/200.merge-management/320.adaptive-compaction.md).

### tokenizer_option

* `SPACE`: The default value. Splits words based on spaces. You can specify the following parameters:

  |        Parameter      | Value range |
  |------------------|---------|
  | min_token_size   | [1, 16] |
  | max_token_size   | [10, 84]|

* `NGRAM`: Splits words based on N-Gram (Chinese). You can specify the following parameter:

  |        Parameter      | Value range |
  |------------------|---------|
  | ngram_token_size | [1, 10] |

* `BENG`: Splits words based on the basic English model. You can specify the following parameters:

  |        Parameter      | Value range |
  |------------------|---------|
  | min_token_size   | [1, 16] |
  | max_token_size   | [10, 84]|

* `IK`: Splits words based on IK (Chinese). The current supported character set is only `utf-8`. You can specify the following parameter:

  |        Parameter      | Value range |
  |------------------|---------|
  | ik_mode          | <ul><li>`smart`</li><li>`max_word`</li></ul>|

  <main id="notice" type='explain'>
    <h4>Note</h4>
    <p>In OceanBase Database V4.3.5, the <code>IK</code> tokenizer is supported since V4.3.5 BP1. </p>
  </main>

You can use the [TOKENIZE](../400.functions-of-mysql-mode/960.text-processing-functions-of-mysql-mode/200.tokenize-of-mysql-mode.md) function to view the tokenization result of a text based on the specified tokenizer and JSON parameters.

### parser_properties

* `min_token_size`: the minimum token length. The default value is 3, and the value range is from 1 to 16.
* `max_token_size`: the maximum token length. The default value is 84, and the value range is from 10 to 84.
* `ngram_token_size`: Represents the token length of `NGRAM`, only valid for the `NGRAM` tokenizer. The default value is 2, and the range is from 1 to 10.
* `ik_mode`: the tokenization mode of the `IK` tokenizer. Valid values:

  * `smart`: the default value. Dictionary words are used to improve tokenization accuracy. The boundaries of dictionary words are given priority, which may reduce unnecessary expansions.
  * `max_word`: words defined in the dictionary are identified, but the maximization expansion of tokens is not affected. In `max_word` mode, the text is still divided into more words even if they are defined in the dictionary.

**Here are some examples:**

1. Create a table named `tbl1`.

    ```sql
    CREATE TABLE tbl1 (col1 VARCHAR(200), col2 TEXT);
    ```

2. Add a full-text index named `fidx` to the `tbl1` table.

    ```sql
    ALTER TABLE tbl1 ADD FULLTEXT INDEX fidx(col2) WITH PARSER ik PARSER_PROPERTIES=(ik_mode='max_word');
    ```

## Example

### Operations to add and drop columns

Execute the following statements to create a table named `tbl1` and return the table information by using the `DESCRIBE` statement.

```sql
obclient> CREATE TABLE tbl1 (c1 INT(11) PRIMARY KEY,c2 VARCHAR(50));
Query OK, 0 rows affected

obclient> DESCRIBE tbl1;
+-------+------------+----------+--------+---------+-------+
| Field | Type       | Null     | Key    | Default | Extra |
+-------+------------+----------+--------+---------+-------+
| c1    | int(11)    | NO       | PRI    | NULL    |       |    
| c2    | varchar(50)| YES      |        | NULL    |       |   
+-------+------------+----------+--------+---------+-------+
```

* Add a column named `c3` and return the table information by using the `DESCRIBE` statement.

  ```sql
  obclient> ALTER TABLE tbl1 ADD c3 INT;
  Query OK, 1 row affected

  obclient> DESCRIBE tbl1;
  +-------+------------+----------+--------+---------+-------+
  | Field | Type       | Null     | Key    | Default | Extra |
  +-------+------------+----------+--------+---------+-------+
  | c1    | int(11)    | NO       | PRI    | NULL    |       |    
  | c2    | varchar(50)| YES      |        | NULL    |       |  
  | c3    | int(11)    | YES      |        | NULL    |       |  
  +-------+------------+----------+--------+---------+-------+
  3 row affected
  ```

* Drop the `c3` column and return the table information by using the `DESCRIBE` statement.

  ```sql
  obclient> ALTER TABLE tbl1 DROP c3;
  Query OK, 1 row affected

  obclient> DESCRIBE tbl1;
  +-------+------------+----------+--------+---------+-------+
  | Field | Type       | Null     | Key    | Default | Extra |
  +-------+------------+----------+--------+---------+-------+
  | c1    | int(11)    | NO       | PRI    | NULL    |       |    
  | c2    | varchar(50)| YES      |        | NULL    |       |  
  +-------+------------+----------+--------+---------+-------+
  2 row affected
  ```

* Rename the `c2` column of the `tbl1` table to `c3` and return the table information by using the `DESCRIBE` statement.

  ```sql
  obclient> ALTER TABLE tbl1 CHANGE COLUMN c2 c3 VARCHAR(50);
  Query OK, 1 row affected

  obclient> DESCRIBE tbl1;
  +-------+------------+----------+--------+---------+-------+
  | Field | Type       | Null     | Key    | Default | Extra |
  +-------+------------+----------+--------+---------+-------+
  | c1    | int(11)    | NO       | PRI    | NULL    |       |    
  | c3    | varchar(50)| YES      |        | NULL    |       |  
  +-------+------------+----------+--------+---------+-------+
  2 rows affected
  ```

* Add a column named `c4` to the `tbl1` table, set the column as the first column of the table, and return the table information by using the `DESCRIBE` statement.

  ```sql
  obclient> ALTER TABLE tbl1 ADD COLUMN c4 INTEGER FIRST;
  Query OK, 1 row affected

  obclient> DESCRIBE tbl1;
  +-------+------------+----------+--------+---------+-------+
  | Field | Type       | Null     | Key    | Default | Extra |
  +-------+------------+----------+--------+---------+-------+
  | c4    | int(11)    | YES      |        | NULL    |       |  
  | c1    | int(11)    | NO       | PRI    | NULL    |       |    
  | c3    | varchar(50)| YES      |        | NULL    |       |  
  +-------+------------+----------+--------+---------+-------+
  3 row affected
  ```

* Add the `c2` column after the `c1` column, and return the table information by using the `DESCRIBE` statement.

  ```sql
  obclient> ALTER TABLE tbl1 ADD COLUMN c2 INTEGER AFTER c1;
  Query OK, 1 row affected

  obclient> DESCRIBE tbl1;
  +-------+------------+----------+--------+---------+-------+
  | Field | Type       | Null     | Key    | Default | Extra |
  +-------+------------+----------+--------+---------+-------+
  | c4    | int(11)    | YES      |        | NULL    |       |  
  | c1    | int(11)    | NO       | PRI    | NULL    |       |
  | c2    | int(11)    | YES      |        | NULL    |       |    
  | c3    | varchar(50)| YES      |        | NULL    |       |  
  +-------+------------+----------+--------+---------+-------+
  4 row affected
  ```

* Add the `c5` column before the `c4` column, and return the table information by using the `DESCRIBE` statement.

  ```sql
  obclient> ALTER TABLE tbl1 ADD COLUMN c5 INT BEFORE c4;
  Query OK, 1 row affected

  obclient> DESCRIBE tbl1;
  +-------+------------+----------+--------+---------+-------+
  | Field | Type       | Null     | Key    | Default | Extra |
  +-------+------------+----------+--------+---------+-------+
  | c5    | int(11)    | YES      |        | NULL    |       |  
  | c4    | int(11)    | YES      |        | NULL    |       |  
  | c1    | int(11)    | NO       | PRI    | NULL    |       |
  | c2    | int(11)    | YES      |        | NULL    |       |    
  | c3    | varchar(50)| YES      |        | NULL    |       |  
  +-------+------------+----------+--------+---------+-------+
  5 row affected
  ```

* Add a foreign key constraint named `fk1` to the `tbl2` table. When an `UPDATE` operation affects the key value in the parent table that matches a row in the child table, execute the `SET NULL` operation.

  ```sql
  obclient> CREATE TABLE tbl2(c1 INT PRIMARY KEY,c2 INT);
  Query OK, 0 row affected
  obclient> CREATE TABLE tbl3(c1 INT PRIMARY KEY,c2 INT);
  Query OK, 0 row affected
  obclient> ALTER TABLE tbl2 ADD CONSTRAINT fk1 FOREIGN KEY (c2) REFERENCES tbl3(c1) ON UPDATE SET NULL;
  Query OK, 0 row affected
  ```

* Drop the foreign key constraint named `fk1` from the `tbl3` table.

  ```sql
  obclient> ALTER TABLE tbl2 DROP FOREIGN KEY fk1;
  Query OK, 0 row affected
  ```

* Drop the primary key of the `tbl2` table.

  ```sql
  obclient> ALTER TABLE tbl2 DROP PRIMARY KEY;
  Query OK, 0 row affected
  ```

### Index operations

* Rename the `ind1` index of the `tbl2` table to `ind2`.

  ```sql
  obclient> CREATE TABLE tbl2(c1 INT(11) PRIMARY KEY,c2 INT(50));
  Query OK, 0 row affected

  obclient> CREATE INDEX ind1 ON tbl2 (c2 ASC);
  Query OK, 0 row affected
  obclient> ALTER TABLE tbl2 RENAME INDEX ind1 TO ind2;
  Query OK, 0 row affected
  ```

* Create an index named `ind3` on the `tbl2` table, referencing the `c1` and `c2` columns.

  ```sql
  obclient> ALTER TABLE tbl2 ADD INDEX ind3 (c1,c2);
  Query OK, 0 row affected
  ```

  You can execute the `SHOW INDEX` statement to query the created index.

  ```sql
  obclient> SHOW INDEX FROM tbl2;
  +-------+------------+----------+--------------+-------------+-----------+-------------+----------+--------+------+------------+-----------+---------------+---------+
  | Table | Non_unique | Key_name | Seq_in_index | Column_name | Collation | Cardinality | Sub_part | Packed | Null | Index_type | Comment   | Index_comment | Visible |
  +-------+------------+----------+--------------+-------------+-----------+-------------+----------+--------+------+------------+-----------+---------------+---------+
  | tbl2  |          0 | PRIMARY  |            1 | c1          | A         |        NULL | NULL     | NULL   |      | BTREE      | available |               | YES     |
  | tbl2  |          1 | ind2     |            1 | c2          | A         |        NULL | NULL     | NULL   |  YES | BTREE      | available |               | YES     |
  | tbl2  |          1 | ind3     |            1 | c1          | A         |        NULL | NULL     | NULL   |      | BTREE      | available |               | YES     |
  | tbl2  |          1 | ind3     |            2 | c2          | A         |        NULL | NULL     | NULL   |  YES | BTREE      | available |               | YES     |
  +-------+------------+----------+--------------+-------------+-----------+-------------+----------+--------+------+------------+-----------+---------------+---------+
  4 rows in set
  ```

* Drop the `ind2` index from the `tbl2` table.

  ```sql
  obclient> ALTER TABLE tbl2 DROP INDEX ind2;
  Query OK, 0 row affected
  ```

  <main id="notice" type='explain'>
    <h4>Note</h4>
    <p>In actual operations, you can perform atomic index changes by using the preceding methods.</p>
  </main>

* Add three function-based indexes to the `t1_func` table, one of which is named `i2`, and the other two are automatically generated with names in the `functional_index` prefix followed by numbers.

  ```shell
  obclient> ALTER TABLE t1_func ADD INDEX ((CONCAT(c1,'a')));
  Query OK, 0 rows affected
  obclient> ALTER TABLE t1_func ADD INDEX ((c1+1));
  Query OK, 0 rows affected
  obclient> ALTER TABLE t1_func ADD INDEX i2 ((CONCAT(c1,'a')));
  Query OK, 0 rows affected
  obclient> SHOW CREATE TABLE t1_func;
  +---------+---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
  | Table   | Create Table                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
  +---------+---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
  | t1_func | CREATE TABLE `t1_func` (
  `c1` int(11) DEFAULT NULL,
  `c2` int(11) DEFAULT NULL,
  KEY `i1` (((`c1` + `c2`) < 1)) BLOCK_SIZE 16384 LOCAL,
  KEY `functional_index` (concat(`c1`,'a')) BLOCK_SIZE 16384 LOCAL,
  KEY `functional_index_2` ((`c1` + 1)) BLOCK_SIZE 16384 LOCAL,
  KEY `i2` (concat(`c1`,'a')) BLOCK_SIZE 16384 LOCAL
  ) DEFAULT CHARSET = utf8mb4 ROW_FORMAT = DYNAMIC COMPRESSION = 'zstd_1.3.8' REPLICA_NUM = 1 BLOCK_SIZE = 16384 USE_BLOOM_FILTER = FALSE TABLET_SIZE = 134217728 PCTFREE = 0 |
  +---------+---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
  1 row in set
  ```

* Create a columnstore index for a table.

  1. Execute the following SQL statement to create a `tbl3` table.

      ```sql
      CREATE TABLE tbl3 (col1 INT, col2 VARCHAR(50));
      ```

  2. Create a columnstore index named `idx1_tbl3` on the `tbl3` table, referencing the `col1` column.

      ```sql
      ALTER TABLE tbl3 ADD INDEX idx1_tbl3 (col1) WITH COLUMN GROUP(each column);
      ```

* Modify an index to be invisible.

  <main id="notice" type='explain'>
    <h4>Note</h4>
    <p>After an invisible index is created, <code>/*!80000 INVISIBLE */</code> is displayed in the index description in the table schema. </p>
  </main>

  1. Create a `tbl4` table.

      ```sql
      CREATE TABLE tbl4 (col1 INT PRIMARY KEY, col2 INT, col3 INT);
      ```

  2. Create an index named `idx1_tbl4` on the `tbl4` table.

      ```sql
      CREATE INDEX idx1_tbl4 ON tbl4(col2);
      ```

  3. Modify the `idx1_tbl4` index to be invisible.

      ```sql
      ALTER TABLE tbl4 ALTER INDEX idx1_tbl4 INVISIBLE;
      ```

  4. View the schema of the `tbl4` table again.

      ```sql
      SHOW CREATE TABLE tbl4;
      ```

      The return result is as follows:

      ```shell
      +-------+---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
      | Table | Create Table                                                                                                                                                                                                                                                                                                                                                                                                              |
      +-------+---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
      | tbl4  | CREATE TABLE `tbl4` (
        `col1` int(11) NOT NULL,
        `col2` int(11) DEFAULT NULL,
        `col3` int(11) DEFAULT NULL,
        PRIMARY KEY (`col1`),
        KEY `idx1_tbl4` (`col2`) BLOCK_SIZE 16384 LOCAL /*!80000 INVISIBLE */
      ) DEFAULT CHARSET = utf8mb4 ROW_FORMAT = DYNAMIC COMPRESSION = 'zstd_1.3.8' REPLICA_NUM = 1 BLOCK_SIZE = 16384 USE_BLOOM_FILTER = FALSE TABLET_SIZE = 134217728 PCTFREE = 0 WITH COLUMN GROUP(each column) |
      +-------+---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
      1 row in set
      ```

### Partition operations

* Purge all data from the `M202001` and `M202002` partitions of the `t_log_part_by_range` partitioned table.

  ```sql
  obclient> CREATE TABLE t_log_part_by_range (
      log_id      bigint NOT NULL
      , log_value varchar(50)
      , log_date  timestamp NOT NULL  
  ) PARTITION BY RANGE(UNIX_TIMESTAMP(log_date))
  (
      PARTITION M202001 VALUES LESS THAN(UNIX_TIMESTAMP('2020/02/01'))
      , PARTITION M202002 VALUES LESS THAN(UNIX_TIMESTAMP('2020/03/01'))
      , PARTITION M202003 VALUES LESS THAN(UNIX_TIMESTAMP('2020/04/01'))
      , PARTITION M202004 VALUES LESS THAN(UNIX_TIMESTAMP('2020/05/01'))
      , PARTITION M202005 VALUES LESS THAN(UNIX_TIMESTAMP('2020/06/01'))
  );
  Query OK, 0 rows affected

  obclient> ALTER TABLE t_log_part_by_range TRUNCATE PARTITION M202001, M202002;
  Query OK, 0 rows affected
  ```

* Add a partition named `M202006` to the `t_log_part_by_range` partitioned table.

  ```sql
  obclient> CREATE TABLE t_log_part_by_range (
      log_id      bigint NOT NULL
      , log_value varchar(50)
      , log_date  timestamp NOT NULL  
  ) PARTITION BY RANGE(UNIX_TIMESTAMP(log_date))
  (
      PARTITION M202001 VALUES LESS THAN(UNIX_TIMESTAMP('2020/02/01'))
      , PARTITION M202002 VALUES LESS THAN(UNIX_TIMESTAMP('2020/03/01'))
      , PARTITION M202003 VALUES LESS THAN(UNIX_TIMESTAMP('2020/04/01'))
      , PARTITION M202004 VALUES LESS THAN(UNIX_TIMESTAMP('2020/05/01'))
      , PARTITION M202005 VALUES LESS THAN(UNIX_TIMESTAMP('2020/06/01'))
  );
  Query OK, 0 rows affected

  obclient> ALTER TABLE t_log_part_by_range ADD PARTITION
           (PARTITION M202006 VALUES LESS THAN(UNIX_TIMESTAMP('2020/07/01'))
           );
  ```

### Modify the degree of parallelism

* Set the degree of parallelism of the `tbl3` table to `2`.

  ```sql
  obclient> ALTER TABLE tbl3 PARALLEL 2;
  ```

  In addition to using the `ALTER TABLE table_name PARALLEL int;` statement, you can also use the `PARALLEL` hint with the syntax `alter /*+ parallel(int) */ table`.

### Operations related to column types

* Modify the ordinary column `col1` of the `tbl4` table to an auto-increment column.

  ```sql
  obclient> CREATE TABLE tbl4 (col1 BIGINT(10) NOT NULL,col2 INT);
  Query OK, 0 rows affected

  obclient> ALTER TABLE tbl4 MODIFY col1 BIGINT(10) AUTO_INCREMENT;
  Query OK, 0 rows affected
  ```

* Add the primary key `col1` to the `tbl4` table, and then change the primary key to `col2`.

  ```sql
  obclient> ALTER TABLE tbl4 ADD PRIMARY KEY (col1);
  Query OK, 0 rows affected

  obclient> ALTER TABLE tbl4 DROP PRIMARY KEY,ADD PRIMARY KEY (`col2`);
  Query OK, 0 rows affected
  ```

* Add a `CHECK` constraint.

  ```sql
  obclient> CREATE TABLE tbl7(col1 VARCHAR(10),col2 VARCHAR(10));
  Query OK, 0 rows affected

  obclient> ALTER TABLE tbl7 ADD CONSTRAINT my_check CHECK (col1> col2) ;
  Query OK, 0 rows affected
  ```

* Drop the `CHECK` constraint from the `tbl7` table.

  ```sql
  obclient> ALTER TABLE tbl7 DROP CHECK my_check;
  Query OK, 0 rows affected
  ```

* Modify the column type to `NOT NULL`.

  ```sql
  obclient> CREATE TABLE dept(  
      deptno           NUMBER(2,0),  
      dname            VARCHAR(14),  
      location         VARCHAR(13),   
      CONSTRAINT pk_dept PRIMARY KEY(deptno)  
  );
  Query OK, 0 rows affected

  obclient> CREATE TABLE emp(  
      empno         NUMBER(4,0) NOT NULL,  
      empname       VARCHAR(10) NOT NULL,  
      job           VARCHAR(9) NOT NULL,  
      mgr           NUMBER(4,0) NOT NULL,  
      hiredate      DATE NOT NULL,  
      sal           NUMBER(7,2) DEFAULT NULL,
      comm          NUMBER(7,2) DEFAULT NULL,        
      deptno        NUMBER(2,0) DEFAULT NULL,   
      CONSTRAINT PK_emp PRIMARY KEY (empno),
      CONSTRAINT FK_deptno  FOREIGN KEY (deptno)  REFERENCES dept (deptno)  
  );
  Query OK, 0 rows affected

  obclient> ALTER TABLE emp MODIFY deptno NUMBER(2,0) DEFAULT '12' NOT NULL;
  Query OK, 0 rows affected
  ```

### Rename columns

* The `RENAME COLUMN` statement does not modify the column definition but only changes the column name. If the target name exists in the table, an error is returned when you execute the `RENAME COLUMN` statement, but the rename operation to the original name is not restricted.

  ```shell
  obclient> CREATE TABLE tbl8 (a INT, b INT);
  Query OK, 0 rows affected

  obclient> ALTER TABLE tbl8 RENAME COLUMN a TO b;
  ERROR 1060 (42S21): Duplicate column name 'b'

  obclient> ALTER TABLE tbl8 RENAME COLUMN a TO a;
  Query OK, 0 rows affected
  ```

* If an index is created on the column to be renamed, you can execute the `RENAME COLUMN` statement, and the index definition will be automatically modified in a cascading manner.

  ```shell
  obclient> CREATE TABLE tbl9 (a INT, b INT, index idx_a(a));
  Query OK, 0 rows affected

  obclient> SHOW INDEX FROM tbl9;
  +-------+------------+----------+--------------+-------------+-----------+-------------+----------+--------+------+------------+-----------+---------------+---------+------------+
  | Table | Non_unique | Key_name | Seq_in_index | Column_name | Collation | Cardinality | Sub_part | Packed | Null | Index_type | Comment   | Index_comment | Visible | Expression |
  +-------+------------+----------+--------------+-------------+-----------+-------------+----------+--------+------+------------+-----------+---------------+---------+------------+
  | tbl9  |          1 | idx_a    |            1 | a           | A         |        NULL | NULL     | NULL   | YES  | BTREE      | available |               | YES     | NULL       |
  +-------+------------+----------+--------------+-------------+-----------+-------------+----------+--------+------+------------+-----------+---------------+---------+------------+
  1 row in set

  obclient> ALTER TABLE tbl9 RENAME COLUMN a TO c;
  Query OK, 0 rows affected

  obclient> SHOW INDEX FROM tbl9;
  +-------+------------+----------+--------------+-------------+-----------+-------------+----------+--------+------+------------+-----------+---------------+---------+------------+
  | Table | Non_unique | Key_name | Seq_in_index | Column_name | Collation | Cardinality | Sub_part | Packed | Null | Index_type | Comment   | Index_comment | Visible | Expression |
  +-------+------------+----------+--------------+-------------+-----------+-------------+----------+--------+------+------------+-----------+---------------+---------+------------+
  | tbl9  |          1 | idx_a    |            1 | c           | A         |        NULL | NULL     | NULL   | YES  | BTREE      | available |               | YES     | NULL       |
  +-------+------------+----------+--------------+-------------+-----------+-------------+----------+--------+------+------------+-----------+---------------+---------+------------+
  1 row in set
  ```

* If the column to be renamed is referenced by a prefixed index, you can execute the `RENAME COLUMN` statement, and the prefixed index supports cascading modification.

  ```shell
  DROP TABLE tbl9;
  obclient> CREATE TABLE tbl9 (c1 INT PRIMARY KEY, c2 BLOB, c3 INT, INDEX i1 (c2(10)));
  Query OK, 0 rows affected

  obclient> ALTER TABLE tbl9 RENAME COLUMN c2 TO c2_, RENAME COLUMN c1 TO c2_1;
  DESC tbl9;
  Query OK, 0 rows affected

  obclient> SHOW INDEX FROM tbl9;
  +-------+------------+----------+--------------+-------------+-----------+-------------+----------+--------+------+------------+-----------+---------------+---------+------------+
  | Table | Non_unique | Key_name | Seq_in_index | Column_name | Collation | Cardinality | Sub_part | Packed | Null | Index_type | Comment   | Index_comment | Visible | Expression |
  +-------+------------+----------+--------------+-------------+-----------+-------------+----------+--------+------+------------+-----------+---------------+---------+------------+
  | tbl9  |          0 | PRIMARY  |            1 | c2_1        | A         |        NULL | NULL     | NULL   |      | BTREE      | available |               | YES     | NULL       |
  | tbl9  |          1 | i1       |            1 | c2_         | A         |        NULL | 10       | NULL   | YES  | BTREE      | available |               | YES     | NULL       |
  +-------+------------+----------+--------------+-------------+-----------+-------------+----------+--------+------+------------+-----------+---------------+---------+------------+
  2 rows in set
  ```

* If a foreign key constraint is created on the column to be renamed, you can execute the `RENAME COLUMN` statement, and the foreign key constraint will be automatically modified in a cascading manner.

  ```shell
  obclient> CREATE TABLE tbl10 (a INT PRIMARY KEY);
  Query OK, 0 rows affected

  obclient> CREATE TABLE tbl11(b INT, FOREIGN KEY (b) REFERENCES tbl10(a));

  obclient> SHOW CREATE TABLE tbl11;
  +-------+-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
  | Table | Create Table                                                                                                                                                                                                                                                                                                                                                      |
  +-------+-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
  | tbl11 | CREATE TABLE `tbl11` (
  `b` int(11) DEFAULT NULL,
  CONSTRAINT `tbl11_OBFK_1694681944513338` FOREIGN KEY (`b`) REFERENCES `test`.`tbl10`(`a`) ON UPDATE RESTRICT ON DELETE RESTRICT
  ) DEFAULT CHARSET = utf8mb4 ROW_FORMAT = DYNAMIC COMPRESSION = 'zstd_1.3.8' REPLICA_NUM = 1 BLOCK_SIZE =  16384 USE_BLOOM_FILTER = FALSE TABLET_SIZE = 134217728 PCTFREE = 0 |
  +-------+-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
  1 row in set

  obclient> ALTER TABLE tbl10 RENAME COLUMN a TO c;
  Query OK, 0 rows affected

  obclient> SHOW CREATE TABLE tbl11;
  +-------+-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
  | Table | Create Table                                                                                                                                                                                                                                                                                                                                                      |
  +-------+-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
  | tbl11 | CREATE TABLE `tbl11` (
  `b` int(11) DEFAULT NULL,
  CONSTRAINT `tbl11_OBFK_1694681944513338` FOREIGN KEY (`b`) REFERENCES `test`.`tbl10`(`c`) ON UPDATE RESTRICT ON DELETE RESTRICT
  ) DEFAULT CHARSET = utf8mb4 ROW_FORMAT = DYNAMIC COMPRESSION = 'zstd_1.3.8' REPLICA_NUM = 1 BLOCK_SIZE = 16384 USE_BLOOM_FILTER = FALSE TABLET_SIZE = 134217728 PCTFREE = 0 |
  +-------+-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
  1 row in set
  ```

OceanBase Database does not support renaming or automatic cascading modification in the following scenarios:

* If the column to be renamed is referenced by a generated list expression, the column name cannot be modified. An error will be returned when you execute the statement.

  ```shell
  obclient> CREATE TABLE tbl12(a INT, b INT AS (a + 1), c INT, d INT, CONSTRAINT d_check CHECK(d > 0)) PARTITION BY HASH(c + 1) PARTITIONS 2;

  obclient> ALTER TABLE tbl12 RENAME COLUMN a TO e;
  ERROR 3108 (HY000): Column 'a' has a generated column dependency
  ```

* If the column to be renamed is referenced by a partitioning expression, the column name cannot be modified. An error will be returned when you execute the statement.

  ```shell
  obclient> ALTER TABLE tbl12 RENAME COLUMN c TO e;
  ERROR 3855 (HY000): Column 'c' has a partitioning function dependency and cannot be dropped or renamed.
  ```

* If the column to be renamed is referenced by a `CHECK` constraint, the column name cannot be modified. An error will be returned when you execute the statement.

  ```shell
  obclient> ALTER TABLE tbl12 RENAME COLUMN d TO e;
  ERROR 3959 (HY000): Check constraint 'd_check' uses column 'd', hence column cannot be dropped or renamed.
  ```

* If the column to be renamed is referenced by a functional index, the column name cannot be modified. An error will be returned when you execute the statement.

  ```shell
  DROP TABLE IF EXISTS tbl12;
  obclient> CREATE TABLE tbl12(i INT, INDEX ((i+1)));
  Query OK, 0 rows affected

  obclient> ALTER TABLE tbl12 RENAME COLUMN i TO j;
  ERROR 3837 (HY000): Column 'i' has a functional index dependency and cannot be dropped or renamed.
  ```

* If the column to be renamed is referenced by a view, the `RENAME COLUMN` statement can be executed successfully, but an error will be returned when you query the view. You must manually modify the view definition.

  ```shell
  obclient> CREATE TABLE tbl13(a INT);
  Query OK, 0 rows affected

  obclient> CREATE VIEW v1 AS SELECT a + 1 FROM tbl13;
  Query OK, 0 rows affected

  obclient> SELECT * FROM v1;
  Empty set

  obclient> ALTER TABLE tbl13 RENAME COLUMN a TO b;
  Query OK, 0 rows affected

  obclient> SELECT * FROM v1;
  ERROR 1356 (42S22): View 'test.v1' references invalid table(s) or column(s) or function(s) or definer/invoker of view lack rights to use them
  ```

* If the column to be renamed is referenced by a stored procedure, the `RENAME COLUMN` statement can be executed successfully, but an error will be returned when you execute the `CALL` procedure. You must manually modify the stored procedure.

  ```shell
  obclient> CREATE TABLE tbl14(a INT);
  Query OK, 0 rows affected

  obclient> CREATE PROCEDURE proc() SELECT a + 1 FROM tbl14;
  Query OK, 0 rows affected

  obclient> CALL proc();
  Empty set

  obclient> ALTER TABLE tbl14 RENAME COLUMN a TO b;
  Query OK, 0 rows affected

  obclient> CALL proc();
  ERROR 1054 (42S22): Unknown column 'a' in 'field list'
  ```

### Change the column storage properties of a table

1. Create a table named `tbl1` by using the following SQL statement:

    ```sql
    CREATE TABLE tbl1 (col1 INT PRIMARY KEY, col2 VARCHAR(50));
    ```

2. Change the `tbl1` table to a rowstore redundant columnstore table and then drop the rowstore columnstore redundant attribute.

    ```sql
    ALTER TABLE tbl1 ADD COLUMN GROUP(all columns, each column);
    ```

    ```sql
    ALTER TABLE tbl1 DROP COLUMN GROUP(all columns, each column);
    ```

3. Change the `tbl1` table to a columnstore table and then drop the columnstore attribute.

    ```sql
    ALTER TABLE tbl1 ADD COLUMN GROUP(each column);
    ```

    ```sql
    ALTER TABLE tbl1 DROP COLUMN GROUP(each column);
    ```

### Modify the skip index attribute of a column

Execute the following SQL statement to create a table named `test_skidx`.

```sql
CREATE TABLE test_skidx(
  col1 INT SKIP_INDEX(MIN_MAX, SUM),
  col2 FLOAT SKIP_INDEX(MIN_MAX),
  col3 VARCHAR(1024) SKIP_INDEX(MIN_MAX),
  col4 CHAR(10)
  );
```

* Modify the skip index attribute of column `col2` in the `test_skidx` table to `SUM` skip index type.

  ```sql
  ALTER TABLE test_skidx MODIFY COLUMN col2 FLOAT SKIP_INDEX(SUM);
  ```

* Add a skip index attribute to a column in a created table. Add a `MIN_MAX` skip index attribute to column `col4` in the `test_skidx` table.

  ```sql
  ALTER TABLE test_skidx MODIFY COLUMN col4 CHAR(10) SKIP_INDEX(MIN_MAX);
  ```

* Drop a skip index attribute from a column in a created table. Drop the skip index attribute from column `col1` in the `test_skidx` table.

  ```sql
  ALTER TABLE test_skidx MODIFY COLUMN col1 INT SKIP_INDEX();
  ```

  or

  ```sql
  ALTER TABLE test_skidx MODIFY COLUMN col1 INT;
  ```

## References

[Modify a table](../../../../300.database-object-management/100.manage-object-of-mysql-mode/200.manage-tables-of-mysql-mode/600.change-table-of-mysql-mode.md)