| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |

# ALTER COMPILE

## Description

This statement triggers the recompilation of a procedure or function.

## Privilege requirements

To execute the `ALTER COMPILE` statement, the current user must have the `ALTER` privilege. For more information about OceanBase Database privileges, see [Privilege types in MySQL mode](../../../../../600.manage/500.security-and-permissions/300.access-control/200.user-and-permission/200.permission-of-mysql-mode/100.permission-classification-of-mysql.md).

## Syntax

```sql
ALTER {procedure｜function} name COMPILE [reuse settings]
```

## Parameter explanation

|                        **Parameter**                         |                                                        **Description**                                                        |
|-------------------------------------------------------|----------------------------------------------------------------------------------------------------------------------|
| procedure | Specifies `alter` procedure. |
| function  | Specifies `alter` function. |
| name      | The name of the stored procedure or function to be recompiled. |
| reuse settings | An optional parameter. If the `reuse settings` clause is specified, the parameters used to compile the original PL object are used to compile the new one. If it is not specified, the parameters in the current session are used to compile the new one. |

## Example

```shell
obclient> ALTER procedure test_p COMPILE reuse settings;
Query OK, 0 rows affected (0.242 sec)
```