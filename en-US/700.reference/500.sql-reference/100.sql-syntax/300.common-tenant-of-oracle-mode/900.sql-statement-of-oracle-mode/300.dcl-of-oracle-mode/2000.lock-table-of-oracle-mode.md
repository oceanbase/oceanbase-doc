| Description   | The LOCK TABLE statement locks one or more tables, partitions, or subpartitions in the specified mode. |
|---------------|-----------------|
| machine-translation ||
| keywords      | LOCK TABLE, table lock, partition lock, subpartition lock, lock mode |
| dir-name      | LOCK TABLE |
| dir-name-en   | LOCK TABLE |
| tenant-type   | Oracle Mode     |
|slug|lock-table-oracle-mode|

# LOCK TABLE

## Description

This statement locks one or more tables, partitions, or subpartitions in the specified mode.

Locked tables remain locked until the transaction is committed, rolled back, or rolled back to a savepoint before the lock was applied.

<main id="notice" type='explain'>
    <h4>Note</h4>
    <p>The table to be locked must be in the user's own schema, or the user must have the <code>LOCK ANY TABLE</code> system privilege. </p>
</main>

## Syntax

```sql
LOCK TABLE [ schema. ]table_name

  [ partition_extension_clause]
    [, [ schema. ] table_name
      [ partition_extension_clause ]
     ]...
  IN lock_mode MODE
  [ NOWAIT
   | WAIT integer
  ]

partition_extension_clause:

    PARTITION (partition_name, ...)
  | SUBPARTITION (subpartition_name, ...)

```

## Syntax

| **Field** | **Description** |
| --- | --- |
| table_name  | The name of the table to be locked. |
| partition_name | The name of the partition to be locked. |
| subpartition_name | The name of the subpartition to be locked. |
| lock_mode | The lock mode. OceanBase Database supports the following lock modes in the current version:<ul><li>`ROW SHARE`: allows concurrent access to the locked table, but prevents users from exclusively locking the entire table. </li><li> `ROW EXCLUSIVE`: same as `ROW SHARE`, but also prevents locking the table in `SHARE` mode. A `ROW EXCLUSIVE` lock is automatically obtained when you perform an update, insert, or delete operation. </li><li> `SHARE`: allows concurrent queries, but prevents updates to the locked table. </li><li>  `SHARE ROW EXCLUSIVE`: allows viewing the entire table and viewing rows in the table by other users, but prevents other users from locking the table or updating rows in `SHARE` mode. </li><li> `EXCLUSIVE`: allows only queries on the locked table and prevents other operations. </li></ul>  |
| NOWAIT | If a lock conflict occurs when locking the table, partition, or subpartition, specifying `NOWAIT` allows the database to immediately return control to the user. In this case, the database will return a message indicating that the table, partition, or subpartition is locked by another user. |
| WAIT | If a lock conflict occurs, the statement will wait for the conflicting lock to be released until the timeout, which is the user-specified `integer` value in seconds. The value of `integer` has no limit. |

## Examples

The following example creates a subpartitioned table and locks its partitions.

1. Create a subpartitioned table named `test`. The subpartition names generated by `SUBPARTITION TEMPLATE` are formed by appending 's' and the subpartition template name to the partition name. For example, the `p0` partition has subpartitions named `p0ssp0`, `p0ssp1`, and `p0ssp2`.

   ```shell
   CREATE TABLE TEST(col1 INT, col2 INT)
   PARTITION BY RANGE (col1)
   SUBPARTITION BY RANGE (col2)
     SUBPARTITION TEMPLATE
       (
       SUBPARTITION sp0 VALUES LESS THAN (3),
       SUBPARTITION sp1 VALUES LESS THAN (6),
       SUBPARTITION sp2 VALUES LESS THAN (9)
       )
   (
     PARTITION p0 VALUES LESS THAN (100),
     PARTITION p1 VALUES LESS THAN (200),
     PARTITION p2 VALUES LESS THAN (300)
   );
   ```

2. Lock the entire `test` table with an exclusive lock.

   ```sql
   obclient> LOCK TABLE test IN EXCLUSIVE MODE;
   ```

3. Lock the `p1` partition of the `test` table with an exclusive lock.

   ```sql
   obclient> LOCK TABLE test PARTITION (p1) IN EXCLUSIVE MODE;
   ```

4. Lock the `p1ssp1` subpartition of the `test` table with an exclusive lock.

   ```sql
   obclient> LOCK TABLE test SUBPARTITION (p1ssp1) IN EXCLUSIVE MODE;
   ```

5. Lock multiple partitions of the `test` table with exclusive locks.

   ```sql
   obclient> LOCK TABLE test PARTITION (p1, p2) IN EXCLUSIVE MODE;
   ```

6. Lock multiple subpartitions of the `test` table with exclusive locks.

   ```sql
   obclient> LOCK TABLE test SUBPARTITION (p0ssp1, p1ssp2) IN EXCLUSIVE MODE;
   ```

7. Lock multiple partitions and subpartitions of the `test` table with exclusive locks.

   ```sql
   obclient> LOCK TABLE test PARTITION (p1, p2), test SUBPARTITION (p0ssp0, p0ssp1) IN EXCLUSIVE MODE;
   ```

8. Attempt to lock a non-existent partition `p3` of the `test` table. In this example, `p3` does not exist, and the `p0` partition also fails to be locked, causing the entire statement to roll back.

   ```sql
   obclient> LOCK TABLE test PARTITION (p0, p3) IN EXCLUSIVE MODE;
   OBE-02149: Specified partition does not exist
   ```

9. Lock the `test` table in two client sessions. One session holds an exclusive lock, while the other requests a shared lock.
   When the `LOCK TABLE` statement does not specify the `WAIT N` or `NOWAIT` keyword, the timeout is determined by the minimum of the statement timeout and transaction timeout. In this example, the lock conflict error will be reported after 1 second.

   ```sql
   # conn1
   obclient> START TRANSACTION;
   obclient> LOCK TABLE test IN EXCLUSIVE MODE;

   # conn2
   obclient> START TRANSACTION;
   obclient> SET ob_trx_timeout = 10000000000; # 10000 seconds
   obclient> SET ob_query_timeout = 1000000;   # 1 second

   # The requesting client will receive a lock conflict error after 1 second
   obclient> LOCK TABLE test IN SHARE MODE;
   OBE-30006: resource busy; acquire with WAIT timeout expired
   ```

10. Lock the `test` table in two client sessions. One session holds an exclusive lock, while the other requests a shared lock, and the `NOWAIT` keyword is used.
    When the `LOCK TABLE` statement specifies the `NOWAIT` keyword, a lock conflict will result in an immediate error message indicating "NOWAIT". This error message differs from when the `WAIT N` or `NOWAIT` keyword is not specified.

    ```shell
    # conn1
    obclient> START TRANSACTION;
    obclient> LOCK TABLE test IN EXCLUSIVE MODE;

    # conn2
    obclient> START TRANSACTION;
    obclient> LOCK TABLE test IN SHARE MODE NOWAIT;
    OBE-00054: resource busy and acquire with NOWAIT specified or timeout expired
    ```

11. Lock the `test` table in two client sessions. One session holds an exclusive lock, while the other requests a shared lock, and the `WAIT N` keyword is used.
    When the `LOCK TABLE` statement specifies the `WAIT N` keyword, a lock conflict will result in a wait for N seconds. If the lock is not released within N seconds, an error message indicating "timeout expired" will be returned. This error message differs from when the `WAIT N` or `NOWAIT` keyword is not specified.

    ```shell
    # conn1
    obclient> START TRANSACTION;
    obclient> LOCK TABLE test IN EXCLUSIVE MODE;

    # conn2
    obclient> START TRANSACTION;

    # The requesting client will receive a lock conflict error after 1 second
    obclient> LOCK TABLE test IN SHARE MODE WAIT 1;
    OBE-00054: resource busy and acquire with NOWAIT specified or timeout expired
    ```