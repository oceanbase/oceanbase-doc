| Description   |                 |
|---------------|-----------------|
| machine-translation ||
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
|slug|shrink-oracle-mode|

# SHRINK

## Purpose

This statement rewrites the data of a table.

## Syntax

```sql
ALTER TABLE table_name SHRINK SPACE
```

## Parameters

|     Parameter     | Description  |
|------------|-----|
| table_name | the name of the table. |

## Examples

1. Create the `employee` table and insert data into it.

   ```sql
   obclient> CREATE TABLE employee (id NUMBER, name VARCHAR2(50));
   obclient> INSERT INTO employee VALUES (1, 'John');
   obclient> INSERT INTO employee VALUES (2, 'Mary');
   ```

2. Rewrite the data of the `employee` table.

   ```sql
   obclient> ALTER TABLE employee SHRINK SPACE;
   ```

3. Verify the space-shrinking result.

   ```sql
   obclient> SELECT segment_name, bytes/1024/1024 MB
   FROM user_segments
   WHERE segment_name = 'EMPLOYEE';
   ```