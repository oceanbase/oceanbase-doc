| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
|machine-translation||

# SHOW

## Purpose

This statement is used to display information about database objects.

## Syntax

```sql
SHOW {
  | TABLE STATUS
  | [FULL] PROCESSLIST
  | RECYCLEBIN
  
  | VARIABLES [LIKE 'pattern']
  | PARAMETERS
  
  | CHARSET | CHARACTER SET
  | COLLATION
  
  | CREATE TABLEGROUP tablegroup_name
  | CREATE TABLE table_name
  | CREATE VIEW view_name
  
  | GRANTS
  | PRIVILEGES
  
  | ERRORS
  | TRACE [FORMAT='JSON']
  | CREATE CATALOG external_catalog_name
  | CATALOGS
};
```

## Parameters

|    Parameter |    Description     |
|-----------|------------|
| TABLE STATUS                       | Displays the details of all tables for the current user.  |
| VARIABLES \[like 'variable_name'\] | Displays variable information. If you do not specify `like 'variable_name'`, all system variables are displayed. `variable_name` is the name of the variable. |
| CHARSET \| CHARACTER SET           | Displays the supported character sets. |
| COLLATION                          | Displays the supported collations.  |
| PARAMETERS                         | Displays all system parameters.  |
| TABLEGROUPS                        | Displays information about table groups.   |
| CREATE TABLEGROUP tablegroup_name  | Displays the statement for creating a table group. `tablegroup_name` is the name of the table group.      |
| CREATE TABLE table_name            | Displays the statement for creating a table. `table_name` is the name of the table.|
| CREATE VIEW view_name              | Displays the statement for creating a view. `view_name` is the name of the view.  |
| ERRORS                             | Displays error information. |
| GRANTS                             | Displays the privileges of the current user. |
| PRIVILEGES                         | Displays descriptions of privileges.|
| RECYCLEBIN                         | Displays the recycle bin. |
| \[FULL\] PROCESSLIST                 | Displays the list of processes for the current tenant. Specifics:<ul><li><code>SHOW PROCESSLIST</code> displays a brief list of processes. Specifics:<ul><li><code>ID</code>: the process ID, which is the Client Session ID of the current session. The ID uniquely identifies the session in the client.</li><li><code>USER</code>: the username of the connected user.</li><li><code>HOST</code>: the IP address and port of the client. If the client is connected through an ODP, the IP address and port of the ODP are displayed.</li><li><code>DB</code>: the name of the database accessed.</li><li><code>COMMAND</code>: the type of the current command. Valid values: <code>Query</code>, <code>Sleep</code>, etc.</li><li><code>TIME</code>: the time taken to execute the current command, in seconds. The time is reset to zero when the command is retried.</li><li><code>STATE</code>: the status of the current session. Valid values: <code>SLEEP</code>, <code>ACTIVE</code>, etc.</li><li><code>INFO</code>: the command being executed. The length of the information displayed cannot exceed 100 characters; otherwise, it will be truncated.</li></ul>  </li><li><code>SHOW FULL PROCESSLIST</code>: displays a complete list of processes, including detailed information about each process. Specifics:<ul><li><code>ID</code>: the process ID, which is the Client Session ID of the current session. The ID uniquely identifies the session in the client.</li><li><code>USER</code>: the username of the connected user.</li><li><code>TENANT</code>: the tenant to which the connection is made.</li><li><code>HOST</code>: the IP address and port of the client. If the client is connected through an ODP, the IP address and port of the ODP are displayed.</li><li><code>DB</code>: the name of the database accessed.</li><li><code>COMMAND</code>: the type of the current command. Valid values: <code>Query</code>, <code>Sleep</code>, etc.</li><li><code>TIME</code>: the time taken to execute the current command, in seconds. The time is reset to zero when the command is retried.</li><li><code>STATE</code>: the status of the current session. Valid values: <code>SLEEP</code>, <code>ACTIVE</code>, etc.</li><li><code>INFO</code>: the command being executed.</li><li><code>IP</code>: the IP address of the server.</li><li><code>PORT</code>: the SQL port number.</li></ul>  </li></ul>  <main id="notice" type='explain'><h4>Note</h4><p>To query the number of sessions and details of session IDs in the current database, execute the <code>SHOW PROCESSLIST</code> statement. For more information, see <a href="../../../../../1200.database-proxy/1500.view-tenant-sessions.md">View tenant sessions</a>. </p></main>|
| TRACE [FORMAT='JSON']              | Displays the execution status of SQL statements. You can choose to output the result in JSON format.|
| CREATE CATALOG external_catalog_name | Displays the statement for creating a catalog.<main id="notice" type='explain'><h4>Note</h4><p>The statement is supported in OceanBase Database V4.3.5 and later, starting from BP2 of V4.3.5. </p></main>|
| CATALOGS | Displays the catalogs of the current tenant.<main id="notice" type='explain'><h4>Note</h4><p>The statement is supported in OceanBase Database V4.3.5 and later, starting from BP2 of V4.3.5. </p></main>|

## Examples

* View the creation information about the `tbl1` table.

  ```sql
    obclient> CREATE TABLE employees (
      emp_id INT PRIMARY KEY,
      emp_name VARCHAR(50) NOT NULL,
      hire_date DATE,
      salary NUMBER(10,2),
      department VARCHAR(30)
    );
    obclient> SHOW CREATE TABLE employees;
  ```

* View whether the recycle bin is enabled.

  ```sql
  obclient> SHOW VARIABLES LIKE 'recyclebin';
  ```

* View the content of the recycle bin.

  ```sql
  obclient> SHOW RECYCLEBIN;
  ```

* Use `SHOW TRACE` to view the execution information about a SQL statement and output the information in JSON format.

  ```shell
  obclient> SET ob_enable_show_trace = 1;

  obclient> CREATE TABLE space_travelers (
      astronaut_id INT PRIMARY KEY,
      mission_code VARCHAR(50) NOT NULL,
      launch_date DATE,
      flight_hours NUMBER(10,2),
      space_station VARCHAR(30)
  );

  obclient> INSERT INTO space_travelers VALUES(1, 'APOLLO-11', TO_DATE('1969-07-16', 'YYYY-MM-DD'), 195.5, 'MOON');

  obclient> INSERT INTO space_travelers VALUES(2, 'ISS-EXP-1', TO_DATE('2000-11-02', 'YYYY-MM-DD'), 4320.8, 'ISS');

  obclient> SELECT/*+PARALLEL(2)*/ COUNT(*) FROM space_travelers;

  obclient> SHOW TRACE FORMAT='JSON'\G
  ```
