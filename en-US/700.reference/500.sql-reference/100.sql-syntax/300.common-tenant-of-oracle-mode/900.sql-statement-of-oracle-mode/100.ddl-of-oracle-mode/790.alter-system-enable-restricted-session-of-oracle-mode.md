| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
|machine-translation| en          |
|slug|alter-system-enable-restricted-session-oracle-mode|

# ALTER SYSTEM ENABLE/DISABLE RESTRICTED SESSION

## Purpose

The `ALTER SYSTEM ENABLE RESTRICTED SESSION` statement is used to enable restricted mode, and the `ALTER SYSTEM DISABLE RESTRICTED SESSION` statement is used to disable restricted mode.

After restricted mode is enabled, the database refuses to establish new regular connections. Only users with the `RESTRICTED SESSION` system privilege can continue logging in. This mode is commonly used for access control during application upgrades and maintenance. After the upgrade or maintenance is complete, you can restore normal service status by executing `ALTER SYSTEM DISABLE RESTRICTED SESSION`.

<main id="notice" type='explain'>
  <h4>Note</h4>
  <p>This statement is available starting with V4.6.1. </p>
</main>

## Syntax

```sql
ALTER SYSTEM { ENABLE | DISABLE } RESTRICTED SESSION;
```

## Parameters

| Parameter | Description |
| --------- | ------ |
| ENABLE | Enables restricted mode. After it is enabled, the database refuses to establish new normal connections; those with`RESTRICTED SESSION`User access with system privileges is not affected. Once enabled, the restricted mode will not be invalidated upon tenant restart. |
| DISABLE | Disables restricted mode. After disabled, the database reverts to its normal service state. |

## Examples

* Enable restricted mode.

  ```sql
  obclient> ALTER SYSTEM ENABLE RESTRICTED SESSION;
  ```

* Disable restricted mode.

  ```sql
  obclient> ALTER SYSTEM DISABLE RESTRICTED SESSION;
  ```

## References

* [RESTRICTED SESSION privilege](../../../../../../600.manage/500.security-and-permissions/300.access-control/200.user-and-permission/300.permission-of-oracle-mode/000.permission-classification-of-oracle-mode.md)
* After enabling or disabling restricted mode, you can check the instance mode via the `LOGINS` field in the [[G]V$INSTANCE](../../../../../../700.reference/700.system-views/500.system-view-of-oracle-mode/300.performance-view-of-oracle-mode/30800.v-instance-of-oracle-mode.md) view. If it is `RESTRICTED`, restricted mode is enabled.