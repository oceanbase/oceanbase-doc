| Description   |                 |
|---------------|-----------------|
| machine-translation ||
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
|slug|create-tablespace-oracle-mode|

# CREATE TABLESPACE

## Purpose

This statement is used to create a tablespace logical object, and the tablespace attributes support encryption.

## Syntax

```sql
CREATE TABLESPACE tablespace_name
    [ ENCRYPTION USING 'algorithm' ]

algorithm:
    'AES-256' | 'AES-128' | 'AES-192' | 'SM4-CBC'
```

## Parameters

|                 Parameter                 |           Description            |
|------------------------------------|-------------------------|
| tablespace_name                    | The name of the tablespace object to be created. |
| ENCRYPTION USING                   | The keyword for encryption.              |
| AES-256\|AES-128\|AES-192\|SM4-CBC | The encryption algorithm to be used.             |

## Examples

* Create a tablespace object.

  ```sql
  obclient> CREATE TABLESPACE tblspace1;
  ```

* Create a tablespace object and use encryption.

  ```sql
  -- Step 1: Set the TDE method to internal key management.
  obclient> ALTER SYSTEM SET TDE_METHOD = 'INTERNAL';

  -- Step 2: Create a key store.
  obclient> ADMINISTER KEY MANAGEMENT CREATE KEYSTORE my_keystore IDENTIFIED BY abcCBAK123;

  -- Step 3: Open the key store.
  obclient> ADMINISTER KEY MANAGEMENT SET KEYSTORE OPEN IDENTIFIED BY abcCBAK123;

  -- Step 4: Create a master key.
  obclient> ADMINISTER KEY MANAGEMENT SET KEY IDENTIFIED BY abcCBAK123;

  -- Step 5: Create an encrypted tablespace.
  obclient> CREATE TABLESPACE tblspace2 ENCRYPTION USING 'SM4-CBC';
  ```

  In the password example, `abcCBAK123` is used. Please replace it with a secure password when using it in practice.

## References

* [DROP TABLESPACE](4100.drop-tablespace-of-oracle-mode.md)
* [ALTER TABLESPACE](1110.alter-tablespace-of-oracle-mode.md)