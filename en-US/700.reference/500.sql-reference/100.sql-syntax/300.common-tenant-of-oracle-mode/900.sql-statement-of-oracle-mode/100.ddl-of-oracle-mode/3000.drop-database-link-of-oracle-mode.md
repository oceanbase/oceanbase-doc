| Description   |                 |
|---------------|-----------------|
| machine-translation ||
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
|slug|drop-database-link-oracle-mode|

# DROP DATABASE LINK

## Purpose

Deletes a database link (dblink) that has been created.

>**Note**
>
>Before you can delete a database link, you must have the `DROP DATABASE LINK` system privilege.

## Syntax

```sql
DROP DATABASE LINK dblink_name
```

## Parameters

`dblink_name` specifies the name of the database link to be deleted.

## Examples

```sql
obclient> DROP DATABASE LINK ob_dblink;
```