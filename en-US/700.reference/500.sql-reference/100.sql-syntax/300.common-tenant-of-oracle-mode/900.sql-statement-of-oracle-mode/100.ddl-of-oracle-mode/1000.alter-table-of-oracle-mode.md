| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |

# ALTER TABLE

## Purpose

This statement is used to modify the structure of an existing table, including modifying the table name and table attributes, adding columns, modifying the column name and attributes, and dropping columns.

## Syntax

```sql
ALTER TABLE table_name alter_table_action_list;

alter_table_action_list:
    alter_table_action [, alter_table_action ...]

alter_table_action:
      ADD {column_definition | (column_definition_list)}
    | ADD [CONSTRAINT [constraint_name]] UNIQUE (column_name [, column_name ]...)
    | ADD [CONSTRAINT [constraint_name]] FOREIGN KEY (column_name_list) references_clause
    | ADD [CONSTRAINT [constraint_name]] CHECK (expr)
    | ADD CONSTRAINT constraint_name PRIMARY KEY (column_name)
    | ADD CONSTRAINT constraint_name FOREIGN KEY(foreign_col_name) REFERENCES              
        reference_tbl_name(column_name);
    | ALTER INDEX index_name [VISIBLE | INVISIBLE]
    | ADD range_partition_list
    | ADD COLUMN GROUP([all columns, ]each column) [DELAYED]
    | DROP {PARTITION | SUBPARTITION} partition_name_list [UPDATE GLOBAL INDEXES]
    | DROP TABLEGROUP
    | DROP CONSTRAINT constraint_name
    | DROP PRIMARY KEY
    | DROP COLUMN column_name [, DROP COLUMN column_name ...]
    | DROP (column_name_list)
    | DROP COLUMN GROUP([all columns, ]each column)
    | {ENABLE | DISABLE} CONSTRAINT constraint_name
    | MODIFY [COLUMN] column_definition
    | MODIFY CONSTRAINT constraint_name { ENABLE | DISABLE }
    | MODIFY PRIMARY KEY (column_name_list)
    | RENAME [TO] table_name
    | RENAME COLUMN old_col_name TO new_col_name
    | RENAME { PARTITION | SUBPARITION } partition_name TO new_name
    | SET TABLEGROUP tablegroup_name
    | [SET] table_option_list
    | TRUNCATE {PARTITION | SUBPARTITION} partition_name_list [UPDATE GLOBAL INDEXES]
    | EXCHANGE PARTITION partition_name WITH TABLE non_partition_table_name INCLUDING INDEXES WITHOUT VALIDATION
    | MODIFY PARTITION partition_name ADD add_subpartition_list
    | MODIFY PARTITION BY partition_option
    | SPLIT PARTITION partition_name split_partition_option
    | [MODIFY] PARTITION BY RANGE([column_name_list]) [SIZE('size_value')] [range_partition_list]
    | DUPLICATE_SCOPE= 'none | cluster'
    | FORCE

rename_table_action_list:
    rename_table_action [, rename_table_action ...]

rename_table_action:
    table_name TO table_name

column_definition_list:
    column_definition [, column_definition ...]

column_definition:
    column_name data_type
    [GENERATED BY DEFAULT AS IDENTITY | GENERATED ALWAYS AS IDENTITY]
    [DEFAULT const_value]
    [NULL | NOT NULL] [[PRIMARY] KEY] [UNIQUE [KEY]] [COMMENT string_value] [SKIP_INDEX(skip_index_option_list)]

skip_index_option_list:
    skip_index_option [,skip_index_option ...]

skip_index_option:
    MIN_MAX
    | SUM

column_desc_list:
    column_desc [, column_desc ...]

column_desc:
     column_name [(length)] [ASC | DESC]

references_clause:
     REFERENCES table_name [ (column_name, column_name ...) ] [ON DELETE {SET NULL | CASCADE}]


index_option:
      [GLOBAL | LOCAL]
    | block_size
    | compression
    | STORING(column_name_list)
    | COMMENT string_value

table_option_list:
    table_option [ table_option ...]

table_option:
      table_group
    | block_size
    | compression
    | COMMENT string_value
    | parallel_clause
    | TABLE_MODE [=] 'table_mode_value'

parallel_clause:
    {NOPARALLEL | PARALLEL integer}

table_mode_value:
    NORMAL
    | QUEUING
    | MODERATE
    | SUPER
    | EXTREME

add_subpartition_list:
    {SUBPARTITION subpartition_name VALUES LESS THAN range_partition_expr, ...}
    | {SUBPARTITION subpartition_name VALUES list_partition_expr, ...}
  

partition_option:
      PARTITION BY HASH(column_name_list)
      [subpartition_option] hash_partition_define
    | PARTITION BY RANGE (column_name_list)
      [subpartition_option] (range_partition_list)
    | PARTITION BY LIST (column_name_list)
      [subpartition_option] (list_partition_list)

/*Template-based subpartitioning*/
subpartition_option:
      SUBPARTITION BY HASH (column_name_list) hash_subpartition_define
    | SUBPARTITION BY RANGE (column_name_list) SUBPARTITION TEMPLATE
      (range_subpartition_list)
    | SUBPARTITION BY LIST (column_name_list) SUBPARTITION TEMPLATE
      (list_subpartition_list)

/*Non-template-based subpartitioning*/
subpartition_option:
      SUBPARTITION BY HASH (column_name_list)
    | SUBPARTITION BY RANGE (column_name_list)
    | SUBPARTITION BY LIST (column_name_list)

subpartition_list:
      (hash_subpartition_list)
    | (range_subpartition_list)
    | (list_subpartition_list)

hash_partition_define:
      PARTITIONS partition_count [TABLESPACE tablespace] [compression]
    | (hash_partition_list)

hash_partition_list:
    hash_partition [, hash_partition, ...]

hash_partition:
    partition [partition_name] [subpartition_list/*Only applicable to non-template-based subpartitioning*/]

hash_subpartition_define:
      SUBPARTITIONS subpartition_count
    | SUBPARTITION TEMPLATE (hash_subpartition_list)

hash_subpartition_list:
    hash_subpartition [, hash_subpartition, ...]

hash_subpartition:
    subpartition [subpartition_name]

range_partition_list:
    range_partition [, range_partition ...]

range_partition:
    PARTITION [partition_name]
    VALUES LESS THAN {(expression_list) | (MAXVALUE)}
    [subpartition_list/*Only applicable to non-template-based subpartitioning*/]
    [ID = num] [physical_attribute_list] [compression]

range_subpartition_list:
    range_subpartition [, range_subpartition ...]

range_subpartition:
    SUBPARTITION subpartition_name
    VALUES LESS THAN {(expression_list) | MAXVALUE} [physical_attribute_list]

list_partition_list:
    list_partition [, list_partition] ...

list_partition:
    PARTITION [partition_name]
    VALUES (DEFAULT|expression_list)
    [subpartition_list/*Only applicable to non-template-based subpartitioning*/]
    [ID num] [physical_attribute_list] [compression]

list_subpartition_list:
    list_subpartition [, list_subpartition] ...

list_subpartition:
    SUBPARTITION [partition_name] VALUES (DEFAULT|expression_list) [physical_attribute_list]

expression_list:
    expression [, expression ...]

column_name_list:
    column_name [, column_name ...]

partition_name_list:
    partition_name [, partition_name ...]

partition_count | subpartition_count:
    INT_VALUE

split_partition_option:
    split_at_format
    | split_into_format

split_at_format:
    AT (value) [INTO (PARTITION split_partition_name, PARTITION [split_partition_name])]

split_into_format:
    INTO (PARTITION split_partition_name VALUES LESS THAN (value)
          [, PARTITION split_partition_name VALUES LESS THAN (value) ...]
           , PARTITION split_partition_name)
```

## Parameters

|                       Parameter                       |                Description                   |
|------------------------------------------------|----------------------------------------------------------------------------------------|
| ADD                                            | Adds a column. Currently, you cannot add a primary key column.      |
| MODIFY COLUMN                             | Modifies the attributes of a column.     |
| MODIFY CONSTRAINT                              | Modifies the status of a constraint to ENABLED or DISABLED. You can modify the status of foreign key constraints and `CHECK` constraints.    |
| DROP PRIMARY KEY                          | Drops a primary key. <main id="notice" type='explain'><h4>Note</h4><p>In Oracle mode, if the table is a parent table that contains foreign key information, you cannot drop its primary key. </p></main>|
| DROP COLUMN \| DROP (column_name_list)    | Drops a column. <main id="notice" type='notice'><h4>Notice</h4><p><ul><li>You cannot drop a primary key column. </li><li>In OceanBase Database V4.3.5, the <code>DROP COLUMN</code> statement was introduced in V4.3.5 BP1 for online DDL operations. </li></ul></p></main>|
| ADD UNIQUE                          | Adds a unique index.       |
| ADD INDEX                                  | Adds a normal index      |
| ALTER INDEX                              | Modifies the attributes of an index.      |
| VISIBLE \| INVISIBLE                     | Specifies whether the index is visible. The default value is VISIBLE. `VISIBLE` indicates visible, and `INVISIBLE` indicates invisible. |
|  FOREIGN KEY     | Adds a foreign key. If you do not specify the name of the foreign key, it will be automatically named by using the table name, `OBFK`, and the creation time. (For example, the name of the foreign key created for the `t1` table at 00:00:00 on August 1, 2021, is `t1_OBFK_1627747200000000`. ) The foreign key allows cross-table references to related data.|
| ADD PARTITION                              | Adds partitions.    |
| DROP {PARTITION \| SUBPARTITION}               | Drops partitions: <ul><li> `PARTITION`: You can drop a specified partition (including all subpartitions in the partition) or all partitions. If you drop a partition that contains subpartitions, all local indexes on the partition will be maintained. </li>   <li> `SUBPARTITION`: You can drop specified subpartitions. All local indexes on the subpartitions will be maintained. </li> </ul>   If you specify the `UPDATE GLOBAL INDEXES` clause, global indexes will be updated synchronously when you drop partitions; otherwise, global indexes on a partition table need to be in an unavailable state. Partition names are separated with commas. <br>**Notice**  To avoid errors or exceptions during the drop operation, make sure that no active transactions or queries are on the partitions to be dropped. |
| TRUNCATE {PARTITION \| SUBPARTITION}           | Truncates data in partitions: <ul><li> `PARTITION`: You can truncate a specified partition (including all subpartitions in the partition) or all partitions. If you truncate a partition that contains subpartitions, all local indexes on the partition will be maintained. </li>   <li> `SUBPARTITION`: You can truncate specified subpartitions. All local indexes on the subpartitions will be maintained. </li> </ul>   If you specify the `UPDATE GLOBAL INDEXES` clause, global indexes will be updated synchronously when you truncate partitions; otherwise, global indexes on a partition table need to be in an unavailable state. Partition names are separated with commas. <br>**Notice**  To avoid errors or exceptions during the truncation operation, make sure that no active transactions or queries are on the partitions to be truncated.       |
| RENAME \[TO\] table_name                       | Renames a table.  |
| RENAME { PARTITION \| SUBPARITION } partition_name TO new_name| Renames a partition or subpartition. `new_name` specifies the new name of the partition or subpartition (case-insensitive). The partition or subpartition renaming operation modifies the name of the corresponding primary table but does not affect the names of local indexes. You can query the `USER_TAB_PARTITIONS` or `USER_TAB_SUBPARTITIONS` view for the names of local indexes after the renaming operation to confirm the renaming result. For more information, see [Rename a partition](../../../../../300.database-object-management/200.manage-object-of-oracle-mode/200.manage-partitions-of-oracle-mode/310.rename-a-partition-of-oracle-mode.md). During the partition renaming operation, if a DML operation that holds a lock on the related partition conflicts with the partition renaming operation, the partition renaming operation will be blocked until the DML operation releases the partition lock.|
| DROP TABLEGROUP                            | Drops a table group.    |
| SET TABLEGROUP                                 | Sets the table group to which a table belongs.   |
| DROP CONSTRAINT                            | Drops a constraint.    |
| SET BLOCK_SIZE                                 | Sets the `BLOCK` size of partitions in a partitioned table.      |
| SET COMPRESSION                                | Sets the compression type for a table.     |
| SET USE_BLOOM_FILTER                           | Specifies whether to use a Bloom filter for a table.       |
| SET COMMENT                                    | Sets the comment on a table.    |
| SET PROGRESSIVE_MERGE_NUM                      | Sets the number of progressive merges. The value range is \[0,100\].   |
| parallel_clause                                | Specifies the degree of parallelism at the table level: <ul><li> `NOPARALLEL`: The degree of parallelism is `1`. This is the default value. </li>   <li> `PARALLEL integer`: The degree of parallelism is specified, and `integer` must be greater than or equal to `1`. </li></ul>  **Notice**  The priority order among the parameters that specify the degree of parallelism is as follows: hint > `ALTER SESSION` > table level.     |
| {ENABLE \| DISABLE} CONSTRAINT constraint_name | Modifies the status of a constraint. You can enable or disable foreign key constraints and `CHECK` constraints.         |
| MODIFY PRIMARY KEY                             |  Modifies a primary key.         |
| ADD COLUMN GROUP([all columns, ]each column)  | Changes a table into a columnstore table. The specific meanings are as follows: <ul><li><code>ADD COLUMN GROUP(all columns, each column)</code>: Changes a table into a rowstore-columnstore hybrid table.</li><li><code>ADD COLUMN GROUP(each column)</code>: Changes a table into a columnstore table.</li><li>`DELAYED`: You can specify this option to asynchronously change a rowstore table into a columnstore table. After the command is executed, the storage format in the table definition is changed, but the actual format change of data in the rowstore table will be performed when a major compaction is triggered. This operation does not block current DML operations. It is an online DDL operation. If you do not specify the `DELAYED` option, by default, a major compaction is performed offline to change the rowstore table into a columnstore table. <main id="notice" type='notice'><h4>Notice</h4><p><ul><li>Currently, you can specify the <code>DELAYED</code> option only to change a rowstore table into a columnstore table (<code>each column</code>) and to change a rowstore table into a rowstore-columnstore hybrid table (<code>all columns, each column</code>). </li><li>After you specify the `DELAYED` option to change a rowstore table into a columnstore table, the data storage format will not be actually changed until a major compaction is triggered. Therefore, the query performance may not meet the expectation during this period. </li></ul></p></main></li></ul>|
| DROP COLUMN GROUP([all columns, ]each column) | Removes the storage format from a table. The specific meanings are as follows: <ul><li><code>DROP COLUMN GROUP(all columns, each column)</code>: Removes the rowstore-columnstore hybrid format from a table. </li><li><code>DROP COLUMN GROUP(all columns)</code>: Removes the rowstore format from a table. </li><li><code>DROP COLUMN GROUP(each column)</code>: Removes the columnstore format from a table. </li></ul>|
| SKIP_INDEX | Modifies the skip index attribute of a column. The valid values are as follows: <ul><li><code>MIN_MAX</code>: This is the most general aggregate data type of skip index. It stores the maximum value, minimum value, and null count of the indexed column at the index node. The data of this type can accelerate filter pushdown and `MIN/MAX` aggregate pushdown. </li><li> <code>SUM</code>: It is used to accelerate the pushdown of the `SUM` aggregate for numeric data. </li></ul>  <main id="notice" type='notice'><h4>Notice</h4><p><ul><li>Columns of JSON or spatial data types do not support skip index creation. </li><li>Generated columns do not support skip index creation. </li></ul></p></main>|
| EXCHANGE PARTITION partition_name WITH TABLE non_partition_table_name INCLUDING INDEXES WITHOUT VALIDATION | Specifies a partition exchange. In this statement, `partition_name` is the name of the partition in the partitioned table, and `non_partition_table_name` is the name of the non-partitioned table. For more information, see [Partition exchange](../../../../../300.database-object-management/200.manage-object-of-oracle-mode/200.manage-partitions-of-oracle-mode/1000.exchange-partition-of-oracle-mode.md).|
| TABLE_MODE | Optional. Specifies the threshold and strategy for triggering a major compaction. For more information, see [table_mode_value](#table_mode_value). |
| MODIFY PARTITION partition_name ADD add_subpartition_list | Adds a new subpartition. <main id="notice" type='notice'><h4>Note</h4><p>Adding new subpartitions is not supported for scenarios where the subpartition type is Hash.</p></main> |
| MODIFY PARTITION BY |  Modifies the partitioning rule of a table. For more information, see [Modify partitioning rule](../../../../../300.database-object-management/200.manage-object-of-oracle-mode/200.manage-partitions-of-oracle-mode/300.modify-partition-rules-of-oracle-mode.md). |
| SPLIT PARTITION partition_name split_partition_option |  Manually splits a partition. The `split_at_format` and `split_into_format` options are supported. For more information, see [split_partition_option](#split_partition_option). |
| [MODIFY] PARTITION BY RANGE([column_name_list]) [SIZE('size_value')] [range_partition_list] |  Modifies the attributes of a range-partitioned table. For more information, see [Automatic partition splitting](../../../../../300.database-object-management/200.manage-object-of-oracle-mode/200.manage-partitions-of-oracle-mode/250.partition-splitting-of-oracle-mode/300.automatic-partition-splitting-of-oracle-mode.md). |
| DUPLICATE_SCOPE |  Changes the duplication scope of a replica table. The valid values are as follows: <ul><li>`none`: It indicates to convert the replica table into a normal table. </li><li>`cluster`: It indicates to convert the normal table into a replica table. </li></ul> For more information, see [Change the duplication scope](../../../../../300.database-object-management/200.manage-object-of-oracle-mode/100.manage-tables-of-oracle-mode/650.change-duplicate-scope-of-oracle-mode.md). |
| FORCE |  Forces the dropping of abandoned columns. <main id="notice" type='explain'><h4>Note</h4><p><ul><li>The feature of abandoning columns is supported starting from OceanBase Database V4.3.5 and was introduced in V4.3.5 BP1. </li><li>When some columns are dropped, the abandoned columns still occupy physical storage space even if they are not used. If you want to remove these abandoned columns and the related storage space, you must perform the column abandonment operation. </li></ul></p></main>|

### table_mode_value

<main id="notice" type='explain'>
  <h4>Note</h4>
  <p>Except for the <code>NORMAL</code> mode, all the <code>TABLE_MODE</code> modes listed below represent <code>QUEUING</code> tables. A <code>QUEUING</code> table is the most basic table type. The other modes listed below represent tables that use more aggressive merge strategies. </p>
</main>

* `NORMAL`: Normal. In this mode, the probability of a minor compaction triggered after data is dumped is very low.
* `QUEUING`: Low. In this mode, the probability of a minor compaction triggered after data is dumped is low.
* `MODERATE`: Moderate. In this mode, the probability of a minor compaction triggered after data is dumped is moderate.
* `SUPER`: High. In this mode, the probability of a minor compaction triggered after data is dumped is high.
* `EXTREME`: Extreme. In this mode, the probability of a minor compaction triggered after data is dumped is relatively high.

For more information about minor compactions, see [Adaptive minor compactions](../../../../../200.system-management/500.manage-data-storage/200.merge-management/320.adaptive-compaction.md).

### split_partition_option

* `SPLIT PARTITION partition_name AT (value) [INTO (PARTITION split_partition_name1, PARTITION [split_partition_name2])]`: When you split a partition using this syntax, the system splits the source partition into two partitions based on the `value` specified. You can also use the `INTO` clause to name the split partitions.

* `SPLIT PARTITION partition_name INTO (PARTITION split_partition_name VALUES LESS THAN (value) [, PARTITION split_partition_name VALUES LESS THAN (value) ...], PARTITION split_partition_name)`: When you split a partition using this syntax, the system can split the partition into multiple partitions. The ranges of `value` specified in the partition split definition must be the same as that of the source partition and in ascending order (the `value` of the last partition cannot be specified).

For more information about how to manually split partitions, see [Split a partition](../../../../../300.database-object-management/200.manage-object-of-oracle-mode/200.manage-partitions-of-oracle-mode/250.partition-splitting-of-oracle-mode/200.manual-partition-splitting-of-oracle-mode.md).

## Examples

* Modify the field type of `col1` in the `tbl1` table.

  ```shell
  obclient> CREATE TABLE tbl1(col1 VARCHAR(3));
  Query OK, 0 rows affected

  obclient> ALTER TABLE tbl1 MODIFY col1 CHAR(10);
  Query OK, 0 rows affected

  obclient> DESCRIBE tbl1;
  +-------+----------+------+-----+---------+-------+
  | FIELD | TYPE     | NULL | KEY | DEFAULT | EXTRA |
  +-------+----------+------+-----+---------+-------+
  | COL1  | CHAR(10) | YES  | NULL | NULL    | NULL  |
  +-------+----------+------+-----+---------+-------+
  1 row in set
  ```

* Rename the `col1` field to `col2` in the `tbl1` table.

  ```shell
  obclient> ALTER TABLE tbl1 RENAME COLUMN col1 TO col2;
  Query OK, 0 rows affected

  obclient> DESCRIBE tbl1;
  +-------+-------------+------+-----+---------+-------+
  | FIELD | TYPE        | NULL | KEY | DEFAULT | EXTRA |
  +-------+-------------+------+-----+---------+-------+
  | COL2  | VARCHAR2(10) | YES  | NULL | NULL    | NULL  |
  +-------+-------------+------+-----+---------+-------+
  1 row in set
  ```

* Add and drop columns.

  * Create a table named `tbl2`.

    ```shell
    obclient> CREATE TABLE tbl2 (col1 NUMBER(30) PRIMARY KEY,col2 VARCHAR(50));
    Query OK, 0 rows affected
    ```

  * Add a column named `col3` to the `tbl2` table.

    ```shell
    obclient> ALTER TABLE tbl2 ADD col3 NUMBER(30);
    Query OK, 0 rows affected

    obclient> DESCRIBE tbl2;
    +-------+--------------+------+-----+---------+-------+
    | FIELD | TYPE         | NULL | KEY | DEFAULT | EXTRA |
    +-------+--------------+------+-----+---------+-------+
    | COL1  | NUMBER(30)   | NO   | PRI | NULL    | NULL  |
    | COL2  | VARCHAR2(50) | YES  | NULL | NULL    | NULL  |
    | COL3  | NUMBER(30)   | YES  | NULL | NULL    | NULL  |
    +-------+--------------+------+-----+---------+-------+
    3 rows in set
    ```

  * Drop the `col3` column from the `tbl2` table.

    ```shell
    obclient> ALTER TABLE tbl2 DROP COLUMN col3;
    Query OK, 0 rows affected

    obclient> DESCRIBE tbl2;
    +-------+--------------+------+-----+---------+-------+
    | FIELD | TYPE         | NULL | KEY | DEFAULT | EXTRA |
    +-------+--------------+------+-----+---------+-------+
    | COL1  | NUMBER(30)   | NO   | PRI | NULL    | NULL  |
    | COL2  | VARCHAR2(50) | YES  | NULL | NULL    | NULL  |
    +-------+--------------+------+-----+---------+-------+
    2 rows in set
    ```

  * Create a unique index on the `tbl2` table.

    ```shell
    obclient> CREATE TABLE tbl2 (col1 NUMBER(30) PRIMARY KEY,col2 VARCHAR(50), col3 INT);
    Query OK, 0 rows affected

    obclient> ALTER TABLE tbl2 ADD CONSTRAINT constraint_TBL2 UNIQUE (col2, col3);
    Query OK, 0 rows affected

    obclient [SYS]> DESC tbl2;
    +-------+--------------+------+-----+---------+-------+
    | FIELD | TYPE         | NULL | KEY | DEFAULT | EXTRA |
    +-------+--------------+------+-----+---------+-------+
    | COL1  | NUMBER(30)   | NO   | PRI | NULL    | NULL  |
    | COL2  | VARCHAR2(50) | YES  | MUL | NULL    | NULL  |
    | COL3  | NUMBER(38)   | YES  | NULL | NULL    | NULL  |
    +-------+--------------+------+-----+---------+-------+
    3 rows in set

    obclient> INSERT INTO tbl2 VALUES('1','2','2');
    Query OK, 1 row affected

    obclient> INSERT INTO tbl2 VALUES('2','2','2');
    OBE-00001: unique constraint '2-2' for key 'CONSTRAINT_TBL2' violated

    obclient> INSERT INTO tbl2 VALUES('2','3','2');
    Query OK, 1 row affected
    ```

* Add a foreign key to the `ref_t2` table. When a `DELETE` operation affects a row that matches the child table, set the value of the primary key in the parent table to `NULL`.

  ```shell
  obclient> CREATE TABLE ref_t1(c1 INT PRIMARY KEY,C2 INT);
  Query OK, 0 rows affected

  obclient> CREATE TABLE ref_t2(c1 INT PRIMARY KEY,C2 INT);
  Query OK, 0 rows affected

  obclient> ALTER TABLE ref_t2 ADD CONSTRAINT fk1 FOREIGN KEY (c2) REFERENCES ref_t1(c1) ON DELETE SET NULL;
  Query OK, 0 row affected
  ```

* Add a subpartition `p1_r4` to the `p1` partition of the non-template-based subpartitioned table `tbl3`.

  ```shell
  obclient> ALTER TABLE tbl3 MODIFY PARTITION p1 ADD SUBPARTITION p1_r4 VALUES LESS THAN(2022);
  Query OK, 0 rows affected
  ```

* Drop the `p3_r3` subpartition from the `tbl3` table.

  ```shell
  obclient> ALTER TABLE tbl3 DROP SUBPARTITION p2_r3;
  Query OK, 0 rows affected
  ```

* Add a partition `p4` to the `tbl3` table. You must specify both the definition of the partition and the definitions of subpartitions within the partition.

  ```shell
  obclient> ALTER TABLE tbl3 ADD PARTITION p4 VALUES LESS THAN (400)
  (
    SUBPARTITION p4_r1 VALUES LESS THAN (2019),
    SUBPARTITION p4_r2 VALUES LESS THAN (2020),
    SUBPARTITION p4_r3 VALUES LESS THAN (2021)
  );
  Query OK, 0 rows affected
  ```

* Add a partition `p3` to the template-based subpartitioned table `tbl4`. You only need to specify the definition of the partition. The definitions of subpartitions will be automatically filled according to the template.

  ```shell
  obclient> CREATE TABLE tbl4(col1 INT, col2 INT, PRIMARY KEY(col1,col2))
       PARTITION BY RANGE(col1)
       SUBPARTITION BY RANGE(col2)
       SUBPARTITION TEMPLATE
       (
       SUBPARTITION p0 VALUES LESS THAN (50),
       SUBPARTITION p1 VALUES LESS THAN (100)
       )
       (
       PARTITION p0 VALUES LESS THAN (100),
       PARTITION p1 VALUES LESS THAN (200),
       PARTITION p2 VALUES LESS THAN (300)
       );
  Query OK, 0 rows affected

  obclient> ALTER TABLE tbl4 ADD PARTITION p3 VALUES LESS THAN (400);
  Query OK, 0 rows affected
  ```

* Modify the parallelism of the `tbl5` table to `3`.

  ```shell
  obclient> CREATE TABLE tbl5(col1 int primary key, col2 int) PARALLEL 5;
  Query OK, 0 rows affected

  obclient> ALTER TABLE tbl5 PARALLEL 3;
  Query OK, 0 rows affected
  ```

  or:

  ```shell
  obclient> CREATE TABLE tbl5(col1 int primary key, col2 int) PARALLEL 5;
  Query OK, 0 rows affected
  obclient> ALTER /*+ parallel(3) */ TABLE tbl5;
  ```

* Modify the status of the foreign key constraint.

  ```shell
  obclient> CREATE TABLE MMS_GROUPUSER (
    "ID" VARCHAR2(254 BYTE) NOT NULL,
    "GROUPID" VARCHAR2(254 BYTE),
    "USERID" VARCHAR2(254 BYTE),
    CONSTRAINT "PK_MMS_GROUPUSER" PRIMARY KEY ("ID"),
    CONSTRAINT "FK_MMS_GROUPUSER_02" FOREIGN KEY ("GROUPID") REFERENCES MMS_GROUPUSER ("ID") ON DELETE CASCADE DISABLE
  );
  Query OK, 0 rows affected

  obclient> SELECT CONSTRAINT_NAME,CONSTRAINT_TYPE,TABLE_NAME,STATUS FROM user_constraints WHERE CONSTRAINT_NAME LIKE 'FK_MMS_GROUPUSE%';
  +---------------------+-----------------+---------------+----------+
  | CONSTRAINT_NAME     | CONSTRAINT_TYPE | TABLE_NAME    | STATUS   |
  +---------------------+-----------------+---------------+----------+
  | FK_MMS_GROUPUSER_02 | R               | MMS_GROUPUSER | DISABLED |
  +---------------------+-----------------+---------------+----------+
  1 row in set

  obclient> ALTER TABLE MMS_GROUPUSER ENABLE CONSTRAINT FK_MMS_GROUPUSER_02;
  Query OK, 0 rows affected

  obclient> SELECT CONSTRAINT_NAME,CONSTRAINT_TYPE,TABLE_NAME,STATUS FROM user_constraints WHERE CONSTRAINT_NAME LIKE 'FK_MMS_GROUPUSE%';
  +---------------------+-----------------+---------------+---------+
  | CONSTRAINT_NAME     | CONSTRAINT_TYPE | TABLE_NAME    | STATUS  |
  +---------------------+-----------------+---------------+---------+
  | FK_MMS_GROUPUSER_02 | R               | MMS_GROUPUSER | ENABLED |
  +---------------------+-----------------+---------------+---------+
  1 row in set
  ```

* Truncate the `M202001` and `M202002` partitions of the partitioned table `tbl6`.

  ```shell
  obclient> CREATE TABLE tbl6 (log_id number NOT NULL,log_value varchar2(50),log_date date NOT NULL DEFAULT sysdate)
  PARTITION BY RANGE(log_date)
  (
      PARTITION M202001 VALUES LESS THAN(TO_DATE('2020/02/01','YYYY/MM/DD'))
      , PARTITION M202002 VALUES LESS THAN(TO_DATE('2020/03/01','YYYY/MM/DD'))
      , PARTITION M202003 VALUES LESS THAN(TO_DATE('2020/04/01','YYYY/MM/DD'))
      , PARTITION M202004 VALUES LESS THAN(TO_DATE('2020/05/01','YYYY/MM/DD'))
      , PARTITION M202005 VALUES LESS THAN(TO_DATE('2020/06/01','YYYY/MM/DD'))
      , PARTITION MMAX VALUES LESS THAN (MAXVALUE)
  );
  Query OK, 0 rows affected

  obclient> ALTER TABLE tbl6 TRUNCATE PARTITION M202001, M202002 UPDATE GLOBAL INDEXES;
  Query OK, 0 rows affected
  ```

* Drop the `CHECK` constraint named `tbl7_equal_check1` from the `tbl7` table.

  ```shell
  obclient> CREATE TABLE tbl7 (col1 INT, col2 INT, col3 INT,CONSTRAINT tbl7_equal_check1 CHECK(col2 = col3 * 2) ENABLE VALIDATE);
  Query OK, 0 rows affected

  obclient> SELECT CONSTRAINT_NAME,CONSTRAINT_TYPE,TABLE_NAME,STATUS FROM user_constraints WHERE TABLE_NAME LIKE 'TBL%';
  +-------------------+-----------------+------------+---------+
  | CONSTRAINT_NAME   | CONSTRAINT_TYPE | TABLE_NAME | STATUS  |
  +-------------------+-----------------+------------+---------+
  | TBL8_EQUAL_CHECK1 | C               | TBL8       | ENABLED |
  | TBL7_EQUAL_CHECK1 | C               | TBL7       | ENABLED |
  +-------------------+-----------------+------------+---------+
  2 rows in set

  obclient> ALTER TABLE tbl7 DROP CONSTRAINT tbl7_equal_check1;
  Query OK, 0 rows affected

  obclient> SELECT CONSTRAINT_NAME,CONSTRAINT_TYPE,TABLE_NAME,STATUS FROM user_constraints WHERE TABLE_NAME LIKE 'TBL%';
  +-------------------+-----------------+------------+---------+
  | CONSTRAINT_NAME   | CONSTRAINT_TYPE | TABLE_NAME | STATUS  |
  +-------------------+-----------------+------------+---------+
  | TBL8_EQUAL_CHECK1 | C               | TBL8       | ENABLED |
  +-------------------+-----------------+------------+---------+
  1 row in set
  ```

* Move the `tbl8` table from the `tblgroup1` table group to the `tblgroup2` table group.

  ```shell
  obclient> SHOW TABLEGROUPS;
  +-----------------+------------+---------------+
  | TABLEGROUP_NAME | TABLE_NAME | DATABASE_NAME |
  +-----------------+------------+---------------+
  | TBLGROUP1             | TBL8         | SYS           |
  | TBLGROUP2             | NULL       | NULL          |
  | oceanbase       | NULL       | NULL          |
  +-----------------+------------+---------------+
  3 rows in set

  obclient> ALTER TABLE tbl8 SET TABLEGROUP tblgroup2;
  Query OK, 0 rows affected

  obclient> SHOW TABLEGROUPS;
  +-----------------+------------+---------------+
  | TABLEGROUP_NAME | TABLE_NAME | DATABASE_NAME |
  +-----------------+------------+---------------+
  | TBLGROUP1             | NULL       | NULL          |
  | TBLGROUP2             | TBL8         | SYS           |
  | oceanbase       | NULL       | NULL          |
  +-----------------+------------+---------------+
  3 rows in set
  ```

* Add a foreign key constraint named `cons_fk1` to the `primary_table` table.

  ```shell
  obclient> CREATE TABLE primary_table (id NUMBER PRIMARY KEY, names VARCHAR(100) NOT NULL, foreign_col NUMBER);
  Query OK, 0 rows affected
  obclient> CREATE TABLE reference_table (id NUMBER PRIMARY key, comments VARCHAR2(100) NOT NULL);
  Query OK, 0 rows affected
  obclient> ALTER TABLE primary_table ADD CONSTRAINT cons_fk1 FOREIGN KEY(foreign_col) REFERENCES reference_table(id);
  Query OK, 0 rows affected
  ```

* Add a primary key constraint named `tbl1_pk` to the `tbl9` table.

  ```shell
  obclient> CREATE TABLE tbl9 (col1 NUMBER, col2 INT,col3 VARCHAR2(100));
  Query OK, 0 rows affected

  obclient> ALTER TABLE tbl9 ADD CONSTRAINT tbl1_pk PRIMARY KEY (col1);
  Query OK, 0 rows affected
  ```

* Modify the primary key of the `tbl9` table to the `col2` field.

  ```shell
  obclient> ALTER TABLE tbl9 MODIFY PRIMARY KEY(col2);
  Query OK, 0 rows affected
  ```

* Drop the primary key from the `tbl9` table.

  ```shell
  obclient> ALTER TABLE tbl9 DROP PRIMARY KEY;
  Query OK, 0 rows affected
  ```

* Rename partitions and subpartitions.

  ```shell
  /* Create a subpartitioned table named range_range_table and create local indexes on the col1 field */
  CREATE TABLE range_range_table(col1 INT, col2 INT, col3 INT)
      PARTITION BY RANGE(col1)
      SUBPARTITION BY RANGE(col2)
         (PARTITION p0 VALUES LESS THAN(100)
             (SUBPARTITION sp0 VALUES LESS THAN(100),
              SUBPARTITION sp1 VALUES LESS THAN(200)
             ),
          PARTITION p1 VALUES LESS THAN(200)
             (SUBPARTITION sp2 VALUES LESS THAN(100),
              SUBPARTITION sp3 VALUES LESS THAN(200),
              SUBPARTITION sp4 VALUES LESS THAN(300)
             )
          );

  CREATE INDEX local_idx_for_range_range_tb ON range_range_table (col1) LOCAL;

  /* Rename partitions. The modifications to the partition names do not affect the local indexes. */

  obclient> SELECT partition_name FROM SYS.USER_TAB_PARTITIONS WHERE table_name = 'RANGE_RANGE_TABLE';
  +----------------+
  | PARTITION_NAME |
  +----------------+
  | P0             |
  | P1             |
  +----------------+
  2 rows in set

  obclient> ALTER TABLE range_range_table RENAME PARTITION p0 TO p10;
  Query OK, 0 rows affected

  obclient> SELECT partition_name FROM SYS.USER_TAB_PARTITIONS WHERE table_name = 'RANGE_RANGE_TABLE';
  +----------------+
  | PARTITION_NAME |
  +----------------+
  | P10            |
  | P1             |
  +----------------+
  2 rows in set

  obclient> SELECT partition_name FROM SYS.USER_IND_PARTITIONS WHERE index_name = 'LOCAL_IDX_FOR_RANGE_RANGE_TB';
  +----------------+
  | PARTITION_NAME |
  +----------------+
  | P0             |
  | P1             |
  +----------------+
  2 rows in set

  /* Rename subpartitions. The modifications to the subpartition names do not affect the local indexes. */
  obclient> SELECT partition_name, subpartition_name FROM SYS.USER_TAB_SUBPARTITIONS WHERE table_name = 'RANGE_RANGE_TABLE';
  +----------------+-------------------+
  | PARTITION_NAME | SUBPARTITION_NAME |
  +----------------+-------------------+
  | P10            | SP0               |
  | P10            | SP1               |
  | P1             | SP2               |
  | P1             | SP3               |
  | P1             | SP4               |
  +----------------+-------------------+
  5 rows in set

  obclient> ALTER TABLE range_range_table RENAME SUBPARTITION sp0 TO sp10;
  Query OK, 0 rows affected

  obclient> SELECT partition_name, subpartition_name FROM SYS.USER_TAB_SUBPARTITIONS WHERE table_name = 'RANGE_RANGE_TABLE';
  +----------------+-------------------+
  | PARTITION_NAME | SUBPARTITION_NAME |
  +----------------+-------------------+
  | P10            | SP10              |
  | P10            | SP1               |
  | P1             | SP2               |
  | P1             | SP3               |
  | P1             | SP4               |
  +----------------+-------------------+
  5 rows in set

  obclient> SELECT partition_name, subpartition_name FROM SYS.USER_IND_SUBPARTITIONS WHERE index_name = 'LOCAL_IDX_FOR_RANGE_RANGE_TB';
  +----------------+-------------------+
  | PARTITION_NAME | SUBPARTITION_NAME |
  +----------------+-------------------+
  | P0             | SP0               |
  | P0             | SP1               |
  | P1             | SP2               |
  | P1             | SP3               |
  | P1             | SP4               |
  +----------------+-------------------+
  5 rows in set
  ```

* Modify the column storage attribute of a table.

  1. Use the following SQL statement to create a table named `tbl1`.

      ```shell
      CREATE TABLE tbl1 (col1 INT PRIMARY KEY, col2 VARCHAR(50));
      ```

  2. Change the `tbl1` table to a rowstore-columnstore hybrid table and then drop the columnstore attribute.

      ```shell
      ALTER TABLE tbl1 ADD COLUMN GROUP(all columns, each column);
      ```

      ```shell
      ALTER TABLE tbl1 DROP COLUMN GROUP(all columns, each column);
      ```

  3. Change the `tbl1` table to a columnstore table and then drop the columnstore attribute.

      ```shell
      ALTER TABLE tbl1 ADD COLUMN GROUP(each column);
      ```

      ```shell
      ALTER TABLE tbl1 DROP COLUMN GROUP(each column);
      ```

* Modify the skip index attribute of a column.

  1. Use the following SQL statement to create a table named `test_skidx`.

      ```shell
      CREATE TABLE test_skidx(
          col1 NUMBER SKIP_INDEX(MIN_MAX, SUM),
          col2 FLOAT SKIP_INDEX(MIN_MAX),
          col3 VARCHAR2(1024) SKIP_INDEX(MIN_MAX),
          col4 CHAR(10)
        );
      ```

  2. Modify the skip index attribute of the `col2` field in the `test_skidx` table to `SUM`.

      ```shell
      ALTER TABLE test_skidx MODIFY col2 FLOAT SKIP_INDEX(SUM);
      ```

  3. Add the `MIN_MAX` skip index attribute to the `col4` field in the `test_skidx` table after the table is created.

      ```shell
      ALTER TABLE test_skidx MODIFY col4 CHAR(10) SKIP_INDEX(MIN_MAX);
      ```

  4. Drop the skip index attribute from the `col1` field in the `test_skidx` table.

      ```shell
      ALTER TABLE test_skidx MODIFY col1 NUMBER SKIP_INDEX();
      ```

## References

[Modify a table](../../../../../300.database-object-management/200.manage-object-of-oracle-mode/100.manage-tables-of-oracle-mode/600.change-table-of-oracle-mode.md)