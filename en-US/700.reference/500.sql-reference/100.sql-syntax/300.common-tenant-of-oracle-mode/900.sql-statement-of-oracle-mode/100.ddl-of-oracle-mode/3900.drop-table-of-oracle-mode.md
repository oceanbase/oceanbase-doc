| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
|machine-translation| en          |
|slug|drop-table-oracle-mode|

# DROP TABLE

## Purpose

This statement is used to delete a table from the database.

## Privilege requirements

To execute the `DROP TABLE` statement, the current user must have the `DROP` system privilege. For more information about OceanBase Database privileges, see [Privilege classification in Oracle-compatible mode](../../../../../../600.manage/500.security-and-permissions/300.access-control/200.user-and-permission/300.permission-of-oracle-mode/000.permission-classification-of-oracle-mode.md).

## Syntax

```sql
DROP TABLE table_name [CASCADE CONSTRAINTS] [PURGE];
```

## Parameters

|         Parameter          |            Description             |
|---------------------|---------------------------|
| table_name          | Specifies the name of the table to be deleted.                 |
| CASCADE CONSTRAINTS | Cascades the deletion to constraints associated with `table_name`. |
| PURGE               | Permanently deletes the table (without placing it in the recycle bin).            |

## Examples

Delete the `tbl1` table.

```sql
obclient> DROP TABLE tbl1;
Query OK, 0 rows affected
```

## References

[Delete a table](../../../../../300.database-object-management/200.manage-object-of-oracle-mode/100.manage-tables-of-oracle-mode/800.delete-a-table-of-oracle-mode.md)