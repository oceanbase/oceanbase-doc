| Description   |                 |
|---------------|-----------------|
| machine-translation ||
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
|slug|foreign-key-operations-oracle-mode|

# Foreign key operations

OceanBase Database supports foreign key operations, including the addition and deletion of foreign keys.

## Add a foreign key constraint

You can run the following statement to add a foreign key constraint:

```sql
ALTER TABLE table_name ADD CONSTRAINT constraint_name
   FOREIGN KEY(foreign_columns) REFERENCES reference_table(column_name);
```

Here is an example of adding a foreign key constraint:

```sql
obclient> CREATE TABLE primary_table (id NUMBER PRIMARY KEY, names VARCHAR(100) NOT NULL, foreign_col NUMBER);
Query OK, 0 rows affected

obclient> CREATE TABLE reference_table (id NUMBER PRIMARY key, comments VARCHAR2(100) NOT NULL);
Query OK, 0 rows affected

obclient> ALTER TABLE primary_table ADD CONSTRAINT cons_fk1 FOREIGN KEY(foreign_col) REFERENCES reference_table(id);
Query OK, 0 rows affected
```

## Delete a foreign key constraint

You can run the following statement to delete a foreign key constraint:

```sql
ALTER TABLE table_name DROP CONSTRAINT constraint_name;
```

Here is an example of deleting a foreign key constraint:

```sql
obclient> ALTER TABLE primary_table DROP CONSTRAINT cons_fk1;
Query OK, 0 rows affected
```