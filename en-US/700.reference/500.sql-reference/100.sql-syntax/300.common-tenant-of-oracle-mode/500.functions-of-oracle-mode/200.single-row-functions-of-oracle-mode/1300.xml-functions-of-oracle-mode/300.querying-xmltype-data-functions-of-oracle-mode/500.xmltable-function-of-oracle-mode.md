| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
|machine-translation| en          |
|slug|xmltable-function-oracle-mode|

# XMLTABLE

## Purpose

This function maps the results returned by an XPath query to rows and columns in a relational table. You can use SQL queries to retrieve the results of a function as a virtual relational table.

The key capabilities of the `XMLTABLE` function include:

* Data extraction: By specifying an appropriate XPath expression in the `XMLTABLE` function, you can precisely extract the required data from complex XML documents.

* Data structuring: `XMLTABLE` can map specific parts of XML data to rows and columns in a relational table, converting semi-structured XML data into a structured table format. This makes it easier to perform SQL queries and analysis.

* Table conversion: You can use the structured data generated by `XMLTABLE` to create virtual tables and further process these tables by generating new tables or views. This allows you to store and manipulate the processed data within the database.

## Syntax

```sql
XMLTABLE( [ XMLnamespaces_clause , ] XQuery_string XMLTABLE_options )

XMLnamespaces_clause:
 XMLNAMESPACES( { string AS identifier } | { DEFAULT string } [, { string AS identifier } | { DEFAULT string } ]...)

XMLTABLE_options:
  [ XML_passing_clause ]
  [ COLUMNS XML_table_column [, XML_table_column]...]

XML_passing_clause:
  PASSING [ BY VALUE ] expr [ AS identifier ]

XML_table_column:
  column { FOR ORDINALITY
          | { datatype | XMLTYPE } [ PATH string ] [ DEFAULT expr ]
  }
```

## Parameters

| **Field** | **Description** |
| -------- | -------- |
| XMLnamespaces_clause | Defines namespace prefixes and URIs for referencing elements and attributes with namespaces in XML queries. This clause contains a set of XML namespace declarations referenced by XPath expressions in the `PATH` subclause of `XML_table_column`, which compute the columns of the entire XMLTable function. If you want to use qualified names in the `PATH` expressions of the `COLUMNS` subclause, you need to specify this clause. |
| XQuery_string | Specifies an XQuery expression for selecting data to be converted from XML data. XQuery is an XML query language for extracting data and implementing complex query operations from XML documents. This parameter allows you to define instructions for extracting specific elements or attributes from XML data using XQuery syntax. <main id="notice" type='notice'><h4>Notice</h4><p>OceanBase Database does not currently support XQuery. </p></main> |
| XML_passing_clause | Specifies the XML data to be passed to the XQuery expression using the PASSING keyword. You can specify data passing by value and the alias for the data. Details are as follows: <ul><li>PASSING: Indicates the start of the XML_passing_clause. The content of the PASSING clause serves as the input for XMLTABLE.</li><li>BY VALUE: An optional keyword indicating that data will be passed by value rather than by reference. In most cases, this option can be omitted as passing by value is the default behavior.</li><li>expr: An expression that returns an XMLType or SQL scalar data type instance, whose result is the XML data to be passed to the XQuery. This expression is typically a column name, variable, or more complex expression that returns XML type data. The `PASSING` clause currently only supports specifying one `expr` in the current version.</li><li>AS identifier: An optional alias (identifier) for the expr parameter result. In XQuery expressions, you can use this alias to reference the passed data. |
| COLUMNS | An optional COLUMNS clause defines the columns of the virtual table created by XMLTable. If you omit the COLUMNS clause, XMLTable returns a single row with a pseudo-column named COLUMN_VALUE of type XMLType. |
| XML_table_column | Defines the columns to be generated, which can be regular data type columns or XMLTYPE columns. You can also specify the XPath path and default value for the column values. FOR ORDINALITY is used to generate row numbers. Details are as follows: <ul><li>FOR ORDINALITY: Specifies that this column will be the generated row number column. Only one FOR ORDINALITY clause can be specified. It is created as a NUMBER column.</li><li> datatype or XMLTYPE: In addition to the FOR ORDINALITY column, you must specify the data type for each result column, which can be XMLType or any other data type. If the column's data type is XMLType, you need to specify the XMLTYPE clause. Referencing XMLType data allows you to define other columns whose path-located nodes are outside the nodes located by the PATH expression of the column. If the column's data type is any other data type, you need to specify the data type. <ul><li>PATH string: An optional string specifying the part of the XQuery result processed by the XQuery expression string to be used as the column content. If omitted, it is assumed to be the XQuery expression column. For example: <code>XMLTable(... COLUMNS xyz)</code>.</li><li>DEFAULT expr: An optional value to use when the result of the PATH expression is an empty sequence. The expr is an XQuery expression for generating the default value.</li></ul></li></ul>

## Return type

The return type depends on the data types of the columns specified in the XMLTable function.

## Examples

Use the `XMLTABLE` function to convert XML data into an SQL table containing employee IDs, names, positions, and salaries. Here's how:

1. Define the default namespace as `http://www.example.com/employees` to match the namespace in the XML data.

2. Define the XQuery expression as `'/employees/employee'` to select all `employee` elements.

3. Use the `PASSING` clause to specify the XML document to be passed to the XQuery expression. In this example, the entire XML document is encapsulated using the XMLTYPE constructor. XMLTYPE converts the XML data from string format into an XML type that the database can process.

4. In the `COLUMNS` clause, define four columns to map the extracted data from the XML. The mapping is as follows:

   * The `employee_id` column maps to the `emp_id` element under each `employee` element.
   * The `employee_name` column maps to the `name` element under each `employee` element.
   * The `position` column maps to the `position` element under each `employee` element.
   * The `salary` column maps to the `salary` element under each `employee` element.

   The PATH clause after each column defines the source of the corresponding value.

```shell
SELECT *
FROM XMLTABLE(
       XMLNAMESPACES(DEFAULT 'http://www.example.com/employees'),
       '/employees/employee'
       PASSING XMLTYPE('
          <employees xmlns="http://www.example.com/employees">
               <employee>
                    <emp_id>1</emp_id>
                    <name>John Doe</name>
                    <position>Developer</position>
                    <salary>5000</salary>
               </employee>
               <employee>
                    <emp_id>2</emp_id>
                    <name>Jane Smith</name>
                    <position>Manager</position>
                    <salary>6500</salary>
               </employee>
          </employees>'
     )
     COLUMNS
          employee_id    INT           PATH 'emp_id',
          employee_name  VARCHAR2(100) PATH 'name',
          position       VARCHAR2(50)  PATH 'position',
          salary         NUMBER        PATH 'salary'
) AS emp_tab;
```

The result is as follows:

```shell
+-------------+---------------+-----------+--------+
| EMPLOYEE_ID | EMPLOYEE_NAME | POSITION  | SALARY |
+-------------+---------------+-----------+--------+
|           1 | John Doe      | Developer |   5000 |
|           2 | Jane Smith    | Manager   |   6500 |
+-------------+---------------+-----------+--------+
2 rows in set
```