| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
|machine-translation| en          |
|slug|arg_max-oracle-compatible-mode|

# ARG_MAX

## Syntax

```sql
ARG_MAX([DISTINCT] maximize_col, return_col)
```

## Purpose

The `ARG_MAX` function is used to find the row in a dataset that maximizes the target expression and returns the value of the specified column or the full record corresponding to that row.

Where:

* `maximize_col` is the column to be maximized.
* `return_col` is the column corresponding to the return value. If `NULL`, it is skipped.

## Examples

```shell
obclient> SELECT * FROM test;
+------+------+
| a    | b    |
+------+------+
| a    |    1 |
| b    |    2 |
| c    |    2 |
| NULL |    3 |
| NULL | NULL |
| d    | NULL |
+------+------+
6 rows in set (0.000 sec)
```

```shell
obclient> SELECT arg_max(a, b), max(b) FROM test;
+---------------+--------+
| arg_max(a, b) | max(b) |
+---------------+--------+
| b             |      3 |
+---------------+--------+
1 row in set (0.002 sec)
```