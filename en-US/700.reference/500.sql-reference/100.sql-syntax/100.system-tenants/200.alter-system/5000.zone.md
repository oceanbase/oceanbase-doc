| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   |                 |
|machine-translation|en,ja-marker|

# ZONE

## Purpose

This statement is used to manage the status of a zone, including adding, deleting, starting, stopping, and isolating zones.

  <main id="notice" type='explain'>
    <h4>Note</h4>
    <p>This statement can only be executed in the sys tenant. </p>
  </main>

## Syntax

```sql
alter_system_zone_stmt:    
    ALTER SYSTEM zone_action;

zone_action:
      ADD ZONE zone_name
      [zone_option_list]
    | {ALTER | CHANGE | MODIFY} ZONE zone_name
      [SET] zone_option_list
    | {DELETE | START | STOP | FORCE STOP} ZONE zone_name
    | ISOLATE ZONE '$zone_name';

zone_option_list:
    zone_option [, zone_option ...]

zone_option:
      region
    | idc
    | ZONE_TYPE {READWRITE | ENCRYPTION}

idc:
    STR_VALUE
```

## Parameters

|          **Parameter**      |          **Description**      |
|------------------------|------------------------|
| ADD ZONE                         | Adds a zone.   |
| {ALTER \| CHANGE \| MODIFY} ZONE | Modifies the region attribute of a zone. <main id="notice" type='explain'><h4>Note</h4><p><code>ALTER</code>, <code>CHANGE</code>, and <code>MODIFY</code> have the same functionality. You can use any of these commands to modify the region attribute of a zone. </p></main>       |
| DELETE ZONE                      | Deletes a zone. Before deleting a zone, make sure that it has no available servers. |
| ISOLATE ZONE                     | Isolates a faulty zone. This operation switches the leader of the faulty zone to another server to restore user write services and restore normal log synchronization within the cluster.   |
| START \| STOP                    | Starts or stops a zone.    |
| FORCE STOP                       | Forcefully stops a zone.     |
| zone_option                      | Specifies the attributes of the target zone. If you specify multiple attributes, separate them with commas (,): <ul><li> `region`: the name of the region where the zone is located. Default value: `default_region`. </li>   <li> `idc`: the name of the IDC where the zone is located. Default value: empty. </li>   <li> `ZONE_TYPE`: specifies whether the target zone is a read/write zone (`READWRITE`) or an encrypted zone (`ENCRYPTION`). If you do not explicitly specify `ZONE_TYPE`, the zone is a read/write zone by default. </li></ul>    |

## Examples

* Deletes a zone. If the zone contains OBServer nodes, the deletion fails.

  ```sql
  obclient> ALTER SYSTEM DELETE ZONE 'z1';
  ERROR 4668 (HY000): The zone is not empty and can not be deleted. You should delete the servers of the zone. There are 1 servers alive and 0 not alive.
  ```

* Isolates a faulty zone.

  ```sql
  obclient> ALTER SYSTEM ISOLATE ZONE 'zone1';
  ```