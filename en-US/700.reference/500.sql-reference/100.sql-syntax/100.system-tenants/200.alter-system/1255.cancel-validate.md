| Description   |                 |
|---------------|-----------------|
| machine-translation ||
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   |                 |
|slug|cancel-validate|

# CANCEL VALIDATE

## Purpose

The `ALTER SYSTEM CANCEL VALIDATE` statement is used to cancel a backup validation task that is currently being executed.

## Privilege requirements

This statement must be executed by the `root` user of the `sys` tenant (`root@sys`) or the administrator of each tenant. Specifically:

* In MySQL mode, the default administrator is the `root` user.
* In Oracle mode, the default administrator is the `SYS` user.

## Syntax

```sql
ALTER SYSTEM CANCEL VALIDATE BACKUP [TENANT = mysql_tenant];
```

## Parameters

|      Parameter        |        Description                                             |
|------------------|---------------------------------------------------------|
| TENANT           | Specifies the user tenant whose validation task is to be canceled. This parameter can only be specified by the system tenant. If not specified, the system tenant will cancel all validation tasks being executed across all user tenants in the cluster.  |

## Examples

* The system tenant cancels the backup validation task being executed by a user tenant.

  * The system tenant cancels the backup validation task being executed by all user tenants.

    ```shell
    obclient(root@sys)[(none)]> ALTER SYSTEM CANCEL VALIDATE BACKUP;
    ```

  * The system tenant cancels the backup validation task being executed by the specified user tenant `mysqltenant`.

    ```shell
    obclient(root@sys)[(none)]> ALTER SYSTEM CANCEL VALIDATE BACKUP TENANT = mysqltenant;
    ```

* The user tenant cancels the backup validation task being executed by itself.

  ```shell
  obclient> ALTER SYSTEM CANCEL VALIDATE BACKUP;
  ```

## References

* [Overview of backup validation](../../../../../600.manage/600.backup-and-recovery/550.backup-validation/100.backup-validation-overview.md)

* [View the backup validation process](../../../../../600.manage/600.backup-and-recovery/550.backup-validation/600.view-the-backup-validation-process.md)

* [View the backup validation history](../../../../../600.manage/600.backup-and-recovery/550.backup-validation/700.view-the-backup-validation-history.md)