| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   |                 |
|machine-translation| en          |

# RESTORE PREVIEW

## Purpose

This statement specifies the original tenant-level archive and backup paths and the expected restore time to preview the data backup sets (`backup_set`) and log archives (`archivelog_piece`) required to restore to the expected time.

## Privilege requirements

To execute this statement, the current user must have the `ALTER SYSTEM` privilege.

## Syntax

```sql
ALTER SYSTEM RESTORE FROM 'uri' UNTIL { TIME='timestamp' | SCN=scn } PREVIEW;
```

## Parameters

|              **Parameter**       |                     **Description**                       |
|-----------------------------|----------------------------------------------------|
| uri                         | Specifies the original data backup path and log archive path, separated by commas. |
| TIME='timestamp' \| SCN=scn | Specifies the expected restore timestamp or SCN. When executing this statement, you must explicitly specify the time. |

## Examples

* OSS

  * Specifies the original data backup path and log archive path, and expects to restore to the specified SCN `1712650554000909004`.

    ```shell
    obclient [oceanbase]> ALTER SYSTEM RESTORE FROM 'oss://test_bucket/data?host=***&access_id=****&access_key=****, oss://test_bucket/archive?host=***&access_id=****&access_key=****' UNTIL SCN = 1712650554000909004 PREVIEW;
    ```

  * Specifies the original data backup path and log archive path, and expects to restore to the specified timestamp `2024-04-09 16:15:54`.

    ```shell
    obclient [oceanbase]> ALTER SYSTEM RESTORE FROM 'oss://test_bucket/data?host=***&access_id=****&access_key=****, oss://test_bucket/archive?host=***&access_id=****&access_key=****' UNTIL TIME = '2024-04-09 16:15:54' PREVIEW;
    ```

* NFS

  * Specifies the original data backup path and log archive path, and expects to restore to the specified SCN `1712650554000909004`.

    ```shell
    ALTER SYSTEM RESTORE FROM 'file:///ob_backup/data, file:///ob_backup/archive' UNTIL SCN = 1712650554000909004 PREVIEW;
    ```

  * Specifies the original data backup path and log archive path, and expects to restore to the specified timestamp `2024-04-09 16:15:54`.

    ```shell
    ALTER SYSTEM RESTORE FROM 'file:///ob_backup/data, file:///ob_backup/archive' UNTIL TIME = '2024-04-09 16:15:54' PREVIEW;
    ```

## References

* [SHOW RESTORE PREVIEW](../1400.show-restore-preview.md)
* [Initiate restore from a specified path](../../../../../600.manage/600.backup-and-recovery/600.restore-data/260.initiate-the-specified-path-restore.md)