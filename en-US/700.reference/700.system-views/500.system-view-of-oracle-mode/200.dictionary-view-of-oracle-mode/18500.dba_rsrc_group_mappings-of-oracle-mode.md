| description ||
|---|---|
| keywords ||
| dir-name ||
| dir-name-en ||
| tenant-type | Oracle Mode |
|machine-translation||

# DBA_RSRC_GROUP_MAPPINGS

<main id="notice" type='explain'>
<h4>Note</h4>
<p>This view is introduced since OceanBase Database V3.2.4. </p>
</main>

## Purpose

The `DBA_RSRC_GROUP_MAPPINGS` view displays the binding relationships between users and resource groups.

## Columns

| Column | Type | Nullable? | Description |
|----------------|----------------|------------|-----------------------------------|
| ATTRIBUTE | VARCHAR2(128) | NO | The category. The current version supports only `ORACLE_USER`.  |
| VALUE | VARBINARY(128) | NO | The value. When the category is `ORACLE_USER`, the value is the username for login.  |
| CONSUMER_GROUP | VARCHAR2(128) | YES | The name of the resource group that the user is bound to.  |
| STATUS | VARCHAR2(128) | NO | The status of the resource group. It is used only for compatibility and the value is fixed to `NULL`.  |

## Sample query

In a user tenant, query the binding relationships between users and resource groups in the tenant.

```shell
obclient [SYS]> SELECT * FROM SYS.DBA_RSRC_GROUP_MAPPINGS;
```

The query result is as follows:

```shell
+-----------+-------+-------------------+--------+
| ATTRIBUTE | VALUE | CONSUMER_GROUP    | STATUS |
+-----------+-------+-------------------+--------+
| user      | sys   | interactive_group | NULL   |
+-----------+-------+-------------------+--------+
1 row in set
```

## References

For information about resource groups in the tenant, see [DBA_RSRC_CONSUMER_GROUPS](18400.dba_rsrc_consumer_groups-of-oracle-mode.md).