|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type|Oracle Mode|
|machine-translation| en |
|slug|dbms_lock_allocated-oracle-compatible-mode|

# DBMS_LOCK_ALLOCATED

<main id="notice" type='explain'>
  <h4>Note</h4>
  <p>This view is available starting with V4.2.1. </p>
</main>

## Purpose

This view displays information about `DBMS_LOCK` requests that are active and have not expired.

## Columns

| **Column** | **Type** | **Nullable** | **Description** |
| --- | --- | --- | --- |
| NAME       | VARCHAR2(128)                     | NO   | The lock name, which is the primary key. |
| LOCKID     | NUMBER(38)                        | NO   | The lock ID. |
| EXPIRATION | TIMESTAMP(6) WITH LOCAL TIME ZONE | NO   | The lock expiration time. |

## Sample query

Query the `DBMS_LOCK` requests that are active and have not expired in the current tenant.

```shell
obclient[SYS]> SELECT * FROM SYS.DBMS_LOCK_ALLOCATED;
```

The query result is as follows:

```shell
+--------+------------+------------------------------+
| NAME   | LOCKID     | EXPIRATION                   |
+--------+------------+------------------------------+
| test01 | 1074054503 | 08-MAY-25 03.40.32.552443 PM |
+--------+------------+------------------------------+
1 row in set
```