|description||
|---|---|
| machine-translation ||
|keywords||
|dir-name||
|dir-name-en||
|tenant-type|Oracle Mode|
|slug|user_subpart_key_columns-oracle-mode|

# USER_SUBPART_KEY_COLUMNS

<main id="notice" type='explain'>
<h4>Note</h4>
<p>This view is available starting with V2.2.77. </p>
</main>

## Purpose

This view displays all columns of the subpartition key in all partitioned objects owned by the current user.

  <main id="notice" >
    <h4>Applicability</h4>
    <p>This view is applicable only to OceanBase Database in Oracle mode. </p>
  </main>

## Columns

|      **Column**      |     **Type**     | **Nullable** |                                                              **Description**                                                              |
|--------------------|----------------|----------------|----------------------------------------------------------------------------------------------------------------------------------|
| NAME               | VARCHAR2(128)  | NO             | The name of the partitioned table or partitioned index.                                   |
| OBJECT_TYPE        | CHAR(5)        | NO             | The type of the partitioned object. Valid values: <ul><li>TABLE   </li><li> INDEX  </li></ul>  |
| COLUMN_NAME        | VARCHAR2(4000) | NO             | The name of the column.                                          |
| COLUMN_POSITION    | NUMBER         | NO             | The position of the column in the partition key.                                     |
| COLLATED_COLUMN_ID | NUMBER         | NO             | This column is not supported. It is NULL by default.                       |

## Sample query

Query the columns of the subpartition key in the partitioned table `TBL2_F_RL` owned by the current user.

```shell
obclient [SYS]> SELECT * FROM SYS.USER_SUBPART_KEY_COLUMNS WHERE NAME='TBL2_F_RL';
```

The query result is as follows:

```shell
+-----------+-------------+-------------+-----------------+--------------------+
| NAME      | OBJECT_TYPE | COLUMN_NAME | COLUMN_POSITION | COLLATED_COLUMN_ID |
+-----------+-------------+-------------+-----------------+--------------------+
| TBL2_F_RL | TABLE       | COL2        |               1 |               NULL |
+-----------+-------------+-------------+-----------------+--------------------+
1 row in set
```

## References

* Query the columns of the subpartition key in all partitioned objects in the current tenant: [DBA_SUBPART_KEY_COLUMNS](20300.dba_subpart_key_columns-of-oracle-mode.md)

* Query the columns of the subpartition key in all partitioned objects accessible to the current user: [ALL_SUBPART_KEY_COLUMNS](4300.all_subpart_key_columns-of-oracle-mode.md)