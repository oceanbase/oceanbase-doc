|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type|Oracle Mode|
|machine-translation||

# ALL_COL_COMMENTS

<main id="notice" type='explain'>
<h4>Note</h4>
<p>This view is available starting with V2.2.30. </p>
</main>

## Purpose

The view displays comments on all columns of views and tables that the user can access.

  <main id="notice" >
    <h4>Applicability</h4>
    <p>This view applies only to OceanBase Database in Oracle mode. </p>
  </main>

## Columns

|  **Column**   |     **Type**     | **Nullable?** | **Description** |
|-------------|----------------|----------------|--------|
| OWNER       | VARCHAR2(128)  | NO             | The owner of the object. |
| OBJECT_NAME | VARCHAR2(128)  | NO             | The name of the object.   |
| COLUMN_NAME | VARCHAR2(128)  | NO             | The name of the column.     |
| COMMENTS    | VARCHAR2(4000) | YES            | The comment on the column.    |

## Sample query

Query all column comments in the `T1` table for all tables accessible to the current user.

```shell
obclient [SYS]> SELECT * FROM SYS.ALL_COL_COMMENTS WHERE TABLE_NAME='T1';
```

The query result is as follows:

```shell
+-------+------------+----------------+----------+
| OWNER | TABLE_NAME | COLUMN_NAME    | COMMENTS |
+-------+------------+----------------+----------+
| SYS   | T1         | __pk_increment | NULL     |
| SYS   | T1         | ID1            | score    |
| SYS   | T1         | ID2            | NULL     |
+-------+------------+----------------+----------+
3 rows in set
```

## References

* [DBA_COL_COMMENTS](7300.dba_col_comments-of-oracle-mode.md) shows column comments for all tables and views in the tenant.

* [USER_COL_COMMENTS](25500.user_col_comments-of-oracle-mode.md) shows column comments for all tables and views owned by the current user.