|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type|Oracle Mode|
|machine-translation| en |

# ALL_PART_KEY_COLUMNS

<main id="notice" type='explain'>
<h4>Note</h4>
<p>This view is available starting with V2.2.77. </p>
</main>

## Purpose

This view displays the columns contained in the primary partitioning key of all partitioned objects accessible to the current user.

  <main id="notice" >
    <h4>Applicability</h4>
    <p>This view is available only in Oracle mode. </p>
  </main>

## Columns

|              **Column**              |     **Type**     | **Nullable** |                                                              **Description**                                                              |
|------------------------------------|----------------|----------------|----------------------------------------------------------------------------------------------------------------------------------|
| OWNER                              | VARCHAR2(128)  | NO             | The owner of the partitioned table or partitioned index.                                  |
| NAME                               | VARCHAR2(128)  | NO             | The name of the partitioned table or partitioned index.                                   |
| OBJECT_TYPE                        | CHAR(5)        | NO             | The type of the partitioned object. Valid values: <ul><li>TABLE   </li><li> INDEX </li></ul>   |
| COLUMN_NAME                        | VARCHAR2(4000) | NO             | The name of the column.                                          |
| COLUMN_POSITION                    | NUMBER         | NO             | The position of the column in the partitioning key.                                     |
| COLLATED_COLUMN_ID | NUMBER         | NO             | This column is not supported. The value of this column is NULL by default.                       |

## Sample query

Query the columns contained in the primary partitioning key of the partitioned index `TBL1_H_IDX1` accessible to the current user.

```shell
obclient [SYS]> SELECT * FROM SYS.ALL_PART_KEY_COLUMNS WHERE NAME='TBL1_H_IDX1';
```

The query result is as follows:

```shell
+-------+-------------+-------------+-------------+-----------------+--------------------+
| OWNER | NAME        | OBJECT_TYPE | COLUMN_NAME | COLUMN_POSITION | COLLATED_COLUMN_ID |
+-------+-------------+-------------+-------------+-----------------+--------------------+
| SYS   | TBL1_H_IDX1 | INDEX       | COL2        |               1 |               NULL |
+-------+-------------+-------------+-------------+-----------------+--------------------+
1 row in set
```

## References

* Query the columns contained in the primary partitioning key of all partitioned objects in the current tenant: [DBA_PART_KEY_COLUMNS](17400.dba_part_key_columns-of-oracle-mode.md)

* Query the columns contained in the primary partitioning key of all partitioned objects owned by the current user: [USER_PART_KEY_COLUMNS](28200.user_part_key_columns-of-oracle-mode.md)