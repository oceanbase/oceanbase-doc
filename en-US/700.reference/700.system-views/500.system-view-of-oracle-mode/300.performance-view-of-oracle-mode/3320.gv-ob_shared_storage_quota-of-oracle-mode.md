|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type| Oracle Mode|
|machine-translation||

# GV$OB_SHARED_STORAGE_QUOTA

<main id="notice" type='explain'>
<h4>Note</h4>
<p>This view was introduced in OceanBase Database V4.3.4. </p>
</main>

## Purpose

This view displays the I/O access resource information of all OBServer nodes.

## Columns

| Column | Type | Nullable? | Description |
|------|------|----------|-------------|
| SVR_IP | VARCHAR2(46) | NO | The IP address of the OBServer node. |
| SVR_PORT | NUMBER(38) | NO | The port of the OBServer node. |
| ENDPOINT | VARCHAR2(256) | NO | The endpoint of shared storage. This field is empty in shared-nothing (SN) mode. |
| PATH | VARCHAR2(1024)| NO | The path in object storage. In SN mode, this field displays the local path. </br> The path can be of the following types: `local://`, `oss://`, `file://`, and `s3://`. |
| CLASS_ID | NUMBER(38) | NO | An ID used for labeling. The meaning of this field varies depending on the purpose of the object storage bucket. <ul><li>If the bucket is used for clog storage or data storage, this field has no meaning and the value is `0`. </li> <li>If the bucket is used for backup and restore, data import, or data export, this field indicates the tenant ID. </li></ul>      |
| TYPE | VARCHAR2(32) | NO | The metric type, which can be one of the following types: <ul><li>`IPS`: the number of instructions per second </li> <li>`OPS`: the number of operations per second </li> <li>`IOPS`: the number of input or output operations per second </li> <li>`IBW`: input bandwidth </li> <li>`OBW`: output bandwidth </li> <li>`IOBW`: input or output bandwidth </li> <li>`TAG`: user-defined tag information</li></ul>     |
| REQUIREMENT | NUMBER(38) | NO | The required resources of the server.    |
| ASSIGN | NUMBER(38) | NO | The upper limit of the resources specified by the throttling algorithm for the server. |

## Sample query

In a user tenant, query the applied and reserved quotas of the following metrics in object storage on all OBServer nodes for clog storage and data storage scenarios. For backup and restore, data import, and data export scenarios, query the applied and reserved quotas of the following metrics in object storage on all OBServer nodes in the tenant.

```shell
obclient> SELECT * FROM SYS.GV$OB_SHARED_STORAGE_QUOTA;
```

## References

[V$OB_SHARED_STORAGE_QUOTA](33320.v-ob_shared_storage_quota-of-oracle-mode.md)