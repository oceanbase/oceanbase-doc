|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type|Oracle Mode|
|machine-translation| en |

# V$OB_PROCESSLIST

<main id="notice" type='explain'>
  <h4>Note</h4>
  <p>This view is available starting with V4.0.0. </p>
</main>

## Purpose

The `V$OB_PROCESSLIST` view displays session information on the current OBServer node.

## Columns

|      Column      |        Type        | Nullable |                              Description                              |
|----------------|------------------|------------|--------------------------------------------------------------|
| SVR_IP         | VARCHAR2(46)     | NO         | The IP address of the server.                                                    |
| SVR_PORT       | NUMBER(38)       | NO         | The port number of the server.                                                       |
| SQL_PORT       | NUMBER(38)       | NO         | The port number of the SQL service.                                                      |
| ID             | NUMBER(38)       | NO         | The session ID. <ul><li>In direct connection mode:<br>This column displays the server session ID. </li> <li>In ODP mode:</li><ul><li>If the <code>client_session_id_version = 2</code> parameter is specified in ODP, this column displays the client session ID. </li> <li>If <code>client_session_id_version = 1</code> is specified, this column displays the server session ID. </li></ul></ul>                                                       |
| USER           | CHAR(193)        | NO         | The username.                                                          |
| HOST           | VARCHAR2(128)    | NO         | The IP address and port number of the client. |
| DB             | VARCHAR2(128)    | YES        | The name of the database being accessed.                                              |
| TENANT         | VARCHAR2(128)    | NO         | The name of the tenant being accessed.                                                      |
| COMMAND        | VARCHAR2(4096)   | NO         | The type of the command being executed.                                                    |
| TIME           | NUMBER(38)       | NO         | The execution time of the current command, in seconds. If the command is retried, the value is reset to 0 and recalculated.                              |
| TOTAL_TIME     | NUMBER(38)       | NO         | The total execution time of the current command, in seconds. If the command is retried, the value is not reset.                                 |
| STATE          | VARCHAR2(128)    | YES        | The current session state.                                                       |
| INFO           | VARCHAR2(262143) | YES        | The command being executed.                                                  |
| PROXY_SESSID   | NUMBER(38)       | YES        | If the client is an ODP, this column displays the proxy session ID.                         |
| MASTER_SESSID  | NUMBER(38)       | YES        | The master session ID, which is used to link multiple sub-sessions of the same SQL statement.                        |
| USER_CLIENT_IP | VARCHAR2(46)     | YES        | The IP address of the client.                                                     |
| USER_HOST      | VARCHAR2(128)    | YES        | The host name of the client.                                                     |
| RETRY_CNT      | NUMBER(38)       | NO         | The number of retries for the current command.                                                    |
| RETRY_INFO     | NUMBER(38)       | NO         | The retry information of the current command, which is generally the error code of the last retry.                                      |
| SQL_ID         | VARCHAR2(32)     | NO         | The SQL ID.                                                    |
| TRANS_ID       | NUMBER(38)       | NO         | The transaction ID.                                                        |
| THREAD_ID      | NUMBER(38)       | NO         | The thread ID.                                                        |
| SSL_CIPHER     | VARCHAR2(4096)   | YES        | The name of the encryption cipher.                                                       |
| TRACE_ID       | VARCHAR2(64)     | YES        | The trace ID.                                                     |
| TRANS_STATE    | VARCHAR2(32)     | YES        | The transaction state. If no transaction is associated with the session, this column is empty. <ul><li>`IDLE`: The transaction is idle, indicating that the transaction has not started. </li> <li>`ACTIVE`: The transaction is explicitly started. You can explicitly start a transaction by using the `START TRANSACTION` or `BEGIN` statement. </li> <li>`IMPLICIT_ACTIVE`: The transaction is implicitly started. In `autocommit = on` mode, a transaction is implicitly started after you execute a DML statement. </li> <li>`ROLLBACK_SAVEPOINT`: The transaction is being rolled back to a savepoint. </li> <li>`IN_TERMINATE`: The transaction is being terminated. This can be caused by internal reasons or by user-initiated termination, such as the execution of the `COMMIT` or `ROLLBACK` statement. </li> <li>`ABORTED`: The transaction is internally terminated or rolled back due to an exception. </li> <li>`ROLLED_BACK`: The transaction has been rolled back. </li> <li>`COMMIT_TIMEOUT`: The transaction commit times out. </li> <li>`COMMIT_UNKNOWN`: The transaction commit result is unknown. </li> <li>`COMMITTED`: The transaction has been successfully committed. </li></ul>      |
| ACTION         | VARCHAR2(4096)   | YES        | The name of the current operation, which is set by calling the DBMS_APPLICATION_INFO.SET_ACTION procedure. |
| MODULE         | VARCHAR2(4096)   | YES        | The name of the current operation, which is set by calling the DBMS_APPLICATION_INFO.SET_MODULE procedure. |
| CLIENT_INFO    | VARCHAR2(4096)   | YES        | The information set by calling the DBMS_APPLICATION_INFO.SET_CLIENT_INFO procedure.              |
| LEVEL          | NUMBER(38)       | NO         | The end-to-end tracing monitoring level of the session. For example, 1 indicates the diagnostic information of level 1. <main id="notice" type='explain'><h4>Note</h4><p>This column is available starting with V4.2.1. </p></main> |
| SAMPLE_PERCENTAGE | NUMBER(38)    | NO         | The end-to-end tracing sampling frequency of the session.  For example, 50 indicates that diagnostic information is sampled at a frequency of 50%. <main id="notice" type='explain'><h4>Note</h4><p>This column is available starting with V4.2.1. </p></main> |
| RECORD_POLICY    | VARCHAR2(32)   | NO         | The end-to-end tracing record strategy of the session. The following three strategies are supported: <ul><li>ALL: All trace points that are sampled and enabled for tracing are printed to the log file. The trace points are printed to the log file at the end of each span. </li><li>ONLY_SLOW_QUERY: All trace points that are sampled and enabled for tracing are printed to the log file only if they belong to slow queries. The trace points of other requests are discarded. The trace log is printed only after the request is determined to be a slow query. In the Proxy, the root span, which is at the transaction level, is printed only when a slow query is detected. </li><li>SAMPLE_AND_SLOW_QUERY: All trace points that are sampled and enabled for tracing are printed to the log file if they meet the following conditions: <ol><li>When the hidden configuration item <code>_print_sample_ppm</code> is used for sampling at a probability of 1/10,000, the trace point is printed to the log file only if it is selected for sampling. </li><li>The trace point meets the conditions for printing in the ONLY_SLOW_QUERY mode. </li></ol> In this mode, the trace log is not printed at the end of the span. If condition 1 is met, the trace log is forcibly printed at the client and the information is passed to the subsequent link components: If condition 2 is met, the trace log is printed at the same time as in the ONLY_SLOW_QUERY mode. </li></ul><main id="notice" type='explain'><h4>Note</h4><p>This column is available starting with V4.2.1. </p></main> |
| LB_VID           | NUMBER(38)     | NO         | If the database is directly connected to the load balancer in the public cloud, this column displays the VPC ID of the load balancer. Otherwise, this column is empty. <main id="notice" type='explain'><h4>Note</h4><p>This column is available starting with V4.2.1. </p></main> |
| LB_VIP           | VARCHAR2(46)   | NO         | If the database is directly connected to the load balancer in the public cloud, this column displays the IP address of the load balancer. Otherwise, this column is empty. <main id="notice" type='explain'><h4>Note</h4><p>This column is available starting with V4.2.1. </p></main> |
| LB_VPORT         | NUMBER(38)     | NO         | If the database is directly connected to the load balancer in the public cloud, this column displays the port of the load balancer. Otherwise, this column is empty. <main id="notice" type='explain'><h4>Note</h4><p>This column is available starting with V4.2.1. </p></main> |
| IN_BYTES         | NUMBER(38)     | NO         | The inflow traffic of the session, in bytes. <main id="notice" type='explain'><h4>Note</h4><p>This column is available starting with V4.2.1. </p></main> |
| OUT_BYTES        | NUMBER(38)     | NO         | The outflow traffic of the session, in bytes. <main id="notice" type='explain'><h4>Note</h4><p>This column is available starting with V4.2.1. </p></main> |
| USER_CLIENT_PORT  | NUMBER(38)          | NO   |   The client address information. <main id="notice" type='explain'><h4>Note</h4><ul><li>This column is available starting with V4.3.0 in V4.3.x. </li> <li>This column is available starting with V4.2.3 in V4.2.x. </li></ul></main> |
| PROXY_USER        | VARCHAR2(128) | YES        | <ul><li>The name of the proxy user in the proxy user login scenario. </li><li>This column is empty in the non-proxy user login scenario. </li></ul><main id="notice" type='explain'><h4>Note</h4><ul><li>This column is available starting with V4.3.2 in V4.3.x. </li><li>This column is available starting with V4.2.3 in V4.2.x. </li></ul></main> |
| SERVICE_NAME     | VARCHAR2(64)   | YES        | The service name of the session that created the session. If the value is `NULL`, the session was not created by a service name. <main id="notice" type='explain'><h4>Note</h4><ul><li>For V4.3.x, this column was introduced in V4.3.1. </li> <li>For V4.2.1, this column was introduced in V4.2.1 BP9. For other V4.2.x versions, this column was introduced in V4.2.4. </li></ul></main> |
| TOTAL_CPU_TIME   | NUMBER(38)     | NO         | The CPU time consumed by the current command, in seconds.  <main id="notice" type='explain'><h4>Note</h4><ul><li>For V4.3.x, this column was introduced in V4.3.1. </li> <li>For V4.2.1, this column was introduced in V4.2.1 BP5. </li></ul></main>    |
| TOP_INFO         | VARCHAR2(262143) | YES      | The top-level PL statement information of the executing SQL. <main id="notice" type='explain'><h4>Note</h4><ul><li>For V4.3.x, this column was introduced in V4.3.3. </li> <li>For V4.2.1, this column was introduced in V4.2.1 BP9. </li></ul></main> |
| TOP_TIME |NUMBER(38)|NO|The top_time column indicates the total execution time of the PL statement. <main id="notice" type='explain'><h4>Note</h4><p>For V4.4.x, this column was introduced in V4.4.1. |
| TOP_TRACE_ID |varchar(64)|NO|The traceid of the top-level statement. <main id="notice" type='explain'><h4>Note</h4><p>For V4.4.x, this column was introduced in V4.4.1. |
| MEMORY_USAGE     | NUMBER(38)     | NO         | The size of memory occupied by the current SQL statement, in bytes.  <main id="notice" type='explain'><h4>Note</h4><p>For V4.4.x, this column was introduced in V4.4.0, but it is not effective yet. The column value is <code>NULL</code> by default. </p></main>  |

## Sample query

Query the session information of the current OBServer node.

```shell
obclient [SYS]> SELECT * FROM SYS.V$OB_PROCESSLIST WHERE ROWNUM < = 1 \G
```

The query result is as follows:

```shell
*************************** 1. row ***************************
           SVR_IP: 6.xx.xxx.xxx
         SVR_PORT: 2882
         SQL_PORT: 2881
               ID: 3221659896
             USER: SYS
             HOST: 100.xx.xxx.xxx:39184
               DB: SYS
           TENANT: oracle001
          COMMAND: Query
             TIME: 0
       TOTAL_TIME: 0
            STATE: ACTIVE
             INFO: SELECT * FROM SYS.V$OB_PROCESSLIST WHERE ROWNUM < = 1
     PROXY_SESSID: NULL
    MASTER_SESSID: NULL
   USER_CLIENT_IP: 100.xx.xxx.xxx
        USER_HOST: %
        RETRY_CNT: 0
       RETRY_INFO: 0
           SQL_ID: EF50802B8426B999EDC33F5F27D0A204
         TRANS_ID: 0
        THREAD_ID: 3304791
       SSL_CIPHER: NULL
         TRACE_ID: YB42060CEB9F-000643607B0D5092-0-0
      TRANS_STATE: NULL
           ACTION: NULL
           MODULE: NULL
      CLIENT_INFO: NULL
            LEVEL: 1
SAMPLE_PERCENTAGE: 10
    RECORD_POLICY: SAMPLE_AND_SLOW_QUERY
           LB_VID: NULL
           LB_VIP: NULL
         LB_VPORT: NULL
         IN_BYTES: 448
        OUT_BYTES: 0
 USER_CLIENT_PORT: 39184
       PROXY_USER: NULL
     SERVICE_NAME: NULL
   TOTAL_CPU_TIME: 0
         TOP_INFO: NULL
     MEMORY_USAGE: 253952
         TOP_TIME: 0
     TOP_TRACE_ID: YB42060CEB9F-000643607B0D5092-0-0
1 row in set (0.047 sec)
```

## References

* [GV$OB_PROCESSLIST](2500.gv-ob_processlist-of-oracle-mode.md)
* [Full-link tracing](../../../../600.manage/900.daily-inspection/900.full-link-detection/300.full-link-diagnosis.md)