|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type|MySQL Mode|
|machine-translation||
|slug|gv-ob_processlist-mysql-mode|

# GV$OB_PROCESSLIST

<main id="notice" type='explain'>
  <h4>Note</h4>
  <p>This view is available starting with V4.0.0. </p>
</main>

## Purpose

The `GV$OB_PROCESSLIST` view displays session information on all OBServer nodes of the current tenant.

## Columns

|      Column      |         Type          | Nullable |                              Description                              |
|----------------|---------------------|------------|--------------------------------------------------------------|
| SVR_IP         | varchar(46)         | NO         | The IP address of the server.                                                    |
| SVR_PORT       | bigint(20)          | NO         | The port number of the server.                                                       |
| SQL_PORT       | bigint(20)          | NO         | The port number of SQL.                                                      |
| ID             | bigint(20) unsigned | NO         | The session ID.                                                        |
| USER           | varchar(32)         | NO         | The username.                                                          |
| HOST           | varchar(128)        | NO         | The name of the client host. (For example, if the client connects through an OBProxy, this field displays the IP address of the OBProxy.)                |
| DB             | varchar(128)        | YES        | The name of the database being accessed.                                              |
| TENANT         | varchar(128)        | NO         | The name of the tenant being accessed.                                                      |
| COMMAND        | varchar(4096)       | NO         | The type of the command being executed.                                                    |
| TIME           | bigint(20)          | NO         | The time when the command is executed, in seconds. If the command is retried, the value is reset to 0 and recalculated.                              |
| TOTAL_TIME     | bigint(20)          | NO         | The total time for the command to be executed, in seconds. <ul><li>If the command is being executed after a request is received and before the response is returned to the client, this field displays the time interval between when the request is received and the current time.  </li><li>If the command is in a state that does not involve receiving a request, this field displays the duration of the current state. </li></ul><main id="notice" type='explain'><h4>Note</h4><p>Before V4.2.5, this field displayed the total execution time of the command, which is not reset when the command is retried. </p></main>                                |
| STATE          | varchar(128)        | YES        | The current session state.                                                       |
| INFO           | varchar(262143)     | YES        | The command being executed.                                                  |
| PROXY_SESSID   | bigint(20) unsigned | YES        | If the client is an OBProxy, this field displays the proxy session ID.                         |
| MASTER_SESSID  | bigint(20) unsigned | YES        | The ID of the main session, which is used to link multiple sub-sessions of the same SQL statement.                        |
| USER_CLIENT_IP | varchar(46)         | YES        | The IP address of the client.                                                     |
| USER_HOST      | varchar(128)        | YES        | The name of the client host.                                                     |
| RETRY_CNT      | bigint(20)          | NO         | The number of retries for the current command.                                                    |
| RETRY_INFO     | bigint(20)          | NO         | The retry information for the current command, which is usually the error code of the last retry.                                      |
| SQL_ID         | varchar(32)         | NO         | The SQL ID.  Information                                                    |
| TRANS_ID       | bigint(20) unsigned | NO         | The transaction ID.                                                        |
| THREAD_ID      | bigint(20) unsigned | NO         | The thread ID.                                                        |
| SSL_CIPHER     | varchar(4096)       | YES        | The name of the encryption cipher.                                                       |
| TRACE_ID       | varchar(64)         | YES        | The trace ID.                                                     |
| TRANS_STATE    | varchar(32)         | YES        |  <ul><li>`IDLE`: idle state. This indicates that the transaction has not started. </li> <li>`ACTIVE`: the transaction is explicitly started by using the `START TRANSACTION` or `BEGIN` statement. </li> <li>`IMPLICIT_ACTIVE`: the transaction is implicitly started. In `autocommit = on` mode, the transaction is started after a DML statement is executed. </li> <li>`ROLLBACK_SAVEPOINT`: the transaction is being rolled back to a savepoint. </li> <li>`IN_TERMINATE`: the transaction is being terminated. This can be caused by internal reasons or by user-initiated operations such as the `COMMIT` or `ROLLBACK` statement. </li> <li>`ABORTED`: the transaction is internally terminated or rolled back due to an exception. </li> <li>`ROLLED_BACK`: the transaction has been rolled back. </li> <li>`COMMIT_TIMEOUT`: the transaction submission times out. </li> <li>`COMMIT_UNKNOWN`: the transaction submission result is unknown. </li> <li>`COMMITTED`: the transaction has been successfully submitted. </li></ul>                                                          |
| ACTION         | varchar(4096)       | YES        |  The name of the current operation, which is set by calling the DBMS_APPLICATION_INFO.SET_ACTION procedure. |
| MODULE         | varchar(4096)       | YES        |  The name of the current operation, which is set by calling the DBMS_APPLICATION_INFO.SET_MODULE procedure. |
| CLIENT_INFO    | varchar(4096)       | YES        |  The information set by calling the DBMS_APPLICATION_INFO.SET_CLIENT_INFO procedure.              |
| LEVEL | bigint(20) | NO |  The end-to-end tracing monitoring level of the session. For example, 1 indicates the diagnostic information of level 1. <main id="notice" type='explain'><h4>Note</h4><p>This column is available starting with V4.2.1. </p></main> |
| SAMPLE_PERCENTAGE | bigint(20) | NO |  The end-to-end tracing sampling frequency of the session.  For example, 50 indicates that the diagnostic information is sampled with a 50% probability. <main id="notice" type='explain'><h4>Note</h4><p>This column is available starting with V4.2.1. </p></main> |
| RECORD_POLICY | varchar(32) | NO |  The end-to-end tracing recording strategy of the session. The following three strategies are supported: <ul><li>ALL: all trace sampling points are printed to the log file. The trace sampling points are printed to the log file when each span ends. </li><li>ONLY_SLOW_QUERY: only the trace sampling points of slow queries are printed to the log file. The trace sampling points of other requests are discarded. The trace log is printed only after the request is determined to be a slow query. In the Proxy, the root span, which is the transaction-level span, is printed only when a slow query is detected. </li><li>SAMPLE_AND_SLOW_QUERY: the trace sampling points that meet the following conditions are printed to the log file: <ol><li>When the hidden parameter <code>_print_sample_ppm</code> is used for sampling at a 0.01% probability, the trace sampling point is printed to the log file only when it is selected for sampling. </li><li>When the trace sampling point meets the conditions for printing in the ONLY_SLOW_QUERY mode. </li></ol> In this mode, the trace log is not printed when the span ends. If condition 1 is met, the trace log is forcibly printed at the client and the information is passed to subsequent components in the chain. If condition 2 is met, the trace log is printed at the same time as in the ONLY_SLOW_QUERY mode. </li></ul> <main id="notice" type='explain'><h4>Note</h4><p>This column is available starting with V4.2.1. </p></main> |
| LB_VID | bigint(20) | YES |  If the database is directly connected to the load balancer in the public cloud, this column displays the VPC ID of the load balancer. Otherwise, it displays NULL. <main id="notice" type='explain'><h4>Note</h4><p>This column is available starting with V4.2.1. </p></main> |
| LB_VIP | varchar(46) | YES |  If the database is directly connected to the load balancer in the public cloud, this column displays the IP address of the client connecting to the load balancer. Otherwise, it displays NULL. <main id="notice" type='explain'><h4>Note</h4><p>This column is available starting with V4.2.1. </p></main> |
| LB_VPORT | bigint(20) | YES |  If the database is directly connected to the load balancer in the public cloud, this column displays the port of the client connecting to the load balancer. Otherwise, it displays NULL. <main id="notice" type='explain'><h4>Note</h4><p>This column is available starting with V4.2.1. </p></main> |
| IN_BYTES | bigint(20) | NO |  The inflow traffic of the session, in bytes.  <main id="notice" type='explain'><h4>Note</h4><p>This column is available starting with V4.2.1. </p></main> |
| OUT_BYTES | bigint(20) | NO |  The outflow traffic of the session, in bytes.  <main id="notice" type='explain'><h4>Note</h4><p>This column is available starting with V4.2.1. </p></main> |
| USER_CLIENT_PORT  | bigint(20)          | NO   |  The client address information. <main id="notice" type='explain'><h4>Note</h4><p>This column is available starting with V4.2.3. </p></main> |
| PROXY_USER        | varchar(128)        | YES  |<ul><li> If the proxy user logs in, the name of the proxy user is returned. </li><li> If the non-proxy user logs in, this column is empty. </li></ul> <main id="notice" type='explain'><h4>Note</h4><p>This column is available starting with V4.2.3. </p></main> |
| SERVICE_NAME      | varchar(64)         | YES   |  The name of the service that creates the session. If this column is `NULL`, the session is not created by a service. <main id="notice" type='explain'><h4>Note</h4><p>This column is available starting with V4.2.4. </p></main> |
| TOTAL_CPU_TIME    | bigint(21)          | NO   | The CPU time consumed by the current command, in seconds. <main id="notice" type='explain'><h4>Note</h4><ul><li>This column was introduced in V4.3.1 of V4.3.x. </li> <li>This column was introduced in V4.2.1 BP5 of V4.2.1 and in V4.2.5 of other V4.2.x versions. </li></ul></main>      |
| TOP_INFO          | varchar(262143)     | YES  | The top-level PL statement information of the executing SQL statement. <main id="notice" type='explain'><h4>Note</h4><ul><li>This column was introduced in V4.3.3 of V4.3.x. </li> <li>This column was introduced in V4.2.1 BP9 of V4.2.1 and in V4.2.5 of other V4.2.x versions. </li></ul></main>     |
| MEMORY_USAGE      | bigint(20)          | YES  | The memory size consumed by the current SQL statement, in bytes. <main id="notice" type='explain'><h4>Note</h4><p>This column was introduced in V4.2.5 BP2. </p></main>|

## Sample query

Query the session information of all OBServer nodes where the tenant is located.

```shell
obclient [oceanbase]> SELECT * FROM oceanbase.GV$OB_PROCESSLIST LIMIT 1\G
```

The query result is as follows:

```shell
*************************** 1. row ***************************
           SVR_IP: xx.xx.xx.xx
         SVR_PORT: 2882
         SQL_PORT: 2881
               ID: 3221711494
             USER: root
             HOST: xx.xx.xx.xx:25892
               DB: oceanbase
           TENANT: mysql001
          COMMAND: Query
             TIME: 0.065386
       TOTAL_TIME: 0.065444
            STATE: ACTIVE
             INFO: SELECT * FROM oceanbase.GV$OB_PROCESSLIST LIMIT 1
     PROXY_SESSID: NULL
    MASTER_SESSID: NULL
   USER_CLIENT_IP: xx.xx.xx.xx
        USER_HOST: %
        RETRY_CNT: 0
       RETRY_INFO: 0
           SQL_ID: 536F00E9EFCC3725E2239429AB69C94D
         TRANS_ID: 0
        THREAD_ID: 26478
       SSL_CIPHER: NULL
         TRACE_ID: YB42AC1E87E1-000XXXXXXXXXXXC-0-0
      TRANS_STATE:
           ACTION:
           MODULE:
      CLIENT_INFO:
            LEVEL: 1
SAMPLE_PERCENTAGE: 10
    RECORD_POLICY: SAMPLE_AND_SLOW_QUERY
           LB_VID: NULL
           LB_VIP: NULL
         LB_VPORT: NULL
         IN_BYTES: 448
        OUT_BYTES: 0
 USER_CLIENT_PORT: 25892
       PROXY_USER:
     SERVICE_NAME: NULL
   TOTAL_CPU_TIME: 0
         TOP_INFO: NULL
     MEMORY_USAGE: 270336
1 row in set
```

## References

* [V$OB_PROCESSLIST](32700.v-ob_processlist-of-mysql-mode.md)

* [View tenant sessions](../../../1200.database-proxy/1500.view-tenant-sessions.md)
