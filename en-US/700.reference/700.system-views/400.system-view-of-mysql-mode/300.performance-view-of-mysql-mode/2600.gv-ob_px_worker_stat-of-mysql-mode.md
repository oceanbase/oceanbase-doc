|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type|MySQL Mode|
|machine-translation||
|slug|gv-ob_px_worker_stat-mysql-mode|

# GV$OB_PX_WORKER_STAT

<main id="notice" type='explain'>
<h4>Note</h4>
<p>This view is available starting with V4.0.0. </p>
</main>

## Purpose

In parallel execution scenarios, this view displays the execution information for each thread in the current tenant on all OBServer nodes.

## Columns

|  **Column**  |      **Type**      | **Nullable** |       **Description**       |
|------------|--------------|----------------|----------------|
| SESSION_ID | bigint(20)   | NO             | For V4.2.x: <ul><li> V4.2.5 BP4 and earlier: <br> In all connection modes (direct mode/ODP mode), this field represents the server session ID. </li><li> V4.2.5 BP4 and later: <ul><li> In direct mode: <br> This field represents the server session ID. </li> <li> In ODP mode: </li><ul><li> When the <code>client_session_id_version = 2</code> parameter is set in ODP, this field represents the client session ID. </li> <li> When <code>client_session_id_version = 1</code>, this field represents the server session ID. </li></ul></ul></li></ul>      |
| TENANT_ID  | bigint(20)   | NO             | Tenant ID          |
| SVR_IP     | varchar(46)  | NO             | Server IP address     |
| SVR_PORT   | bigint(20)   | NO             | Server port          |
| TRACE_ID   | varchar(128) | NO             | SQL trace_id |
| QC_ID      | bigint(20)   | NO             | px qc_id    |
| SQC_ID     | bigint(20)   | NO             | px sqc_id   |
| WORKER_ID  | bigint(20)   | NO             | Thread ID          |
| DFO_ID     | bigint(20)   | NO             | px dfo_id   |
| START_TIME | timestamp(6) | NO             | The time when the thread started running.      |

## Sample query

Query the execution information for all threads in the current tenant on all OBServer nodes in parallel execution scenarios.

```shell
obclient [oceanbase]> SELECT * FROM oceanbase.GV$OB_PX_WORKER_STAT\G
```

The query result is as follows:

```shell
***************** 1. row *****************
SESSION_ID: 3221520411
 TENANT_ID: 1002
    SVR_IP: 192.xx.xx.xx
  SVR_PORT: 19510
  TRACE_ID: Y4Cxxxxxxxxxxxxxxxxxxxxxxx-0-0
     QC_ID: 1
    SQC_ID: 0
 WORKER_ID: 0
    DFO_ID: 0
START_TIME: 2023-04-23 17:29:17.372461
1 row in set
```