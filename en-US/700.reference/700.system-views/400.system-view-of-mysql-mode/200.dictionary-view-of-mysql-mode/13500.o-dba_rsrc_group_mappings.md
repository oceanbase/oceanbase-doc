|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type|MySQL Mode|
|machine-translation|en,ja-marker|
|slug|oceanbase-dba_rsrc_group_mappings-mysql-compatible-mode|

# oceanbase.DBA_RSRC_GROUP_MAPPINGS

<main id="notice" type='explain'>
<h4>Note</h4>
<p>This view is available starting with V3.2.4. </p>
</main>

## Overview

Displays the binding relationships between users and resource groups in the current tenant.

## Columns

| Field type | Type | Whether can be NULL | Description |
| ---------------- | ---------------- | ------------ | ----------------------------------- |
| ATTRIBUTE | varchar(128) | NO | Category. Currently, only the following category is supported:`ORACLE_USER`. |
| VALUE | varbinary(128) | NO | value. When the category is`ORACLE_USER`When it is `username`, the logged-in username is recorded. |
| CONSUMER_GROUP | varchar(128) | YES | The name of the resource group to which the user is bound. |
| STATUS | varchar(128) | NO | Resource group status, used only for compatibility. The current value is fixed as`NULL`. |

## Sample query

You can query the binding relationships between users and resource groups within the current tenant.

```shell
obclient [oceanbase]> SELECT * FROM oceanbase.DBA_RSRC_GROUP_MAPPINGS;
```

The query result is as follows:

```shell
+-----------+-------+-------------------+--------+
| ATTRIBUTE | VALUE | CONSUMER_GROUP    | STATUS |
+-----------+-------+-------------------+--------+
| user      | root  | interactive_group | NULL   |
+-----------+-------+-------------------+--------+
1 row in set
```

## References

For information about resource groups in the current tenant, see [oceanbase.DBA_RSRC_CONSUMER_GROUPS](13400.o-dba_rsrc_consumer_groups.md).
