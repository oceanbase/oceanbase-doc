| description ||
|---|---|
| keywords ||
| dir-name ||
| dir-name-en ||
| tenant-type | MySQL Mode |
|machine-translation||

# oceanbase.DBA_OB_TABLEGROUP_TABLES

<main id="notice" type='explain'>
<h4>Note</h4>
<p>This view is introduced since OceanBase Database V4.0.0. </p>
</main>

## Purpose

The `oceanbase.DBA_OB_TABLEGROUP_TABLES` view displays associations between table groups and tables in the current tenant. Tables that do not belong to a table group are not displayed.

## Columns

| Column | Type | Nullable? | Description |
|-----------------|--------------|------------|---------------|
| TABLEGROUP_NAME | varchar(128) | NO | The name of the table group. |
| OWNER | varchar(128) | NO | The name of the database where the table belongs. |
| TABLE_NAME | varchar(256) | NO | The name of a table. |
| SHARDING        | varchar(10)   | NO         | The SHARDING attribute of the table group. Valid values: <ul><li>`NONE`: All partitions of tables in the table group are aggregated on the same server, and no restrictions are placed on tables to be added to the table group. </li> <li>`PARTITION`: The data of each table in the table group is scattered by partition. All subpartitions in a partition are aggregated together. </li> <li>`ADAPTIVE`: The data of each table in the table group is adaptively scattered. Specifically, tables partitioned by partition in the table group are partitioned by partition. Subpartitions in each partition are subpartitioned by subpartition. </li></ul>    |

## Sample query

A user tenant queries the associations of the table group named `tblgroup1` within the current tenant.

```shell
obclient [oceanbase]> SELECT * FROM oceanbase.DBA_OB_TABLEGROUP_TABLES WHERE tablegroup_name = 'tblgroup1';
```

The query result is as follows:

```shell
+-----------------+-----------+------------+----------+
| TABLEGROUP_NAME | OWNER     | TABLE_NAME | SHARDING |
+-----------------+-----------+------------+----------+
| tblgroup1       | test      | tbl3       | ADAPTIVE |
+-----------------+-----------+------------+----------+
1 row in set
```

## References

* [CDB_OB_TABLEGROUP_TABLES](../../300.system-view-of-sys-tenant/200.dictionary-view-of-sys-tenant/10900.o-cdb_ob_tablegroup_tables-of-sys-tenant.md)

* [View information about a table group](../../../300.database-object-management/100.manage-object-of-mysql-mode/400.manage-table-groups-of-mysql-mode/300.view-the-information-of-a-table-group-of-mysql-mode.md)