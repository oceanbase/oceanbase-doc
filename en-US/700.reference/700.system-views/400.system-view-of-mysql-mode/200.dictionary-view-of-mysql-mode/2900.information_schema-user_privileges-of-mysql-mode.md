|description||
|---|---|
| machine-translation ||
|keywords||
|dir-name||
|dir-name-en||
|tenant-type|MySQL Mode|
|slug|information_schema-user_privileges-mysql-mode|

# information_schema.USER_PRIVILEGES

<main id="notice" type='explain'>
<h4>Note</h4>
<p>This view is available starting with V1.4. </p>
</main>

## Purpose

This view records the privilege information of a user.

## Columns

|    **Column**    |    **Type**     | **Nullable** | **Description** |
|----------------|---------------|----------------|--------|
| GRANTEE        | varchar(81)   | NO             | The account name of the user who is granted the privilege.   |
| TABLE_CATALOG  | varchar(512)  | NO             | The table catalog, which is fixed to `def`.    |
| PRIVILEGE_TYPE | varchar(64)   | NO             | The privilege granted.   |
| IS_GRANTABLE   | varchar(3)    | NO             | Indicates whether the privilege can be granted to other users.   |

## Sample query

Query the privilege information of a user.

```shell
obclient > SELECT * FROM information_schema.USER_PRIVILEGES;
```

Sample query:

```shell
+-------------+---------------+----------------+--------------+
| GRANTEE     | TABLE_CATALOG | PRIVILEGE_TYPE | IS_GRANTABLE |
+-------------+---------------+----------------+--------------+
| 'test2'@'%' | def           | USAGE          | NO           |
+-------------+---------------+----------------+--------------+
1 row in set
```