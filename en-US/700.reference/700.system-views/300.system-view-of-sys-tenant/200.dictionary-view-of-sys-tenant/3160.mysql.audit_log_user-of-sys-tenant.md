|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||
|machine-translation|en,ja-marker|
|slug|mysql-audit_log_user-sys-tenant|

# mysql.audit_log_user

<main id="notice" type='explain'>
 <h4>Note</h4>
 <p>This view is available starting with V4.2.4. </p>
</main>

## Overview

The `mysql.audit_log_user` view is used to display the mapping relationship between audit filters and users.

<main id="notice" >
  <h4>Applicability</h4>
  <p>This topic applies only to OceanBase Database Enterprise Edition. OceanBase Database Community Edition does not support the audit feature.</p>
</main>

## Columns

| **Field** | **Type** | **Nullable** | **Description** |
| ------------ | ------------- | --------------------- | ---------------------------------------- |
| USER | varchar(128) | NO | Username |
| HOST | varchar(128) | NO | Host name |
| FILTERNAME | varchar(64) | NO | Filter Name |

## Sample query

View the mapping between audit filters and users.

```shell
obclient [mysql]> select * from mysql.audit_log_user;
```

The query result is as follows:

```shell
+---------+------+------------+
| USER    | HOST | FILTERNAME |
+---------+------+------------+
| user001 | %    | log_all    |
+---------+------+------------+
1 row in set (0.003 sec)
```

## References

[mysql.audit_log_filter](3150.mysql-audit_log_filter-of-sys-tenant.md)