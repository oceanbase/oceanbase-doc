|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||
|machine-translation| en |
|slug|o-cdb_ob_sensitive_rule_plainaccess_users-sys-tenant|

# oceanbase.CDB_OB_SENSITIVE_RULE_PLAINACCESS_USERS

## Purpose

This view displays users and roles that have plaintext data access permissions for a sensitive rule column in all tenants.

## Columns

|        Column         |      Type       | Nullable |                      Description                                |
|---------------------|---------------|------------|-----------------------------------------------------------------|
| TENANT_ID         | int(20)    | NO         | The tenant ID.                                           |
| RULE_NAME         | varchar(256)    | NO         | The name of the sensitive rule.                                           |
| USER_NAME      | varchar(128)    | NO         | The name of the user.                                        |
| USER_TYPE        | varchar(4)    | NO         | The type of the user. Valid values: `USER` and `ROLE`. `USER` indicates a user, and `ROLE` indicates a role.                                      |

## Sample query

Query users and roles that have plaintext data access permissions for a sensitive rule column in all tenants.

```shell
obclient [oceanbase]> SELECT * FROM oceanbase.CDB_OB_SENSITIVE_RULE_PLAINACCESS_USERS;
```

The query result is as follows:

```shell
+-----------+-----------+-----------+-----------+
| TENANT_ID | RULE_NAME | USER_NAME | USER_TYPE |
+-----------+-----------+-----------+-----------+
| 1         | r1        | root      | USER      |
+-----------+-----------+-----------+-----------+
1 row in set
```