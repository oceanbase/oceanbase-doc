| Description   |                 |
|---------------|-----------------|
| machine-translation ||
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |

# Modify and drop triggers

PL/SQL supports the modification and dropping of triggers.

  <main id="notice" >
    <h4>Applicability</h4>
    <p>This topic applies only to OceanBase Database Enterprise Edition. OceanBase Database Community Edition does not support this feature. </p>
  </main>

## Modify a trigger

Currently, you can only use the `REPLACE TRIGGER` statement in DDL statements to modify a trigger.

Typically, you can write the creation and modification of a trigger in the following statement:

```javascript
CREATE OR REPLACE TRIGGER trigger_name ...
```

If `trigger_name` does not exist, a trigger is created. If `trigger_name` already exists, the trigger is modified.

## Drop a trigger

You can use the `DROP TRIGGER` statement to drop a trigger. The syntax is as follows:

```javascript
DROP TRIGGER trigger_name;
```

In addition, when you drop a table, the triggers established on the table are also dropped.