| Description   |                 |
|---------------|-----------------|
| machine-translation ||
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |

# Overview of DBMS_LOB

The `DBMS_LOB` package provides most LOB operations for reading and modifying `BLOB` and `CLOB` values.

  <main id="notice" >
    <h4>Applicability</h4>
    <p>This topic applies only to OceanBase Database Enterprise Edition. OceanBase Database Community Edition provides only MySQL mode. </p>
  </main>

## Privilege requirements for DBMS_LOB

This package must be created in the `SYS` schema. However, the operations provided by this package are executed under the current calling user, not in the `SYS` schema.

Any `DBMS_LOB` subprogram called from an anonymous PL/SQL block is executed using the privileges of the current user. Any `DBMS_LOB` subprogram called from a stored subprogram is executed using the privileges of the stored subprogram owner.

When creating a subprogram, you can set the `AUTHID` clause to specify whether the subprogram uses the privileges of the definer or the invoker. Here are two examples:

```sql
CREATE PROCEDURE proc1 AUTHID DEFINER ...
```

or

```sql
CREATE PROCEDURE proc1 AUTHID CURRENT_USER ...
```

## Data types used by DBMS_LOB

| **Data type** |               **Description**                |
|----------|-------------------------------------|
| BLOB     | The source or destination binary `LOB`.                      |
| RAW      | The source or destination `RAW` buffer (used with `BLOB`).      |
| CLOB     | The source or destination character `LOB`.                       |
| VARCHAR2 | The source or destination character buffer (used with `CLOB`).           |
| INTEGER  | Specifies the size of the buffer or `LOB`, the offset of the `LOB`, or the number of elements to access. |

## Considerations for using DBMS_LOB

All `DBMS_LOB` subprograms operate based on `LOB` locators. To successfully execute a `DBMS_LOB` subprogram, you must provide an input locator that represents an existing `LOB` in the database tablespace or external file system.

All `DBMS_LOB` APIs now support operations on remote `LOB` locators. Any API that uses two locators must be executed within the same database.

## Overview of DBMS_LOB subprograms

The following table lists the DBMS_LOB subprograms supported in the current version of OceanBase Database and their brief descriptions.

|                            **Subprogram**                             |                  **Description**                  |
|----------------------------------------------------------------|------------------------------------------|
| [APPEND](../9300.dbms-lob-oracle/200.append-oracle.md)          | Appends the content of the source `LOB` to the destination `LOB`.                 |
| [CLOSE](../9300.dbms-lob-oracle/300.close-oracle.md)           | Closes a previously opened internal or external `LOB`.                      |
| [COPY](../9300.dbms-lob-oracle/500.copy-oracle.md)            | Copies all or part of the source internal `LOB` to the destination internal `LOB`.         |
| [CREATETEMPORARY](../9300.dbms-lob-oracle/600.createtemporary-oracle.md) | Creates a temporary `BLOB` or `CLOB` and the corresponding index in the default temporary tablespace. |
| [ERASE](../9300.dbms-lob-oracle/700.eraese-oracle.md)           | Erases all or part of the `LOB`.                           |
| [FREETEMPORARY](../9300.dbms-lob-oracle/800.freetemporary-oracle.md)   | Frees the temporary `BLOB` or `CLOB` in the default temporary tablespace.          |
| [GETLENGTH](../9300.dbms-lob-oracle/900.getlength-oracle.md)       | Retrieves the length of the `LOB` value.                           |
| [ISOPEN](../9300.dbms-lob-oracle/1000.isopen-oracle.md)          | Checks whether the `LOB` is already opened using the input locator.                  |
| [INSTR](../9300.dbms-lob-oracle/1100.instr-oracle.md)           | Returns the position of the `n`th occurrence of a match in `LOB` mode.              |
| [ISTEMPORARY](1150.istemporary-of-oracle-mode.md)| Checks whether the locator points to a temporary LOB.|
| [OPEN](../9300.dbms-lob-oracle/1200.open-oracle.md)            | Opens the `LOB` in the specified mode (internal, external, or temporary).               |
| [READ](../9300.dbms-lob-oracle/1300.read-oracle.md)            | Reads data from the `LOB` starting at the specified offset.                    |
| [SUBSTR](../9300.dbms-lob-oracle/1400.substr-oracle.md)          | Returns a portion of the `LOB` value starting at the specified offset.                   |
| [TRIM](../9300.dbms-lob-oracle/1500.trim-oracle.md)            | Trims the `LOB` value to the specified shorter length.                     |
| [WRITE](../9300.dbms-lob-oracle/1600.write-oracle.md)           | Writes data to the `LOB` starting at the specified offset.                    |
| [WRITEAPPEND](../9300.dbms-lob-oracle/1700.writeappend-oracle.md)     | Writes the buffer to the end of the `LOB`.                        |
| [CONVERTTOBLOB](../9300.dbms-lob-oracle/1800.converttoblob-oracle.md)  | Converts the character data of the source `CLOB` to binary data and writes it to the destination `BLOB`. |