| Description   |                 |
|---------------|-----------------|
| machine-translation ||
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
|slug|ob_client_id_trace_enable-oracle-mode|

# OB_CLIENT_ID_TRACE_ENABLE

The `OB_CLIENT_ID_TRACE_ENABLE` procedure enables full-link tracing at the `client_id` level.

<main id="notice" >
  <h4>Applicability</h4>
  <p>This topic applies only to OceanBase Database Enterprise Edition. </p>
</main>

## Syntax

```sql
DBMS_MONITOR.OB_CLIENT_ID_TRACE_ENABLE(
 client_id    IN  VARCHAR2,
  level        IN  INT,
  sample_pct   IN  NUMBER,
  record_policy IN VARCHAR2);
```

## Parameters

| **Parameter** | **Description**                                                |
|----------------|---------------------------------------------------------|
| client_id      | The `client_id` for which full-link tracing is enabled.                           |
| level          | The `trace` collection level for full-link tracing, which is an integer.             |
| sample_pct     | The sampling rate, which ranges from 0 to 1.                               |
| record_policy  | The printing strategy: <ul><li> `ALL`: `trace` is printed for all sampled queries. </li></ul>              <ul><li>`ONLY_SLOW_QUERY`: `trace` is printed only for slow queries. </li></ul>          <ul><li>- `SAMPLE_AND_SLOW_QUERY`: `trace` is printed for slow queries, and other requests are printed based on the probability specified by `_print_sample_percentage`. </li></ul> |