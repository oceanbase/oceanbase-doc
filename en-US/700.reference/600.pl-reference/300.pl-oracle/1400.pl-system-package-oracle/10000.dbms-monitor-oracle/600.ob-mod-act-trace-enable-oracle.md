| Description   |                 |
|---------------|-----------------|
| machine-translation ||
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
|slug|ob_mod_act_trace_enable-oracle-mode|

# OB_MOD_ACT_TRACE_ENABLE

The `OB_MOD_ACT_TRACE_ENABLE` procedure enables full-link tracing at the module/action level.

<main id="notice" >
  <h4>Applicability</h4>
  <p>This topic applies only to OceanBase Database Enterprise Edition. </p>
</main>

## Syntax

```sql
DBMS_MONITOR.OB_MOD_ACT_TRACE_ENABLE(
  module_name     IN VARCHAR2,
  action_name     IN VARCHAR2,
  level        IN  INT,
  sample_pct   IN  NUMBER,
  record_policy IN VARCHAR2);
```

## Parameters

| **Parameter**        | **Description**                                     |
|------------------|----------------------------------------------|
| module_name      | The `module_name` to enable full-link tracing for.             |
| action_name      | The `action_name` to enable full-link tracing for.             |
| level            |    |
| sample_pct       | The sampling rate, which ranges from 0 to 1.                 |
| record_policy    | The printing strategy: <ul><li>`ALL` (trace all sampled queries). </li></ul>  <ul><li>`ONLY_SLOW_QUERY` (print trace only for slow queries). </li></ul> <ul><li> `SAMPLE_AND_SLOW_QUERY` (print trace for slow queries and other requests based on the value of `_print_sample_percentage`). </li></ul> |