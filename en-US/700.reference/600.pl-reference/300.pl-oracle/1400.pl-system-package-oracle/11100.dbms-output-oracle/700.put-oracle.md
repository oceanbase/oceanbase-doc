| Description   |                 |
|---------------|-----------------|
| machine-translation ||
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
|slug|put-oracle-mode|

# PUT

#docslug#/oceanbase-database/oceanbase-database/V4.0.0/PUT
The `PUT` procedure is used to buffer part of a row of data.

  <main id="notice" >
    <h4>Applicability</h4>
    <p>This topic applies only to OceanBase Database Enterprise Edition. OceanBase Database Community Edition does not support this feature. </p>
  </main>

## Syntax

```sql
DBMS_OUTPUT.PUT (
item IN VARCHAR2);
```

## Parameters

| **Parameter** | **Description** |
|--------|--------|
| item   | The row to be buffered. |

## Considerations

* You can call the `PUT` procedure multiple times to build an information row in segments, or you can call the `PUT_LINE` procedure to buffer an entire row of information.

* A line terminator is automatically added after a call to the `PUT_LINE` procedure. If you create a line by calling the `PUT` procedure, you must call the `NEW_LINE` procedure to add a line terminator. The `GET_LINE` and `GET_LINES` procedures do not return lines that are not terminated with a line terminator.

* An error message is returned if the number of lines exceeds the limit.

* Output created by using the `PUT` or `PUT_LINE` procedure is buffered. The output cannot be retrieved until the PL program unit that buffers the output returns to its caller.

  For example, OBClient does not display `DBMS_OUTPUT` messages until the PL program completes. There is no mechanism in the PL program to refresh the `DBMS_OUTPUT` buffer.

  ```sql
  obclient> SET SERVEROUTPUT ON;
  obclient> BEGIN
      DBMS_OUTPUT.PUT_LINE ('hello');
      DBMS_LOCK.SLEEP (10);
      END;
  ```