| Description   |                 |
|---------------|-----------------|
| machine-translation ||
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
|slug|utl_file-overview-oracle-mode|

# Overview of UTL_FILE

The `UTL_FILE` package enables PL/SQL programs to read and write text files on the operating system. `UTL_FILE` provides a restricted version of operating system stream file I/O.

  <main id="notice" >
    <h4>Applicability</h4>
    <p>This topic applies only to OceanBase Database Enterprise Edition. OceanBase Database Community Edition does not support this feature. </p>
  </main>

## Privilege requirements

The user must have the `EXECUTE` privilege on the `UTL_FILE` package.

## Data structures

The `UTL_FILE` package defines a `RECORD` type named `FILE_TYPE`.

The contents of `FILE_TYPE` are private to the `UTL_FILE` package and cannot be referenced or modified.

### Syntax

```sql
TYPE file_type IS RECORD (
id                BINARY_INTEGER,
datatype          BINARY_INTEGER,
byte_mode         BOOLEAN);
```

### Parameters

|    Parameter     |                Description                 |
|-----------|-----------------------------------|
| id        | The numeric value of the internal file handle.                      |
| datatype  | The file type: `CHAR`, `NCHAR`, or other (binary). |
| byte_mode | Indicates whether the file is opened as a binary file or a text file.           |

## Exceptions

|        Exception         |         Description                     |
|---------------------|---------------------------------|
| INVALID_PATH        | The file path is invalid.        |
| INVALID_MODE        | The `open_mode` parameter in the `FOPEN` function is invalid.   |
| INVALID_FILEHANDLE  | The file handle is invalid.   |
| INVALID_OPERATION   | The file operation is not supported.    |
| READ_ERROR          | The read buffer is too small, or an operating system-related error occurred during the read.    |
| WRITE_ERROR         | An operating system-related error occurred during the write. |
| INTERNAL_ERROR      | A PL/SQL error defined internally.   |
| FILE_OPEN           | An error occurred while opening the file.  |
| INVALID_MAXLINESIZE | The `max_linesize` parameter in the `FOPEN` function is invalid. The supported range is \[1,32767\]. |
| INVALID_FILENAME    | The filename parameter is invalid. |
| ACCESS_DENIED       | The file cannot be accessed due to insufficient privileges.  |
| INVALID_OFFSET      | Possible reasons:<ul><li> Both `ABSOLUTE_OFFSET` and `ABSOLUTE_OFFSET` are `NULL`    </li><li> `ABSOLUTE_OFFSET` is less than 0    </li><li> When `ABSOLUTE_OFFSET` is `NULL`, the specified `RELATIVE_OFFSET` exceeds the file length. </li></ul>   |

## Subprograms

The following table lists the `UTL_FILE` subprograms supported in the current OceanBase Database version and their brief descriptions.

|                           Subprogram                           |                Description                 |
|---------------------------------------------------------|-----------------------------------|
| [FCLOSE](../26100.utl-file-oracle/200.fclose-oracle.md)   | Closes the file.                             |
| [FFLUSH](../26100.utl-file-oracle/300.fflush-oracle.md)   | Flushes the data in the cache to the file.                     |
| [FOPEN](../26100.utl-file-oracle/400.fopen-oracle.md)    | Opens the file based on the specified information.                    |
| [FGETATTR](../26100.utl-file-oracle/450.fgetattr-oracle.md) | Reads and returns the attributes of a disk file.                     |
| [FGETPOS](../26100.utl-file-oracle/500.fgetpos-oracle.md)  | Retrieves the current access position (offset) of the file, in bytes.         |
| [FREMOVE](../26100.utl-file-oracle/600.fremove-oracle.md)  | Deletes the disk file (requires appropriate deletion privileges), similar to the UNIX `rm` command. |
| [FRENAME](../26100.utl-file-oracle/700.frename-oracle.md)  | Renames the existing file to a new name, similar to the UNIX `mv` command.    |
| [FSEEK](../26100.utl-file-oracle/800.fseek-oracle.md)    | Adjusts the file position forward or backward by the specified number of bytes.               |
| [IS_OPEN](../26100.utl-file-oracle/850.is-open-oracle.md) | Tests whether a file handle identifies an open file.                |
| [NEW_LINE](../26100.utl-file-oracle/900.new-line-utl-oracle.md) | Writes one or more operating system-specified line terminators to the file.            |
| [GET_LINE](../26100.utl-file-oracle/1000.get-line-utl-oracle.md) | Reads a line of text.                           |
| [PUT](../26100.utl-file-oracle/1100.put-utl-oracle.md)      | Writes a string to the file.                         |
| [PUT_LINE](../26100.utl-file-oracle/1200.put-line-utl-oracle.md) | Writes a line to the file and appends an operating system-specified line terminator.           |