| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |

# EHLO

You can execute the `EHLO` statement to perform an initial handshake with the SMTP service.

<main id="notice" >
    <h4>Applicability</h4>
    <p>This topic applies only to OceanBase Database in Oracle mode. </p>
  </main>

## Syntax

```sql
UTL_SMTP.EHLO (
   c       IN OUT NOCOPY connection, 
   domain  IN) 
RETURN replies;

UTL_SMTP.EHLO (
   c       IN OUT NOCOPY connection, 
   domain  IN);
```

## Parameter explanation

| **Parameter** |         **Description**         |
|--------|------------------------|
|c|The SMTP connection. |
|domain|The domain name of the sending host, used for identification. |

## Return value

| **Return value** |          **Description**          |
|---------|--------------------------|
|reply|The reply to the command. If multiple replies are returned, the last reply is returned. |

## Remarks

The `ehlo` statement is an extension of the `helo` statement, allowing the server to return descriptive information about its configuration. For compatibility, each line of information is prefixed with the status code `250`.