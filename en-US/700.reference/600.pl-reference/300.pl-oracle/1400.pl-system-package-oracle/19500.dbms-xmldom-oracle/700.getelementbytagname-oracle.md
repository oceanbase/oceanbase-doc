| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
| machine-translation | en          |
|slug|getelementbytagname-oracle-mode|

# GETELEMENTSBYTAGNAME

The `getElementsByTagName` function is used to find all element nodes with the tag name `tag_name` in a depth-first manner throughout the subtree of the specified document `doc`, and returns them as a list of nodes.

<main id="notice" >
  <h4>Applicability</h4>
  <p>This content applies only to OceanBase Database Enterprise Edition. OceanBase Database Community Edition provides only the MySQL-compatible mode.</p>
</main>

<main id="notice" type='explain'>
  <h4>Note</h4>
  <p>For V4.4.2, this subprogram is available starting with OceanBase Database V4.4.2 BP2.</p>
</main>

## Syntax

```sql
DBMS_XMLDOM.getElementsByTagName (
   doc      IN DBMS_XMLDOM.DOMDOCUMENT,
   tag_name IN VARCHAR2);
```

## Parameters

| **Parameter** | **Description** |
| ---------- | ---------- |
| doc | The `DBMS_XMLDOM.DOMDOCUMENT` handle of the XML document. |
| tag_name | The element tag name to match. A value of `'*'` matches any element tag. |

## Return value

A `DBMS_XMLDOM.DOMNODELIST` of matching element nodes.

## Considerations

An empty return list indicates no matching elements were found. To traverse the results, use [GETLENGTH](../19500.dbms-xmldom-oracle/800.getlength-xmldom-oracle.md) and [ITEM](../19500.dbms-xmldom-oracle/1000.item-oracle.md); to read element text, call [GETNODEVALUE](../19500.dbms-xmldom-oracle/900.getnodevalue-oracle.md).
