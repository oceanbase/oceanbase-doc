| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
| machine-translation | en          |
|slug|getlength-xmldom-oracle-mode|

# GETLENGTH

The `GETLENGTH` function returns the number of nodes in a node list. It is commonly used to obtain an upper limit before traversing nodes or to implement secure access when used with `ITEM`.

<main id="notice" >
  <h4>Applicability</h4>
  <p>This content applies only to OceanBase Database Enterprise Edition. OceanBase Database Community Edition provides only the MySQL-compatible mode.</p>
</main>

<main id="notice" type='explain'>
  <h4>Note</h4>
  <p>For V4.4.2, this subprogram is available starting with OceanBase Database V4.4.2 BP2.</p>
</main>

## Syntax

```sql
DBMS_XMLDOM.getLength (
   nodelist IN DBMS_XMLDOM.DOMNODELIST);
```

## Parameters

| **Parameters** | **Explanation** |
| ---------- | ---------- |
| nodelist | To be counted`DBMS_XMLDOM.DOMNODELIST`Handle. |

## Return value

The number of nodes in the node list, which is of the `NUMBER` type.

## Usage instructions

The index starts at 0 and has a valid range of `[0, GETLENGTH(nodelist) - 1]`. If the list is empty, 0 is returned.

## Examples

This example uses the `DBMS_XMLPARSER` system package to parse an XML document and obtain a `DOMDOCUMENT` handle. Then, it uses the `DBMS_XMLDOM` system package to obtain a `DOMNODELIST` handle for the XML document and retrieve the number of nodes in the list:

```sql
-- Declare parser and document handle variables
DECLARE
  p   DBMS_XMLPARSER.PARSER;
  doc DBMS_XMLDOM.DOMDOCUMENT;
  nl  DBMS_XMLDOM.DOMNODELIST;
  len NUMBER;
BEGIN
  -- Create a parser instance
  p := DBMS_XMLPARSER.NEWPARSER;
  -- Parse the XML document in the parser.
  DBMS_XMLPARSER.PARSECLOB(p, '<root><a/><b/></root>');
  -- Get the DOMDocument handle of an XML document
  doc := DBMS_XMLPARSER.GETDOCUMENT(p);
  -- Release parser instance
  DBMS_XMLPARSER.FREEPARSER(p);
  -- Get list of all elements in an XML document
  nl := DBMS_XMLDOM.GETELEMENTSBYTAGNAME(doc, '*');
  -- Get the number of nodes in the list
  len := DBMS_XMLDOM.GETLENGTH(nl);
  -- Release XML Document Resources
  DBMS_XMLDOM.FREEDOCUMENT(doc);
END;
/
```
