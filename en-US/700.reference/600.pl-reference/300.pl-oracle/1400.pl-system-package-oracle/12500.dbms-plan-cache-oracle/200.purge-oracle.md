| Description   |                 |
|---------------|-----------------|
| machine-translation ||
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
|slug|purge-pl-oracle-mode|

# PURGE

#docslug#/oceanbase-database/oceanbase-database/V4.0.0/PURGE
The `PURGE` procedure is used to remove a specified SQL statement from the plan cache.

  <main id="notice" >
    <h4>Applicability</h4>
    <p>This topic applies only to OceanBase Database Enterprise Edition. OceanBase Database Community Edition does not support this feature. </p>
  </main>

## Syntax

```sql
DBMS_PLAN_CACHE.PURGE (
sql_id           VARCHAR2 NOT NULL,
schema        VARCHAR2 DEFAULT NULL
global          BOOLEAN  DEFAULT FALSE);
```

## Parameters

|   Parameter   |                                      Description                                       |
|--------|-------------------------------------------------------------------------------|
| sql_id | The SQL identifier, which is 32 bytes in length. After the SQL statement is removed, all its execution plans become invalid.                                    |
| schema | The schema from which the execution plans are generated. If this parameter is set to `NULL`, the execution plans of all schemas become invalid. |
| global | If the value is `FALSE`, only the plan cache of the current server is cleared. If the value is `TRUE`, the plan cache of all servers in the current tenant is cleared.                   |