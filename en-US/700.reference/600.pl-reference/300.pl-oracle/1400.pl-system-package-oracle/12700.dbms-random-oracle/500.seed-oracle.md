| Description   |                 |
|---------------|-----------------|
| machine-translation ||
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
|slug|seed-oracle-mode|

# SEED

`SEED` is a stored procedure that resets the seed.

  <main id="notice" >
    <h4>Applicability</h4>
    <p>This topic applies only to OceanBase Database Enterprise Edition. OceanBase Database Community Edition does not support this feature. </p>
  </main>

## Syntax

```sql
DBMS_RANDOM.SEED (
   seed  IN  BINARY_INTEGER);

DBMS_RANDOM.SEED (
   seed  IN  VARCHAR2)
```

## Parameters

| **Parameter** |      **Description**      |
|--------|------------------|
| seed   | The seed number or string used to generate random numbers. |

## Considerations

The seed can be a string of up to 2000 characters.