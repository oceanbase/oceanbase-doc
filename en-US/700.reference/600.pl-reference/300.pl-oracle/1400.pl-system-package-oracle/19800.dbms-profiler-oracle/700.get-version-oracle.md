| Description   |                 |
|---------------|-----------------|
| machine-translation ||
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
|slug|get_version-oracle-mode|

# GET_VERSION

`GET_VERSION` returns the version of the `DBMS_PROFILER` package.

  <main id="notice" >
    <h4>Applicability</h4>
    <p>This topic applies only to OceanBase Database Enterprise Edition. OceanBase Database Community Edition does not support this feature. </p>
  </main>

## Syntax

```sql
DBMS_PROFILER.GET_VERSION (
   major  OUT BINARY_INTEGER,
   minor  OUT BINARY_INTEGER);
```

## Parameters

|       Parameter       |               Description                |
|----------------|---------------------------------|
| major | The major version of the `DBMS_PROFILER` package. |
| minor | The minor version of the `DBMS_PROFILER` package.                  |

## Examples

```shell
obclient [SYS]> DECLARE
    ->    -- Define variables to store the major and minor versions.
    ->    l_major BINARY_INTEGER;
    ->    l_minor BINARY_INTEGER;
    -> BEGIN
    ->    -- Call the GET_VERSION procedure from the DBMS_PROFILER package.
    ->    DBMS_PROFILER.get_version(l_major, l_minor);
    ->
    ->    -- Output the results to the console.
    ->    DBMS_OUTPUT.PUT_LINE('DBMS_PROFILER Major Version: ' || l_major);
    ->    DBMS_OUTPUT.PUT_LINE('DBMS_PROFILER Minor Version: ' || l_minor);
    -> END;
    -> /
Query OK, 1 row affected (0.155 sec)

DBMS_PROFILER Major Version: 2
DBMS_PROFILER Minor Version: 0
```