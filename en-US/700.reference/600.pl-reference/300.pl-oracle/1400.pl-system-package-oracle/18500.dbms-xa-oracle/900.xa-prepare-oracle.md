| Description   |                 |
|---------------|-----------------|
| machine-translation ||
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
|slug|xa_prepare-oracle-mode|

# XA_PREPARE

The `XA_PREPARE` procedure prepares the transaction branch specified by `xid` for commit.

  <main id="notice" >
    <h4>Applicability</h4>
    <p>This topic applies only to OceanBase Database Enterprise Edition. OceanBase Database Community Edition does not support this feature. </p>
  </main>

## Syntax

```sql
DBMS_XA.XA_PREPARE (
   xid   IN  DBMS_XA_XID)
RETURN PLS_INTEGER;
```

## Parameters

| **Parameter** |                                  **Description**                                   |
|--------|---------------------------------------------------------------------------|
| xid    | See [DBMS_XA_XID object type](../18500.dbms-xa-oracle/300.the-type-of-the-dbms-xa-xid-object-oracle.md). |

## Return value

See [Possible constant return values of DBMS_XA](../18500.dbms-xa-oracle/200.dbms-xa-constant-oracle.md).

The procedure may return the following values: `XA_OK`, `XA_RDONLY`, `XA_RB\*`, `XAER_RMERR`, `XAER_RMFAIL`, `XAER_NOTA`, `XAER_INVAL`, or `XAER_PROTO`.

## Considerations

* An application must track all branches of a global transaction and prepare each branch. An application can call `XA_COMMIT` only after all branches of the global transaction have been successfully prepared and `XA_PREPARE` returns `XA_OK`.