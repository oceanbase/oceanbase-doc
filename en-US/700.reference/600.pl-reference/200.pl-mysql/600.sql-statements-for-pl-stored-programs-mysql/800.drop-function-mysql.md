| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |
|machine-translation|                 |
|slug|drop-function-mysql|

# DROP FUNCTION

You can use the `DROP FUNCTION` statement to drop a stored function.

## Syntax

```sql
DROP FUNCTION [IF EXISTS] sp_name
```

## Parameters

| **Parameter** | **Description** |
|--------------|----------|
| IF EXISTS | Optional. If the function does not exist, it does not return an error but generates a warning. |
| sp_name | The name of the stored function to drop. You can use `database_name.sp_name` to specify the database. |

To drop a stored routine, you must have the `ALTER ROUTINE` privilege on the routine.

If the stored function does not exist, you can use the `IF EXISTS` clause to avoid errors and generate a warning. You can use the `SHOW WARNINGS` statement to view the warning.

## Examples

Drop a stored function.

```sql
obclient> DROP FUNCTION IF EXISTS my_func;
```

Drop a stored function in the specified database:

```sql
obclient> DROP FUNCTION IF EXISTS test_db.calculate_sum;
```
