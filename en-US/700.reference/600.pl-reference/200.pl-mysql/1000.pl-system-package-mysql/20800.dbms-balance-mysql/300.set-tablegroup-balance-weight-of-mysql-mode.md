| Description   |                 |
|---------------|-----------------|
| machine-translation ||
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |
|slug|set_tablegroup_balance_weight-mysql-mode|

# SET_TABLEGROUP_BALANCE_WEIGHT

The `SET_TABLEGROUP_BALANCE_WEIGHT` procedure is used to set the weight of a table group of the `SHARDING NONE` type.

<main id="notice" type='explain'>
  <h4>Note</h4>
  <p>This procedure is available starting from OceanBase Database V4.4.1.</p>
</main>

## Syntax

```sql
DBMS_BALANCE.SET_TABLEGROUP_BALANCE_WEIGHT(
    weight                INT,
    tablegroup_name       VARCHAR(65535)
    ); 
```

## Parameters

| Parameter | Description |
|------|------|
| weight | The weight of the table group. This parameter is an integer in the range [1, +∞). The larger the value, the higher the resource allocation ratio. |
| tablegroup_name | The name of the table group. |

## Considerations

* `DBMS_BALANCE.SET_TABLEGROUP_BALANCE_WEIGHT` is used to set the weight of a `SHARDING NONE` table group.

* When `enable_database_sharding_none = true`, the default weight of the table group bound to the new database is 1. Other `SHARDING NONE` table groups without specified weights have a default weight of 0 and do not participate in weight balancing.
* Table-level and partition-level weights in weighted table groups are invalid.
* We recommend that you divide the table group weights into three levels:

  * The weight of the first level = 100% * the number of `SHARDING NONE` table groups.
  * The weight of the second level = 50% * the number of `SHARDING NONE` table groups.
  * The weight of the third level = 1.

## Examples

* Call the procedure in the sys tenant to set the weight of the `TG_TD_test` table group to 10 in the `mysql` tenant.

    ```shell
    obclient> CALL DBMS_BALANCE.SET_TABLEGROUP_BALANCE_WEIGHT(10, 'TG_TD_test') TENANT = 'mysql';
    ```

* Call the procedure in the user tenant to set the weight of the `TG_TD_test` table group to 10 in the user tenant.

    ```shell
    obclient> CALL DBMS_BALANCE.SET_TABLEGROUP_BALANCE_WEIGHT(10, 'TG_TD_test');
    ```

## References

* For more information about how to remove the weight of a `SHARDING NONE` table group, see [CLEAR_TABLEGROUP_BALANCE_WEIGHT](400.clear-tablegroup-balance-weight-of-mysql-mode.md).
* For more information about how to query the weight of a table group, see [oceanbase.CDB_OB_DATABASES](../../../../700.system-views/300.system-view-of-sys-tenant/200.dictionary-view-of-sys-tenant/8300.o-cdb_ob_databases-of-sys-tenant.md) or [oceanbase.DBA_OB_DATABASES](../../../../700.system-views/400.system-view-of-mysql-mode/200.dictionary-view-of-mysql-mode/8500.o-dba_ob_databases-of-mysql-mode.md).
* For more information about how to control whether automatic aggregation is enabled for user tables when a database is created, see [enable_database_sharding_none](../../../../800.configuration-items-and-system-variables/100.system-configuration-items/400.tenant-level-configuration-items/2450.enable_database_sharding_none.md).