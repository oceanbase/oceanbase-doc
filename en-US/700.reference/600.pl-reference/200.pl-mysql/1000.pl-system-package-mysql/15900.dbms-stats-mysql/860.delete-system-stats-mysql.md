| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |
|machine-translation| en          |

# DELETE_SYSTEM_STATS

The `DELETE_SYSTEM_STATS` procedure is used to delete system statistics.

## Syntax

```sql
DBMS_STATS.DELETE_SYSTEM_STATS();
```

## Exceptions

|    Error code    |        Description     |
|-----------|------------------|
| HY000     | The name of the system statistics is incorrect. |

## Considerations

* To run this procedure, you must connect as the specified user or have the `SYSDBA` privilege.

## Examples

Using the `DBMS_STATS.DELETE_SYSTEM_STATS` stored procedure to delete all system statistics is very simple because it requires no parameters.

```sql
BEGIN
    -- Delete all system statistics
    DBMS_STATS.DELETE_SYSTEM_STATS();
END;
/
```