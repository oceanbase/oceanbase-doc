| Description   |                 |
|---------------|-----------------|
| machine-translation ||
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |
|slug|refresh-mysql|

# REFRESH

`REFRESHE` is used to refresh a specified materialized view.

<main id="notice" >
    <h4>Applicability</h4>
    <p>This topic applies only to OceanBase Database Enterprise Edition. OceanBase Database Community Edition does not support this feature. </p>
  </main>

## Syntax

```sql
DBMS_MVIEW.REFRESH (
    mv_name                IN    VARCHAR(65535)
    [, method              IN    VARCHAR(65535) DEFAULT NULL]
    [, refresh_parallel    IN    INT DEFAULT 0]
    [, nested              IN    BOOLEAN DEFAULT FALSE]
    [, nested_refresh_mode IN    VARCHAR DEFAULT NULL]
);
```

## Parameters

<main id="notice" type='explain'>
  <h4>Note</h4>
  <p>Non-nested materialized views do not support cascading refresh. Specifying the <code>nested</code> and <code>nested_refresh_mode</code> parameters is meaningless. By default, independent refresh is performed. </p>
</main>

|       **Parameter**      |       **Description**        |
|---------------------|-----------------------|
| mv_name             | The name of the materialized view to be refreshed.|
| method              | The refresh strategy of the materialized view. You can specify the default refresh strategy when you create a materialized view. Valid values: <ul><li>`F` or `f`: fast refresh. </li><li>`?`: force refresh. </li><li>`C` or `c`: complete refresh. </li><li>`A` or `a`: always refresh, which is equivalent to `C`. </li></ul>|
| refresh_parallel    | The concurrency level of the refresh operation, that is, the number of threads that can be executed in parallel.|
| nested              | The refresh mode of a nested materialized view. Valid values: <ul><li>`FALSE`: default value. Independent refresh is performed. </li><li>`TRUE`: cascading refresh is performed. </li></ul>|
| nested_refresh_mode | The refresh mode of a nested materialized view. Valid values: <ul><li>When the value of the `nested` parameter is `FALSE`, the value of the `nested_refresh_mode` parameter is invalid. </li><li>When the value of the `nested` parameter is `TRUE`: <ul><li>Valid values of the `nested_refresh_mode` parameter: <ul><li>`inconsistent`: non-consistent cascading refresh. </li><li>`consistent`: consistent cascading refresh. </li></ul> </li><li>If the `nested_refresh_mode` parameter is not specified, non-consistent cascading refresh is performed by default. </li></ul> </li></ul>|

## Examples

* Independent refresh:

  * Specify only the `mv_name` parameter.

    ```shell
    obclient> CALL DBMS_MVIEW.REFRESH('mv1');
    ```

  * Specify the `mv_name` and `nested` parameters, and set the value of the `nested` parameter to `false`.

    ```shell
    obclient> CALL DBMS_MVIEW.REFRESH(
        'mv1',
        nested=> false);
    ```

* Non-consistent cascading refresh:

  * Specify the `mv_name` and `nested` parameters, and set the value of the `nested` parameter to `true`. Do not specify the `nested_refresh_mode` parameter.

    ```shell
    obclient> CALL DBMS_MVIEW.REFRESH(
        'mv1',
        nested=> true);
    ```

  * Specify the `mv_name`, `nested`, and `nested_refresh_mode` parameters, and set the value of the `nested` parameter to `true` and the value of the `nested_refresh_mode` parameter to `inconsistent`.

    ```shell
    obclient> CALL DBMS_MVIEW.REFRESH(
        'mv1',
        nested=> true,
        nested_refresh_mode => 'inconsistent');
    ```

* Consistent cascading refresh:

  Specify the `mv_name`, `nested`, and `nested_refresh_mode` parameters, and set the value of the `nested` parameter to `true` and the value of the `nested_refresh_mode` parameter to `consistent`.

  ```shell
  obclient> CALL DBMS_MVIEW.REFRESH(
      'mv1',
      nested=> true,
      nested_refresh_mode => 'consistent');
  ```

* The following examples show error scenarios:

  * The value of the `nested` parameter is `false`, and the value of the `nested_refresh_mode` parameter is specified. An error is returned.

    ```shell
    obclient> CALL DBMS_MVIEW.REFRESH(
        'mv1',
        nested=> false,
        nested_refresh_mode => 'xxxx');
    ```

  * The value of the `nested` parameter is not specified, and the value of the `nested_refresh_mode` parameter is specified. An error is returned.

    ```shell
    obclient> CALL DBMS_MVIEW.REFRESH(
        'mv1',
        nested_refresh_mode => 'xxxx');
    ```

  * The value of the `nested` parameter is `true`, and the value of the `nested_refresh_mode` parameter is an invalid string. An error is returned.

    ```shell
    obclient> CALL DBMS_MVIEW.REFRESH(
        'mv1',
        nested=> true,
        nested_refresh_mode => 'illegal string');
    ```

## References

For more information about refreshing a materialized view, see [Refresh a materialized view](../../../../300.database-object-management/100.manage-object-of-mysql-mode/600.manage-views-of-mysql-mode/200.manage-materialized-views-of-mysql-mode/400.refresh-materialized-views-of-mysql-mode.md).