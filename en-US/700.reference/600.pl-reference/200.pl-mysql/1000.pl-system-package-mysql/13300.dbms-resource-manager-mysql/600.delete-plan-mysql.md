| Description   |                 |
|---------------|-----------------|
| machine-translation ||
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |
|slug|delete_plan-mysql-mode|

# DELETE_PLAN

The `DELETE_PLAN` procedure is used to delete a specified plan and all the plan instructions it references.

## Syntax

```sql
DBMS_RESOURCE_MANAGER.DELETE_PLAN (
    plan IN VARCHAR2);
```

## Parameters

`plan` specifies the name of the resource plan to be deleted.

## Examples

```sql
obclient> CALL DBMS_RESOURCE_MANAGER.DELETE_PLAN('NIGHT');
Query OK, 0 rows affected
```