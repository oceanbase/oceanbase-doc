| Description   |                 |
|---------------|-----------------|
| machine-translation ||
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |
|slug|auto_refresh_external_table-mysql-mode|

# AUTO_REFRESH_EXTERNAL_TABLE

`auto_refresh_external_table` is used to periodically refresh the metadata of an external table.

## Syntax

```sql
DBMS_EXTERNAL_TABLE.auto_refresh_external_table(interval int)
```

## Parameters

| **Parameter**           | **Description**                                            |
|------------------|-----------------------------------------------------|
| interval int | Specifies the time interval for refreshing the metadata of an external table. The value ranges from -1 to infinity, in seconds.<ul><li>When the value is -1, the periodic refresh task is disabled.</li><li>When the value is 0, the metadata of an external table is refreshed immediately.</li><li>When the value is greater than 0, the metadata of an external table is refreshed every specified seconds.</li></ul>|

## Examples

Execute the following command to disable the periodic refresh task.

```shell
obclient> CALL DBMS_EXTERNAL_TABLE.auto_refresh_external_table(-1);
Query OK, 0 rows affected (0.104 sec)
```