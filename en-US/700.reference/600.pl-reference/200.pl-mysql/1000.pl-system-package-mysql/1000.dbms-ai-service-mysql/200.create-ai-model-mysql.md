| Description   |                 |
|---------------|-----------------|
| machine-translation ||
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |
|slug|create_ai_model-mysql-mode|

# CREATE_AI_MODEL

The `CREATE_AI_MODEL` procedure is used to create an AI model object, specifying properties such as the model's name and type.

## Syntax

```sql
PROCEDURE create_ai_model(
    IN name VARCHAR(128), 
    IN params JSON);
```

## Parameters

<table>
  <thead>
    <tr>
      <th>Parameter</th>
      <th>Description</th>
      <th>Type</th>
      <th>Value range</th>
      <th>Nullable</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>name</td>
      <td>The name of the AI model object.</td>
      <td>VARCHAR(128)</td>
      <td></td>
      <td>No</td>
    </tr>
    <tr>
      <td rowspan="2">params</td>
      <td rowspan="2">The parameters related to the AI model, which must be a JSON object. Supported parameters include:<ul><li><code>type</code>: the type of AI model functionality</li><li><code>model_name</code>: the name of the large model</li></ul></td>
      <td rowspan="2"><code>type</code> and <code>model_name</code> are both JSON strings</td>
      <td><code>type</code> can only be <code>dense_embedding</code>, <code>sparse_embedding</code>, <code>completion</code>, or <code>rerank</code></td>
      <td>No</td>
    </tr>
    <tr>
      <td><code>model_name</code> specifies the name of the large model, for example, <code>text-embedding-v2</code></td>
      <td>No</td>
    </tr>
  </tbody>
</table>

## Examples

Create an AI model object of type `dense_embedding` with the large model `text-embedding-v2`.

```sql
CALL DBMS_AI_SERVICE.CREATE_AI_MODEL (
'my_ai_model_1', '{
    "type": "dense_embedding",
    "model_name": "text-embedding-v2"
  }');
```

## References

* [DBMS_AI_SERVICE Subprograms Overview](100.dbms-ai-service-overview-mysql.md)
* [DROP_AI_MODEL](300.drop-ai-model-mysql.md)