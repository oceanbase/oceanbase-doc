|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type|Oracle Mode|
|slug|modify-and-drop-trigger-oracle-compatible-mode|

# トリガーの変更と削除

PLはトリガーの変更と削除をサポートしています。

## トリガーの変更

現在、トリガーを変更するには、DDLステートメント内の `REPLACE TRIGGER` ステートメントを使用する必要があります。

通常、トリガーの作成と変更は以下のようなステートメントで記述されます：

```sql
CREATE OR REPLACE TRIGGER trigger_name ...
```

`trigger_name` が存在しない場合はトリガーが作成され、存在する場合はトリガーが変更されます。

## トリガーの削除

`DROP TRIGGER` を使用してトリガーを削除できます。構文は次のとおりです：

```sql
DROP TRIGGER trigger_name;
```

さらに、テーブルを削除すると、そのオブジェクト上に作成されたトリガーも自動的に削除されます。