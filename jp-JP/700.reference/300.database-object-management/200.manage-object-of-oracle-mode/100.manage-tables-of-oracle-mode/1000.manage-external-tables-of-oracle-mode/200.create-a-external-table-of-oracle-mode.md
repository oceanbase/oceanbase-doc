|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type|Oracle Mode|
|machine-translation|ja-marker|
|slug|create-external-table-oracle-compatible-mode-management|

# 外部テーブルの作成とクエリ

本記事では、Oracleモードで**ファイル外部テーブルを作成し、クエリする**手順について説明します。完全な構文や`LOCATION`、`FORMAT`などのパラメータの説明については、[CREATE EXTERNAL TABLE](../../../../500.sql-reference/100.sql-syntax/300.common-tenant-of-oracle-mode/900.sql-statement-of-oracle-mode/100.ddl-of-oracle-mode/1600.create-external-table-of-oracle-mode.md)を参照してください。

外部ファイルを一時的に読み取るだけの場合は、[FILESテーブル関数](../../../../500.sql-reference/100.sql-syntax/300.common-tenant-of-oracle-mode/500.functions-of-oracle-mode/550.table-functions-of-oracle-mode/200.files-table-function-of-oracle-mode.md)を使用してください。

## 前提条件

* クラスタがデプロイ済みでOracleモードのテナントが作成されており、データベースに接続されていること。
* 現在のユーザーに`CREATE`権限が付与されていること。現在のユーザー権限を確認する操作については、[ユーザー権限の確認](../../../../../600.manage/500.security-and-permissions/300.access-control/200.user-and-permission/300.permission-of-oracle-mode/600.view-user-permissions-of-oracle-mode.md)を参照してください。`CREATE TABLE`権限がない場合は、管理者に連絡し権限の追加を依頼してください。ユーザー権限の付与に関する操作については、[直接権限付与](../../../../../600.manage/500.security-and-permissions/300.access-control/200.user-and-permission/300.permission-of-oracle-mode/200.authority-of-oracle-mode.md)を参照してください。
* ローカルファイルのパスが[secure_file_priv](../../../../800.configuration-items-and-system-variables/200.system-variable/300.global-system-variable/12000.secure_file_priv-global.md)で許可されている範囲内であること。
* HDFSへのアクセスには、[OceanBaseデータベースJAVA SDK環境のデプロイ](../../../../../400.deploy/300.deploy-oceanbase-enterprise-edition/500.deploy-the-ob-java-sdk-environment.md)の設定が完了している必要があります。

## 例

### ローカルCSVファイル外部テーブルの作成

ローカルマシンの`/home/admin/oceanbase/`ディレクトリに`data.csv`ファイルが存在すると仮定します。ファイルの内容は以下の通りです。

```shell
1,"lin",98
2,"hei",90
3,"ali",95
```

1. OBServerノード上で、テナント管理者がローカルUnixソケットを介してクラスタのOracleテナントに接続します。

   接続例：

   ```shell
   obclient -S /home/admin/oceanbase/run/sql.sock -uroot@sys -p********
   ```

   ローカルUnixソケットを介してOceanBaseデータベースに接続する具体的な操作と説明については、[secure_file_priv](../../../../800.configuration-items-and-system-variables/200.system-variable/300.global-system-variable/12000.secure_file_priv-global.md)を参照してください。

2. データベースがアクセス可能なパス`/home/admin/oceanbase/`を設定します。

    ```sql
    SET GLOBAL secure_file_priv = "/home/admin/oceanbase/";
    ```

    コマンドの実行が成功した後、変更を有効にするにはセッションを再起動する必要があります。

3. データベースに再接続した後、外部テーブル`ext_t3`を作成します。

    ```sql
    CREATE EXTERNAL TABLE ext_t3(ID NUMBER(32), NAME VARCHAR2(30),SCORE NUMBER(32))
    LOCATION = '/home/admin/oceanbase/'
    FORMAT = (
      TYPE = 'CSV'
      FIELD_DELIMITER = ','
      FIELD_OPTIONALLY_ENCLOSED_BY ='"'
    )
    PATTERN = 'data.csv';
    ```

外部テーブルの作成が成功すると、通常のテーブルと同様に`SHOW CREATE TABLE`ステートメントを使用してテーブルの定義を確認できます。

```sql
SHOW CREATE TABLE ext_t3;
```

クエリ結果は次のとおりです：

```shell
+--------+-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
| TABLE  | CREATE TABLE                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
+--------+-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
| EXT_T3 | CREATE EXTERNAL TABLE "EXT_T3" (
  "ID" NUMBER(32) GENERATED ALWAYS AS (METADATA$FILECOL1),
  "NAME" VARCHAR2(30) GENERATED ALWAYS AS (METADATA$FILECOL2),
  "SCORE" NUMBER(32) GENERATED ALWAYS AS (METADATA$FILECOL3)
)
LOCATION='file:///home/admin/oceanbase/'
PATTERN='data.csv'
FORMAT (
  TYPE = 'CSV',
  FIELD_DELIMITER = ',',
  FIELD_OPTIONALLY_ENCLOSED_BY = '"',
  ENCODING = 'utf8mb4'
)COMPRESS FOR ARCHIVE REPLICA_NUM = 1 BLOCK_SIZE = 16384 USE_BLOOM_FILTER = FALSE TABLET_SIZE = 134217728 PCTFREE = 0 |
+--------+-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
1 row in set
```

通常のテーブルと同じようにアクセスすることもできます。外部テーブルをクエリする際、システムは外部テーブルのドライバ層を通じて外部ファイルを直接読み取り、ファイル形式に従って解析し、OceanBaseデータベースの内部データ型に変換してからデータ行を返します。先ほど作成した外部テーブル `ext_t3` のクエリ例を以下に示します。

```sql
SELECT * FROM ext_t3;
```

クエリ結果は次のとおりです：

```shell
+----+------+-------+
| id | name | score |
+----+------+-------+
|  1 | lin  |    98 |
|  2 | hei  |    90 |
|  3 | ali  |    95 |
+----+------+-------+
3 rows in set
```

さらに、外部テーブルは通常のテーブルと組み合わせてクエリ操作を実行することもできます。現在のデータベースに通常のテーブル `info` があり、そのデータは以下のとおりです：

```shell
+------+--------+------+
| name | sex    | age  |
+------+--------+------+
| lin  | male   |    8 |
| hei  | male   |    9 |
| li   | female |    8 |
+------+--------+------+
3 rows in set
```

外部テーブル `ext_t3` と通常のテーブル `info` を組み合わせたクエリの例を以下に示します。

```sql
SELECT info.* FROM info, ext_t3 WHERE info.name = ext_t3.name AND ext_t3.score > 90;
```

クエリ結果は次のとおりです：

```shell
+------+--------+------+
| name | sex    | age  |
+------+--------+------+
| lin  | male   |    8 |
| li   | female |    8 |
+------+--------+------+
2 rows in set
```

詳細なクエリ操作については、[データの読み取り](../../../../../300.develop/100.application-development-of-mysql-mode/500.read-data-of-mysql-mode/100.single-table-query-of-mysql-mode.md)を参照してください。

## 次のステップ

* ファイルリストの更新：`ALTER EXTERNAL TABLE table_name REFRESH;`。詳細については、[外部ファイルの管理](300.manage-external-files-of-oracle-mode.md)および[ALTER EXTERNAL TABLE](../../../../500.sql-reference/100.sql-syntax/300.common-tenant-of-oracle-mode/900.sql-statement-of-oracle-mode/100.ddl-of-oracle-mode/100.alter-external-table-of-oracle-mode.md)を参照してください。

## 関連ドキュメント

* [外部テーブルと外部ファイルについて](100.about-external-tables-of-oracle-mode.md)
* [CREATE EXTERNAL TABLE](../../../../500.sql-reference/100.sql-syntax/300.common-tenant-of-oracle-mode/900.sql-statement-of-oracle-mode/100.ddl-of-oracle-mode/1600.create-external-table-of-oracle-mode.md)
* [FILES テーブル関数](../../../../500.sql-reference/100.sql-syntax/300.common-tenant-of-oracle-mode/500.functions-of-oracle-mode/550.table-functions-of-oracle-mode/200.files-table-function-of-oracle-mode.md)
* [外部ファイルの管理](300.manage-external-files-of-oracle-mode.md)