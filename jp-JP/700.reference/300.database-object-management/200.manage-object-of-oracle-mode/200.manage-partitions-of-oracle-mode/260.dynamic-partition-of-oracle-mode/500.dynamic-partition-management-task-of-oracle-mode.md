|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type|Oracle Mode|
|slug|dynamic-partition-management-task-oracle-compatible-mode|

# 動的パーティション管理タスク

OceanBaseデータベースの動的パーティション管理には、システムパッケージ`DBMS_PARTITION`が追加され、動的パーティション管理をトリガーするサブプログラム`MANAGE_DYNAMIC_PARTITION()`を提供します。

## 動的パーティション管理の定期実行

動的パーティション管理タスクは、PLシステムパッケージ`DBMS_SCHEDULER`に基づいて定期的にトリガーされます。ユーザーテナント作成時には、定期的な動的パーティション管理タスク`SCHEDULED_MANAGE_DYNAMIC_PARTITION_HOURLY`と`SCHEDULED_MANAGE_DYNAMIC_PARTITION_DAILY`が組み込まれており、定期的に`DBMS_PARTITION.MANAGE_DYNAMIC_PARTITION()`を呼び出します。

* `SCHEDULED_MANAGE_DYNAMIC_PARTITION_HOURLY`は`TIME_UNIT`を`hour`に設定した動的パーティション管理をスケジュールし、毎正時に1回実行されます。スケジュール時間は変更できません。
* `SCHEDULED_MANAGE_DYNAMIC_PARTITION_DAILY`は`TIME_UNIT`を`day`、`week`、`month`、`year`に設定した動的パーティション管理をスケジュールし、毎日1回実行されます。デフォルトでは午前0時に実行されます。ユーザーはスケジュール時間を変更し、DDL業務のピーク時間帯を避けることができます。

### 定期的な動的パーティション管理タスクの確認

ユーザーテナント内で、`sys.DBA_SCHEDULER_JOBS`ビューを使用して、`JOB_NAME`を`SCHEDULED_MANAGE_DYNAMIC_PARTITION_DAILY`または`SCHEDULED_MANAGE_DYNAMIC_PARTITION_HOURLY`と指定することで、定期的な動的パーティション管理タスクを確認できます。

**例：**

* `JOB_NAME`を`SCHEDULED_MANAGE_DYNAMIC_PARTITION_DAILY`と指定します。

    ```sql
    SELECT JOB_NAME, JOB_ACTION, START_DATE, REPEAT_INTERVAL, ENABLED, NEXT_RUN_DATE, MAX_RUN_DURATION, COMMENTS
    FROM sys.DBA_SCHEDULER_JOBS
    WHERE JOB_NAME = 'SCHEDULED_MANAGE_DYNAMIC_PARTITION_DAILY'\G
    ```

    実行結果は次のとおりです：

    ```shell
    *************************** 1. row ***************************
            JOB_NAME: SCHEDULED_MANAGE_DYNAMIC_PARTITION_DAILY
          JOB_ACTION: DBMS_PARTITION.MANAGE_DYNAMIC_PARTITION(null, 'DAY,WEEK,MONTH,YEAR')
          START_DATE: 2025-04-16 00:00:00.000000
     REPEAT_INTERVAL: FREQ=DAILY;INTERVAL=1
             ENABLED: 1
       NEXT_RUN_DATE: 2025-04-17 00:00:00.000000
    MAX_RUN_DURATION: 3600
            COMMENTS: used to perform manage dynamic partition periodically
    1 row in set
    ```

* `JOB_NAME`を`SCHEDULED_MANAGE_DYNAMIC_PARTITION_HOURLY`と指定します。

    ```sql
    SELECT JOB_NAME, JOB_ACTION, START_DATE, REPEAT_INTERVAL, ENABLED, NEXT_RUN_DATE, MAX_RUN_DURATION, COMMENTS
    FROM sys.DBA_SCHEDULER_JOBS
    WHERE JOB_NAME = 'SCHEDULED_MANAGE_DYNAMIC_PARTITION_HOURLY'\G
    ```

    実行結果は次のとおりです：

    ```shell
    *************************** 1. row ***************************
            JOB_NAME: SCHEDULED_MANAGE_DYNAMIC_PARTITION_HOURLY
          JOB_ACTION: DBMS_PARTITION.MANAGE_DYNAMIC_PARTITION(null, 'HOUR')
          START_DATE: 2025-04-15 15:00:00.000000
     REPEAT_INTERVAL: FREQ=HOURLY;INTERVAL=1
             ENABLED: 1
       NEXT_RUN_DATE: 2025-04-16 18:00:00.000000
    MAX_RUN_DURATION: 3600
            COMMENTS: used to perform manage dynamic partition periodically
    1 row in set
    ```

### 定期的な動的パーティション管理タスクの変更

ユーザーテナント内で、現在のテナントの定期的な動的パーティション管理タスクを変更できます。

#### 定期的なパーティション管理タスクの無効化

```sql
CALL DBMS_SCHEDULER.DISABLE('SCHEDULED_MANAGE_DYNAMIC_PARTITION_DAILY');
```

```sql
CALL DBMS_SCHEDULER.DISABLE('SCHEDULED_MANAGE_DYNAMIC_PARTITION_HOURLY');
```

#### スケジュールされたパーティション管理タスクの有効化

```sql
CALL DBMS_SCHEDULER.ENABLE('SCHEDULED_MANAGE_DYNAMIC_PARTITION_DAILY');
```

```sql
CALL DBMS_SCHEDULER.ENABLE('SCHEDULED_MANAGE_DYNAMIC_PARTITION_HOURLY');
```

#### スケジュールされたパーティション管理タスクの日次スケジュール時間の変更

`SCHEDULED_MANAGE_DYNAMIC_PARTITION_DAILY` スケジュールタスクの日次スケジュール時間のみを変更できます。例えば、現在時刻が `2025-03-11 10:00:00` の場合、スケジュール時間を毎日の12時に変更したいとします：

```sql
CALL DBMS_SCHEDULER.SET_ATTRIBUTE('SCHEDULED_MANAGE_DYNAMIC_PARTITION_DAILY', 'START_DATE', TIMESTAMP '2025-03-11 12:00:00');
```

次回のスケジュール時間は `2025-03-11 12:00:00` となり、その後は毎日12時にスケジュールされます。

## 動的パーティション管理の手動実行

`DBMS_PARTITION.MANAGE_DYNAMIC_PARTITION()` を直接呼び出すことで、動的パーティション管理タスクを1回実行できます：

```sql
CALL DBMS_PARTITION.MANAGE_DYNAMIC_PARTITION();
```

`MANAGE_DYNAMIC_PARTITION` には `precreate_time` と `time_unit` の2つのパラメータがあります。詳細については、[MANAGE_DYNAMIC_PARTITION](../../../../600.pl-reference/300.pl-oracle/1400.pl-system-package-oracle/12600.dbms_partition-oracle/200.manage_dynamic_partition-of-oracle-mode.md) を参照してください。

* `precreate_time` を指定する場合、指定値とテーブルの `precreate_time` のうち大きい方の値で事前作成されます。例：

    ```sql
    CALL DBMS_PARTITION.MANAGE_DYNAMIC_PARTITION('3DAY');
    ```

* `time_unit` を指定する場合、テーブルの `time_unit` が指定された `time_unit` と一致する場合にのみ動的パーティション管理が実行されます。例：

    ```sql
    CALL DBMS_PARTITION.MANAGE_DYNAMIC_PARTITION(NULL, 'day, week');
    ```

## 動的パーティション管理タスクの履歴実行レコードのクエリ

ビュー [DBA_OB_TENANT_EVENT_HISTORY](../../../../700.system-views/500.system-view-of-oracle-mode/200.dictionary-view-of-oracle-mode/16100.dba_ob_tenant_event_history-of-oracle-mode.md) は、現在のテナント配下での動的パーティション管理タスクの履歴実行状況を記録しており、実行成功および失敗したテーブルID情報が含まれます：

```sql
SELECT * FROM sys.DBA_OB_TENANT_EVENT_HISTORY
WHERE EVENT = 'MANAGE_DYNAMIC_PARTITION';
```

戻り結果は次のとおりです：

```shell
+------------------------------+----------------+--------------------------+-----------------------+--------------------------+----------------------+--------+-------+--------+-------+--------+-------+--------+-------+--------+------------+----------------+----------+-----------------------------------+-----------+----------+-----------+
| TIMESTAMP                    | MODULE         | EVENT                    | NAME1                 | VALUE1                   | NAME2                | VALUE2 | NAME3 | VALUE3 | NAME4 | VALUE4 | NAME5 | VALUE5 | NAME6 | VALUE6 | EXTRA_INFO | SVR_IP         | SVR_PORT | TRACE_ID                          | COST_TIME | RET_CODE | ERROR_MSG |
+------------------------------+----------------+--------------------------+-----------------------+--------------------------+----------------------+--------+-------+--------+-------+--------+-------+--------+-------+--------+------------+----------------+----------+-----------------------------------+-----------+----------+-----------+
| 15-APR-25 04.00.04.528017 PM | DBMS_PARTITION | MANAGE_DYNAMIC_PARTITION | SUCCESS_TABLE_ID_LIST | [500004]                 | FAILED_TABLE_ID_LIST | []     | NULL  | NULL   | NULL  | NULL   | NULL  | NULL   | NULL  | NULL   | NULL       | xxx.xx.xxx.xxx |     2882 | YB42AC1E87C3-000632CB999BC733-0-0 |   4505353 |        0 | NULL      |
+------------------------------+----------------+--------------------------+-----------------------+--------------------------+----------------------+--------+-------+--------+-------+--------+-------+--------+-------+--------+------------+----------------+----------+-----------------------------------+-----------+----------+-----------+
1 rows in set
```

## 関連ドキュメント

* [動的パーティションの概要](100.dynamic-partition-overview-of-oracle-mode.md)
* [動的パーティションテーブルの作成](200.create-a-dynamic-partition-table-of-oracle-mode.md)
* [動的パーティションテーブルの表示](300.view-dynamic-partition-table-of-oracle-mode.md)
* [動的パーティションテーブルの変更](400.modify-dynamic-partition-table-of-oracle-mode.md)