|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type|MySQL Mode|
|machine-translation|ja-marker|

# シーケンスの確認

シーケンスを作成した後、`SHOW SEQUENCES` ステートメントまたはビューを使用してシーケンスを確認できます。

## SHOW SEQUENCESステートメントを使用してシーケンスを表示する

`SHOW SEQUENCES` ステートメントは、現在のテナントで作成されたすべてのシーケンスを表示します。

ステートメントは次のとおりです：

```sql
obclient> SHOW SEQUENCES
              [LIKE expression
               | LIKE expression IN database_name
               | IN database_name
               | WHERE sequence_name='sequence_name'
              ];
```

関連パラメータの説明は以下のとおりです：

* `LIKE expression`：クエリ対象のシーケンス名に関する情報を指定します。

* `IN database_name`：クエリ対象のシーケンスが属するデータベースを指定します。

* `WHERE sequence_name='sequence_name'`：クエリ対象のシーケンス名を指定します。

例：

* 現在のテナントで作成されたすべてのシーケンスを表示します。

  ```sql
  obclient> SHOW SEQUENCES;
  +-------------------+
  | Sequences_in_test |
  +-------------------+
  | seq1              |
  | meq2              |
  +-------------------+
  2 rows in set
  ```

* シーケンス名に `s` を含むすべてのシーケンスを表示します。

  ```sql
  obclient> SHOW SEQUENCES LIKE '%s%';
  +-------------------------+
  | Sequences_in_test (%s%) |
  +-------------------------+
  | seq1                    |
  +-------------------------+
  1 rows in set
  ```

* データベース `test` 内のすべてのシーケンスを表示します。

  ```sql
  obclient> SHOW SEQUENCES IN test;
  +-------------------+
  | Sequences_in_test |
  +-------------------+
  | seq1              |
  | meq2              |
  +-------------------+
  2 rows in set
  ```

* 現在のテナント内で名前が `seq1` のシーケンスを表示します。

  ```sql
  obclient> SHOW SEQUENCES WHERE sequence_name='seq1';
  +-------------------+
  | Sequences_in_test |
  +-------------------+
  | seq1              |
  +-------------------+
  1 row in set
  ```

## ビューを使用したシーケンスの確認

OceanBaseデータベースのMySQLテナントは、`oceanbase.DBA_OB_SEQUENCE_OBJECTS`ビューを照会することで、現在のテナント内のすべてのシーケンス情報を確認できます。これには、シーケンスID、シーケンス名、シーケンスが属するデータベース、シーケンス値の開始値、シーケンスの各値取得時のステップサイズ、シーケンスの繰り返し属性、およびシーケンスが取ることができる最大値と最小値などの情報が含まれます。

クエリ例は以下のとおりです：

```sql
obclient> SELECT * FROM oceanbase.DBA_OB_SEQUENCE_OBJECTS;
+-------------+------------------+-------------+---------------+-----------+-----------+--------------+------------+------------+------------+------------+---------------------+
| SEQUENCE_ID | SCHEMA_VERSION   | DATABASE_ID | SEQUENCE_NAME | MIN_VALUE | MAX_VALUE | INCREMENT_BY | START_WITH | CACHE_SIZE | ORDER_FLAG | CYCLE_FLAG | IS_SYSTEM_GENERATED |
+-------------+------------------+-------------+---------------+-----------+-----------+--------------+------------+------------+------------+------------+---------------------+
|      500003 | 1675748057234736 |      500002 | seq1          |         1 |        10 |            2 |          1 |         30 |          0 |          0 |                   0 |
+-------------+------------------+-------------+---------------+-----------+-----------+--------------+------------+------------+------------+------------+---------------------+
1 row in set
```

`oceanbase.DBA_OB_SEQUENCE_OBJECTS`ビューのフィールドの詳細については、[oceanbase.DBA_OB_SEQUENCE_OBJECTS](../../../700.system-views/400.system-view-of-mysql-mode/200.dictionary-view-of-mysql-mode/10700.o-dba_ob_sequence_objects-of-mysql-mode.md)を参照してください。

## 関連ドキュメント

シーケンス関連のその他の操作については、次のドキュメントを参照してください：

* [シーケンスの作成](../700.manage-sequence-of-mysql-mode/100.create-a-sequence-of-mysql-mode.md)

* [シーケンスのインポート](../700.manage-sequence-of-mysql-mode/300.use-a-sequence-of-mysql-mode.md)

* [シーケンスの変更](../700.manage-sequence-of-mysql-mode/400.modify-a-sequence-of-mysql-mode.md)

* [シーケンスの削除](../700.manage-sequence-of-mysql-mode/500.delete-a-squence-of-mysql-mode.md)