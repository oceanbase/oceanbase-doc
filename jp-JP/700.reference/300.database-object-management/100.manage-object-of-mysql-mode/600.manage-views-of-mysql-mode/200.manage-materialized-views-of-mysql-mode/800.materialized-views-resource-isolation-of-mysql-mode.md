|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type|MySQL Mode|
|slug|materialized-views-resource-isolation-mysql-compatible-mode|

# マテリアライズドビューのリソース分離

OceanBaseデータベースは、`DBMS_RESOURCE_MANAGER`がマテリアライズドビューの増分更新およびMLOG Purgeに対してリソース分離機能を提供することをサポートしており、マテリアライズドビューが使用できるリソースの上限を制限できます。

## 例

1. リソースプラン `mview_task` を作成します。

    ```sql
    CALL DBMS_RESOURCE_MANAGER.CREATE_PLAN('mview_task','このリソースプランはマテリアライズドビュー処理用です');
    ```

    リソースプランの作成に関する詳細は、[CREATE_PLAN](../../../../600.pl-reference/200.pl-mysql/1000.pl-system-package-mysql/13300.dbms-resource-manager-mysql/300.create-plan-mysql.md)を参照してください。

2. リソースユーザーグループ `mview_group` を作成します。

    ```sql
    CALL DBMS_RESOURCE_MANAGER.CREATE_CONSUMER_GROUP('mview_group','マテリアライズドビュー処理グループ');
    ```

    リソースユーザーグループの作成に関する詳細は、[CREATE_CONSUMER_GROUP](../../../../600.pl-reference/200.pl-mysql/1000.pl-system-package-mysql/13300.dbms-resource-manager-mysql/200.create-consumer-group-mysql.md)を参照してください。

3. リソースプラン指示子を作成し、CPUリソース使用率の上限を5%に制限します。

    ```sql
    CALL DBMS_RESOURCE_MANAGER.CREATE_PLAN_DIRECTIVE(
        PLAN => 'mview_task',
        GROUP_OR_SUBPLAN => 'mview_group',
        UTILIZATION_LIMIT => 5
        );
    ```

    リソースプラン指示子の作成に関する詳細は、[CREATE_PLAN_DIRECTIVE](../../../../600.pl-reference/200.pl-mysql/1000.pl-system-package-mysql/13300.dbms-resource-manager-mysql/400.create-plan-directive-mysql.md)を参照してください。

4. リソース使用グループ `mview_group` を `FUNCTION` にバインドします。

    ```sql
    CALL DBMS_RESOURCE_MANAGER.SET_CONSUMER_GROUP_MAPPING(
        ATTRIBUTE => 'FUNCTION',
        VALUE => 'MVIEW',
        CONSUMER_GROUP => 'mview_group'
        );
    ```

    セッションをリソースグループにマッピングするエントリに関する詳細は、[SET_CONSUMER_GROUP_MAPPING](../../../../600.pl-reference/200.pl-mysql/1000.pl-system-package-mysql/13300.dbms-resource-manager-mysql/800.set-consumer-group-mappingn-directive-mysql.md)を参照してください。

5. リソース管理プラン `mview_task` を有効化します。

    ```sql
    SET GLOBAL resource_manager_plan = 'mview_task';
    ```

    リソース管理プランの有効化に関する詳細は、[resource_manager_plan](../../../../800.configuration-items-and-system-variables/200.system-variable/300.global-system-variable/11500.resource_manager_plan-global.md)を参照してください。

6. CPUリソース使用率の上限を30%に変更します。

    ```sql
    CALL DBMS_RESOURCE_MANAGER.UPDATE_PLAN_DIRECTIVE(
        PLAN => 'mview_task',
        GROUP_OR_SUBPLAN => 'mview_group',
        NEW_UTILIZATION_LIMIT => 30
        );
    ```

    リソースプランの更新に関する詳細は、[UPDATE_PLAN_DIRECTIVE](../../../../600.pl-reference/200.pl-mysql/1000.pl-system-package-mysql/13300.dbms-resource-manager-mysql/900.update-plan-directive-mysql.md)を参照してください。

## 関連ドキュメント

* [マテリアライズドビューのログ](200.materialized-views-log-of-mysql-mode.md)
* [マテリアライズドビューのリフレッシュ](400.refresh-materialized-views-of-mysql-mode.md)
* [DBMS_RESOURCE_MANAGERの概要](../../../../600.pl-reference/200.pl-mysql/1000.pl-system-package-mysql/13300.dbms-resource-manager-mysql/100.dbms-resource-manager-overview-mysql.md)