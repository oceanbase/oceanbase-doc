|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type|MySQL Mode|
|machine-translation|ja-marker|

# テーブルグループの削除

テーブルグループ内にテーブルが存在しない場合、つまりテーブルグループがどのテーブルにも参照されていない場合は、そのテーブルグループを削除できます。

## 前提条件

テーブルグループを削除する前に、ビュー`CDB_OB_TABLEGROUP_TABLES`または`DBA_OB_TABLEGROUP_TABLES`を使用して、テーブルグループ内にテーブルが存在するかどうかを確認してください。テーブルグループ内にテーブルが存在しない場合にのみ、テーブルグループを削除できます。ビューを使用してテーブルグループ内にテーブルが存在するかどうかを確認する詳細な操作については、[テーブルグループ情報の表示](../400.manage-table-groups-of-mysql-mode/300.view-the-information-of-a-table-group-of-mysql-mode.md)を参照してください。

## テーブルグループの削除

テーブルグループを削除するSQLステートメントは次のとおりです：

```sql
DROP TABLEGROUP [IF EXISTS] tablegroup_name;
```

ここで、`tablegroup_name`はテーブルグループ名を表します。削除対象のテーブルグループが存在せず、かつ`IF EXISTS`が指定されていない場合、システムはエラーを報告します。このステートメントを実行するには、現在のユーザーがグローバル`DROP`権限を持っている必要があります。

テーブルグループ`tblgroup1`を削除する例は次のとおりです：

```sql
DROP TABLEGROUP IF EXISTS tblgroup1;
```

## 関連ドキュメント

[テーブルグループ情報の表示](../400.manage-table-groups-of-mysql-mode/300.view-the-information-of-a-table-group-of-mysql-mode.md)