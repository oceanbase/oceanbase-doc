|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||
|machine-translation|ja-marker|

# 自動トリガーによるメジャーコンパクション

メジャーコンパクションは、自動トリガーによって実行できます。

## 前提条件

コンパクションをトリガーする前に、グローバルなコンパクションスイッチが有効になっていることを確認する必要があります。グローバルなコンパクションスイッチは、クラスタレベルの構成パラメータ`enable_major_freeze`によって制御され、デフォルト値は`True`であり、これはコンパクションスイッチが有効であることを示します。

現在のクラスタでグローバルなコンパクションスイッチが有効になっていない場合は、次の手順に従って有効にしてください。

1. `root`ユーザーでクラスタの`sys`テナントにログインします。

2. グローバルなコンパクションスイッチが有効になっているかどうかを確認します。

   ```sql
   obclient> SHOW PARAMETERS LIKE 'enable_major_freeze';
   ```

3. 有効になっていない場合は、以下のステートメントを実行してコンパクションスイッチを有効にします。

   ```sql
   obclient> ALTER SYSTEM SET enable_major_freeze='True';
   ```

   パラメータ`enable_major_freeze`の詳細については、[enable_major_freeze](../../../800.configuration-items-and-system-variables/100.system-configuration-items/300.cluster-level-configuration-items/7300.enable_major_freeze.md)を参照してください。

## 自動コンパクションのトリガー条件

フリーズ（minor freeze）の回数が一定のしきい値に達すると、自動的にコンパクションがトリガーされます。

## 関連ドキュメント

* [定期的なコンパクションのトリガー](../200.merge-management/300.scheduled-trigger-merge.md)

* [手動によるコンパクションのトリガー](../200.merge-management/400.manually-trigger-a-merge.md)

* [手動によるコンパクションの制御](../200.merge-management/500.manually-control-a-merge.md)

* [コンパクションプロセスの表示](../200.merge-management/500.view-merge-process.md)