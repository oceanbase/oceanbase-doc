|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type|Oracle Mode|
|machine-translation| en |
|slug|dba_source-oracle-mode|

# DBA_SOURCE

<main id="notice" type='explain'>
<h4>説明</h4>
<p>このビューはV2.2.30バージョンから導入されました。</p>
</main>

## 機能の概要

現在のテナント配下にあるすべてのストレージオブジェクトのテキストソースを表示します。列は`ALL_SOURCE`と同じです。

  <main id="notice" >
    <h4>適用対象</h4>
    <p>このビューはOceanBaseデータベースのOracleモードでのみ適用されます。</p>
  </main>

## フィールド説明

|   **フィールド名**    |    **型**    | **NULL許容** |       **説明**       |
|---------------|--------------|----------------|--------------------|
| OWNER         | VARCHAR2(30) | NO             | オブジェクト所有者           |
| NAME          | VARCHAR2(30) | NO             | オブジェクト名                |
| TYPE          | VARCHAR2(12) | NO             | オブジェクトタイプ               |
| LINE          | NUMBER       | NO             | TEXTフィールドに対応するソースコードの行番号  |
| TEXT          | CLOB         | NO             | 現在行のソースコード            |
| ORIGIN_CON_ID | NUMBER(38)   | NO             | オブジェクトが属するテナントのテナントID。システムパッケージの場合は0を返す|

## クエリ例

現在のテナント配下にあるすべてのストレージオブジェクトのテキストソースを確認し、最初の2件のレコードを表示します。

```shell
obclient [SYS]> SELECT * FROM SYS.DBA_SOURCE WHERE ROWNUM<=2\G
```

クエリ結果は次のとおりです：

```shell
*************************** 1. row ***************************
        OWNER: SYS
         NAME: JOB_UTILS
         TYPE: PACKAGE
         LINE: 1
         TEXT: PACKAGE JOB_UTILS IS
  PROCEDURE insert_date;
  PROCEDURE insert_date_program ( job IN VARCHAR2, job_name IN VARCHAR2);
END;
ORIGIN_CON_ID: 1004
*************************** 2. row ***************************
        OWNER: SYS
         NAME: JOB_UTILS
         TYPE: PACKAGE BODY
         LINE: 1
         TEXT: PACKAGE BODY JOB_UTILS IS
  PROCEDURE insert_date IS
  BEGIN
    INSERT INTO DBMS_SCHEDULER_T1 VALUES(TO_CHAR(sysdate));
    COMMIT;
  END;
  PROCEDURE insert_date_program ( job IN VARCHAR2, job_name IN VARCHAR2) IS
  BEGIN
    INSERT INTO DBMS_SCHEDULER_T2 VALUES(sysdate, job, job_name);
    COMMIT;
  END;
END;
ORIGIN_CON_ID: 1004
2 rows in set
```

## 関連ビューまたはドキュメント

* 現在のユーザーがアクセス可能なすべてのストレージオブジェクトのテキストソースを確認する：[ALL_SOURCE](4000.all_source-of-oracle-mode.md)

* 現在のユーザーが所有するストレージオブジェクトのテキストソースを確認する：[USER_SOURCE](29600.user_source-of-oracle-mode.md)