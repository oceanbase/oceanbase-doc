|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type|Oracle Mode|
|machine-translation|en,ja-marker|
|slug|dba_policy_groups-oracle-mode|

# DBA_POLICY_GROUPS

<main id="notice" type='explain'>
<h4>説明</h4>
<p>このビューは、V4.1.0バージョンから導入されました。</p>
</main>

## 機能の概要

現在のテナント内のすべてのビューに対するポリシーグループ情報を表示し、ポリシーコンテキストを利用して異なるユーザーに対して異なるポリシーを適用します。

## フィールドの説明

| フィールド | タイプ | NULLを許容するか | 説明 |
| --- | --- | --- | --- |
| OBJECT_OWNER | VARCHAR2(128) | YES | ポリシーによって制御されるオブジェクトの所有者 |
| OBJECT_NAME | VARCHAR2(128) | YES | ポリシーによって制御されるオブジェクトの名前 |
| POLICY_GROUP | VARCHAR2(128) | NO | ポリシーグループ名 |
| COMMON | VARCHAR2(3) | NO | 互換性のためにのみ使用され、現在の値は固定でNOです。 |
| INHERITED | VARCHAR2(3) | NO | 互換性のためにのみ使用され、現在の値は固定でNOです。 |

## クエリ例

現在のテナント内のすべてのビューに対するポリシーグループ情報を確認します。

```shell
obclient [SYS]> SELECT * FROM SYS.DBA_POLICY_GROUPS;
```

クエリ結果は次のとおりです：

```shell
+--------------+----------------+--------------+--------+-----------+
| OBJECT_OWNER | OBJECT_NAME    | POLICY_GROUP | COMMON | INHERITED |
+--------------+----------------+--------------+--------+-----------+
| SYS          | V_ORDERS_CTX   | GROUP_REGION | NO     | NO        |
| SYS          | V_SALES_SECURE | GROUP_SALES  | NO     | NO        |
+--------------+----------------+--------------+--------+-----------+
2 rows in set
```

## 関連するビューまたはドキュメント

* 現在のユーザーがアクセスできるビューのポリシーグループ情報を確認するには：[ALL_POLICY_GROUPS](3000.all_policy_groups-oracle.md)

* 現在のユーザーが所有するビューのポリシーグループ情報を確認するには：[USER_POLICY_GROUPS](27100.user_policy_groups-oracle.md)