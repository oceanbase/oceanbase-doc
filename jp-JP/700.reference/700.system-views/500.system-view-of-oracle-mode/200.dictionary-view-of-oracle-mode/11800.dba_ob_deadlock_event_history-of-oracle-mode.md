|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type|Oracle Mode|
|machine-translation|en,ja-marker|
|slug|dba_ob_deadlock_event_history-oracle-compatible-mode|

# DBA_OB_deadLOCK_EVENT_HISTORY

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このビューは、V4.0.0バージョンから導入されました。</p>
</main>

## 機能の概要

ビュー`DBA_OB_deadLOCK_EVENT_HISTORY`は、現在のテナントにおけるデッドロックイベントの履歴を表示します。

## フィールドの説明

| フィールド名 | タイプ | NULL値を許容するか | 説明 |
| ------- | ---- | -------------- | ---- |
| EVENT_ID       | NUMBER(38)                        | NO  | デッドロックイベントの番号 |
| SVR_IP         | VARCHAR2(46)                      | NO  | このデッドロックイベントに参加するノードのIPアドレス |
| SVR_PORT       | NUMBER(38)                        | NO  | このデッドロックイベントに参加するノードのポート番号 |
| DETECTOR_ID    | NUMBER(38)                        | NO  | このデッドロックイベントに参加するノードID識別子（プロセス内で一意） |
| REPORT_TIME    | TIMESTAMP(6) WITH LOCAL TIME ZONE | NO  | このデッドロックイベントの報告時間（報告サーバーのタイムスタンプを基準とする） |
| CYCLE_IDX      | NUMBER(38)                        | NO  | このノードのループ内の番号 |
| CYCLE_SIZE     | NUMBER(38)                        | NO  | このデッドロックイベントに参加するノードの数 |
| ROLE           | CLOB                              | NO  | このノードがこのデッドロックイベントにおける役割：<ul><li>witness</li><li>victim </li></ul>|
| PRIORITY_LEVEL | CLOB                              | NO  | このノードの優先順位のレベルの説明：<ul><li>extreme-low</li><li>low</li><li>normal</li><li>high</li><li>extreme-high </li></ul>|
| PRIORITY       | NUMBER(38)                        | NO  | このノードが持つ優先順位の値。優先順位が同じ場合にのみ、優先順位の値は比較可能であり、ループ内で優先順位値が最も低いノードが除外される |
| CREATE_TIME    | TIMESTAMP(6) WITH LOCAL TIME ZONE | NO  | このノードの作成時間 |
| START_DELAY_US | NUMBER(38)                        | NO  | このノードが作成されてから、デッドロックの検出を開始するまでの許可される間隔時間。単位はマイクロ秒 |
| MODULE         | CLOB                              | NO  | このノードが属するモジュール。値はtransactionに固定されている |
| VISITOR        | CLOB                              | NO  | このノードが表すリソースアクセス者。形式は `{session_id:$1}:{txid:$2}` で、`$1` と `$2` はどちらも数字<main id="notice" type='explain'><h4>説明</h4><p>V4.2.5以前のバージョンでは、このフィールドの形式は <code>{txid:xx}</code> でした。</p></main> |
| OBJECT         | CLOB                              | NO  | このノードが取得しようとしているリソース。行の主キー内容の説明。形式は `{addr:“$1:$2”}:{ls:$3}:{tablet:$4}:{row_key:{$5}}` で、その内容は以下の通りです。<ul><li><code>\$1</code> はIPアドレスです。 </li><li><code>\$2</code> はPORTです。 <code>\$1</code> と共に、競合が発生したマシンのプロセスアドレスを構成します。 </li><li><code>\$3</code> は競合が発生した際のログストリームです。 </li><li><code>\$4</code> は競合が発生した際のTabletです。 </li><li><code>\$5</code> は行ロックの主キーの文字列表現です。 </li></ul> <main id="notice" type='explain'><h4>説明</h4><p>V4.2.5以前のバージョンでは、このフィールドの形式は <code>{row_key:xxx}</code> でした。</p></main> |
| EXTRA_NAME1    | CLOB                              | YES | 上層部がデッドロック報告イベントに追加したい追加情報の名前1。形式は `wait_sql`<main id="notice" type='explain'><h4>説明</h4><p>このフィールドは、V4.2.5以前のバージョンでは <code>current sql</code> の形式でした。</p></main> |
| EXTRA_VALUE1   | CLOB                              | YES | トランザクションがデッドロックを発生した現在実行中のSQL内容 |
| EXTRA_NAME2    | CLOB                              | YES | 上層部がデッドロック報告イベントに追加したい追加情報の名前2。形式は `hold_sql_request_time`<main id="notice" type='explain'><h4>説明</h4><p>このフィールドは、V4.2.5以前のバージョンでは空として表示されていました。</p></main>  |
| EXTRA_VALUE2   | CLOB                              | YES | ロック保持ステートメントのリクエスト時間<main id="notice" type='explain'><h4>説明</h4><p>このフィールドは、V4.2.5以前のバージョンでは空として表示されていました。</p></main> |
| EXTRA_NAME3    | CLOB                              | YES | 上層部がデッドロック報告イベントに追加したい追加情報の名前3。形式は `hold_sql`<main id="notice" type='explain'><h4>説明</h4><p>このフィールドは、V4.2.5以前のバージョンでは空として表示されていました。</p></main>3 |
| EXTRA_VALUE3   | CLOB                              | YES | `cycle_idx - 1`行のレコードに対応するObjectを保持するSQLステートメント<main id="notice" type='explain'><h4>説明</h4><p>このフィールドは、V4.2.5以前のバージョンでは空として表示されていました。</p></main> |

## クエリ例

このテナントのデッドロックイベントの履歴を照会します。

```shell
obclient [SYS]> SELECT * FROM SYS.DBA_OB_DEADLOCK_EVENT_HISTORY LIMIT 10;
```