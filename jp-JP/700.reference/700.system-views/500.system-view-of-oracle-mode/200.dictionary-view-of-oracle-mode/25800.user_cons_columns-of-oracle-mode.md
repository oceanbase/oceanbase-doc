|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type|Oracle Mode|
|machine-translation| en |
|slug|user_cons_columns-oracle-mode|

# USER_CONS_COLUMNS

<main id="notice" type='explain'>
<h4>説明</h4>
<p>このビューはV2.2.30から導入されました。</p>
</main>

## 機能の概要

ユーザーが所有するすべてのテーブルの制約に含まれる列情報を表示します。

  <main id="notice" >
    <h4>適用対象</h4>
    <p>このビューはOceanBaseデータベースのOracleモードでのみ適用されます。</p>
  </main>

## フィールド説明

|    **フィールド名**     |     **型**     | **NULL許容** |  **説明**  |
|-----------------|----------------|----------------|----------|
| OWNER           | VARCHAR2(128)  | VARCHAR2(128)  | 列の所有者    |
| CONSTRAINT_NAME | VARCHAR2(128)  | NO             | 制約名     |
| TABLE_NAME      | VARCHAR2(128)  | NO             | 制約が存在するテーブル名 |
| COLUMN_NAME     | VARCHAR2(4000) | YES            | 制約で指定する列名   |
| POSITION        | NUMBER         | YES            | 制約内における列の位置 |

## クエリ例

ユーザーが所有するテーブルのうち、テーブル `TBL3` の制約に含まれる列情報を確認します。

```shell
obclient [SYS]> SELECT * FROM SYS.USER_CONS_COLUMNS WHERE TABLE_NAME='TBL3';
```

クエリ結果は次のとおりです：

```shell
+-------+----------------------------+------------+-------------+----------+
| OWNER | CONSTRAINT_NAME            | TABLE_NAME | COLUMN_NAME | POSITION |
+-------+----------------------------+------------+-------------+----------+
| SYS   | TBL3_OBPK_1744012420113641 | TBL3       | COL1        |        1 |
+-------+----------------------------+------------+-------------+----------+
1 row in set
```

## 関連ビューまたはドキュメント

* 現在のユーザーがアクセス可能なすべてのテーブルの制約情報を確認する：[ALL_CONS_COLUMNS](700.all_cons_columns-of-oracle-mode.md)

* 現在のテナント内のすべてのテーブルの制約情報を確認する：[DBA_CONS_COLUMNS](7600.dba_cons_columns-of-oracle-mode.md)

* 制約の詳細については、[列の制約タイプの定義](../../../300.database-object-management/200.manage-object-of-oracle-mode/100.manage-tables-of-oracle-mode/400.define-the-constraint-type-for-a-column-of-oracle-mode.md)を参照してください。