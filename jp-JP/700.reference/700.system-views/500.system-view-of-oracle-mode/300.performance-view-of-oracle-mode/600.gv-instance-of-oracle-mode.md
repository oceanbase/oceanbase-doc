|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type|Oracle Mode|
|machine-translation|en,ja-marker|
|slug|gv-instance-oracle-mode|

# GV$INSTANCE

<main id="notice" type='explain'>
<h4>説明</h4>
<p>このビューは、V2.2.77バージョンから導入されました。</p>
</main>

## 機能の概要

このビューは、現在のデータベースインスタンスの状態を表示します。

  <main id="notice" >
    <h4>適用対象</h4>
    <p>このビューはOceanBaseデータベースのOracleモードにのみ適用されます。</p>
  </main>

## フィールドの説明

|     **フィールド名**     |    **タイプ**    | **NULL値を許容するか** |                        **説明**                         |
|------------------|--------------|---------------|-------------------------------------------------------------------|
| INST_ID          | NUMBER       | NO            | データベースインスタンスに対応するID                                           |
| INSTANCE_NUMBER  | NUMBER       | NO            | 現在このフィールドはサポートされておらず、現在のデフォルト値はNULLです      |
| INSTANCE_NAME    | VARCHAR2(16) | NO            | 現在このフィールドはサポートされておらず、現在のデフォルト値はNULLです                        |
| HOST_NAME        | VARCHAR2(64) | NO            | インスタンスが存在するServerのアドレス                                       |
| VERSION          | VARCHAR2(17) | NO            | データベースインスタンスのバージョン                                              |
| STARTUP_TIME     | DATE         | NO            | 現在このフィールドはサポートされておらず、現在のデフォルト値はNULLです                      |
| STATUS           | VARCHAR2(12) | NO            | 現在このフィールドはサポートされておらず、現在のデフォルト値はNULLです                        |
| PARALLEL         | VARCHAR2(3)  | NO            | 現在このフィールドはサポートされておらず、現在のデフォルト値はNULLです                                                                     |
| THREAD#          | NUMBER       | NO            | 現在このフィールドはサポートされておらず、現在のデフォルト値はNULLです                     |
| ARCHIVER         | VARCHAR2(7)  | NO            | 現在このフィールドはサポートされておらず、現在のデフォルト値はNULL   |
| LOG_SWITCH_WAIT  | VARCHAR2(15) | NO            | 現在このフィールドはサポートされておらず、現在のデフォルト値はNULL                   |
| LOGINS           | VARCHAR2(10) | NO            | 現在このフィールドはサポートされておらず、現在のデフォルト値はNULL  |
| SHUTDOWN_PENDING | VARCHAR2(3)  | NO            | 現在このフィールドはサポートされておらず、現在のデフォルト値はNULLです                     |
| DATABASE_STATUS  | VARCHAR2(17) | NO            | データベースの状態                                                |
| INSTANCE_ROLE    | VARCHAR2(18) | NO            | 現在このフィールドはサポートされておらず、現在のデフォルト値はNULL |
| ACTIVE_STATE     | VARCHAR2(9)  | NO            | 現在このフィールドはサポートされておらず、現在のデフォルト値はNULLです                      |
| BLOCKED          | VARCHAR2(2)  | NO            | 現在このフィールドはサポートされておらず、現在のデフォルト値はNULLです                                                                    |
| CON_ID           | NUMBER       | NO            | 現在このフィールドはサポートされておらず、現在のデフォルト値はNULLです           |
| INSTANCE_MODE    | VARCHAR2(11) | NO            | 現在このフィールドはサポートされておらず、現在のデフォルト値はNULLです           |
| EDITION          | VARCHAR2(7)  | NO            | 現在このフィールドはサポートされておらず、現在のデフォルト値はNULLです                      |
| FAMILY           | VARCHAR2(80) | NO            | 現在このフィールドはサポートされておらず、現在のデフォルト値はNULLです                       |
| DATABASE_TYPE    | VARCHAR2(15) | NO            | 現在このフィールドはサポートされておらず、現在のデフォルト値はNULLです                     |

## クエリ例

現在のデータベースインスタンスの状態を確認します。

```shell
obclient [SYS]> SELECT * FROM SYS.GV$INSTANCE\G
```

クエリ結果は次のとおりです：

```shell
*************************** 1. row ***************************
         INST_ID: 1
 INSTANCE_NUMBER: NULL
   INSTANCE_NAME: NULL
       HOST_NAME: xx.xx.xx.xx:28825
         VERSION: xxxxx
    STARTUP_TIME: NULL
          STATUS: NULL
        PARALLEL: NULL
         THREAD#: NULL
        ARCHIVER: NULL
 LOG_SWITCH_WAIT: NULL
          LOGINS: NULL
SHUTDOWN_PENDING: NULL
 DATABASE_STATUS: ACTIVE
   INSTANCE_ROLE: NULL
    ACTIVE_STATE: NULL
         BLOCKED: NULL
          CON_ID: NULL
   INSTANCE_MODE: NULL
         EDITION: NULL
          FAMILY: NULL
   DATABASE_TYPE: NULL
1 row in set (0.052 sec)
```