|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type|Oracle Mode|
|machine-translation|en,ja-marker|
|slug|v-ob_processlist-oracle-mode|

# V$OB_PROCESSLIST

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このビューはV4.0.0バージョンから導入されました。</p>
</main>

## 機能の概要

ビュー`V$OB_PROCESSLIST`は、現在のOBServerノード上のセッション情報を表示するために使用されます。

## フィールドの説明

|      フィールド名      |        タイプ        | NULLを許容するか |                              説明                              |
|----------------|------------------|------------|--------------------------------------------------------------|
| SVR_IP         | VARCHAR2(46)     | NO         | サーバーのIPアドレス                                                    |
| SVR_PORT       | NUMBER(38)       | NO         | サーバーのポート番号                                                       |
| SQL_PORT       | NUMBER(38)       | NO         | SQLポート番号                                                      |
| ID             | NUMBER(38)       | NO         | セッションID <br>V4.2.xバージョンの場合：<ul><li> V4.2.5 BP5より前のバージョン：<br>すべての接続モード（直結モード/ODPモード）で、このフィールドはServer Session IDを表します。</li><li> V4.2.5 BP5以降のバージョン：<ul><li>直結モードの場合：<br>このフィールドはServer Session IDを表します。</li> <li>ODPモードの場合：</li><ul><li>ODPの設定項目<code>client_session_id_version = 2</code>の場合、このフィールドはClient Session IDを表します。</li> <li><code>client_session_id_version = 1</code>の場合、このフィールドはServer Session IDを表します。</li></ul></ul></li></ul> |
| USER           | CHAR(193)        | NO         | ユーザー名                                                          |
| HOST           | VARCHAR2(128)    | NO         | クライアントIP:PORT                |
| DB             | VARCHAR2(128)    | NO         | アクセスするデータベース名                                              |
| TENANT         | VARCHAR2(128)    | NO         | アクセスするテナント名                                                      |
| COMMAND        | VARCHAR2(4096)   | NO         | 現在実行中のコマンドタイプ                                                    |
| TIME           | NUMBER(38)       | NO         | 現在のコマンド実行時間、単位は秒です。コマンドがリトライされた場合、ゼロにリセットされて再計算されます                              |
| TOTAL_TIME     | NUMBER(38)       | NO         | コマンドの実行に基づく：<ul><li>リクエスト受信後からクライアントへの返却前までの期間を表します。</li><li>リクエスト受信以外の状態では、現在の状態の持続時間を表します。</li></ul>単位は秒です。<main id="notice" type='explain'><h4>説明</h4><p>V4.2.5以前のバージョンでは、このフィールドの意味は「現在のコマンドの実行総時間であり、コマンドのリトライ時にはリセットされない」となっていました。</p></main>                                 |
| STATE          | VARCHAR2(128)    | NO         | 現在のセッションの状態                                                       |
| INFO           | VARCHAR2(262143) | NO         | 現在実行中のコマンドを表示します                                                  |
| PROXY_SESSID   | NUMBER(38)       | NO         | クライアントがobproxyの場合、この列にはproxy session idが表示されます                         |
| MASTER_SESSID  | NUMBER(38)       | NO         | メインセッションIDであり、同一SQLの複数のサブセッションを連携させるために使用されます                        |
| USER_CLIENT_IP | VARCHAR2(46)     | NO         | ユーザークライアントのIPアドレス                                                     |
| USER_HOST      | VARCHAR2(128)    | NO         | ユーザークライアントのホスト名                                                     |
| RETRY_CNT      | NUMBER(38)       | NO         | 現在のコマンドのリトライ回数                                                    |
| RETRY_INFO     | NUMBER(38)       | NO         | 現在のコマンドのリトライ情報であり、通常は最後のリトライ時のエラーコードです                                      |
| SQL_ID         | VARCHAR2(32)     | NO         | SQL ID情報                                                    |
| TRANS_ID       | NUMBER(38)       | NO         | トランザクションID                                                        |
| THREAD_ID      | NUMBER(38)       | NO         | スレッドID                                                        |
| SSL_cipher     | VARCHAR2(4096)   | NO         | 暗号化パスワード名                                                       |
| TRACE_ID       | VARCHAR2(64)     | NO         | Trace ID                                                     |
| TRANS_STATE    | VARCHAR2(32)     | NO         | トランザクションの状態（セッション上にトランザクションがない場合は空として表示されます）：<ul><li>`IDLE`：アイドル状態であり、トランザクションが開始されていないことを示します。</li> <li>`ACTIVE`：トランザクションが明示的に開始されました。`START TRANSACTION`、`BEGIN`などのステートメントによって明示的に開始されます。</li> <li>`IMPLICIT_ACTIVE`：トランザクションが暗黙的に開始されました。`autocommit = on`モードでは、DMLなどのデータ変更ステートメントを実行した後にトランザクションが開始されます。</li> <li>`ROLLBACK_SAVEPOINT`：トランザクションがセーブポイントまでロールバック中です。</li> <li>`IN_TERMINATE`：トランザクションが終了中であり、内部要因による終了やユーザーによるトランザクションの終了（例えば、`COMMIT`、`ROLLBACK`ステートメントの実行）が含まれます。</li> <li>`ABORTED`：トランザクションが例外発生により内部で終了またはロールバックされました。</li> <li>`ROLLED_BACK`：トランザクションがすでにロールバックされました。</li> <li>`COMMIT_TIMEOUT`：トランザクションコミットタイムアウト。</li> <li>`COMMIT_UNKNOWN`：トランザクションコミット結果が不明です。</li> <li>`COMMITTED`：トランザクションが正常にコミットされました。</li></ul>                                                          |
| ACTION         | VARCHAR2(4096)   | NO         | DBMS_APPLICATION_INFO.SET_ACTIONプロシージャの呼び出しによって設定された現在実行中の操作の名前 |
| MODULE         | VARCHAR2(4096)   | NO         | DBMS_APPLICATION_INFO.SET_MODULEプロシージャの呼び出しによって設定された現在実行中の操作の名前 |
| CLIENT_INFO    | VARCHAR2(4096)   | NO         | DBMS_APPLICATION_INFO.SET_CLIENT_INFOプロセスによって設定された情報              |
| LEVEL | NUMBER(38) | NO | このセッションのフルリンクトレース監視レベルを示します。例えば、1はレベル1の診断情報を示します<main id="notice" type='explain'><h4>説明</h4><p>このフィールドはV4.2.1バージョンから導入されました。</p></main> |
| SAMPLE_PERCENTAGE | NUMBER(38) | NO | このセッションのフルリンクトレースのサンプリング頻度を示します。例えば、50は診断情報のサンプリングを50%の確率で行うことを示します<main id="notice" type='explain'><h4>説明</h4><p>このフィールドはV4.2.1バージョンから導入されました。</p></main> |
| RECORD_POLICY | VARCHAR2(32) | NO | このセッションのフルリンクトレース記録ポリシーを示します。主に以下の3種類のポリシーをサポートします：<ul><li>ALL：すべてのヒットサンプリングで開始されたTraceのTraceポイント情報は、ログファイルに出力され、各span終了時にログファイルに出力されます。 </li><li>ONLY_SLOW_QUERY：すべてのヒットサンプリングで開始されたTraceのTraceポイント情報のうち、Slow Queryに属するリクエストTraceはログファイルに出力され、その他は破棄されます。この出力ポリシーでは、Traceログの出力タイミングはリクエスト終了後にSlow Queryと判定された場合にのみ出力されます。Proxyでは、ルートspan、つまりトランザクションレベルのspanは、Slow Queryが検出された場合にのみルートspanが出力されます。 </li><li>SAMPLE_AND_SLOW_QUERY：すべてのヒットサンプリングで開始されたTraceのTraceポイント情報のうち、以下の条件を満たすTraceはすべてログファイルに出力されます：<ol><li>隠れた構成パラメータ<code>_print_sample_ppm</code>を使用して1万分の1の確率でサンプリングし、サンプリング結果が選択された場合にのみログに出力されます</li><li>ONLY_SLOW_QUERYモードでの出力条件を満たす</li></ol>このモードでは、ログの出力タイミングもspan終了時ではありません。条件1を満たす場合、クライアント側でそのTraceログが強制的に出力されるとマークされ、その情報は後続のリンクコンポーネントに伝達されます：条件2を満たす場合、出力タイミングはONLY_SLOW_QUERYと同じです。</li></ul><main id="notice" type='explain'><h4>説明</h4><p>このフィールドはV4.2.1バージョンから導入されました。</p></main> |
| LB_VID | NUMBER(38) | NO | パブリッククラウド環境でロードバランシングを介してデータベースに直接接続されている場合、この列にはロードバランシングサービスのVPC IDが表示されます。それ以外の場合はNULLが表示されます<main id="notice" type='explain'><h4>説明</h4><p>このフィールドはV4.2.1バージョンから導入されました。</p></main> |
| LB_VIP | VARCHAR2(46) | NO | パブリッククラウド環境でロードバランシングを介してデータベースに直接接続されている場合、この列にはクライアントがロードバランシングサービスに接続するためのIPアドレスが表示されます。それ以外の場合はNULLが表示されます<main id="notice" type='explain'><h4>説明</h4><p>このフィールドはV4.2.1バージョンから導入されました。</p></main> |
| LB_VPORT | NUMBER(38) | NO | パブリッククラウド環境でロードバランシングを介してデータベースに直接接続する場合、この列にはクライアントがロードバランシングサービスに接続するためのポート番号が表示されます。それ以外の場合はNULLとなります。<main id="notice" type='explain'><h4>説明</h4><p>このフィールドはV4.2.1バージョンから導入されました。</p></main> |
| IN_BYTES | NUMBER(38) | NO | このセッションの流入トラフィックをバイト単位で表します。<main id="notice" type='explain'><h4>説明</h4><p>このフィールドはV4.2.1バージョンから導入されました。</p></main> |
| OUT_BYTES | NUMBER(38) | NO | このセッションの流出トラフィックをバイト単位で表します。<main id="notice" type='explain'><h4>説明</h4><p>このフィールドはV4.2.1バージョンから導入されました。</p></main> |
| USER_CLIENT_PORT  | NUMBER(38))          | NO   | クライアントアドレス情報 <main id="notice" type='explain'><h4>説明</h4><p>このフィールドはV4.2.3バージョンから導入されました。</p></main>|
| PROXY_USER        | VARCHAR2(128)        | NO  |<ul><li>プロキシユーザーによるログインシナリオでは、プロキシユーザーの名前が返されます。</li><li>プロキシユーザー以外のユーザーによるログインシナリオでは、空として表示されます。</li></ul><main id="notice" type='explain'><h4>説明</h4><p>このフィールドはV4.2.3バージョンから導入されました。</p></main> |
| TOTAL_CPU_TIME    | VARCHAR2(38)          | NO   | 現在のコマンド実行におけるCPU使用時間を秒単位で表します。<main id="notice" type='explain'><h4>説明</h4><ul><li>V4.3.x系では、このフィールドはV4.3.1バージョンから導入されました。</li> <li>V4.2.1バージョンでは、このフィールドはV4.2.1 BP5バージョンから導入されました。V4.2.x系の他のバージョンでは、このフィールドはV4.2.5バージョンから導入されました。</li></ul></main>      |
| SERVICE_NAME      | VARCHAR2(64)         | NO   | このセッションがどのサービス名によって作成されたかを表します。値が`NULL`の場合、このセッションはサービス名によって作成されていないことを意味します。<main id="notice" type='explain'><h4>説明</h4><p>このフィールドはV4.2.4バージョンから導入されました。</p></main> |
| TOP_INFO          | VARCHAR2(262143)     | NO   | 実行中のSQLが属する最上位PLステートメントの情報を表示します。<main id="notice" type='explain'><h4>説明</h4><ul><li>V4.3.x系では、このフィールドはV4.3.3バージョンから導入されました。</li> <li>V4.2.1バージョンでは、このフィールドはV4.2.1 BP9バージョンから導入されました。V4.2.x系の他のバージョンでは、このフィールドはV4.2.5バージョンから導入されました。</li></ul></main>     |
| MEMORY_USAGE      | NUMBER(38)           | NO  | 単一のSQLステートメントが現在使用しているメモリサイズをバイト単位で表します。<main id="notice" type='explain'><h4>説明</h4><p>V4.2.5バージョンでは、このフィールドはV4.2.5 BP2バージョンから導入されました。</p></main>|

## クエリ例

このOBServerノードのセッション情報を照会します。

```shell
obclient [SYS]> SELECT * FROM SYS.V$OB_PROCESSLIST WHERE ROWNUM <= 1\G
```

クエリ結果は次のとおりです：

```shell
*************************** 1. row ***************************
           SVR_IP: xx.xx.xx.xx
         SVR_PORT: 2882
         SQL_PORT: 2881
               ID: 3221671593
             USER: SYS
             HOST: xx.xx.xx.xx:51303
               DB: SYS
           TENANT: oracle001
          COMMAND: Query
             TIME: 0
       TOTAL_TIME: 0
            STATE: ACTIVE
             INFO: SELECT * FROM SYS.V$OB_PROCESSLIST WHERE ROWNUM <= 1
     PROXY_SESSID: NULL
    MASTER_SESSID: NULL
   USER_CLIENT_IP: xx.xx.xx.xx
        USER_HOST: %
        RETRY_CNT: 0
       RETRY_INFO: 0
           SQL_ID: 5A7EEC4E56E30D30D96959FABC60764E
         TRANS_ID: 0
        THREAD_ID: 33243
       SSL_CIPHER: NULL
         TRACE_ID: YB42AC1E87E1-000XXXXXXXX-0-0
      TRANS_STATE: NULL
           ACTION: NULL
           MODULE: NULL
      CLIENT_INFO: NULL
            LEVEL: 1
SAMPLE_PERCENTAGE: 10
    RECORD_POLICY: SAMPLE_AND_SLOW_QUERY
           LB_VID: NULL
           LB_VIP: NULL
         LB_VPORT: NULL
         IN_BYTES: 449
        OUT_BYTES: 0
 USER_CLIENT_PORT: 51303
       PROXY_USER: NULL
   TOTAL_CPU_TIME: 0
     SERVICE_NAME: NULL
         TOP_INFO: NULL
     MEMORY_USAGE: 4030464
1 row in set
```

## 関連するビューまたはドキュメント

* [GV$OB_PROCESSLIST](2400.gv-ob_processlist-of-oracle-mode.md)

* [テナントセッションの表示](../../../1200.database-proxy/1500.view-tenant-sessions.md)
