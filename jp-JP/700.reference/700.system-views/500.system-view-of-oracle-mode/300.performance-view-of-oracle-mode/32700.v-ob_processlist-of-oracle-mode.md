|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type|Oracle Mode|
|machine-translation| en |
|slug|v-ob_processlist-oracle-compatible-mode|

# V$OB_PROCESSLIST

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このビューはV4.0.0バージョンから導入されました。</p>
</main>

## 機能の概要

ビュー `V$OB_PROCESSLIST` は、現在のOBServerノード上のセッション情報を表示するために使用されます。

## フィールド説明

|      フィールド名      |        型        | NULL許容 |                              説明                              |
|----------------|------------------|------------|--------------------------------------------------------------|
| SVR_IP         | VARCHAR2(46)     | NO         | サーバーのIPアドレス                                                    |
| SVR_PORT       | NUMBER(38)       | NO         | サーバーのポート番号                                                       |
| SQL_PORT       | NUMBER(38)       | NO         | SQLポート番号                                                      |
| ID             | NUMBER(38)       | NO         | セッションID <ul><li>直接接続モードの場合：<br>このフィールドはサーバーセッションIDを表します。</li> <li>ODPモードの場合：</li><ul><li>ODPで<code>client_session_id_version = 2</code>が設定されている場合、このフィールドはクライアントセッションIDを表します。 </li> <li><code>client_session_id_version = 1</code>の場合、このフィールドはサーバーセッションIDを表します。</li></ul></ul>                                                       |
| USER           | CHAR(193)        | NO         | ユーザー名                                                          |
| HOST           | VARCHAR2(128)    | NO         | クライアントIP:PORT|
| DB             | VARCHAR2(128)    | YES        | アクセスするデータベース名                                              |
| TENANT         | VARCHAR2(128)    | NO         | アクセスするテナント名                                                      |
| COMMAND        | VARCHAR2(4096)   | NO         | 現在実行中のコマンドタイプ                                                    |
| TIME           | NUMBER(38)       | NO         | 現在のコマンド実行時間（秒単位）。コマンドが再試行された場合はリセットされ、再度計算されます                              |
| TOTAL_TIME     | NUMBER(38)       | NO         | 現在のコマンド実行総時間（秒単位）。コマンドが再試行された場合はリセットされません                                 |
| STATE          | VARCHAR2(128)    | YES        | 現在のセッション状態                                                       |
| INFO           | VARCHAR2(262143) | YES        | 現在実行中のコマンドを表示                                                  |
| PROXY_SESSID   | NUMBER(38)       | YES        | クライアントがobproxyの場合、この列にproxy session idが表示されます                         |
| MASTER_SESSID  | NUMBER(38)       | YES        | マスターセッションID。同一SQLの複数のサブセッションをつなぐために使用されます                        |
| USER_CLIENT_IP | VARCHAR2(46)     | YES        | ユーザーのクライアントIP                                                     |
| USER_HOST      | VARCHAR2(128)    | YES        | ユーザーのクライアントホスト名                                                     |
| RETRY_CNT      | NUMBER(38)       | NO         | 現在のコマンドの再試行回数                                                    |
| RETRY_INFO     | NUMBER(38)       | NO         | 現在のコマンドの再試行情報。通常は最後の再試行のエラーコードです                                      |
| SQL_ID         | VARCHAR2(32)     | NO         | SQL ID情報                                                    |
| TRANS_ID       | NUMBER(38)       | NO         | トランザクションID                                                        |
| THREAD_ID      | NUMBER(38)       | NO         | スレッドID                                                        |
| SSL_CIPHER     | VARCHAR2(4096)   | YES        | 暗号化パスワード名                                                       |
| TRACE_ID       | VARCHAR2(64)     | YES        | Trace ID                                                     |
| TRANS_STATE    | VARCHAR2(32)     | YES        | トランザクション状態（セッションにトランザクションがない場合は空として表示）：<ul><li>`IDLE`：アイドル状態。トランザクションが開始されていないことを示します。</li> <li>`ACTIVE`：トランザクションが明示的に開始されました。`START TRANSACTION`、`BEGIN` などのステートメントで明示的に開始されます。</li> <li>`IMPLICIT_ACTIVE`：トランザクションが暗黙的に開始されました。`autocommit = on`モードで、DMLなどのデータ変更ステートメントを実行した後にトランザクションが開始されます。</li> <li>`ROLLBACK_SAVEPOINT`：トランザクションがセーブポイントまでロールバック中です。</li> <li>`IN_TERMINATE`：トランザクションが終了中です。内部原因による終了や、ユーザーが発行したトランザクション終了（例えば、`COMMIT`、`ROLLBACK` ステートメントの実行）が含まれます。</li> <li>`ABORTED`：トランザクションが異常により内部で終了またはロールバックされました。</li> <li>`ROLLED_BACK`：トランザクションがロールバックされました。</li> <li>`COMMIT_TIMEOUT`：トランザクションコミットがタイムアウトしました。</li> <li>`COMMIT_UNKNOWN`：トランザクションコミット結果が不明です。</li> <li>`COMMITTED`：トランザクションが正常にコミットされました。</li></ul>      |
| ACTION         | VARCHAR2(4096)   | YES        | DBMS_APPLICATION_INFO.SET_ACTION procedureを呼び出して設定された現在実行中の操作名 |
| MODULE         | VARCHAR2(4096)   | YES        | DBMS_APPLICATION_INFO.SET_MODULE procedureを呼び出して設定された現在実行中の操作名 |
| CLIENT_INFO    | VARCHAR2(4096)   | YES        | DBMS_APPLICATION_INFO.SET_CLIENT_INFOプロシージャで設定された情報              |
| LEVEL          | NUMBER(38)       | NO         | そのセッションのエンドツーエンドトレース監視レベルを示します。例えば、1はレベル1の診断情報を意味します<main id="notice" type='explain'><h4>説明</h4><p>このフィールドはV4.2.1バージョンから導入されました。</p></main> |
| SAMPLE_PERCENTAGE | NUMBER(38)    | NO         | そのセッションのエンドツーエンドトレースのサンプリング頻度を示します。例えば、50は50%の頻度で診断情報をサンプリングすることを意味します<main id="notice" type='explain'><h4>説明</h4><p>このフィールドはV4.2.1バージョンから導入されました。</p></main> |
| RECORD_POLICY    | VARCHAR2(32)   | NO         | セッションのエンドツーエンド・トレース記録ポリシーを表します。主に以下の3種類のポリシーをサポートしています：<ul><li>ALL：All Hits Samplingでトレースを有効にしたすべてのトレースのスポット情報をログファイルに出力し、各Span終了時にログファイルに出力します。 </li><li>ONLY_SLOW_QUERY：All Hits Samplingでトレースを有効にしたすべてのトレースのスポット情報のうち、Slow Queryのリクエストトレースのみをログファイルに出力し、その他は破棄します。この出力ポリシーでは、トレースログの出力タイミングは、リクエストが終了してSlow Queryと判断された後です。Proxyでは、ルートSpan、つまりトランザクションレベルのSpanは、Slow Queryが検出された場合にのみ出力されます。 </li><li>SAMPLE_AND_SLOW_QUERY：All Hits Samplingでトレースを有効にしたすべてのトレースのスポット情報のうち、以下の条件を満たすトレースをログファイルに出力します：<ol><li>隠れたパラメータ<code>_print_sample_ppm</code>を使用して万分の一の確率でサンプリングし、サンプリング結果が選択された場合にのみログに出力する場合</li><li>ONLY_SLOW_QUERYモードの出力条件を満たす場合</li></ol>このモードでは、ログの出力タイミングもSpan終了時ではありません。条件1を満たす場合、クライアント側でそのトレースログが強制的に出力されることがマークされ、その情報は後続のリンクコンポーネントに渡されます。条件2を満たす場合、出力タイミングはONLY_SLOW_QUERYと同じです。</li></ul><main id="notice" type='explain'><h4>説明</h4><p>このフィールドはV4.2.1バージョンから導入されました。</p></main> |
| LB_VID           | NUMBER(38)     | NO         | パブリッククラウド環境でロードバランシングを介してデータベースに直接接続する場合、この列にはロードバランシングサービスのVPC IDが表示されます。それ以外の場合はNULLを表示します<main id="notice" type='explain'><h4>説明</h4><p>このフィールドはV4.2.1バージョンから導入されました。</p></main> |
| LB_VIP           | VARCHAR2(46)   | NO         | パブリッククラウド環境でロードバランシングを介してデータベースに直接接続する場合、この列にはクライアントがロードバランシングサービスに接続するIPアドレスが表示されます。それ以外の場合はNULLを表示します<main id="notice" type='explain'><h4>説明</h4><p>このフィールドはV4.2.1バージョンから導入されました。</p></main> |
| LB_VPORT         | NUMBER(38)     | NO         | パブリッククラウド環境でロードバランシングを介してデータベースに直接接続する場合、この列にはクライアントがロードバランシングサービスに接続するポートが表示されます。それ以外の場合はNULLを表示します<main id="notice" type='explain'><h4>説明</h4><p>このフィールドはV4.2.1バージョンから導入されました。</p></main> |
| IN_BYTES         | NUMBER(38)     | NO         | セッションのインフロトラフィックを表し、単位はbyteです  <main id="notice" type='explain'><h4>説明</h4><p>このフィールドはV4.2.1バージョンから導入されました。</p></main> |
| OUT_BYTES        | NUMBER(38)     | NO         | セッションのアウトフロトラフィックを表し、単位はbyteです  <main id="notice" type='explain'><h4>説明</h4><p>このフィールドはV4.2.1バージョンから導入されました。</p></main> |
| USER_CLIENT_PORT  | NUMBER(38)          | NO   |   クライアントアドレス情報 <main id="notice" type='explain'><h4>説明</h4><ul><li>V4.3.x系では、このフィールドはV4.3.0バージョンから導入されました。</li> <li>V4.2.x系では、このフィールドはV4.2.3バージョンから導入されました。</li></ul></main> |
| PROXY_USER        | VARCHAR2(128) | YES        | <ul><li>プロキシユーザーログインシナリオでは、プロキシユーザーの名前を返します。</li><li>非プロキシユーザーログインシナリオでは、空文字列を表示します。</li></ul><main id="notice" type='explain'><h4>説明</h4><ul><li>V4.3.x系では、このフィールドはV4.3.2バージョンから導入されました。</li><li>V4.2.x系では、このフィールドはV4.2.3バージョンから導入されました。</li></ul></main> |
| SERVICE_NAME     | VARCHAR2(64)   | YES        | このセッションがどのサービス名によって作成されたかを表します。値が`NULL`の場合、そのセッションはサービス名によって作成されていないことを意味します。 <main id="notice" type='explain'><h4>説明</h4><ul><li>V4.3.x系では、このフィールドはV4.3.1バージョンから導入されました。</li> <li>V4.2.1系では、このフィールドはV4.2.1 BP9バージョンから導入されました。V4.2.x系の他のバージョンでは、このフィールドはV4.2.4バージョンから導入されました。</li></ul></main> |
| TOTAL_CPU_TIME   | NUMBER(38)     | NO         | 現在のコマンド実行CPU使用時間、単位は秒です  <main id="notice" type='explain'><h4>説明</h4><ul><li>V4.3.x系では、このフィールドはV4.3.1バージョンから導入されました。</li> <li>V4.2.1系では、このフィールドはV4.2.1 BP5バージョンから導入されました。</li></ul></main>    |
| TOP_INFO         | VARCHAR2(262143) | YES      | 実行中のSQLが存在するトップレベルPL文の情報を表示します。 <main id="notice" type='explain'><h4>説明</h4><ul><li>V4.3.x系では、このフィールドはV4.3.3バージョンから導入されました。</li> <li>V4.2.1系では、このフィールドはV4.2.1 BP9バージョンから導入されました。</li></ul></main> |
| TOP_TIME |NUMBER(38)|NO|PLシナリオでのtop_time列は、PL全体の実行時間を表します。<main id="notice" type='explain'><h4>説明</h4><p>V4.4.x系では、このフィールドはV4.4.1バージョンから導入されました。|
| TOP_TRACE_ID |varchar(64)|NO|トップレベル文のtraceidを表示するために使用されます。<main id="notice" type='explain'><h4>説明</h4><p>V4.4.x系では、このフィールドはV4.4.1バージョンから導入されました。|
| MEMORY_USAGE     | NUMBER(38)     | NO         | 単一のSQL文が現在使用しているメモリサイズ、単位はバイトです  <main id="notice" type='explain'><h4>説明</h4><p>V4.4.x系では、このフィールドはV4.4.0バージョンから導入されましたが、現在はまだ有効ではなく、フィールド値はデフォルトで<code>NULL</code>です。</p></main>  |

## クエリ例

このOBServerノードのセッション情報を照会します。

```shell
obclient [SYS]> SELECT * FROM SYS.V$OB_PROCESSLIST WHERE ROWNUM < = 1 \G
```

クエリ結果は次のとおりです：

```shell
*************************** 1. row ***************************
           SVR_IP: 6.xx.xxx.xxx
         SVR_PORT: 2882
         SQL_PORT: 2881
               ID: 3221659896
             USER: SYS
             HOST: 100.xx.xxx.xxx:39184
               DB: SYS
           TENANT: oracle001
          COMMAND: Query
             TIME: 0
       TOTAL_TIME: 0
            STATE: ACTIVE
             INFO: SELECT * FROM SYS.V$OB_PROCESSLIST WHERE ROWNUM < = 1
     PROXY_SESSID: NULL
    MASTER_SESSID: NULL
   USER_CLIENT_IP: 100.xx.xxx.xxx
        USER_HOST: %
        RETRY_CNT: 0
       RETRY_INFO: 0
           SQL_ID: EF50802B8426B999EDC33F5F27D0A204
         TRANS_ID: 0
        THREAD_ID: 3304791
       SSL_CIPHER: NULL
         TRACE_ID: YB42060CEB9F-000643607B0D5092-0-0
      TRANS_STATE: NULL
           ACTION: NULL
           MODULE: NULL
      CLIENT_INFO: NULL
            LEVEL: 1
SAMPLE_PERCENTAGE: 10
    RECORD_POLICY: SAMPLE_AND_SLOW_QUERY
           LB_VID: NULL
           LB_VIP: NULL
         LB_VPORT: NULL
         IN_BYTES: 448
        OUT_BYTES: 0
 USER_CLIENT_PORT: 39184
       PROXY_USER: NULL
     SERVICE_NAME: NULL
   TOTAL_CPU_TIME: 0
         TOP_INFO: NULL
     MEMORY_USAGE: 253952
         TOP_TIME: 0
     TOP_TRACE_ID: YB42060CEB9F-000643607B0D5092-0-0
1 row in set (0.047 sec)
```

## 関連ビューまたはドキュメント

* [GV$OB_PROCESSLIST](2500.gv-ob_processlist-of-oracle-mode.md)
* [フルリンクトレースの方法](../../../../600.manage/900.daily-inspection/900.full-link-detection/300.full-link-diagnosis.md)