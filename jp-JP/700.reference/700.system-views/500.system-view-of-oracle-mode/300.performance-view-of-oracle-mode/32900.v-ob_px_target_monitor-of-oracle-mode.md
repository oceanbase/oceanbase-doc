|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type|Oracle Mode|
|machine-translation| en |
|slug|v-ob_px_target_monitor-oracle-compatible-mode|

# V$OB_PX_TARGET_MONITOR

<main id="notice" type='explain'>
<h4>説明</h4>
<p>このビューはV4.0.0バージョンから導入されました。</p>
</main>

## 機能の概要

現在のテナントが現在のOBServerノード上で使用しているPXスレッドの状況を表示します。

## フィールドの説明

|             フィールド名             |      型      | NULL許容 |    説明     |
|------------------------------|--------------|------------|-----------|
| SVR_IP                       | VARCHAR2(46) | NO         | ローカルサーバーのIPアドレス |
| SVR_PORT                     | NUMBER(38)   | NO         | ローカルサーバーのポート番号    |
| TENANT_ID                    | NUMBER(38)   | NO         | テナントID     |
| IS_LEADER                    | CHAR(1)      | NO         | Dummyテーブルのリーダーかどうか          |
| VERSION                      | NUMBER(38)   | NO         | 統計情報の再収集回数         |
| PEER_IP                      | VARCHAR2(46) | NO         | 相手側サーバーのIPアドレス          |
| PEER_PORT                    | NUMBER(38)   | NO         | 相手側サーバーのポート番号         |
| PEER_TARGET                  | NUMBER(38)   | NO         | 相手側サーバーのPXスレッド総数          |
| PEER_TARGET_USED             | NUMBER(38)   | NO         | 相手側サーバーのPXスレッド使用数          |
| LOCAL_TARGET_USED            | NUMBER(38)   | NO         | ローカルサーバーが使用する相手側PXスレッド数          |
| LOCAL_PARALLEL_SESSION_COUNT | NUMBER(38)   | NO         | ローカルサーバー上で並列実行中のセッション数          |

## クエリ例

ユーザーテナント内で、そのテナントの現在のOBServerノード上のPXスレッド使用状況を照会します。

```shell
obclient [SYS]> SELECT * FROM SYS.V$OB_PX_TARGET_MONITOR;
```

クエリ結果は次のとおりです：

```shell
+----------------+----------+-----------+-----------+-----------------+----------------+-----------+-------------+------------------+-------------------+------------------------------+
| SVR_IP         | SVR_PORT | TENANT_ID | IS_LEADER | VERSION         | PEER_IP        | PEER_PORT | PEER_TARGET | PEER_TARGET_USED | LOCAL_TARGET_USED | LOCAL_PARALLEL_SESSION_COUNT |
+----------------+----------+-----------+-----------+-----------------+----------------+-----------+-------------+------------------+-------------------+------------------------------+
| 11.xxx.xxx.xxx |    28825 |      1004 | Y         | 334988734352026 | 11.xxx.xxx.xxx |     28825 |          10 |                0 |                 0 |                            0 |
+----------------+----------+-----------+-----------+-----------------+----------------+-----------+-------------+------------------+-------------------+------------------------------+
1 row in set
```