|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type|MySQL Mode|
|machine-translation| en |
|slug|v-ob_vector_memory-mysql-compatible-mode|

# V$OB_VECTOR_MEMORY

<main id="notice" type='explain'>
  <h4>説明</h4>
  <ul><li>このビューはV4.4.1バージョンから導入されました。</li></ul>
</main>

## 機能の概要

現在のOBServerノード上で、現在のテナントのベクトルインデックスデータメモリ使用状況の詳細統計情報を表示します。

## フィールドの説明

| **フィールド** | **タイプ** | **NULL許容** | **説明** |
| ---- | ---- | ---- | ---- |
| TENANT_ID         | bigint(20)          | NO   |  テナントID：<ul><li>1：システムテナントID  </li><li>その他の値：ユーザーテナントまたはMetaテナントID </li></ul>   |
| SVR_IP            | varchar(46)         | NO   |  ノードアドレス   |
| SVR_PORT          | bigint(20)          | NO   |  ノードポート   |
| VECTOR_MEM_HOLD   | bigint(20)          | NO   |  ベクトルインデックスデータのメモリ使用量、単位はバイト   |
| VECTOR_MEM_LIMIT  | bigint(20)          | NO   |  ベクトルインデックスデータのメモリ設定上限、単位はバイト   |
| VECTOR_MEM_USED   | bigint(20)          | NO   |  ベクトルインデックスデータの実際の使用メモリ、単位はバイト   |

## クエリ例

現在のテナント配下のベクトルインデックスデータメモリ使用状況の詳細統計情報を照会します。

```shell
obclient [oceanbase]> SELECT * FROM oceanbase.V$OB_VECTOR_MEMORY  \G
```

クエリ結果は次のとおりです：

```shell
*************************** 1. row ***************************
                svr_ip: 11.xxx.xx.xx
              svr_port: 46002
             tenant_id: 1004
       vector_mem_hold: 82231296
       vector_mem_used: 77876328
      vector_mem_limit: 429496720
1 row in set
```

## 関連ドキュメント

[GV$OB_VECTOR_MEMORY](4650.gv-ob_vector_memory-of-mysql-mode.md)