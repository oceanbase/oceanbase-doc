|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type|MySQL Mode|
|machine-translation| en |
|slug|gv-ob_sql_plan-mysql-mode|

# GV$OB_SQL_PLAN

<main id="notice" type='explain'>
<h4>説明</h4>
<p>このビューはV4.2.0バージョンから導入されました。</p>
</main>

## 機能の概要

現在のテナントがすべてのOBServerノードでEXPLAIN PLANを実行して収集した実行計画情報を表示します。

## フィールド説明

| フィールド名 | 型 | NULL許容 | 説明 |
| --- | --- | --- | --- |
| TENANT_ID | bigint(20) | NO | プラン情報が属するテナントID<main id="notice" type='explain'><h4>説明</h4><p>このフィールドはV4.3.5 BP1バージョンから導入されました。</p></main> |
| SVR_IP          | varchar(46)         | NO | レプリカが存在するノードのアドレス |
| SVR_PORT        | bigint(20)          | NO | レプリカが存在するノードのポート |
| PLAN_ID         | bigint(20)          |  | プランID |
| SQL_ID    | varchar(32)         | NO | クエリID |
| DB_ID     | bigint(20)          | NO | クエリが属するスキーマID |
| PLAN_HASH  | bigint(20) unsigned | NO | プランのハッシュ値 |
| GMT_CREATE   | timestamp(6)        | NO | レコード作成時間 |
| OPERATOR     | varchar(255)        | NO | 演算子名（例：TABLE SCAN、SORT） |
| OBJECT_NODE    | varchar(40)         | NO | 現在の演算子がDBLINK関連である場合、DBLINKの名前を示します |
| OBJECT_ID         | bigint(20)          | NO | スキャンオブジェクトID（例：TABLE SCAN演算子がスキャンする物理テーブルID） |
| OBJECT_OWNER    | varchar(128)        | NO | スキャンオブジェクトが属するユーザー |
| OBJECT_NAME    | varchar(128)        | NO | スキャンオブジェクト名（例：TABLE SCAN演算子がスキャンする物理テーブル名） |
| OBJECT_ALIAS    | varchar(261)        | NO | スキャンオブジェクトのエイリアス（例：TABLE SCAN演算子がスキャンする物理テーブルのエイリアス） |
| OBJECT_TYPE    | varchar(20)         | NO | スキャンオブジェクトタイプ（例：SYNONYM、DBLINK、BASIC TABLE） |
| OPTIMIZER     | varchar(4000)       | NO | インデックス関連情報（例：物理行、論理行、再テーブル行数、使用する統計情報タイプ、統計情報バージョン番号） |
| ID     | bigint(20)          | NO | 論理演算子ID |
| PARENT_ID     | bigint(20)          | NO | 論理演算子の親演算子ID |
| DEPTH     | bigint(20)          | NO | 論理演算子が現在のプランにおける深さ、つまり二分プラン木の何層目に位置するか |
| POSITION     | bigint(20)          | NO | 現在の論理演算子が親演算子の何番目の子ノードであるかを示す演算子 |
| COST     | bigint(20)          | NO | 最適化エンジンが見積もったコスト |
| REAL_COST | bigint(20)   | NO | プランが初めて実行された際の実際のコスト（実行フィードバック情報） |
| CARDINALITY    | bigint(20)          | NO | 最適化エンジンが現在の演算子の出力行数を見積もった値 |
| REAL_CARDINALITY    | bigint(20)          | NO | プランが初めて実行された際の現在の演算子の実際の出力行数（実行フィードバック情報） |
| IO_COST    | bigint(20)          | NO | プランが初めて実行された際の現在の演算子の実際のI/Oコスト（実行フィードバック情報） |
| CPU_COST    | bigint(20)          | NO | プランが初めて実行された際の現在の演算子の実際のCPUコスト（実行フィードバック情報） |
| BYTES     | bigint(20)          | NO | 最適化エンジンが現在の演算子のデータ幅を見積もった値 |
| ROWSET | bigint(20)          | NO | 現在の演算子のベクトル化サイズ |
| OTHER_TAG     | varchar(4000)       | NO | 現在のクエリで使用されたヒント。プランの最初の行にのみ格納されます |
| PARTITION_START  | varchar(4000)       | NO | TABLE SCANでスキャンするパーティション情報 |
| OTHER     | varchar(4000)       | NO | 最適化情報。例：プランタイプ、Fast Parserのパラメータ化情報、Plan Note、プランがヒットした制約情報など |
| ACCESS_PREDICATES | varchar(4000)       | NO | 現在の演算子がアクセスする必要がある式の情報 |
| FILTER_PREDICATES | varchar(4000)       | NO | 現在の演算子のフィルター条件 |
| STARTUP_PREDICATES | varchar(4000)    | NO | 現在の演算子の開始条件 |
| PROJECTION   | varchar(4000)       | NO | 現在の演算子の出力式情報 |
| SPECIAL_PREDICATES | varchar(4000)   | NO | 現在の演算子固有の式情報。例：Join Condition、GROUP EXPRS、SORT EXPRSなど |
| QBLOCK_NAME   | varchar(128)       | NO | 現在の演算子が存在するクエリブロックの名前 |
| REMARKS    | varchar(4000)       | NO | qb_nameの変更追跡情報 |
| OTHER_XML    | varchar(4000)         | NO | OUTLINE DATA |

## クエリ例

ユーザーテナント内で、そのテナントがすべてのノードでEXPLAIN PLANを実行して収集した実行計画情報を確認し、1件のレコードを表示します。

```shell
obclient [oceanbase]> SELECT * FROM oceanbase.GV$OB_SQL_PLAN LIMIT 1\G
```

クエリ結果は次のとおりです：

```shell
*************************** 1. row ***************************
         TENANT_ID: 1002
            SVR_IP: 11.xxx.xxx.xxx
          SVR_PORT: 28825
           PLAN_ID: 3813
            SQL_ID: 07E5B378A3CD3778A58E18DB9AD2A430
             DB_ID: 201009
         PLAN_HASH: 7420493073239164301
        GMT_CREATE: 2025-03-25 17:35:21.524475
          OPERATOR: LIMIT
       OBJECT_NODE:
         OBJECT_ID: 0
      OBJECT_OWNER:
       OBJECT_NAME:
      OBJECT_ALIAS:
       OBJECT_TYPE:
         OPTIMIZER:
                ID: 0
         PARENT_ID: -1
             DEPTH: 0
          POSITION: 1
              COST: 1
         REAL_COST: 0
       CARDINALITY: 1
  REAL_CARDINALITY: 1
           IO_COST: 0
          CPU_COST: 71
             BYTES: 0
            ROWSET: 16
         OTHER_TAG:   /*+

  */
   PARTITION_START:
             OTHER:   Plan Type:
      LOCAL

  Parameters:
      :0 => 1

  Note:
      Degree of Parallelisim is 1 because of table property

 ACCESS_PREDICATES:
 FILTER_PREDICATES:
STARTUP_PREDICATES:
        PROJECTION: output([get_sys_var('version_comment', 0)], [get_sys_var('version', 0)])
SPECIAL_PREDICATES: limit(1), offset(nil)
       QBLOCK_NAME: SEL$1
           REMARKS:   stmt_id:0, SEL$1

         OTHER_XML:   /*+
      BEGIN_OUTLINE_DATA
      OPTIMIZER_FEATURES_ENABLE('4.3.5.1')
      END_OUTLINE_DATA
  */
1 row in set
```