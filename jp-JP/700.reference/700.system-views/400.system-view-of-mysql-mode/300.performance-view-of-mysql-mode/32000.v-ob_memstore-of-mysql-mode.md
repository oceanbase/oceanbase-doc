|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type|MySQL Mode|
|machine-translation| en |
|slug|v-ob_memstore-mysql-compatible-mode|

# V$OB_MEMSTORE

<main id="notice" type='explain'>
<h4>説明</h4>
<p>ビュー <code>V$SERVER_MEMSTORE</code> は V4.0.0 以降、<code>V$OB_MEMSTORE</code> に名前が変更されました。V4.0.0 以降のバージョンでは、新しいビュー名 <code>V$OB_MEMSTORE</code> を使用してクエリを実行する必要があります。旧ビュー名 <code>V$SERVER_MEMSTORE</code> でクエリを実行すると、エラーが発生します。</p>
</main>

## 機能の概要

現在の OBServer ノード上の現在のテナントの MemStore 統計情報を表示します。

## フィールドの説明

|    **フィールド名**    |   **タイプ**    | **NULL許容** |              **説明**              |
|----------------|-------------|----------------|----------------------------------|
| SVR_IP         | varchar(46) | NO             | サーバーの IP アドレス                          |
| SVR_PORT       | bigint(20)  | NO             | サーバーのポート番号                            |
| TENANT_ID      | bigint(20)  | NO             | テナント ID                            |
| ACTIVE_SPAN    | bigint(20)  | NO             | アクティブな MemTable が占用するメモリの推定値（過大評価される可能性あり）、単位：バイト |
| FREEZE_TRIGGER | bigint(20)  | NO             | MemTable 凍結をトリガーしたメモリサイズ、単位：バイト        |
| FREEZE_CNT     | bigint(20)  | NO             | MemTable の凍結回数                   |
| MEMSTORE_USED  | bigint(20)  | NO             | MemTable が現在使用中のメモリサイズの合計、単位：バイト        |
| MEMSTORE_LIMIT      | bigint(20)  | NO             | MemTable のメモリサイズ制限、単位：バイト           |

## クエリ例

ユーザーテナントが自身のテナントの現在のノード上の MemTable のメモリ使用状況を確認します。

```shell
obclient [oceanbase]> SELECT * FROM oceanbase.V$OB_MEMSTORE;
```

クエリ結果は次のとおりです：

```shell
+----------------+----------+-----------+-------------+----------------+------------+---------------+----------------+
| SVR_IP         | SVR_PORT | TENANT_ID | ACTIVE_SPAN | FREEZE_TRIGGER | FREEZE_CNT | MEMSTORE_USED | MEMSTORE_LIMIT |
+----------------+----------+-----------+-------------+----------------+------------+---------------+----------------+
| 172.xx.xxx.xxx |     2882 |      1002 |    62914560 |      603982068 |          0 |      62914560 |     2684354550 |
+----------------+----------+-----------+-------------+----------------+------------+---------------+----------------+
1 row in set
```

## 関連ビュー

* すべてのノード上の MemStore 統計情報を表示する：[GV$OB_MEMSTORE](1600.gv-ob_memstore-of-mysql-mode.md)

* 現在のノード上のすべてのパーティションの MemTable の詳細情報を表示する：[V$OB_MEMSTORE_INFO](32100.v-ob_memstore_info-of-mysql-mode.md)