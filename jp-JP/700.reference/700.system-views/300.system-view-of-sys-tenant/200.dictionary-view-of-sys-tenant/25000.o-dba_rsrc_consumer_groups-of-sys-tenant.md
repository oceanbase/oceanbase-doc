|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||
|machine-translation| en |
|slug|oceanbase-dba_rsrc_consumer_groups-sys-tenant|

# oceanbase.DBA_RSRC_CONSUMER_GROUPS

<main id="notice" type='explain'>
<h4>説明</h4>
<p>このビューはV3.2.4バージョンから導入されました。</p>
</main>

## 機能の概要

本テナント内のすべてのリソースグループ情報を表示します。

## フィールドの説明

|       フィールドタイプ        |       タイプ       | NULL許容 |                  説明                  |
|-------------------|----------------|------------|--------------------------------------|
| CONSUMER_GROUP_ID | bigint(20)     | NO         | リソースグループID。                              |
| CONSUMER_GROUP    | varchar(128)  | NO         | ユーザーが紐づけられたリソースグループ名。                        |
| CPU_METHOD        | varchar(128)  | NO         | 互換性のみに使用され、現在の値は常に`NULL`です。                 |
| MGMT_METHOD       | varchar(128)  | NO         | 互換性のみに使用され、現在の値は常に`NULL`です。                 |
| INTERNAL_USE      | varchar(3)    | NO         | このリソースグループが内部使用専用かどうかを示します。互換性のみに使用され、現在の値は常に`NULL`です。 |
| COMMENTS          | varchar(2000) | YES        | リソースグループの備考情報。                           |
| CATEGORY          | varchar(128)  | NO         | リソースグループのカテゴリ。互換性のみに使用され、現在の値は常に`NULL`です。          |
| STATUS            | varchar(128)  | NO         | リソースグループのステータス。互換性のみに使用され、現在の値は常に`NULL`です。          |
| MANDATORY         | varchar(3)    | NO         | このリソースグループが必須かどうかを示します。互換性のみに使用され、現在の値は常に`NULL`です。     |

## クエリ例

システムテナントが本テナント内のすべてのリソースグループ情報を照会します。

```shell
obclient [oceanbase]> SELECT CONSUMER_GROUP_ID, CONSUMER_GROUP FROM oceanbase.DBA_RSRC_CONSUMER_GROUPS;
```

クエリ結果は次のとおりです：

```shell
+-------------------+-------------------+
| CONSUMER_GROUP_ID | CONSUMER_GROUP    |
+-------------------+-------------------+
|             10001 | batch_group       |
|             10000 | interactive_group |
+-------------------+-------------------+
2 rows in set
```

## 関連ビューまたはドキュメント

ユーザーとリソースグループ間の紐づけ関係を確認するには、[oceanbase.DBA_RSRC_GROUP_MAPPINGS](25100.o-dba_rsrc_group_mappings-of-sys-tenant.md)を参照してください。