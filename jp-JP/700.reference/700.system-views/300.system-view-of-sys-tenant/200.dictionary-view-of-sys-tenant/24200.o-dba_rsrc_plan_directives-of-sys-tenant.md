|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||
|machine-translation|en,ja-marker|
|slug|oceanbase-dba_rsrc_plan_directives-sys-tenant|

# oceanbase.DBA_RSRC_PLAN_DIRECTIVES

<main id="notice" type='explain'>
<h4>説明</h4>
<p>このビューは、V3.1.2バージョンから導入されました。</p>
</main>

## 機能の概要

このテナントの各リソースプラン命令の詳細情報を表示します。

## フィールドの説明

|            フィールド名            |       タイプ       | NULLを許容するか |          説明          |
|----------------------------|----------------|------------|----------------------|
| PLAN                       | varchar(128)  | NO         | ステートメントが属するリソース計画の名前。          |
| GROUP_OR_SUBPLAN           | varchar(128)  | NO         | ステートメントが制御するリソースグループの名前。           |
| TYPE                       | varchar(14)   | NO         | 互換性のためにのみ使用され、現在の値は固定で `NULL` です。 |
| CPU_P1                     | decimal(10,0)         | NO         | 互換性のためにのみ使用され、現在の値は固定で `NULL` です。 |
| CPU_P2                     | decimal(10,0)         | NO         | 互換性のためにのみ使用され、現在の値は固定で `NULL` です。 |
| CPU_P3                     | decimal(10,0)         | NO         | 互換性のためにのみ使用され、現在の値は固定で `NULL` です。 |
| CPU_P4                     | decimal(10,0)         | NO         | 互換性のためにのみ使用され、現在の値は固定で `NULL` です。 |
| CPU_P5                     | decimal(10,0)         | NO         | 互換性のためにのみ使用され、現在の値は固定で `NULL` です。 |
| CPU_P6                     | decimal(10,0)         | NO         | 互換性のためにのみ使用され、現在の値は固定で `NULL` です。 |
| CPU_P7                     | decimal(10,0)         | NO         | 互換性のためにのみ使用され、現在の値は固定で `NULL` です。 |
| CPU_P8                     | decimal(10,0)         | NO         | 互換性のためにのみ使用され、現在の値は固定で `NULL` です。 |
| MGMT_P1                    | bigint(20)     | NO         | フルロードシナリオで利用可能な最大CPUシェア。 |
| MGMT_P2                    | decimal(10,0)         | NO         | 互換性のためにのみ使用され、現在の値は固定で `NULL` です。 |
| MGMT_P3                    | decimal(10,0)         | NO         | 互換性のためにのみ使用され、現在の値は固定で `NULL` です。 |
| MGMT_P4                    | decimal(10,0)         | NO         | 互換性のためにのみ使用され、現在の値は固定で `NULL` です。 |
| MGMT_P5                    | decimal(10,0)         | NO         | 互換性のためにのみ使用され、現在の値は固定で `NULL` です。 |
| MGMT_P6                    | decimal(10,0)         | NO         | 互換性のためにのみ使用され、現在の値は固定で `NULL` です。 |
| MGMT_P7                    | decimal(10,0)         | NO         | 互換性のためにのみ使用され、現在の値は固定で `NULL` です。 |
| MGMT_P8                    | decimal(10,0)         | NO         | 互換性のためにのみ使用され、現在の値は固定で `NULL` です。 |
| ACTIVE_SESS_POOL_P1        | decimal(10,0)         | NO         | 互換性のためにのみ使用され、現在の値は固定で `NULL` です。 |
| QUEUEING_P1                | decimal(10,0)         | NO         | 應用互換性のためにのみ使用され、現在の値は固定されており`NULL`です。 |
| PARALLEL_TARGET_PERCENTAGE | decimal(10,0)         | NO         | 應用互換性のためにのみ使用され、現在の値は固定されており`NULL`です。 |
| PARALLEL_DEGREE_LIMIT_P1   | decimal(10,0)         | NO         | 應用互換性のためにのみ使用され、現在の値は固定されており`NULL`です。 |
| SWITCH_GROUP               | varchar(128)  | NO         | 應用互換性のためにのみ使用され、現在の値は固定されており`NULL`です。 |
| SWITCH_FOR_CALL            | varchar(5)    | NO         | 應用互換性のためにのみ使用され、現在の値は固定されており`NULL`です。 |
| SWITCH_TIME                | decimal(10,0)         | NO         | 應用互換性のためにのみ使用され、現在の値は固定されており`NULL`です。 |
| SWITCH_IO_MEGABYTES        | decimal(10,0)         | NO         | 應用互換性のためにのみ使用され、現在の値は固定されており`NULL`です。 |
| SWITCH_IO_REQS             | decimal(10,0)         | NO         | 應用互換性のためにのみ使用され、現在の値は固定されており`NULL`です。 |
| SWITCH_ESTIMATE            | varchar(5)    | NO         | 應用互換性のためにのみ使用され、現在の値は固定されており`NULL`です。 |
| MAX_EST_EXEC_TIME          | decimal(10,0)         | NO         | 應用互換性のためにのみ使用され、現在の値は固定されており`NULL`です。 |
| UNDO_POOL                  | decimal(10,0)         | NO         | 應用互換性のためにのみ使用され、現在の値は固定されており`NULL`です。 |
| MAX_IDLE_TIME              | decimal(10,0)         | NO         | 應用互換性のためにのみ使用され、現在の値は固定されており`NULL`です。 |
| MAX_IDLE_BLOCKER_TIME      | decimal(10,0)         | NO         | 應用互換性のためにのみ使用され、現在の値は固定されており`NULL`です。 |
| MAX_UTILIZATION_LIMIT      | decimal(10,0)         | NO         | 應用互換性のためにのみ使用され、現在の値は固定されており`NULL`です。 |
| PARALLEL_QUEUE_TIMEOUT     | decimal(10,0)         | NO         | 應用互換性のためにのみ使用され、値は常に`NULL`です。   |
| SWITCH_TIME_IN_CALL        | decimal(10,0)         | NO         | 應用互換性のためにのみ使用され、現在の値は固定されており`NULL`です。 |
| SWITCH_IO_LOGICAL          | decimal(10,0)         | NO         | 應用互換性のためにのみ使用され、現在の値は固定されており`NULL`です。 |
| SWITCH_ELAPSED_TIME        | decimal(10,0)         | NO         | 應用互換性のためにのみ使用され、現在の値は固定されており`NULL`です。 |
| PARALLEL_SERVER_LIMIT      | decimal(10,0)         | NO         | 應用互換性のためにのみ使用され、現在の値は固定されており`NULL`です。 |
| UTILIZATION_LIMIT          | bigint(20)    | NO         | 應用互換性のためにのみ使用され、現在の値は固定されており`NULL`です。 |
| PARALLEL_STMT_critical     | varchar(12)   | NO         | 互換性のためにのみ使用され、現在の値は固定で `NULL` です。 |
| SESSION_PGA_LIMIT          | decimal(10,0)         | NO         | 互換性のためにのみ使用され、現在の値は固定で `NULL` です。 |
| PQ_TIMEOUT_ACTION          | varchar(6)    | NO         | 互換性のためにのみ使用され、現在の値は固定で `NULL` です。 |
| コメント                   | varchar(2000) | はい        | ステートメントの備考情報              |
| ステータス                     | varchar(128)  | いいえ         | 互換性のためにのみ使用され、現在の値は固定で `NULL` です。 |
| 必須                  | varchar(3)    | いいえ         | 互換性のためにのみ使用され、現在の値は固定で `NULL` です。 |

## クエリ例

システムテナントは、自身の各リソースプラン指示に関する詳細情報を照会します。

```shell
obclient [oceanbase]> SELECT PLAN, GROUP_OR_SUBPLAN FROM oceanbase.DBA_RSRC_PLAN_DIRECTIVES;
```

クエリ結果は次のとおりです：

```shell
+---------+------------------+
| PLAN    | GROUP_OR_SUBPLAN |
+---------+------------------+
| daytime | batch_group      |
+---------+------------------+
1 row in set
```

## 関連ビュー

* [oceanbase.DBA_RSRC_PLANS](24300.o-dba_rsrc_plans-mysql-of-sys-tenant.md)

* [V$RSRC_PLAN](../300.performance-view-of-sys-tenant/35300.v-rsrc_plan-of-sys-tenant.md)
