|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type|MySQL Mode|
|slug|5000-5999-mysql-compatible-mode|

# 5000 ~ 5999

この範囲はSQL層のエラーコードを表します。SQLエラーコード、WITH句のエラーコード、およびFactoringエラーコードが含まれます。

## ERROR 5002 (HY000) : Resolve error

* OceanBase エラーコード：5002

* エラー原因：解析エラー。

* 解決策：テクニカルサポートにお問い合わせください。

## ERROR 5010 (21000) : Unknown operator

* OceanBase エラーコード：5010

* エラー原因：解析段階の内部防御チェックエラーコード。

* 解決策：テクニカルサポートにお問い合わせください。

## ERROR 5011 (42000) : Duplicated star

* OceanBase エラーコード：5011

* エラー原因：`*` の使用方法が誤っています。SQL文では、`*` を1つのみ使用できます。

* 解決策：SQL文から余分な `*` を削除してください。

## ERROR 5012 (HY000) : Illegal ID

* OceanBase エラーコード：5012

* エラー原因：内部パラメータ防御エラー。

* 解決策：テクニカルサポートにお問い合わせください。

## ERROR 5014 (HY000) : Illegal value

* OceanBase エラーコード：5014

* エラー原因：数値型の変換エラー。

* 解決策：SQL文内のCAST文が正しいか確認してください。例えば、暗黙的な型変換が存在するかどうかを確認してください。

## ERROR 5016 (HY000) : Generate logical plan error

* OceanBase エラーコード：5016

* エラー原因：計画生成エラー。

* 解決策：テクニカルサポートにお問い合わせください。

## ERROR 5017 (HY000) : Schema not set

* OceanBase エラーコード：5017

* エラー原因：内部エラーコードです。

* 解決策：テクニカルサポートにお問い合わせください。

## ERROR 5023 (S1004) : unsupport MySQL type (number). Maybe you should use java.sql.Timestamp instead of java.util.Date

* OceanBase エラーコード：5023

* エラー原因：サポートされていないタイプです。

* 解決策：テクニカルサポートにお問い合わせください。

## ERROR 5031 (HY000) : Column not found

* OceanBase エラーコード：5031

* エラー原因：列が存在しません。

* 解決策：操作対象となる列が存在するかどうか確認するか、テクニカルサポートにお問い合わせください。

## ERROR 5034 (01007) : No user

* OceanBase エラーコード：5034

* エラー原因：ユーザー名が空です。

* 解決策：ユーザー名を入力してください。

## ERROR 5050 (42710) : User exists

* OceanBase エラーコード：5050

* エラー原因：ユーザーまたはロールが既に存在します。

* 解決策：操作対象のユーザーまたはロールが既に存在するかどうか確認するか、テクニカルサポートにお問い合わせください。

## ERROR 5053 (HY093) : Incorrect arguments number to EXECUTE, need %ld arguments but give %ld

* OceanBase エラーコード：5053

* エラー原因：EXECUTE時のバインド変数の数とPREPAREで指定したパラメータ数が一致しません。

* 解決策：EXECUTEでバインドする変数の数とPREPAREで必要なパラメータ数が一致するようにしてください。

関連する事例：

* [カーソル実行失敗、エラーコード5053](https://www.oceanbase.com/knowledge-base/oceanbase-database-20000001059?back=kb)

## ERROR 5058 (42000) : Duplicated parameters

* OceanBase エラーコード：5058

* エラーの原因：重複するパラメータ。

* 解決策：テクニカルサポートにお問い合わせください。

## ERROR 5063 (42000) : Unknown hint

* OceanBase エラーコード：5063

* エラーの原因：解析エラー。

* 解決策：テクニカルサポートにお問い合わせください。

## ERROR 5066 (HY000) : Session interrupted

* OceanBase エラーコード：5066

* エラーの原因：セッションが終了しました。

* 解決策：システム管理者またはデータベース管理者にお問い合わせください。

## ERROR 5071 (42000) : Cannot alter index column

* OceanBase エラーコード：5071

* エラーの原因：インデックス参照列を削除しました。

* 解決策：インデックス参照列の削除は許可されていません。

## ERROR 5074 (23000) : NOP cannot be used here

* OceanBase エラーコード：5074

* エラーの原因：カーネルのバグ。

* 解決策：テクニカルサポートにお問い合わせください。

## ERROR 5081 (25000) : The server is read only now

* OceanBase エラーコード：5081

* エラーの原因：現在操作対象のデータは読み取り専用です。

* 解決策：現在操作対象のデータが読み取り専用かどうか確認するか、テクニカルサポートにお問い合わせください。

## ERROR 5083 (22000) : invalid obj type for type promotion: left_type=%s right_type=%s

* OceanBase エラーコード：5083

* エラーの原因：型変換エラー。

* 解決策：テクニカルサポートにお問い合わせいただき、調査をお手伝いいたします。

関連する事例：

* [OracleモードでUpdateしてClobを更新する際にret_code=-5083が発生する](https://www.oceanbase.com/knowledge-base/oceanbase-database-1000000000322504)

## ERROR 5084 (22000) : Can not cast varchar value to bool type

* OceanBase エラーコード：5084

* エラーの原因：型変換エラー。

* 解決策：テクニカルサポートにお問い合わせいただき、調査をお手伝いいたします。

## ERROR 5085 (22000) : Not a number Can not cast varchar value to number type

* OceanBase エラーコード：5085

* エラーの原因：型変換エラー。

* 解決策：テクニカルサポートにお問い合わせいただき、調査をお手伝いいたします。

## ERROR 5086 (22000) : Not timestamp Can not cast varchar value to timestamp type

* OceanBase エラーコード：5086

* エラーの原因：型変換エラー。

* 解決策：テクニカルサポートにお問い合わせいただき、調査をお手伝いいたします。

## ERROR 5087 (22000) : Result value was out of range when cast to number

* OceanBase エラーコード：5087

* エラーの原因：数値が範囲外です。

* 解決策：テクニカルサポートにお問い合わせいただき、調査をお手伝いいたします。

## ERROR 5090 (22000) : Precision was out of range

* OceanBase エラーコード：5090

* エラーの原因：精度が範囲外です。

* 解決策：テクニカルサポートにお問い合わせいただき、調査をお手伝いいたします。

## ERROR 5091 (22000) : Scale value was out of range

* OceanBase エラーコード：5091

* エラー原因：精度が範囲を超えています。

* 解決策：テクニカルサポートにお問い合わせください。

## ERROR 5092 (42000) : Unknown index

* OceanBase エラーコード：5092

* エラー原因：未知のインデックスです。

* 解決策：インターフェースで誤ってインデックスが使用されていないか確認してください。

## ERROR 5098 (22001) : Data too long(%d\>%ld) for column '%s'

* OceanBase エラーコード：5098

* エラー原因：数値入力が上限を超えています。

* 解決策：数値の長さを短縮してください。

## ERROR 5099 (42000) : System config unknown

* OceanBase エラーコード：5099

* エラー原因：無効なパラメータです。

* 解決策：パラメータ名が正しいか確認してください。

## ERROR 5106 (42000) : EXTRACT() expected timestamp or a string as date argument

* OceanBase エラーコード：5106

* エラー原因：データ型が誤っています。

* 解決策：テクニカルサポートにお問い合わせください。

## ERROR 5107 (42000) : Invalid operand type for IS operator, lval=%s

* OceanBase エラーコード：5107

* エラー原因：`IS` 式のパラメータが誤っています。

* 解決策：テクニカルサポートにお問い合わせください。

## ERROR 5108 (42000) : function LENGTH（) expected a varchar argument

* OceanBase エラーコード：5108

* エラー原因：`LENGTH` 式のパラメータが誤っています。

* 解決策：テクニカルサポートにお問い合わせいただき、調査をお手伝いいたします。

## ERROR 5110 (42000) : TIME_TO_USEC() expected timestamp or a string as date argument

* OceanBase エラーコード：5110

* エラー原因：`TIME_TO_USEC` 関数のパラメータは `VARCHAR`、`TIME`、`DATATIME`、または `TIMESTAMP` 型のみとして指定できます。

* 解決策：パラメータの型を修正してください。

## ERROR 5111 (42000) : USEC_TO_TIME expected a interger number as usec argument

* OceanBase エラーコード：5111

* エラー原因：`USEC_TO_TIME` 関数のパラメータは `TINYINT`、`MEDIUMINT`、`INT`、または `BIGINT` 型のみとして指定できます。

* 解決策：パラメータの型を修正してください。

## ERROR 5112 (42P01) : Variable %.\*s does not exists

* OceanBase エラーコード：5112

* エラー原因：変数が存在しません。

* 解決策：変数のスペルミスがないか確認してください。

## ERROR 5114 (42000) : Invalid numeric char '%c'

* OceanBase エラーコード：5114

* エラー原因：無効な数字です。

* 解決策：テクニカルサポートにお問い合わせいただき、調査をお手伝いいたします。

関連する問題事例：

* [テーブル作成時にINFORMATION_SCHEMA.STATISTICSビューを参照した際にエラー5114が発生する](https://www.oceanbase.com/knowledge-base/oceanbase-database-1000000000279883)

## ERROR 5116 (HY000) : Logical operator child index overflow

* OceanBase エラーコード：5116

* エラー原因：カーネルエラーです。

* 解決策：テクニカルサポートにお問い合わせいただき、調査をお手伝いいたします。

## ERROR 5117 (HY000) : fail to explain plan

* OceanBaseエラーコード：5117

* エラー原因：プランの生成に失敗しました。

* 解決策：テクニカルサポートにお問い合わせいただき、調査をお手伝いください。

## ERROR 5118 (HY000) : fail to copy logical operator

* OceanBase エラーコード：5118

* エラー原因：実行計画の生成に失敗しました。

* 解決策：テクニカルサポートにお問い合わせください。

## ERROR 5119 (HY000) : fail to generate plan

* OceanBase エラーコード：5119

* エラー原因：実行計画の生成に失敗しました。

* 解決策：テクニカルサポートにお問い合わせください。

## ERROR 5120 (HY000) : fail to create raw expr

* OceanBase エラーコード：5120

* エラー原因：実行計画の生成に失敗しました。

* 解決策：テクニカルサポートにお問い合わせください。

## ERROR 5121 (HY000) : fail to generate join order

* OceanBase エラーコード：5121

* エラー原因：実行計画の生成に失敗しました。

* 解決策：テクニカルサポートにお問い合わせください。

## ERROR 5122 (HY000) : optimizer general error

* OceanBase エラーコード：5122

* エラー原因：実行計画の生成に失敗しました。

* 解決策：テクニカルサポートにお問い合わせください。

## ERROR 5130 (HY000) : sql resolver no memory

* OceanBase エラーコード：5130

* エラー原因：メモリ不足です。

* 解決策：SQLメモリを増量するか、テクニカルサポートにお問い合わせください。

## ERROR 5131 (HY000) : plan cache support dml only

* OceanBase エラーコード：5131

* エラー原因：非DMLステートメントに`OUTLINE`バインディングを実行しました。

* 解決策：非DMLステートメントに`OUTLINE`バインディングを実行しないでください。

## ERROR 5133 (42000) : No such grant defined

* OceanBase エラーコード：5133

* エラー原因：権限を回収する際、その権限が付与されていないことが判明しました。

## ERROR 5136 (HY000) : plan exists in plan cache already

* OceanBase エラーコード：5136

* エラー原因：実行計画を追加する際、計画キャッシュ内に同じ計画が既に存在していました。

* 解決策：内部エラーコードです。サポートチームにお問い合わせいただき、調査のご協力をお願いします。

## ERROR 5137 (HY000) : plan is expired

* OceanBase エラーコード：5137

* エラー原因：計画が期限切れになっています。

* 解決策：内部エラーコードです。サポートチームにお問い合わせいただき、調査のご協力をお願いします。

## ERROR 5138 (HY000) : no plan exist

* OceanBase エラーコード：5138

* エラー原因：get plan時に、計画が存在しません。

* 解決策：内部エラーコードです。サポートチームにお問い合わせいただき、調査のご協力をお願いします。

## ERROR 5144 (HY000) : COLLATION '%.\*s' is notvalid for CHARACTER SET '%.\*s'

* OceanBase エラーコード：5144

* エラー原因：以下のいずれかの可能性があります：

  * 演算子または組み込み関数の文字列オペランド（`nlsparams`パラメータを除く）が、同じ文字セットを使用していません。

  * `nlsparams`オペランドがデータベースの文字セットに含まれていません。

  * 組み込み関数に渡される文字列データがデータベースの文字セット以外の文字セットを使用している場合、その組み込み関数がその文字セットを受け付けていません。

  * `CHR()`または`CSCONVERT()`の2番目のパラメータが`CHAR_CS`または`NCHAR_CS`ではありません。

  * `INSERT` ステートメントの `VALUES` 句内の文字列式、または `UPDATE` ステートメントの `SET` 句内の文字列式の文字セットが、挿入対象の列で使用されている文字セットと一致しません。

  * テーブル作成時に `DEFAULT` 句で指定した値の文字セットが、列で宣言された文字セットと一致しません。

  * `PL/SQL` 関数のパラメータが、対応するパラメータの文字セット要件を満たしていません。

## ERROR 5150 (HY000) : Tenant not in this server

* OceanBase エラーコード：5150

* エラー原因：該当するテナントリソースがマシン上に存在しません。

* 解決策：再試行してください。問題が継続する場合は、テクニカルサポートにお問い合わせいただき、調査のご協力をお願いします。

関連する問題事例：

* [一般テナントでSELECT DBA_OB_LS_LOCATIONSがタイムアウトエラー-5150を返す](https://www.oceanbase.com/knowledge-base/oceanbase-database-1000000000233832)

## ERROR 5151 (42P01) : tablegroup not exist

* OceanBase エラーコード：5151

* エラー原因：Table groupが存在しない、無効なTable group名が指定されています。

* 解決策：Table group名が正しいか確認してください。

## ERROR 5156 (HY000) : tenant \\'%s\\' already exist

* OceanBase エラーコード：5156

* エラー原因：作成したテナント名が重複しているか、内部エラーが発生しています。

* 解決策：テナント名が重複している場合は、テナント名を変更して再作成してください。その他の場合は、テクニカルサポートにお問い合わせいただき、調査のご協力をお願いします。

## ERROR 5157 (HY000) : Unknown tenant '%.\*s'

* OceanBase エラーコード：5157

* エラー原因：スキーマのリフレッシュが遅れているか、テナントが削除されています。

* 解決策：再試行してください。問題が継続する場合は、テクニカルサポートにお問い合わせいただき、調査のご協力をお願いします。

関連する問題事例：

* [ログにエラーコードret=-5157が常に出力される](https://www.oceanbase.com/knowledge-base/oceanbase-database-1000000000207724)

## ERROR 5159 (HY000) : tablegroup already exist

* OceanBase エラーコード：5159

* エラーの原因：テーブルグループは既に存在します。

* 解決策：テーブルグループ名が正しいか確認してください。

## ERROR 5160 (HY000) : invalid tenant name specified in connection string

* OceanBaseエラーコード：5160

* エラーの原因：指定されたテナント名が存在しません。

* 解決策：指定されたテナントが存在するか確認してください。存在する場合は、技術サポートにお問い合わせいただき、調査をお手伝いいただけますでしょうか。

## ERROR 5212 (42000) : auto-increment column '%.\*s' should not be part of partition key

* OceanBaseエラーコード：5212

* エラーの原因：自動インクリメント列はパーティションキーの一部にすることはできません。

* 解決策：自動インクリメント列をパーティションキーから削除してください。

## ERROR 5223 (42000) : wrong ident name

* OceanBaseエラーコード：5223

* エラーの原因：変数名が誤っています。

* 解決策：有効な変数名を使用してください。

## ERROR 5226 (42000) : plan cache memory used reach the high water mark

* OceanBaseエラーコード：5226

* エラーの原因：プランキャッシュの使用メモリが上限に達したため、自動的に淘汰がトリガーされました。

* 解決策：プランキャッシュのメモリ上限を引き上げてください。

関連する事例：

* [Plan Cacheメモリがいっぱい](https://www.oceanbase.com/knowledge-base/oceanbase-database-1000000000209934)
* [add_planエラー：-5226 plan cache memory used reach limit](https://www.oceanbase.com/knowledge-base/oceanbase-database-1000000000209936)
* [UDTモジュールのメモリリークによりplan cacheがいっぱいになる](https://www.oceanbase.com/knowledge-base/oceanbase-database-1000000000209938)

## ERROR 5234 (HY000) : The database \\'%.\*s\\' is read only so it cannot execute this statement

* OceanBaseエラーコード：5234

* エラーの原因：読み取り専用データベースに対して操作を実行しました。

* 解決策：読み取り専用のデータベースには操作を行わないでください。

## ERROR 5235 (HY000) : The table \\'%.\*s.%.\*s\\' is read only so it cannot execute this statement

* OceanBase エラーコード：5235

* エラー原因：読み取り専用テーブルに対して操作を実行しました。

* 解決策：読み取り専用のテーブルには操作を実行しないでください。

関連する問題例：

* [Oracleモードの通常ユーザーが読み取り専用ビューを作成後、DROPまたはREPLACEビューできない場合](https://www.oceanbase.com/knowledge-base/oceanbase-database-1000000000207728)

## ERROR 5243 (HY000) : system variables' version maybe different

* OceanBase エラーコード：5243

* エラー原因：内部エラーコード。

* 解決策：サポートチームにお問い合わせいただき、調査をお手伝いいたします。

## ERROR 5245 (HY000) : some tasks are skipped, skipped server addr is '%s', the orginal error code is %d

* OceanBase エラーコード：5245

* エラー原因：内部エラーコード。

* 解決策：サポートチームにお問い合わせいただき、調査をお手伝いいたします。

## ERROR 5248 (HY000) : Alter table add auto_increment column is dangerous, table_name=\\'%.\*s\\', column_name=\\'%s\\'

* OceanBase エラーコード：5248

* エラー原因：テーブルの変更時に自動インクリメント列を追加することは推奨されません。

* 解決策：この操作は、テーブル内にデータがない場合にのみ実行することを推奨します。

## ERROR 5252 (HY000) : Column \\'%.\*s\\' definition is ambiguous. Column has both NULL and NOT NULL attributes

* OceanBase エラーコード：5252

* エラー原因：フィールド定義が `NULL` と `NOT NULL` の両方を指定しています。

* 解決策：どちらか一方のみを保持します。

## ERROR 5258 (HY000) : Invalid block size, block size should between 1024 and 1048576

* OceanBase エラーコード：5258

* エラー原因：無効なBlockサイズです。

* 解決策：Blockサイズを変更します。

## ERROR 5260 (HY000) : Tenant \\'%.\*s\\' is locked

* OceanBaseエラーコード：5260

* エラーの原因：テナントがロックされています。

* 解決策：テナントのロックを解除します。

## ERROR 5264 (HY000) : Outline '%.\*s' already exists

* OceanBaseエラーコード：5264

* エラーの原因：`OUTLINE` は既に存在します。

* 解決策：他のSQLが同じ `OUTLINE` とバインドされていないか確認します。

## ERROR 5265 (HY000) : Outline \\'%.\*s.%.\*s\\' doesn\\'t exist

* OceanBase エラーコード：5265

* エラー原因：`OUTLINE` が存在しません。

* 解決策：対応する `OUTLINE` が作成されているか確認してください。

## ERROR 5267 (HY000) : invalid outline ,error info:%s

* OceanBase エラーコード：5267

* エラー原因：無効な `OUTLINE`。

* 解決策：テクニカルサポートにお問い合わせいただき、調査をお手伝いいたします。

## ERROR 5268 (HY000) : SQL reach max concurrent num %ld

* OceanBase エラーコード：5268

* エラー原因：SQLの同時実行数の上限に達しました。

* 解決策：並列処理の数を減らしてください。

## ERROR 5269 (HY000) : can not perform DDL/DML over objects in Recycle Bin

* OceanBase エラーコード：5269

* エラー原因：ごみ箱内のオブジェクトには操作できません。

* 解決策：オブジェクトをごみ箱から復元してから操作してください。

## ERROR 5270 (HY000) : object not in RECYCLE BIN

* OceanBase エラーコード：5270

* エラー原因：オブジェクトはごみ箱にありません。

* 解決策：テクニカルサポートにお問い合わせいただき、調査をお手伝いいたします。

## ERROR 5272 (HY000) : Outline content '%.\*s' of outline '%.\*s' already exists when added

* OceanBase エラーコード：5272

* エラー原因：`OUTLINE` が既に作成されています。

* 解決策：以前に作成した `OUTLINE` が期待通りか確認してください。期待通りであれば、再度 `OUTLINE` を作成する必要はありません。期待通りでない場合は、`DROP OUTLINE` してから再作成できます。

## ERROR 5273 (HY000) : Max concurrent in outline '%.\*s' already exists when added

* OceanBase エラーコード：5273

* エラー原因：作成しようとしているレート制限ルールは既に存在します。

* 解決策：レート制限を作成するステートメントに、既存のレート制限ルールが含まれていないか確認してください。

## ERROR 5278 (HY000) : This partition value with incorrect charset type

* OceanBase エラーコード：5278

* エラー原因：パーティション値のメタ情報が一致しません。

* 解決策：パーティション値を修正し、メタ情報を一致させてください。

## ERROR 5291 (HY000) : The CTXCAT column must be contiguous in the index column list

* OceanBase エラーコード：5291

* エラー原因：`CTXCAT` 列は連続していなければなりません。

* 解決策：`CTXCAT` 列を調整するか、テクニカルサポートにお問い合わせください。

## ERROR 5296 (42000) : there has no readable replica

* OceanBase エラーコード：5296

* エラー原因：計画生成時に利用可能なレプリカが選択されませんでした。

* 解決策：テクニカルサポートにお問い合わせください。

## ERROR 5297 (HY000) : unexpected time zone info transition

* OceanBase エラーコード：5297

* エラー原因：gapまたはoverlap時間帯が使用されています。

* 解決策：非gap時間帯を使用するか、`error_on_overlap_time` 変数を `false` に設定してください。

## ERROR 5298 (HY000) : synonym '%.\*s' already exists

* OceanBase エラーコード：5298

* エラー原因：シノニムが既に存在します。

* 解決策：競合するシノニム名がないか確認し、異なる名前のシノニムを作成してください。

## ERROR 5299 (HY000) : synonym \\'%.\*s.%.\*s\\' doesn\\'t exist

* OceanBase エラーコード：5299

* エラーの原因：シノニムが存在しません。

* 解決策：存在しないシノニムを使用していないか確認します。

## ERROR 5300 (HY000) : missing ORDER BY expression in the window specification

* OceanBase エラーコード：5300

* エラーの原因：ウィンドウ関数内で `ORDER BY` 式が欠落しています。

* 解決策：関連する `ORDER BY` 式をウィンドウ関数に追加します。

## ERROR 5301 (HY000) : The argument of the window function should be a constant for a partition

* OceanBase エラーコード：5301

* エラーの原因：ウィンドウ関数のパラメータは定数である必要があります。

* 解決策：ウィンドウ関数内の関連パラメータを定数値に修正します。

## ERROR 5307 (HY000) : Not enough thread resource to execute the query. Require setting parallel_servers_target to a value no less than %ld for this query

* OceanBase エラーコード：5307

* エラーの原因：実行スレッドが不足しています。

* 解決策：`parallel_servers_target` システム変数の値を大きく設定します。

関連する問題事例：

* [OBServerログにout of px worker resourceと表示される原因](https://www.oceanbase.com/knowledge-base/oceanbase-database-1000000000267156)
* [PX実行でスレッドを取得できず、ob_query_timeoutタイムアウトまで無限ループする](https://www.oceanbase.com/knowledge-base/oceanbase-database-1000000000217870)
* [OceanBaseデータベースログob_px_admissionでout of px worker resourceエラーが発生する](https://www.oceanbase.com/knowledge-base/oceanbase-database-1000000000209995)

  <main id="notice" type='explain'>
    <h4>説明</h4>
    <p>このエラーコードはV4.0.0バージョンから変更されました。エラーメッセージが <code>ERROR 5307 (HY000) : Scheduler thread number is not enough </code> から <code>ERROR 5307 (HY000) : Not enough thread resource to execute the query. Require setting parallel_servers_target to a value no less than %ld for this query </code> に調整されました。</p>
  </main>

## ERROR 5309 (HY000) : Ignore sql in restore process

* OceanBase エラーコード：5309

* エラーの原因：論理復元時に特定のSQLをスキップする必要があります。

* 解決策：処理は不要です。

## ERROR 5311 (HY000) : Invalid column id for %.\*s

* OceanBase エラーコード：5311

* エラーの原因：無効な列 `ID`。

* 解決策：テクニカルサポートにお問い合わせください。

## ERROR 5312 (HY000) : Duplicated ddl id '%.\*s'

* OceanBase エラーコード：5312

* エラーの原因：重複するDDL `ID`。

* 解決策：テクニカルサポートにお問い合わせください。

## ERROR 5319 (HY000) : Child column \\'%.\*s\\' data length cannot be less than parent column \\'%.\*s\\' data length

* OceanBase エラーコード：5319

* エラーの原因：外部キー列の長さは、参照される列の長さより短くすることはできません。

* 解決策：関連する列の長さを調整します。

## ERROR 5320 (HY000) : \\'%.\*s\\': used in a foreign key constraint

* OceanBase エラーコード：5320

* エラーの原因：フィールドが外部キー制約で使用されています。

* 解決策：そのフィールドには操作できません。

## ERROR 5325 (HY000) : ORDER SIBLINGS BY clause not allowed here

* OceanBase エラーコード：5325

* エラーの原因：`ORDER SIBLINGS BY` が誤った場所に記述されています。

* 解決策：`ORDER SIBLINGS BY` は階層クエリステートメント内でのみ使用できます。

## ERROR 5326 (HY000) : when using SYS_CONNECT_BY_PATH function, cannot have separator as part of column value

* OceanBase エラーコード：5326

* エラーの原因：列値に `SYS_CONNECT_BY_PATH` 関数が区切り文字として使用する文字列が含まれています。

* 解決策：`SYS_CONNECT_BY_PATH` 関数に他の区切り文字を指定し、その区切り文字がどの列値にも含まれていないことを確認してから、操作を再試行します。

## ERROR 5327 (HY000) : Database can not be specified in public synonym

* OceanBase エラーコード：5327

* エラーの原因：パブリックシノニムの使用時に DATABASE を指定しました。

* 解決策：公有シノニムの使用には、DATABASEを実行する必要はありません。

## ERROR 5328 (HY000) : Looping chain of synonyms

* OceanBaseエラーコード：5328

* エラー原因：循環依存のシノニムが作成されました。

* 解決策：循環依存のシノニムを確認し、異なる名前のシノニムを作成して循環依存を解消します。

## ERROR 5329 (HY000) : Cannot create a synonym with same name as object

* OceanBaseエラーコード：5329

* エラー原因：シノニム名とオブジェクト名が同じです。

* 解決策：オブジェクト名とは異なるシノニムを作成します。

## ERROR 5330 (HY000) : Synonym %s translation is no longer valid

* OceanBaseエラーコード：5330

* エラー原因：シノニムが参照するオブジェクトが存在しません。

* 解決策：シノニムが依存するオブジェクトを作成します。

## ERROR 5331 (HY000) : name is already used by an existing object

* OceanBaseエラーコード：5331

* エラー原因：オブジェクトが既に存在します。

* 解決策：PLで対象となっているオブジェクトは既に存在しており、重複して作成することはできません。

## ERROR 5337 (HY000) : rowid is wrong

* OceanBase エラーコード：5337

* エラー原因：`ROWID` から解析された内容が期待と一致しません。

* 解決策：妥当な `ROWID` を渡します。

## ERROR 5338 (HY000) : Window Function not allowed here

* OceanBase エラーコード：5338

* エラー原因：SQLのある式にウィンドウ関数が含まれています。

* 解決策：SQLの該当する式からウィンドウ関数を削除します。

## ERROR 5339 (HY000) : Fail to parse list partition

* OceanBase エラーコード：5339

* エラー原因：LISTパーティション定義において、パーティション数の定義とパーティション指定句が一致しません。

* 解決策：パーティション数の定義を修正します。

## ERROR 5341 (HY000) : timezone region ID is invalid

* OceanBase エラーコード：5341

* エラー原因：`TIMESTAMP` データに記録された `Region ID` を解析できません。

* 解決策：テクニカルサポートにお問い合わせください。

## ERROR 5342 (HY000) : invalid hex number

* OceanBase エラーコード：5342

* エラー原因：無効な16進数です。

* 解決策：テクニカルサポートにお問い合わせください。

## ERROR 5343 (42000) : wrong number or types of arguments in call to '%.\*s'

* OceanBase エラーコード：5343

* エラー原因：パラメータリストが誤っています。

* 解決策：パラメータリストを確認します。

## ERROR 5345 (HY000) : insufficient parallel query worker available

* OceanBase エラーコード：5345

* エラーの原因：PXスレッドが不足しています。

* 解決策：PXスレッド数を増やします。

## ERROR 5346 (HY000) : FOR UPDATE of this query expression is not allowed

* OceanBase エラーコード：5346

* エラーの原因：`FOR UPDATE` 句で無効な式が指定されています。

* 解決策：`FOR UPDATE` の構文を修正します。

## ERROR 5347 (HY000) : argument should be a function of expressions in PARTITION BY

* OceanBase エラーコード：5347

* エラーの原因：構文解析エラーです。

* 解決策：SQLを修正します。

## ERROR 5348 (HY000) : result of string concatenation is too long

* OceanBase エラーコード：5348

* エラーの原因：`WM_CONCAT` の連結文字列が長すぎます。

* 解決策：連結後の長さが `group_concat_max_len` を超えないか確認し、超える場合は `group_concat_max_len` を増やします。

  <main id="notice" type='explain'>
    <h4>説明</h4>
    <p>このシステム変数の詳細については、OceanBaseデータベース公式ドキュメントの「リファレンスガイド（MySQLモード）」のシステム変数セクションを参照してください。</p>
  </main>

## ERROR 5350 (HY000) : updating partition key column would cause a partition change

* OceanBase エラーコード：5350

* エラーの原因：`UPDATE` 操作により、行が配置されているパーティションが変更されます。

* 解決策：`ALTER TABLE` ステートメントを使用して、テーブルの `ENABLE ROW MOVEMENT` プロパティを有効にします。

## ERROR 5351 (HY000) : invalid type given for an argument

* OceanBase エラーコード：5351

* エラーの原因：パラメータタイプが誤っています。

* 解決策：パラメータタイプを確認します。

## ERROR 5352 (HY000) : specified field not found in datetime or interval

* OceanBaseエラーコード：5352

* エラー原因：タイムゾーン変換時に誤ったデータが検出された。

* 解決策：テクニカルサポートにお問い合わせいただき、調査をご依頼ください。

## ERROR 5360 (HY000) : insufficient privileges

* OceanBaseエラーコード：5360

* エラー原因：権限が不足しています。

* 解決策：対応する権限を付与します。

関連する問題事例：

* [テーブル作成失敗、エラーコード5360/OBE-01031](https://www.oceanbase.com/knowledge-base/oceanbase-database-20000001004)

## ERROR 5361 (HY000) : user %.\*s lacks CREATE SESSION privilege; logon denied

* OceanBaseエラーコード：5361

* エラー原因：ユーザーにログイン権限がありません。

* 解決策：ユーザーにログイン権限を付与します。

## ERROR 5363 (HY000) : system privileges not granted to '%.\*s'

* OceanBaseエラーコード：5363

* エラー原因：ユーザーにシステム権限が付与されていません。

* 解決策：ユーザーに対応するシステム権限を付与します。

## ERROR 5364 (HY000) : only SELECT and ALTER privileges are valid for sequences

* OceanBaseエラーコード：5364

* エラー原因：SEQUENCEオブジェクトに `SELECT`、`ALTER` 以外の権限が付与されています。

* 解決策：SEQUENCEオブジェクトに `SELECT`、`ALTER` 以外の権限は付与しないでください。

## ERROR 5365 (HY000) : EXECUTE privilege not allowed for tables

* OceanBaseエラーコード：5365

* エラー原因：テーブルに `EXECUTE` 権限が付与されています。

* 解決策：テーブルに `EXECUTE` 権限を付与しないでください。

## ERROR 5366 (HY000) : only EXECUTE and DEBUG privileges are valid for procedures

* OceanBase エラーコード：5366

* エラーの原因：PROCEDUREオブジェクトに `EXECUTE`、`DEBUG` 以外の権限が付与されています。

* 解決策：PROCEDUREオブジェクトに `EXECUTE`、`DEBUG` 以外の権限を付与しないでください。

## ERROR 5367 (HY000) : only EXECUTE, DEBUG, and UNDER privileges are valid for types

* OceanBase エラーコード：5367

* エラーの原因：TYPEオブジェクトに `EXECUTE`、`DEBUG`、`UNDER` 以外の権限が付与されています。

* 解決策：TYPEオブジェクトに `EXECUTE`、`DEBUG`、`UNDER` 以外の権限を付与しないでください。

## ERROR 5368 (HY000) : ADMIN option not granted for role '%.\*s'

* OceanBase エラーコード：5368

* エラーの原因：ロールに対応する ADMINオプションがありません。

* 解決策：ユーザーのロールに ADMINオプションを付与します。

## ERROR 5369 (HY000) : user or role '%.\*s' does not exist

* OceanBase エラーコード：5369

* エラーの原因：ユーザーが存在しません。

* 解決策：ユーザー名が間違っていないか確認してください。間違っている場合は正しいユーザー名に修正してください。正しい場合は、必要なユーザー名を作成してください。

## ERROR 5370 (HY000) : missing ON keyword

* OceanBase エラーコード：5370

* エラーの原因：`ON` キーワードが欠けています。

* 解決策：`ON` を追加します。

## ERROR 5371 (HY000) : grant option does not exist for '%.\*s.%.\*s'

* OceanBase エラーコード：5371

* エラーの原因：ユーザーに対応する再付与権限がありません。

* 解決策：対応する再付与権限を追加します。

## ERROR 5372 (HY000) : ALTER, INDEX and EXECUTE not allowed for views

* OceanBaseエラーコード：5372

* エラー原因：VIEWオブジェクトに`ALTER`、`INDEX`、`EXECUTE`権限を付与しようとしています。

* 解決策：VIEWオブジェクトに`ALTER`、`INDEX`、または`EXECUTE`権限を付与しないでください。

## ERROR 5373 (HY000) : circular role grant detected

* OceanBaseエラーコード：5373

* エラー原因：ロールが循環的な権限付与を作成しています。

* 解決策：ロールに対して循環的な権限付与を行わないでください。

## ERROR 5374 (HY000) : invalid privilege on directories

* OceanBaseエラーコード：5374

* エラー原因：ディレクトリオブジェクトに誤った権限が付与されています。

* 解決策：ディレクトリオブジェクトを操作するための正しい権限を付与してください。

## ERROR 5375 (HY000) : directory access denied

* OceanBaseエラーコード：5375

* エラー原因：ユーザーにはそのディレクトリを操作する権限がありません。

* 解決策：そのディレクトリにアクセスする必要がある場合は、システム管理者に連絡し、ユーザーにそのディレクトリを操作する権限を付与してもらってください。

## ERROR 5376 (HY000) : missing or invalid role name

* OceanBaseエラーコード：5376

* エラー原因：ロール名が間違っています。

* 解決策：ロール名を修正してください。

## ERROR 5377 (HY000) : role '%.\*s' not granted or does not exist

* OceanBaseエラーコード：5377

* エラー原因：ロールが付与されていない、または存在しません。

## ERROR 5378 (HY000) : DEFAULT ROLE '%.\*s' not granted to user

* OceanBaseエラーコード：5378

* エラーの原因：デフォルトのロールがユーザーに付与されていません。

* 解決策：ユーザーにデフォルトのロールを付与します。

## ERROR 5379 (HY000) : ROLE '%.\*s' not granted to '%.\*s'

* OceanBaseエラーコード：5379

* エラーの原因：ユーザーのロールを取り消す際、そのロールがユーザーに付与されていないことが判明しました。

## ERROR 5380 (HY000) : cannot GRANT to a role WITH GRANT OPTION

* OceanBaseエラーコード：5380

* エラーの原因：ユーザーがロールを再付与する際、再付与権限がないことが判明しました。

* 解決策：ユーザーに再付与権限を追加します。

## ERROR 5381 (HY000) : duplicate username in list

* OceanBase エラーコード：5381

* エラーの原因：`GRANT` ステートメント内に重複するユーザーが存在します。

* 解決策：余分なユーザーを削除します。

## ERROR 5382 (HY000) : cannot grant %.\*s to a role

* OceanBase エラーコード：5382

* エラーの原因：`REFERENCES` 権限または `INDEX` 権限をロールに付与できません。

* 解決策：ロールに付与された `REFERENCES` 権限または `INDEX` 権限を削除します。

## ERROR 5383 (HY000) : CASCADE CONSTRAINTS must be specified to perform this revoke

* OceanBase エラーコード：5383

* エラーの原因：`REVOKE` ステートメントでは、`CASCADE CONSTRAINTS` を指定する必要があります。

* 解決策：`CASCADE CONSTRAINTS` を指定します。

## ERROR 5384 (HY000) : you may not GRANT/REVOKE privileges to/from yourself

* OceanBase エラーコード：5384

* エラーの原因：自己自身に権限を付与または取り消すことはできません。

* 解決策：OceanBase データベースは、ユーザーが自己自身に権限を付与または取り消すことをサポートしていません。自身の権限を変更する必要がある場合は、システム管理者に連絡し、関連する操作を実行してもらってください。

## ERROR 5386 (HY000) : Unable to read data -* Table definition has changed

* OceanBase エラーコード：5386
* MySQL 互換エラーコード：1412
* エラーの原因：テーブルの定義が変更されており、読み取ったスナップショットポイントが古すぎて正しく読み取ったり解析したりできません。
* 解決策：テーブルの定義に変更があったかどうか確認し、列の追加や削除、データ型や長さの変更などがないか確認します。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードはV4.2.0バージョンから導入されました。</p>
</main>

## ERROR 5401 (HY000) : resultant data type of virtual column is not supported

* OceanBase エラーコード：5401

* エラーの原因：生成列はBLOB型を参照できません。

* 解決策：BLOB型データを削除します。

## ERROR 5403 (HY000) : ddl schema version not match

* OceanBase エラーコード：5403

* エラー原因：並行DDLの競合。

* 解決策：該当するDDLを再実行します。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードはV4.0.0バージョンから導入されました。</p>
</main>

## ERROR 5409 (HY000) : Duplicate object name exists, schema refreshing may be hung

* OceanBase エラーコード：5409
* エラー原因：DDLスレッドの実行中に同名のオブジェクトが存在した場合。

* 解決策：テクニカルサポートにお問い合わせください。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードはV4.1.0バージョンから導入されました。</p>
</main>

## ERROR 5410 (HY000) : Refresh schema failed continuously, ddl may be hung

* OceanBase エラーコード：5410
* エラー原因：DDLスレッドがハングし、新しいDDLリクエストを受信できない場合。
* 解決策：テクニカルサポートにお問い合わせください。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードはV4.1.0バージョンから導入されました。</p>
</main>

## ERROR 5434 (HY000) : retry sql due to spm

* OceanBase エラーコード：5434

* エラー原因：SPM機能によりSQLが自動的に再試行される必要がある場合。

* 解決策：テクニカルサポートにお問い合わせください。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードはV4.0.0バージョンから導入されました。</p>
</main>

## ERROR 5435 (HY000) : outline not reproducible

* OceanBase エラーコード：5435

* エラー原因：Outlineで指定された計画を再現できません。

* 解決策：テクニカルサポートにお問い合わせいただき、調査のご協力をお願いします。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードはV4.0.0バージョンから導入されました。</p>
</main>

## ERROR 5436 (42000) : TIMESTAMP_TO_SCN expected timestamp argument

* OceanBase エラーコード：5436

* エラー原因：パラメータタイプが期待と一致しません。TIMESTAMP_TO_SCN関数に渡すパラメータタイプはTimestamp型である必要があります。

* 解決策：関数のパラメータを修正してください。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードはV4.0.0バージョンから導入されました。</p>
</main>

## ERROR 5437 (42000) : SCN_TO_TIMESTAMP expected a integer number as nsec argument

* OceanBase エラーコード：5437

* エラー原因：パラメータタイプが期待と一致しません。SCN_TO_TIMESTAMP関数に渡すパラメータタイプは整数型である必要があります。

* 解決策：関数のパラメータを修正してください。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードはV4.0.0バージョンから導入されました。</p>
</main>

## ERROR 5500 (HY000) : Legitimacy check failed for generated columns.

* OceanBase エラーコード：5500
* エラー原因：生成列または関数インデックスの値に対する正当性チェックにおいて、既存の値が、生成列またはインデックスの定義に基づいて現在の環境で再計算された値と異なることが検出されました。これは、システム変数の影響を受ける計算結果、または本質的に動作が不確定な組み込み関数を使用して生成列やインデックスを定義したために発生する可能性があります。`_enable_defensive_check`構成パラメータが有効な場合、防御的にエラーが報告されます。
* 解決方法：生成列と関数インデックスの定義を確認し、システム変数の変更が計算結果の変化を引き起こしたことが確定した場合は、生成列/インデックスを削除して再構築してください。その他の場合や問題が解決しない場合は、テクニカルサポートにお問い合わせいただき、調査のご協力をお願いします。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードはV4.2.0バージョンから導入されました。</p>
</main>

## ERROR 5540 (HY000) : System variable '%.*s' is different from the old value solidified for '%.*s'(old value:%.*s).

* OceanBase エラーコード：5540

* エラーの原因：現在のセッションで設定されているシステム変数の値が、関数インデックスまたは生成列を作成した際に固定された値と異なります。計算時には、関数インデックスや生成列は、作成時に固定された値を使用し、セッション内の現在の変数値ではありません。

* 解決策：現在のセッションのシステム変数の値に基づいて計算する必要がある場合は、生成列または関数インデックスを削除して再作成してください。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードはV4.2.2バージョンから導入されました。</p>
</main>

## ERROR 5558 (42000) : %s \\'%.\*s.%.\*s\\' already exists

* OceanBase エラーコード：5558

* エラーの原因：作成しようとしているPACKAGEが既に存在します。

* 解決策：`CREATE [OR REPLACE]` ステートメントを使用して作成できます。作成を実行すると、以前に存在した同名のPACKAGEは上書きされます。

## ERROR 5559 (42000) : %s \\'%.\*s.%.\*s\\' does not exist

* OceanBase エラーコード：5559

* エラーの原因：アクセスしようとしているPACKAGEが存在しません。

* 解決策：ロジックが正しいか確認してください。

関連する問題事例：

* [シノニムを含むパッケージのコンパイルでエラー -5559](https://www.oceanbase.com/knowledge-base/oceanbase-database-1000000000209967)

## ERROR 5561 (HY000) : Invalid use of window function

* OceanBase エラーコード：5561

* エラーの原因：無効なウィンドウ関数の使用。例えば、不適切なパラメータ設定などです。

* 解決策：ウィンドウを修正して正しいウィンドウ関数にします。

## ERROR 5562 (HY000) : Duplicate constraint name '%.\*s'

* OceanBase エラーコード：5562

* エラーの原因：制約名が既に存在します。

* 解決策：制約名を変更してください。

## ERROR 5563 (HY000) : Constraint not found

* OceanBase エラーコード：5563

* エラーの原因：操作対象の制約が存在しません。

* 解決策：制約名が正しいか確認してください。

## ERROR 5564 (HY000) : Duplicate alter index operations on column \\'%.\*s\\'

* OceanBase エラーコード：5564

* エラーの原因：同一のSQLステートメント内で、同一の列に対して `ALTER INDEX` 操作を繰り返し実行しています。

* 解決策：SQLステートメントを修正してください。

## ERROR 5572 (HY000) : a partition may be split into exactly two new partitions

* OceanBase エラーコード：5572

* エラーの原因：`SPLIT` パーティションで指定されたパーティション数が2ではありません。

* 解決策：分割するパーティションを2つに指定してください。

## ERROR 5573 (HY000) : this physical attribute may not be specified for a table partition

* OceanBase エラーコード：5573

* エラーの原因：パーティション分割時に、分割ポイントが最大値をサポートしていません。

* 解決策：分割ポイントを修正してください。

## ERROR 5574 (HY000) : missing VALUES keyword

* OceanBase エラーコード：5574

* エラーの原因：構文エラーです。

* 解決策：`VALUES` キーワードを追加してください。

## ERROR 5575 (HY000) : missing AT or VALUES keyword

* OceanBase エラーコード：5575

* エラーの原因：構文エラーです。

* 解決策：`AT` または `VALUES` キーワードを追加してください。

## ERROR 5578 (HY000) : Plan cache lock conflict

* OceanBase エラーコード：5578

* エラーの原因：プランキャッシュのロックが失敗しました。

* 解決策：再試行してください。

## ERROR 5597 (HY000) : last resulting partition cannot contain bounds

* OceanBase エラーコード：5597

* エラーの原因：パーティションの分割前後で、パーティション範囲が一致しません。

* 解決策：SQLステートメントを修正してください。

## ERROR 5598 (HY000) : cannot add partition when DEFAULT partition exists

* OceanBase エラーコード：5598

* エラーの原因：LISTパーティションに既にデフォルトパーティションが存在するため、新しいパーティションを追加できません。

* 解決策：新しいパーティションを追加せず、パーティションの分割によって新しいパーティションを作成してください。

## ERROR 5599 (HY000) : cannot split partition into one partition, use rename instead

* OceanBase エラーコード：5599

* エラーの原因：分割先のパーティションが1つしかない場合、分割は許可されません。

## ERROR 5600 (HY000) : can not create user %s in sys tenant, name %.\*s

* OceanBase エラーコード：5600

* エラーの原因：sysテナント内にユーザーを作成できません。

## ERROR 5601 (HY000) : Percentage should between 1 and 99

* OceanBase エラーコード：5601

* エラーの原因：解析エラーです。

* 解決策：SQLを修正してください。

## ERROR 5603 (0A000) : Attempt to create, alter or drop an index on temporary table already in use

* OceanBase エラーコード：5603

* エラーの原因：使用中の一時テーブルに対してインデックスの作成、変更、または削除を試みました。

* 解決策：一時テーブルを使用しているすべてのセッション接続を切断し、その後操作を実行します。

## ERROR 5604 (HY000) : invalid NLS parameter string used in SQL function

* OceanBaseエラーコード：5604

* エラー原因：日付時刻型パラメータが誤っています。

* 解決策：サポートチームにお問い合わせいただき、調査をお手伝いいたします。

## ERROR 5605 (HY000) : datetime/interval precision is out of range

* OceanBase エラーコード：5605

* エラー原因：日付時刻型の精度が範囲外です。

* 解決策：サポートチームにお問い合わせいただき、調査をお手伝いいたします。

## ERROR 5606 (42000) : Invalid number format model

* OceanBase エラーコード：5606

* エラー原因：数値形式が誤っています。

* 解決策：サポートチームにお問い合わせいただき、調査をお手伝いいたします。

## ERROR 5607 (HY000) : SQL command not properly ended

* OceanBase エラーコード：5607

* エラー原因：コマンドが正しく終了していません。

* 解決策：コマンドの合法性を確認してください。

## ERROR 5608 (42000) : invalid number format model

* OceanBase エラーコード：5608

* エラー原因：数値形式が誤っています。

* 解決策：サポートチームにお問い合わせいただき、調査をお手伝いいたします。

## ERROR 5613 (HY000) : too many values

* OceanBase エラーコード：5613

* エラー原因：ステートメントで指定された値が多すぎます。これは `INSERT` ステートメントでよく見られます。

* 解決策：値の数を調整するか、サポートチームにお問い合わせいただき、調査をお手伝いいたします。

## ERROR 5614 (HY000) : not enough values

* OceanBaseエラーコード：5614

* エラー原因：ステートメントで指定された値が不足しています。これは`INSERT`ステートメントでよく発生します。

* 解決策：値の数を調整するか、テクニカルサポートに連絡し、問題の調査を依頼します。

## ERROR 5615 (HY000) : single-row subquery returns more than one row

* OceanBaseエラーコード：5615

* エラー原因：スカラーサブクエリの式に誤りがあり、1行を返すことが期待されているにもかかわらず、実際には複数行が返されます。

* 解決策：スカラーサブクエリの式を確認し、実行ごとに結果セットとして1行のみが返されるようにします。

## ERROR 5616 (HY000) : UPDATE ... SET expression must be a subquery

* OceanBaseエラーコード：5616

* エラー原因：rowset assignmentのシナリオでは、`UPDATE`ステートメントの`SET`句は`subquery`として指定する必要があります。

* 解決策：`UPDATE`の`SET`句の構文を調整するか、テクニカルサポートに連絡し、問題の調査を依頼します。

## ERROR 5617 (HY000) : inappropriate INTO

* OceanBaseエラーコード：5617

* エラー原因：`INTO`句の使用方法が間違っています。

* 解決策：テクニカルサポートに連絡し、問題の調査を依頼します。

## ERROR 5619 (HY000) : Column part of using clause can not have qualifier

* OceanBaseエラーコード：5619

* エラー原因：`USING`句に現れる列名に`database name`または`table name`を含めることはできません。

* 解決策：`USING`句内の列名から`database name`または`table name`を削除します。

## ERROR 5623 (HY000) : outer join operator (+) not allowed in operand of OR or IN

* OceanBaseエラーコード：5623

* エラー原因：`OR`句内に外部結合が現れています。

* 解決策：SQLステートメントを修正します。

## ERROR 5624 (HY000) : a column may not be outer-joined to a subquery

* OceanBase エラーコード：5624

* エラーの原因：`+` 外部結合では、列をサブクエリに外部結合できません。

* 解決策：SQLステートメントを修正します。

## ERROR 5625 (HY000) : old style outer join (+) cannot be used with ANSI joins

* OceanBase エラーコード：5625

* エラーの原因：クエリブロックでANSIスタイルの結合を使用する場合、外部結合（+）を指定する古い表記法は使用できません。

* 解決策：SQLステートメントを修正します。

## ERROR 5626 (HY000) : outer join operator (+) is not allowed here

* OceanBase エラーコード：5626

* エラーの原因：`select_list`、`CONNECT BY` 句、`START WITH` 句、または `ORDER BY` 句で (+) を参照しようとしました。

* 解決策：SQLステートメントを修正します。

## ERROR 5627 (HY000) : Schema try again

* OceanBase エラーコード：5627

* エラーの原因：ローカルSchemaのバージョンが古いです。

* 解決策：再試行するか、サポート技術者に連絡して調査を依頼します。

## ERROR 5628 (HY000) : zero-length columns are not allowed

* OceanBase エラーコード：5628

* エラーの原因：列データの長さが0です。

* 解決策：サポート技術者に連絡して調査を依頼します。

## ERROR 5629 (HY000) : year conflicts with Julian date

* OceanBase エラーコード：5629

* エラーの原因：年とユリウス日が競合しています。例：`SELECT TO_DATE('2021-03-23 2459063', 'YYYY MM DD J') FROM DUAL;`。

## ERROR 5630 (HY000) : day of year conflicts with Julian date

* OceanBase エラーコード：5630

* エラーの原因：年間の日付（1 - 366）とユリウス日が競合しています。

## ERROR 5631 (HY000) : month conflicts with Julian date

* OceanBase エラーコード：5631

* エラー原因：月とユリウス日が競合します。例：`SELECT TO_DATE('11 2459063','MM J') FROM DUAL;`。

## ERROR 5632 (HY000) : day of month conflicts with Julian date

* OceanBase エラーコード：5632

* エラー原因：月の日付（1～31）とユリウス日が競合します。例：`SELECT TO_DATE('11 2459063','DD J') FROM DUAL;`。

## ERROR 5633 (HY000) : day of week conflicts with Julian date

* OceanBase エラーコード：5633

* エラー原因：曜日の日付（1～7）とユリウス日が競合します。例：`SELECT TO_DATE('3 2459063','D J') FROM DUAL;`。

## ERROR 5634 (HY000) : hour conflicts with seconds in day

* OceanBase エラーコード：5634

* エラー原因：時間と日の秒が競合します。

## ERROR 5635 (HY000) : minutes of hour conflicts with seconds in day

* OceanBase エラーコード：5635

* エラー原因：時間の分と日の秒が競合します。

## ERROR 5636 (HY000) : seconds of minute conflicts with seconds in day

* OceanBase エラーコード：5636

* エラー原因：分の秒と日の秒が競合します。

## ERROR 5637 (HY000) : date not valid for month specified

* OceanBase エラーコード：5637

* エラー原因：指定された月に対して、日付が有効ではありません。

## ERROR 5638 (HY000) : input value not long enough for date format

* OceanBase エラーコード：5638

* エラー原因：入力値が日付形式に使用するには長さが足りません。

## ERROR 5639 (HY000) : (full) year must be between -4713 and +9999, and not be 0

* OceanBase エラーコード：5639

* エラーの原因：有効な日付の年は1から9999の間でなければなりません。

## ERROR 5640 (HY000) : quarter must be between 1 and 4

* OceanBase エラーコード：5640

* エラーの原因：四半期の値は1から4の間でなければなりません。

## ERROR 5641 (HY000) : not a valid month

* OceanBase エラーコード：5641

* エラー原因：有効な月ではありません。

関連する問題事例：

* [テーブル更新エラー、エラーコード5641/OBE-01843](https://www.oceanbase.com/knowledge-base/oceanbase-database-20000000197?back=kb)

## ERROR 5642 (HY000) : not a valid day of the week

* OceanBase エラーコード：5642

* エラー原因：有効な日付ではありません。

## ERROR 5643 (HY000) : day of year must be between 1 and 365 (366 for leap year)

* OceanBase エラーコード：5643

* エラー原因：年の日付は1から365の間でなければなりません（閏年は366）。

## ERROR 5644 (HY000) : hour must be between 1 and 12

* OceanBase エラーコード：5644

* エラー原因：時刻は1から12の間でなければなりません。

## ERROR 5645 (HY000) : hour must be between 0 and 23

* OceanBase エラーコード：5645

* エラー原因：時刻は0から23の間でなければなりません。

## ERROR 5646 (HY000) : minutes must be between 0 and 59

* OceanBase エラーコード：5646

* エラー原因：分は0から59の間でなければなりません。

## ERROR 5647 (HY000) : seconds must be between 0 and 59

* OceanBase エラーコード：5647

* エラー原因：秒数は0から59の間でなければなりません。

## ERROR 5648 (HY000) : seconds in day must be between 0 and 86399

* OceanBase エラーコード：5648

* エラーの原因：1日の秒数は0から86399の間でなければなりません。

## ERROR 5649 (HY000) : julian date must be between 1 and 5373484

* OceanBaseエラーコード：5649

* エラーの原因：無効なユリウス日が入力されました。

* 解決策：入力されたユリウス日は1から5373484の間である必要があります。

## ERROR 5650 (HY000) : AM/A.M. or PM/P.M. required

* OceanBaseエラーコード：5650

* エラーの原因：日付時刻形式に`AM`、`A.M.`、`PM`または`P.M.`を指定する必要があります。

## ERROR 5651 (HY000) : BC/B.C. or AD/A.D. required

* OceanBaseエラーコード：5651

* エラーの原因：日付時刻形式に`BC`、`B.C.`、`AD`または`A.D.`を指定する必要があります。

## ERROR 5652 (HY000) : format code appears twice

* OceanBaseエラーコード：5652

* エラーの原因：入力されたフォーマットモデルが誤っています。

* 解決策：テクニカルサポートにお問い合わせいただき、調査をお手伝いいたします。

## ERROR 5653 (HY000) : day of week may only be specified once

* OceanBaseエラーコード：5653

* エラーの原因：曜日は1回だけ指定できます。

## ERROR 5654 (HY000) : signed year precludes use of BC/AD

* OceanBaseエラーコード：5654

* エラーの原因：符号付き日付では`BC`と`AD`を同時に使用できません。

## ERROR 5655 (HY000) : Julian date precludes use of day of year

* OceanBaseエラーコード：5655

* エラーの原因：ユリウス日と年の日付を同時に使用できません。

## ERROR 5656 (HY000) : year may only be specified once

* OceanBase エラーコード：5656

* エラーの原因：年は1回のみ指定できます。

## ERROR 5657 (HY000) : hour may only be specified once

* OceanBase エラーコード：5657

* エラーの原因：時は1回のみ指定できます。

## ERROR 5658 (HY000) : AM/PM conflicts with use of A.M./P.M

* OceanBase エラーコード：5658

* エラーの原因：`AM`または`PM`が対応する`A.M.`または`P.M.`と競合しています。

## ERROR 5659 (HY000) : BC/AD conflicts with use of B.C./A.D

* OceanBase エラーコード：5659

* エラーの原因：`BC`または`AD`が対応する`B.C.`または`A.D.`と競合しています。

## ERROR 5660 (HY000) : month may only be specified once

* OceanBase エラーコード：5660

* エラーの原因：月は1回のみ指定できます。

## ERROR 5661 (HY000) : day of week may only be specified once

* OceanBase エラーコード：5661

* エラーの原因：曜日は1回のみ指定できます。

## ERROR 5662 (HY000) : format code cannot appear in date input format

* OceanBaseエラーコード：5662

* エラーの原因：format modelが誤っています。

* 解決策：number format modelが構文要件に適合しているか確認します。

## ERROR 5663 (HY000) : a non-numeric character was found where a numeric was expected

* OceanBase エラーコード：5663

* エラーの原因：無効な数字文字。

* 解決策：SQLステートメント内の数字に無効な文字がないか確認します。

## ERROR 5664 (HY000) : 'HH24' precludes use of meridian indicator

* OceanBase エラーコード：5664

* エラー原因：format model H24 に `AM`、`PM` 関連の文字が含まれています。

* 解決策：テクニカルサポートにお問い合わせください。

## ERROR 5665 (HY000) : missing or illegal character following the escape character

* OceanBase エラーコード：5665

* エラー原因：`LIKE` クエリステートメントの `ESCAPE` フィールドの後に続く文字が無効です。

* 解決策：SQL の `LIKE` ステートメントにおいて、`ESCAPE` フィールドの後に続く文字が要件を満たしているか確認してください。

## ERROR 5666 (HY000) : escape character must be character string of length 1

* OceanBase エラーコード：5666

* エラー原因：`LIKE` 計算時に、`ESCAPE` フィールドの後に続く文字が単一文字ではありません。

* 解決策：`ESCAPE` フィールドの後に続く文字を単一文字に指定してください。

## ERROR 5667 (HY000) : day of month must be between 1 and last day of month

* OceanBase エラーコード：5667

* エラー原因：月の日付は1からその月の最終日の間でなければなりません。

## ERROR 5668 (HY000) : not a SELECTed expression

* OceanBase エラーコード：5668

* エラー原因：`GROUP BY` 句で式を指定しましたが、その式は `SELECT` 句に現れていません。

* 解決策：SQLの構文を修正してください。

## ERROR 5670 (HY000) : (full) year must be between -4713 and +9999, and not be 0

* OceanBase エラーコード：5670

* エラー原因：有効な日付の年は1から9999の間でなければなりません。

## ERROR 5671 (HY000) : such unique or primary key already exists in the table

* OceanBase エラーコード：5671

* エラー原因：一意インデックスが既に存在します。

* 解決策：一意インデックスの重複作成は許可されません。

## ERROR 5672 (HY000) : such column list already indexed

* OceanBase エラーコード：5672

* エラー原因：インデックスを重複して作成しました。

* 解決策：インデックスの重複作成は許可されません。

## ERROR 5674 (HY000) : argument '%ld' is out of range

* OceanBase エラーコード：5674

* エラー原因：指定したパラメータが範囲外です。

* 解決策：パラメータ値を調整してください。**

## ERROR 5675 (HY000) : ORDER BY item must be the number of a SELECT-list expression

* OceanBase エラーコード：5675

* エラー原因：`ORDER BY` 項目が `SELECT` 項目の正しい番号ではありません。

* 解決策：SQLの `ORDER BY` 句を調整してください。

## ERROR 5676 (HY000) : the interval is invalid

* OceanBase エラーコード：5676

* エラー原因：パラメータが無効です。

* 解決策：テクニカルサポートにお問い合わせいただき、調査をお手伝いいたします。

関連する問題事例：

* [JSON SQLの実行で the interval is invalid エラーが発生しました](https://www.oceanbase.com/knowledge-base/oceanbase-database-20000045546)

## ERROR 5677 (HY000) : PL/SQL: numeric or value error: %.\*s

* OceanBase エラーコード：5677

* エラー原因：PL実行中に無効な数値や他のデータ型に遭遇しました。

* 解決策：ロジックが正しいか確認してください。

## ERROR 5678 (HY000) : name already used by an existing constraint

* OceanBase エラーコード：5678

* エラーの原因：制約名が既に存在します。

* 解決策：制約名を変更してください。

## ERROR 5679 (HY000) : table must have at least one column that is not invisible

* OceanBase エラーコード：5679
* MySQL 互換エラーコード：4028
* エラーの原因：関数インデックスはテーブルに隠れた列を追加します。テーブルに隠れた列が存在する場合、そのテーブルのすべての可視列(visible column)を削除するとこのエラーが返されます。
* 解決策：データテーブルの列定義とメタデータを確認し、削除操作後に少なくとも1つの列が可視列であることを確認します。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードはV4.2.0バージョンから導入されました。</p>
</main>

## ERROR 5680 (HY000) : Invisible column is not supported on this type of table

* OceanBase エラーコード：5680

* エラーの原因：Oracleの一時テーブルでは、列を `INVISIBLE` として定義することはサポートされていません。

## ERROR 5681 (HY000) : Column visibility modifications cannot be combined with any other modified column DDL option

* OceanBase エラーコード：5681

* エラーの原因：DDLステートメントでは、列の可視性と他の列属性を同時に調整することはサポートされていません。

* 解決策：DDLステートメントを分割し、列の可視性を個別に調整します。

## ERROR 5682 (HY000) : The visibility of a column from a table owned by a SYS user cannot be changed

* OceanBase エラーコード：5682

* エラーの原因：SYSユーザー配下のテーブルでは、列の可視性を調整することは許可されていません。

## ERROR 5683 (HY000) : too many arguments for function

* OceanBase エラーコード：5683

* エラーの原因：関数の引数が多すぎます。

* 解決策：引数の数を調整します。

## ERROR 5684 (HY000) : PX sql need retry

* OceanBase エラーコード：5684

* エラーの原因：内部エラーコードです。PXにSQLの再試行を指示します。

* 解決策：通常、このエラーは発生しません。このエラーが発生した場合は、システムのリーダー切り替えやネットワーク状態が正常であるか確認してください。

## ERROR 5685 (HY000) : Tenant '(string)' has been dropped

* OceanBase エラーコード：5685

* エラーの原因：テナントが削除されました。

* 解決策：内部エラーコードです。サポートチームにお問い合わせいただき、調査をお手伝いください。

## ERROR 5686 (HY000) : invalid extract field for extract source

* OceanBase エラーコード：5686

* エラーの原因：`EXTRACT` 関数が指定された値域を解釈できません。

* 解決策：指定された値域が有効かどうか確認してください。

## ERROR 5687 (42000) : %s \\'%.\*s.%.\*s\\' compile error

* OceanBase エラーコード：5687

* エラーの原因：PACKAGEまたはUDTのコンパイルに失敗しました。

* 解決策：エラーメッセージの指示に従って、誤りを修正してください。

## ERROR 5690 (HY000) : subquery not allowed here

* OceanBase エラーコード：5690

* エラーの原因：この場所ではサブクエリを使用できません。

* 解決策：サポートチームにお問い合わせいただき、調査をお手伝いください。

## ERROR 5691 (HY000) : date or system variable wrongly specified in CHECK constraint

* OceanBase エラーコード：5691

* エラーの原因：`CHECK` 制約では `DATE` 関数やシステム変数の使用は許可されていません。

## ERROR 5692 (HY000) : Cannot drop constraint - nonexistent constraint

* OceanBase エラーコード：5692

* エラーの原因：制約が存在しません。

## ERROR 5693 (HY000) : check constraint violated

* OceanBase エラーコード：5693

* エラーの原因：`CHECK` 制約を満たしていません。

* 解決策：DML操作に関連する列データが既存の `CHECK` 制約を満たしているかどうか確認してください。

## ERROR 5694 (HY000) : group function is not allowed here

* OceanBase エラーコード：5694

* エラー原因：不正な場所に集約関数が記述されています。

* 解決策：SQL内の集約関数を調整します。

## ERROR 5695 (HY000) : policy string not found

* OceanBase エラーコード：5695

* エラー原因：セキュリティポリシーが見つかりません。

* 解決策：セキュリティポリシーを作成します。

## ERROR 5696 (HY000) : invalid label string

* OceanBase エラーコード：5696

* エラー原因：不正なラベル文字列です。

* 解決策：有効なラベル文字列を指定します。

## ERROR 5697 (HY000) : undefined compartment string for policy string

* OceanBase エラーコード：5697

* エラー原因：不正なCompartment文字列です。

* 解決策：有効なCompartment文字列を指定します。

## ERROR 5698 (HY000) : undefined level string for policy string

* OceanBase エラーコード：5698

* エラー原因：不正なLevel文字列です。

* 解決策：有効なLevel文字列を指定します。

## ERROR 5699 (HY000) : undefined group string for policy string

* OceanBase エラーコード：5699

* エラー原因：不正なGroup文字列です。

* 解決策：有効なGroup文字列を指定します。

## ERROR 5700 (HY000) : LBAC error: %s

* OceanBase エラーコード：5700

* エラー原因：一般的なタグセキュリティエラーです。詳細は具体的なエラーメッセージを参照してください。

* 解決策：エラーメッセージに基づいて、対応する操作を修正します。

## ERROR 5701 (HY000) : policy role already exists for policy string

* OceanBase エラーコード：5701

* エラー原因：該当するポリシー名が既に存在します。

* 解決策：ポリシー名を確認し、修正するか、既存のポリシーを削除します。

## ERROR 5702 (HY000) : NULL or invalid user label: %s

* OceanBase エラーコード：5702

* エラー原因：無効なユーザーラベルです。

* 解決策：正しいユーザーラベルを指定してください。

## ERROR 5703 (HY000) : Add index failed

* OceanBase エラーコード：5703

* エラー原因：インデックスの追加に失敗しました。

* 解決策：インデックスが有効かどうか確認してください。まず削除してから、再度作成することもできます。

関連する問題事例：

* [インデックス作成失敗、エラーコード5703](https://www.oceanbase.com/knowledge-base/oceanbase-database-20000001012?back=kb)
* [Work Areaメモリ不足によりインデックス作成失敗](https://www.oceanbase.com/knowledge-base/oceanbase-database-1000000000225632)

## ERROR 5704 (HY000) : profile %.\*s does not exist

* OceanBase エラーコード：5704

* エラー原因：Profile名が間違っています。

* 解決策：Profile名を確認してください。

## ERROR 5705 (HY000) : invalid resource limit %s

* OceanBase エラーコード：5705

* エラー原因：Profile関連のDDLで、指定された`FAILED_LOGIN_ATTEMPTS`や`PASSWORD_LOCK_TIME`が合法的なINT型ではない、または値が`0`である場合。

* 解決策：Profile内の関連DDLのパラメータ値を調整してください。

## ERROR 5706 (HY000) : profile %.\*s already exists

* OceanBase エラーコード：5706

* エラーの原因：Profileは既に存在します。

* 解決策：Profile名を変更してください。**

## ERROR 5707 (HY000) : profile %.\*s has users assigned, cannot drop without CASCADE

* OceanBase エラーコード：5707

* エラーの原因：Profileがユーザーに割り当てられているため、CASCADEなしでは削除できません。

* 解決策：`CASCADE` を追加します。

## ERROR 5708 (HY000) : the leading precision of the interval is too small

* OceanBase エラーコード：5708

* エラーの原因：精度が無効です。

* 解決策：テクニカルサポートにお問い合わせいただき、調査をお手伝いいたします。

## ERROR 5709 (HY000) : time zone hour must be between -12 and 14

* OceanBase エラーコード：5709

* エラーの原因：`time_zone` システム変数を設定する際、時刻が有効範囲を超えています。

* 解決策：MySQLモードの範囲は \[-12, 14\] です。

## ERROR 5710 (HY000) : time zone minute must be between -59 and 59

* OceanBase エラーコード：5710

* エラーの原因：`time_zone` システム変数を設定する際、分が有効範囲を超えています。

* 解決策：有効範囲は \[-59, 59\] です。

## ERROR 5711 (HY000) : not a valid time zone

* OceanBase エラーコード：5711

* エラーの原因： `TZD`、`TZH`、`TZM` および `TZR` は `DATE` 形式に含めることはできません。

## ERROR 5712 (HY000) : date format is too long for internal buffer

* OceanBase エラーコード：5712

* エラー原因：`DATE`形式の長さが長すぎます。

* 解決策：`DATE`形式の長さを短縮します。

## ERROR 5713 (HY000) : cannot validate (%.\*s.%.\*s) - check constraint violated

* OceanBaseエラーコード：5713

* エラー原因：入力が制約に違反しています。

* 解決策：テクニカルサポートにお問い合わせいただき、調査をご依頼ください。

## ERROR 5714 (HY000) : data manipulation operation not legal on this view

* OceanBaseエラーコード：5714

* エラーの原因：`INSERT`、`UPDATE`、`DELETE` ステートメントを使用して、更新できないビュー（生成列、式列、複数のテーブルからデータを取得するビューなど）を変更しようとした場合。

* 解決策：ベーステーブルを直接変更するか、ビューを再定義します。

## ERROR 5715 (HY000) : virtual column not allowed here

* OceanBaseエラーコード：5715

* エラーの原因：`INSERT`、`DELETE`、`UPDATE` ステートメントを使用して、ビュー内の式を変更しようとした場合。

* 解決策：ベーステーブルを直接変更します。

## ERROR 5716 (HY000) : cannot modify more than one base table through a join view

* OceanBaseエラーコード：5716

* エラーの原因：列が複数のテーブルに属しているため、挿入または更新できません。

* 解決策：ステートメントを複数のステートメントに分割します。

## ERROR 5721 (HY000) : cannot delete from view without exactly one key-preserved table

* OceanBaseエラーコード：5721

* エラーの原因：削除対象のテーブルにkey-preserved tableが存在しない、または複数のkey-preserved tableが存在する、あるいはそのkey-preserved tableが読み取り専用ビューから取得されたものである場合。

* 解決策：ビューを再定義するか、ベーステーブルで削除操作を実行します。

## ERROR 5722 (HY000) : cannot modify a column which maps to a non key-preserved table

* OceanBaseエラーコード：5722

* エラーの原因：JOINビュー内のnon-key-preserved tableに対して挿入または更新を試みた場合。

* 解決策：ベーステーブルを直接変更します。

## ERROR 5723 (HY000) : cannot perform a DML operation on a read-only view

* OceanBaseエラーコード：5723

* エラーの原因：`INSERT`、`DELETE`、`UPDATE` ステートメントを使用して、読み取り専用ビューとして定義されたビューを変更しようとした場合。

* 解決策：ベーステーブルを直接変更するか、ビューを更新可能なビューに変更します。

## ERROR 5724 (HY000) : invalid INITRANS option value

* OceanBase エラーコード：5724

* エラーの原因：`INITRANS`パラメータ値が無効です。

* 解決策：パラメータ値を調整します。

## ERROR 5725 (HY000) : invalid MAXTRANS option value

* OceanBase エラーコード：5725

* エラーの原因：`MAXTRANS`パラメータ値が無効です。

* 解決策：パラメータ値を調整します。

## ERROR 5726 (HY000) : invalid value for PCTFREE or PCTUSED

* OceanBase エラーコード：5726

* エラーの原因：`PCTFREE`または`PCTUSED`パラメータ値が無効です。

* 解決策：パラメータ値を調整します。

## ERROR 5728 (HY000) : illegal argument for function

* OceanBase エラーコード：5728

* エラーの原因：パラメータが無効です。

* 解決策：パラメータ値を調整します。

## ERROR 5729 (HY000) : this operator cannot be used with lists

* OceanBase エラーコード：5729

* エラーの原因：ベクトル比較操作はサポートされていません。

* 解決策：ベクトル比較操作を削除します。

## ERROR 5730 (HY000) : SAMPLE percentage must be in the range \[0.000001,100)

* OceanBase エラーコード：5730

* エラーの原因：構文エラー。

* 解決策：SQLステートメントを修正します。

## ERROR 5731 (HY000) : specifying owner's name of the table is not allowed

* OceanBase エラーコード：5731

* エラー原因：名前を変更する対象のテーブルオブジェクトには、ユーザー名を含めることはできません。

## ERROR 5732 (HY000) : stmt trigger with when clause

* OceanBase エラーコード：5732

* エラー原因：ステートメントレベルトリガーでは `WHEN` 条件を指定できません。

* 解決策：ステートメントレベルトリガーの `WHEN` 条件を削除します。

## ERROR 5732 (HY000) : not enough arguments for function

* OceanBase エラーコード：5732

* エラー原因：関数の引数が不足しています。

* 解決策：引数を補完します。

## ERROR 5733 (HY000) : trigger '%.\*s' does not exist

* OceanBase エラーコード：5733

* エラー原因：指定されたトリガーが存在しません。

* 解決策：指定したトリガーが存在するか確認します。

## ERROR 5734 (HY000) : trigger '%.\*s' already exist

* OceanBase エラーコード：5734

* エラー原因：指定されたトリガーが既に存在します。

* 解決策：指定したトリガーが存在するか確認します。

## ERROR 5735 (HY000) : trigger '%.\*s' already exists on another table, cannot replace it

* OceanBase エラーコード：5735

* エラー原因：他のテーブルに同名のトリガーが存在するため、現在のトリガーを作成できません。

* 解決策：現在のトリガーを名前変更してから作成します。

## ERROR 5736 (HY000) : error signaled in parallel query server

* OceanBase エラーコード：5736

* エラー原因：内部エラーコード。PXが実行を終了する必要がある場合、このエラーコードをすべての実行ノードにブロードキャストして、現在の実行を終了します。

* 解決策：内部エラーコードです。サポートチームにお問い合わせいただき、調査のご協力をお願いいたします。

## ERROR 5763 (HY000) : Specified pseudo column or operator not allowed here

* OceanBase エラーコード：5763

* エラー原因：この場所では `LEVEL`、`PRIOR`、`ROWNUM`、`CONNECT_BY_ROOT`、`CONNECT_BY_ISLEAF` または `CONNECT_BY_ISCYCLE` キーワードの使用は許可されていません。

* 解決策：`LEVEL`、`PRIOR`、`ROWNUM`、`CONNECT_BY_ROOT`、`CONNECT_BY_ISLEAF` または `CONNECT_BY_ISCYCLE` を削除します。

## ERROR 5764 (HY000) : CONNECT BY loop in user data

* OceanBase エラーコード：5764

* エラー原因：ユーザーデータに `CONNECT BY` ループが存在します。

## ERROR 5765 (HY000) : cannot have join with CONNECT BY

* OceanBase エラーコード：5765

* エラー原因：`CONNECT BY` と結合することはできません。

## ERROR 5766 (HY000) : CONNECT BY clause required in this query block

* OceanBase エラーコード：5766

* エラー原因：このクエリブロックでは `CONNECT BY` 句が必要です。

## ERROR 5768 (HY000) : SYS_CONNECT_BY_PATH function is not allowed here

* OceanBase エラーコード：5768

* エラー原因：`SYS_CONNECT_BY_PATH` 関数が `SELECT` リストや `ORDER BY` 句以外の場所で呼び出されています。

* 解決方法：`SELECT` リストや `ORDER BY` 句以外の場所での `SYS_CONNECT_BY_PATH` 関数の呼び出しを削除します。

## ERROR 5769 (HY000) : illegal parameter in SYS_CONNECT_BY_PATH function

* OceanBase エラーコード：5769

* エラー原因：`SYS_CONNECT_BY_PATH` 関数に無効なパラメータが含まれています。

## ERROR 5770 (HY000) : A column value contained the string that the SYS_CONNECT_BY_PATH function was to use to separate column values

* OceanBase エラーコード：5770

* エラー原因：列値に `SYS_CONNECT_BY_PATH` 関数が列値を区切るために使用する文字列が含まれています。

* 解決方法：`SYS_CONNECT_BY_PATH` 関数に他の区切り文字を指定します。その区切り文字がどの列値にも含まれていないことを確認する必要があります。

## ERROR 5771 (HY000) : CONNECT BY ROOT operator is not supported in the START WITH or in the CONNECT BY condition

* OceanBase エラーコード：5771

* エラーの原因：`START WITH` または `CONNECT BY` 条件で `CONNECT BY ROOT` 演算子を使用しようとしました。

* 解決策：`START WITH` または `CONNECT BY` 条件から `CONNECT BY ROOT` 演算子を削除します。

## ERROR 5772 (HY000) : ORDER SIBLINGS BY clause not allowed here

* OceanBase エラーコード：5772

* エラーの原因：指定された `ORDER SIBLINGS BY` 句を含むクエリに `CONNECT BY` 句が含まれていません。

* 解決策：`ORDER SIBLINGS BY` 句を削除するか、クエリに `CONNECT BY` 句を追加します。

## ERROR 5773 (HY000) : NOCYCLE keyword is required with CONNECT_BY_ISCYCLE pseudo column

* OceanBase エラーコード：5773

* エラーの原因：クエリで `CONNECT_BY_ISCYCLE` を指定しましたが、そのクエリに `NOCYCLE` キーワードが含まれていません。

* 解決策：`CONNECT_BY_ISCYCLE` を削除するか、`NOCYCLE` キーワードを追加します。

## ERROR 5774 (HY000) : not enough arguments for function

* OceanBase エラーコード：5774

* エラーの原因：関数の引数が不足しています。

## ERROR 5777 (HY000) : Prepare statement checksum error

* OceanBase エラーコード：5777

* エラーの原因：サーバーセッションが回復不能な状態にあります。

* 解決策：OceanBase データベースに再度ログインすると、新しいサーバーセッションが自動的に作成されます。詳細については、セッショントレースファイルを確認してください。

## ERROR 5778 (HY000) : cannot enable constraint (%.\*s) - no such constraint

* OceanBase エラーコード：5778

* エラーの原因：このテーブルに指定された制約が存在しません。

## ERROR 5779 (HY000) : cannot disable constraint (%.\*s) - no such constraint

* OceanBase エラーコード：5779

* エラーの原因：このテーブルに指定された制約が存在しません。

## ERROR 5780 (HY000) : PX DOP downgrade from %ld to %ld

* OceanBase エラーコード：5780

* エラーの原因：正常終了。

## ERROR 5781 (HY000) : parallel_max_servers downgrade due to insufficent cpu resource from %ld to %ld. deprecated

* OceanBase エラーコード：5781

* エラーの原因：正常終了。

  <main id="notice" type='explain'>
    <h4>説明</h4>
    <p>現在のバージョンでは、このエラーコードは使用されなくなりました。</p>
  </main>

## ERROR 5785 (HY000) : cannot validate (%.\*s.%.\*s) - parent keys not found

* OceanBase エラーコード：5785

* エラーの原因：このテーブルに孤立した子レコードがあるため、テーブル変更操作の検証制約に失敗しました。

## ERROR 5786 (HY000) : Column check constraint cannot reference other columns

* OceanBase エラーコード：5786

* エラーの原因：別の列を参照する列チェック制約の定義を試みました。

* 解決策：テーブルチェック制約として定義してください。

## ERROR 5787 (HY000) : batched multi statement execution needs rollback

* OceanBaseエラーコード：5787

* エラー原因：batched multi-stmtの実行方式を満たしていません。

* 解決策：処理は不要です。サーバーが自動的に単一ステートメントの実行にロールバックします。

## ERROR 5788 (HY000) : cannot select FOR UPDATE from view with ISTINCT, GROUP BY, etc

* OceanBaseエラーコード：5788

* エラー原因：未マージのビューから`FOR UPDATE`を選択しようとしました。ビューが未マージなのは、ビューでマージを妨げる構文（例えば`DISTINCT`や`GROUP BY`）が使用されているか、初期化ファイルのパラメータ設定によるものです。

* 解決策：ビューが未マージである原因を特定し、ビューをマージ可能にするよう変更します。

## ERROR 5789 (HY000) : policy with check option violation

* OceanBaseエラーコード：5789

* エラー原因：データ書き込み時のセキュリティマーカーが誤っています。

## ERROR 5790 (HY000) : policy already applied to table

* OceanBaseエラーコード：5790

* エラー原因：既にそのポリシーで保護されているテーブルにポリシーを適用しようとしました。

* 解決策：ポリシーオプション、述語、またはラベル値を変更する場合は、テーブルからそのポリシーを削除してから、再度適用してください。

## ERROR 5791 (HY000) : table '%.\*s'.'%.\*s' is mutating, trigger/function may not see it

* OceanBaseエラーコード：5791

* エラー原因：トリガー（またはこのステートメントで参照されているユーザー定義の`plsql`関数）がテーブルを参照（または変更）しようとしていますが、同時にそのテーブルを変更するステートメントが実行されています。

* 解決策：トリガー（または関数）を書き直し、そのテーブルを読み取らないようにします。

関連する問題事例：

* [DMLステートメントで関数を呼び出す際にtable is mutatingエラーが発生する](https://www.oceanbase.com/knowledge-base/oceanbase-database-1000000000210009)

## ERROR 5792 (HY000) : column is referenced in a multi-column constraint

* OceanBaseエラーコード：5792

* エラー原因：制約で参照されている列を削除しようとしました。

* 解決策：その列を参照しているすべての制約を削除するか、ステートメントで`CASCADE CONSTRAINTS`を指定します。

## ERROR 5793 (HY000) : cannot drop parent key column

* OceanBase エラーコード：5793

* エラーの原因：制約によって参照されている親キー列を削除しようとしました。

* 解決策：親キー列を参照しているすべての制約を削除するか、ステートメントで `CASCADE CONSTRAINTS` を指定します。

## ERROR 5794 (HY000) : auto increment service busy

* OceanBase エラーコード：5794

* エラーの原因：内部エラーコードです。

* 解決策：サポートチームにお問い合わせいただき、調査のお手伝いをお願いいたします。

## ERROR 5795 (HY000) : No insert/update/delete on table with constraint (%.\*s.%.\*s) disabled and validated

* OceanBase エラーコード：5795

* エラーの原因：`DISABLE VALIDATE` 制約を含むテーブルに対して、挿入、更新、または削除操作を試みました。

* 解決策：制約の状態を変更します。

## ERROR 5796 (HY000) : active autonomous transaction detected and rolled back

* OceanBase エラーコード：5796

* エラーの原因：自治PLブロックから戻る前に、そのブロック内で開始されたすべての自治トランザクションは完了（コミットまたはロールバック）していなければなりません。そうでない場合、実行中の自治トランザクションは暗黙的にロールバックされ、このエラーコードが返されます。

* 解決策：自治PLブロックから戻る前に、進行中のすべての自治トランザクションを明示的に指定またはロールバックしてください。

## ERROR 5797 (HY000) : ORDER BY not allowed here

* OceanBase エラーコード：5797

* エラーの原因：`DISTINCT` 関数と `RATIO_TO_REPORT` では `ORDER BY` を使用できません。

## ERROR 5798 (HY000) : DISTINCT not allowed here

* OceanBase エラーコード：5798

* エラーの原因：この関数では `DISTINCT` オプションの使用は許可されていません。

## ERROR 5799 (HY000) : assign user variable with := only allowed in select filed list and as root expression

* OceanBase エラーコード：5799

* エラーの原因：`:=` を使用してユーザー変数に代入する場合、`SELECT` 句内でのみ使用可能であり、式のルートノードである必要があります。

* 解決策：ユーザー変数に値を代入する際は、`:=` の使用を避けることを推奨します。

## ERROR 5800 (HY000) : cannot modify constraint (%.\*s) - no such constraint

* OceanBase エラーコード：5800

* エラー原因：このテーブルに指定された制約が存在しません。

## ERROR 5801 (HY000) : implementation restriction: exception handler in nested transaction is illegal

* OceanBase エラーコード：5801

* エラー原因：ネストされたトランザクション内での例外キャッチは許可されていません。

* 解決策：ネストされたトランザクション内の例外キャッチを削除します。

## ERROR 5802 (HY000) : invalid ROWID

* OceanBase エラーコード：5802

* エラー原因：無効な `ROWID`。

## ERROR 5803 (HY000) : Invalid column in the INSERT VALUES Clause:'%.\*s'.'%.\*s'

* OceanBase エラーコード：5803

* エラー原因：`INSERT VALUES` 句が無効なテーブル列を参照しています。

## ERROR 5804 (HY000) : incorrect use of the \['%.\*s'\] operator

* OceanBase エラーコード：5804

* エラー原因：演算操作の実行時にエラーが発生しました。

* 解決策：演算子のパラメータと戻り値の型を確認します。

## ERROR 5805 (HY000) : non-constant expression is not allowed for pivot\|unpivot values

* OceanBase エラーコード：5805

* エラー原因：`pivot|unpivot` 値に対して非常量式を使用しようとしました。

* 解決策：`pivot|unpivot` 値に対して定数式を使用します。

## ERROR 5806 (HY000) : expect aggregate function inside pivot operation

* OceanBase エラーコード：5806

* エラー原因：`pivot` 操作内で集約関数以外の関数を使用しようとしました。

* 解決策：集約関数を使用します。

## ERROR 5807 (HY000) : expression must have same datatype as corresponding expression

* OceanBase エラーコード：5807

* エラーの原因：式は対応する式と同じデータ型を持たなければなりません。

## ERROR 5808 (HY000) : character set mismatch

* OceanBase エラーコード：5808

* エラーの原因：以下のいずれかの可能性があります：

  * 演算子または組み込み関数の文字列オペランド（`nlsparams` パラメータを除く）が、同じ文字セットを使用していない場合。

  * `nlsparams` パラメータがデータベースの文字セットに含まれていない場合。

  * 組み込み関数に渡される文字列データがデータベースの文字セット以外の文字セットを使用しており、その組み込み関数がその文字セットを受け付けていない場合。

  * `CHR()` または `CSCONVERT()` の2番目のパラメータが `CHAR_CS` または `NCHAR_CS` ではない場合。

  * `INSERT` ステートメントの `VALUES` 句内の文字列式、または `UPDATE` ステートメントの `SET` 句内の文字列式の文字セットが、挿入対象の列で使用されている文字セットと一致しない場合。

  * テーブル作成時に `DEFAULT` 句で指定した値の文字セットが、列で宣言された文字セットと一致しない場合。

  * `PL/SQL` 関数のパラメータが、対応するパラメータの文字セット要件を満たしていない場合。

## ERROR 5809 (HY000) : regular expression failed to match

* OceanBase エラーコード：5809

* エラー原因：正規表現のマッチングに失敗し、一致する文字列が見つかりませんでした。

* 解決策：内部エラーコードです。テクニカルサポートにお問い合わせいただき、調査をご依頼ください。

## ERROR 5810 (HY000) : invalid regular expression (reg version 0.8)

* OceanBase エラーコード：5810

* エラー原因：指定された正規表現 `pattern` は無効です。

* 解決策：指定された正規表現 `pattern` を調整してください。

## ERROR 5811 (HY000) : invalid escape \\\\ sequence in regular expression

* OceanBase エラーコード：5811

* エラー原因：正規表現内に無効な `ESCAPE` が含まれています。

* 解決策：無効な `ESCAPE` を削除してください。

## ERROR 5812 (HY000) : unmatched bracket in regular expression

* OceanBase エラーコード：5812

* エラー原因：正規表現に対応する角括弧がありません。

* 解決策：角括弧を対称に配置してください。

## ERROR 5813 (HY000) : unmatched parentheses in regular expression

* OceanBase エラーコード：5813

* エラー原因：正規表現に対応する丸括弧がありません。

* 解決策：丸括弧を対称に配置してください。

## ERROR 5814 (HY000) : invalid back reference in regular expression

* OceanBase エラーコード：5814

* エラー原因：サブパターン内に逆参照があります。

* 解決策：有効なサブパターンを参照してください。

## ERROR 5815 (HY000) : invalid range in regular expression

* OceanBase エラーコード：5815

* エラーの原因：正規表現内で無効な範囲が検出されました。

* 解決策：有効な範囲を使用してください。

## ERROR 5816 (HY000) : invalid character class in regular expression

* OceanBase エラーコード：5816

* エラーの原因：正規表現内で未知の文字クラスが見つかりました。

* 解決策：有効な文字クラスを使用してください。

## ERROR 5817 (HY000) : invalid collation class in regular expression

* OceanBase エラーコード：5817

* エラーの原因：正規表現に未知の照合順序クラスが含まれています。

* 解決策：有効な照合順序クラスを使用してください。

## ERROR 5818 (HY000) : braces {} not balanced in in regular expression

* OceanBase エラーコード：5818

* エラーの原因：正規表現内の `{}` が左右完全に閉じていません。

* 解決策：正規表現内の `{}` を修正してください。

## ERROR 5819 (HY000) : invalid repetition count(s) in regular expression

* OceanBase エラーコード：5819

* エラーの原因：正規表現エンジンで無効な繰り返しマッチ回数が発生しました。

* 解決策：テクニカルサポートにお問い合わせいただき、調査をご依頼ください。

## ERROR 5820 (HY000) : The regular expression was too complex and current library can't be parsed

* OceanBase エラーコード：5820

* エラーの原因：正規表現が複雑すぎて、現在の正規表現エンジンでは解析できません。

* 解決策：テクニカルサポートにお問い合わせいただき、調査をご依頼ください。

## ERROR 5821 (HY000) : regular expression internal error

* OceanBase エラーコード：5821

* エラーの原因：正規表現が複雑すぎて、現在の正規表現エンジンでは解析できません。

* 解決策：テクニカルサポートにお問い合わせいただき、調査をお手伝いいたします。

## ERROR 5822 (HY000) : invalid argument in regular expression

* OceanBase エラーコード：5822

* エラーの原因：正規表現に無効なパラメータが含まれています。

* 解決策：正規表現内のパラメータの **有効性** を確認してください。

## ERROR 5823 (HY000) : character widths of regex and string differ in regular expression

* OceanBase エラーコード：5823

* エラーの原因：正規表現エンジンで異なる文字セットの混在が発生しました。

* 解決策：テクニカルサポートにお問い合わせいただき、調査をお手伝いいたします。

## ERROR 5824 (HY000) : invalid embedded option in regular expression

* OceanBase エラーコード：5824

* エラーの原因：正規表現の `pattern` で無効なマッチングパラメータが指定されています。

* 解決策：正規表現の `pattern` マッチングパラメータを修正してください。

## ERROR 5825 (HY000) : nfa has too many states in regular expression, may be the regular expression too long

* OceanBase エラーコード：5825

* エラーの原因：正規表現エンジンの状態遷移機械に異常が発生しました。

* 解決策：テクニカルサポートにお問い合わせいただき、調査をお手伝いいたします。

## ERROR 5827 (HY000) : the definition of relative objects have been modified, please check and retry

* OceanBase エラーコード：5827

* エラーの原因：並行DDLで競合が発生しました。

* 解決策：内部エラーコードです。テクニカルサポートにお問い合わせいただき、調査をお手伝いいたします。

関連するケース：

* [インデックス作成失敗、エラーコード5827](https://www.oceanbase.com/knowledge-base/oceanbase-database-20000000182?back=kb)

## ERROR 5828 (HY000) : Subscript beyond count

* OceanBase エラーコード：5828

* エラー原因：インデックスがVARRAY配列の最大長を超えているか、ネストされたテーブルに対してインデックスが大きすぎます。

* 解決策：プログラムロジックを確認し、必要に応じて明示的に拡張します。

## ERROR 5829 (HY000) : PARTITION () clause on non partitioned table

* OceanBase エラーコード：5829

* エラー原因：テーブルまたはインデックスがパーティション化されていません。無効な構文です。

* 解決策：正しい構文でコマンドを再試行します。

## ERROR 5830 (HY000) : Unknown subpartition

* OceanBase エラーコード：5830

* エラー原因：オブジェクトのサブパーティションが見つかりません。

* 解決策：正しいサブパーティション名を使用して再試行します。

## ERROR 5831 (HY000) : Invalid SQL ROW LIMITING expression was specified

* OceanBase エラーコード：5831

* エラー原因：SQLの`ROW LIMITING`式に、テキスト、バインド変数、関連変数、またはサブクエリ以外の要素が含まれています。

* 解決策：SQLの`ROW LIMITING`式を修正して操作を再試行します。

## ERROR 5833 (HY000) : not implemented in SQL static typing engine

* OceanBase エラーコード：5833

* エラー原因：新しいエンジンではこの機能が実装されていません。

* 解決策：処理は不要です。SQLエンジンが自動的に再試行します。

## ERROR 5834 (HY000) : name is already used by an existing object

* OceanBase エラーコード：5834

* エラー原因：オブジェクトが既に存在します。

* 解決策：PLで対象となっているオブジェクトは既に存在しており、重複して作成することはできません。

## ERROR 5835 (HY000) : connection description for remote database not found

* OceanBase エラーコード：5835

* エラーの原因：リモートデータベースへの接続情報が見つかりませんでした。

## ERROR 5836 (HY000) : database link not found

* OceanBase エラーコード：5836

* エラーの原因：削除対象のデータベースリンクが辞書内に見つかりません。

* 解決策：データベースリンク名を修正してください。

## ERROR 5837 (HY000) : Reference to uninitialized composite

* OceanBase エラーコード：5837

* エラーの原因：左側でオブジェクト、`LOB` またはその他の複合パーティションを参照していますが、そのオブジェクト、`LOB` またはその他の複合パーティションが初期化されていません。

* 解決策：適切なコンストラクタまたは完全オブジェクト代入を使用して、複合パーティションを初期化してください。

## ERROR 5838 (HY000) : Reference to uninitialized collection

* OceanBase エラーコード：5838

* エラーの原因：ネストされたテーブルや `Varray` の要素、または `Member` 関数を参照していますが、コレクションが初期化されていません。

* 解決策：適切なコンストラクタまたは完全オブジェクト代入を使用して、コレクションを初期化してください。

## ERROR 5839 (HY000) : no more rows needed

* OceanBase エラーコード：5839

* エラーの原因：パイプライン関数の呼び出し元は、パイプライン関数によってさらに行を生成する必要がありません。

* 解決策：例外処理ブロック内で `NO_DATA_NEEDED` 例外をキャッチしないでください。

## ERROR 5840 (HY000) : PL/SQL: program error

* OceanBase エラーコード：5840

* エラーの原因：これは内部エラーメッセージです。PLプログラムでエラーが検出されました。

* 解決策：内部エラーコードです。サポートエンジニアに連絡し、調査を依頼してください。

## ERROR 5841 (HY000) : PL/SQL: Return types of Result Set variables or query do not match

* OceanBase エラーコード：5841

* エラーの原因：クエリ内の列数または列型と結果セット変数が宣言する戻り値の型が一致しない、または2つの結果セット変数が宣言する型が一致しない。

* 解決策：プログラムのステートメントまたは宣言を変更してください。

## ERROR 5842 (HY000) : PL/SQL: storage error

* OceanBase エラーコード：5842

* エラーの原因：PLが他のストレージを割り当てることができません。このエラーは、制御不能なプログラムによって引き起こされることがあります。

* 解決策：

  1. PLプログラム内で追加のメモリが使用されていないか、問題やエラーがないことを確認します。

  2. プログラミングにより未使用のオブジェクトを解放します（例えば、`NULL`に設定する）。

  3. 利用可能な共有メモリまたはプロセスメモリを増やします（該当する場合）。

## ERROR 5843 (HY000) : Subscript outside of limit

* OceanBase エラーコード：5843

* エラー原因：インデックスが `VARRAY` の制限値を超えているか、または `VARRAY` またはネストされたテーブルの非正の値です。

* 解決策：プログラムロジックを確認し、必要に応じて `VARRAY` の制限値を増やします。

## ERROR 5844 (HY000) : invalid cursor

* OceanBase エラーコード：5844

* エラー原因：無効なカーソルです。

## ERROR 5845 (HY000) : invalid username/password; logon denied

* OceanBase エラーコード：5845

* エラー原因：ユーザー名/パスワードが無効で、ログインが拒否されました。

## ERROR 5846 (HY000) : not logged on

* OceanBase エラーコード：5846

* エラー原因：未ログインです。

## ERROR 5847 (HY000) : method dispatch on NULL SELF argument is disallowed

* OceanBase エラーコード：5847

* エラー原因：特定のタイプの `Member` メソッドが `NULL SELF` パラメータから呼び出されています。

* 解決策：メソッド呼び出しタイプを変更して、有効な `SELF` パラメータを渡します。

## ERROR 5848 (HY000) : timeout occurred while waiting for a resource

* OceanBase エラーコード：5848

* エラー原因：通常はDeadインスタンスが原因です。

* 解決策：Deadまたは回復していないインスタンスがないか確認します。ある場合は、回復させます。

## ERROR 5849 (HY000) : column to be modified to NOT NULL is already NOT NULL

* OceanBase エラーコード：5849

* エラー原因：`NOT NULL` に変更する列は既に `NOT NULL` です。

## ERROR 5850 (HY000) : column to be modified to NULL cannot be modified to NULL

* OceanBase エラーコード：5850

* エラーの原因：列に `NULL` 値が許可されている可能性があります。`NOT NULL` 制約が主キーまたは `CHECK` 制約の一部です。

* 解決策：主キーまたは `CHECK` 制約が `NOT NULL` 制約を強制する場合、その制約を削除します。

## ERROR 5851 (HY000) :cannot enable (%.\*s.%.\*s) - null values found

* OceanBase エラーコード：5851

* エラーの原因：テーブルの制約を有効にする操作が失敗しました。テーブルに含まれる一部の値が制約を満たしていないためです。

## ERROR 5852 (HY000) : Argument should be a constant

* OceanBase エラーコード：5852

* エラーの原因：パラメータは定数である必要があります。

## ERROR 5853 (HY000) : not a single-group group function

* OceanBase エラーコード：5853

* エラーの原因：単一グループの `GROUP` 関数ではありません。

## ERROR 5854 (HY000) : illegal zero-length identifier

* OceanBase エラーコード：5854

* エラーの原因：無効なゼロ長識別子です。

## ERROR 5855 (HY000) : parameter cannot be modified because specified value is invalid

* OceanBase エラーコード：5855

* エラーの原因：初期化パラメータは変更可能ですが、変更後の値が無効です。

* 解決策：OceanBaseデータベースの対応するバージョンの「リファレンスガイド」を参照し、そのパラメータの値の範囲を確認してください。

## ERROR 5856 (HY000) : DBMS_SQL access denied

* OceanBase エラーコード：5856

* エラーの原因：セキュリティ上の理由により、`DBMS_SQL` のアクセスが拒否されました。

* 解決策：AlertログとTraceファイルから詳細な情報を取得してみてください。

## ERROR 5857 (HY000) : not all variables bound

* OceanBase エラーコード：5857

* エラー原因：一部の変数が設定されていません。

## ERROR 5859 (HY000) : column is referenced in a multi-column constraint

* OceanBase エラーコード：5859

* エラー原因：制約によって参照されている列を削除しようとしました。

* 解決策：その列を参照するすべての制約を削除するか、ステートメントで `CASCADE CONSTRAINTS` を指定します。

## ERROR 5860 (HY000) : cannot modify column datatype with current constraint(s)

* OceanBase エラーコード：5860

* SQLSTATE：HY000

* エラー原因：`CHECK` 制約を持つ列のデータ型を変更しようとしました。しかし、`CHECK` 制約を持つ列では、データ型を `CHAR` から `VARCHAR` にのみ変更できます。

* 解決策：制約を削除するか、違反する操作を実行しないでください。

## ERROR 5861 (HY000) : The percentile value should be a number between 0 and 1

* OceanBase エラーコード：5861

* エラー原因：`PERCENTILE_CONT` または `PERCENTILE_DISC` 関数に指定されたパーセンタイル値が適切な範囲外です。

* 解決策：\[0,1\] の範囲内の値を指定してください。

## ERROR 5862 (HY000) : The argument should be of numeric or date/datetime type

* OceanBase エラーコード：5862

* エラー原因：パラメータは数値または日付/日付時刻型である必要があります。

## ERROR 5863 (HY000) : ALTER TABLE\|INDEX RENAME may not be combined with other operations

* OceanBase エラーコード：5863

* エラー原因：`ALTER TABLE` または `ALTER INDEX` ステートメントが `RENAME` 操作を他の操作と結合しようとしたとき、これは無効です。

* 解決策：`ALTER TABLE` または `ALTER INDEX` ステートメントに `RENAME` 操作のみが含まれていることを確認してください。

## ERROR 5864 (HY000) : only simple column names allowed here

* OceanBase エラーコード：5864

* エラー原因：ここでは単純な列名のみが許可されています。

## ERROR 5866 (HY000) : cannot specify \<(sub)partition-description\> clause in CREATE TABLE or CREATE INDEX)

* OceanBase エラーコード：5866

* エラーの原因：リクエストでデフォルトのパーティション記述（`PARTITIONS number-of-partitions` による）を生成すると同時に、`<(sub)partition-description>` 句を指定することは、無効です。

* 解決策：違反する句のどちらか一方を削除します。

## ERROR 5867 (HY000) :Duplicate partition name %.\*s

* OceanBase エラーコード：5867

* エラーの原因：テーブルまたはインデックスで作成されたサブパーティションまたはインデックスの名前が一意ではありません。

* 解決策：サブパーティションの名前を変更し、テーブルまたはインデックスで作成されたサブパーティション内で一意であることを確認します。

## ERROR 5868 (HY000) : Incorrect usage of UPDATE and ORDER BY

* OceanBase エラーコード：5868

* エラーの原因：複数テーブルの `UPDATE` 時に `ORDER BY` を使用できません。

* 解決策：`ORDER BY` を削除します。

関連する問題例：

* [MySQLモード Update + Order by エラー 5868](https://www.oceanbase.com/knowledge-base/oceanbase-database-1000000000209975)

## ERROR 5869 (HY000) : Incorrect usage of UPDATE and LIMIT

* OceanBase エラーコード：5869

* エラーの原因：複数テーブルの `UPDATE` 時に `LIMIT` を使用できません。

* 解決策：`LIMIT` を削除します。

## ERROR 5870 (HY000) : rowid type mismatch, expect %.\*s, got %.\*s

* OceanBase エラーコード：5870

* エラーの原因：内部エラーコード。

* 解決策：テクニカルサポートにお問い合わせいただき、調査をお手伝いいただけますでしょうか。

## ERROR 5871 (HY000) : rowid num mismatch, expect %ld, actual %ld

* OceanBase エラーコード：5871

* エラーの原因：内部エラーコード。

* 解決策：テクニカルサポートにお問い合わせいただき、調査をお手伝いいただけますでしょうか。

## ERROR 5872 (HY000) :must name %.\*s with a column alias

* OceanBase エラーコード：5872

* SQLSTATE：HY000

* エラーの原因：この式には列エイリアスを指定する必要があります。

## ERROR 5873 (HY000) : the numeric value does not match the length of the format item

* OceanBase エラーコード：5873

* エラーの原因：入力日付にFXまたはFM形式コードを指定した場合、数値の桁数は形式コードで指定された桁数と完全に一致している必要があります。例えば、`9` は形式指示子DDとマッチしませんが、`09` はマッチします。

* 解決策：入力日付を修正するか、フォーマット文字列からFXまたはFM形式指示子を削除します。

## ERROR 5874 (HY000) : invalid datatype

* OceanBase エラーコード：5874

* エラーの原因：無効なデータ型です。

## ERROR 5875 (HY000) : table is not partitioned by composite partition method

* OceanBase エラーコード：5875

* エラーの原因：サブパーティションテーブルではないテーブルに対してサブパーティションのメンテナンス操作を実行しようとしました。

* 解決策：テーブルがサブパーティションテーブルであることを確認します。

## ERROR 5876 (HY000) : VALUES IN (\<value list\>) cannot be used for Range subpartitioned tables

* OceanBase エラーコード：5876

* エラーの原因：`VALUES (value list)` 句はRANGEサブパーティションテーブルに使用されます。

* 解決策：RANGEサブパーティションテーブルと共に `VALUES LESS THAN` または `AT` 句を使用します。

## ERROR 5877 (HY000) : VALUES IN (\<value list\>) clause expected

* OceanBase エラーコード：5877

* エラー原因：LISTサブパーティションに`VALUES (value list)`句を指定していません。

* 解決策：`VALUES (``value list)`句を指定します。

## ERROR 5879 (HY000) : Expecting VALUES LESS THAN or AT clause

* OceanBase エラーコード：5879

* エラー原因：`VALUES (value list)`句はLISTパーティションテーブルとのみ使用できます。

* 解決策：`VALUES LESS THAN`句はRANGEパーティションテーブルと共に使用します。

## ERROR 5880 (HY000) : procedure, function, package, or package body does not exist

* OceanBase エラーコード：5880

* エラー原因：存在しないプロシージャ、関数、パッケージ、またはパッケージ本体にアクセスしようとしました。

* 解決策：アクセスするオブジェクト名が正しいことを確認します。

## ERROR 5882 (HY000) : invalid time limit specified

* OceanBase エラーコード：5882

* エラー原因：`dbms_lock.sleep(time)`パッケージの入力パラメータ`time`に負の値が指定されています。

* 解決策：正の値を指定します。

## ERROR 5883 (HY000) : ## ERROR(s) in array DML

* OceanBase エラーコード：5883

* エラー原因：DMLステートメント内に1行以上の失敗があります。

* 解決策：例外ハンドリング段階でエラースタックを確認し、具体的な例外を取得します。

## ERROR 5884 (42000) : %s \\'%.\*s.%.\*s\\' compile error

* OceanBase エラーコード：5884

* エラー原因：トリガーを作成するDDLステートメントに誤りがあります。

* 解決策：具体的なヒントに従って、DDLステートメントを修正し、再度実行します。

## ERROR 5887 (HY000) : missing or invalid password(s)

* OceanBase エラーコード：5887

* エラーの原因：パスワードが間違っています。

* 解決策：正しいパスワードを使用していることを確認し、再度試行してください。

## ERROR 5889 (HY000) : no matching unique or primary key for this column-list

* OceanBase エラーコード：5889

* エラーの原因：`CREATE TABLE` または `ALTER TABLE` ステートメント内の `REFERENCES` 句で指定された列リストに対応する、参照テーブル内の一意キーまたは主キー制約が見つかりません。

* 解決策：`ALL_CONS_COLUMNS` ディレクトリビューを使用して正しい列名を検索します。

## ERROR 5890 (HY000) : duplicate referential constraint specifications

* OceanBase エラーコード：5890

* エラーの原因：テーブル作成時に、同一の外部キー制約が複数存在します。

* 解決策：`CREATE TABLE` ステートメントを修正し、同一の外部キー制約が複数存在しないようにします。

## ERROR 5891 (HY000) : such a referential constraint already exists in the table

* OceanBase エラーコード：5891

* エラーの原因：テーブル変更時に、同一の外部キー制約が複数存在します。

* 解決策：`ALTER TABLE` ステートメントを修正し、同一の外部キー制約が複数存在しないようにします。

## ERROR 5893 (HY000) : specified data type is not supported for a virtual column

* OceanBase エラーコード：5893

* エラーの原因：生成列のデータ型が現在サポートされていない `LOB` 型です。

* 解決策：生成列のデータ型を調整します。

## ERROR 5894 (HY000) : referenced table does not have a primary key

* OceanBase エラーコード：5894

* エラーの原因：外部キーの参照先列を指定しなかった場合、外部キーの主表に主キーがありません。

* 解決策：外部キーを作成する前に、参照先列に主キーまたは一意インデックスを作成する必要があります。

## ERROR 5895 (HY000) : data type or length of a table partitioning column may not be changed

* OceanBase エラーコード：5895

* エラーの原因：パーティション列のデータ型と長さは変更できません。

## ERROR 5896 (HY000) : data type or length of a table partitioning column may not be changed

* OceanBase エラーコード：5896

* エラーの原因：サブパーティション列のデータ型と長さは変更できません。

## ERROR 5897 (HY000) : cannot decrease column length because some value is too big

* OceanBase エラーコード：5897

* エラーの原因：現在、列の最大長制限を短縮することはサポートされていません。

## ERROR 5898 (HY000) : Datetime/Interval internal error

* OceanBase エラーコード：5898

* エラーの原因：内部エラーです。

* 解決策：テクニカルサポートにお問い合わせいただき、調査をご依頼ください。

## ERROR 5899 (HY000) : partition extended table name cannot refer to a remote object

* OceanBase エラーコード：5899

* エラーの原因：拡張パーティションテーブル構文を使用してリモートオブジェクトに名前を付けようとしました。

* 解決策：式を修正して再試行してください。

## ERROR 5901 ( HY000 ) : a view is not appropriate here

* OceanBase エラーコード：5901

* エラーの原因：ビューに `EDITIONING_VIEW` を定義しようとしました。

* 解決策：`EDITIONING_VIEW` はベーステーブルにのみ作成できます。

## ERROR 5902 (HY000) : cannot select ROWID from,or sample, a join view without a key-preserved table

* OceanBase エラーコード：5902

* エラーの原因：key-preserved tableがない場合、結合ビューから `ROWID` を取得したり、サンプリングしたりできません。

## ERROR 5903 (HY000) : cannot select ROWID from, or sample, a view with DISTINCT, GROUP BY, etc

* OceanBase エラーコード：5903

* エラーの原因：`DISTINCT` または `GROUP BY` などを含むビューから `ROWID` を取得できません。

## ERROR 5904 (HY000) : table must have at least 1 column that is not virtual

* OceanBase エラーコード：5904

* エラーの原因：仮想列のみを含むテーブルを作成しようとしています。

* 解決策：作成するテーブルに、少なくとも1つの非仮想列を含めてください。

## ERROR 5905 (HY000) : only pure functions can be indexed

* OceanBase エラーコード：5905

* エラーの原因：インデックス関数で `SYSDATE` またはユーザー環境が使用されています。

* 解決策：PL関数は純粋関数（`RNDS`、`RNPS`、`WNDS`、`WNPS`）である必要があります。

## ERROR 5906 (HY000) : only pure functions can be specified in a virtual column expression

* OceanBase エラーコード：5906

* エラーの原因：仮想列の式に値が不確定な関数が含まれています。

* 解決策：式を書き直し、純粋関数のみを参照するようにします。

## ERROR 5907 (HY000) : UPDATE operation disallowed on virtual columns

* OceanBase エラーコード：5907

* エラーの原因：仮想列の値を更新しようとしています。

* 解決策：そのステートメントを再実行し、仮想列の値を設定しないでください。

## ERROR 5908 (HY000) : Invalid column expression was specified

* OceanBase エラーコード：5908

* エラーの原因：仮想列の式は有効な算術式ではありません。テーブル内の別の列を参照している可能性があります。

* 解決策：仮想列の式を変更してください。

## ERROR 5909 (HY000) : table can have only one identity column

* OceanBase エラーコード：5909

* エラーの原因：1つのテーブルに複数の識別列を指定しようとしています。

* 解決策：各テーブルには1つの識別列のみを指定してください。

## ERROR 5910 (HY000) : invalid NOT NULL constraint specified on an identity column

* OceanBase エラーコード：5910

* エラーの原因：識別列に `NOT NULL` 制約が正しく指定されていません。

* 解決策：識別列に `NOT NULL` 制約を指定する場合は、それを有効にする必要があり、遅延することはできません。

## ERROR 5911 (HY000) : cannot modify NOT NULL constraint on an identity column

* OceanBase エラーコード：5911

* エラーの原因：識別列の `NOT NULL` 制約を変更できません。

* 解決策：識別列の `NOT NULL` 制約を変更しないでください。

## ERROR 5912 (HY000) : cannot drop NOT NULL constraint on an identity column

* OceanBase エラーコード：5912

* エラーの原因：識別列の `NOT NULL` 制約を削除できません。

* 解決策：識別列から `NOT NULL` 制約を削除しないでください。制約を削除する唯一の方法は、`ALTER TABLE MODIFY` の `DROP IDENTITY` を使用して列の `identity` 属性を削除することです。

## ERROR 5913 (HY000) : column to be modified is not an identity column

* OceanBase エラーコード：5913

* エラーの原因：識別属性ではない列の識別属性を変更しようとしています。

* 解決策：識別列の識別属性を変更してください。

## ERROR 5914 (HY000) : identity column cannot have a default value

* OceanBase エラーコード：5914

* エラーの原因：デフォルト値を識別列に追加しようとしています。

* 解決策：識別列にデフォルト値を指定することはできません。

## ERROR 5915 (HY000) : identity column must be a numeric type

* OceanBase エラーコード：5915

* エラーの原因：識別列の値を非数値データ型に変更しようとしています。

* 解決策：識別列に非数値データ型を割り当てないでください。

## ERROR 5916 (HY000) : prebuilt table managed column cannot be an identity column

* OceanBase エラーコード：5916

* エラーの原因：識別列を持つプリビルドテーブルにインスタンス化ビューを作成しようとしています。

* 解決策：`identity` 属性を削除するか、インスタンス化ビューの定義から識別列を削除します。

## ERROR 5917 (HY000) : cannot alter a system-generated sequence

* OceanBase エラーコード：5917

* エラーの原因：システム生成のシーケンスを変更しようとしています。

* 解決策：システム生成のシーケンス（例えば、識別列のために作成されたシーケンス）は変更できません。

## ERROR 5918 (HY000) : cannot drop a system-generated sequence

* OceanBase エラーコード：5918

* エラーの原因：システム生成のシーケンスを削除しようとしています。

* 解決策：システム生成のシーケンス（例えば、識別列のために作成されたシーケンス）は削除できません。

## ERROR 5919 (HY000)：cannot insert into a generated always identity column

* OceanBase エラーコード：5919

* エラーの原因：`GENERATED ALWAYS` キーワードを使用して作成された識別列に値を挿入しようとしています。

* 解決策：`GENERATED ALWAYS` キーワードで生成された識別列には直接値を挿入できません。代わりに、関連するシーケンスジェネレーターがその値を提供します。

## ERROR 5920 (HY000) : cannot update a generated always identity column

* OceanBase エラーコード：5920

* エラーの原因：`GENERATED ALWAYS` キーワードを使用して作成された識別列を更新しようとしています。

* 解決策：`GENERATED ALWAYS` キーワードで生成された識別列は直接更新できません。

## ERROR 5921 (HY000) : identity column sequence mismatch in ALTER TABLE EXCHANGE PARTITION

* OceanBaseエラーコード：5921

* エラー原因：`EXCHANGE`で指定された2つのテーブルの識別列のシーケンスが増加も減少もしません。

* 解決策：`INCREMENT BY`を持つ識別列には、同じ符号を持つシーケンスを設定してください。

## ERROR 5922 (HY000) : cannot rename a system-generated sequence

* OceanBaseエラーコード：5922

* エラー原因：システム生成のシーケンスを名前変更しようとしています。

* 解決策：システム生成のシーケンス（例えば、識別列のために作成されたシーケンス）は名前変更できません。

## ERROR 5923 (HY000) : UPDATE/REFERENCES may only be REVOKEd from the whole table, not by column

* OceanBaseエラーコード：5923

* エラー原因：`UPDATE`または`REFERENCES`は列単位ではなく、テーブル全体からのみ取り消すことができます。

## ERROR 5924 (HY000) : not executed, type body '%.\*s' does not exist

* OceanBaseエラーコード：5924

* エラー原因：存在しないストアドプログラムを実行しようとしています。

* 解決策：正しい名前を指定してください。

## ERROR 5925 (HY000) : The argument \[%s\] of WIDTH_BUCKET function is NULL or invalid

* OceanBaseエラーコード：5925

* エラー原因：`WIDTH_BUCKET` 関数のパラメータ `[%s]` が `NULL` または無効です。

## ERROR 5926 (HY000) : Not enough memory for CONNECT BY operation

* OceanBaseエラーコード：5926

* エラー原因：新しいエンジンでは、再帰階層が深すぎる場合にエラーが発生します。この制限は2の30乗です。一方、古いエンジンでは、ルートノードから現在のノードへの経路上で、すべてのノードとその子ノードの数が一定の値（2の30乗）を超える場合にもエラーが発生します。現在の設定値は固定されており、通常はこの値を超える問題には直面しません。

* 解決策：現時点では解決策は見つかっていません。メモリサイズを増やしてもこの問題は解決しません。唯一の解決策は、再帰階層を縮小するか、データ量を削減することです。

## ERROR 5927 (HY000) : illegal parameter in SYS_CONNECT_BY_PATH function

* OceanBaseエラーコード：5927

* エラー原因：`SYS_CONNECT_BY_PATH` 関数に無効なパラメータが含まれています。

## ERROR 5928 (HY000) : host %.\*s unknown

* OceanBase エラーコード：5928

* エラー原因：指定されたホストが不明です。

* 解決策：ホスト名またはIPアドレスの綴りを確認し、有効であることを保証してください。

## ERROR 5929 (HY000) : Window name '%.\*s' is not defined

* OceanBase エラーコード：5929

* エラー原因：指定されたウィンドウ関数が存在しません。

* 解決策：関連するウィンドウ関数が作成されていることを確認し、正しいウィンドウ関数名を使用してください。

## ERROR 5930 (HY000) : maximum open cursors exceeded

* OceanBase エラーコード：5930

* エラー原因：システムで同時に開いているカーソルの数が制限を超えています。

* 解決策：カーソルを閉じるか、`open_cursors` パラメータを調整してください。

関連する問題事例：

* [業務で maximum open cursors exceeded が発生した場合](https://www.oceanbase.com/knowledge-base/oceanbase-database-1000000000209966)
* [PL 積み重ね呼び出しで refcursor がエラー 5930 を報告した場合](https://www.oceanbase.com/knowledge-base/oceanbase-database-1000000000422799?back=kb)

## ERROR 5931 (HY000) : argument is null, invalid, or out of range

* OceanBase エラーコード：5931

* エラー原因：渡されたパラメータ値が `NULL`、無効、または範囲外です。例えば、`LOB / FILE` の位置またはサイズパラメータの値が1から(4GB-1)の範囲を超えている場合、無効なオープンモードでファイルを開いた場合などです。

* 解決方法：プログラムを確認し、`NULL`、無効、または範囲外のパラメータ値を渡さないよう修正してください。

## ERROR 5932 (HY000) : unexpected name string

* OceanBase エラーコード：5932

* エラー原因：誤った名前文字。

## ERROR 5933 (HY000) : PL/SQL: could not find program unit being called: '%.\*s'

* OceanBase エラーコード：5933

* エラー原因：プログラムユニットが見つかりません。

## ERROR 5934 (HY000) : fetch out of sequence

* OceanBase エラーコード：5934

* SQLSTATE：HY000

* エラーの原因：無効なカーソルからアクセスを試みた場合。このエラーが発生する原因は以下のとおりです：

  * 最後の行まで検索して `OBE-1403` エラーが返された後、カーソルからアクセスを試みた場合。

  * `FOR UPDATE` 句を使用してカーソルを開いた後、`COMMIT` を実行してからアクセスを試みた場合。

  * SQLステートメント内のすべてのプレースホルダーを再バインドし、そのステートメントを再実行する前にアクセスを試みた場合。

  <main id="notice" type='explain'>
    <h4>説明</h4>
    <p>PLカーソルのループでは、暗黙的にデータを取得するため、このエラーが発生する可能性があります。</p>
  </main>

* 解決策：

  * 最後の行まで検索した後、`FETCH` ステートメントを実行しないでください。

  * `FOR UPDATE` 句を使用して開いたカーソルに対して、`COMMIT` を実行した後にアクセスを試みないでください。

  * SQLステートメントを再バインドして実行し、その後にアクセスを試みてください。

## ERROR 5959 (HY000) : invalid SIZE specified

* OceanBase エラーコード：5959

* エラーの原因：指定されたヒストグラムの `SIZE` 値が範囲を超えています。

* 解決策：\[1，2048\] の範囲内の `SIZE` 値を指定してください。

## ERROR 5975 (HY000) : \ndblink remote error code: %d,\nremote error msg: %.*s"

* OceanBase エラーコード：5975

* エラーの原因：リモートデータベースへの接続にエラーが発生し、そのエラーコードをOceanBaseのエラーコードにマッピングできません。

* 解決策：エラーメッセージに記載されているエラーコードに基づいて、関連する原因を調査してください。

## ERROR 5976 (HY000) : can not find the expected version of OCI LIB: %.*s

* OceanBase エラーコード：5976

* エラーの原因：期待されるバージョンのOCI LIBが見つかりません。

* 解決策：[OCIのインストールと設定](../../300.database-object-management/200.manage-object-of-oracle-mode/1000.manage-dblink-of-oracle-mode/600.install-and-configure-the-oci.md)を参照してください。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>V4.3.0以降、このエラーコードのメッセージが<code>oci lib not founded</code>から<code>can not find the expected version of OCI LIB: %.*s</code>に調整されました。</p>
</main>