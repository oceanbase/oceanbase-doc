|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type|Oracle Mode|
|slug|obe-50000-99999-oracle-mode|

# OBE-50000 ~ OBE-99999

  <main id="notice" >
    <h4>機能の適用範囲</h4>
    <p>この内容はOceanBaseデータベースのOracleモードでのみ適用されます。</p>
  </main>

## OBE-54002：only pure functions can be specified in a virtual column expression

* Ocean Base エラーコード：5906

* SQLSTATE：HY000

* エラー原因：仮想列式には、値が不確定な関数が含まれています。

* 解決策：式を書き直し、純粋関数のみを参照するようにします。

## OBE-54003：specified data type is not supported for a virtual column

* Ocean Base エラーコード：5893

* SQLSTATE：HY000

* エラー原因：仮想列はスカラーデータ型のみをサポートします。仮想列は `LONG`、`BLOB`、`REF`、`BFILE` データ型をサポートしません。

* 解決策：式列に対してサポートされているスカラーデータ型を指定します。

## OBE-54004：resultant data type of virtual column is not supported

* Ocean Base エラーコード：5401

* SQLSTATE：HY000

* エラー原因：仮想列は基底式のデータ型をサポートせず、スカラーデータ型のみをサポートします。仮想列は `LONG`、`BLOB`、`REF`、`BFILE` データ型をサポートしません。

* 解決策：仮想列の式に対してサポートされているスカラーデータ型を指定します。

## OBE-54013：INSERT operation disallowed on virtual columns

* Ocean Base エラーコード：5290

* SQLSTATE：HY000

* エラー原因：仮想列に値を挿入しようとしました。

* 解決策：そのステートメントを再実行しますが、仮想列の値は指定しません。

## OBE-54016：Invalid column expression was specified

* Ocean Base エラーコード：5908

* SQLSTATE：HY000

* エラー原因：仮想列式は有効な算術式ではありません。テーブル内の別の列を参照している可能性があります。

* 解決策：仮想列の式を変更します。

## OBE-54017：UPDATE operation disallowed on virtual columns

* Ocean Base エラーコード：5907

* SQLSTATE：HY000

* エラー原因：仮想列の値を更新しようとしました。

* 解決策：そのステートメントを再実行しますが、仮想列の値は設定しません。

## OBE-54021：Cannot use PL/SQL expressions in partitioning or subpartitioning columns

* Ocean Base エラーコード：5995

* SQLSTATE：HY000

* エラー原因：PL/SQL 式を含む仮想列を使用してテーブルをパーティション化しようとしました。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは V4.0.0 から導入されました。</p>
</main>

## OBE-54023：Virtual column expression cannot be changed because a constraint is defined on column

* Ocean Base エラーコード：5948

* SQLSTATE：HY000

* エラー原因：トリガーボディ内で仮想列の値を変更することは許可されていません。

* 解決策：トリガー内で仮想列の値を変更しないでください。

## ## OBE-54025：Virtual column cannot have a default value

* Ocean Base エラーコード：5504
* SQLSTATE：HY000
* エラー原因：仮想列にはデフォルト値を設定できません。
* 解決策：仮想列のデフォルト値を削除します。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは V4.2.1 から導入されました。</p>
</main>

## OBE-54026：Real column cannot have an expression

* Ocean Base エラーコード：5505
* SQLSTATE：HY000
* エラー原因：実列に式を含めることはできません。
* 解決策：実列の式を削除します。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは V4.2.1 から導入されました。</p>
</main>

## OBE-54027：cannot modify data-type of virtual column

* OceanBase エラーコード：5506
* SQLSTATE：HY000
* エラー原因：仮想列のデータ型を変更できません。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードはV4.2.1バージョンから導入されました。</p>
</main>

## OBE-54029：Virtual column cannot be updated in trigger body

* Ocean Base エラーコード：5703

* SQLSTATE：HY000

* エラー原因：トリガー本体内の仮想列の値を変更しようとしています。

* 解決策：無効な操作です。トリガー定義を変更する必要があります。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードはV4.0.0バージョンから導入されました。</p>
</main>

## OBE-54036：cannot define referential constraint with ON DELETE SET NULL clause on virtual column

* OceanBase エラーコード：5513
* SQLSTATE：HY000
* エラー原因：仮想列にON DELETE SET NULL句を含む外部キー制約を定義できません。
* 解決策：仮想列にON DELETE SET NULL句を含む外部キー制約を定義しないでください。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードはV4.2.1バージョンから導入されました。</p>
</main>

## OBE-54037：table must have at least 1 column that is not virtual

* OceanBase エラーコード：5904

* SQLSTATE：42000

* エラー原因：仮想列のみを含むテーブルの作成を試みています。

* 解決策：作成するテーブルに、少なくとも1つの非仮想列を含めてください。

## OBE-54039：table must have at least one column that is not invisible

* OceanBase エラーコード：5679

* SQLSTATE：HY000

* エラー原因：テーブルを作成または変更しようとしており、そのテーブルには不可視列のみが含まれています。

* 解決策：作成または変更するテーブルには、少なくとも1つの可視列を含めてください。

## OBE-54042：Invisible column is not supported on this type of table

* OceanBase エラーコード：5680

* SQLSTATE：HY000

* エラー原因：不可視列の作成や列の可視性の変更を試みていますが、現在のテーブルは不可視列をサポートしていません。ヒープインデックス組織テーブル(IOT)のみが不可視列をサポートします。

* 解決策：これらの列はサポートされていません。DDLを変更してください。

## OBE-54046：Column visibility modifications cannot be combined with any other modified column DDL option

* OceanBase エラーコード：5681

* SQLSTATE：HY000

* エラー原因：列の可視性の変更操作を他の列属性の変更操作と組み合わせようとしています。

* 解決策：列の可視性の変更操作を独立したDDLに配置してください。

## OBE-54048：Column expression modifications cannot be combined with any other column DDL operation

* OceanBase エラーコード：5947

* SQLSTATE：HY000

* エラー原因：列式の変更を他の列操作と組み合わせようとしています。

* 解決策：列式の変更操作を独立したDDLに分けてください。

## OBE-54053：The visibility of a column from a table owned by a SYS user cannot be changed

* OceanBase エラーコード：5682

* SQLSTATE：HY000

* エラー原因：SYSユーザーが所有するテーブルの列の可視性を変更しようとしています。

* 解決策：非SYSユーザーが所有するテーブルでこのDDLを実行してください。

## OBE-56901：non-constant expression is not allowed for pivot\|unpivot values

* OceanBase エラーコード：5805

* SQLSTATE：HY000

* エラーの原因：`pivot|unpivot` 値に対して非常数式を使用しようとしました。

* 解決策：`pivot|unpivot` 値に定数式を使用してください。

## OBE-56902：expect aggregate function inside pivot operation

* OceanBase エラーコード：5806

* SQLSTATE：HY000

* エラーの原因：`pivot` 操作内で集約式以外の式を使用しようとしました。

* 解決策：集約式を使用してください。

## OBE-56713：insufficient Resource Manager privileges

* OceanBase エラーコード：4715

* SQLSTATE：HY000

* エラーの原因：Resource Managerを操作する権限がありません。

* 解決策：rootユーザーとして関連操作を実行してください。

## OBE-62550：Invalid SQL ROW LIMITING expression was specified

* OceanBase エラーコード：5831

* SQLSTATE：HY000

* エラーの原因：SQLの `ROW LIMITING` 式に、テキスト、バインド変数、関連変数、またはサブクエリ以外の要素が含まれています。

* 解決策：SQLの `ROW LIMITING` 式を修正して操作を再試行してください。

## OBE-64601：duplicate RESTART specified

* OceanBase エラーコード：4382

* SQLSTATE：HY000

* エラーの原因：`ALTER SEQUENCE` コマンドで重複する `RESTART` 指示が指定されています。

* 解決策：重複する `RESTART` 指示を削除してください。

## ## OBE-64403：Input cannot be null

* OceanBase エラーコード：5471
* SQLSTATE：42000
* エラーの原因：入力はNULLにすることができません。
* 解決策：入力パラメータを指定してください。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードはV4.1.0バージョンから導入されました。</p>
</main>