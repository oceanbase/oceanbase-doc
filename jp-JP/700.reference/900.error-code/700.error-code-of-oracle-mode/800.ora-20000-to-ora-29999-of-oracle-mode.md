
|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type|Oracle Mode|
|machine-translation|ja-marker|
|slug|ora-20000-29999-oracle-mode|

# ORA-20000 ~ ORA-29999

  <main id="notice" >
    <h4>適用対象</h4>
    <p>この内容は、OceanBaseデータベースのOracleモードにのみ適用されます。</p>
  </main>

## ORA-20000： The stored procedure  'raise_application_error' was called which causes this error to be  generated", "ORA%06ld: %.*s


* OceanBase エラーコード：20000

* SQLSTATE：HY000

* エラー原因：プロシージャのカスタム エラーコード `RAISE_APPLICATION_ERROR` が呼び出され、このエラーが発生しました。

* 解決策：エラー メッセージの説明に従って問題を修正するか、アプリケーション管理者またはデータベース管理者に連絡して詳細を確認してください。

## ORA-21000：error number argument to raise_application_error of stringstring is out of range


* OceanBase エラーコード：21000

* SQLSTATE：HY000

* エラー原因：`RAISE_APPLICATION_ERROR` のエラー番号引数がパラメータの範囲を超えています。

## ORA-21560：argument %.\*s is null, invalid, or out of range


* OceanBase エラーコード：5934

* SQLSTATE：HY000

* エラー原因：渡されたパラメータ値が `NULL`、無効、または範囲外です。たとえば、`LOB / FILE` の位置またはサイズのパラメータ値が 1 から (4 GB-1) の範囲を超える場合、または無効なオープン モードでファイルを開く場合などです。

* 解決策：プログラムを確認し、`NULL`、無効、または範囲外のパラメータ値を渡さないように修正してください。

## ORA-22003：value is out of range


* OceanBase エラーコード：4157

* SQLSTATE：22003

* エラー原因：指定された `TCP/IP` 関数 `func` を実行する際、予期しない通信エラーが発生しました。`TCP/IP` 関数の戻りコードは `rc`、関数 `func` のパラメータは `parm` です。

* 解決策：テクニカル サポートに連絡してトラブルシューティングを依頼してください。

## ORA-22369：invalid parameter encountered in method %s


* OceanBase エラーコード：5955

* SQLSTATE：HY000

* エラー原因：`SYS` に無効なパラメータが渡されています。無効なパラメータには、`SYS. any type`、`SYS. any data`、または `SYS. any dataset` が含まれます。

* 解決策：渡されたパラメータを確認し、許可されたパラメータであることを確認してください。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは、V4.0.0 以降のバージョンから導入されました。</p>
</main>

## ORA-22370：incorrect usage of method %s


* OceanBase エラーコード：5956

* SQLSTATE：HY000

* エラー原因：`SYS. any type`、`SYS. any data`、または `SYS. any dataset` が不適切に使用されています。

* 解決策：正しい使用法を採用してください。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは、V4.0.0 以降のバージョンから導入されました。</p>
</main>

## ORA-22625：OCIAnyData is not well-formed


* OceanBase エラーコード：5965

* SQLSTATE：HY000

* エラー原因：`OCIAnyData` を未初期化のコンストラクターで使用しています。

* 解決策：プロパティを追加する前に、`OCIAnyDataBeginConstruct` を使用してハンドルを初期化します。コンストラクターを完了するには、`OCIAnyDataEndConstruct` を使用します。または、`OCIAny-DataConvert` を使用してコンストラクターを構築します。プロパティにアクセスする前に、それが正しく構築されていることを確認してください。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは、V4.0.0 以降のバージョンから導入されました。</p>
</main>

## ORA-22626：Type Mismatch while constructing or accessing OCIAnyData


* OceanBase エラーコード：5957

* SQLSTATE：HY000

* エラー原因：提供された型が AnyData の型と一致しません。セグメント構築またはアクセスを試行している場合は、提供された型が現在のプロパティの型と一致しません。

* 解決策：構築またはアクセスするオブジェクトの型と提供された型が一致していることを確認してください。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは V4.0.0 以降のバージョンから導入されました。</p>
</main>

## ORA-22627：tc \[%s\] must be that of object/varray/nested table


* OceanBase エラーコード：5966

* SQLSTATE：HY000

* エラー原因：型コードが object/varray/nested table の型コードではありません。

* 解決策：型コードが `OCI_TYPECODE_OBJECT` または `OCI_TYPECODE_VARRAY` または `OCI_TYPECODE_TABLE` であることを確認してください。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは V4.0.0 以降のバージョンから導入されました。</p>
</main>

## ORA-22806：not an object or REF

* OceanBase エラーコード：5477
* SQLSTATE：42000
* エラー原因：オブジェクトまたは REF ではありません。
* 解決策：オブジェクトまたは REF を使用して操作を再実行してください。

## ORA-22816：unsupported feature with RETURNING clause


* OceanBase エラーコード：9573

* SQLSTATE：HY000

* エラー原因：オブジェクト型列は現在 RETURNING 句をサポートしていません。LONG 列、リモートテーブル、サブクエリを含む INSERT 文、および INSTEAD OF トリガー。

* 解決策：個別のステートメントを使用して値を取得してください。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは V4.0.0 以降のバージョンから導入されました。</p>
</main>

## ORA-22858：invalid alteration of datatype

* OceanBase エラーコード：7414
* SQLSTATE：42000
* エラー原因：データ型を変更しようとしたが、データ型は変更できず、変更後のデータ型も無効です。
* 解決策：
   1. データ型を確認する：変更しようとしているデータ型が変更可能であることを確認し、変更後のデータ型が有効であることを確認してください。
   2. データサイズを確認する：変更しようとしているデータサイズを確認し、変更後のデータ型がすべてのデータを格納できるようにすることを確認してください。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは V4.2.0 以降のバージョンから導入されました。</p>
</main>

## ORA-22859：invalid modification of columns

* OceanBase エラーコード：7415
* SQLSTATE：42000
* エラー原因：変更できない列またはテーブル構造を変更しようとした。
* 解決策：
   1. テーブル構造を確認する：変更しようとしているテーブル構造を確認し、テーブル構造が変更可能であることを確認してください。
   2. 列属性を確認する：変更しようとしている列属性を確認し、列属性が変更可能であることを確認してください。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは V4.2.0 以降のバージョンから導入されました。</p>
</main>

## ORA-22859：Expected XML tag , got no content

* OceanBase エラーコード：7416
* SQLSTATE：42000
* エラー原因：XMLデータには、タグの内容が不足しています。
* 解決策：
   1. XMLデータを確認する：XMLデータが正しいことを確認し、すべてのタグに内容があることを確認してください。
   2. XML形式を確認する：XML形式が正しいことを確認し、すべてのタグが正しくネストされ、閉じていることを確認してください。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは V4.2.0 以降のバージョンから導入されました。</p>
</main>

## ORA-22866：cannot replace a type with table dependents

* OceanBase エラーコード：11006

* SQLSTATE：HY000

* エラー原因：テーブル依存関係がある型を置き換えることはできません。

* 解決策：現在の型に依存するすべてのテーブルを削除し、再試行してください。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは V4.2.2 から導入されました。</p>
</main>

## ORA-22901：cannot compare VARRAY or LOB attributes of an object type

* OceanBase エラーコード：7432

* SQLSTATE：42000

* エラー原因：MAP または ORDER メソッドが定義されていない場合に、オブジェクト型の VARRAY または LOB 属性を比較しようとした。

* 解決策：オブジェクト型に MAP または ORDER メソッドを定義してください。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは V4.2.2 から導入されました。</p>
</main>

## ORA-22903：MULTISET expression not allowed

* OceanBase エラーコード：9716
* SQLSTATE：HY000
* エラー原因：MULTISET 表達式の使用は許可されていません。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは V4.1.0 から導入されました。</p>
</main>

## ORA-22907：invalid CAST to a type that is not a nested table or VARRAY

* OceanBase エラーコード：9717
* SQLSTATE：HY000
* エラー原因：ネストされたテーブルまたは VARRAY 以外の型への無効な CAST。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは V4.1.0 から導入されました。</p>
</main>

## ORA-22922：LOB value does not exist

* OceanBase エラーコード：7419
* SQLSTATE：HY000
* エラー原因：LOB 値にアクセスしようとしたが、その値は存在しません。
* 解決策：LOB 値が存在するテーブルまたはインデックスが存在するか削除されたか確認し、テーブルまたはインデックスを再復元できるかどうかを確認してください。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは V4.2.0 から導入されました。</p>
</main>

## ORA-22928：invalid privilege on directories


* OceanBase エラーコード：5374

* SQLSTATE：HY000

* エラー原因：ディレクトリに対する権限を付与または取り消そうとしたが、その権限はディレクトリに対して有効ではありません。

* 解決策：ディレクトリに対する権限は `CREATE`、`DELETE`、`READ`、`WRITE`、および `EXECUTE` のみ付与または取り消すことができます。他の権限を付与または取り消すことはできません。

## ORA-22950：cannot ORDER objects without MAP or ORDER method

* OceanBase エラーコード：7412
* SQLSTATE：42000
* エラー原因：オブジェクトを比較する際に、オブジェクトに MAP または ORDER メソッドが定義されていない。
* 解決策：
   1. MAP または ORDER メソッドを定義する：オブジェクトの定義に MAP または ORDER メソッドを定義することで、並べ替え時に使用できます。MAP メソッドはオブジェクトをスカラ値にマッピングするために使用され、ORDER メソッドは2つのオブジェクトの大きさを比較するために使用されます。
   2. 並べ替えの方法を変更する：MAP または ORDER メソッドを定義できない場合は、他の方法で並べ替えることを検討してください。例えば、他の列を使用して並べ替える方法があります。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは V4.2.0 から導入されました。</p>
</main>

## ORA-22990：LOB locators cannot span transactions

* OceanBase エラーコード：9715

* SQLSTATE：HY000

* エラー原因：LOB ハンドルはトランザクション間での書き込みをサポートしていません。

* 解決策：LOB ハンドルを再代入した後に書き込みを実行してください。

## ORA-22998：CLOB or NCLOB in multibyte character set not supported


* OceanBase エラーコード：22998

* SQLSTATE：HY000

* エラーの原因：固定幅または可変幅の多バイト文字セット内の `CLOB` または `NCLOB` が SQL 文字関数に渡されましたが、その関数は多バイト `LOB` データをサポートしていません。

* 解決策：`DBMS_LOB` 関数（例：`DBMS_LOB.INSTR()` および `DBMS_LOB.SUBSTR()`）を使用するか、`PLSQL` の `DBMS_LOB.READ/WRITE` を使用して `LOB` データにアクセスします。

## ORA-23481：unexpected name string'%.\*s'


* OceanBase エラーコード：5932

* SQLSTATE：HY000

* エラーの原因：不正な名前文字。

## ORA-24323：value not allowed

* OceanBase エラーコード：7297

* SQLSTATE：HY000

* エラーの原因：パラメータに NULL 値または無効な値が渡されました。

* 解決策：すべての必須パラメータに正しい値が渡されていることを確認してください。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは V4.2.2 から導入されました。</p>
</main>

## ORA-24234：unable to get source of string \\'%.\*s\\'.\\'%.\*s\\', insufficient privileges or does not exist


* OceanBase エラーコード：5962

* SQLSTATE：HY000

* エラーの原因：`DBMS_PREPROCESSOR` サブプログラムで指定された PL/SQL オブジェクトが存在しない、またはそのソースコードを確認する権限がありません。

* 解決策：指定されたオブジェクトが存在し、そのソースコードを確認する権限があることを確認してください。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは V4.0.0 から導入されました。</p>
</main>

## ORA-24235：bad value for object type: %.\*s


* OceanBase エラーコード：5961

* SQLSTATE：HY000

* エラーの原因：指定されたオブジェクトのタイプが不適切です。

* 解決策：指定されたオブジェクトのタイプがパッケージ、パッケージ本体、プロシージャ、関数、トリガー、タイプ、タイプ本体のいずれかであることを確認してください。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは V4.0.0 から導入されました。</p>
</main>

## ORA-24236：source text is empty


* OceanBase エラーコード：5960

* SQLSTATE：HY000

* エラーの原因：`DBMS_PREPROCESSOR` サブプログラムに渡された入力ソーステキストが空です。

* 解決策：空でない入力ソーステキストを入力として渡します。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは V4.0.0 から導入されました。</p>
</main>

## ORA-24334：no descriptor for this position


* OceanBase エラーコード：5973

* SQLSTATE：HY000

* エラーの原因：アプリケーションがハンドルから無効な位置の記述子を取得しようとしました。

* 解決策：位置番号を確認してください。

## ORA-24381：error(s) in array DML


* OceanBase エラーコード：5883

* SQLSTATE：HY000

* エラーの原因：DML ステートメントの 1 行または複数の行が失敗しました。

* 解決策：エラー ハンドルのエラー スタックを参照してください。

## ORA-24761：transaction rolled back


* OceanBase エラーコード：6002

* SQLSTATE：40000

* エラーの原因：トランザクションがロールバックされました。

## ORA-24761：transaction rolled back: transaction context does not exist


* OceanBase エラーコード：6213

* SQLSTATE：40000

* エラーの原因：トランザクションがロールバックされ、トランザクション コンテキストが存在しません。

## ORA-24761：transaction rolled back: transaction is killed


* OceanBase エラーコード：6211

* SQLSTATE：25000

* エラー原因：トランザクションがロールバックされ、トランザクションが終了されました。

## ORA-24761：transaction rolled back: partition is frozen


* OceanBase エラーコード：6214

* SQLSTATE：25000

* エラー原因：トランザクションがロールバックされ、パーティションが凍結されました。

## ORA-24761：transaction rolled back: Transaction exiting


* OceanBase エラーコード：6223

* SQLSTATE：25000

* エラー原因：トランザクションがロールバックされ、トランザクションが存在します。

## ORA-24761：transaction rolled back: transaction needs rollback


* OceanBase エラーコード：6224

* SQLSTATE：25000

* エラー原因：トランザクションがロールバックされ、トランザクションがロールバックが必要です。

## ORA-24761：transaction rolled back: transaction idle timeout


* OceanBase エラーコード：6278

* SQLSTATE：25000

* エラー原因：トランザクションがロールバックされ、トランザクションがタイムアウトしました。

関連する問題ケース：

* [トランザクションがアイドルタイムアウト、エラーコード ORA-24761](https://www.oceanbase.com/knowledge-base/oceanbase-database-1000000000217853)

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードはV4.0.0バージョンから導入されました。</p>
</main>

## ORA-24761：transaction rolled back: transaction context count reach limit


* OceanBase エラーコード：6234

* SQLSTATE：25000

* エラー原因：トランザクションがロールバックされ、トランザクションのコンテキスト数が制限に達しました。

## ORA-25002 : cannot create INSTEAD OF triggers on tables


* OceanBase エラーコード：9572

* SQLSTATE：HY000

* エラー原因：`INSTEAD OF`トリガーはテーブルに作成できません。

* 解決策：ビューに `INSTEAD OF`トリガーを作成するか、テーブルに `DML`トリガーを作成してください。

## ORA-25004 : WHEN clause is not allowed in INSTEAD OF triggers


* OceanBase エラーコード：9571

* SQLSTATE：HY000

* エラー原因：INSTEAD OF トリガーでWHEN句を指定しました。

* 解決策：INSTEAD OF トリガーを作成する際にWHEN句を削除してください。

## ORA-25005 : cannot CREATE INSTEAD OF trigger on a read-only view


* OceanBase エラーコード：9663

* SQLSTATE：HY000

* エラー原因：`INSTEAD OF`トリガーは読み取り専用ビューに作成できません。

* 解決策：`INSTEAD OF`トリガーを読み取り専用でないビューに作成してください。

## ORA-25009 : Nested tableclause allowed only for INSTEAD OF triggers


* OceanBase エラーコード：9569

* SQLSTATE：HY000

* エラー原因：ネストテーブル句はINSTEAD OF トリガーにのみ許可されます。

* 解決策：ビューのネストテーブル列を使用してネストテーブルトリガーを定義してください。

## ORA-25021 : cannot reference a trigger defined on another table


* OceanBase エラーコード：9705

* SQLSTATE：HY000

* エラー原因：FOLLOWS句で参照しているトリガーは別のテーブルに定義されています。

* 解決策：参照するトリガーを同じテーブルに作成してください。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードはV4.0.0バージョンから導入されました。</p>
</main>

## ORA-25022 : cannot reference a trigger of a different type


* OceanBase エラーコード：9706

* SQLSTATE：HY000

* エラー原因：FOLLOWS または PRECEDES 句で参照しているトリガーのタイプが異なる、または結合トリガーではありません。

* 解決策：参照するトリガーを同じタイプまたは結合トリガーとして作成します。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは V4.0.0 から導入されました。</p>
</main>

## ORA-25023 : Cyclic trigger dependency is not allowed


* OceanBase エラーコード：9707

* SQLSTATE：HY000

* エラー原因：トリガーの依存関係にサイクルが形成されています。

* 解決策：トリガーの依存関係にサイクルが形成されないようにします。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは V4.0.0 から導入されました。</p>
</main>

## ORA-25025 : cannot specify PRECEDES clause


* OceanBase エラーコード：9708

* SQLSTATE：HY000

* エラー原因：通常のトリガーを作成する際に PRECEDES 句を指定しています。

* 解決策：PRECEDES 句を指定した通常のトリガーを作成しないようにします。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは V4.0.0 から導入されました。</p>
</main>

## ORA-25128：No insert/update/delete on table with constraint disabled and validated


* OceanBase エラーコード：5795

* SQLSTATE：HY000

* エラー原因：`DISABLE VALIDATE` 約束を持つテーブルに、挿入、更新、または削除操作を実行しようとしています。

* 解決策：制約の状態を変更します。

## ORA-25129：cannot modify constraint (%.\*s) - no such constraint


* OceanBase エラーコード：5800

* SQLSTATE：HY000

* エラー原因：このテーブルに指定された制約は存在しません。

## ORA-25137：Data value %s out of range in '%s'


* OceanBase エラーコード：4157

* SQLSTATE：22003

* エラー原因：変換後の演算子の値が変換先の値の範囲を超えています。

* 解決策：変換先の値の範囲を拡大します。

## ORA-25154：Column part of using clause can not have qualifier


* OceanBase エラーコード：5619

* SQLSTATE：HY000

* エラー原因：結合 ( `NATURAL` 結合または `USING` 句を使用する結合) に使用される列には、明示的な限定子を指定することはできません。

* 解決策：限定子を削除します。

## ORA-25156：old style outer join (+) cannot be used with ANSI joins


* OceanBase エラーコード：5625

* SQLSTATE：HY000

* エラー原因：クエリブロックで ANSI スタイルの結合を使用している場合、外部結合 `+` を指定する旧形式の表記は使用できません。

* 解決策：ANSI スタイルを使用して外部結合を指定します。

## ORA-25405：Transaction state unknown


* OceanBase エラーコード：6222

* SQLSTATE：HY000

* エラー原因：トランザクションのコミットを試行中に障害が発生しました。フェイルオーバーによってトランザクションの状態が自動的に特定される可能性はありません。

* 解決策：ユーザーが手動でトランザクションの状態を特定する必要があります。

## ORA-28001：the password has expired


* OceanBase エラーコード：4366

* SQLSTATE：HY000

* エラー原因：ユーザーのアカウントが期限切れとなり、パスワードの変更が必要です。

* 解決策：パスワードを変更するか、データベース管理者に連絡してください。

## ORA-28101：policy already exists

* OceanBaseエラー コード: 9718
* SQLSTATE：HY000
* エラーリasons：ポリシーが存在します。
* 解決策：ポリシーが追加されているかどうか、または異なるポリシー名を使用しているかどうか確認してください。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードはV4.1.0バージョンから導入されました。</p>
</main>

## ORA-28102：policy does not exist

* OceanBase エラー コード: 9719
* SQLSTATE：HY000
* エラーリasons: ストリームが存在しません。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは、バージョン V4.1.0 以降から導入されました。</p>
</main>

## ORA-28103：adding a policy to an object owned by SYS is not allowed

* OceanBase エラーコード：9720
* SQLSTATE：HY000
* エラーの原因：SYSが所有するオブジェクトにポリシーを追加することは許可されません。
* 解決策: この操作を実行することはできません。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは、V4.1.0 バージョンより導入されました。</p>
</main>

## ORA-28104：input value for %s is not valid

* OceanBaseエラーコード：9721
* SQLSTATE：HY000
* エラーの原因： パラメータの入力値が無効です。
* 解決策：有効な引数値を指定してください。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは V4.1.0 から導入されました。</p>
</main>

## ORA-28105：cannot create security relevant column policy in an object view

* OceanBase エラー番号：9722
* SQLSTATE：HY000
* エラーの原因： オブジェクトビューでセキュリティに関連する列ポリシーを作成できません。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは V4.1.0 から導入されました。</p>
</main>

## ORA-28106：input value for argument %s is not valid

* OceanBaseエラーコード：9723
* SQLSTATE：HY000
* エラーの原因：パラメータの入力値が無効または存在しない。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは、V4.1.0バージョンから導入されました。</p>
</main>

## ORA-28107：policy was disabled

* OceanBase エラーコード：9724
* SQLSTATE：HY000
* エラーの原因：ポリシーが無効化されています。
* 解決策：このポリシーを強制的に適用する場合、ポリシーを有効にしなければなりません。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは V4.1.0 で導入されました。</p>
</main>

## ORA-28108：circular security policies detected

* OceanBase エラー コード:9725
* SQLSTATE：HY000
* 誤りの原因：同じ対象に対するポリシーが相互に参照されています。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは V4.1.0バージョンから導入されました。</p>
</main>

## ORA-28109：the number of related policies has exceeded the limit of 16

* OceanBaseのエラーコード: 9726
* SQLSTATE：HY000
* エラーの原因：1つのオブジェクトに関連付けられたポリシーが多すぎる。
* 解決方法：1つまたは複数のポリシーを除外する。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは V4.1.0 から導入されました。</p>
</main>

## ORA-28110：policy function or package %.*s.%.*s has error

* OceanBase エラーコード：9727
* SQLSTATE：HY000
* エラー原因：ポリシー関数またはパッケージに誤りがあり、削除されたか、または有効ではありません。
* 解決策：機能の状態を確認し、問題を修正します。または、有効な機能を持つポリシーを再作成します。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは、V4.1.0バージョンから導入されました。</p>
</main>

## ORA-28111：insufficient privilege to evaluate policy predicate

* OceanBase エラーコード：9728
* SQLSTATE：HY000
* エラー原因：ポリシー関数のオーナーがアクセスする権限を持たないオブジェクトです。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは、V4.1.0バージョンから導入されました。</p>
</main>

## ORA-28112：failed to execute policy function

* OceanBase エラーコード：9729
* SQLSTATE：HY000
* エラー原因：ポリシー機能を実行できません。ポリシー機能の実行中に1つ以上のエラーが発生しました。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは、V4.1.0バージョンから導入されました。</p>
</main>

## ORA-28113：policy predicate has error

* OceanBase エラーコード：9730
* SQLSTATE：HY000
* エラー原因：ポリシープレディケートにエラーがあります。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは、V4.1.0バージョンから導入されました。</p>
</main>

## ORA-28116：insufficient privileges to do direct path access

* OceanBase エラーコード：9731
* SQLSTATE：HY000
* エラー原因：権限不足により、直接パスアクセスできません。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは、V4.1.0バージョンから導入されました。</p>
</main>

## ORA-28117：integrity constraint violated * parent record not found

* OceanBase エラーコード：9732
* SQLSTATE：HY000
* エラー原因：整合性制約が違反され、親レコードが見つかりません。
* 解決策：更新された外部キー値も親レコードに存在する必要があります。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは、V4.1.0バージョンから導入されました。</p>
</main>

## ORA-28118：policy group already exists

* OceanBase エラーコード：9733
* SQLSTATE：HY000
* エラー原因：ポリシー グループが既に存在します。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは、V4.1.0バージョンから導入されました。</p>
</main>

## ORA-28119：policy group does not exist

* OceanBase エラーコード：9734
* SQLSTATE：HY000
* エラー原因：ポリシー グループは存在しません。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは、V4.1.0バージョンから導入されました。</p>
</main>

## ORA-28120：driving context already exists

* OceanBase エラーコード：9735
* SQLSTATE：HY000
* エラー原因：ドライビング コンテキストが既に存在します。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは、V4.1.0バージョンから導入されました。</p>
</main>

## ORA-28121：driving context does not exist

* OceanBase エラーコード：9736
* SQLSTATE：HY000
* エラー原因：ドライバーコンテキストが存在しません。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは V4.1.0 バージョンから導入されました。</p>
</main>

## ORA-28122：can not update SYS_DEFAULT policy group

* OceanBase エラーコード：9737
* SQLSTATE：HY000
* エラー原因：SYS_DEFAULT ポリシーグループを更新できません。
* 解決策：SYS_DEFAULT ポリシーグループの作成または削除は行わないでください。この操作は許可されていません。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは V4.1.0 バージョンから導入されました。</p>
</main>

## ORA-28123：Driving context %.*s,%.*s contains invalid group %.*s

* OceanBase エラーコード：9738
* SQLSTATE：HY000
* エラー原因：ドライバーコンテキストに無効なグループが含まれています。
* 解決策：初期化時に有効なポリシーグループを DBMS_SESSION.SET_CONTEXT に渡すようにします。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは V4.1.0 バージョンから導入されました。</p>
</main>

## ORA-28124：Column %.*s in sec_relevant_cols cannot be of an object data type

* OceanBase エラーコード：9739
* SQLSTATE：HY000
* エラー原因：`sec_relevant_cols` に指定された列はオブジェクトデータ型または XMLType にできません。
* 解決策：`sec_relevant_cols` に指定された列がオブジェクトデータ型または XMLType でないことを確認してください。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは V4.1.0 バージョンから導入されました。</p>
</main>

## ORA-28125：A protected base column was referenced in an unprotected virtual column expression

* OceanBase エラーコード：9740
* SQLSTATE：HY000
* エラー原因：保護されていない仮想列式で保護されたベース列が参照されました。
* 解決策：ポリシーの定義を確認し、仮想列も保護されていることを確認してください。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは V4.1.0 バージョンから導入されました。</p>
</main>

## ORA-28126：Attribute association failed for policy %.*s

* OceanBase エラーコード：9741
* SQLSTATE：HY000
* エラー原因：ポリシーの属性アソシエーションに失敗しました。
* 解決策：ローカルアプリケーションコンテキスト属性を指定してください。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは V4.1.0 バージョンから導入されました。</p>
</main>

## ORA-28132：The MERGE INTO syntax does not support the security policy

* OceanBase エラーコード：9742
* SQLSTATE：HY000
* エラー原因：MERGE INTO はセキュリティポリシーをサポートしません。
* 解決策：
  * セキュリティポリシーを変更する権限がない場合は、既にセキュリティポリシーが定義されているテーブルに対して、INSERT、UPDATE、DELETE DML ステートメントを使用してください。
  * セキュリティポリシーを変更する権限がある場合は、statement_types にすべての DML ステートメント (INSERT、UPDATE、DELETE) を含むように再定義するか、このポリシーにセキュリティ関連の列がないことを確認してください。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは V4.1.0 バージョンから導入されました。</p>
</main>

## ORA-28115：policy with check option violation


* OceanBase エラーコード：5789

* SQLSTATE：HY000

* エラー原因：データのセキュリティマーカーが誤っています。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは V4.0.0 バージョンから導入されました。</p>
</main>

## ORA-28106：input value for argument #%d is not valid


* OceanBase エラーコード：9687

* SQLSTATE：HY000

* エラー原因：入力パラメータが無効です。

## ORA-28264：Client identifier is too long


* OceanBase エラーコード：9688

* SQLSTATE：HY000

* エラー原因：クライアントIDの上限64バイトを超えています。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは V3.2.4 バージョンから導入されました。</p>
</main>

## ORA-28265：Namespace beginning with 'SYS_' is not allowed


* OceanBase エラーコード：9690

* SQLSTATE：HY000

* エラー原因：作成された `context namespace` に `'SYS_'` 前綴辞を付けることはできません。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは V3.2.4 バージョンから導入されました。</p>
</main>

## ORA-28267：The maximum size specified by the _session_context_size parameter was exceeded


* OceanBase エラーコード：9691

* SQLSTATE：HY000

* エラー原因：`session based context` のキー値対の数が `_session_context_size` で指定された上限を超えています。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは V3.2.4 バージョンから導入されました。</p>
</main>

## ORA-28268：Invalid NameSpace Value


* OceanBase エラーコード：9689

* SQLSTATE：HY000

* エラー原因：`context namespace namespace` の名前が無効です。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは V3.2.4 バージョンから導入されました。</p>
</main>

## ORA-29253：Invalid count argument passed to procedure dbms_sql.define_array


* OceanBase エラーコード：9679

* SQLSTATE：HY000

* エラー原因：パッケージ `dbms_sql` のプロシージャ define_array に渡された `count` パラメータの値が無効です。無効な値は負の数とNULLです。パラメータは正の整数でなければなりません。

* 解決策：PL/SQLプログラムを修正して、配列を定義する際に有効なパラメータのみを渡すようにし、再試行してください。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは V4.0.0 バージョンから導入されました。</p>
</main>

## ORA-29257：host %.\*s unknown

* OceanBase エラーコード：5928

* SQLSTATE：HY000

* エラー原因：指定されたホストは存在しません。

* 解決策：ホスト名またはIPアドレスのスペルを確認し、有効なホスト名またはIPアドレスであることを確認してください。

## ORA-29258：buffer too small

* OceanBase エラーコード：9785

* SQLSTATE：HY000

* エラー原因：TCPネットワーク通信を行う際に、操作対象のバッファサイズが読み取りまたは書き込みに必要なデータ量を収容するには不十分です。

* 解決策：バッファサイズを拡大してください。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは V4.2.5 BP1 バージョンから導入されました。</p>
</main>

## ORA-29259：end-of-input reached

* OceanBase エラーコード：9786

* SQLSTATE：HY000

* エラー原因：このエラーは以下のいずれかの原因により発生する可能性があります。

  * ネットワークまたはサーバー側の中断。
  * セキュリティプロトコルの互換性の問題。
  * 要求またはデータ形式の問題。
  * データベース側のリソースまたはタイムアウト制限。

* 解決策：

  * ネットワーク接続とターゲットサービスの状態を確認します。
  * セキュリティプロトコルの互換性を確認し、必要に応じてアップグレードします。
  * データベースのネットワーク設定とタイムアウトを調整します。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは、V4.2.5 BP1バージョンから導入されました。</p>
</main>

## ORA-29260：network error: %.*s

* OceanBase エラーコード：9787

* SQLSTATE：HY000

* エラー原因：ネットワークエラー。

* 解決策：ネットワーク設定を確認します。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは、V4.2.5 BP1バージョンから導入されました。</p>
</main>

## ORA-29261：bad argument

* OceanBase エラーコード：5951、5968、9788

* SQLSTATE：HY000

* エラー原因：PL APIに不正な引数が渡されました。

* 解決策：PL APIに渡された引数を確認し、再試行します。

## ORA-29275：partial multibyte character

* OceanBase エラーコード：9789

* SQLSTATE：HY000

* エラー原因：データベースがデータ処理中に不完全なマルチバイト文字シーケンスに遭遇しました。

* 解決策：文字セット設定を確認します。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは、V4.2.5 BP1バージョンから導入されました。</p>
</main>

## ORA-29276：transfer timeout

* OceanBase エラーコード：9790

* SQLSTATE：HY000

* エラー原因：転送タイムアウト。

* 解決策：ネットワーク接続の問題を確認します。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは、V4.2.5 BP1バージョンから導入されました。</p>
</main>

## ORA-24274：network access denied by access control list (ACL)

* OceanBase エラーコード：9791

* SQLSTATE：HY000

* エラー原因：アクセス制御リスト（ACL）によってネットワークアクセスが拒否されました。

* 解決策：ACLの権限を設定します。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは、V4.2.5 BP1バージョンから導入されました。</p>
</main>

## ORA-30678：too many open connections

* OceanBase エラーコード：9792

* SQLSTATE：HY000

* エラー原因：`UTL_TCP`システムパッケージを使用して外部TCP接続を確立する際、許可された最大同時接続数を超えました。

* 解決策：同時接続数が最大同時接続数を超えないようにします。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは、V4.2.5 BP1バージョンから導入されました。</p>
</main>

## ORA-53203：security violation

* OceanBase エラーコード：9793

* SQLSTATE：HY000

* エラー原因：セキュリティ違反。

* 解決策：データベースセキュリティ製品の設定を確認します。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは、V4.2.5 BP1バージョンから導入されました。</p>
</main>

## ORA-29277：invalid SMTP operation

* OceanBase エラーコード：9794

* SQLSTATE：HY000

* エラー原因：SMTP操作が無効です。

* 解決策：SMTPプロトコルコマンドの呼び出し順序とメールデータ形式のコンプライアンスを確認します。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは、V4.2.5 BP1バージョンから導入されました。</p>
</main>

## ORA-29278：SMTP transient error: %.*s

* OceanBase エラーコード：9795

* SQLSTATE：HY000

* エラー原因：SMTP通信で一時的な障害が発生しました。

* 解決策：SMTPサーバーの情報を確認し、ネットワークや環境の制限をチェックしてください。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは V4.2.5 BP1 から導入されました。</p>
</main>

## ORA-29279：SMTP permanent error: %.*s

* OceanBase エラーコード：9796

* SQLSTATE：HY000

* エラー原因：データベースのメール機能（UTL_SMTPパッケージなど）を使用する際に、メール送信を妨げる永続的な障害が発生しました。

* 解決策：エラーの詳細から、アドレスエラー、認証失敗、サーバー側のポリシー拒否、またはネットワーク接続不良のいずれかであるかを判断してください。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは V4.2.5 BP1 から導入されました。</p>
</main>

## ORA-24249：Authentication scheme '%.*s' not supported

* OceanBase エラーコード：9797

* SQLSTATE：HY000

* エラー原因：認証方式がサポートされていません。

* 解決策：サーバーがサポートする認証方式を確認してください。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは V4.2.5 BP1 から導入されました。</p>
</main>

## ORA-24250：No supported authentication scheme found

* OceanBase エラーコード：9798

* SQLSTATE：HY000

* エラー原因：サポートされている認証方式が見つかりません。

* 解決策：OceanBaseクライアントの環境設定を確認してください。特定のSMTPなどのシナリオで問題が発生する場合は、クライアントがリクエストする認証方式がサーバー側で公告されている可用方式と一致しているか確認してください。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは V4.2.5 BP1 から導入されました。</p>
</main>

## ORA-29295：invalid mime header tag


* OceanBase エラーコード：5970

* SQLSTATE：HY000

* エラー原因：`mime` ヘッダーのタグをスキャンする際にエラーが発生しました。

* 解決策：ソースデータが有効な `mime` ヘッダ文字列であり、形式が charset/encoding/encoded string であるかどうかを確認してください。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このエラーコードは V4.0.0 から導入されました。</p>
</main>

## ORA-29357：%s %.\*s already exists


* OceanBase エラーコード：4702

* SQLSTATE：HY000

* エラー原因：プログラム `CREATE_PLAN` 、`CREATE_CDB_PLAN`、`CREATE_CONSUMER_GROUP`、またはパッケージ `DBMS_RESOURCE_MANAGER` の `CREATE_CATEGORY` のパラメータ名が既に使用されています。

* 解決策：使用されていないパラメータ名を指定してください。

## ORA-29358：resource plan %.\*s does not exist


* OceanBase エラーコード：4703

* SQLSTATE：HY000

* エラー原因：パッケージ `DBMS_RESOURCE_MANAGER` の `UPDATE_PLAN` または `UPDATE_CDB_PLAN` のパラメータとして、存在しないプラン名を指定しました。

* 解決策：存在するプラン名を指定してください。

## ORA-29361：value %ld for %s is outside valid range of 0 to 100


* OceanBase エラーコード：4704

* SQLSTATE：HY000

* エラー原因：プラン指令パラメータに有効な値が指定されていません。

* 解決策：`0` から `100` の範囲内の値（`0` と `100` を含む）を指定してください。

## ORA-29362：plan directive %.\*s, %.\*s does not exist


* OceanBase エラーコード：4705

* SQLSTATE：HY000

* エラー原因：プログラム `DBMS_RESOURCE_MANAGER` の `UPDATE_PLAN_DIRECTIVE` または `UPDATE_CDB_PLAN_DIRECTIVE` に存在しないプランディレクティブが指定されました。

* 解決策：既存のプランディレクティブを指定してください。

## ORA-29364：plan directive %.\*s, %.\*s already exists


* OceanBase エラーコード：4706

* SQLSTATE：HY000

* エラー原因：既に存在するプランディレクティブを作成しようとした。

* 解決策：他の値を使用して作成操作を再試行してください。

## ORA-29366：plan directive name '%.\*s' not supported


* OceanBase エラーコード：4707

* SQLSTATE：HY000

* エラー原因：サポートされていないプランディレクティブの名前。

## ORA-29368：consumer group %.\*s does not exist


* OceanBase エラーコード：4710

* SQLSTATE：HY000

* エラー原因：存在しない消費者グループが指定されました。

* 解決策：存在する消費者グループを指定してください。

## ORA-29471：DBMS_SQL access denied


* OceanBase エラーコード：5856

* SQLSTATE：HY000

* エラー原因：セキュリティ上の理由により、`DBMS_SQL` のアクセスが拒否されました。

* 解決策：アラートログとトレースファイルからさらに情報を取得してください。

## ORA-29474：DBMS_SQL.OPEN_CURSOR failed. security_level of 0 is not allowed


* OceanBase エラーコード：9677

* SQLSTATE：HY000

* エラー原因：`DBMS_SQL.OPEN_CURSOR` で指定されたセキュリティレベルが 0 です。

* 解決策：有効なセキュリティレベルを指定してください。
