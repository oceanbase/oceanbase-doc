|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||
|machine-translation|ja-marker|
|slug|observer-memory-management-overview|

# メモリ管理の概要

OceanBaseデータベースはマルチテナント設計のデータベースであり、同一プロセス内で複数のテナントのリクエストが実行されます。テナントのリソース割り当てには、500テナントメモリ、システムテナントメモリ、業務テナントメモリの3種類があります。

* 500は特殊な仮想テナントであり、共有型であり、物理的なテナントではないため、消費されるメモリはすべてOceanBaseデータベースによって500テナントに割り当てられます。

* システムテナントは、OceanBaseデータベースが自動的に作成する最初の物理テナントであり、クラスタ関連の内部テーブルを管理しています。これらの内部テーブル上で発生するリクエストによってトリガーされるメモリは、sysテナントに割り当てられます。

* 業務テナントとは、クラスタのインストール後にDBAが作成し、業務トラフィックを処理する物理テナントのことです。これらのテナントで発生するリクエストによってトリガーされるメモリは、各テナント自身に割り当てられます。