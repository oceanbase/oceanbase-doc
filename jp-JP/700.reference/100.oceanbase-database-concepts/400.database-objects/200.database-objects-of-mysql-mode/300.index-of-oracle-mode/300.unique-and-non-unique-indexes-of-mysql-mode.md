|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type|MySQL Mode|
|machine-translation|ja-marker|
|slug|unique-and-non-unique-indexes-of-mysql-mode|

# 一意インデックスと非一意インデックス

インデックステーブルは、一意インデックスと非一意インデックスに分類されます。一意インデックスでは、テーブル内のインデックス列に完全に同一の値を持つ2行が存在しないことを保証しますが、非一意インデックスでは完全に同一の値が存在する可能性があります。OceanBaseデータベースでは、NULL値もインデックスに格納されます。

​非一意インデックスの場合、インデックステーブルのストレージキーはユーザーが指定したインデックス列と主テーブルの主キーです。一方、一意インデックス列の場合、インデックステーブルのストレージキーはユーザーが指定したインデックス列と変動する主テーブルの主キー列であり、ここで変動とは主キー列の値がインデックス列の値によって異なることを意味します。

以下の例のように、非一意インデックステーブル `i1` のストレージキーは `c2` と `c1` であり、一意インデックステーブル `i2` のストレージキーは `c3` で、`c1` は変動します。`c3` が `NULL` の場合、変動する `c1` 列の値は主テーブル `c1` 列の値となります。`c3` が `NULL` でない場合、変動する `c1` 列の値は `NULL` となります。

```sql
CREATE TABLE t1(c1 INT, c2 INT, c3 INT, c4 INT, PRIMARY KEY(c1));
CREATE INDEX i1 ON t1(c2);
CREATE UNIQUE INDEX i2 ON t1(c3);
```