|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type|Oracle Mode|
|machine-translation|ja-marker|
|slug|oracle-compatible-view-of-oracle-mode|

# Oracle互換ビュー

OceanBaseデータベースのOracleモードにおける関連ビューの情報を紹介します。

## 保存場所

Oracle互換ビューはSYS配下にあります。

## ビューの分類

OceanBaseデータベースのOracleモードにおけるデータディクショナリは、システムテーブルとディクショナリビューで構成されており、データベースの重要な部分です。

### データディクショナリビュー

データディクショナリビューには、以下の情報が含まれますが、これに限定されません：

* データベース内の各Schemaオブジェクトの定義、列のデフォルト値や整合性制約情報を含みます。
* Schemaオブジェクトに割り当てられたストレージ容量と現在使用中のストレージ容量。
* データベースユーザーの名前、ユーザーに付与された権限とロール、およびユーザーに関連する監査情報。

データディクショナリビューの構成は以下の通りです：

* SYS.ALL_*

  `SYS.ALL_*` をプレフィックスとするビューは、ユーザーがデータベース全体を概観するために使用されます。ユーザーが所有するSchemaオブジェクトに加えて、これらのビューは公開または明示的にユーザーに権限を付与されたSchemaオブジェクトも返します。

* SYS.DBA_*

  `SYS.DBA_*` をプレフィックスとするビューは、データベース全体のすべての関連情報を表示します。`SYS.DBA_*` ビューには管理者権限でアクセスする必要があります。

* SYS.USER_*
  * ユーザーのプライベート環境をデータベース内で参照し、ユーザーが作成したSchemaオブジェクトやユーザーが付与した権限などのメタデータを含みます。
  * ユーザーに関連する行のみを表示し、`SYS.ALL_*` ビューの情報のサブセットを返します。
  * 他のビューと同じ列を持ちますが、暗黙の列 `OWNER` が含まれます。

### 動的性能ビュー

動的性能ビューには `SYS.V$*` と `SYS.GV$*` が含まれます：

* SYS.V$*

  クライアントが接続するノードの実行状態とパフォーマンス指標情報を表示するために使用されます。この種のビューは、クライアントが接続するノードの状態情報を提供するためにのみ使用できます。他のノードの状態情報にアクセスする必要がある場合は、`SYS.GV$*` ビューを使用してください。

* SYS.GV$*

  クラスタ全体の実行状態とパフォーマンス指標情報を表示するために使用されます。この種のビューはクラスタ全体でアクセス可能であり、グローバルな状態情報を提供します。
  アクセス権限によって以下のように区別されます：

  * SYSテナントからアクセスする場合、すべてのテナントの情報が表示されます。
  * 通常のテナントからアクセスする場合、そのテナントの情報が表示されます。

## 関連ドキュメント

* [ビューの概要](100.view-overview-of-oracle-mode.md)
* [OceanBase独自のビュー](200.oceanBase-own-view-of-oracle-mode.md)