|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type|Oracle Mode|
|slug|uniqueness-constraint-oracle-compatible-mode|

# 一意性制約

一意キー（UNIQUE key）制約は、一意の列値（Unique Column Value）に関するルールであり、特定の列または列ファミリに定義されます。その役割は、挿入または更新されるデータ行の列または列ファミリの値が一意であることを保証し、テーブル内の任意の2行における特定の列または列セットの値が重複しないようにすることです。

本記事では、以下の具体的な例を通じて、一意性制約の主な特性を説明します。

```sql
obclient> CREATE TABLE t1(c1 INT, c2 INT, CONSTRAINT t1_uk_c1_c2 UNIQUE(c1, c2));
Query OK, 0 rows affected

obclient> INSERT INTO t1 VALUES(1, 1);
Query OK, 1 row affected

obclient> INSERT INTO t1 VALUES(1, 1);
OBE-00001: unique constraint '1-1' for key 'T1_UK_C1_C2' violated

obclient> INSERT INTO t1 VALUES(null, 1);
Query OK, 1 row affected

obclient> INSERT INTO t1 VALUES(null, 1);
OBE-00001: unique constraint 'NULL-1' for key 'T1_UK_C1_C2' violated

obclient> INSERT INTO t1 VALUES(null, null);
Query OK, 1 row affected

obclient> INSERT INTO t1 VALUES(null, null);
Query OK, 1 row affected
```

また、下図のように、`dept` テーブルの `dname` 列に `UNIQUE` キー制約を定義すると、このテーブルに重複する部門名の存在は許可されませんが、ユーザーは `dname` 列にNULL値を挿入することができます。`dname` 列に `NOT NULL` 制約も定義している場合は、ユーザーは `dname` 列にNULL値を挿入することができません。

<!-- ![1](https://obbusiness-private.oss-cn-shanghai.aliyuncs.com/doc/img/observer-enterprise/V4.2.1/700.reference/100.oceanbase-database-concepts/800.data-integrity-of-oracle-mode/300.uniqueness-constraint-of-oracle-mode/1.uniqueness_constraint1.png) -->

`UNIQUE` キーが複数の列で構成される場合、このデータ列のグループは複合一意キー（Composite Unique Key）と呼ばれます。下図のように、`customer` テーブルで定義された `UNIQUE` キー制約は複合一意キーを使用しており、`area` と `phone` の2列を含みます。これは、任意の2行におけるキーの値が重複してはならないことを要求します。ユーザーは列にNULL値を入力することができます。ただし、列に `NOT NULL` 制約も定義している場合は、NULL値の入力は許可されません。

ユーザーは `customer` テーブルに任意のレコードを挿入できますが、上述の `UNIQUE` キー制約の制限により、テーブル内の各行のエリアコード（Area Code）と電話番号（Telephone Number）の組み合わせは重複してはなりません。これにより、不注意による電話番号の重複問題を回避できます。

<!-- ![一意性制約2](https://obbusiness-private.oss-cn-shanghai.aliyuncs.com/doc/img/observer/V4.3.3/700.reference/100.oceanbase-database-concepts/400.database-objects/%E5%94%AF%E4%B8%80%E7%BA%A6%E6%9D%9F-%E5%9B%BE2-20241113.jpg) -->