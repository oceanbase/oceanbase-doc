|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|slug|refresh-obproxy-configuration|

# OBProxy設定のリフレッシュ

本記事では、OBProxyの設定をリフレッシュする方法について説明します。

## SQLステートメントによるOBProxy設定のリフレッシュ

`ALTER PROXYCONFIG` ステートメントを使用して、個々のOBProxyの設定をリフレッシュできます。

1. `root@proxysys` アカウントを使用してOBProxyに接続します。

2. 以下のコマンドを実行し、リフレッシュ対象のパラメータ値を確認します。

   例：

   ```sql
   obclient> SHOW PROXYCONFIG LIKE 'fetch_proxy_bin_random_time';
   ```

   `SHOW PROXYCONFIG` ステートメントを使用して、現在のOBProxyのすべてのパラメータを確認することもできます。

3. 以下のコマンドを実行して、パラメータの値を変更します。

   ```sql
   obclient> ALTER PROXYCONFIG SET fetch_proxy_bin_random_time='300s';
   ```