| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |
|machine-translation|en,ja-marker|
|slug|clear-balance-weight-mysql|

# CLEAR_BALANCE_WEIGHT

`CLEAR_BALANCE_WEIGHT`は、パーティションの重みをクリアするために使用されます。

## 制限事項と注意点

* V4.2.5バージョンでは、V4.2.5 BP2以降でパーティションの重みをクリアする機能がサポートされています。

* ユーザーテナントのみが`CLEAR_BALANCE_WEIGHT`サブルーチンを呼び出してパーティションの重みをクリアできます。システムテナントはパーティションの重みをクリアすることができません。

* テーブルレベルのパーティションの重みをクリアすると、すべてのパーティションに設定されたテーブルレベルの重みが削除されます。そのテーブルにパーティションレベルの重みが設定されている場合、システムはパーティションレベルの重みを保持します。

## 構文

```sql
DBMS_BALANCE.CLEAR_BALANCE_WEIGHT(
    database_name       VARCHAR(65535),
    table_name          VARCHAR(65535),
    partition_name      VARCHAR(65535) DEFAULT NULL,
    subpartition_name   VARCHAR(65535) DEFAULT NULL);
```

## パラメータの説明

| **パラメータ**         | **説明**                                            |
|------------------|-----------------------------------------------------|
| database_name    | テーブルが属するデータベース名  |
| table_name       | テーブル名             |
| partition_name   | パーティション名        |
| subpartition_name| サブパーティション名。構文はサポートされていますが、現在サブパーティションの重み付け設定はサポートされていません。        |

## 例

1. ユーザーの`test`データベースに、パーティション重みが設定されたテーブル`tbl1_h`があるとします。

    パーティションの重み情報を照会するステートメントは次のとおりです：

    ```shell
    obclient[test]> SELECT * FROM oceanbase.DBA_OB_OBJECT_BALANCE_WEIGHT;
    ```

    クエリ結果は次のとおりです：

    ```shell
    +----------+--------------+-----------------+--------+---------------+------------+----------------+-------------------+-----------------+-------------+---------------+-----------+
    | TABLE_ID | PARTITION_ID | SUBPARTITION_ID | WEIGHT | DATABASE_NAME | TABLE_NAME | PARTITION_NAME | SUBPARTITION_NAME | TABLEGROUP_NAME | DATABASE_ID | TABLEGROUP_ID | OBJECT_ID |
    +----------+--------------+-----------------+--------+---------------+------------+----------------+-------------------+-----------------+-------------+---------------+-----------+
    |   500002 |         NULL |            NULL |      1 | test          | tbl1_h     | NULL           | NULL              | NULL            |      500001 |          NULL |    500002 |
    |   500002 |       500003 |            NULL |      2 | test          | tbl1_h     | p0             | NULL              | NULL            |      500001 |          NULL |    500003 |
    +----------+--------------+-----------------+--------+---------------+------------+----------------+-------------------+-----------------+-------------+---------------+-----------+
    2 rows in set
    ```

2. テーブル`tbl1_h`のテーブルレベルのパーティション重みをクリアします。

    ```shell
    obclient[test]> CALL DBMS_BALANCE.CLEAR_BALANCE_WEIGHT('test','tbl1_h');
    ```

3. クリア後、テーブルに設定されたパーティションの重み情報を照会します。

    ```shell
    obclient[test]> SELECT * FROM oceanbase.DBA_OB_OBJECT_BALANCE_WEIGHT;
    ```

    クエリ結果は次のとおりです：

    ```shell
    +----------+--------------+-----------------+--------+---------------+------------+----------------+-------------------+-----------------+-------------+---------------+-----------+
    | TABLE_ID | PARTITION_ID | SUBPARTITION_ID | WEIGHT | DATABASE_NAME | TABLE_NAME | PARTITION_NAME | SUBPARTITION_NAME | TABLEGROUP_NAME | DATABASE_ID | TABLEGROUP_ID | OBJECT_ID |
    +----------+--------------+-----------------+--------+---------------+------------+----------------+-------------------+-----------------+-------------+---------------+-----------+
    |   500002 |       500003 |            NULL |      2 | test          | tbl1_h     | p0             | NULL              | NULL            |      500001 |          NULL |    500003 |
    +----------+--------------+-----------------+--------+---------------+------------+----------------+-------------------+-----------------+-------------+---------------+-----------+
    1 row in set
    ```

4. テーブル`tbl1_h`のパーティション`p0`のパーティション重みをクリアします。

    ```shell
    obclient[test]> CALL DBMS_BALANCE.CLEAR_BALANCE_WEIGHT('test','tbl1_h','p0');
    ```

5. クリア後、再度テーブルに設定されたパーティションの重み情報を照会します。

    ```shell
    obclient[test]> SELECT * FROM oceanbase.DBA_OB_OBJECT_BALANCE_WEIGHT;
    ```

    クエリ結果は空です。

## 関連ドキュメント

* [テナント内の均衡](../../../../../100.oceanbase-database-concepts/500.distributed-database-objects/300.data-partitions-and-replicas/500.data-balancing/100.intra-tenant-balancing.md)

* [パーティション重みの設定](../../../../../../600.manage/300.replica-management/500.load-balancing-manage/450.balance-weight-management/100.set-the-balance-weight-for-a-table.md)

* [パーティション重みのクリア](../../../../../../600.manage/300.replica-management/500.load-balancing-manage/450.balance-weight-management/300.clear-the-balance-weight.md)