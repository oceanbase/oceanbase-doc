| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |
|machine-translation|en,ja-marker|
|slug|delete_consumer_group-mysql-mode|

# DELETE_CONSUMER_GROUP

`DELETE_CONSUMER_GROUP`プロシージャは、リソース使用グループを削除するために使用されます。

## 構文

```sql
DBMS_RESOURCE_MANAGER.DELETE_CONSUMER_GROUP (
    consumer_group IN VARCHAR2);
```

## パラメータの説明

`consumer_group`は、削除対象の使用グループ名を指定します。

## 例

```sql
obclient> CALL DBMS_RESOURCE_MANAGER.DELETE_CONSUMER_GROUP ('group1');
Query OK, 0 rows affected
```