| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
|machine-translation|en,ja-marker|
|slug|fflush-oracle|

# FFLUSH

`FFLUSH` ステートメントは、キャッシュ内のデータをファイルにフラッシュするために使用されます。

  <main id="notice" >
    <h4>適用対象</h4>
    <p>この内容はOceanBaseデータベースEnterprise Editionにのみ適用されます。OceanBaseデータベースCommunity EditionはMySQLモードのみを提供します。</p>
  </main>

## 構文

```sql
UTL_FILE.FFLUSH (
file  IN FILE_TYPE);
```

## パラメータの説明

| パラメータ | 説明 |
|------|----------------------------------------|
| file | `FOPEN`または`FOPEN_NCHAR`呼び出しによって返されるアクティブなファイルハンドル。 |

## 例外

* `INVALID_MAXILINESIZE`

* `INVALID_OPERATION`

* `INVALID_FILENAME`

* `WRITE_ERROR`

## 使用方法

`FFLUSH`は、行終了文字で終了するデータのみをファイルに書き込みます。行終了文字で終了していない場合、データはファイルに書き込まれず、バッファに残ります。

`FFLUSH`はファイルに対する`w`権限が必要であり、そうでない場合は`INVALID_OPERATION`例外が報告されます。バッファ内のデータ長が`FOPEN`で指定された最大行長を超えると、`INVALID_MAXILINESIZE`例外が報告されます。