| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
|machine-translation|en,ja-marker|
|slug|utl_file-overview-oracle-compatible-mode|

# UTL_FILEの概要

`UTL_FILE` システムパッケージを使用することで、PLプログラムはオペレーティングシステムのテキストファイルを読み書きできます。`UTL_FILE` は、オペレーティングシステムのストリームファイルI/Oの制限されたバージョンを提供します。

  <main id="notice" >
    <h4>機能の適用範囲</h4>
    <p>この内容はOceanBaseデータベースEnterprise Editionにのみ適用されます。OceanBaseデータベースCommunity EditionはMySQLモードのみを提供します。</p>
  </main>

## UTL_FILE権限の説明

ユーザーは `UTL_FILE` システムパッケージに対する `EXECUTE` 権限が必要です。

## UTL_FILEデータ構造

`UTL_FILE` システムパッケージは、`FILE_TYPE` という `RECORD` 型を定義しています。

`FILE_TYPE` の内容は `UTL_FILE` システムパッケージのプライベートな内容であるため、参照や変更はできません。

### 構文

```sql
TYPE file_type IS RECORD (
id                BINARY_INTEGER,
datatype          BINARY_INTEGER,
byte_mode         BOOLEAN);
```

### パラメータの説明

|    パラメータ     |                説明                 |
|-----------|-----------------------------------|
| id        | 内部ファイルハンドル番号の数値。                      |
| datatype  | ファイルが `CHAR` ファイル、`Nchar` ファイル、またはその他（バイナリ）かどうかを示す。 |
| byte_mode | ファイルをバイナリファイルとして開くか、テキストファイルとして開くかを示す。           |

## UTL_FILEの例外
|        例外名         |         説明                     |

|---------------------|---------------------------------|
| INVALID_PATH        | ファイルの場所が無効です。        |
| INVALID_MODE        | `FOPEN` 関数の `open_mode` パラメータが無効です。   |
| INVALID_FILEHANDLE  | ファイルハンドルが無効です。   |
| INVALID_OPERATION   | サポートされていないファイル操作です。    |
| READ_ERROR          | 読み取りキャッシュが小さすぎるか、読み取り中にオペレーティングシステム関連のエラーが発生しました。    |
| WRITE_ERROR         | 書き込み中にオペレーティングシステム関連のエラーが発生しました。 |
| INTERNAL_ERROR      | 内部定義のPLエラー。   |
| FILE_OPEN           | ファイルを開く際にエラーが発生しました。  |
| INVALID_MAXLINESIZE | `FOPEN` 関数のパラメータ `max_linesize` が無効です。サポートされている範囲は \[1,32767\] です。 |
| INVALID_FILENAME    | パラメータファイル名が無効です。 |
| ACCESS_DENIED       | 権限不足のためファイルにアクセスできません。  |
| INVALID_OFFSET      | 考えられる原因は以下の通りです：<ul><li> `ABSOLUTE_OFFSET` と `RELATIVE_OFFSET` の両方が `NULL` の場合    </li><li> `ABSOLUTE_OFFSET` \< 0    </li><li> `ABSOLUTE_OFFSET` が `NULL` の場合、指定された `RELATIVE_OFFSET` がファイルの長さを超えている場合 </li></ul>   |
## UTL_FILEサブプログラムの概要

次の表は、OceanBaseデータベースの現在のバージョンでサポートされている `UTL_FILE` サブプログラムとその簡単な説明です。

|                           サブプログラム                           |                説明                 |

|---------------------------------------------------------|-----------------------------------|
| [FCLOSE](../26100.utl-file-oracle/200.fclose-oracle.md)   | ファイルを閉じます。                             |
| [FFLUSH](../26100.utl-file-oracle/300.fflush-oracle.md)   | バッファ内のデータをファイルにフラッシュします。                     |
| [FOPEN](../26100.utl-file-oracle/400.fopen-oracle.md)    | ユーザーが指定した情報に基づいてファイルを開きます。                    |
| [FGETPOS](../26100.utl-file-oracle/500.fgetpos-oracle.md)  | ファイルの現在のアクセス位置（オフセット）をバイト単位で取得します。         |
| [F_REMOVE](../26100.utl-file-oracle/600.fremove-oracle.md)  | ディスク上のファイルを削除します（対応する削除権限が必要です）。UNIXの `rm` コマンドに類似しています。 |
| [FRENAME](../26100.utl-file-oracle/700.frename-oracle.md)  | 既存のファイルを新しい名前にリネームします。UNIXの `mv` コマンドに類似しています。    |
| [FSEEK](../26100.utl-file-oracle/800.fseek-oracle.md)    | 指定されたバイト数だけファイルの位置を前方または後方に調整します。               |
| [NEW_LINE](../26100.utl-file-oracle/900.new-line-utl-oracle.md) | 1つ以上のOS指定の行終端文字をファイルに書き込みます。            |
| [GET_LINE](../26100.utl-file-oracle/1000.get-line-utl-oracle.md) | 1行のテキストを読み取ります。                           |
| [PUT](../26100.utl-file-oracle/1100.put-utl-oracle.md)      | 文字列をファイルに書き込みます。                         |
| [PUT_LINE](../26100.utl-file-oracle/1200.put-line-utl-oracle.md) | 1行をファイルに書き込み、OS指定の行終端文字を追加します。           |
| [PUT_LINE](../26100.utl-file-oracle/1200.put-line-utl-oracle.md) | 将一行写入文件，并附加操作系统指定的行终止符。           |