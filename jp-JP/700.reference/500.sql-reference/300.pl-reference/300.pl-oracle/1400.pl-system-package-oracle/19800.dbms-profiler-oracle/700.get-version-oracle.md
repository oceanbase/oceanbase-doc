| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
|machine-translation|en,ja-marker|
|slug|get-version-oracle|

# GET_VERSION

`GET_VERSION`は、現在の`DBMS_PROFILER`のバージョンを返すために使用されます。

  <main id="notice" >
    <h4>適用対象</h4>
    <p>この内容はOceanBaseデータベースEnterprise Editionにのみ適用されます。OceanBaseデータベースCommunity EditionはMySQLモードのみを提供します。</p>
  </main>

## 構文

```sql
DBMS_PROFILER.GET_VERSION (
   major  OUT BINARY_INTEGER,
   minor  OUT BINARY_INTEGER);
```

## パラメータの説明

|       パラメータ       |               説明                |
|----------------|---------------------------------|
| major | 主要バージョンDBMS_PROFILER。 |
| minor | 次要バージョンDBMS_PROFILER。                  |

## 例

```shell
obclient [SYS]> DECLARE
    ->    -- 主要バージョン番号とマイナーバージョン番号を格納する変数を定義
    ->    l_major BINARY_INTEGER;
    ->    l_minor BINARY_INTEGER;
    -> BEGIN
    ->    -- DBMS_PROFILERパッケージからGET_VERSIONプロシージャを呼び出す
    ->    DBMS_PROFILER.get_version(l_major, l_minor);
    ->
    ->    -- 結果をコンソールに出力
    ->    DBMS_OUTPUT.PUT_LINE('DBMS_PROFILER Major Version: ' || l_major);
    ->    DBMS_OUTPUT.PUT_LINE('DBMS_PROFILER Minor Version: ' || l_minor);
    -> END;
    -> /
Query OK, 1 row affected (0.155 sec)

DBMS_PROFILER Major Version: 2
DBMS_PROFILER Minor Version: 0
```