| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
|machine-translation|en,ja-marker|
|slug|get-lines-oracle|

# GET_LINES

#docslug#/oceanbase-database/oceanbase-database/V4.0.0/GET_LINES
`GET_LINES` ステートメントは、バッファから一連の行データを取得するために使用されます。

  <main id="notice" >
    <h4>適用対象</h4>
    <p>この内容はOceanBaseデータベースEnterprise Editionにのみ適用されます。OceanBaseデータベースCommunity EditionはMySQLモードのみを提供します。</p>
  </main>

## 構文

```sql
DBMS_OUTPUT.GET_LINES (
   lines           OUT     CHARARR,
   numlines    IN OUT  INTEGER);

DBMS_OUTPUT.GET_LINES (
   lines           OUT     DBMSOUTPUT_LINESARRAY,
   numlines    IN OUT INTEGER);
```

## パラメータの説明

|    パラメータ    |                                        説明                                         |
|----------|-----------------------------------------------------------------------------------|
| lines    | 複数行のバッファ情報を返します。 グループ内の各行の最大長は32767バイトです。                                    |
| numlines | バッファから検索する行数。 指定された行数を検索した後、このストアドプロシージャは実際に検索された行数を返します。この数がリクエストされた行数より少ない場合、バッファには他の行が存在しないことを意味します。 |

## 使用方法

* バッファから1行または複数の行を取得することができます。`GET_LINE` ステートメントを呼び出して単一行のバッファ情報を検索します。`GET_LINES` ステートメントは、サーバーへの呼び出し回数を削減できます。

* `GET_LINE` または `GET_LINES` を呼び出した後、取得されなかった行データは、次回 `PUT`、`PUT_LINE`、または `NEW_LINE` を呼び出す前に破棄されます。これは、後続の情報と混同されるのを防ぐためです。