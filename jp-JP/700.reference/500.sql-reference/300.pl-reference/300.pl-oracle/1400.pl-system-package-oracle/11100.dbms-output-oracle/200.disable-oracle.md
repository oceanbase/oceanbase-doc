| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
|machine-translation|en,ja-marker|
|slug|disable-oracle|

# DISABLE

#docslug#/oceanbase-database/oceanbase-database/V4.0.0/DISABLE
DISABLEストアドプロシージャは、PUT、PUT_LINE、NEW_LINE、およびGET_LINEの呼び出しを無効にし、バッファ内の残りのすべての情報をクリアするために使用されます。

ENABLEプログラムと同様に、OBClientの`SERVEROUTPUT`オプションを使用している場合は、このプログラムを呼び出す必要はありません。

  <main id="notice" >
    <h4>適用対象</h4>
    <p>この内容はOceanBaseデータベースEnterprise Editionにのみ適用されます。OceanBaseデータベースCommunity EditionはMySQLモードのみを提供します。</p>
  </main>

## 構文

```sql
DBMS_OUTPUT.DISABLE;
```