| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
|machine-translation|en,ja-marker|
|slug|dbms_lob-overview-oracle-compatible-mode|

# DBMS_LOBの概要

`DBMS_LOB` システムパッケージは、`BLOB` および `CLOB` の読み取りと変更を提供し、ほとんどの `LOB` 操作をサポートします。

  <main id="notice" >
    <h4>機能の適用範囲</h4>
    <p>この内容はOceanBaseデータベースEnterprise Editionにのみ適用されます。OceanBaseデータベースCommunity EditionはMySQLモードのみを提供します。</p>
  </main>

## DBMS_LOB権限の説明
このシステムパッケージは `SYS` ユーザー配下で作成する必要があります。ただし、このシステムパッケージで提供される操作は `SYS` ユーザーではなく、現在呼び出しているユーザーの権限で実行されます。

匿名PLブロックから呼び出されるすべての `DBMS_LOB` サブプログラムは、現在のユーザー権限を使用して実行されます。ストアドサブプログラムから呼び出されるすべての `DBMS_LOB` サブプログラムは、そのストアドサブプログラムの所有者権限を使用して実行されます。

サブプログラム作成時に、ユーザーは `AUTHID` を設定することで、定義者の権限か呼び出し者の権限が必要かを指定できます。例：



```sql
CREATE PROCEDURE proc1 AUTHID DEFINER ...
```



```sql
CREATE PROCEDURE proc1 AUTHID CURRENT_USER ...
```

| **データ型** |               **説明**                |
|----------|-------------------------------------|

| BLOB     | ソースまたはターゲットのバイナリ `LOB`。                      |
| RAW      | ソースまたはターゲットの `RAW` バッファ（`BLOB` と併用）。      |
| CLOB     | ソースまたはターゲットの文字列 `LOB`。                       |
| VARCHAR2 | ソースまたはターゲットの文字列バッファ（`CLOB` と併用）。           |
| INTEGER  | バッファまたは `LOB` のサイズ、`LOB` のオフセット、またはアクセスする数を指定します。 |
## DBMS_LOBの使用方法
すべての `DBMS_LOB` サブプログラムは `LOB` ロケータに基づいて動作します。`DBMS_LOB` サブプログラムの操作を正常に完了するには、入力ロケータを指定する必要があります。この入力ロケータは、データベースのテーブル領域または外部ファイルシステム内に既に存在する `LOB` を表します。

すべての `DBMS_LOB` APIは、現在、リモート `LOB` ロケータの操作をサポートしています。2つのロケータを使用するAPIは、すべて2つの `LOB` を同一データベース内に配置する必要があります。
## DBMS_LOBサブプログラムの概要

次の表は、OceanBaseデータベースの現在のバージョンでサポートされているDBMS_LOBサブプログラムとその概要を示しています。

|                            **サブプログラム**                             |                  **説明**                  |

|----------------------------------------------------------------|------------------------------------------|
| [APPEND](../9300.dbms-lob-oracle/200.append-oracle.md)          | ソース `LOB` の内容をターゲット `LOB` に追加します。                 |

| [CLOSE](../9300.dbms-lob-oracle/300.close-oracle.md)           | 以前に開いた内部または外部 `LOB` を閉じます。                      |

| [COPY](../9300.dbms-lob-oracle/500.copy-oracle.md)            | ソース内部 `LOB` の全部または一部をターゲット内部 `LOB` にコピーします。         |
| [CREATETEMPORARY](../9300.dbms-lob-oracle/600.createtemporary-oracle.md) | デフォルトの一時テーブル領域に一時 `BLOB` または `CLOB` および対応するインデックスを作成します。 |
| [ERASE](../9300.dbms-lob-oracle/700.eraese-oracle.md)           | 全部または一部の `LOB` を消去します。                           |
| [FREETEMPORARY](../9300.dbms-lob-oracle/800.freetemporary-oracle.md)   | デフォルトの一時テーブル領域内の一時 `BLOB` または `CLOB` を解放します。          |
| [GETLENGTH](../9300.dbms-lob-oracle/900.getlength-oracle.md)       | `LOB` 値の長さを取得します。                           |
| [ISOPEN](../9300.dbms-lob-oracle/1000.isopen-oracle.md)          | 入力ロケーターを使用して `LOB` が既に開かれているかどうかを確認します。                  |
| [INSTR](../9300.dbms-lob-oracle/1100.instr-oracle.md)           | `LOB` モードで `n` 回目にマッチした位置を返します。              |
| [ISTEMPORARY](1150.istemporary-of-oracle-mode.md)| ロケーターが一時LOBを指しているかどうかを確認します。|
| [OPEN](../9300.dbms-lob-oracle/1200.open-oracle.md)            | 指定されたモードで `LOB`（内部、外部、または一時）を開きます。               |
| [READ](../9300.dbms-lob-oracle/1300.read-oracle.md)            | 指定されたオフセットから `LOB` のデータを読み取ります。                    |
| [SUBSTR](../9300.dbms-lob-oracle/1400.substr-oracle.md)          | 指定されたオフセットから `LOB` 値の一部を返します。                   |
| [TRIM](../9300.dbms-lob-oracle/1500.trim-oracle.md)            | `LOB` 値を指定された短い長さに切り詰めます。                     |
| [WRITE](../9300.dbms-lob-oracle/1600.write-oracle.md)           | 指定されたオフセットからデータを `LOB` に書き込みます。                    |
| [WRITEAPPEND](../9300.dbms-lob-oracle/1700.writeappend-oracle.md)     | バッファを `LOB` の末尾に書き込みます。                        |
| [SUBSTR](../9300.dbms-lob-oracle/1400.substr-oracle.md)          | 从指定的偏移量开始返回部分 `LOB` 值。                   |
| [TRIM](../9300.dbms-lob-oracle/1500.trim-oracle.md)            | 将 `LOB` 值修剪为指定的较短长度。                     |
| [WRITE](../9300.dbms-lob-oracle/1600.write-oracle.md)           | 从指定的偏移量开始将数据写入 `LOB`。                    |
| [WRITEAPPEND](../9300.dbms-lob-oracle/1700.writeappend-oracle.md)     | 将缓冲区写入 `LOB` 的末尾。                        |