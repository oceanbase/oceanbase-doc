| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
|machine-translation|en,ja-marker|
|slug|active_instances-oracle-compatible-mode|

# ACTIVE_INSTANCES

`ACTIVE_INSTANCES` ステートメントは、アクティブなインスタンスを返します。

  <main id="notice" >
    <h4>適用対象</h4>
    <p>この内容はOceanBaseデータベースEnterprise Editionにのみ適用されます。OceanBaseデータベースCommunity EditionはMySQLモードのみを提供します。</p>
  </main>

## 構文

```sql
DBMS_UTILITY.ACTIVE_INSTANCES (
instance_table   OUT INSTANCE_TABLE,
instance_count   OUT NUMBER);
```

## パラメータの説明

|       パラメータ       |               説明                |
|----------------|---------------------------------|
| instance_table | アクティブなインスタンスの番号と名前を含むリストです。現在起動されているインスタンスがない場合、リストは空になります。 |
| instance_count | アクティブなインスタンスの数。                        |