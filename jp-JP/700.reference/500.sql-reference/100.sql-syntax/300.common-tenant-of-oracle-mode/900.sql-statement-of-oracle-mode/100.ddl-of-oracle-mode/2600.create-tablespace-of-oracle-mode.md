| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
|machine-translation| en          |
|slug|create-tablespace-oracle-compatible-mode|

# CREATE TABLESPACE

## 説明

このステートメントは、TableSpaceの論理オブジェクトを作成するために使用します。TableSpace属性は暗号化をサポートしています。

## 構文

```sql
CREATE TABLESPACE tablespace_name
    [ ENCRYPTION USING 'algorithm' ]

algorithm:
    'AES-256' | 'AES-128' | 'AES-192' | 'SM4-CBC'
```

## パラメータの説明

|                 パラメータ                 |           説明            |
|------------------------------------|-------------------------|
| tablespace_name                    | 作成するTableSpaceオブジェクト名を指定します。 |
| ENCRYPTION USING                   | 暗号化に使用するキーワード。              |
| AES-256\|AES-128\|AES-192\|SM4-CBC | 使用する暗号化アルゴリズムを選択します。             |

## 例

* TableSpaceオブジェクトを作成します。

  ```sql
  obclient> CREATE TABLESPACE tblspace1;
  ```

* TableSpaceオブジェクトを作成し、暗号化を使用します。

  ```sql
  -- ステップ1：TDE方式を内部キー管理に設定
  obclient> ALTER SYSTEM SET TDE_METHOD = 'INTERNAL';

  -- ステップ2：キーストアを作成
  obclient> ADMINISTER KEY MANAGEMENT CREATE KEYSTORE my_keystore IDENTIFIED BY abcCBAK123;

  -- ステップ3：キーストアを開く
  obclient> ADMINISTER KEY MANAGEMENT SET KEYSTORE OPEN IDENTIFIED BY abcCBAK123;

  -- ステップ4：マスターキーを作成
  obclient> ADMINISTER KEY MANAGEMENT SET KEY IDENTIFIED BY abcCBAK123;

  -- ステップ5：暗号化されたTableSpaceを作成
  obclient> CREATE TABLESPACE tblspace2 ENCRYPTION USING 'SM4-CBC';
  ```

  パスワードの例では `abcCBAK123` を使用していますが、実際の使用時には安全なパスワードに置き換えてください。

## 関連ドキュメント

* [DROP TABLESPACE](4100.drop-tablespace-of-oracle-mode.md)
* [ALTER TABLESPACE](1110.alter-tablespace-of-oracle-mode.md)