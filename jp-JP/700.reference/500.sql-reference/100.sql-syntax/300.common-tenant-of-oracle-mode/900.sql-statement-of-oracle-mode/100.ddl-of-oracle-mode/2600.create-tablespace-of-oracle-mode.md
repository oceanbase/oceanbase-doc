| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
|machine-translation|en,ja-marker|
|slug|create-tablespace-oracle-compatible-mode|

# CREATE TABLESPACE

## 説明

このステートメントは、TableSpaceの論理オブジェクトを作成するために使用されます。TableSpaceのプロパティは暗号化をサポートしています。

## 構文

```sql
CREATE TABLESPACE tablespace_name [ENCRYPTION USING 'AES-256|AES-128|AES-192|SM4-CBC']
```

## パラメータの説明

|                 パラメータ                 |           説明            |
|------------------------------------|-------------------------|
| tablespace_name                    | 作成するTableSpaceオブジェクトの名前を指定します。 |
| ENCRYPTION USING                   | 暗号化に使用するキーワードです。              |
| AES-256\|AES-128\|AES-192\|SM4-CBC | 使用する暗号化アルゴリズムを選択します。             |

## 例

* TableSpaceオブジェクトを作成します。

  ```sql
  obclient> CREATE TABLESPACE tblspace1;
  Query OK, 0 rows affected
  ```

* TableSpaceオブジェクトを作成し、暗号化を使用します。

  ```sql
  /*Keystoreオブジェクトを作成し、ストレージキーを有効にする*/
  ALTER SYSTEM SET TDE_METHOD = 'INTERNAL';
  ADMINISTER KEY MANAGEMENT CREATE KEYSTORE my_keystore IDENTIFIED BY abcCBAK123;
  /*主要な暗号化キーを作成する*/
  ADMINISTER KEY MANAGEMENT SET KEYSTORE OPEN IDENTIFIED BY abcCBAK123;
  ADMINISTER KEY MANAGEMENT SET KEY IDENTIFIED BY abcCBAK123;

  obclient>CREATE TABLESPACE tblspace1 ENCRYPTION USING 'SM4-CBC';
  Query OK, 0 rows affected
  ```