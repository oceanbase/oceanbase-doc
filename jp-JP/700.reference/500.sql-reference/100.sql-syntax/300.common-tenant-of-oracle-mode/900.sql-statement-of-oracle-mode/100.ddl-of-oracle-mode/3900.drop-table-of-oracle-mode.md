| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
|machine-translation|en,ja-marker|
|slug|drop-table-of-oracle-mode|

# DROP TABLE

## 説明

このステートメントは、データベース内のテーブルを削除するために使用されます。

## 権限要件

`DROP TABLE` ステートメントを実行するには、現在のユーザーが `DROP` のシステム権限を持っている必要があります。OceanBaseデータベースの権限の詳細については、[Oracleモードの権限分類](../../../../../../600.manage/500.security-and-permissions/300.access-control/200.user-and-permission/300.permission-of-oracle-mode/000.permission-classification-of-oracle-mode.md)を参照してください。

## 構文

```sql
DROP TABLE table_name [CASCADE CONSTRAINTS] [PURGE];
```

## パラメータの説明

|         パラメータ          |            説明             |
|---------------------|---------------------------|
| table_name          | 削除するテーブル名を指定します。                 |
| CASCADE CONSTRAINTS | `table_name` に関連付けられた制約をカスケード削除します。 |
| PURGE               | テーブルを完全に削除します（ゴミ箱には移動しません）。            |

## 例

テーブル `tbl1` を削除します。

```sql
obclient> DROP TABLE tbl1;
Query OK, 0 rows affected
```

## 関連ドキュメント

[テーブルの削除](../../../../../300.database-object-management/200.manage-object-of-oracle-mode/100.manage-tables-of-oracle-mode/800.delete-a-table-of-oracle-mode.md)