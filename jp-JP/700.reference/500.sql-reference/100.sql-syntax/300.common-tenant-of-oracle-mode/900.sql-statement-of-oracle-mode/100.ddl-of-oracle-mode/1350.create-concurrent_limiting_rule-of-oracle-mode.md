| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
|machine-translation| en          |
|slug|create-concurrent_limiting_rule-oracle-compatible-mode|

# CREATE CONCURRENT_LIMITING_RULE

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>V4.3.X系では、このステートメントはOceanBaseデータベースV4.3.5 BP3バージョンから導入されました。<br>V4.4.X系では、このステートメントはOceanBaseデータベースV4.4.1バージョンから導入されました。</p>
  </main>

## 説明

このステートメントは、レート制限ルールを作成し、特定のSQLの同時実行数を制限するためのレート制限範囲を指定します。

## 注意事項

OceanBaseデータベースのOracleモードで`CREATE CONCURRENT_LIMITING_RULE`ステートメントを実行するには、専用権限`create any concurrent_limiting_rule`が必要です。

## 構文

```sql
CREATE CONCURRENT_LIMITING_RULE ccl_rule_name
ON `database`.`table`
TO '<usename>'@'ip'
FOR { ALL | UPDATE | SELECT | INSERT | DELETE }
filter_options:
    [ FILTER BY KEYWORD('KEYWORD1', 'KEYWORD2',…) ]
 with_options:
    WITH MAX_CONCURRENCY = value1 [per sql]
```

## パラメータの説明

| パラメータ             |       説明        |
|----------------------------|----------|
|`ccl_rule_name`|必須。レート制限ルールの名前を表します。|
|`database`.`table`|必須。データベースとデータテーブルの名前を表します。アスタリスク（*）を使用して任意の文字列に一致させることができます。<main id="notice" type='explain'><h4>説明</h4><p>名前がSQLキーワードと競合するのを避けるため、ルール名の前後にバッククォート（`）を付加することを推奨します。</p></main>|
|`<usename>@<host>`|必須。アカウント名を表します。Host部分ではパーセント記号（%）を使用して任意の文字列に一致させることができます。|
|`ALL`\|`UPDATE \| SELECT \| INSERT\| DELETE`|必須。SQLステートメントのタイプを表します。現在`ALL`、`UPDATE`、`SELECT`、`INSERT`、`DELETE`タイプをサポートしています。<main id="notice" type='explain'><h4>説明</h4><p>各レート制限ルールは、1種類のSQLステートメントタイプのみをサポートします。</p></main>|
|`[ filter_options ]`|オプション。レート制限のキーワードを表します。<main id="notice" type='explain'><h4>説明</h4><p><ul><li>キーワード間はワイルドカード<code>%</code>で結合されます。例えば、3つのキーワード<code>"asdasd", "c2 = 123", "xxyyzz"</code>が与えられた場合、<code>"%asdasd%c2 = 123%xxyyzz%"</code>という文字列に変換され、その後この文字列に基づいてSQLテキストがワイルドカードマッチングされます。</li><li>キーワードに<code>'_'</code>が含まれる場合、エスケープされます。例えば、<code>"table_t1"</code>は<code>"%table\_t1%"</code>として記録されます。</li><li>レート制限のキーワードに個数制限はありませんが、組み合わせた後の文字列の長さは <code>OB_MAX_VARCHAR_LENGTH=1048576</code> を超えてはなりません。</li></ul></p></main>|
|`with_options`|必須。レート制限の同時実行数を制御し、レート制限の統計粒度がルールレベルかFormat SQLIDステートメントレベルかを指定します。現在は`MAX_CONCURRENCY`パラメータのみをサポートしており、このレート制限ルールに一致するSQLステートメントの最大同時実行数を表します。|

## 例

以下のコマンドを実行して、キーワードによるレート制限を行います：

```shell
obclient> CREATE CONCURRENT_LIMITING_RULE   sql1_keywords_ccl_rule
ON *.* TO '%'@'%' FOR SELECT
FILTER BY KEYWORD('table1', 'table2', 'c1') WITH MAX_CONCURRENCY 100;
```