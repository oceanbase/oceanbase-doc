| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
|machine-translation| en          |
|slug|rename-oracle-mode|

# RENAME

## 説明

このステートメントは、テーブル、ビュー、マテリアライズドビュー、プライベートシノニム、またはシーケンスの名前を変更するために使用します。

## 構文

```sql
RENAME object_name TO new_object_name;
```

## パラメータの説明

| パラメータ | 説明 |
|------|------|
| object_name | 重命名するオブジェクト名。テーブル、ビュー、マテリアライズドビュー、プライベートシノニム、またはシーケンスを指定できます。 |
| new_object_name | オブジェクトの新しい名前。新しい名前は既存のオブジェクトと重複してはなりません。 |

## 例

1. テーブル `tbl1` を作成します。

    ```shell
    obclient> CREATE TABLE tbl1(col1 INT, col2 INT);
    ```

2. テーブル `tbl1` の名前を `tbl2` に変更します。

    ```shell
    obclient> RENAME tbl1 TO tbl2;
    ```