| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
|machine-translation|en,ja-marker|
|slug|create-keystore-oracle-mode|

# CREATE KEYSTORE

## 説明

このステートメントは、キーを格納するために使用されるKeystoreオブジェクトを作成します。透過的暗号化のシナリオでは、Keystoreオブジェクトを作成する必要があり、各テナントは最大で1つのKeystoreオブジェクトしか作成できません。

## 構文

```sql
ADMINISTER KEY MANAGEMENT CREATE KEYSTORE [keystore_name] IDENTIFIED BY [password] ;
```

## パラメータの説明

|      パラメータ       |          説明          |
|---------------|----------------------|
| keystore_name | Keystoreの名前を指定します。      |
| password      | アクセス制御用Keystoreにアクセスするためのパスワードを指定します。 |

## 例

Keystoreオブジェクト`keystore1`を作成します。

```sql
obclient> ADMINISTER KEY MANAGEMENT CREATE KEYSTORE keystore1 IDENTIFIED BY **1***;
```