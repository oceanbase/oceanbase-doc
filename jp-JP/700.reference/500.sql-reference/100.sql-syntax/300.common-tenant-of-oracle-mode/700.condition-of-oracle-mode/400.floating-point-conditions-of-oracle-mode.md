| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
|machine-translation| en          |
|slug|floating-point-conditions-oracle-mode|

# 浮動小数点条件

浮動小数点条件（Floating-Point Condition）を使用すると、式が無限大または定義されていない結果（数値またはNaNではない）の演算であるかどうかを判断できます。

浮動小数点条件の構文は次のとおりです：

```sql
expr IS [ NOT ] { NAN | INFINITE }
```

`{ NAN | INFINITE }` の2種類の形式の浮動小数点条件において、`expr` は数値データ型、または暗黙的に数値データ型へ変換可能な任意のデータ型に解析されなければなりません。

浮動小数点条件の詳細は以下の表のとおりです。

|        条件タイプ         |                                                         演算                                                         |                            例                             |
|---------------------|--------------------------------------------------------------------------------------------------------------------|-----------------------------------------------------------|
| IS \[NOT\] NAN      | `NOT` を指定しない場合、`expr` が特別値 NaN の場合は TRUE を返します。`NOT` を指定した場合、`expr` が特別値 NaN 以外の場合は TRUE を返します。                               | `SELECT COUNT(*) FROM emp WHERE comm_pct IS NOT NAN;`     |
| IS \[NOT\] INFINITE | NOT を指定しない場合、`expr` が特別値 +INF または -INF の場合は TRUE を返します。`NOT` を指定した場合、`expr` が +INF でも -INF でもない場合は TRUE を返します。 | `SELECT last_name FROM emp WHERE salary IS NOT INFINITE;` |