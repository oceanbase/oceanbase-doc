| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
|machine-translation|en,ja-marker|
|slug|tz_offset-oracle-compatible-mode|

# TZ_OFFSET

## 説明

この関数は、現在のセッションタイムゾーン `SESSIONTIMEZONE` またはデータベースタイムゾーン `DBTIMEZONE` のタイムゾーンオフセットを返します。

  <main id="notice" type='explain'>
    <h4>説明</h4>
    <p>タイムゾーンオフセットとは、グリニッジ標準時GMTとの差（計算単位は時間と分）を指します。</p>
  </main>

## 構文

```sql
TZ_OFFSET({ SESSIONTIMEZONE | DBTIMEZONE})
```

## パラメータの説明

|       パラメータ        |   説明    |
|-----------------|---------|
| SESSIONTIMEZONE | 現在のセッションのタイムゾーン。 |
| DBTIMEZONE      | データベースのタイムゾーン。  |

## 戻り値の型

`VARCHAR2` データ型を返します。

## 例

現在のセッションタイムゾーン `SESSIONTIMEZONE` およびデータベースタイムゾーン `DBTIMEZONE` のタイムゾーンオフセットを返します。

```sql
obclient> SELECT TZ_OFFSET(SESSIONTIMEZONE),TZ_OFFSET(DBTIMEZONE) FROM DUAL;
+----------------------------+-----------------------+
| TZ_OFFSET(SESSIONTIMEZONE) | TZ_OFFSET(DBTIMEZONE) |
+----------------------------+-----------------------+
| +08:00                     | +00:00                |
+----------------------------+-----------------------+
1 row in set
```