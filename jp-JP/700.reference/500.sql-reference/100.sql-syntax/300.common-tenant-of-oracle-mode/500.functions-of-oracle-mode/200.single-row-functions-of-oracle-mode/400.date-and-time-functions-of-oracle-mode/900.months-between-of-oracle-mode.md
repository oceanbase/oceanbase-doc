| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
|machine-translation| en          |
|slug|months_between-oracle-compatible-mode|

# MONTHS_BETWEEN

## 説明

この関数は、パラメータ `date1` と `date2` の間の月数を返します。

## 構文

```sql
MONTHS_BETWEEN (date1，date2)
```

## パラメータの説明

| パラメータ | 説明 |
|-------|----------------|
| date1 | `DATE` 型の値。 |
| date2 | `DATE` 型の値。 |

## 戻り値の型

`NUMBER` 型を返します。

* `date1` が `date2` より大きい場合、正の数を返します。

* `date1` が `date2` より小さい場合、負の数を返します。

## 例

`2030-11-17` と `2022-01-15` の間の月数を計算します。

```sql
obclient> SELECT MONTHS_BETWEEN(TO_DATE('2030-11-17','YYYY-MM-DD'),TO_DATE('2022-01-15','YYYY-MM-DD')) "MONTHS" FROM DUAL;
+------------------------------------------+
| MONTHS                                   |
+------------------------------------------+
| 106.064516129032258064516129032258064516 |
+------------------------------------------+
1 row in set
```