| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
|machine-translation|en,ja-marker|
|slug|sessiontimezone-oracle-mode|

# SESSIONTIMEZONE

## 説明

この関数は、現在のセッションのタイムゾーンを返します。

## 構文

```sql
SESSIONTIMEZONE
```

## 戻り値型

`VARCHAR2`データ型を返します。

## 例

データベースのタイムゾーンと現在のセッションのタイムゾーンを返します。

```sql
obclient> SELECT DBTIMEZONE,SESSIONTIMEZONE FROM DUAL;
+------------+-----------------+
| DBTIMEZONE | SESSIONTIMEZONE |
+------------+-----------------+
| +00:00     | +08:00          |
+------------+-----------------+
1 row in set
```