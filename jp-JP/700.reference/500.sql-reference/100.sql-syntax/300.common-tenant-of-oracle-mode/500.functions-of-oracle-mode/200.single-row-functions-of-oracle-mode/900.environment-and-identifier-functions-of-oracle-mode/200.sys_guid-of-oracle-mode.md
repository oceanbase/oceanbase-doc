| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
|machine-translation| en          |
|slug|sys_guid-oracle-mode|

# SYS_GUID

## 説明

この関数は、16バイトで構成されるグローバル一意の識別子、すなわちグローバルな一意のシーケンス番号を生成して返します。

## 構文

```sql
SYS_GUID()
```

## 戻り値の型

16進数で表現された32文字の長さの文字列を返します。

## 例

テーブル `tbl1` を作成し、一意の識別子を持つ3件のデータを挿入します。

```sql
obclient> CREATE TABLE tbl1(col1 INT,col2 RAW(16));
Query OK, 0 rows affected

obclient> INSERT INTO tbl1 VALUES(1,SYS_GUID()),(2,SYS_GUID()),(3,SYS_GUID());
Query OK, 3 rows affected
Records: 3  Duplicates: 0  Warnings: 0

obclient> SELECT * FROM tbl1;
+------+----------------------------------+
| COL1 | COL2                             |
+------+----------------------------------+
|    1 | 442E52AC599411EC8C99B6945CA6C7B0 |
|    2 | 442EC8D1599411EC8C99B6945CA6C7B0 |
|    3 | 442ED1F1599411EC8C99B6945CA6C7B0 |
+------+----------------------------------+
3 rows in set
```