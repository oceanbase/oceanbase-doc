| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
|machine-translation| en          |
|slug|regexp_substr-oracle-mode|

# REGEXP_SUBSTR

## 説明

この関数は、正規表現を使用して文字列を検索し、一致する文字列を返します。これにより、`SUBSTR` 関数の機能が拡張されます。
この関数は `REGEXP_INSTR` と似ていますが、サブストリングの位置ではなく、サブストリングそのものを返します。マッチする文字列の内容は必要だが、ソース文字列内での位置を返す必要はない場合に、この関数が適しています。

## 構文

```sql
REGEXP_SUBSTR(source_char, pattern
               [, position [, occurrence [, match_param [, subexpr] ] ] ]
              )
```

## パラメータの説明

|     パラメータ      |                                                                                                                                                                                      説明                                                                                                                                                                                      |
|-------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| source_char | サーチ値として使用する文字式を指定します。データ型は `CHAR`、`VARCHAR2`、`NCHAR`、`NVARCHAR2`、または `CLOB` が可能です。                                                                                                                                                                                                                                                                                                        |
| pattern     | 正規表現の抽出ルールを指定します。通常はテキストリテラルであり、文字型は `CHAR`、`VARCHAR2`、`NCHAR`、または `NVARCHAR2` が可能です。                                                                                                                                                                                                                                                                                                     |
| position    | 正規表現のマッチング開始位置を指定します。値は正の整数で、オプションです。デフォルト値は `1` で、これは最初の文字から `source_char` を検索することを意味します。                                                                                                                                                                                                                                                                                                          |
| occurrence  | `source_char` 内の正規表現のマッチ位置を指定します。オプションです。デフォルト値は `1` です。`occurrence` が 1 より大きい場合、最初のマッチ後の最初の文字から2番目の正規表現のマッチを検索し、以降も同様に処理します。                                                                                                                                                                                                                                                        |
| match_param | 正規表現のデフォルトのマッチ方法を変更するために使用されます。オプションです。データ型 `VARCHAR2` または `CHAR` の文字式です。 <ul><li> `i` は大文字小文字を区別しないことを示します。   </li><li> `c` は大文字小文字を区別することを示します。   </li><li> `n` はピリオド `.` が改行文字と一致できることを示します。   </li><li> `m` は複数行モードを示します。  </li><li> `x` は空白文字を無視することを示します。デフォルトでは、空白文字は互いに一致します。</li></ul>    |
| subexpr     | `pattern` が返すサブ式を指定します。`0` から `9` の範囲の非負整数で、オプションです。デフォルト値は `0` で、最初のサブ式を返すことを意味します。                                                                                                                                                                                                                                                                                                        |

## 戻り値の型

パラメータ `source_char` と同じ `VARCHAR2` または `CLOB` データ型を返します。

## 例

* `abc,def,ghi` を `abc`、`def`、`ghi` に分割します。

  ```sql
  obclient> SELECT REGEXP_SUBSTR('abc,def,ghi', '[^,]+',1,1) AS "first",
                REGEXP_SUBSTR('abc,def,ghi', '[^,]+',1,2) AS "second",
                REGEXP_SUBSTR('abc,def,ghi', '[^,]+',1,3) AS "third"
       FROM DUAL;
  +-----------+-----------+-----------+
  | first     | second   | third   |
  +-----------+-----------+-----------+
  | abc       | def       | ghi       |
  +-----------+-----------+-----------+
  1 row in set
  ```

* 式 `abc(d(e(f)gh)i)` の3番目のサブ式を返します。

  ```sql
  obclient> SELECT REGEXP_SUBSTR('abcdefghi', 'abc(d(e(f)gh)i)',1,1,'i',2) FROM DUAL;
  +--------------------------------------------------------+
  | REGEXP_SUBSTR('ABCDEFGHI','ABC(D(E(F)GH)I)',1,1,'I',2) |
  +--------------------------------------------------------+
  | efgh                                                   |
  +--------------------------------------------------------+
  1 row in set
  ```

* 文字列 `<div><p>Hello</p><p>World</p></div>` から最初の `<p>` タグとその内容を抽出します。

  ```sql
  obclient [SYS]> SELECT REGEXP_SUBSTR('<div><p>Hello</p><p>World</p></div>', '<p>.*?</p>') FROM DUAL;
  +-------------------------------------------------------------------+
  | REGEXP_SUBSTR('<DIV><P>HELLO</P><P>WORLD</P></DIV>','<P>.*?</P>') |
  +-------------------------------------------------------------------+
  | <p>Hello</p>                                                      |
  +-------------------------------------------------------------------+
  1 row in set
  ```