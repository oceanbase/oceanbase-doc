| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
|machine-translation| en          |
|slug|upper-oracle-mode|

# UPPER

## 説明

この関数は、文字列内のすべての英字を大文字に変換します。

## 構文

```sql
UPPER(char)
```

## パラメータの説明

`char` は変換対象の文字列で、データ型は `CHAR`、`VARCHAR2`、`NCHAR`、`NVARCHAR2`、または `CLOB` が可能です。

## 戻り値の型

`char` と同じデータ型を返します。

## 例

文字列 `OceanBase` のすべての文字を大文字に変換します。

```sql
obclient> SELECT UPPER('OceanBase') "UPPER" FROM DUAL;
+-----------+
| UPPER     |
+-----------+
| OCEANBASE |
+-----------+
1 row in set
```