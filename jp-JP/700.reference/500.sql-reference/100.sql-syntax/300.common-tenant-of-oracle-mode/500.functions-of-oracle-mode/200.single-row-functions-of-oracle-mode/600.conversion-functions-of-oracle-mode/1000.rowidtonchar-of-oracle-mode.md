| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
|machine-translation|en,ja-marker|
|slug|rowidtonchar-oracle-mode|

# ROWIDTONCHAR

## 説明

この関数は、`rowid` 値を `NVARCHAR2` データ型の値に変換します。

## 構文

```sql
ROWIDTONCHAR(rowid)
```

## パラメータの説明

パラメータ `rowid` は `ROWID` 形式の値です。

## 戻り値のタイプ

`NVARCHAR2` データ型を返します。

## 例

テーブル `tbl1` 内の `ROWID` 値を `NVARCHAR2` タイプの値に変換し、対応するバイト長を返します。

```sql
obclient> SELECT LENGTHB(ROWIDTONCHAR(ROWID)),LENGTHB(ROWID),ROWIDTONCHAR(ROWID) FROM tbl1;
+------------------------------+----------------+---------------------+
| LENGTHB(ROWIDTONCHAR(ROWID)) | LENGTHB(ROWID) | ROWIDTONCHAR(ROWID) |
+------------------------------+----------------+---------------------+
|                           34 |             17 | *AAIKAQAAAAAAAAA=   |
|                           34 |             17 | *AAIKAgAAAAAAAAA=   |
|                           34 |             17 | *AAIKAwAAAAAAAAA=   |
+------------------------------+----------------+---------------------+
3 rows in set
```