| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
|machine-translation|en,ja-marker|
|slug|cosh-of-oracle-mode|

# COSH

## 説明

この関数は双曲コサイン関数であり、数値パラメータの双曲コサイン値を返します。戻り値の範囲は \[1,+∞) です。

## 構文

```sql
COSH(number)
```

## パラメータの説明

`number` は数値データ型（`NUMBER`、`FLOAT`、`BINARY_FLOAT`、および `BINARY_DOUBLE`）または暗黙的に数値データ型に変換可能なパラメータです。

## 戻り値のタイプ

パラメータ `number` が `BINARY_FLOAT` タイプの場合、戻り値のタイプは `BINARY_DOUBLE` です。それ以外の場合、パラメータ `number` と同じ数値データ型を返します。

## 例

`0` の双曲コサイン値を返します。

```sql
obclient> SELECT COSH(0) FROM DUAL;
+---------+
| COSH(0) |
+---------+
|       1 |
+---------+
1 row in set
```