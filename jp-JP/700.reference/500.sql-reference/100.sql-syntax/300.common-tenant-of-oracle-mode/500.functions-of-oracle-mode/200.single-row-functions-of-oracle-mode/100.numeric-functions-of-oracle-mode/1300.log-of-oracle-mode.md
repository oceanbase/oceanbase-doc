| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
|machine-translation| en          |
|slug|log-oracle-compatible-mode|

# LOG

## 説明

この関数は、`x` を底とする `y` の対数を返します。

## 構文

```sql
LOG (x,y)
```

## パラメータの説明

| パラメータ |                                                   説明                                                   |
|----|--------------------------------------------------------------------------------------------------------|
| x  | 对数の底を表す数値型（`NUMBER`、`FLOAT`、`BINARY_FLOAT`、`BINARY_DOUBLE`）の式。`x`は`0`より大きく、`1`と等しくないこと。 |
| y  | 真数を表す数値型（`NUMBER`、`FLOAT`、`BINARY_FLOAT`、`BINARY_DOUBLE`）の式。`y`は`0`より大きいこと。            |

## 戻り値の型

パラメータが `BINARY_FLOAT` または `BINARY_DOUBLE` 型の場合、戻り値の型は `BINARY_DOUBLE` です。それ以外の場合、戻り値の型は `NUMBER` です。

## 例

`2` を底とする `8` の対数を返します。

```sql
obclient> SELECT LOG(2,8) FROM DUAL;
+----------+
| LOG(2,8) |
+----------+
|        3 |
+----------+
1 row in set
```