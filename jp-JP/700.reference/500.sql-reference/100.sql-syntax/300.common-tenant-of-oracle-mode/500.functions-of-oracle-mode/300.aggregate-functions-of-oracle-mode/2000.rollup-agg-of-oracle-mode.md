| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
|machine-translation| en          |
|slug|rollup-agg-oracle-mode|

# ROLLUP

## 説明

この関数は集計関数であり、各グループに対して小計を返すと同時に、すべてのグループに対して合計も返します。これは `GROUP BY` ステートメントの簡単な拡張であり、データ統計やレポート生成のプロセスにおいて、`GROUP BY` と `UNION` を組み合わせる方法よりも効率的です。グループ統計に関するタスクでは、`ROLLUP` 関数を使用することが非常に効率的です。例えば、時間や地理などの階層的な次元に沿って小計を取る場合、`ROLLUP(y, m, day)` または `ROLLUP(country, state, city)` というクエリだけで済みます。データウェアハウス管理者は `ROLLUP` 関数を使用することで、集計表のメンテナンスを簡素化し、高速化できます。

`ROLLUP` 関数の実行順序は以下のとおりです：

1. パラメータで指定された列に基づき、右から左へと段階的にグループ化します。

2. 各グループごとに小計を計算し、その後すべてのグループに対して合計を計算します。

3. `ORDER BY col1 (，col2，col3，col4 ...)` の順に並べ替えます。

## 構文

```sql
SELECT ... GROUP BY ROLLUP(col_name [,col_name...])
```

## パラメータの説明

`col_name` はグループ化操作を行う列名を指します。`ROLLUP` のパラメータが `N` 個の `col_name` の場合、それは `N+1` 個の `GROUP BY` グループの `UNION` を組み合わせたものと同じになります。

## 例

テーブル `group_tbl1` を作成し、データを挿入します。

```sql
obclient> CREATE TABLE group_tbl1 (group_id INT,job VARCHAR2(10),name VARCHAR2(10),salary INT);
Query OK, 0 rows affected

obclient> INSERT INTO group_tbl1 VALUES
    (10,'Coding','Bruce',1000),
    (10,'Programmer','Clair',1000),
    (20,'Coding','Jason',2000),
    (20,'Programmer','Joey',2000),
    (30,'Coding','Rebecca',3000),
    (30,'Programmer','Rex',3000),
    (40,'Coding','Samuel',4000),
    (40,'Programmer','Susy',4000);
Query OK, 8 rows affected (0.01 sec)
Records: 8  Duplicates: 0  Warnings: 0

obclient> SELECT * FROM group_tbl1;
+----------+------------+---------+--------+
| GROUP_ID | JOB        | NAME    | SALARY |
+----------+------------+---------+--------+
|       10 | Coding     | Bruce   |   1000 |
|       10 | Programmer | Clair   |   1000 |
|       20 | Coding     | Jason   |   2000 |
|       20 | Programmer | Joey    |   2000 |
|       30 | Coding     | Rebecca |   3000 |
|       30 | Programmer | Rex     |   3000 |
|       40 | Coding     | Samuel  |   4000 |
|       40 | Programmer | Susy    |   4000 |
+----------+------------+---------+--------+
8 rows in set
```

`group_id` で `GROUP BY` グループ化を行い、`salary` の合計を求めます。

```sql
obclient> SELECT group_id,SUM(salary) FROM group_tbl1 GROUP BY group_id;
+----------+-------------+
| GROUP_ID | SUM(SALARY) |
+----------+-------------+
|       10 |        2000 |
|       20 |        4000 |
|       30 |        6000 |
|       40 |        8000 |
+----------+-------------+
4 rows in set
```

`group_id` に対して `ROLLUP` 関数を使用してグループ化し、同時に `salary` の合計を求めます。

```javascript
obclient> SELECT group_id,SUM(salary) FROM group_tbl1 GROUP BY ROLLUP (group_id);
+----------+-------------+
| GROUP_ID | SUM(SALARY) |
+----------+-------------+
|       10 |        2000 |
|       20 |        4000 |
|       30 |        6000 |
|       40 |        8000 |
|     NULL |       20000 |
+----------+-------------+
5 rows in set
```

`group_id` 列と `job` 列に対して `ROLLUP` 関数を使用してグループ化し、同時に `salary` の合計を求めます。

```sql
obclient> SELECT group_id,job,SUM(salary) FROM group_tbl1 GROUP BY ROLLUP (group_id,job);
+----------+------------+-------------+
| GROUP_ID | JOB        | SUM(SALARY) |
+----------+------------+-------------+
|       10 | Coding     |        1000 |
|       10 | Programmer |        1000 |
|       10 | NULL       |        2000 |
|       20 | Coding     |        2000 |
|       20 | Programmer |        2000 |
|       20 | NULL       |        4000 |
|       30 | Coding     |        3000 |
|       30 | Programmer |        3000 |
|       30 | NULL       |        6000 |
|       40 | Coding     |        4000 |
|       40 | Programmer |        4000 |
|       40 | NULL       |        8000 |
|     NULL | NULL       |       20000 |
+----------+------------+-------------+
13 rows in set
```

上記のSQL文は `GROUP BY` と `UNION ALL` を組み合わせた方法で置き換えることができます。出力結果は `ROLLUP` 関数と同じですが、`ROLLUP` 関数の方が簡潔で効率的です。

```sql
obclient> SELECT group_id,job,SUM(salary) FROM group_tbl1 GROUP BY group_id, job
    UNION ALL
    SELECT group_id,NULL,SUM(salary) FROM group_tbl1 GROUP BY group_id
    UNION ALL
    SELECT NULL,NULL,SUM(salary) FROM group_tbl1 ORDER BY 1,2;
+----------+------------+-------------+
| GROUP_ID | JOB        | SUM(SALARY) |
+----------+------------+-------------+
|       10 | Coding     |        1000 |
|       10 | Programmer |        1000 |
|       10 | NULL       |        2000 |
|       20 | Coding     |        2000 |
|       20 | Programmer |        2000 |
|       20 | NULL       |        4000 |
|       30 | Coding     |        3000 |
|       30 | Programmer |        3000 |
|       30 | NULL       |        6000 |
|       40 | Coding     |        4000 |
|       40 | Programmer |        4000 |
|       40 | NULL       |        8000 |
|     NULL | NULL       |       20000 |
+----------+------------+-------------+
13 rows in set
```