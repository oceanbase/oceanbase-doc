| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
|machine-translation|en,ja-marker|
|slug|wmsys-wm-concat-and-wm-concat-of-oracle-mode|

# WMSYS.WM_CONCAT/WM_CONCAT

## 説明

この関数は、列を行に変換し、測定列の値を結合するために使用されます。集計または分析関数として使用できます。

  <main id="notice" type='explain'>
    <h4>説明</h4>
    <ul>
    <li>分析関数として使用する場合、<code>OVER</code> 句を使用してウィンドウを定義し、計算を実行する必要があります。この関数は、<code>query_partition_clause</code> 内の1つ以上の式に基づいて、クエリ結果セットをいくつかのグループに分割します。</li>
    <li>集計関数として使用する場合、<code>OVER</code> キーワードを追加する必要はありません。
    <ul>
    <li>単一の集合としての集計関数として、<code>WM_CONCAT</code> はすべての行に対して操作を実行し、単一の出力行を返します。</li>
    <li>グループ集計として、<code>WM_CONCAT</code> は <code>GROUP BY</code> 句で定義された各グループに対して操作を実行し、出力行を返します。</li>
    </ul>
    </li>
    </ul>
  </main>

## 構文

```sql
WM_CONCAT ([DISTINCT] measure_expr) [OVER query_partition_clause]
```

## パラメータの説明

|      パラメータ      |                                           説明                                            |
|--------------|-----------------------------------------------------------------------------------------|
| DISTINCT     | `DISTINCT` を指定した場合、返される結果から重複する値が削除されます。オプションです。                                                      |
| measure_expr | 任意の式を指定できます。測定列のNULL値は無視されます。                                                                   |
| OVER         | `OVER` 句を使用してウィンドウを定義し、計算を行います。詳細については、[分析関数の説明](../400.analysis-functions-of-oracle-mode/100.window-function-description-of-oracle-mode.md)を参照してください。 |

## 返り値の型

`CLOB` 型のデータ値を返します。

## 例

既に作成されたテーブル `employees` があります。

```sql
obclient> SELECT * FROM employees;
+---------------+-----------+------------+--------+
| DEPARTMENT_ID | LAST_NAME | HIREDATE   | SALARY |
+---------------+-----------+------------+--------+
|            30 | Raphaely  | 2017-07-01 |   1700 |
|            30 | De Haan   | 2018-05-01 |  11000 |
|            40 | Errazuriz | 2017-07-21 |   1400 |
|            50 | Hartstein | 2019-10-05 |  14000 |
|            50 | Raphaely  | 2017-07-22 |   1700 |
|            50 | Weiss     | 2019-10-05 |  13500 |
|            90 | Russell   | 2019-07-11 |  13000 |
|            90 | Partners  | 2018-12-01 |  14000 |
+---------------+-----------+------------+--------+
8 rows in set
```

### 集計関数の例

列 `last_name` のすべての人名を1行に表示します。

```sql
obclient> SELECT WM_CONCAT(last_name) "Emp_list" FROM employees;
+----------------------------------------------------------------------+
| Emp_list                                                             |
+----------------------------------------------------------------------+
| Raphaely,De Haan,Errazuriz,Hartstein,Raphaely,Weiss,Russell,Partners |
+----------------------------------------------------------------------+
1 row in set
```

### 分析関数の例

列 `department_id` でグループ化し、同一グループ内のすべての `last_name` を照会します。

```sql
obclient> SELECT department_id,hiredate,last_name,
              WM_CONCAT(last_name) OVER (PARTITION BY department_id) as "Emp_list"
    FROM employees
    ORDER BY 1,2;
+---------------+------------+-----------+--------------------------+
| DEPARTMENT_ID | HIREDATE   | LAST_NAME | Emp_list                 |
+---------------+------------+-----------+--------------------------+
|            30 | 2017-07-01 | Raphaely  | Raphaely,De Haan         |
|            30 | 2018-05-01 | De Haan   | Raphaely,De Haan         |
|            40 | 2017-07-21 | Errazuriz | Errazuriz                |
|            50 | 2017-07-22 | Raphaely  | Hartstein,Raphaely,Weiss |
|            50 | 2019-10-05 | Hartstein | Hartstein,Raphaely,Weiss |
|            50 | 2019-10-05 | Weiss     | Hartstein,Raphaely,Weiss |
|            90 | 2018-12-01 | Partners  | Russell,Partners         |
|            90 | 2019-07-11 | Russell   | Russell,Partners         |
+---------------+------------+-----------+--------------------------+
8 rows in set
```