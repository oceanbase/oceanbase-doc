| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
|machine-translation| en          |
|slug|conversion-rules-from-string-to-date-oracle-compatible-mode|

# 文字列から日付への変換ルール

文字列値を日付値に変換する際は、一定の変換ルールに従う必要があります。

変換ルールは以下のとおりです：

* 数値書式モデル要素のすべての数値（先頭のゼロを含む）を指定した場合、日付文字列から書式文字列に含まれる句読点を省略できます。例えば、2桁の書式要素（`MM`、`DD`、`YY`など）には、2ではなく02を指定します。

* 書式文字列の末尾にある時間フィールドは、日付文字列から省略できます。

* 日付文字列では、書式文字列内の句読点に一致させるために、任意の非英字数字文字を使用できます。