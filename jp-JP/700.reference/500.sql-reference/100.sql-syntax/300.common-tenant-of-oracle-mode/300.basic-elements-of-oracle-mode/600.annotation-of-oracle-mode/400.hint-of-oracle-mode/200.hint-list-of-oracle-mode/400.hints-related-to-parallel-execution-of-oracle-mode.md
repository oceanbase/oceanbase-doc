| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
|machine-translation|en,ja-marker|
|slug|hints-related-to-parallel-execution-oracle-compatible-mode|

# パラレル実行に関連するヒント

パラレル実行に関連するヒントには、PARALLELヒント、NO_PARALLELヒント、USE_PXヒント、ENABLE_PARALLEL_DAS_DMLヒント、DISABLE_PARALLEL_DAS_DMLヒント、およびPQ_DISTRIBUTEヒントが含まれます。

## PARALLEL ヒント

`PARALLEL` ヒントはステートメントレベルのヒントであり、オプティマイザーに対して並列操作で使用可能な並列スレッド数を指定するよう指示します。このヒントは、初期化パラメータ `PARALLEL_DEGREE_POLICY` の値を上書きします。このヒントは、`SELECT`、`INSERT`、`UPDATE`、および `DELETE` ステートメントの一部として、またテーブルスキャンの一部として使用できます。いずれかの並列制限に違反した場合、`PARALLEL` ヒントは無視されます。

`PARALLEL` ヒントの構文は次のとおりです：

```sql
/*+ PARALLEL(n) */
```

`PARALLEL` ヒントのパラメータ `n` の値は、並列度を指定するために使用されます。ソートまたはグループ化操作が同時に実行される場合、使用可能なスレッド数は `PARALLEL` ヒントで設定された値の2倍になります。

次の例では、データベースの並列度を5に指定しています：

```sql
SELECT /*+ PARALLEL(5) */ last_name
FROM emp;
```

OceanBaseデータベースは、テーブルレベルの `PARALLEL` ヒントもサポートしており、その構文は次のとおりです：

```sql
/*+ PARALLEL(table_name n) */
```

グローバル並列度とテーブルレベルの並列度が同時に指定されている場合、テーブルレベルの並列度は有効になりません。

## NO_PARALLEL ヒント

`NO_PARALLEL` ヒントは、オプティマイザーにステートメントをシリアル実行するよう指示します。これは、テーブルの作成または変更に使用される `PARALLEL` パラメータを上書きします。

`NO_PARALLEL` ヒントの構文は次のとおりです：

```sql
/*+ NO_PARALLEL ( [ @queryblock ] tablespec ) */
```

次の例では、`NO_PARALLEL` ヒントが `PARALLEL` パラメータ値を上書きしています。

```sql
ALTER TABLE emp PARALLEL 6;
SELECT /*+ NO_PARALLEL(hr_emp) */ last_name
  FROM emp hr_emp;
```

## USE_PX ヒント

`USE_PX` ヒントは、サーバーに対しSQL文の実行時にPXモードを使用するよう強制的に指示します。PXモードでは、文の実行時にマルチスレッド方式を採用できます。一般的に、`USE_PX` ヒントと `PARALLEL` ヒントは組み合わせて使用されます。

`USE_PX` ヒントの構文は次のとおりです：

```sql
/*+ USE_PX */
```

`USE_PX` ヒントの例は次のとおりです：

```sql
SELECT /*+ USE_PX PARALLEL(4)*/ e.dept_id, sum(e.salary)
FROM emp e
WHERE e.dept_id = 1001 GROUP BY e.dept_id;
```

## ENABLE_PARALLEL_DML ヒント

`ENABLE_PARALLEL_DML` ヒントは、現在のクエリでパラレルDMLを有効にすることを指定します。その逆のヒントは `DISABLE_PARALLEL_DML` であり、パラレルDMLを無効にするために使用されます。

パラレルDMLの詳細については、[パラレルDML](../../../../../../../1000.performance-tuning-guide/500.sql-optimization/300.distributed-execution-plan/1000.parallel-dml.md)を参照してください。

### 注意事項

- `/*+ENABLE_PARALLEL_DML PARALLEL(n)*/` を使用する場合、システムは分散パラレルDML（PDML）の使用を優先します。現在の環境がPDMLをサポートしていない場合、システムはパフォーマンス最適化のためにDAS並列書き込みにフォールバックします。

- セッションレベルで強制的な並列度が設定されている場合、その実行動作は上記の動作と一致します。

- 現在の環境がパラレルDML（PDML）をサポートしていない場合でも、システムパラメータ `_enable_parallel_das_dml` が `ALTER SYSTEM SET` コマンドで `true` に設定されている場合（デフォルト値は `false`）、ヒント `/*+ENABLE_PARALLEL_DML PARALLEL(n)*/` を使用しても、システムはDAS（分散アクティブストレージ）のパラレル書き込み操作を有効にしません。

- `_enable_parallel_das_dml` を `ALTER SYSTEM SET` コマンドで `true` に設定した場合（デフォルトは `false`）、かつセッションレベルで強制的な並列度が設定されている場合、現在の環境がPDMLをサポートしていない場合でも、ヒント `/*+ENABLE_PARALLEL_DML PARALLEL(n)*/` を使用しても、DASのパラレル書き込み機能は有効になりません。

### 構文

```sql
/*+ ENABLE_PARALLEL_DML */
```

### 例

```sql
insert /*+ enable_parallel_dml parallel(8) */ into t2 select * from t1;
```

## DISABLE_PARALLEL_DML ヒント

`DISABLE_PARALLEL_DML` ヒントは、現在のクエリでパラレルDMLを無効にすることを指定します。その逆のヒントは `ENABLE_PARALLEL_DML` であり、パラレルDMLを有効にするために使用されます。

パラレルDMLの詳細については、[パラレルDML](../../../../../../../1000.performance-tuning-guide/500.sql-optimization/300.distributed-execution-plan/1000.parallel-dml.md)を参照してください。

### 構文

```sql
/*+ DISABLE_PARALLEL_DML */
```

### 例

```sql
insert /*+ disable_parallel_dml parallel(8) */ into t2 select * from t1;
```

## ENABLE_PARALLEL_DAS_DML ヒント

`ENABLE_PARALLEL_DAS_DML` ヒントは、現在のDMLステートメントでDistributed Active Storage (DAS)並列書き込み最適化を強制的に有効にするよう指定します。その逆ヒントは `DISABLE_PARALLEL_DAS_DML` です。

### 注意事項

- `ENABLE_PARALLEL_DAS_DML` は `ENABLE_PARALLEL_DML` と一緒に使用する必要があります。

- ヒント `/*+ENABLE_PARALLEL_DAS_DML ENABLE_PARALLEL_DML PARALLEL(n)*/` の作用により、システムはDAS並列書き込みを強制オプションとし、`PARALLEL(n)` で指定された並列度に基づいて操作を実行します。

- システムパラメータ `_enable_parallel_das_dml` を `ALTER SYSTEM SET` ステートメントで `true` に設定すると（デフォルトは `false`）、ヒント `/*+ENABLE_PARALLEL_DAS_DML ENABLE_PARALLEL_DML PARALLEL(n)*/` は無効になります。この場合、システムはDASの並列書き込み操作を実行しません。

### 構文

```sql
/*+ ENABLE_PARALLEL_DAS_DML */
```

### 例

```sql
insert /*+ ENABLE_PARALLEL_DAS_DML ENABLE_PARALLEL_DML PARALLEL(10)*/
into t1 select * from t2;
```

## DISABLE_PARALLEL_DAS_DML ヒント

`DISABLE_PARALLEL_DAS_DML` ヒントは、現在のDMLステートメントでDistributed Active Storage (DAS)並列書き込み最適化を強制的に無効にするよう指定します。その逆のヒントは `ENABLE_PARALLEL_DAS_DML` です。

### 注意事項

- `DISABLE_PARALLEL_DAS_DML` は `ENABLE_PARALLEL_DML` と一緒に使用する必要があります。

- ヒント `/*+DISABLE_PARALLEL_DAS_DML ENABLE_PARALLEL_DML PARALLEL(n)*/` を使用すると、PDMLなど他の並列書き込み最適化オプションが有効になっていても、システムはDAS並列書き込みを無効にします。

### 構文

```sql
/*+ DISABLE_PARALLEL_DAS_DML */
```

### 例

```sql
insert /*+ DISABLE_PARALLEL_DAS_DML ENABLE_PARALLEL_DML PARALLEL(10)*/
into t1 select * from t2;
```

## PQ_DISTRIBUTE ヒント

`PQ_DISTRIBUTE` ヒントは、プログラム（クエリ）サーバーと消費（負荷）クエリサーバー間で行をどのように割り当てるかをオプティマイザーに指示します。このヒントを使用して、結合または負荷の行分布を制御できます。
`PQ_DISTRIBUTE` ヒントの構文は次のとおりです：

```sql
/*+ PQ_DISTRIBUTE
  ( [ @queryblock ] tablespec
    { distribution | outer_distribution inner_distribution }
  ) */
```

### 接合の分散を制御する

指定された2つの分散方法を使用して、結合の分散方法を制御できます。

構文の下部に示すように：

* `outer_distribution` 左側のテーブルのデータ分散方式を指定します。

* `inner_distribution` 右側のテーブルのデータ分散方式を指定します。

分散方式には `HASH`、`BROADCAST`、`PARTITION`、`NONE` が含まれます。有効な組み合わせは次の6種類のみです：

|      ディストリビューション方式       |                                                                                             説明                                                                                             |
|-----------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| HASH, HASH      | 接続キー上のハッシュ関数を使用して、各テーブルの行をクエリサーバーにマッピングします。マッピング完了後、各クエリサーバーは一対の結果パーティション間で結合を実行します。 テーブルのサイズが比較可能であり、結合操作がハッシュ結合またはソート・コンパクション結合によって実現される場合、このディストリビューション方式を使用することを推奨します。                                                                        |
| BROADCAST, NONE | 外部テーブルのすべての行が各クエリサーバーにブロードキャストされます。内部テーブルの行はランダムにパーティション化されます。 外部テーブルが内部テーブルに比べて非常に小さい場合、この分散方法を使用することを推奨します。通常、内部テーブルのサイズにクエリサーバー数を乗じた値が外部テーブルのサイズを上回る場合も、このディストリビューション方式を使用することを推奨します。                                                                            |
| NONE, BROADCAST | 内部テーブルのすべての行が各クエリサーバーにブロードキャストされます。外部テーブルの行はランダムにパーティション化されます。 内部テーブルが外部テーブルに比べて非常に小さい場合、この分散方法を使用することを推奨します。通常、内部テーブルのサイズにクエリサーバー数を乗じた値が外部テーブルのサイズを下回る場合も、このディストリビューション方式を使用することを推奨します。                                                                            |
| PARTITION, NONE | 外部テーブルの行は内部テーブルのパーティションを使用してマッピングされます。内部テーブルは結合キー上でパーティション化されていなければなりません。 外部テーブルのパーティション数がクエリサーバー数の倍数に等しいか、ほぼ等しい場合、このディストリビューション方式を使用することを推奨します。例えば、14個のパーティションと15個のクエリサーバーがある場合。<br>**注記**  内部テーブルがパーティション化されていない場合、またはパーティションキー上で均等に結合されていない場合、オプティマイザーはこのヒントを無視します。  |
| NONE, PARTITION | 内部テーブルの行は外部テーブルのパーティションを使用してマッピングされます。外部テーブルは結合キー上でパーティション化されていなければなりません。 外部テーブルのパーティション数がクエリサーバー数の倍数に等しいか、ほぼ等しい場合、このディストリビューション方式を使用することを推奨します。例えば、14個のパーティションと15個のクエリサーバーがある場合。<br>**注記**  外部テーブルがパーティションキー上でパーティション化されていない場合、または均等に結合されていない場合、オプティマイザーはこのヒントを無視します。 |
| NONE, NONE      | 各クエリサーバーは、マッチする一対のパーティション間で結合操作を実行します。各テーブルにはそれぞれ1つのパーティションが存在します。2つのテーブルは結合キー上で均等に分割されていなければなりません。                                                                                                                                             |

たとえば、ハッシュ結合を使用して2つのテーブル `r` と `s` を結合する場合、次のクエリにはハッシュ分散方式を使用するヒントが含まれています：

```sql
SELECT /*+ORDERED PQ_DISTRIBUTE(s HASH, HASH) USE_HASH (s)*/ column_list
FROM r,s
WHERE r.c=s.c;
```

外部テーブル `r` をブロードキャストする場合、ヒントを含むクエリステートメントは次のとおりです：

```sql
SELECT /*+ORDERED PQ_DISTRIBUTE(s BROADCAST, NONE) USE_HASH (s) */ column_list
FROM r,s
WHERE r.c=s.c;
```