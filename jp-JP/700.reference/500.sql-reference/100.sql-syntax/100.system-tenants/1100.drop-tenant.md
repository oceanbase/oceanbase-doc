| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   |                 |
|machine-translation| en          |
|slug|drop-tenant|

# DROP TENANT

## 説明

このステートメントは、OceanBaseデータベースのテナントを削除するために使用されます。

  <main id="notice" type='notice'>
    <h4>注意</h4>
    <p><code>DROP TENANT</code> を実行してテナントを削除するには、rootユーザーでsysテナントに接続している必要があります。</p>
  </main>

## 構文

```sql
drop_tenant_stmt:
DROP TENANT [IF EXISTS] tenant_name [PURGE|FORCE];
```

## パラメータの説明

|   **パラメータ**    |                **説明**                |
|-------------|--------------------------------------|
| tenant_name | 削除するテナント名を指定します。                           |
| PURGE       | テナントを削除し、削除後のテナントは必ずごみ箱に入らないことを示します。`sys` テナントのごみ箱機能が無効な場合、`DROP TENANT` ステートメントと `DROP TENANT... PURGE` ステートメントの効果は同じです。 |
| FORCE       | テナントを強制的に削除することを示します。物理バックアップデータの整合性は保証されません。削除後のテナントはごみ箱に入りません。 |

## 例

テナント `test_tenant1` を削除します。

```sql
obclient> DROP TENANT test_tenant1;
```