| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   |                 |
|machine-translation| en          |
|slug|sql-tenant-switchover|

# SWITCHOVER

## 説明

このステートメントは、テナントのプライマリ/スタンバイロール切り替えコマンドであり、プライマリテナントを非破壊的にスタンバイテナントに切り替えたり、スタンバイテナントをプライマリテナントに切り替えたりすることができます。

## 構文

```sql
ALTER SYSTEM SWITCHOVER TO {STANDBY | PRIMARY}
    [TENANT tenant_name]
    [VERIFY];
```

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p><ul><li>システムテナントがこのコマンドを実行する場合は、<code>TENANT tenant_name</code> パラメータを指定する必要があります。</li><li>通常のテナントは、自身に対してのみこのコマンドを実行できます。</li></ul></p>
</main>

## 構文の説明

|   **フィールド**  | **説明** |
| ----------- | -------- |
| STANDBY     | プライマリテナントをスタンバイテナントにロスレスで切り替えます。|
| PRIMARY     | スタンバイテナントをプライマリテナントに切り替えます。|
| tenant_name | プライマリ/スタンバイテナント名を指定します。|
| VERIFY      | オプションです。このパラメータを使用する場合、`SWITCHOVER` コマンドの実行に必要な前提条件がすでに整っているかどうかを確認するための事前検証を行います。 <main id="notice" type='notice'><h4>注意</h4><p><ul><li>プライマリ/スタンバイテナントのロール切り替えには失敗のリスクが伴います。<code>VERIFY</code> コマンドは事前検証に過ぎず、ロール切り替えの前提条件を満たしているか評価するものであり、操作の失敗可能性を低減するのに役立ちますが、<code>SWITCHOVER</code> 操作自体は実際には実行しません。</li><li><code>VERIFY</code> コマンドが正常に実行されたからといって、その後の <code>SWITCHOVER</code> 操作が必ず成功するとは限りません。</li></ul></p></main> |

## 例

プライマリテナントに対してプライマリからスタンバイへの切り替えコマンドを実行し、テナントをスタンバイテナント `tenant1` に変更します。その後、プライマリテナントがスタンバイテナントに切り替わり、`TENANT_ROLE` が `STANDBY` に、`SWITCHOVER_STATUS` が `NORMAL` になっているか確認します。

```shell
obclient> ALTER SYSTEM SWITCHOVER TO STANDBY TENANT tenant1;
Query OK, 0 rows affected

obclient> SELECT TENANT_NAME,TENANT_TYPE,TENANT_ROLE,SWITCHOVER_STATUS FROM DBA_OB_TENANTS
    WHERE TENANT_NAME='tenant1';
+---------------------+-------------+-------------+-------------------+
| TENANT_NAME         | TENANT_TYPE | TENANT_ROLE | SWITCHOVER_STATUS |
+---------------------+-------------+-------------+-------------------+
| t1                  | USER        | STANDBY     | NORMAL            |
+---------------------+-------------+-------------+-------------------+
1 row in set
```

## 関連ドキュメント

`SWITCHOVER` コマンドの使用方法の詳細については、[Switchover](../../../../../600.manage/400.high-availability/300.physical-standby-database-disaster-recovery/600.role-switch/200.perform-switchover.md)を参照してください。