| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   |                 |
|machine-translation|en,ja-marker|
|slug|sql-tenant-bootstrap|

# BOOTSTRAP

## 説明

このステートメントは、OceanBaseクラスタを起動するために使用されます。

このコマンドは、OceanBaseデータベースが初めて起動される際にのみ実行する必要があり、各クラスタでこのコマンドを実行できるのは一度だけです。このコマンドの実行に失敗した場合、再実行することはできず、OceanBaseデータベースクラスタを再構築する必要があります。

このコマンドを実行する前に、指定された各OBServerノードがすべて正常に起動していることを確認してください。

## 構文

```sql
alter_system_bootstrap_stmt_primary:
    ALTER SYSTEM BOOTSTRAP region_zone_server_list;
region_zone_server_list:
    region_zone_server [, region_zone_server ...]
region_zone_server:
    {
      REGION [=] region_name
      ZONE [=] zone_name [ IDC [=] idc_info ]
      SERVER [=] ip_port
      }
ip_port:
    'STR_VALUE:INT_VALUE'
```

## パラメータの説明

|          **パラメータ**         |                **説明**               |
|--------------------------|---------------------------------------|
| region_name              | Root Serviceが配置されるリージョンを指定します。"マルチリージョン・マルチデータセンター"構成でのデプロイ時に指定します。 |
| zone_name                | Root Serviceが所属するゾーンを指定します。              |
| ip_port                  | Root ServiceのIPアドレスとRPCポート番号を指定します。               |
| idc_info                 | ゾーンが所属するIDC情報。 |

## 例

Root Serviceマシンリストを指定してクラスタを起動します。リストに複数のOBServerノードが含まれる場合は、カンマ（,）で区切る必要があります。

```shell
obclient> ALTER SYSTEM BOOTSTRAP ZONE 'zone1' SERVER 'xxx.xxx.xx.xx1:xxxx', ZONE 'zone2' SERVER 'xxx.xxx.xx.xx2:xxxx', ZONE 'zone3' SERVER 'xxx.xxx.xx.xx3:xxxx';
```
