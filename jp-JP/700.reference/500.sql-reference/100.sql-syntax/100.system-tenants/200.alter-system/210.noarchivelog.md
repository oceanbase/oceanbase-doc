| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   |                 |
|machine-translation|en,ja-marker|
|slug|noarchivelog|

# NOARCHIVELOG

## 説明

ログアーカイブモードには、アーカイブ（ARCHIVELOG）と非アーカイブ（NOARCHIVELOG）の2種類があります。ログアーカイブモードを有効にした後、`ALTER SYSTEM NOARCHIVELOG` ステートメントを使用してユーザーテナントのアーカイブモードを無効にできます。

## 制限事項と注意点

`ALTER SYSTEM NOARCHIVELOG` ステートメントを使用してアーカイブモードを無効にする前に、現在のテナントで進行中または一時停止されているログアーカイブタスクがある場合、アーカイブモードを無効にすると、システムはこれらのアーカイブタスクを自動的に停止します。

## 権限要件

`sys` テナントの `root` ユーザー（`root@sys`）または各テナントの管理者ユーザーが実行する必要があります。そのうち：

* MySQLモードのデフォルト管理者ユーザーは `root` ユーザーです。
* Oracleモードのデフォルト管理者ユーザーは `SYS` ユーザーです。

## 構文

```sql
ALTER SYSTEM NOARCHIVELOG [TENANT = {all | tenant_name[, tenant_name...]}] [DESCRIPTION [=] 'description'];
```

## パラメータの説明

| パラメータ                  | 説明                                                                                                   |
|-------------------------|--------------------------------------------------------------------------------------------------------|
| all                     | システムテナントがクラスタ内のすべてのユーザーテナントに対してアーカイブモードを無効にします。                                                                  |
| tenant_name[, tenant_name...]    | システムテナントがアーカイブモードを無効にするテナント名を指定します。複数のテナントを同時に指定でき、異なるテナント間はカンマ（,）で区切ります。<main id="notice" type='notice'><h4>注意</h4><p>このコマンドを実行する際は、システムテナントのみが <code>TENANT tenant_name</code> 句を使用して操作対象のテナントを指定する必要があります。ユーザーテナントは自身のテナントに対してのみこのコマンドを実行できるため、この句を指定する必要はありません。</p></main>  |
| description             | この操作の説明情報を指定します。省略可能です。                                                                              |

## 例

現在のクラスタに `sys`、`mysql_tenant`、`oracle_tenant` の3つのテナントがあり、ユーザーテナント `mysql_tenant` と `oracle_tenant` はどちらもアーカイブモードが有効になっていると仮定します。次のステートメントを使用して、テナントのアーカイブモードを有効にできます。

* システムテナントがテナント `mysql_tenant` のアーカイブモードを無効にします。

   ```sql
   obclient [oceanbase]> ALTER SYSTEM NOARCHIVELOG TENANT = mysql_tenant;
   ```

* システムテナントがテナント `mysql_tenant` と `oracle_tenant` のアーカイブモードを無効にします。

   ```sql
   obclient [oceanbase]> ALTER SYSTEM NOARCHIVELOG TENANT = mysql_tenant, oracle_tenant;
   ```

* システムテナントがすべてのユーザーテナントのアーカイブモードを無効にします。

   ```sql
   obclient [oceanbase]> ALTER SYSTEM NOARCHIVELOG TENANT = all;
   ```

* ユーザーテナント `mysql_tenant` と `oracle_tenant` がそれぞれ自身のアーカイブモードを無効にします。

   ```sql
   obclient [oceanbase]> ALTER SYSTEM NOARCHIVELOG;
   ```

## 関連ドキュメント

* [SET LOG_ARCHIVE_DEST](150.set-log-archive-dest.md)

* [ARCHIVELOG](200.archivelog.md)

* [アーカイブモードの無効化](../../../../../600.manage/600.backup-and-recovery/300.log-archive/400.close-the-log-archive-mode.md)