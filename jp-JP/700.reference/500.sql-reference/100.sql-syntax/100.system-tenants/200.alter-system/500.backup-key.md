| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   |                 |
|machine-translation| en          |
|slug|sql-tenant-backup-key|

# BACKUP KEY

## 説明

データバックアップの前に、元テナントで透過的データ暗号化が設定されている場合は、`ALTER SYSTEM BACKUP KEY` ステートメントを使用して、ソーステナントのバックアップキーを作成する必要があります。

## 使用上の制限と注意事項

バックアップキーを作成した後、データを復元する前に、ソーステナントで再度キーローテーションがトリガーされた場合は、ソーステナントのキーを再度バックアップする必要があります。

## 権限要件

`sys` テナントの `root` ユーザー（`root@sys`）または各テナントの管理者ユーザーが実行する必要があります。具体的には：

* MySQLモードのデフォルト管理者ユーザーは `root` ユーザーです。
* Oracleモードのデフォルト管理者ユーザーは `SYS` ユーザーです。

## 構文

```sql
ALTER SYSTEM BACKUP KEY [TENANT [=] {tenant_name[, tenant_name]...}] TO 'backup_key_path' [ENCRYPTED BY 'password'];
```

## パラメータ説明

| パラメータ                   | 説明                                                                                                                                      |
|-----------------------|--------------------------------------------------------------------------------------------------------------------------------------------|
| tenant_name           | システムテナントがバックアップキーを指定するテナント名を指定します。複数のテナント名を同時に指定することができ、異なるテナント名は半角カンマ（,）で区切ります。システムテナントがテナントを指定しない場合、クラスタ内のすべてのユーザーテナントのキーをバックアップすることを意味します。<main id="notice" type='notice'> <h4>注意</h4><p>このコマンドを実行する際、システムテナントのみが <code>TENANT = tenant_name</code> 句を使用して操作対象のテナントを指定できます。ユーザーテナントは自身のテナントでのみこのコマンドを実行できるため、<code>TENANT = tenant_name</code> 句を指定することはできません。</p></main>                        |
| backup_key_path       | バックアップキーを保存するパスを指定します。このパスはユーザーが自由に設定でき、データバックアップやログアーカイブのパスと同じにすることはできません。                                                     |
| password              | バックアップキーのパスを暗号化するためのパスワードを指定します。指定しない場合、デフォルトではバックアップパスは暗号化されません。                                                                                 |

## 例

* システムテナントが

* システムテナントがテナント `mysql_tenant` のキーを `file:///data_backup_dest/key` にバックアップし、暗号化パスワードを設定します。

  ```sql
  obclient [oceanbase]> ALTER SYSTEM BACKUP KEY TENANT = mysql_tenant TO 'file:///data_backup_dest/key' ENCRYPTED BY '******';
  ```

* テナント `mysql_tenant` が自身のキーを `file:///data_backup_dest/key` にバックアップします。

  ```sql
  obclient [oceanbase]> ALTER SYSTEM BACKUP KEY TO 'file:///data_backup_dest/key';
  ```

## 関連ドキュメント

* [データ透過的暗号化の概要](../../../../../600.manage/500.security-and-permissions/500.data-encryption/400.data-storage-encryption/100.datastore-encryption-overview.md)

* [バックアップ前の準備](../../../../../600.manage/600.backup-and-recovery/400.data-backup/100.preparation-before-data-backup.md)

* [RESTORE](3300.restore.md)

* [RECOVER TABLE](3400.recover-table.md)