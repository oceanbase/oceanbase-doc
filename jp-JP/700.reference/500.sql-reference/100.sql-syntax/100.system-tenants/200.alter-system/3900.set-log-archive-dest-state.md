| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   |                 |
|machine-translation| en          |
|slug|sql-tenant-set-log_archive_dest_state|

# SET LOG_ARCHIVE_DEST_STATE

## 説明

`SET LOG_ARCHIVE_DEST_STATE` ステートメントは、アーカイブ宛先の状態を設定します。

## 権限要件

`sys` テナントの `root` ユーザー(`root@sys`)または各テナントの管理者ユーザーが実行する必要があります。具体的には：

* MySQLモードのデフォルト管理者ユーザーは `root` ユーザーです。
* Oracleモードのデフォルト管理者ユーザーは `SYS` ユーザーです。

## 構文

```sql
ALTER SYSTEM SET LOG_ARCHIVE_DEST_STATE={'ENABLE' | 'DEFER'} [TENANT = tenant_name];
```

## パラメータ説明

| パラメータ                    | 説明                                                                                                   |
|-------------------------|--------------------------------------------------------------------------------------------------------|
| 'ENABLE' \| 'DEFER'     | アーカイブ宛先の状態を指定します：<ul><li>`ENABLE`：有効状態。この状態でアーカイブモードが有効で、まだアーカイブが開始されていない場合、システムはアーカイブタスクの実行を開始します。アーカイブ宛先の設定が成功すると、デフォルトでこの状態になります。</li> <li>`DEFER`：一時停止状態。この状態でアーカイブモードが有効で、アーカイブタスクがある場合、システムはアーカイブタスクを一時停止します。</li></ul>                                                                   |
| tenant_name             | システムテナントが設定対象のテナント名を指定します。1文につき1テナントのアーカイブ宛先のみ設定できます。<main id="notice" type='notice'><h4>注意</h4><p>このコマンドを実行する際は、システムテナントのみが <code>TENANT tenant_name</code> 句で操作対象のテナントを指定する必要があります。ユーザーテナントは自身のテナントでのみこのコマンドを実行できるため、この句の指定は不要です。</p></main>  |

## 例

テナント `mysql_tenant` のアーカイブ宛先設定が完了していることを前提とした例です：

* システムテナントがテナント `mysql_tenant` の宛先上でのログアーカイブを一時停止します。

  ```sql
  obclient [oceanbase]> ALTER SYSTEM SET LOG_ARCHIVE_DEST_STATE = 'DEFER' TENANT = mysql_tenant;
  ```

* テナント `mysql_tenant` が自身のテナントの宛先上でのログアーカイブを開始します。

  ```sql
  obclient [oceanbase]> ALTER SYSTEM SET LOG_ARCHIVE_DEST_STATE = 'ENABLE';
  ```

## 関連ドキュメント

* [SET LOG_ARCHIVE_DEST](3800.set-log-archive-dest.md)

* [ARCHIVELOG](200.archivelog.md)

* [ログアーカイブの準備](../../../../../600.manage/600.backup-and-recovery/300.log-archive/200.preparation-before-log-archive.md)