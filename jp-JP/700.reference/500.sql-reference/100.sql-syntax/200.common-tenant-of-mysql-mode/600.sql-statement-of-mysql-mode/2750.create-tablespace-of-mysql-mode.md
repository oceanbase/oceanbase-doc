| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |
|machine-translation| en          |
|slug|create-tablespace-mysql-compatible-mode|

# CREATE TABLESPACE

## 説明

このステートメントは、TableSpace（テーブルスペース）の論理オブジェクトを作成するために使用します。TableSpace属性は暗号化をサポートしています。

## 構文

* 暗号化されたテーブルスペースを作成します。

  ```sql
  CREATE TABLESPACE tablespace_name [ENCRYPTION [=] 'tablespace_encryption_option'];

  tablespace_encryption_option:
      N
      | Y
      | AES-256
      | AES-256-GCM
      | AES-128
      | AES-128-GCM
      | AES-192
      | AES-192-GCM
      | SM4-CBC
      | SM4-GCM
  ```

* テーブルスペースを作成します。

  ```sql
    CREATE TABLESPACE tablespace_name
    [ADD DATAFILE 'file_name']
    [FILE_BLOCK_SIZE = value]
    USE LOGFILE GROUP logfile_group
    [EXTENT_SIZE [=] extent_size]
    [INITIAL_SIZE [=] initial_size]
    [AUTOEXTEND_SIZE [=] autoextend_size]
    [MAX_SIZE [=] max_size]
    [NODEGROUP [=] nodegroup_id]
    [WAIT]
    [COMMENT [=] 'string']
    [ENGINE [=] engine_name]
  ```

  <main id="notice" type='notice'>
  <h4>注意</h4>
  <p>このキーワード構文のみがサポートされており、機能は有効ではありません。この構文のキーワードは <code>ENCRYPTION</code> と同時に実行できません。そうした場合、構文エラーが発生します。</p>
  </main>

## パラメータ説明

|        パラメータ        |          説明           |
|-------------------|-------------------------|
| tablespace_name   | 作成するTableSpaceオブジェクト名を指定します。|
| ENCRYPTION        | オプションです。暗号化に使用するキーワードを指定し、TableSpaceで使用する暗号化アルゴリズムを指定します。<code>ENCRYPTION`キーワードで暗号化アルゴリズムを指定しない場合、デフォルトで暗号化されていないTableSpaceが作成されます。暗号化アルゴリズムの詳細については、後述の [tablespace_encryption_option](#tablespace_encryption_option) を参照してください。|

### tablespace_encryption_option

* `N`：暗号化しないことを示します。
* `Y`：`Y`を使用する場合、デフォルトで`AES-256`が使用されます。
* `AES-256`：256ビットの鍵を使用して暗号化および復号化を行います。
* `AES-256-GCM`：256ビットの鍵を使用したGCMモードです。
* `AES-128`：128ビットの鍵を使用して暗号化および復号化を行います。
* `AES-128-GCM`：128ビットの鍵を使用したGCMモードです。
* `AES-192`：192ビットの鍵を使用して暗号化および復号化を行います。
* `AES-192-GCM`：192ビットの鍵を使用したGCMモードです。
* `SM4-CBC`：SM4アルゴリズムはCBCモードを採用しており、各データブロックは前のブロックの暗号化出力とXOR演算を行った後に暗号化されます。
* `SM4-GCM`：SM4アルゴリズムはGCMモードを採用しており、このモードではSM4がデータの暗号化と認証の両方の機能を提供します。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p><ul><li>AES (Advanced Encryption Standard) は対称鍵暗号方式であり、同じ鍵をデータの暗号化と復号化に使用します。AES アルゴリズム自体はブロック暗号に基づいており、通常は128ビットのブロックでデータを暗号化します。</li><li>AES-GCM (Galois/Counter Mode) は、ブロック暗号アルゴリズム（例：AES）を処理するための運用モードであり、暗号化とデータ認証（メッセージ認証コードとも呼ばれる）を提供します。</li><li>SM4 アルゴリズムのブロック長は128ビット、鍵長も128ビットです。</li></ul></p>
</main>

## 例

* 暗号化されていないTableSpaceオブジェクトを作成します。

  ```shell
  CREATE TABLESPACE tablespace001;
  ```

* 透過的データ暗号化(TDE)を使用するTableSpaceオブジェクトを作成します。

  1. 暗号化方式を組み込みの透過的暗号化に設定します。

     ```shell
     ALTER SYSTEM SET tde_method = 'internal';
     ```

  2. 現在の暗号化方式の設定を表示します。

     ```shell
     SHOW PARAMETERS LIKE 'tde_method';
     ```

  3. 新しいマスター暗号鍵を生成します。

     ```shell
     ALTER INSTANCE ROTATE INNODB MASTER KEY;
     ```

      <main id="notice" type='notice'>
        <h4>注意</h4>
        <p>このコマンドは実行可能ですが、ディスク容量が満杯の場合、新しいマスター暗号鍵の生成は完了しません。</p>
      </main>

  4. `tablespace2` という名前の暗号化されたTableSpaceを作成し、`SM4-GCM` 暗号アルゴリズムを使用します。

     ```shell
     CREATE TABLESPACE tablespace002 ENCRYPTION = 'SM4-GCM';
     ```

## 関連ドキュメント

* [tde_method](../../../../800.configuration-items-and-system-variables/100.system-configuration-items/400.tenant-level-configuration-items/7800.tde_method.md)
* [新規作成テーブルへの透過的暗号化の設定](../../../../../600.manage/500.security-and-permissions/500.data-encryption/400.data-storage-encryption/200.data-storage-encryption-of-mysql-mode/100.configure-internal-storage-encryption-of-mysql-mode.md)