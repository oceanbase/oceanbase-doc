| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |
|machine-translation| en          |
|slug|cache-index-mysql-mode|

# CACHE INDEX

## 説明

このステートメントは、特定のデータベーステーブルのインデックスをキャッシュにロードし、クエリ性能を向上させるために使用されます。インデックスがキャッシュにロードされると、クエリはインデックスのデータにより迅速にアクセスできるため、クエリの実行速度が向上します。

## 構文

```sql
CACHE INDEX {
    tbl_index_list [, tbl_index_list ...]
    | tbl_name PARTITION (partition_list)
    }
    IN key_cache_name;

tbl_index_list:
    tbl_name [{INDEX|KEY} (index_name[, index_name ...])]

partition_list:
    partition_name [, partition_name ...]
    | ALL
```

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>現在のバージョンではこのステートメントの構文のみサポートされており、機能は有効ではありません。</p>
</main>