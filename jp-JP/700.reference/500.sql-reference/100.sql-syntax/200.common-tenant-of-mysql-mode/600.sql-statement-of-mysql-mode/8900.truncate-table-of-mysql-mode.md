| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |
|machine-translation| en          |
|slug|truncate-table-mysql-compatible-mode|

# TRUNCATE TABLE

## 説明

このステートメントは、指定されたテーブルを完全に削除しますが、テーブル構造（テーブルで定義されたパーティション情報を含む）は保持します。論理的には、このステートメントはすべての行を削除する `DELETE FROM` ステートメントと同じです。

## 使用上の制限と注意事項

* `TRUNCATE TABLE` 操作は、テーブル内のすべてのデータを削除し、テーブルのメタデータをリセットすることで、効率的にテーブルを空にすることを実現します。この方法は、行ごとの削除（`DELETE FROM` ステートメントの使用）に比べて、特に大規模なデータテーブルを処理する際に、行ごとのロックやログ記録が不要であるため、顕著な性能上の利点があります。

* `TRUNCATE TABLE` ステートメントの実行結果では、影響を受けた行数は常に `0` 行と表示されます。

* `TRUNCATE TABLE` ステートメントを使用すると、テーブル管理プログラムは最後に使用された `AUTO_INCREMENT` の値を覚えていませんが、最初からカウントを開始します。

* `TRUNCATE` ステートメントは、トランザクション処理中やテーブルロック中には実行できません。使用するとエラーが発生します。

* テーブル定義ファイルが有効であれば、データやインデックスファイルが破損していても、`TRUNCATE TABLE` を使用してテーブルを空のテーブルとして再作成できます。

## 権限要件

`TRUNCATE TABLE` ステートメントを実行するには、現在のユーザーがテーブルに対する `DROP` 権限を持っている必要があります。OceanBaseデータベースの権限の詳細については、[MySQLモードの権限分類](../../../../../600.manage/500.security-and-permissions/300.access-control/200.user-and-permission/200.permission-of-mysql-mode/100.permission-classification-of-mysql.md)を参照してください。

## 構文

```sql
TRUNCATE [TABLE] table_name;
```

## パラメータの説明

|  **パラメータ**  | **説明** |
|------------|----------|
| table_name | 指定するテーブル名。|

## 例

1. テーブル `test_tbl1` を作成します。

   ```sql
   CREATE TABLE test_tbl1(col1 INT AUTO_INCREMENT PRIMARY KEY, col2 VARCHAR(20))
       PARTITION BY HASH(col1) PARTITIONS 5;
   ```

2. テーブル `test_tbl1` にテストデータを挿入します。

   ```sql
   INSERT INTO test_tbl1(col2) VALUES('A1'),('A2'),('A3');
   ```

   実行結果は次のとおりです：

   ```shell
   Query OK, 3 rows affected
   Records: 3  Duplicates: 0  Warnings: 0
   ```

3. テーブル `test_tbl1` を完全にクリアします。

   ```sql
   TRUNCATE TABLE test_tbl1;
   ```

4. テーブル `test_tbl1` のデータを確認します。

   ```sql
   SELECT * FROM test_tbl1;
   ```

   実行結果は次のとおりです：

   ```shell
   Empty set
   ```

5. テーブル `test_tbl1` の定義を確認します。

   ```sql
   SHOW CREATE TABLE test_tbl1;
   ```

   実行結果は次のとおりです：

   ```shell
   +-----------+-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
   | Table     | Create Table                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
   +-----------+-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
   | test_tbl1 | CREATE TABLE `test_tbl1` (
   `col1` int(11) NOT NULL AUTO_INCREMENT,
   `col2` varchar(20) DEFAULT NULL,
   PRIMARY KEY (`col1`)
   ) AUTO_INCREMENT = 1 AUTO_INCREMENT_MODE = 'ORDER' DEFAULT CHARSET = utf8mb4 ROW_FORMAT = DYNAMIC COMPRESSION = 'zstd_1.3.8' REPLICA_NUM = 1 BLOCK_SIZE = 16384 USE_BLOOM_FILTER = FALSE TABLET_SIZE = 134217728 PCTFREE = 0
   partition by hash(col1)
   (partition `p0`,
   partition `p1`,
   partition `p2`,
   partition `p3`,
   partition `p4`) |
   +-----------+-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
   1 row in set
   ```

## 関連ドキュメント

[テーブルのクリア](../../../../300.database-object-management/100.manage-object-of-mysql-mode/200.manage-tables-of-mysql-mode/700.clear-table-of-mysql-mode.md)