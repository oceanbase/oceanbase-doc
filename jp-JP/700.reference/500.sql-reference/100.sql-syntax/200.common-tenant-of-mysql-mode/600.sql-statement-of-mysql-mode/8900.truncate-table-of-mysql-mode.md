| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |
|machine-translation|en,ja-marker|
|slug|truncate-table-of-mysql-mode|

# TRUNCATE TABLE

## 説明

このステートメントは、指定されたテーブルを完全に削除しますが、テーブル構造、つまりテーブルで定義されたパーティション情報は保持されます。論理的には、このステートメントはすべての行を削除するための `DELETE FROM` ステートメントと同じです。

`TRUNCATE` ステートメントを実行するには、テーブルの削除（`DROP`）権限が必要です。これはDDLステートメントに属します。

`TRUNCATE TABLE` ステートメントと `DELETE FROM` ステートメントの違いは以下のとおりです：

* `TRUNCATE` 操作はテーブルを削除して再作成するため、特に大きなテーブルの場合、各行を削除するよりもはるかに高速です。

* `TRUNCATE TABLE` ステートメントの実行結果に表示される影響を受ける行数は常に `0` 行となります。

* `TRUNCATE TABLE` ステートメントを使用すると、テーブルマネージャーは最後に使用された `AUTO_INCREMENT` 値を覚えていませんが、最初からカウントを開始します。

* `TRUNCATE` ステートメントは、トランザクション処理やテーブルロックが進行中の場合には使用できず、使用した場合エラーが発生します。

* テーブル定義ファイルが有効であれば、データやインデックスファイルが破損していても、`TRUNCATE TABLE` を使用して空のテーブルとして再作成できます。

## 構文

```sql
TRUNCATE [TABLE] table_name;
```

## パラメータの説明

|   **パラメータ**   | **説明** |
|------------|--------|
| table_name | テーブル名を指定します。  |

## 例

テーブル `a` を完全に削除します。

```sql
obclient> TRUNCATE TABLE a;
Query OK, 0 rows affected
```