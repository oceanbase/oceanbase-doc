| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |
|machine-translation| en          |
|slug|drop-sensitive-rule-mysql-mode|

# DROP SENSITIVE RULE

## 説明

このステートメントは、機密データ保護ルールを削除するために使用します。

## 権限要件

`DROP SENSITIVE RULE` ステートメントを実行するには、現在のユーザーが `CREATE SENSITIVE RULE` 権限を持っている必要があります。OceanBaseデータベースの権限の詳細については、[MySQLモードの権限分類](../../../../../600.manage/500.security-and-permissions/300.access-control/200.user-and-permission/200.permission-of-mysql-mode/100.permission-classification-of-mysql.md)を参照してください。

## 構文

```sql
DROP SENSITIVE RULE <rule_name>;
```

## パラメータ説明

具体的なパラメータ説明は以下のとおりです：

| パラメータ | データ型 | デフォルト値 | 説明 |
|--|--|--|--|
| rule_name | 文字列 |  | 感度ルールの名前で、テナント内で一意である必要があります。 |

説明は以下のとおりです：

* ルールの削除は即時に反映されます。

制限事項：

* 一度に削除できるルールは1つだけです。

## 例

例：

```sql
-- 機密ルールr1を削除
DROP SENSITIVE RULE r1;
```

# 関連ドキュメント

* [CREATE SENSITIVE RULE](2550.create-sensitive-rule-of-mysql-mode.md)
* [ALTER SENSITIVE RULE](1350.alter-sensitive-rule-of-mysql-mode.md)