| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |
|machine-translation| en          |
|slug|drop-materialized-views-log-mysql-compatible-mode-in-sql|

# DROP MATERIALIZED VIEW LOG

## 説明

このステートメントは、マテリアライズドビューのログを削除するために使用されます。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>マテリアライズドビューのログを削除する際、ベーステーブルで実行中のトランザクション操作がある場合、そのトランザクションが終了するまで削除操作はブロックされます。</p>
</main>

## 権限要件

マテリアライズドビューのログを削除するには、`DROP TABLE` 権限が必要です。OceanBaseデータベースの権限の詳細については、[MySQLモードの権限分類](../../../../../600.manage/500.security-and-permissions/300.access-control/200.user-and-permission/200.permission-of-mysql-mode/100.permission-classification-of-mysql.md)を参照してください。

## 構文

```sql
DROP MATERIALIZED VIEW LOG ON [database.] table;
```

## パラメータ説明

| **パラメータ**  |       **説明**           |
|-----------|--------------------------|
| database.   | オプションです。マテリアライズドビューのログのベーステーブルが存在するデータベースを指定します。`database.` を省略した場合、デフォルトでベーステーブルは作成者自身のデータベースにあります。|
| table     | マテリアライズドビューのログに対応するベーステーブル名を指定します。|

## 例

テーブル `test_tbl1` のマテリアライズドビューのログを削除します。

```shell
obclient [test_db]> DROP MATERIALIZED VIEW LOG ON test_tbl1;
```

結果は次のとおりです：

```shell
Query OK, 0 rows affected
```

## 関連ドキュメント

[マテリアライズドビューのログ](../../../../300.database-object-management/100.manage-object-of-mysql-mode/600.manage-views-of-mysql-mode/200.manage-materialized-views-of-mysql-mode/200.materialized-views-log-of-mysql-mode.md)