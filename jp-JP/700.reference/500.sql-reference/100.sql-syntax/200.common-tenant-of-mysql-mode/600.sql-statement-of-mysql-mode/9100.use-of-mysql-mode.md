| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |
|machine-translation| en          |
|slug|use-mysql-mode|

# USE

## 説明

このステートメントは、使用するデータベースを切り替えるために使用します。

## 構文

```sql
USE database_name
```

## パラメータの説明

|      パラメータ       |    説明     |
|---------------|-----------|
| database_name | 使用するデータベース名。 |

## 例

現在のデータベースを切り替えます。

```sql
obclient> USE test
Database changed
```