| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |
|machine-translation|en,ja-marker|
|slug|schema-object-name-mysql-compatible-mode|

# スキーマオブジェクト名

スキーマオブジェクトには、データベース、テーブル、インデックス、列、エイリアス、ビュー、ストアドプロシージャ、パーティション、テーブルスペースなどが含まれ、これらは総称して識別子と呼ばれます。

## 引用識別子

識別子の引用文字はバッククォート（\`）であり、識別子には引用符を付けることも、付けないこともできます。識別子に特殊な文字や予約語が含まれている場合は、引用する際には必ず引用符を付ける必要があります。

```sql
obclient> SELECT * FROM `table` WHERE `table`.id > 10;
```

内部では、識別子はUnicode (UTF-8) に変換されて保存され、識別子には以下の文字を含めることができます：

* 引用符なしの識別子で許可される文字：

  * ASCII: \[0-9,a-z,A-Z$_\] (ASCII: \[0-基本的なラテン文字、数字0-9、ドル記号、アンダースコア])

  * 拡張文字：U+0080 .. U+FFFF

* 引用符付きの識別子で許可される文字には、完全なUnicode BMPが含まれますが、U+0000は除かれます：

  * ASCII: U+0001 .. U+007F

  * 拡張文字：U+0080 .. U+FFFF

* 引用符付きまたは引用符なしの識別子では、ASCII NUL (U+0000) および補完文字（U+10000以上）の使用は許可されていません。

* 識別子は数字で始めることができますが、引用されていない限り、数字のみで構成されることはできません。

* データベース、テーブル、列名はスペース文字で終了することはできません。

識別子を引用する場合、識別子の引用文字を識別子内に含めることができます。識別子内に含まれる文字が、識別子自体を引用するために使用される文字と同じ場合は、2回引用する必要があります。

次の例のように、`a"b`という名前のテーブルを作成し、その中に``c`d``という名前の列を含めます：

```sql
obclient> CREATE TABLE `a"b` (`c``d` INT);
Query OK, 0 rows affected
```

ステートメント内の他の部分でエイリアスを引用する場合、識別子を使用して引用する必要があります。そうでない場合、文字列リテラルと見なされます。

```sql
obclient> SELECT 1 AS `one`, 2 AS 'two';
+-----+-----+
| one | two |
+-----+-----+
|   1 |   2 |
+-----+-----+
1 row in set
```

## 指識子の限定子

オブジェクト名は限定されたものと限定されていない（省略された）ものがあります。限定された名前には少なくとも1つの限定子が含まれます。

以下の例のように、限定されていない名前 `t1` を使用してテーブルを作成します：

```sql
obclient> CREATE TABLE t1 (c INT);
Query OK, 0 rows affected
```

`t1` にはデータベースを指定するための限定子が含まれていないため、このステートメントはデフォルトのデータベースにテーブルを作成します。デフォルトのデータベースがない場合、エラーが発生します。

以下の例のように、限定された名前 `ob1.t1` を使用してテーブルを作成します：

```sql
obclient> CREATE TABLE ob1.t1 (c INT);
Query OK, 0 rows affected
```

`ob1.t1` にはデータベース限定子 `ob1` が含まれているため、このステートメントは `ob1` という名前のデータベースに `t1` を作成します。デフォルトのデータベースがない場合、限定子を指定する必要があります。デフォルトのデータベースが存在する場合は、限定子を使用して非デフォルトのデータベースを指定できます。

限定子には以下の特徴があります：

* 限定されていない名前は単一の識別子で構成されます。限定された名前は複数の識別子で構成されます。

* 識別子と限定子はピリオド (.) 文字で区切られます。

* 限定子は独立したマーカーであり、識別子と限定子の間にはスペースを入れることができます。たとえば、`tbl_name.col_name` と `tbl_name . col_name` は同等です。

* 識別子と限定子が同時に存在する場合、それぞれを個別に参照する必要があります。たとえば、```my-table`.`my-column``` は正しいですが、```my-table.my-column``` は誤りです。

* 限定された名前のピリオド (.) の後ろの予約語は識別子である必要があります。

* 構文 `.tbl_name` はデフォルトのデータベース内のテーブル `tbl_name` を表します。

オブジェクト名に許可される限定子はオブジェクトタイプによって異なり、ルールは以下のとおりです：

* データベース名は完全に限定されており、限定子は含まれません：

  ```sql
  CREATE DATABASE obdb1;
  ```

* テーブル、ビュー、またはストアドプログラムの名前には、データベース限定子を1つ指定できます。`CREATE` ステートメントにおける限定されていない名前と限定された名前の例は次のとおりです：

  ```sql
  CREATE TABLE mytable ...;
  CREATE VIEW myview ...;

  CREATE TABLE obdb.mytable ...;
  CREATE VIEW obdb.myview ...;
  ```

* 列名には複数の限定子を指定できます。以下の表を参照してください。

  |             列参照             |                      意味                      |
  |-----------------------------|----------------------------------------------|
  | `col_name`                  | ステートメントで使用される任意のテーブル内の `col_name` という名前を持つ列。               |
  | `tbl_name.col_name`         | デフォルトデータベースのテーブル `tbl_name` 内の列 `col_name`。           |
  | `db_name.tbl_name.col_name` | `db_name` データベースの `tbl_name` テーブルの列 `col_name`。 |

  同じステートメント内で異なるデータベースの同一テーブルから取得するには、テーブルを限定する必要があります。これらのテーブルの列を参照する場合、両方のテーブルに同時に存在する列名のみを限定する必要があります。
