| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |
|machine-translation| en          |
|slug|length-mysql-compatible-mode|

# LENGTH

## 宣言

```sql
LENGTH(str)
```

## 説明

`str` のバイト長を返します。

複数バイトの文字は、複数の文字としてカウントされます。例えば、5つの2バイト文字を入力した場合、`CHAR_LENGTH()` は `5` を返し、`LENGTH()` は `10` を返します。

## 例

```sql
obclient> SELECT LENGTH('中国'), LENGTH('hello');
+------------------+-----------------+
| LENGTH('中国') | LENGTH('hello') |
+------------------+-----------------+
|                6 |               5 |
+------------------+-----------------+
1 row in set
```