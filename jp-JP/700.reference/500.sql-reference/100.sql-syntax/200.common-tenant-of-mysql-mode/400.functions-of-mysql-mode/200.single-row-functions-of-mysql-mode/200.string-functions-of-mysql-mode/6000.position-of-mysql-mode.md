| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |
|machine-translation| en          |
|slug|position-mysql-compatible-mode|

# POSITION

## 宣言

```sql
POSITION(substr IN str)
```

## 説明

文字列 `str` 内でサブ文字列 `substr` が最初に現れる位置を返します。

## 例

```sql
obclient> SELECT POSITION('bar' IN 'foobarbar');
+--------------------------------+
| POSITION('bar' IN 'foobarbar') |
+--------------------------------+
|                              4 |
+--------------------------------+
1 row in set
```