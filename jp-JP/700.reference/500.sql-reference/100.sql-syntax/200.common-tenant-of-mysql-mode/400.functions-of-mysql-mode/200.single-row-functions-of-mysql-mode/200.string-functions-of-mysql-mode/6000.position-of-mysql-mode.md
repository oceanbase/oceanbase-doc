| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |
|machine-translation|en,ja-marker|
|slug|position-of-mysql-mode|

# POSITION

## 宣言

```sql
POSITION(substr IN str)
```

## 説明

文字列 `str` 内の部分文字列 `substr` の最初の出現位置を返します。

## 例

```sql
obclient> SELECT POSITION('bar' IN 'foobarbar');
+--------------------------------+
| POSITION('bar' IN 'foobarbar') |
+--------------------------------+
|                              4 |
+--------------------------------+
1 row in set
```