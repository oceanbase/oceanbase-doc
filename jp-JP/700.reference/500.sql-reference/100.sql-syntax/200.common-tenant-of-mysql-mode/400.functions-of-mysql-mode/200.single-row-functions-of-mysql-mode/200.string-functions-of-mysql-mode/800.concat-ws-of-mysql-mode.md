| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |
|machine-translation|en,ja-marker|
|slug|concat_ws-mysql-mode|

# CONCAT_WS

## 宣言

```sql
CONCAT_WS(separator, str1, .., strN)
```

## 説明

複数の文字列を1つの文字列に連結し、隣接する文字列間は `separator` で区切ります。パラメータに `NULL` が含まれる場合、`NULL` は無視されます。

この関数は空の文字列を無視しません。

## 例

```sql
obclient> SELECT
     CONCAT_WS('_', 'First', 'Second'),
     CONCAT_WS('_', 'First', NULL, 'Second')
     \G
*************************** 1. row ***************************
      CONCAT_WS('_', 'First', 'Second'): First_Second
CONCAT_WS('_', 'First', NULL, 'Second'): First_Second
1 row in set
```