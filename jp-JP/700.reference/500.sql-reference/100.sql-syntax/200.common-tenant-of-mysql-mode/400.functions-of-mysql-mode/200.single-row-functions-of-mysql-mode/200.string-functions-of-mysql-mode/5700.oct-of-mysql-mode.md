| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |
|machine-translation| en          |
|slug|oct-mysql-compatible-mode|

# OCT

## 宣言

```sql
OCT(N)
```

## 説明

`N` の8進数値を返します。この関数は `CONV(N,10,8)` と同じです。

`N` が `NULL` の場合、この関数は `NULL` を返します。

## 例

```sql
obclient> SELECT OCT(12);
+---------+
| OCT(12) |
+---------+
| 14      |
+---------+
1 row in set
```