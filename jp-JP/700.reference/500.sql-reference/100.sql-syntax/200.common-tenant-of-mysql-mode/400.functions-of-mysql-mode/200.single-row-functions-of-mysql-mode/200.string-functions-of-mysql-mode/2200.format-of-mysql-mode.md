| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |
|machine-translation| en          |
|slug|format-mysql-compatible-mode|

# FORMAT

## 宣言

```sql
FORMAT(X,D)
```

## 説明

数値 `X` を `#,###,###.##` 形式にフォーマットし、`D` 桁小数まで四捨五入して文字列として返します。

整数部が3桁を超える場合、カンマ（,）を千分位の区切り文字として使用します。

`D` が0の場合、結果には小数点または小数部がありません。

## 例

```sql
obclient> SELECT FORMAT(12332.123456, 4) FROM DUAL;
+-------------------------+
| format(12332.123456, 4) |
+-------------------------+
| 12,332.1235             |
+-------------------------+
1 row in set

obclient> SELECT FORMAT(12332.1, 4) FROM DUAL;
+--------------------+
| format(12332.1, 4) |
+--------------------+
| 12,332.1000        |
+--------------------+
1 row in set

obclient> SELECT FORMAT(12332.2, 0) FROM DUAL;
+--------------------+
| format(12332.2, 0) |
+--------------------+
| 12,332             |
+--------------------+
1 row in set
```