| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |
|machine-translation| en          |
|slug|mid-mysql-mode|

# MID

## 宣言

```sql
MID(str,pos,len)
```

## 説明

`str` のサブ文字列を返します。開始位置は `pos`、長さは `len` です。パラメータに `NULL` が含まれる場合、`NULL` を返します。

* `len` を指定しない場合、返されるサブ文字列は `pos` の位置から `str` の終端までの範囲となります。

* `pos` の値が負の場合、`str` の末尾から順に戻りながら開始位置を決定します。

* `len` が0以下、または `pos` で指定された開始位置が無効な場合、空文字列を返します。

この関数は `SUBSTRING(str,pos,len)` のエイリアスです。

## 例

```sql
obclient> SELECT MID('OceanBase',6);
+--------------------+
| MID('OceanBase',6) |
+--------------------+
| Base               |
+--------------------+
1 row in set
```