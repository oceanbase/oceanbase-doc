| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |
|machine-translation|en,ja-marker|
|slug|concat-mysql-compatible-mode|

# CONCAT

## 宣言

```sql
CONCAT(str1, str2, ...)
```

## 説明

複数の文字列を1つの文字列に連結します。すべての引数が非バイナリ文字列である場合、結果は非バイナリ文字列になります。引数にバイナリ文字列が含まれている場合、結果はバイナリ文字列になります。数値型の引数は、同等の非バイナリ文字列形式に変換されます。

引数に`NULL`が含まれる場合、`NULL`返します。

## 例

```sql
obclient> SELECT
     CONCAT('test','obclient', '1.0'),
     CONCAT('test','obclient', NULL)
     \G
*************************** 1. row ***************************
CONCAT('test','obclient', '1.0'): testobclient1.0
 CONCAT('test','obclient', NULL): NULL
1 row in set
```