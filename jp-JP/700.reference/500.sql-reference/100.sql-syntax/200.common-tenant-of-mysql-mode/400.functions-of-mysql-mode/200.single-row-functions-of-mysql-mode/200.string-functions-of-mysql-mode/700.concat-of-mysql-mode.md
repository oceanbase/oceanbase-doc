| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |
|machine-translation| en          |
|slug|concat-mysql-compatible-mode|

# CONCAT

## 宣言

```sql
CONCAT(str1, str2, ...)
```

## 説明

複数の文字列を1つの文字列に結合します。すべてのパラメータが非バイナリ文字列の場合、結果は非バイナリ文字列となります。パラメータにバイナリ文字列が含まれる場合、結果はバイナリ文字列となります。数値型のパラメータは、等価な非バイナリ文字列表現に変換されます。

パラメータに `NULL` が含まれる場合、`NULL` を返します。

## 例

```sql
obclient> SELECT
     CONCAT('test','obclient', '1.0'),
     CONCAT('test','obclient', NULL)
     \G
*************************** 1. row ***************************
CONCAT('test','obclient', '1.0'): testobclient1.0
 CONCAT('test','obclient', NULL): NULL
1 row in set
```