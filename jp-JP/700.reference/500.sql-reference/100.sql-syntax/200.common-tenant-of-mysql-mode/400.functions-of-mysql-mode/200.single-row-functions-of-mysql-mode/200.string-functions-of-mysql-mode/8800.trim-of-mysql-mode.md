| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |
|machine-translation|en,ja-marker|
|slug|trim-mysql-compatible-mode|

# TRIM

## 宣言

```sql
TRIM([[{BOTH | LEADING | TRAILING}] [remstr] FROM] str
```

## 説明

文字列のすべての接頭辞および/または接尾辞を削除します。デフォルトは`BOTH`です。パラメータに`NULL`が含まれる場合、`NULL`返します。

## 例

```sql
obclient> SELECT
     TRIM(' bar '),
     TRIM(LEADING 'x' FROM 'xxxbarxxx'),
     TRIM(BOTH 'x' FROM 'xxxbarxxx'),
     TRIM(TRAILING 'x' FROM 'xxxbarxxx')
     \G
*************************** 1. row ***************************
                      TRIM(' bar '): bar
 TRIM(LEADING 'x' FROM 'xxxbarxxx'): barxxx
    TRIM(BOTH 'x' FROM 'xxxbarxxx'): bar
TRIM(TRAILING 'x' FROM 'xxxbarxxx'): xxxbar
1 row in set
```