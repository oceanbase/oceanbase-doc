| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |
|machine-translation|en,ja-marker|
|slug|extract-of-mysql-mode|

# EXTRACT

## 宣言

```javascript
EXTRACT(unit FROM date)
```

## 説明

`date` の指定された部分値を整数型で返します。複数の部分を指定した場合、すべての値が順番に連結されます。

パラメータ `unit` の詳細については、[DATE_ADD](../100.date-and-time-functions-of-mysql-mode/900.date-add-of-mysql-mode.md)を参照してください。`unit` が `WEEK` の場合、日曜日を週の最初の日とします。

## 例

```javascript
obclient> SELECT EXTRACT(WEEK FROM '2013-01-01'),
     EXTRACT(WEEK FROM '2013-01-06'),
     EXTRACT(YEAR_MONTH FROM '2012-03-09'),
     EXTRACT(DAY FROM NOW())\G;
*************************** 1. row ***************************
      EXTRACT(WEEK FROM '2013-01-01'): 0
      EXTRACT(WEEK FROM '2013-01-06'): 1
EXTRACT(YEAR_MONTH FROM '2012-03-09'): 201203
              EXTRACT(DAY FROM NOW()): 16
1 row in set
```