| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |
|machine-translation| en          |
|slug|localtimestamp-mysql-mode|

# LOCALTIMESTAMP

## 宣言

```sql
LOCALTIMESTAMP([scale])
```

## 説明

現在の日付と時刻を返します。`scale` はマイクロ秒単位の精度を表し、有効な値は `0` から `6` の整数です。`LOCALTIMESTAMP` と `LOCALTIMESTAMP()` は `NOW()` の同義語です。

## 例

```sql
obclient> SELECT LOCALTIMESTAMP,LOCALTIMESTAMP(0),LOCALTIMESTAMP(6);
+---------------------+---------------------+----------------------------+
| LOCALTIMESTAMP      | LOCALTIMESTAMP(0)   | LOCALTIMESTAMP(6)          |
+---------------------+---------------------+----------------------------+
| 2021-08-25 17:17:08 | 2021-08-25 17:17:08 | 2021-08-25 17:17:08.952508 |
+---------------------+---------------------+----------------------------+
1 row in set
```