| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |
|machine-translation| en          |
|slug|datediff-mysql-compatible-mode|

# DATEDIFF

## 宣言

```javascript
DATEDIFF(date1, date2)
```

## 説明

`date1` と `date2` の日数の差を返します。

計算ではパラメータの日付部分のみが使用され、時刻部分は無視されます。

## 例

```javascript
obclient> SELECT DATEDIFF('2015-06-19','1994-12-17');
+-------------------------------------+
| DATEDIFF('2015-06-19','1994-12-17') |
+-------------------------------------+
|                                7489 |
+-------------------------------------+
1 row in set
```