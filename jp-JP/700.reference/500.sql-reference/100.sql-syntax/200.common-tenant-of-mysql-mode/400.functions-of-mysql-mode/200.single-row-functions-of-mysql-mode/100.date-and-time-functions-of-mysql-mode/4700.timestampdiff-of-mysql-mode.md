| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |
|machine-translation| en          |
|slug|timestampdiff-mysql-compatible-mode|

# TIMESTAMPDIFF

## 宣言

```javascript
TIMESTAMPDIFF(unit, date1, date2)
```

## 説明

`unit` を単位として、2つの日付時刻の間隔を返します。`unit` は **DATE_ADD** の独立した単位である場合にのみ指定できます。

## 例

```javascript
obclient> SELECT
     TIMESTAMPDIFF(SECOND, NOW(), '2011-01-01 11:11:11'),
     TIMESTAMPDIFF(DAY, '2011-01-01 11:11:11', NOW())
     \G
*************************** 1. row ***************************
TIMESTAMPDIFF(SECOND, NOW(), '2011-01-01 11:11:11'): -231677498
   TIMESTAMPDIFF(DAY, '2011-01-01 11:11:11', NOW()): 2681
1 row in set
```