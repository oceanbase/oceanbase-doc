| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |
|machine-translation| en          |
|slug|nullif-mysql-compatible-mode|

# NULLIF

## 宣言

```sql
NULLIF(expr1, expr2)
```

## 説明

`expr1` = `expr2` の条件が成り立つ場合、戻り値は `NULL` となります。条件が成り立たない場合、戻り値は `expr1` となります。これは `CASE WHEN`

*`expr1 = expr2 THEN NULL ELSE expr1 END` と同じです。パラメータが等しくない場合、両方の式で求められる値は `expr1` となります。*

## 例

```sql
obclient> SELECT NULLIF('ABC', 123), NULLIF('123',123), NULLIF(NULL, 'abc');
+--------------------+-------------------+---------------------+
| NULLIF('ABC', 123) | NULLIF('123',123) | NULLIF(NULL, 'abc') |
+--------------------+-------------------+---------------------+
| ABC                | NULL              | NULL                |
+--------------------+-------------------+---------------------+
1 row in set, 1 warning
```