| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |
|machine-translation| en          |
|slug|truncate-mysql-compatible-mode|

# TRUNCATE

## 宣言

```sql
TRUNCATE(X,D)
```

## 説明

`X` を小数点以下 `D` 桁に四捨五入して返します。`D` が `0` の場合は、`X` の整数部を返します。`D` が負の数の場合は、`X` の小数点より左側の `D` 桁を `0` にします。

## 例

```sql
obclient> SELECT TRUNCATE(3.27,1);
+------------------+
| TRUNCATE(3.27,1) |
+------------------+
|              3.2 |
+------------------+
1 row in set

obclient> SELECT TRUNCATE(3.27,0);
+------------------+
| TRUNCATE(3.27,0) |
+------------------+
|                3 |
+------------------+
1 row in set

obclient> SELECT TRUNCATE(3.27,-1);
+-------------------+
| TRUNCATE(3.27,-1) |
+-------------------+
|                 0 |
+-------------------+
1 row in set
```