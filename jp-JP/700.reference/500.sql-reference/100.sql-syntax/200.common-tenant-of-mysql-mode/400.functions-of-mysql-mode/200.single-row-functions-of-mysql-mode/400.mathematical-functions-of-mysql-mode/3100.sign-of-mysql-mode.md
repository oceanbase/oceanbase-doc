| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |
|machine-translation| en          |
|slug|sign-mysql-compatible-mode|

# SIGN

## 宣言

```sql
SIGN(X)
```

## 説明

`SIGN(X)` は、パラメータの符号を返します。この符号は、`X` の値が負、ゼロ、または正によって決まります。浮動小数点数と16進数をサポートしています。戻り値は次のとおりです：

* 負の値：`-1`

* ゼロの値：`0`

* 正の値：`1`

この関数は比較演算をサポートしており、結果は数値型に変換されて処理され、1 (`TRUE`) または 0 (`FALSE`) が生成されます。

入力が `NULL` の場合、戻り値は `NULL` です。

## 例

```sql
obclient> SELECT SIGN(-32), SIGN(0), SIGN(234);
+-----------+---------+-----------+
| SIGN(-32) | SIGN(0) | SIGN(234) |
+-----------+---------+-----------+
|        -1 |       0 |         1 |
+-----------+---------+-----------+
1 row in set

obclient> SELECT SIGN(NULL),SIGN(FALSE),SIGN(0X01);
+------------+-------------+------------+
| SIGN(NULL) | SIGN(FALSE) | SIGN(0X01) |
+------------+-------------+------------+
|       NULL |           0 |          1 |
+------------+-------------+------------+
1 row in set
```