| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |
|machine-translation|en,ja-marker|
|slug|wait-untle-sql-thread-after-gtid-mysql-mode|

# WAIT_UNTIL_SQL_THREAD_AFTER_GTIDS()

## 説明

この関数は、SQLスレッドが指定されたGTID集合内のすべてのトランザクションを実行するまで待機します。

## 構文

```sql
WAIT_UNTIL_SQL_THREAD_AFTER_GTIDS(gtid_set[, timeout][,channel])
```

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>このキーワード構文のみサポートされており、機能は有効ではありません。</p>
</main>