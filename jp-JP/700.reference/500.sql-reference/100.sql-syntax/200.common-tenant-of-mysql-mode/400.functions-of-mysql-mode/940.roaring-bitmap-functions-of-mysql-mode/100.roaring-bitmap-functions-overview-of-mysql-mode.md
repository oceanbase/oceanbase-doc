| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |
|machine-translation| en          |
|slug|roaring-bitmap-functions-overview-mysql-mode|

# ビットマップ関数の概要

ビットマップ関数は、OceanBaseデータベースシステムが提供する、効率的に圧縮されたビットマップデータを処理するための専用関数群です。これらの関数により、ユーザーはデータベースレベルで複雑なビットマップクエリや分析を実行できるため、ユーザー行動分析、ユーザープロファイリング、セグメンテーションなどのアプリケーションがビットマップデータを効率的に格納、クエリ、操作できます。

以下は、OceanBaseのビットマップ関数の種類とその用途の概要です：

## ビットマップ関数の種類

| ビットマップ関数タイプ                               | 説明                                                                |
|------------------------------------------|--------------------------------------------------------------------|
| [ビットマップコンストラクタ](200.roaring-bitmap-constructor-functions-of-mysql-mode.md) | 新しいビットマップデータ型を構築するための関数です。 |
| [ビットマップカードナリティ計算関数](300.roaring-bitmap-cardinality-calculation-functions-of-mysql-mode.md) | ビットマップデータのカードナリティを計算するための関数です。 |
| [ビットマップ演算関数](400.roaring-bitmap-operation-functions-of-mysql-mode.md) | 交差、和集合、排他的論理和、非など、ビットマップデータ間の基本的な演算を提供する関数です。 |
| [ビットマップ判定関数](500.roaring-bitmap-decision-functions-of-mysql-mode.md)  | 入力されたビットマップデータに対して判断を行い、ブール値を返す関数です。 |
| [ビットマップ出力関数](600.roaring-bitmap-output-functions-of-mysql-mode.md) | ビットマップデータの出力形式を選択するための関数です。例えば、文字列としてビットマップデータの各要素を順次出力し、カンマで区切る場合などが該当します。 |
| [ビットマップ集約関数](700.roaring-bitmap-aggregate-functions-of-mysql-mode.md)    | 数値型をビットマップ型、すなわちビットマップデータ間の集約演算に集約する関数を提供します。 |

## ビットマップ関数一覧

OceanBaseデータベースで現在サポートされているすべてのビットマップ関数は以下のとおりです：

| 関数タイプ | 関数名 | 説明 |
|---------|---------|------|
| ビットマップコンストラクタ | rb_build_empty | 空のビットマップデータを構築するために使用されます。 |
| ビットマップコンストラクタ | rb_build_empty | 空のビットマップデータを構築するために使用されます。 |
| ビットマップコンストラクタ | rb_build_varbinary | varbinaryを使用してビットマップデータを構築します。 |
| ビットマップコンストラクタ | rb_from_string | 特定の形式の文字列からビットマップデータを構築します。 |
| ビットマップコンストラクタ | rb_select | 指定された条件に基づいて、ビットマップデータの値から局所範囲を選択し、新しいビットマップデータとして保存してその値を返します。 |
| ビットマップカードナリティ関数 | rb_cardinality | ビットマップデータの基数を計算するために使用されます。 |
| ビットマップカードナリティ関数 | rb_and_cardinality および rb_and_null2empty_cardinality | 2つのビットマップデータをAND演算した後の新しいビットマップデータの基数を返します。 |
| ビットマップカードナリティ関数 | rb_or_cardinality および rb_or_null2empty_cardinality | 2つのビットマップデータをOR演算した後の新しいビットマップデータの基数を返します。 |
| ビットマップカードナリティ関数 | rb_xor_cardinality | 2つのビットマップデータをXOR演算した後の新しいビットマップデータの基数を返します。 |
| ビットマップカードナリティ関数 | rb_andnot_cardinality および rb_andnot_null2empty_cardinality | 2つのビットマップデータをNAND演算した後の新しいビットマップデータの基数を返します。 |
| ビットマップカードナリティ関数 | rb_or_cardinality_agg | ビットマップ列の和集合の基数を計算します。 |
| ビットマップカードナリティ関数 | rb_and_cardinality_agg | ビットマップ列の共通部分の基数を計算します。 |
| ビットマップ演算関数 | rb_and および rb_and_null2empty | 2つのビットマップデータの共通部分を計算します。 |
| ビットマップ演算関数 | rb_or および rb_or_null2empty | 2つのビットマップデータの和集合を計算します。 |
| ビットマップ演算関数 | rb_xor | 2つのビットマップデータのXOR演算を提供します。 |
| ビットマップ演算関数 | rb_andnot および rb_andnot_null2empty | 2つのビットマップデータのNAND演算を提供します。 |
| ビットマップ判断関数 | rb_is_empty | 入力されたビットマップデータが空かどうかを判断します。 |
| ビットマップ判断関数 | rb_contains | 2つの用法：<ul><li>入力された最初のビットマップデータが、入力された2番目のビットマップデータを完全に含むかどうかを判断します。</li><li>入力されたビットマップデータに特定のオフセットが含まれているかどうかを判断します。</li></ul> |
| ビットマップ出力関数 | rb_to_varbinary | ビットマップデータをvarbinary形式で出力するために使用されます。 |
| ビットマップ出力関数 | rb_to_string | 文字列形式でビットマップデータの各要素を順次出力し、カンマで区切ります。 |
| ビットマップ出力関数 | rb_to_array | 配列形式でビットマップデータを出力します。 |
| ビットマップ集約関数 | rb_build_agg | 数値列をビットマップデータに集約します。 |
| ビットマップ集約関数 | rb_or_agg | ビットマップ列の複数行データにOR演算を行い、ビットマップデータに集約します。 |
| ビットマップ集約関数 | rb_and_agg | ビットマップ列の複数行データにAND演算を行い、ビットマップデータに集約します。 |