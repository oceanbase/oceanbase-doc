|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type| MySQL |
|machine-translation| en |
|slug|ob_ivf_nprobes|

# ob_ivf_nprobes

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>この変数はV4.3.5 BP1バージョンから導入されました。</p>
</main>

## 機能の概要

`ob_ivf_nprobes`は、ベクトルインデックスクエリを実行する際に、最も近いクラスタ中心をいくつ検索するかを設定します。この変数はIVF系クエリのパフォーマンスと再現率に影響します。値が大きいほど再現率は高くなりますが、同時に検索時間も長くなります。

<main id="notice" type='explain'>
  <h4>適用対象</h4>
  <p>この変数はOceanBaseデータベースのMySQLモードでのみ適用されます。</p>
</main>

## 権限要件

* クエリ変数

  * `sys`テナントとすべてのユーザーテナントは`SHOW VARIABLES`ステートメント、または`information_schema.SESSION_VARIABLES`ビュー(MySQLモード)を使用して、セッションシステム変数の値を確認できます。

* 変数の変更

  * sysテナントとすべてのMySQLユーザーテナントは、自身のテナントのセッションシステム変数の値を直接変更できます。

## プロパティの説明

| **プロパティ**  | **説明**  |
|---------|---------|
| パラメータタイプ    | Int |
| デフォルト値     | 8    |
| 値の範囲    | [1,65536]    |
| 適用範囲    | セッション |
| 変更は可能か | はい。`SET`ステートメントを使用して変更できます。 |

## 設定例

このクエリで最も近いクラスタ中心を10個検索するように設定します。

```shell
obclient> SET ob_ivf_nprobes = 10;
```

## 関連ドキュメント

* [IVFシリーズインデックス](../../../../640.ob-vector-search/200.ob-vector-index/300.ob-dense-vector-index/300.ob-ivf-index.md)