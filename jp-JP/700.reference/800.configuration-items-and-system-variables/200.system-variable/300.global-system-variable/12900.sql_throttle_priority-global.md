|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||
|machine-translation| en |
|slug|sql_throttle_priority-global|

# sql_throttle_priority

<main id="notice" type='explain'>
<h4>説明</h4>
<p>この変数はV2.2.77バージョンから導入されました。</p>
</main>

## 機能の概要

`sql_throttle_priority`はスロットリングの優先順位を設定します。`sql_throttle_current_priority`が`sql_throttle_priority`より小さいセッション上のリクエストのみがスロットリングされます。

## 権限要件

* 変数の確認

  `sys`テナントとすべてのユーザーテナントは`SHOW VARIABLES`ステートメント、または`SYS.TENANT_VIRTUAL_GLOBAL_VARIABLE`ビュー（Oracleモード）および`information_schema.GLOBAL_VARIABLES`ビュー（MySQLモード）を使用してグローバルシステム変数の値を確認できます。

* 変数の変更

  * `sys`テナントはグローバルシステム変数の値を直接変更できます。

  * MySQLユーザーテナントは`SUPER`または`ALTER SYSTEM`権限を持っている必要があります。

  * Oracleユーザーテナントは`ALTER SYSTEM`権限を持っている必要があります。

## プロパティの説明

| **プロパティ** |                      **説明**                       |
|--------|---------------------------------------------------|
| パラメータタイプ   | Int                                               |
| デフォルト値    | -1                                                |
| 値の範囲   | 値の範囲なし。実際の使用時は`sql_throttle_current_priority`の値に依存します |
| 適用範囲   | Global                      |
| 変更は可能か  | はい。`SET`ステートメントを使用して変更できます。|

## 使用方法

クエリの優先順位がこの値より低い場合、クエリの実行が許可されない可能性があります。

## 設定例

レート制限の優先順位を1に設定します。

```shell
obclient> SET GLOBAL sql_throttle_priority = 1;
```