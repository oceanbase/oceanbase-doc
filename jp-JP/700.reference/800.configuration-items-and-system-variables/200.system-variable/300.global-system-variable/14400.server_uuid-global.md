|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||
|machine-translation|en,ja-marker,ja-autoPR|
|slug|server_uuid-global|

# server_uuid

<main id="notice" type='explain'>
<h4>説明</h4>
<p>この変数はV3.2.4バージョンから導入されました。</p>
</main>

## 機能の概要

`server_uuid`は、OBServerノードのUUIDを確認するために使用されます。

OBServerノードが起動すると、自動的にUUIDが作成されます。例：`182d0ebb-1209-11ed-b01e-a237b1d47168`。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>この変数はMySQLモードでのみサポートされています。</p>
</main>

## 権限要件

* 変数の確認

  `sys`テナントとすべてのユーザーテナントは`SHOW VARIABLES`ステートメント、または`information_schema.GLOBAL_VARIABLES`ビュー（MySQLモード）を使用してグローバルシステム変数の値を確認できます。

* 変数の変更

  この変数は変更できません。

## プロパティの説明

| **プロパティ** | **説明** |
|---------|------------------|
| パラメータタイプ | Varchar             |
| 適用範囲 | グローバル           |
| 変更は可能か | 不可。この変数は読み取り専用です。                 |