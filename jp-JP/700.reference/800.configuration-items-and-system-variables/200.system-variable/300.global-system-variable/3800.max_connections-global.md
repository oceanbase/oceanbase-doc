|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||
|machine-translation| en |
|slug|max_connections-global|

# max_connections

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>この変数はV2.2.77バージョンから導入されました。</p>
</main>

## 機能の概要

`max_connections`は、テナント全体の最大接続数を設定します。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>この変数はOceanBaseデータベースのMySQLモードでのみ適用されます。</p>
</main>

## 権限要件

* 変数の確認

  `sys`テナントとすべてのユーザーテナントは`SHOW VARIABLES`ステートメント、または`information_schema.GLOBAL_VARIABLES`ビュー(MySQLモード)を使用してグローバルシステム変数の値を確認できます。

* 変数の変更

  * `sys`テナントはグローバルシステム変数の値を直接変更できます。

  * MySQLユーザーテナントは、`SUPER`または`ALTER SYSTEM`権限を持っている必要があります。

## プロパティの説明

|  **プロパティ**  |      **説明**      |
|------------|-------------------|
| パラメータタイプ     | Uint <main id="notice" type='explain'><h4>説明</h4><p>この変数はV4.0.0バージョンから、パラメータタイプがIntからUintに調整されました。</p></main>            |
| デフォルト値       | 2147483647<main id="notice" type='explain'><h4>説明</h4><p>この変数はV4.0.0バージョンから、デフォルト値が151から2147483647に調整されました。</p></main>       |
| 値の範囲     | [1,2147483647]<main id="notice" type='explain'><h4>説明</h4><p>この変数はV4.0.0バージョンから、値の範囲が[1，100000]から[1，2147483647]に調整されました。</p></main> |
| 適用範囲     | Global <main id="notice" type='explain'><h4>説明</h4><p>この変数はV4.0.0バージョンから、適用範囲がGLOBALとSESSIONからGLOBALに調整されました。</p></main>          |
| 変更は可能か   | はい。`SET`ステートメントを使用して変更できます。|

## 設定例

テナント全体の最大接続数を2147483647に設定します。

```shell
obclient> SET GLOBAL max_connections=2147483647;
```