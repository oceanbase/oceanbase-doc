|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||
|machine-translation| en |
|slug|innodb_strict_mode-global|

# innodb_strict_mode

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>この変数はV4.0.0バージョンから導入されました。</p>
</main>

## 機能の概要

`innodb_strict_mode`は、特定の構文を持つSQLのチェックモードを設定します。

## 権限要件

* クエリ変数

  * グローバルレベル

    `sys`テナントとすべてのユーザーテナントは`SHOW VARIABLES`ステートメント、または`SYS.TENANT_VIRTUAL_GLOBAL_VARIABLE`ビュー（Oracleモード）および`information_schema.GLOBAL_VARIABLES`ビュー（MySQLモード）を使用してグローバルシステム変数の値を確認できます。

  * セッションレベル

    `sys`テナントとすべてのユーザーテナントは`SHOW VARIABLES`ステートメント、または`SYS.TENANT_VIRTUAL_SESSION_VARIABLE`ビュー（Oracleモード）および`information_schema.SESSION_VARIABLES`ビュー（MySQLモード）を使用してセッションシステム変数の値を確認できます。

* 変数の変更

  * グローバル有効

    * `sys`テナントはグローバルシステム変数の値を直接変更できます。

    * MySQLユーザーテナントは`SUPER`または`ALTER SYSTEM`権限が必要です。

    * Oracleユーザーテナントは`ALTER SYSTEM`権限が必要です。

  * セッション有効

    `sys`テナントとすべてのユーザーテナントは、自身のテナントに属するセッションシステム変数の値を直接変更できます。

## プロパティの説明

| **プロパティ**  |    **説明**  |
|-----------|---------------|
| パラメータタイプ     | Bool        |
| デフォルト値       | 1         |
| 値の範囲     | <ul><li>1：厳密モードを使用します。`CREATE TABLE`、`ALTER TABLE`、および `CREATE INDEX` ステートメントを使用する際、特定のシナリオで発生するエラー（例：`ROW_FORMAT` がサポートされない場合）は警告情報を表示せず、直接エラーとして返されます。</li><li>0：非厳密モードを使用します。緩やかな構文チェックを実行し、誤った構文に対してデフォルトの構文を適用します。</li></ul>|
| 適用範囲     | <ul><li>Global</li><li>Session</li></ul>   |
| 変更は可能か   | はい。`SET` ステートメントを使用して変更できます。|

## 設定例

特定の構文のSQLチェックモードを1に設定します。

* Global適用

  ```shell
  obclient> SET GLOBAL innodb_strict_mode =1;
  ```

* Session適用

  ```shell
  obclient> SET innodb_strict_mode =1;
  ```