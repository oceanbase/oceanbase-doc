|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||
|machine-translation| en |
|slug|sql_mode-global|

# sql_mode

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>この変数はV1.4バージョンから導入されました。</p>
</main>

## 機能の概要

`sql_mode`はSQLモードを設定するために使用され、異なるSQLモードはデータベースの動作に大きな影響を与えます。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>この変数はOceanBaseデータベースのMySQLモードでのみ適用されます。</p>
</main>

## 権限要件

* 変数の確認

  * グローバルレベル

    `sys`テナントとすべてのユーザーテナントは`SHOW VARIABLES`ステートメント、または`information_schema.GLOBAL_VARIABLES`ビュー（MySQLモード）を使用してグローバルシステム変数の値を確認できます。

  * セッションレベル

    `sys`テナントとすべてのユーザーテナントは`SHOW VARIABLES`ステートメント、または`information_schema.SESSION_VARIABLES`ビュー（MySQLモード）を使用してセッションシステム変数の値を確認できます。

* 変数の変更

  * グローバル変更

    * `sys`テナントはグローバルシステム変数の値を直接変更できます。

    * MySQLユーザーテナントは`SUPER`または`ALTER SYSTEM`権限を持っている必要があります。

  * セッション変更

    `sys`テナントとすべてのユーザーテナントは、自身のテナント内のセッションシステム変数の値を直接変更できます。

## プロパティの説明

| **プロパティ** |  **説明**  |
|--------|----------|
| パラメータタイプ   | Uint   |
| デフォルト値     | STRICT_ALL_TABLES,NO_ZERO_IN_DATE,NO_AUTO_CREATE_USER  <main id="notice" type='explain'><h4>説明</h4><ul><li>V4.3.x系では、V4.3.0バージョン以降、デフォルト値が<code>STRICT_ALL_TABLES,NO_ZERO_IN_DATE</code>から<code>STRICT_ALL_TABLES,NO_ZERO_IN_DATE,NO_AUTO_CREATE_USER</code>に調整されました。</li><li>V4.2.x系では、V4.2.1 BP1バージョン以降、デフォルト値が<code>STRICT_ALL_TABLES,NO_ZERO_IN_DATE</code>から<code>STRICT_ALL_TABLES,NO_ZERO_IN_DATE,NO_AUTO_CREATE_USER</code>に調整されました。</li></ul></main>|
| 値の範囲   | 次の値の任意の組み合わせを指定できます。<ul><li>ANSI_QUOTES：文字列のクォート文字（二重引用符 "）を識別子のクォート文字（バッククォート `）として解釈するため、二重引用符で文字列を引用することはできません。</li><li>ERROR_FOR_DIVISION_BY_ZERO：INSERTまたはUPDATEステートメントの実行時に、データが0で割算された場合にエラーを発生させます。</li><ul><li>ERROR_FOR_DIVISION_BY_ZEROモードが無効な場合、データが0で割算されるとNULLを返します。</li><li>ERROR_FOR_DIVISION_BY_ZEROモードは有効ですが、厳密SQLモード（STRICT_ALL_TABLESまたはSTRICT_TRANS_TABLES）が無効な場合、データが0で割算されるとNULLを返します。</li><li>厳密SQLモード（STRICT_ALL_TABLESまたはSTRICT_TRANS_TABLES）とERROR_FOR_DIVISION_BY_ZEROモードの両方が有効な場合、データの0での割算は許可されず、エラーが発生します。</li><li>厳密SQLモード（STRICT_ALL_TABLESまたはSTRICT_TRANS_TABLES）とERROR_FOR_DIVISION_BY_ZEROモードの両方が有効で、ignoreが指定されている場合、データが0で割算されるとNULLを返します。</li> </ul><li>HIGH_NOT_PRECEDENCE：NOT演算子の優先順位により、<code>NOT a BETWEEN b AND c </code>などの式がNOT(a BETWEEN b AND c)として解析されます。</li> <li>IGNORE_SPACE：関数名と文字の間にスペースを含めることができます。このフィールドは現在互換性のためにのみ使用され、設定は可能ですが無効です。</li> <li>NO_AUTO_CREATE_USER：GRANTステートメントでパスワードが空のユーザーを作成することを禁止します。</li><li>NO_DIR_IN_CREATE：テーブル作成時にすべてのINDEX DIRECTORYおよびDATA DIRECTORY指示を無視します。このフィールドは現在互換性のためにのみ使用され、設定は可能ですが無効です。また、OceanBaseデータベースにおける動作はMySQLと一致しません。</li> <li>NO_ENGINE_SUBSTITUTION：使用するストレージエンジンが無効またはコンパイルされていない場合、エラーが発生します。この値が設定されていない場合、デフォルトのストレージエンジンが代用され、例外が表示されます。このフィールドは現在互換性のためにのみ使用され、設定は可能ですが無効です。また、OceanBaseデータベースにおける動作はMySQLと一致しません。</li><li>NO_FIELD_OPTIONS：<code>SHOW CREATE TABLE</code>の出力にMySQLモード固有の列オプションを出力することはできません。</li><li>NO_KEY_OPTIONS：<code>SHOW CREATE TABLE</code>の出力にMySQLモード固有のインデックスオプションを出力することはできません。</li><li>NO_TABLE_OPTIONS：<code>SHOW CREATE TABLE</code>の出力にMySQLモード固有のテーブルオプション（例：ENGINE）を出力することはできません。</li><li>NOUnsigned_SUBTRACTION：デフォルトでは、整数値同士の減算（そのうち一方がUNSIGNED型の場合）は符号なし結果を生成します。</li> <li>NO_ZERO_DATE：'0000-00-00'形式の日付を許可するかどうか。</li><ul><li>NO_ZERO_DATEモードが無効な場合、'0000-00-00'形式の日付を許可します。</li><li>NO_ZERO_DATEモードは有効ですが、厳密SQLモード（STRICT_ALL_TABLESまたはSTRICT_TRANS_TABLES）が無効な場合、'0000-00-00'形式の日付を許可します。</li><li>厳密SQLモード（STRICT_ALL_TABLESまたはSTRICT_TRANS_TABLES）とNO_ZERO_DATEモードの両方が有効な場合、'0000-00-00'形式の日付は許可されず、エラーが発生します。</li><li>厳密SQLモード（STRICT_ALL_TABLESまたはSTRICT_TRANS_TABLES）とNO_ZERO_DATEモードの両方が有効で、ignoreが指定されている場合、'0000-00-00'形式の日付を許可します。</li></ul><li>NO_ZERO_IN_DATE：年が0以外、月または日が0の形式のログ（例：'2010-00-01'または'2010-01-00'）を許可します。<ul><li>NO_ZERO_IN_DATEモードが無効な場合、年が0以外、月または日が0の形式の日付を許可します。</li><li>NO_ZERO_IN_DATEモードは有効ですが、厳密SQLモード（STRICT_ALL_TABLESまたはSTRICT_TRANS_TABLES）が無効な場合、年が0以外、月または日が0の形式の日付は'0000-00-00'形式の日付に変換されます。</li><li>厳密SQLモード（STRICT_ALL_TABLESまたはSTRICT_TRANS_TABLES）とNO_ZERO_IN_DATEモードの両方が有効な場合、年が0以外、月または日が0の形式の日付は許可されず、エラーが発生します。</li><li>厳密SQLモード（STRICT_ALL_TABLESまたはSTRICT_TRANS_TABLES）とNO_ZERO_IN_DATEモードの両方が有効で、ignoreが指定されている場合、年が0以外、月または日が0の形式の日付は'0000-00-00'形式の日付に変換されます。</li></ul><li>ONLY_FULL_GROUP_BY：SELECTリスト、HAVING条件、ORDER BY列には、GROUP BY列によって一意に決定される列のみを含めることができます。</li><li>PIPES_AS_CONCAT：(\|\|)を文字列連結演算子（CONCAT()と同じ）と見なし、ORの同義語とは見なしません。</li> <li>PAD_CHAR_TO_FULL_LENGTH：CHAR検索時に列の末尾のスペースを保持します。また、検索されたCHAR値は全長に合わせて埋めます。</li><li>REAL_AS_FLOAT：デフォルトではREALをDOUBLEの同義語と見なします。設定後はREALをFLOATの同義語と見なします。</li> <li>STRICT_ALL_TABLES：すべてのストレージエンジンで厳密SQLモードを有効にし、無効なデータ値を拒否します。</li><li>STRICT_TRANS_TABLES：トランザクションストレージエンジンで厳密SQLモードを有効にし、可能であれば非トランザクションストレージエンジンでも厳密SQLモードを有効にします。</li><li>TIME_TRUNCATE_FRACTIONAL：小数秒部分を持つTIME、DATE、TIMESTAMP値を、同じ型ですが小数点以下の桁数が少ない列に挿入する際に四捨五入を行うかどうかを制御します。デフォルトは四捨五入ですが、このモードを有効にすると切り捨てられます。</li> <li>ANSI：REAL_AS_FLOAT、PIPES_AS_CONCAT、ANSI_QUOTES、IGNORE_SPACE、ONLY_FULL_GROUP_BYの組み合わせに相当します。</li> <li>DB2：PIPES_AS_CONCAT、ANSI_QUOTES、IGNORE_SPACE、NO_KEY_OPTIONS、NO_TABLE_OPTIONS、NO_FIELD_OPTIONSの組み合わせに相当します。</li><li>MAXDB：PIPES_AS_CONCAT、ANSI_QUOTES、IGNORE_SPACE、NO_KEY_OPTIONS、NO_TABLE_OPTIONS、NO_FIELD_OPTIONS、NO_AUTO_CREATE_USERの組み合わせに相当します。</li> <li>MSSQL：PIPES_AS_CONCAT、ANSI_QUOTES、IGNORE_SPACE、NO_KEY_OPTIONS、NO_TABLE_OPTIONS、NO_FIELD_OPTIONSの組み合わせに相当します。</li><li>MYSQL323：HIGH_NOT_PRECEDENCEにMYSQL323の一部の機能を追加したものに相当します。</li><li>MYSQL40：HIGH_NOT_PRECEDENCEにMYSQL40の一部の機能を追加したものに相当します。</li><li>ORACLE：PIPES_AS_CONCAT、ANSI_QUOTES、IGNORE_SPACE、NO_KEY_OPTIONS、NO_TABLE_OPTIONS、NO_FIELD_OPTIONS、NO_AUTO_CREATE_USERの組み合わせに相当します。</li><li>POSTGRESQL：PIPES_AS_CONCAT、ANSI_QUOTES、IGNORE_SPACE、NO_KEY_OPTIONS、NO_TABLE_OPTIONS、NO_FIELD_OPTIONSの組み合わせに相当します。</li></ul>       |
| 適用範囲   | <ul><li> グローバル</li><li> セッション</li></ul>   |
| 変更は可能か | はい。`SET`ステートメントを使用して変更できます。|

## 設定例

SQLモードを `STRICT_ALL_TABLES`、`NO_ZERO_IN_DATE`、`NO_AUTO_CREATE_USER` に設定します。

* セッション単位で有効

  ```shell
  obclient> SET sql_mode = 'STRICT_ALL_TABLES,NO_ZERO_IN_DATE,NO_AUTO_CREATE_USER';
  ```

* グローバル単位で有効

  ```shell
  obclient> SET GLOBAL sql_mode = 'STRICT_ALL_TABLES,NO_ZERO_IN_DATE,NO_AUTO_CREATE_USER';
  ```

## 関連ドキュメント

[INFORMATION_SCHEMA TRIGGERS](../../../600.pl-reference/200.pl-mysql/800.information-schema-dictionary-view-mysql/300.information-schema-triggers-mysql.md)