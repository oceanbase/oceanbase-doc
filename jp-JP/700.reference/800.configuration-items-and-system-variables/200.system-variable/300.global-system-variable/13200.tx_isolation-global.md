|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||
|machine-translation|en,ja-marker,ja-autoPR|
|slug|tx_isolation-global|

# tx_isolation

<main id="notice" type='explain'>
<h4>説明</h4>
<p>この変数はV1.4バージョンから導入されました。</p>
</main>

## 機能の概要

`tx_isolation`は、トランザクション分離レベルを設定します。システム変数`transaction_isolation`と機能が同じであり、どちらか一方を設定するだけでよいです。

## 権限要件

* 変数の確認

  * グローバルレベル

    `sys`テナントとすべてのユーザーテナントは`SHOW VARIABLES`ステートメント、または`SYS.TENANT_VIRTUAL_GLOBAL_VARIABLE`ビュー(Oracleモード)および`information_schema.GLOBAL_VARIABLES`ビュー(MySQLモード)を使用してグローバルシステム変数の値を確認できます。

  * セッションレベル

    `sys`テナントとすべてのユーザーテナントは`SHOW VARIABLES`ステートメント、または`SYS.TENANT_VIRTUAL_SESSION_VARIABLE`ビュー(Oracleモード)および`information_schema.SESSION_VARIABLES`ビュー(MySQLモード)を使用してセッションシステム変数の値を確認できます。

* 変数の変更

  * グローバル有効

    * `sys`テナントはグローバルシステム変数の値を直接変更できます。

    * MySQLユーザーテナントは`SUPER`または`ALTER SYSTEM`権限を持っている必要があります。

    * Oracleユーザーテナントは`ALTER SYSTEM`権限を持っている必要があります。

  * セッション有効

    `sys`テナントとすべてのユーザーテナントは、自身のテナントのセッションシステム変数の値を直接変更できます。

## プロパティの説明

| **プロパティ** |                            **説明**                            |
|---------|------------------------------------------------------------------|
| パラメータタイプ    | Varchar                                                      |
| デフォルト値      | READ-COMMITTED                                               |
| 値の範囲    | <ul><li>`READ-COMMITTED`：読み取りがコミットされた </li> <li> `REPEATABLE-READ`：リピータブルリード </li> <li>`SERIALIZABLE`：シリアライズ可能</li></ul>    |
| 適用範囲    | <ul><li>Global</li> <li>Session </li></ul>      |
| 変更は可能か  | はい。`SET` ステートメントを使用して変更できます。|

## 設定例

トランザクションの分離レベルを`READ-COMMITTED`に設定します。

* セッション有効

  ```shell
  obclient> SET tx_isolation = 'READ-COMMITTED';
  ```

* グローバル有効

  ```shell
  obclient> SET GLOBAL tx_isolation = 'READ-COMMITTED';
  ```

## 関連ドキュメント

トランザクション分離レベルの詳細については、[トランザクション分離レベルの概要](../../../100.oceanbase-database-concepts/800.transaction-management/200.transaction-concurrency-and-consistency/400.transaction-isolation-level/100.transaction-isolation-level-overview.md)を参照してください。