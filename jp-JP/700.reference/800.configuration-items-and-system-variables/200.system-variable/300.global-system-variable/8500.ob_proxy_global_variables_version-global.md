|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||
|machine-translation| en |
|slug|ob_proxy_global_variables_version-global|

# ob_proxy_global_variables_version

<main id="notice" type='explain'>
<h4>説明</h4>
<p>この変数はV2.2.77バージョンから導入されました。</p>
</main>

## 機能の概要

`ob_proxy_global_variables_version`は、obproxyとOBServerノード間の内部通信で使用されます。OBServerノードがobproxyに返す必要があるグローバル変数の最新バージョン情報を設定するために使用されます。

## プロパティの説明

| **プロパティ** |                                                 **説明**                                                  |
|--------|---------------------------------------------------------------------------------------------------------|
| パラメータタイプ   | Int                  |
| デフォルト値    | 0                    |
| 値の範囲   | \[0, INT64_MAX)      |
| 適用範囲   | セッション              |
| 変更は可能か  | 不可。この変数はテナント作成時にのみ指定可能で、テナント作成後はSQLステートメントで変更できません。 |

## 使用方法

ユーザーは`SHOW VARIABLES`ステートメント、または`SYS.TENANT_VIRTUAL_SESSION_VARIABLE`ビュー（Oracleモード）および`information_schema.SESSION_VARIABLES`ビュー（MySQLモード）を使用してこの変数の値を照会することはできません。ユーザーは`SYS.DBA_OB_SYS_VARIABLES`ビュー（Oracleモード）または`oceanbase.DBA_OB_SYS_VARIABLES`ビュー（MySQLモード）をクエリすることで、この変数の値を確認できます。

また、ユーザーは`SELECT`ステートメントを使用して、現在のグローバル変数の最新バージョン番号を取得することもできます：

```shell
obclient> SELECT @@ob_proxy_global_variables_version;
```

実行結果は次のとおりです：

```shell
+-------------------------------------+
| @@ob_proxy_global_variables_version |
+-------------------------------------+
|                                   0 |
+-------------------------------------+
1 row in set
```