|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type|Oracle Mode|
|machine-translation|en,ja-marker,ja-autoPR|
|slug|nls_date_format-global|

# nls_date_format

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>この変数はV2.2.30バージョンから導入されました。</p>
</main>

## 機能の概要

`nls_date_format`は、date型をstrに変換する際のフォーマットと、strを暗黙的にdateに変換する際のフォーマットを制御します。

<main id="notice">
  <h4>適用対象</h4>
  <p>この変数はOceanBaseデータベースのOracleモードにのみ適用されます。</p>
</main>

## 権限要件

* 変数の確認

  * グローバルレベル

    `sys`テナントとすべてのユーザーテナントは`SHOW VARIABLES`ステートメント、または`SYS.TENANT_VIRTUAL_GLOBAL_VARIABLE`ビュー（Oracleモード）を使用してグローバルシステム変数の値を確認できます。

  * セッションレベル

    `sys`テナントとすべてのユーザーテナントは`SHOW VARIABLES`ステートメント、または`SYS.TENANT_VIRTUAL_SESSION_VARIABLE`ビュー（Oracleモード）を使用してセッションシステム変数の値を確認できます。

* 変数の変更

  * グローバル有効

    * `sys`テナントはグローバルシステム変数の値を直接変更できます。

    * Oracleユーザーテナントは`ALTER SYSTEM`権限を持っている必要があります。

  * セッション有効

    `sys`テナントとすべてのユーザーテナントは、自身のテナントのセッションシステム変数の値を直接変更できます。

## プロパティの説明

| **プロパティ**  | **説明**  |
|---------|---------|
| パラメータタイプ    | Varchar |
| デフォルト値      | DD-MON-RR |
| 適用範囲    | <ul><li>Global</li><li>Session</li></ul> |
| 変更は可能か  | はい。`SET` ステートメントを使用して変更できます。|

## 使用方法

`start_date`のデータ型はDATEであり、システム変数`NLS_DATE_FORMAT`で指定されたフォーマットモデルを使用して`start_date`の値をテキストに変換し、その結果をSQLテキストに渡します。日付時刻フォーマットモデルは、ダブルクォーテーションマークで囲まれたテキストで簡単に構成できます。

## 設定例

date型をstrに変換するフォーマットを`DD-MON-RR`に設定します。

* セッション有効

  ```shell
  obclient> SET nls_date_format='DD-MON-RR';
  ```

* グローバル有効

  ```shell
  obclient> SET GLOBAL nls_date_format='DD-MON-RR';
  ```

## 関連ドキュメント

* [日付時刻式](../../../../700.reference/500.sql-reference/100.sql-syntax/300.common-tenant-of-oracle-mode/600.expression-of-oracle-mode/700.date-and-time-expressions-of-oracle-mode.md)
* [データ変換の注意点](../../../../700.reference/500.sql-reference/100.sql-syntax/300.common-tenant-of-oracle-mode/300.basic-elements-of-oracle-mode/200.data-type-comparison-rules-of-oracle-mode/700.security-considerations-for-data-conversion-of-oracle-mode.md)