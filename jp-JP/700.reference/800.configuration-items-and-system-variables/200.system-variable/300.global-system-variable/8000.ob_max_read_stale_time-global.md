|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||
|machine-translation| en |
|slug|ob_max_read_stale_time-global|

# ob_max_read_stale_time

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>この変数はV4.1.0バージョンから導入されました。  </p>
</main>

## 機能の概要

`ob_max_read_stale_time`は、弱い読み取りクエリの最大遅延しきい値（μs）を設定します。

<main id="notice">
  <h4>適用対象</h4>
  <p>この変数はOceanBaseデータベースのMySQLモードでのみ適用されます。</p>
</main>

## 権限要件

* 変数の確認

  * グローバルレベル

    `sys`テナントとすべてのユーザーテナントは`SHOW VARIABLES`ステートメント、または`information_schema.GLOBAL_VARIABLES`ビュー（MySQLモード）を使用してグローバルシステム変数の値を確認できます。

  * セッションレベル

    `sys`テナントとすべてのユーザーテナントは`SHOW VARIABLES`ステートメント、または`information_schema.SESSION_VARIABLES`ビュー（MySQLモード）を使用してセッションシステム変数の値を確認できます。

* 変数の変更

  * グローバル有効

    * `sys`テナントはグローバルシステム変数の値を直接変更できます。

    * MySQLユーザーテナントは`SUPER`または`ALTER SYSTEM`権限を持っている必要があります。

  * セッション有効

    `sys`テナントとすべてのユーザーテナントは、自身のテナントにおけるセッションシステム変数の値を直接変更できます。

## プロパティの説明

|**プロパティ**| **説明**|
|--------|---------|
|パラメータタイプ     | Int  |
|デフォルト値       | -1、無効を表します。<main id="notice" type='explain'><h4>説明</h4><p>V4.1.0 BP2バージョンからデフォルト値が5000000から-1に調整されました。 </p></main> |
|値の範囲     | [-1, 9223372036854775807] us <main id="notice" type='explain'><h4>説明</h4><p>V4.1.0 BP2バージョンから値の範囲が[0, 9223372036854775807] usから[-1, 9223372036854775807] usに調整されました。 </p></main>    |
|適用範囲     |<ul><li>Global</li><li>Session</li></ul>|
| 変更は可能か  | はい。`SET`ステートメントを使用して変更できます。|

## 使用方法

セッションレベルでは弱い読み取りの遅延しきい値を制御します。現在のセッションの弱い読み取りリクエストでは、読み取るデータは必ず設定された遅延しきい値以内にあります。また、異なるセッションに対して異なる遅延しきい値を設定できます。

## 設定例

弱い読み取りクエリの最大遅延しきい値を5000000usに設定します。

* セッションに有効

  ```shell
  obclient> SET ob_max_read_stale_time =5000000;
  ```

* グローバルに有効

  ```shell
  obclient> SET GLOBAL ob_max_read_stale_time =5000000;
  ```