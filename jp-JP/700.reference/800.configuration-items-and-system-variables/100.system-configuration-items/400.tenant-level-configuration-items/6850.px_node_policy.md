|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||
|machine-translation| en |
|slug|px_node_policy|

# px_node_policy

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>この構成パラメータは、V4.3.5 BP1バージョンから導入されました。</p>
</main>

## 機能の概要

`px_node_policy`は、PX計算ノードのリソースを独立してスケーリングできるようにするために使用され、計算ノードの候補プールを表します。

## 権限要件

* パラメータの確認

  `sys`テナントとすべてのユーザーテナントは`SHOW PARAMETERS`ステートメント、または`GV$OB_PARAMETERS`ビューを使用してこのパラメータを確認できます。

* パラメータの変更

  `sys`テナントとユーザーテナントはこの構成パラメータを変更できます。

## プロパティの説明

|プロパティ|説明|
|-----|----|
|パラメータタイプ              |String|
|デフォルト値                |DATA|
|値の範囲              |<ul><li>`DATA`：PX計算ノードは、現在のSQLが関連するすべてのデータノードを選択できます。</li> <li>`ZONE`：PX計算ノードは、現在のSQLが関連するゾーン内で、そのテナントに属するすべてのノードを選択できます。</li> <li>`CLUSTER`：PX計算ノードは、クラスタ内で現在のテナントが関連するすべてのノードを選択できます。</li></ul> |
| 変更は可能か           | はい。`ALTER SYSTEM SET`ステートメントを使用して変更できます。|
|OBServerノードの再起動が必要か|不要です。設定は即時に反映されます。|

## 設定例

`sys`テナントは、`mysql`テナントのPX計算ノードが現在のSQLが関連するすべてのデータノードを選択できるように指定します。

```shell
obclient> ALTER SYSTEM SET px_node_policy = "DATA" tenant = 'mysql';
```