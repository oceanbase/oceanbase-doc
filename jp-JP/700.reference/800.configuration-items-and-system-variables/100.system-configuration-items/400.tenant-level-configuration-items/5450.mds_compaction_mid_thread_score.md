|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||
|machine-translation| en |
|slug|mds_compaction_mid_thread_score|

# mds_compaction_mid_thread_score

<main id="notice" type='explain'>
<h4>説明</h4>
<p>この構成パラメータは、V4.4.1バージョンから導入されました。</p>
</main>

## 機能の概要

`mds_compaction_mid_thread_score`は、中優先順位MDS CompactionのDAG（Direct Acyclic Graph）スレッド数を設定します。MDS Compactionの中優先順位キューは、MDS Minor Compactionで使用されます。

## 権限要件

* パラメータの確認

  `sys`テナントとすべてのユーザーテナントは`SHOW PARAMETERS`ステートメント、または`GV$OB_PARAMETERS`ビューを使用してこのパラメータを確認できます。

* パラメータの変更

  `sys`テナントとユーザーテナントはこの構成パラメータを変更できます。

## プロパティの説明

|      **プロパティ**      |    **説明**    |
|------------------|--------------|
| パラメータタイプ             | Int      |
| デフォルト値               | 0、デフォルト値2を使用します        |
| 値の範囲             | [0,100] |
| 変更は可能か           | はい。`ALTER SYSTEM SET`ステートメントを使用して変更できます。|
| OBServerノードの再起動が必要か | 不要です。設定は即時に反映されます。     |

## 設定例

* ユーザーテナントが自身のテナント内の中優先順位MDS CompactionのDAGスレッド数を2に設定します。

  ```shell
  obclient> ALTER SYSTEM SET mds_compaction_mid_thread_score = 2;
  ```

* システムテナント(`sys`テナント)がテナント`mysql`内の中優先順位MDS CompactionのDAGスレッド数を2に設定します。

  ```shell
  obclient> ALTER SYSTEM SET mds_compaction_mid_thread_score = 2 TENANT = 'mysql';
  ```

## 関連ドキュメント

* [mds_compaction_high_thread_score](5440.mds_compaction_high_thread_score.md)
* [mds_minor_compact_trigger](5460.mds_minor_compact_trigger.md)
* データCompactionに関するパラメータは以下のとおりです：

  * [compaction_high_thread_score](1000.compaction_high_thread_score.md)
  * [compaction_mid_thread_score](1200.compaction_mid_thread_score.md)
  * [compaction_low_thread_score](1100.compaction_low_thread_score.md)