|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||
|machine-translation|en,ja-marker,ja-autoPR|
|slug|ha_low_thread_score|

# ha_low_thread_score

<main id="notice" type='explain'>
<h4>説明</h4>
<p>この構成パラメータは、V4.0.0バージョンから導入されました。</p>
</main>

## 機能の概要

`ha_low_thread_score`は、高可用性で低優先順位のスレッドに対する現在のワーカースレッド数を設定します。

## 権限要件

* パラメータの確認

  `sys`テナントとすべてのユーザーテナントは`SHOW PARAMETERS`ステートメント、または`GV$OB_PARAMETERS`ビューを使用してこのパラメータを確認できます。

* パラメータの変更

  `sys`テナントとユーザーテナントはこの構成パラメータを変更できます。

## プロパティの説明

| **プロパティ** | **説明** |
| --- | --- |
| パラメータタイプ    | Int |
| デフォルト値      | 0、デフォルトのワーカースレッド数は2を表します |
| 値の範囲    | [0, 100] |
| 変更は可能か  | はい。`ALTER SYSTEM SET` ステートメントを使用して変更できます。|
| OBServerノードの再起動が必要か | 不要です。設定は即時に反映されます。 |

## 使用方法

V4.x系では、移行レプリケーションやバックアップ復旧など、高可用性（High Availability）に関連するタスクはすべてテナントレベルのDagSchedulerによってスケジュールされて実行されます。各テナントのスレッドリソースには限りがあるため、具体的なタスクタイプに基づいてパラメータを区別するのではなく、タスクの優先順位と重要性に基づいて3つの高可用性関連パラメータが設定されています。各パラメータは異なるタスクタイプに対応します：

* ha_high_thread_score：レプリケーション、Rebuild、復旧などの高優先順位で高可用性のタスクキューで使用するスレッドの並列数を設定します。
* ha_mid_thread_score：移行などの中優先順位で高可用性のタスクキューで使用するスレッドの並列数を設定します。
* ha_low_thread_score：バックアップ、バックアップのバックアップ、バックアップのクリーンアップなどの低優先順位で高可用性のタスクキューで使用するスレッドの並列数を設定します。

## 設定例

高可用性で低優先順位のスレッドの現在のワーカースレッド数を4に設定します。

```shell
obclient> ALTER SYSTEM SET ha_low_thread_score=4;
```

## 関連ドキュメント

* 高可用性で高優先順位のスレッドの現在のワーカースレッド数を設定する：[ha_high_thread_score](3500.ha_high_thread_score.md)
* 高可用性で中優先順位のスレッドの現在のワーカースレッド数を設定する：[ha_mid_thread_score](3700.ha_mid_thread_score.md)