|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||
|machine-translation| en |
|slug|enable_ls_leader_balance|

# enable_ls_leader_balance

<main id="notice" type='explain'>
<h4>説明</h4>
<ul><li>V4.4.x系では、このパラメータはV4.4.2バージョンから導入されました。</li>
<li>V4.2.5系では、このパラメータはV4.2.5 BP6バージョンから導入されました。</li></ul>
</main>

## 機能の概要

`enable_ls_leader_balance`は、ログストリームのリーダーに対する自動ロードバランシングを有効にするかどうかを制御します。

## 権限要件

* パラメータの確認

  `sys`テナントとすべてのユーザーテナントは`SHOW PARAMETERS`ステートメント、または`GV$OB_PARAMETERS`ビューを使用してこのパラメータを確認できます。

* パラメータの変更

  `sys`テナントとユーザーテナントは`ALTER SYSTEM SET`ステートメントを使用してこのパラメータを変更できます。

## プロパティの説明

|  **プロパティ** | **説明** |
|-----------|----------|
| パラメータタイプ   | Bool |
| デフォルト値     | True |
| 値の範囲   | <ul><li>`True`：ログストリームのリーダーに対する自動ロードバランシングを有効にする</li><li>`False`：ログストリームのリーダーに対する自動ロードバランシングを無効にする</li></ul>|
| 変更は可能か | はい。`ALTER SYSTEM SET`ステートメントを使用して変更できます。|
| OBServerノードの再起動が必要か | 不要です。設定は即時に反映されます。|

## 使用方法

* [enable_rebalance](2800.enable_rebalance.md)が有効（値は`True`）の場合、`enable_ls_leader_balance`はログストリームのリーダーに対する自動ロードバランシングを無効にするかどうかを個別に制御できます。

* このパラメータが無効の場合（値は`False`）、ログストリームレベルでの自動リーダー切り替えは発生しませんが、パーティションレベルでは異なるリーダーのログストリーム間で転送が行われることによりリーダー切り替えが発生する可能性があります。

## 設定例

* ユーザーテナントが自身のテナントのログストリームリーダーに対する自動ロードバランシングを無効にします。

  ```shell
  obclient> ALTER SYSTEM SET enable_ls_leader_balance = False;
  ```

* システムテナントは`mysql`テナントのログストリームリーダーの自動バランシングを無効にすることを指定します。

  ```shell
  obclient> ALTER SYSTEM SET enable_ls_leader_balance = False TENANT = 'mysql';
  ```

## 関連ドキュメント

* 自動ロードバランシングを有効にするかどうかをを制御します：[enable_rebalance](2800.enable_rebalance.md)

* ログストリームの自動転送を許可するかどうかをを制御します：[enable_transfer](3000.enable_transfer.md)

* ログストリームレプリカの自動移行・複製の総合スイッチ：[enable_rereplication](../300.cluster-level-configuration-items/8000.enable_rereplication.md)

* ログストリームリーダーのバランシングに関する詳細は、[テナント内のバランシング](../../../100.oceanbase-database-concepts/500.distributed-database-objects/300.data-partitions-and-replicas/500.data-balancing/100.intra-tenant-balancing.md)を参照してください。