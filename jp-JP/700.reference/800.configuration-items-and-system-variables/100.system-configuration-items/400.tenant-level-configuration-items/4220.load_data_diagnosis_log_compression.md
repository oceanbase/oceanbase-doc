|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||
|machine-translation| en |
|slug|load_data_diagnosis_log_compression|

# load_data_diagnosis_log_compression

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>この構成パラメータは、V4.4.0バージョンから導入されました。</p>
</main>

## 機能の概要

`load_data_diagnosis_log_compression`は、診断ログで使用する圧縮アルゴリズムを設定します。

## 権限要件

* パラメータの確認

  `sys`テナントとすべてのユーザーテナントは`SHOW PARAMETERS`ステートメント、または`GV$OB_PARAMETERS`ビューを使用してこのパラメータを確認できます。

* パラメータの変更

  `sys`テナントとユーザーテナントはこの構成パラメータを変更できます。

## プロパティの説明

| **プロパティ** | **説明** |
|----------|----------|
| パラメータタイプ   | String |
| デフォルト値     | AUTO |
| 値の範囲   | <ul><li>AUTO</li><li>NONE</li><li>ZSTD</li><li>GZIP</li><li>DEFLATE</li></ul> <main id="notice" type='explain'><h4>説明</h4><p>圧縮アルゴリズムを <code>AUTO</code> に指定した場合、エクスポートされたログファイル名の拡張子に基づいて圧縮アルゴリズムが選択されます。</p></main>|
| 変更は可能か | はい。`ALTER SYSTEM SET`ステートメントを使用して変更できます。|
| OBServerノードの再起動が必要か | 不要です。設定は即時に反映されます。|

## 使用方法

この構成パラメータは、診断ログファイルを圧縮するかどうか、および使用する圧縮アルゴリズムを制御します。

## 設定例

診断ログで使用する圧縮アルゴリズムを`ZSTD`に設定します。

```shell
obclient> ALTER SYSTEM SET load_data_diagnosis_log_compression = 'ZSTD';
```

## 関連ドキュメント

* 単一診断ログファイルのサイズ設定については、[load_data_diagnosis_log_max_size](4230.load_data_diagnosis_log_max_size.md)を参照してください。
* インポートURL外部テーブル処理中のエラー診断を有効にする方法については、[LOAD DATA (MySQLモード)](../../../500.sql-reference/100.sql-syntax/200.common-tenant-of-mysql-mode/600.sql-statement-of-mysql-mode/5900.load-data-of-mysql-mode.md)および[LOAD DATA (Oracleモード)](../../../500.sql-reference/100.sql-syntax/300.common-tenant-of-oracle-mode/900.sql-statement-of-oracle-mode/300.dcl-of-oracle-mode/1900.load-data-of-oracle-mode.md)を参照してください。