|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||
|machine-translation|en,ja-marker,ja-autoPR|
|slug|enable_kv_ttl|

# enable_kv_ttl

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>この構成パラメータは、V3.1.4バージョンから導入されました。</p>
</main>

## 機能の概要

`enable_kv_ttl`は、TTLバックグラウンドタスクを開始するかどうかを制御します。この構成パラメータは、周期的なTTLタスクに対して有効であり、ユーザー管理コマンドはこの構成パラメータによって制限されません。

<main id="notice" type='notice'>
  <h4>注意</h4>
  <p>期限切れデータの削除（TTL）機能は、OceanBaseのKVシナリオでのみ使用可能です。SQLシナリオでは無効になっており、有効にすると予期しないデータの誤削除が発生する可能性があります。</p>
</main>

## 権限要件

* パラメータの確認

  `sys`テナントとすべてのユーザーテナントは`SHOW PARAMETERS`ステートメント、または`GV$OB_PARAMETERS`ビューを使用してこのパラメータを確認できます。

* パラメータの変更

  `sys`テナントとユーザーテナントはこの構成パラメータを変更できます。

## プロパティの説明

| **プロパティ** | **説明** |
| -------- | -------- |
| パラメータタイプ             | Bool |
| デフォルト値               | False |
| 値の範囲             | <ul><li> True : 有効化</li><li> False : 無効化 </li></ul>|
| 変更は可能か           | はい。`ALTER SYSTEM SET` ステートメントを使用して変更できます。|
| OBServerノードの再起動が必要か | 不要です。設定は即時に反映されます。 |

## 設定例

TTLバックグラウンドタスクを開始しないように設定します。

```shell
obclient> ALTER SYSTEM SET enable_kv_ttl= false;
```