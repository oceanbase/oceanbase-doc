|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||
|machine-translation|en,ja-marker,ja-autoPR|
|slug|micro_block_merge_verify_level|

# micro_block_merge_verify_level

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>この構成パラメータは、V2.2.50バージョンから導入されました。</p>
</main>

## 機能の概要

`micro_block_merge_verify_level`は、マージ時のマクロブロックの検証レベルを設定します。

## 権限要件

* パラメータの確認

  `sys`テナントとすべてのユーザーテナントは`SHOW PARAMETERS`ステートメント、または`GV$OB_PARAMETERS`ビューを使用してこのパラメータを確認できます。

* パラメータの変更

  この構成パラメータは`sys`テナントのみが変更でき、ユーザーテナントでは変更できません。

## プロパティの説明

| **プロパティ** | **説明** |
| -------- | -------- |
| パラメータタイプ             | Int            |
| デフォルト値               | 2             |
| 値の範囲             | <ul><li>0：検証を行わないことを示します</li><li> 1：エンコーディングアルゴリズムを検証し、データが正確であることを確認するためにエンコードされたマイクロブロックを読み取ります  </li><li>2：エンコーディングおよび圧縮アルゴリズムを検証し、エンコーディング検証に加えて、圧縮ブロックを解凍してデータが正確であることを確認します  </li><li> 3：エンコーディング、圧縮アルゴリズム、および損失書き込み保護を検証します </li></ul>|
| 変更は可能か           | はい。`ALTER SYSTEM SET` ステートメントを使用して変更できます。|
| OBServerノードの再起動が必要か | 不要です。設定は即時に反映されます。 |

## 設定例

マージ時のマクロブロックの検証レベルを2に設定します。

```shell
obclient> ALTER SYSTEM SET micro_block_merge_verify_level = 2;
```