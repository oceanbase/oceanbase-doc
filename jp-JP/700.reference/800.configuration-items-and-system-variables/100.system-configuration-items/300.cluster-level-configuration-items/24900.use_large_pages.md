|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||
|machine-translation|en,ja-marker,ja-autoPR|
|slug|use_large_pages|

# use_large_pages

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>この構成パラメータは、V2.2.50バージョンから導入されました。</p>
</main>

## 機能の概要

`use_large_pages`は、データベースが使用するメモリの大ページ（Huge Pages）を管理します。

## 権限要件

* パラメータの確認

  `sys`テナントとすべてのユーザーテナントは`SHOW PARAMETERS`ステートメント、または`GV$OB_PARAMETERS`ビューを使用してこのパラメータを確認できます。

* パラメータの変更

  この構成パラメータは`sys`テナントのみが変更でき、ユーザーテナントでは変更できません。

## プロパティの説明

| **プロパティ** | **説明** |
| -------- | -------- |
| パラメータタイプ             | String      |
| デフォルト値               | false       |
| 値の範囲             | <ul><li>false：大ページの使用を禁止します。</li> <li>true：大ページと通常ページの混合使用を許可します。</li> <li>only：システムによる大ページの完全な使用を強制します。</li></ul>    |
| 変更は可能か           | はい。`ALTER SYSTEM SET` ステートメントを使用して変更できます。|
| OBServerノードの再起動が必要か | はい。設定後、ノードの再起動が必要です。 |

## 設定例

データベースでメモリの大ページの使用を禁止します。

```shell
obclient> ALTER SYSTEM SET use_large_pages = 'false';
```

## 関連ドキュメント

* [メモリ関連パラメータ](../../../../700.reference/100.oceanbase-database-concepts/1200.observer-node-architecture/500.memory-management/200.memory-related-parameters.md)