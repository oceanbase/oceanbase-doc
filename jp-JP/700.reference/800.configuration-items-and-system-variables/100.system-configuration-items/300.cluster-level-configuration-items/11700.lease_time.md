|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||
|machine-translation|en,ja-marker,ja-autoPR|
|slug|lease_time|

# lease_time

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>この構成パラメータは、V1.4バージョンから導入されました。</p>
</main>

## 機能の概要

`lease_time`は、ハートビートリースの時間を設定します。

## 権限要件

* パラメータの確認

  `sys`テナントとすべてのユーザーテナントは`SHOW PARAMETERS`ステートメント、または`GV$OB_PARAMETERS`ビューを使用してこのパラメータを確認できます。

* パラメータの変更

  この構成パラメータは`sys`テナントのみが変更でき、ユーザーテナントでは変更できません。

## プロパティの説明

| **プロパティ** | **説明** |
| -------- | -------- |
| パラメータタイプ    | Time         |
| デフォルト値      | 10s          |
| 値の範囲    | [1s, 5min] |
| 変更は可能か  | はい <main id="notice" type='explain'><h4>説明</h4><p>この構成パラメータを変更することは一般的に推奨されません。</p></main>|
| OBServerノードの再起動が必要か | 不要です |

## 使用方法

Root Serviceは、ハートビートリース期間内にOBServerノードからハートビート情報を受信しない場合、そのOBServerノードをオフライン状態と見なします。

## 設定例

ハートビートリース期間を10秒に設定します。

```shell
obclient> ALTER SYSTEM SET lease_time ='10s';
```

## 関連ドキュメント

Root Serviceの詳細については、[データベース層の高可用性](../../../../600.manage/400.high-availability/200.multi-replica-disaster-recovery/300.database-high-availability.md)を参照してください。