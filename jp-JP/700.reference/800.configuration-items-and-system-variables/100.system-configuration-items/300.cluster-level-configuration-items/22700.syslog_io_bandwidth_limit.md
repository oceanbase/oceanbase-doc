|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||
|machine-translation|en,ja-marker,ja-autoPR|
|slug|syslog_io_bandwidth_limit|

# syslog_io_bandwidth_limit

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>この構成パラメータは、V1.4バージョンから導入されました。</p>
</main>

## 機能の概要

`syslog_io_bandwidth_limit`は、システムログが使用できるディスクI/O帯域幅の上限を設定します。帯域幅の上限を超えるシステムログは破棄されます。

## 権限要件

* パラメータの確認

  `sys`テナントとすべてのユーザーテナントは`SHOW PARAMETERS`ステートメント、または`GV$OB_PARAMETERS`ビューを使用してこのパラメータを確認できます。

* パラメータの変更

  この構成パラメータは`sys`テナントのみが変更でき、ユーザーテナントでは変更できません。

## プロパティの説明

| **プロパティ** | **説明** |
| -------- | -------- |
| パラメータタイプ             | Capacity  |
| デフォルト値               | 30MB、デフォルト単位はMBです。設定時に単位を明記する必要があります。   |
| 変更は可能か           | はい。`ALTER SYSTEM SET` ステートメントを使用して変更できます。|
| OBServerノードの再起動が必要か | 不要です。設定は即時に反映されます。 |

## 設定例

システムログが使用できるディスクI/O帯域幅を30MBに設定します。

```shell
obclient> ALTER SYSTEM SET syslog_io_bandwidth_limit = '30MB';
```

## 関連ドキュメント

* [ログ](../../../../700.reference/100.oceanbase-database-concepts/1200.observer-node-architecture/400.log.md)
* [ログ制御方式](../../../../600.manage/800.logging/400.log-control.md)
* [ログの安定性](../../../../600.manage/800.logging/300.log-stablization.md)