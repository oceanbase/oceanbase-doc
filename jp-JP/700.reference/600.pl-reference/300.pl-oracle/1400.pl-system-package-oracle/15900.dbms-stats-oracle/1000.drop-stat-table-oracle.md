| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
| machine-translation | en          |
|slug|drop_stat_table-oracle-compatible-mode|

# DROP_STAT_TABLE

`DROP_STAT_TABLE` プロシージャは、ユーザーの情報統計テーブルを削除するために使用されます。

  <main id="notice" >
    <h4>適用対象</h4>
    <p>この内容はOceanBaseデータベースEnterprise Editionにのみ適用されます。OceanBaseデータベースCommunity EditionはMySQLモードのみを提供します。</p>
  </main>

## 構文

```sql
DBMS_STATS.DROP_STAT_TABLE (
  ownname VARCHAR2,
  stattab VARCHAR2);
```

## パラメータ説明

|   パラメータ    |                説明                 |
|---------|-----------------------------------|
| ownname | ユーザー名。`NULL`に設定した場合、現在ログインしているユーザー名がデフォルトで使用されます。 |
| stattab | 削除するユーザー統計情報テーブルの名前。                   |

## 異常時の動作

エラーコード `OBE-20000` は、テーブルが存在しないか、権限が不足していることを示します。

## 使用方法

このプロシージャを呼び出すには、指定されたユーザー内でそのテーブルを削除する権限が必要です。

## 例

ユーザー `testUser01` の統計情報テーブル `test_stat` を削除します。

```sql
obclient> CALL DBMS_STATS.DROP_STAT_TABLE('testUser01', 'test_stat');
Query OK, 0 rows affected
```