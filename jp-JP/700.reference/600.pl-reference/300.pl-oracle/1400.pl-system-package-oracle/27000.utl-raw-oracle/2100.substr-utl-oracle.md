| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
| machine-translation | en          |
|slug|substr-utl-oracle-compatible-mode|

# SUBSTR

`SUBSTR` 関数は、`len` バイトを返します。これは、`RAW r` の `pos` ビットから計算されます。

  <main id="notice" >
    <h4>機能の適用範囲</h4>
    <p>この内容はOceanBaseデータベースEnterprise Editionにのみ適用されます。OceanBaseデータベースCommunity EditionはMySQLモードのみを提供します。</p>
  </main>

## 構文

```sql
UTL_RAW.SUBSTR (
   r   IN RAW,
   pos IN BINARY_INTEGER,
   len IN BINARY_INTEGER DEFAULT NULL)
  RETURN RAW;
```

## パラメータの説明

| **パラメータ** |                  **説明**                  |
|--------|------------------------------------------|
| r      | RAWから抽出する部分バイトの文字列。                   |
| pos    | `r` からバイトを抽出する位置。                          |
| len    | `r` から抽出するバイト数。`RAW r` の `pos` ビットから計算します(オプション)。 |

## デフォルト値とオプションパラメータ

| **オプションパラメータ** |        **説明**        |
|----------|----------------------|
| len      | `pos` から `r` の終端まで。 |

## 戻り値

|   **戻り値**    |           **説明**            |
|--------------|-----------------------------|
| portion of r | `pos` ビットから計算して `len` バイトの長さ。 |
| NULL         | `r` の入力パラメータが `NULL` の場合。            |

## 使用方法

* `pos` が正の場合、`SUBSTR` は `r` の開始から数えて最初のバイトを見つけます。`pos` が負の場合、`SUBSTR` は `r` の終端から逆算します。`pos` の値は0にすることはできません。

* `len` を省略した場合、`SUBSTR` は `r` の終端までのすべてのバイトを返します。`len` の値は1以上である必要があります。

## 例外状況

|   **例外**   |        **説明**           |
|-------------|-----------------------------|
| VALUE_ERROR | 以下の場合に `VALUE_ERROR` を返す可能性があります： <ul><li> `pos` が0または `r` の長さより大きい場合 </li> <li> `len` が1未満、または `r - (pos - 1)` より大きい場合。</li></ul>    |