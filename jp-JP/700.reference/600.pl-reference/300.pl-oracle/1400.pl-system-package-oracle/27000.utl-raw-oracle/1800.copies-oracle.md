| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
| machine-translation | en          |
|slug|copies-oracle-compatible-mode|

# COPIES

`COPIES` 関数は、n 個の `r` を連結したものを返します。

  <main id="notice" >
    <h4>適用対象</h4>
    <p>この内容はOceanBaseデータベースEnterprise Editionにのみ適用されます。OceanBaseデータベースCommunity EditionはMySQLモードのみ提供しています。</p>
  </main>

## 構文

```sql
UTL_RAW.COPIES (
     r IN RAW,
     n IN NUMBER)
  RETURN RAW;
```

## パラメータ説明

| **パラメータ** |       **説明**       |
|--------|--------------------|
| r      | コピー対象の `RAW` データ。         |
| n      | `RAW` データのコピー回数（正の整数）。 |

## 戻り値

n回コピーした `RAW` を返します。

## 例外状況

|   **例外**    |        **説明**      |
|-------------|------------------------|
| VALUE_ERROR | 次のいずれかの状況が発生する可能性があります： <ul><li> r が欠落している、`NULL` である、または長さが0である。</li>   <li> n \< 1。</li>   <li> 結果の長さがRAWの最大長を超えています。</li></ul>    |