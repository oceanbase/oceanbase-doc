| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
| machine-translation | en          |
|slug|set_balance_weight-oracle-compatible-mode|

# SET_BALANCE_WEIGHT

`SET_BALANCE_WEIGHT`はパーティションの重みを設定します。レベルは上から順に、テーブルレベルとパーティションレベルのパーティション重みを設定できます。

## 使用制限と注意事項

* ユーザーテナントのみが手動でパーティション重みを設定できます。システムテナントでは設定できません。

* 現在、パーティション重みの設定は、非パーティションテーブルおよびパーティションテーブルのパーティションに対してのみサポートされています。サブパーティションへの重み設定は現在サポートされていません。

* 既存のエンティティパーティションに対してのみ重みを設定できます。テーブル作成時に重みを指定することはできません。つまり、まずテーブルを作成し、その後でテーブルのパーティション重みを設定する必要があります。

* パーティション重みは整数のみをサポートし、値の範囲は[1, +∞)です。デフォルトでは、パーティションには重みがありません。

* パーティション重みを設定する際は、可能な限り少ない段階に分けることを推奨します。最大で3段階に分けることを推奨します。

  * 大重み = 100% × パーティション数

  * 中重み = 50% × パーティション数

  * 小重み = 1

* 重みの均等化に参加させる必要がないパーティションテーブルについては、パーティション重みを設定しないことを推奨します。

* パーティション重みを設定する際、下位のパーティションは上位のパーティションの重みをデフォルトで継承します。複数のレベルで同時にパーティション重みが設定されている場合、下位の重みが上位の重みを上書きします。

* パーティション重みを設定した後、ユーザーがOffline DDL操作（例：Truncate Table、Truncate Partitionなどの操作）を実行してパーティションのエンティティに変更が生じた場合、元のパーティション重みも無効になり、ユーザーは再度パーティション重みを設定する必要があります。パーティション重みが無効になる可能性のあるOffline DDLについては、[Online DDLとOffline DDL操作（Oracleモード）](../../../../500.sql-reference/100.sql-syntax/300.common-tenant-of-oracle-mode/1000.ddl-function-of-oracle-mode/150.online-and-offline-ddl-list-of-oracle-mode.md)を参照してください。

## 構文

```sql
DBMS_BALANCE.SET_BALANCE_WEIGHT(
    weight              IN BINARY_INTEGER,
    schema_name         IN VARCHAR2,
    table_name          IN VARCHAR2,
    partition_name      IN VARCHAR2 DEFAULT NULL,
    subpartition_name   IN VARCHAR2 DEFAULT NULL);
```

## パラメータ説明

| **パラメータ**         | **説明**                                            |
|------------------|-----------------------------------------------------|
| weight           | パーティションの重み。パーティション間でリソース（CPU、メモリ、ディスクなど）を使用する相対的な割合を示します。値の範囲は[1, +∞)です。 |
| schema_name      | テーブルが属するスキーマ名  |
| table_name       | テーブル名             |
| partition_name   | パーティション名        |
| subpartition_name| サブパーティション名。構文的にはサポートされていますが、現在サブパーティションの重み設定はサポートされていません。         |

## 例

1. ユーザー `SYS` がHashパーティションテーブル `TBL1_H` を作成したと仮定します。

    ```shell
    obclient(SYS@oracle001)[SYS]> CREATE TABLE TBL1_H(col1 INT,col2 VARCHAR(50)) PARTITION BY HASH(col1) PARTITIONS 5;
    ```

2. このテーブルのパーティション情報を確認するステートメントは以下のとおりです。

    ```shell
    obclient(SYS@oracle001)[SYS]> SELECT TABLE_NAME, PARTITION_NAME FROM USER_TAB_PARTITIONS WHERE TABLE_NAME = 'TBL1_H';
    ```

    実際に得られるパーティション情報は以下のとおりです：

    ```shell
    +------------+----------------+
    | TABLE_NAME | PARTITION_NAME |
    +------------+----------------+
    | TBL1_H     | P4             |
    | TBL1_H     | P3             |
    | TBL1_H     | P2             |
    | TBL1_H     | P1             |
    | TBL1_H     | P0             |
    +------------+----------------+
    5 rows in set
    ```

3. テーブル `TBL1_H` のテーブルレベルのパーティション重みを1に設定します。

    ```shell
   obclient(SYS@oracle001)[SYS]> delimiter //
    ```

    ```shell
    obclient(SYS@oracle001)t[SYS]> BEGIN
                  DBMS_BALANCE.SET_BALANCE_WEIGHT(1,'SYS','TBL1_H');
                  END;//
    ```

    ```shell
    obclient [SYS]> delimiter ;
    ```

4. テーブル `TBL1_H` のテーブルレベルのパーティション重みを設定した後、設定されたパーティション重み情報をクエリします。

    ```shell
    obclient(SYS@oracle001)[SYS]> SELECT * FROM SYS.DBA_OB_OBJECT_BALANCE_WEIGHT;
    ```

    結果は以下のとおりです：

    ```shell
    +----------+--------------+-----------------+--------+---------------+------------+----------------+-------------------+-----------------+-------------+---------------+-----------+
    | TABLE_ID | PARTITION_ID | SUBPARTITION_ID | WEIGHT | DATABASE_NAME | TABLE_NAME | PARTITION_NAME | SUBPARTITION_NAME | TABLEGROUP_NAME | DATABASE_ID | TABLEGROUP_ID | OBJECT_ID |
    +----------+--------------+-----------------+--------+---------------+------------+----------------+-------------------+-----------------+-------------+---------------+-----------+
    |   500002 |         NULL |            NULL |      1 | SYS           | TBL1_H     | NULL           | NULL              | NULL            |      201006 |          NULL |    500002 |
    +----------+--------------+-----------------+--------+---------------+------------+----------------+-------------------+-----------------+-------------+---------------+-----------+
    1 row in set
    ```

5. テーブル `TBL1_H` 内のパーティション `P0` のパーティション重みを2に設定します。

    ```shell
    obclient(SYS@oracle001)[SYS]> delimiter //
    ```

    ```shell
    obclient(SYS@oracle001)[SYS]> BEGIN
                  DBMS_BALANCE.SET_BALANCE_WEIGHT(2,'SYS','TBL1_H','P0');
                  END;//
    ```

    ```shell
    obclient(SYS@oracle001)[SYS]> delimiter ;
    ```

6. 再度、設定されたパーティション重み情報をクエリします。

    ```shell
    obclient(SYS@oracle001)[SYS]> obclient[SYS]> SELECT * FROM SYS.DBA_OB_OBJECT_BALANCE_WEIGHT;
    ```

    クエリ結果は以下のとおりです：

    ```shell
    +----------+--------------+-----------------+--------+---------------+------------+----------------+-------------------+-----------------+-------------+---------------+-----------+
    | TABLE_ID | PARTITION_ID | SUBPARTITION_ID | WEIGHT | DATABASE_NAME | TABLE_NAME | PARTITION_NAME | SUBPARTITION_NAME | TABLEGROUP_NAME | DATABASE_ID | TABLEGROUP_ID | OBJECT_ID |
    +----------+--------------+-----------------+--------+---------------+------------+----------------+-------------------+-----------------+-------------+---------------+-----------+
    |   500002 |         NULL |            NULL |      1 | SYS           | TBL1_H     | NULL           | NULL              | NULL            |      201006 |          NULL |    500002 |
    |   500002 |       500003 |            NULL |      2 | SYS           | TBL1_H     | P0             | NULL              | NULL            |      201006 |          NULL |    500003 |
    +----------+--------------+-----------------+--------+---------------+------------+----------------+-------------------+-----------------+-------------+---------------+-----------+
    2 rows in set
    ```

## 関連ドキュメント

* [テナント内での均等化](../../../../100.oceanbase-database-concepts/500.distributed-database-objects/300.data-partitions-and-replicas/500.data-balancing/100.intra-tenant-balancing.md)

* [パーティションの重みを設定する](../../../../../600.manage/350.load-balancing-manage/450.balance-weight-management/100.set-the-balance-weight-for-a-table.md)

* [パーティションの重みをクリアする](../../../../../600.manage/350.load-balancing-manage/450.balance-weight-management/300.clear-the-balance-weight.md)