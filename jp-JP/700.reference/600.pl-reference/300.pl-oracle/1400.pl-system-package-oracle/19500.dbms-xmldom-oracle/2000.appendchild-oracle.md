| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
| machine-translation | en          |
|slug|appendchild-oracle-compatible-mode|

# APPENDCHILD

`APPENDCHILD` 関数は、指定した親ノード `n` の下に子ノード `newchild` を追加し、DOMツリーの構築や変更に使用します。典型的な使用例は、`CREATEELEMENT` や `CREATETEXTNODE` で作成したノードを、ドキュメントのルートノードや他の要素ノードの下に追加する処理です。

<main id="notice" >
  <h4>機能の適用範囲</h4>
  <p>この内容はOceanBaseデータベースEnterprise版にのみ適用されます。OceanBaseデータベースCommunity版はMySQLモードのみを提供します。</p>
</main>

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>V4.4.2系では、このサブプログラムはOceanBaseデータベースV4.4.2 BP3から導入されました。</p>
</main>

## 構文

```sql
DBMS_XMLDOM.appendChild (
   n        IN DBMS_XMLDOM.DOMNODE,
   newchild IN DBMS_XMLDOM.DOMNODE);
```

## パラメータの説明

| **パラメータ** | **説明** |
|----------|----------|
| n | 親ノードの `DBMS_XMLDOM.DOMNODE` ハンドル。 |
| newchild | 追加する子ノードの `DBMS_XMLDOM.DOMNODE` ハンドル。 |

## 戻り値

追加された子ノードのハンドル（`DBMS_XMLDOM.DOMNODE`）。

## 使用方法

親ノードと子ノードは、どちらも有効な `DOMNODE` である必要があります。要素やテキストなどの具体的な型は、先に `MAKENODE` で変換してください。

## 例

```sql
DECLARE
  doc        DBMS_XMLDOM.DOMDOCUMENT;
  root_elem  DBMS_XMLDOM.DOMELEMENT;
  child_elem DBMS_XMLDOM.DOMELEMENT;
  root_node  DBMS_XMLDOM.DOMNODE;
  child_node DBMS_XMLDOM.DOMNODE;
  ret_node   DBMS_XMLDOM.DOMNODE;
BEGIN
  doc := DBMS_XMLDOM.newDOMDocument;
  root_elem := DBMS_XMLDOM.createElement(doc, 'root');
  child_elem := DBMS_XMLDOM.createElement(doc, 'item');
  root_node := DBMS_XMLDOM.makeNode(root_elem);
  child_node := DBMS_XMLDOM.makeNode(child_elem);
  ret_node := DBMS_XMLDOM.appendChild(root_node, child_node);
  DBMS_XMLDOM.freeDocument(doc);
END;
/
```
