| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
| machine-translation | en          |
|slug|getnodetype-oracle-compatible-mode|

# GETNODETYPE

`GETNODETYPE` 関数は、DOMノードの型番号を取得します。これにより、要素ノード、属性ノード、テキストノード、ドキュメントノードなどのDOMノードの種類を区別できます。

<main id="notice" >
  <h4>機能の適用範囲</h4>
  <p>この内容はOceanBaseデータベースEnterprise版にのみ適用されます。OceanBaseデータベースCommunity版はMySQLモードのみを提供します。</p>
</main>

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>V4.4.2系では、このサブプログラムはOceanBaseデータベースV4.4.2 BP3から導入されました。</p>
</main>

## 構文

```sql
DBMS_XMLDOM.getNodeType (
   n IN DBMS_XMLDOM.DOMNODE);
```

## パラメータの説明

| **パラメータ** | **説明** |
|----------|----------|
| n | 照会対象の `DBMS_XMLDOM.DOMNODE` ハンドル。 |

## 戻り値

ノードの型番号。型は `NUMBER` です。一般的な戻り値は、以下の `DBMS_XMLDOM` 定数と比較できます：

| **定数** | **値** | **説明** |
| --- | --- | --- |
| `ELEMENT_NODE` | 1 | 要素ノード |
| `ATTRIBUTE_NODE` | 2 | 属性ノード |
| `TEXT_NODE` | 3 | テキストノード |
| `CDATA_SECTION_NODE` | 4 | CDATAノード |
| `ENTITY_REFERENCE_NODE` | 5 | エンティティ参照ノード |
| `ENTITY_NODE` | 6 | エンティティノード |
| `PROCESSING_INSTRUCTION_NODE` | 7 | 処理命令ノード |
| `COMMENT_NODE` | 8 | コメントノード |
| `DOCUMENT_NODE` | 9 | ドキュメントノード |
| `DOCUMENT_TYPE_NODE` | 10 | ドキュメント型ノード |
| `DOCUMENT_FRAGMENT_NODE` | 11 | ドキュメントフラグメントノード |
| `NOTATION_NODE` | 12 | 記法ノード |

## 使用方法

戻り値の意味は、Oracle DOMのノード型定数と一致します。`DBMS_XMLDOM.ELEMENT_NODE`、`DBMS_XMLDOM.TEXT_NODE` などの定数と直接比較し、ノードの種類に応じて処理を分岐できます。

## 例

```sql
DECLARE
  n         DBMS_XMLDOM.DOMNODE;
  node_type NUMBER;
BEGIN
  node_type := DBMS_XMLDOM.getNodeType(n);
  IF node_type = DBMS_XMLDOM.ELEMENT_NODE THEN
    NULL; -- 要素ノードの処理
  ELSIF node_type = DBMS_XMLDOM.TEXT_NODE THEN
    NULL; -- テキストノードの処理
  END IF;
END;
/
```
