| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
| machine-translation | en          |
|slug|domdocument-oracle-compatible-mode|

# DOMDOCUMENT

`DOMDOCUMENT`は、パース済みXMLドキュメント全体を表すオプアクティブハンドルであり、`GETELEMENTSBYTAGNAME`や`FREEDOCUMENT`などのAPIにおけるドキュメントレベルの入力パラメータとして使用されます。オプアクティブ型は、パース済みXMLドキュメントを参照するオブジェクトを表し、DOM関連APIへの入出力にのみ使用され、その内部構造には直接アクセスできません。

<main id="notice" >
  <h4>機能の適用範囲</h4>
  <p>この内容はOceanBaseデータベースEnterprise Editionにのみ適用されます。OceanBaseデータベースCommunity EditionはMySQLモードのみ提供します。</p>
</main>

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>V4.4.2バージョンでは、この型はOceanBaseデータベースV4.4.2 BP2バージョンから導入されました。</p>
</main>

## 構文

```sql
doc DBMS_XMLDOM.DOMDOCUMENT;
```

## パラメータの説明

| **パラメータ** | **説明** |
|----------|----------|
| doc | ドキュメントハンドル。 |

## 戻り値

ドキュメントハンドル。

## 使用方法

`DBMS_XMLDOM.DOMDOCUMENT`は通常、[DBMS_XMLPARSER.GETDOCUMENT](../19750.dbms-xmlparser-oracle/500.getdocument-oracle.md)によって返され、直接作成することはできません。

## 関連サブプログラム

| **サブプログラム** | **説明** |
|------------|----------|
| [GETELEMENTSBYTAGNAME](../19500.dbms-xmldom-oracle/700.getelementbytagname-oracle.md) | タグ名に基づいて、XMLドキュメント全体から一致するすべての要素ノードを一括取得するために使用されます。このメソッドでは、`DOMDOCUMENT`型のドキュメントハンドルを入力パラメータとして渡す必要があります。|
| [FREEDOCUMENT](../19500.dbms-xmldom-oracle/500.freedocument-oracle.md) | `DOMDOCUMENT`型ハンドルが識別するXMLドキュメントリソースを解放し、リソースリークを防ぎます。使用時には、解放対象のドキュメントハンドルを入力パラメータとして渡します。 |

## 例

この例では、`DBMS_XMLPARSER`システムパッケージを使用してXMLドキュメントを解析し、`DOMDOCUMENT`ハンドルを取得します。最後に`DBMS_XMLDOM`システムパッケージを使用してドキュメントリソースを解放します。

```sql
-- パーサーとドキュメントハンドルの変数を宣言する
DECLARE
  p   DBMS_XMLPARSER.PARSER;
  doc DBMS_XMLDOM.DOMDOCUMENT;
BEGIN
  -- パーサーインスタンスを作成する
  p := DBMS_XMLPARSER.NEWPARSER;
  -- パーサーでXMLドキュメントを解析する
  DBMS_XMLPARSER.PARSECLOB(p, '<root/>');
  -- XMLドキュメントのDOMDOCUMENTハンドルを取得する
  doc := DBMS_XMLPARSER.GETDOCUMENT(p);
  -- パーサーインスタンスを解放する
  DBMS_XMLPARSER.FREEPARSER(p);
  -- XMLドキュメントリソースを解放する
  DBMS_XMLDOM.FREEDOCUMENT(doc);
END;
/
```