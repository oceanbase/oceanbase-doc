| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
| machine-translation | en          |
|slug|debug_on-oracle-compatible-mode|

# DEBUG_ON

`DEBUG_ON` ストアドプロシージャは、対象セッションにマーカーを付けて、デバッグモードですべてのPLを実行するために使用されます。この操作は、デバッグの実行開始前に完了しておく必要があります。

  <main id="notice" >
    <h4>機能の適用範囲</h4>
    <p>この内容はOceanBaseデータベースEnterprise Editionにのみ適用されます。OceanBaseデータベースCommunity EditionはMySQLモードのみを提供します。</p>
  </main>

## 構文

```sql
DBMS_DEBUG.DEBUG_ON (
no_client_side_plsql_engine BOOLEAN := TRUE,
immediate                   BOOLEAN := FALSE);
```

## パラメータの説明

|           **パラメータ**            |                    **説明**                    |
|-----------------------------|----------------------------------------------|
| no_client_side_plsql_engine | デバッグセッションがクライアントPLエンジンから実行される場合を除き、デフォルト値のままにしておいてください。              |
| immediate                   | 値が `TRUE` の場合、呼び出し中にInterpreterが通常モードからデバッグモードに即座に切り替わります。 |