| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
| machine-translation | en          |
|slug|remove_rule-oracle-mode|

# REMOVE_RULE

`REMOVE_RULE` プロシージャは、カスタムリライトルールを削除するために使用されます。

  <main id="notice" >
    <h4>適用対象</h4>
    <p>この内容はOceanBaseデータベースEnterprise Editionにのみ適用されます。OceanBaseデータベースCommunity EditionはMySQLモードのみを提供します。</p>
  </main>

## 構文

```sql
PROCEDURE REMOVE_RULE (
    rule_name          VARCHAR(256));
```

## パラメータ説明

| パラメータ | 説明 |
| --- | --- |
| rule_name | ルール名。 |

## 例

```shell
obclient> CALL DBMS_UDR.REMOVE_RULE('rule1');
Query OK, 0 rows affected

obclient> SELECT DB_NAME, RULE_NAME, PATTERN, REPLACEMENT, NORMALIZED_PATTERN, STATUS, PATTERN_DIGEST
    FROM oceanbase.DBA_OB_USER_DEFINED_RULES;
Empty set
```