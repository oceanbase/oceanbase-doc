| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
| machine-translation | en          |
|slug|putf-oracle-compatible-mode|

# PUTF

`PUTF` ストアドプロシージャは、指定されたファイルにフォーマット文字列を書き込むために使用されます。

<main id="notice" >
    <h4>機能の適用範囲</h4>
    <p>この内容はOceanBaseデータベースEnterprise Editionにのみ適用されます。OceanBaseデータベースCommunity Editionは現在サポートされていません。</p>
  </main>

## 構文

```sql
UTL_FILE.PUTF (
    file   IN FILE_TYPE,
    format IN VARCHAR2,
    arg1   IN VARCHAR2 DEFAULT NULL,
    arg2   IN VARCHAR2 DEFAULT NULL,
    arg3   IN VARCHAR2 DEFAULT NULL,
    arg4   IN VARCHAR2 DEFAULT NULL,
    arg5   IN VARCHAR2 DEFAULT NULL
);
```

## パラメータの説明

|    パラメータ     |           説明            |
|-----------|-------------------------|
|file|`FOPEN` を呼び出して返されたアクティブなファイルハンドル。|
|format|フォーマット文字列。以下を含むことができます：<ul><li><code>%s</code>：最大5つのプレースホルダーがあり、順にargNパラメータの値で置き換えられます。<li><code>\n</code>：改行文字です。文字列内で複数回使用できます。</li></ul>|
|arg1 から arg5|最大5つのオプションパラメータがあり、`format` 内の `%s` に順次置き換えられます。パラメータがプレースホルダーより少ない場合、置き換えられなかった `%s` は元のまま残るか無視されます。|