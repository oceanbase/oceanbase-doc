| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
| machine-translation | en          |
|slug|utl_file-overview-oracle-compatible-mode|

# UTL_FILEの概要

`UTL_FILE` システムパッケージを使用すると、PLプログラムはオペレーティングシステムのテキストファイルを読み書きできます。`UTL_FILE` は、オペレーティングシステムのストリームファイルI/Oの制限されたバージョンを提供します。

  <main id="notice" >
    <h4>機能の適用範囲</h4>
    <p>この内容はOceanBaseデータベースEnterprise Editionにのみ適用されます。OceanBaseデータベースCommunity Editionは現在サポートされていません。</p>
  </main>

## UTL_FILE権限の説明

ユーザーは `UTL_FILE` システムパッケージに対する `EXECUTE` 権限を持っている必要があります。

## UTL_FILEデータ構造

`UTL_FILE` システムパッケージは、`RECORD` 型として `FILE_TYPE` を定義しています。

`FILE_TYPE` の内容は `UTL_FILE` システムパッケージのプライベートな内容であるため、参照または変更することはできません。

### 構文

```sql
TYPE file_type IS RECORD (
id                BINARY_INTEGER,
datatype          BINARY_INTEGER,
byte_mode         BOOLEAN);
```

### パラメータの説明

|    パラメータ     |                説明                 |
|-----------|-----------------------------------|
| id        | 内部ファイルハンドル番号の数値。                      |
| datatype  | ファイルが `CHAR` ファイル、`Nchar` ファイル、またはその他（バイナリ）であるかどうかを示します。 |
| byte_mode | ファイルをバイナリファイルとして開くか、テキストファイルとして開くかを指定します。           |

## UTL_FILE例外状況

|        エラー名         |         意味                     |
|---------------------|---------------------------------|
| INVALID_PATH        | ファイルのパスが無効です。        |
| INVALID_MODE        | `FOPEN` 関数の `open_mode` パラメータが無効です。   |
| INVALID_FILEHANDLE  | ファイルハンドルが無効です。   |
| INVALID_OPERATION   | サポートされていないファイル操作です。    |
| READ_ERROR          | 読み取りキャッシュが小さすぎるか、読み取り中にオペレーティングシステム関連のエラーが発生しました。    |
| WRITE_ERROR         | 書き込み中にオペレーティングシステム関連のエラーが発生しました。 |
| INTERNAL_ERROR      | 内部で定義されたPLエラーです。   |
| FILE_OPEN           | ファイルを開く際にエラーが発生しました。  |
| INVALID_MAXLINESIZE | `FOPEN` 関数のパラメータ `max_linesize` が無効です。サポート範囲は \[1,32767\] です。 |
| INVALID_FILENAME    | パラメータとして指定されたファイル名が無効です。 |
| ACCESS_DENIED       | 権限不足のためファイルにアクセスできません。  |
| INVALID_OFFSET      | 考えられる原因は以下の通りです。<ul><li> `ABSOLUTE_OFFSET` と `RELATIVE_OFFSET` の両方が `NULL` の場合    </li><li> `ABSOLUTE_OFFSET` \< 0    </li><li> `ABSOLUTE_OFFSET` が `NULL` の場合、指定された `RELATIVE_OFFSET` がファイルの長さを超えています </li></ul>   |

## UTL_FILEサブプログラムの概要

次の表は、OceanBaseデータベースの現在のバージョンでサポートされている`UTL_FILE`サブプログラムとその簡単な説明です。

|                           サブプログラム                           |                説明                 |
|---------------------------------------------------------|-----------------------------------|
| [FCLOSE](../26100.utl-file-oracle/200.fclose-oracle.md)   | ファイルを閉じます。                             |
|[FCOPY](240.fcopy-oracle.md)|ソースファイルからターゲットファイルに行をコピーします。|
|[FCLOSE_ALL](250.fclose-all-oracle.md)|現在のセッションで `UTL_FILE` が開いているすべてのファイルを閉じます。|
| [FFLUSH](../26100.utl-file-oracle/300.fflush-oracle.md)   | バッファ内のデータをファイルに刷新します。                     |
| [FOPEN](../26100.utl-file-oracle/400.fopen-oracle.md)    | ユーザーが指定した情報に基づいてファイルを開きます。                    |
|[FOPEN_NCHAR](450.fopen_nchar-oracle.md)|NCHARモードでファイルを開きます。|
| [FGETPOS](../26100.utl-file-oracle/500.fgetpos-oracle.md)  | ファイルの現在のアクセス位置（オフセット）をバイト単位で取得します。         |
|[FGETATTR](550.fgetattr-oracle.md)|オペレーティングシステムのファイル属性（存在、サイズなど）を読み取るプロシージャです。|
| [F_REMOVE](../26100.utl-file-oracle/600.fremove-oracle.md)  | ディスク上のファイルを削除します（削除権限が必要）。UNIXの `rm` コマンドに類似しています。 |
| [FRENAME](../26100.utl-file-oracle/700.frename-oracle.md)  | 既存のファイルを新しい名前にリネームします。UNIXの `mv` コマンドに類似しています。    |
| [FSEEK](../26100.utl-file-oracle/800.fseek-oracle.md)    | 指定されたバイト数だけファイルの位置を前方または後方に調整します。               |
| [NEW_LINE](../26100.utl-file-oracle/900.new-line-utl-oracle.md) | 1つ以上のオペレーティングシステム指定の行終端文字をファイルに書き込みます。            |
|[NEW_LINE_NCHAR](950.new-line-nchar-oracle.md)|ファイルに改行文字を挿入するために使用されます。|
|[GET_RAW](960.get-raw-oracle.md)|オペレーティングシステムのファイルから生のバイナリデータを読み取るために使用されます。|
| [GET_LINE](../26100.utl-file-oracle/1000.get-line-utl-oracle.md) | 1行のテキストを読み取ります。                           |
|[GET_LINE_NCHAR](1050.get-line-nchar-oracle.md)|`Unicode`モードでテキストファイルから1行の内容を読み取るために使用されます。|
| [PUT](../26100.utl-file-oracle/1100.put-utl-oracle.md)      | 文字列をファイルに書き込みます。                         |
|[PUT_RAW](1150.put-raw-oracle.md)|入力パラメータをRAW形式で指定されたファイルに書き込むストアドプロシージャです。|
| [PUT_LINE](../26100.utl-file-oracle/1200.put-line-utl-oracle.md) | 1行をファイルに書き込み、オペレーティングシステムが指定する行終端文字を付加します。           |
|[PUT_NCHAR](1250.put-nchar-oracle.md)|`NCHAR`モードで現在の行バッファに追加するために使用されます。|
|[PUT_LINE_NCHAR](1260.put-line-nchar-oracle.md)|ファイルにUnicode文字を書き込むストアドプロシージャです。|
|[PUTF](1270.putf-oracle.md)|フォーマット文字列を指定されたファイルに書き込むために使用されます。|
|[PUTF_NCHAR](1280.putf-nchar-oracle.md)|`NCHAR`方式でフォーマットを指定したファイルに書き込むために使用されます。|
|[IS_OPEN](1300.utl-is-open-oracle.md)|ハンドルがまだ開いているかどうかを判断するために使用されます。|