| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
| machine-translation | en          |
|slug|put_nchar-oracle-compatible-mode|

# PUT_NCHAR

`PUT_NCHAR` ステアリングプロシージャは、現在の行バッファに `NCHAR` 方式で追加するために使用されます。

<main id="notice" >
    <h4>機能の適用範囲</h4>
    <p>この内容はOceanBaseデータベースEnterprise Editionにのみ適用されます。OceanBaseデータベースCommunity Editionは現在サポートされていません。</p>
  </main>

## 構文

```sql
UTL_FILE.PUT_NCHAR(
    file    IN FILE_TYPE,
    buffer  IN VARCHAR2
);
```

## パラメータの説明

|    パラメータ     |           説明            |
|-----------|-------------------------|
| file      | `FOPEN` を呼び出して返されたアクティブなファイルハンドル。  |
| buffer    | テキストバッファ。ファイルに書き込む行を含みます。        |