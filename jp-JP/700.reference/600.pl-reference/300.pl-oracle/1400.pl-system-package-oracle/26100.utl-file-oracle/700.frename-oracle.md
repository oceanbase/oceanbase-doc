| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
| machine-translation | en          |
|slug|frename-oracle-compatible-mode|

# FRENAME

`FRENAME` ストアドプロシージャは、ファイルの名前を変更するために使用されます。

  <main id="notice" >
    <h4>機能の適用範囲</h4>
    <p>この内容はOceanBaseデータベースEnterprise Editionにのみ適用されます。OceanBaseデータベースCommunity Editionは現在サポートされていません。</p>
  </main>

## 構文

```sql
UTL_FILE.FRENAME (
src_location       IN   VARCHAR2,
src_filename       IN   VARCHAR2,
dest_location      IN   VARCHAR2,
dest_filename      IN   VARCHAR2,
overwrite          IN   BOOLEAN DEFAULT FALSE);
```

## パラメータの説明

|      パラメータ       |                                                                        説明                                                                        |
|---------------|--------------------------------------------------------------------------------------------------------------------------------------------------|
| src_location  | ソースファイルのパスとファイル名（大文字小文字を区別します）。                                                                                                                             |
| src_filename  | 名前を変更するソースファイル。                                                                                                                                        |
| dest_location | ターゲットファイルのパス。                                                                                                                                       |
| dest_filename | ファイルの新しい名前。                                                                                                                                          |
| overwrite     | ターゲットファイルが存在する場合、それを上書きするかどうかを示します。デフォルト値は `FALSE` で、ターゲットファイルは上書きされません。ターゲットファイルが存在する場合、`RENAME_FAILED` 例外が返されます。また、OceanBaseデータベースは、ターゲットパスとファイルが存在するかどうか、およびAdminユーザーが十分な権限を持っているかどうかを検証しません。 |

## 例外

* `ACCESS_DENIED`

* `INVALID_FILENAME`

* `INVALID_PATH`

* `RENAME_FAILED`