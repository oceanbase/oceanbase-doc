| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
| machine-translation | en          |
|slug|new_line_nchar-oracle-mode|

# NEW_LINE_NCHAR

`NEW_LINE_NCHAR`は、ファイルに改行文字を挿入するために使用されます。

 <main id="notice" >
    <h4>適用対象</h4>
    <p>この内容はOceanBaseデータベースEnterprise Editionにのみ適用されます。OceanBaseデータベースCommunity Editionは現在サポートされていません。</p>
  </main>

## 構文

```sql
UTL_FILE.NEW_LINE (
file      IN FILE_TYPE,
lines    IN BINARY_INTEGER := 1);
```

## パラメータの説明

| パラメータ |                   説明                   |
|-------|----------------------------------------|
| file  | 既に開いているファイルのハンドルを表します。 |
| lines | 書き込む行終端文字列の数を指定するために使用されます。デフォルト値は`1`です。このパラメータは、ファイル内に挿入する改行文字の数を決定します。                           |