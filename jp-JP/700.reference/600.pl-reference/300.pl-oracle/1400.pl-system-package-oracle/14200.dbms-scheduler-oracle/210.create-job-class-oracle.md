| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
| machine-translation | en          |
|slug|create_job_class-oracle-compatible-mode|

# CREATE_JOB_CLASS

`CREATE_JOB_CLASS`は、JOBクラスを作成するために使用されます。

<main id="notice" >
    <h4>適用対象</h4>
    <p>この内容はOceanBaseデータベースEnterprise EditionのOracleテナントにのみ適用されます。OceanBaseデータベースCommunity Editionはサポートしていません。</p>
  </main>

## 構文

```sql
DBMS_SCHEDULER.CREATE_JOB_CLASS( job_class_name          IN VARCHAR2,
                               resource_consumer_group   IN VARCHAR2 DEFAULT NULL,
                               service                   IN VARCHAR2 DEFAULT NULL,
                               logging_level             IN VARCHAR2 DEFAULT 'RUNS',
                               log_history               IN PLS_INTEGER DEFAULT NULL,
                               comments                  IN VARCHAR2 DEFAULT NULL);
```

## パラメータ説明

|         パラメータ        |       説明           |
|--------------------|-----------------------|
|job_class_name| ジョブクラスの名前。|
|resource_consumer_group|ジョブクラスに関連付けるリソース消費グループの名前。|
|logging_level| ログ記録レベルのパラメータ。|
|log_history|ログの履歴保持日数。|
|comments|ジョブクラスを説明またはコメントするテキスト。|

## 使用例

```shell
obclient> BEGIN
    ->   DBMS_SCHEDULER.CREATE_JOB_CLASS (
    ->     job_class_name => 'my_job_class',
    ->     resource_consumer_group => 'DEFAULT_CONSUMER_GROUP',
    ->     logging_level => DBMS_SCHEDULER.LOGGING_RUNS,
    ->     comments => 'This is my custom job class for specific tasks'
    ->   );
    -> END;
    -> /
Query OK, 1 row affected (0.038 sec)
```