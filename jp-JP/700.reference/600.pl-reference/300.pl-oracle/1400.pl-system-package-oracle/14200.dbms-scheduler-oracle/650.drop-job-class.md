| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
| machine-translation | en          |
|slug|drop_job_class|

# DROP_JOB_CLASS

`DROP_JOB_CLASS`は、JOBクラスのログを削除するために使用されます。

<main id="notice" >
    <h4>機能の適用範囲</h4>
    <p>この内容はOceanBaseデータベースEnterprise EditionのOracleテナントにのみ適用されます。OceanBaseデータベースCommunity Editionはサポートしていません。</p>
  </main>

## 構文

```sql
  DBMS_SCHEDULER.DROP_JOB_CLASS ( job_class_name          IN VARCHAR2,
                             force                   IN BOOLEAN DEFAULT FALSE);
```

## パラメータの説明

|        パラメータ       |      説明      |
|------------------|-----------------|
|job_class_name|JOBクラスの名前。|
|force|このフィールドは`false`のみをサポートします。`job_class`に関連付けられたJOBが存在する場合、`drop class`はエラーとなり失敗します。|

## 例

```shell
obclient> BEGIN
    ->   DBMS_SCHEDULER.DROP_JOB_CLASS(
    ->     job_class_name => 'my_job_class'
    ->   );
    -> END;
    -> /
Query OK, 1 row affected (0.021 sec)
```