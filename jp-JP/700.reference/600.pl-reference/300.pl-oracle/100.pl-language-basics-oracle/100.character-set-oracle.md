| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
| machine-translation | en          |
|slug|character-set-oracle-compatible-mode|

# 文字セット

PLによって処理されたりデータベースに保存されたりするすべての文字データは、バイト順リストで表現されなければなりません。単一の文字のバイト表現を文字コードと呼び、一連の文字コードを文字セットと呼びます。

OceanBaseデータベースは、データベース文字セットと国別文字セットをサポートしています。PLもこれらの文字セットの使用をサポートしています。

  <main id="notice" >
    <h4>機能の適用範囲</h4>
    <p>この内容はOceanBaseデータベースEnterprise Editionにのみ適用されます。OceanBaseデータベースCommunity EditionはMySQLモードのみを提供します。</p>
  </main>

## データベース文字セット

PLはデータベース文字セットを使用して以下の情報を表現します：

* PLユニットに保存されるソーステキスト。

* `CHAR`、`VARCHAR2`、`CLOB`、`LONG` データ型の文字値。

データベース文字セットは、サポートされる各文字を特定の1バイトにマッピングするシングルバイト形式である場合もあれば、1、2、3、または4つの可変幅のマルチバイトシーケンスにマッピングする形式である場合もあります。文字コードの最大バイト数は、特定の文字セットによって異なります。

各データベース文字セットには、以下の基本文字が含まれます：

* ラテン文字：A から Z および a から z

* 数字：0 から 9

* 点字句（下表参照）

* スペース文字：スペース、タブ、改行、キャリッジリターン

基本文字のみを使用したPLソーステキストは、任意のデータベースに保存およびコンパイルできます。非基本文字を使用したPLソーステキストは、それらの非基本文字をサポートするデータベースでのみ保存およびコンパイルできます。

データベース文字セットの点字句は以下の表のとおりです：

| **記号** | **名前** |
|--------|--------|
| (      | 左括弧    |
| )      | 右括弧    |
| \<     | 左尖括号   |
| \>     | 右尖括号   |
| +      | プラス記号     |
| -      | チルダまたはマイナス記号 |
| \*     | アスタリスク     |
| /      | スラッシュ     |
| =      | イコール記号     |
| ,      | コンマ     |
| ;      | セミコロン     |
| :      | コロン     |
| .      | ピリオド     |
| !      | アスタリスク    |
| ?      | クエスチョンマーク |
| '      | アポストロフィーまたはシングルクォート |
| "      | ダブルクォート |
| @      | アット記号  |
| %      | パーセント記号    |
| #      | ニューメラックマーク     |
| $      | ドル記号   |
| _      | アンダースコア    |
| \|     | バーティカルバー     |

## 国家文字セット

PLは国家文字セットを使用して、`NCHAR`、`NVARCHAR2`、および`NCLOB`データ型の文字値を表現します。