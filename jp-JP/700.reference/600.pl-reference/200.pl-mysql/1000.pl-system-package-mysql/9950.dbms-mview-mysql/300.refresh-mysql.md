| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |
|machine-translation| en          |
|slug|refresh-mysql-compatible-mode|

# REFRESH

`REFRESH`は、指定されたマテリアライズドビューを更新するために使用します。

<main id="notice" >
  <h4>適用対象</h4>
  <p>この内容はOceanBaseデータベースEnterprise Editionにのみ適用されます。OceanBaseデータベースCommunity EditionはMySQLモードのみを提供します。</p>
</main>

## 構文

```sql
DBMS_MVIEW.REFRESH (
    mv_name                IN    VARCHAR(65535)
    [, method              IN    VARCHAR(65535) DEFAULT NULL]
    [, refresh_parallel    IN    INT DEFAULT 0]
    [, nested              IN    BOOLEAN DEFAULT FALSE]
    [, nested_refresh_mode IN    VARCHAR DEFAULT NULL]
    [, async               IN    BOOLEAN DEFAULT FALSE]
);
```

## パラメータの説明

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>ネストされていないマテリアライズドビューには連鎖更新の動作が存在しないため、<code>nested</code>および<code>nested_refresh_mode</code>パラメータを指定しても意味がありません。デフォルトでは独立更新されます。</p>
</main>

|       **パラメータ**      |       **説明**        |
|---------------------|-----------------------|
| mv_name             | 更新するマテリアライズドビューの名前。|
| method              | マテリアライズドビューの更新ポリシーを定義します。マテリアライズドビュー作成時にデフォルトのポリシーを指定できます。選択可能な方法は以下のとおりです：<ul><li>`F`または`f`：クイック更新。</li><li>`?`：強制更新。</li><li>`C`または`c`：フル更新。</li><li>`A`または`a`：常に更新。`C`と同じです。</li></ul>|
| refresh_parallel    | 更新操作の並列度、つまり同時に実行される更新操作のスレッド数。|
| nested              | ネストされたマテリアライズドビューの更新モードを指定します。取り得る値は以下のとおりです：<ul><li>`FALSE`：デフォルト値で、ネストされたマテリアライズドビューを独立して更新します。</li><li>`TRUE`：ネストされたマテリアライズドビューを連鎖更新します。</li></ul>|
| nested_refresh_mode | ネストされたマテリアライズドビューの連鎖更新を一貫性のある更新動作とするか、一貫性のない更新動作とするかを指定します。取り得る値は以下のとおりです：<ul><li>パラメータ`nested`を`FALSE`に指定した場合、パラメータ`nested_refresh_mode`を指定するとエラーが発生します。</li><li>パラメータ`nested`を`TRUE`に指定した場合：<ul><li>パラメータ`nested_refresh_mode`の取り得る値は以下のとおりです：<ul><li>`inconsistent`：連鎖非一貫性更新を示します。</li><li>`consistent`：連鎖一貫性更新を示します。</li></ul> </li><li>パラメータ`nested_refresh_mode`を指定しない場合、デフォルトで連鎖非一貫性更新となります。</li></ul> </li></ul>|
| async               | 非同期更新を行うかどうかを指定します。取り得る値は以下のとおりです：<ul><li>`FALSE`：デフォルト値で、更新タスク完了後に戻ります。</li><li>`TRUE`：`REFRESH`を呼び出した後、バックグラウンドで更新タスクを作成し、すぐに戻ります。</li></ul> <main id="notice" type='explain'><h4>説明</h4><p>V4.4.2バージョンでは、このパラメータはV4.4.2 BP2バージョンから導入されました。</p></main>|

## 例

* 独立更新モード：

  * パラメータ`mv_name`のみを指定します。

    ```shell
    obclient> CALL DBMS_MVIEW.REFRESH('mv1');
    ```

  * パラメータ`mv_name`とパラメータ`nested`の値を`false`に指定します。

    ```shell
    obclient> CALL DBMS_MVIEW.REFRESH(
        'mv1',
        nested=> false);
    ```

* ネストされた非一貫性リフレッシュモード：

  * パラメータ `mv_name` を指定し、パラメータ `nested` の値を `true` に設定しますが、パラメータ `nested_refresh_mode` を指定しません。

    ```shell
    obclient> CALL DBMS_MVIEW.REFRESH(
        'mv1',
        nested=> true);
    ```

  * パラメータ `mv_name` を指定し、パラメータ `nested` の値を `true` に設定するとともに、パラメータ `nested_refresh_mode` の値を `inconsistent` に設定します。

    ```shell
    obclient> CALL DBMS_MVIEW.REFRESH(
        'mv1',
        nested=> true,
        nested_refresh_mode => 'inconsistent');
    ```

* ネストされた一貫性リフレッシュモード：

  パラメータ `mv_name` を指定し、パラメータ `nested` の値を `true` に設定するとともに、パラメータ `nested_refresh_mode` の値を `consistent` に設定します。

  ```shell
  obclient> CALL DBMS_MVIEW.REFRESH(
      'mv1',
      nested=> true,
      nested_refresh_mode => 'consistent');
  ```

* 以下はエラーが発生するシナリオです：

  * パラメータ `nested` の値を `false` に設定し、かつパラメータ `nested_refresh_mode` の値を指定した場合、エラーが発生します。

    ```shell
    obclient> CALL DBMS_MVIEW.REFRESH(
        'mv1',
        nested=> false,
        nested_refresh_mode => 'xxxx');
    ```

  * パラメータ `nested` の値を指定せず、パラメータ `nested_refresh_mode` のみの値を指定した場合、エラーが発生します。

    ```shell
    obclient> CALL DBMS_MVIEW.REFRESH(
        'mv1',
        nested_refresh_mode => 'xxxx');
    ```

  * パラメータ `nested` の値を `true` に設定し、パラメータ `nested_refresh_mode` の値を無効な文字列に設定した場合、エラーが発生します。

    ```shell
    obclient> CALL DBMS_MVIEW.REFRESH(
        'mv1',
        nested=> true,
        nested_refresh_mode => '無効な文字列');
    ```

## 関連ドキュメント

マテリアライズドビューのリフレッシュに関する情報については、[マテリアライズドビューのリフレッシュ](../../../../300.database-object-management/100.manage-object-of-mysql-mode/600.manage-views-of-mysql-mode/200.manage-materialized-views-of-mysql-mode/400.refresh-materialized-views-of-mysql-mode.md)を参照してください。