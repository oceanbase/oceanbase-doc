| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |
|machine-translation| en          |
|slug|create_snapshot-mysql-mode|

# CREATE_SNAPSHOT

`CREATE_SNAPSHOT` プロシージャは、クラスタのスナップショット生成をトリガーするために使用されます。

  <main id="notice" >
    <h4>機能の適用範囲</h4>
    <p>このシステムパッケージは、sysテナントでの実行のみをサポートします。</p>
  </main>

## 構文

```sql
DBMS_WORKLOAD_REPOSITORY.CREATE_SNAPSHOT(
    flush_level  VARCHAR(64) DEFAULT 'TYPICAL'
);
```

## パラメータ説明

| **パラメータ** | **説明** |
| --- | --- |
| flush_level | スナップショットタイプを指定します。現在、主に統計データを収集するための定期スナップショットTYPICALのみがサポートされています。 |

## 使用方法

ユーザーが手動でスナップショットを作成すると、タイマーが再開始されます。例えば、デフォルトの INTERVAL が1時間の場合：

```shell
12:21 タイマーが発生し、スナップショットを作成
13:21 タイマーが発生し、次のスナップショットを作成
13:45 ユーザーが手動でスナップショットを作成
14:45 タイマーが発生し、次のスナップショットを作成
```

## 例

手動で新しいクラスタスナップショットを作成し、定期的にデータを収集します。

```shell
obclient(root@sys)[(none)]> CALL DBMS_WORKLOAD_REPOSITORY.CREATE_SNAPSHOT(flush_level => 'TYPICAL');
```

## 関連ドキュメント

WRにおけるスナップショットの詳細については、[スナップショットポリシーの設定](../../../../../700.reference/1000.performance-tuning-guide/400.performance-diagnosis/450.wr-management/200.manage-wr.md)を参照してください。