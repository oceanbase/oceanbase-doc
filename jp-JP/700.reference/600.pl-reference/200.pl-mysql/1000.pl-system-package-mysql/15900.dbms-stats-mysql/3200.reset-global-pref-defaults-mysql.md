| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |
|machine-translation| en          |
|slug|reset_global_pref_defaults-mysql-compatible-mode|

# RESET_GLOBAL_PREF_DEFAULTS

`RESET_GLOBAL_PREF_DEFAULTS` プロシージャは、グローバルPrefをデフォルト値に設定するために使用されます。

## 構文

```sql
DBMS_STATS.RESET_GLOBAL_PREF_DEFAULTS;
```

## 例

グローバルレベルのすべてのPrefデフォルト値を復元します。

```sql
obclient> CALL DBMS_STATS.RESET_GLOBAL_PREF_DEFAULTS();
Query OK, 0 rows affected
```