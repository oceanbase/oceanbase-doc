| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |
|machine-translation| en          |
|slug|manage_dynamic_partition-mysql-compatible-mode|

# MANAGE_DYNAMIC_PARTITION

`MANAGE_DYNAMIC_PARTITION`は、動的パーティション管理タスクを1回実行し、現在のテナント配下の動的パーティションテーブルに対して将来のパーティションを事前作成し、期限切れのパーティションを削除します。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>OceanBaseデータベースV4.3.5では、V4.3.5 BP2バージョンからサブプログラム<code>MANAGE_DYNAMIC_PARTITION</code>がサポートされています。</p>
</main>

## 構文

```sql
DBMS_PARTITION.MANAGE_DYNAMIC_PARTITION (
    precreate_time VARCHAR(64) DEFAULT NULL,
    time_unit VARCHAR(64) DEFAULT NULL);
```

## パラメータ説明

|      パラメータ      |   説明     |
|----------------|-----------|
| precreate_time | オプション値。`n {hour \| day \| week \| month \| year}`。<ul><li>デフォルト値は`null`で、事前作成時間を別途指定しないことを意味します。</li><li>明示的に指定された場合、動的パーティションテーブルのパーティションを事前作成する際に、指定された`precreate_time`とテーブル上の`precreate_time`のうち大きい方の値を事前作成時間とします。</li></ul>|
| time_unit      | オプション値。`null \| hour \| day \| week \| month \| year`。<ul><li>デフォルト値は`null`で、すべての動的パーティションテーブルに対して動的パーティション管理タスクを実行することを意味します。</li><li>値が`hour`の場合、`time_unit`が`hour`のテーブルに対してのみ動的パーティション管理タスクを実行します。</li><li>値が`day`の場合、`time_unit`が`day`のテーブルに対してのみ動的パーティション管理タスクを実行します。</li><li>値が`week`の場合、`time_unit`が`week`のテーブルに対してのみ動的パーティション管理タスクを実行します。</li><li>値が`month`の場合、`time_unit`が`month`のテーブルに対してのみ動的パーティション管理タスクを実行します。</li><li>値が`year`の場合、`time_unit`が`year`のテーブルに対してのみ動的パーティション管理タスクを実行します。</li></ul>|

## 例

```sql
CALL DBMS_PARTITION.MANAGE_DYNAMIC_PARTITION();
```

```sql
CALL DBMS_PARTITION.MANAGE_DYNAMIC_PARTITION('3DAY');
```

```sql
CALL DBMS_PARTITION.MANAGE_DYNAMIC_PARTITION('3DAY', 'HOUR');
```

## 関連ドキュメント

[動的パーティション管理タスク](../../../../300.database-object-management/100.manage-object-of-mysql-mode/300.manage-partitions-of-mysql-mode/260.dynamic-partition-of-mysql-mode/500.dynamic-partition-management-task-of-mysql-mode.md)