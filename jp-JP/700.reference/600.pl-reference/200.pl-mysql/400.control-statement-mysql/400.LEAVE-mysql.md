| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |
|machine-translation| en          |
|slug|leave-mysql|

# LEAVE

`LEAVE` ステートメントは、指定されたラベルを持つプロシージャ制御文から抜け出すために使用します。ラベルが最も外側のストアドプログラムブロックに対応する場合、LEAVE を使用するとそのプログラムを終了します。

`LEAVE` ステートメントの構文は次のとおりです：

```sql
LEAVE label
```

`LEAVE` ステートメントは、`BEGIN ... END` ブロック内、および `LOOP`、`REPEAT`、`WHILE` ループ文内で使用できます。