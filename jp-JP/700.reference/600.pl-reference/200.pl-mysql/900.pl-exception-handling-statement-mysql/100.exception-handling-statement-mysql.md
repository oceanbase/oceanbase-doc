| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |
|machine-translation| en          |
|slug|exception-handling-statement-mysql-compatible-mode|

# 例外処理の概要

ストアドプログラムの実行中には、現在のプログラムブロックを終了するなど、特別な状況が発生し、それに対処する必要が生じることがあります。ユーザーは、警告や例外などの状況に対処するハンドラーを定義したり、エラーコードを指定したりできます。

OceanBaseデータベースは、以下のPL例外処理ステートメントをサポートしています：

* `DECLARE ... CONDITION` ステートメントは、例外条件に名前を付けるために使用されます。詳細については、[DECLARE ... CONDITION](../900.pl-exception-handling-statement-mysql/200.declare-condition-mysql.md)を参照してください。

* `DECLARE ... HANDLER` ステートメントは、ハンドラーを宣言するために使用されます。詳細については、[DECLARE ... HANDLER](../900.pl-exception-handling-statement-mysql/300.declare-handler-mysql.md)を参照してください。

* `RESIGNAL` ステートメントは、例外処理ハンドラー内の例外情報を変更するために使用されます。詳細については、[RESIGNAL](../900.pl-exception-handling-statement-mysql/500.RESIGNAL-mysql.md)を参照してください。

* `SIGNAL` ステートメントは、例外条件を発生させるために使用されます。詳細については、[SIGNAL](../900.pl-exception-handling-statement-mysql/600.signal-mysql.md)を参照してください。

* `GET DIAGNOSTICS` ステートメントは、診断領域から情報を取得するために使用されます。詳細については、[GET DIAGNOSTICS](../900.pl-exception-handling-statement-mysql/400.get-diagnostics-mysql.md)を参照してください。

例外状況が発生した場合、サーバーがどのようにハンドラーを選択するかの情報については、[ハンドラーの有効範囲](../900.pl-exception-handling-statement-mysql/700.scope-rules-for-handlers-mysql.md)を参照してください。

診断領域の情報については、[診断領域](../900.pl-exception-handling-statement-mysql/800.diagnostic-area-mysql.md)を参照してください。