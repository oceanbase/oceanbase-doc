|description||
|---|---|
|machine-translation|ja-marker|
|slug|obcdc-parameters|

# obcdc構成パラメータの説明

構成パラメータのbool型設定では、0がFalseを、1がTrueを表します。

## クラスタ設定

### メタ情報の設定

|                 パラメータ                  |                意味                 |              デフォルト値              | 動的変更は可能か |                                               備考                                               |
|--------------------------------------|-----------------------------------|-------------------------------|--------|------------------------------------------------------------------------------------------------|
| init_log_level                       | 起動時のログレベル                           | ALL.\*:INFO;SHARE.SCHEMA:INFO | いいえ      | 起動時のログレベル。より詳細なログを出力するために使用され、起動後は log_level の設定に調整されます。                                                   |
| log_level                            | 起動後のログレベル                           | ALL.\*:INFO;SHARE.SCHEMA:WARN | はい      |      なし                                                                                          |
| max_log_file_count                   | ログファイルの最大数          | 0                  | いいえ      | 値の範囲は0以上です。値が0の場合、ログは無制限です。ログファイル数を減らすには、obcdcを再起動する必要があります。      |
| enable_log_limit                   | ログスロットリングを有効にするかどうか          | 1                  | はい      |  1：有効  0：無効     |
| rootserver_list                      | 起動時に rootservice が存在するマシン情報を指定          | `|`（英字半角）                  | はい      | フォーマット：`server_ip:server_rpc_port:server_sql_port`。                                                  |
| cluster_url                          | 起動時に指定する cluster url                | `|`（英字半角）                  | いいえ      | OceanBaseにcluster urlがある場合に使用します。obcdcはrootserver情報を取得するために使用され、サーバーの高可用性をサポートします。                          |
| cluster_user                         | sysテナント内のユーザー名                        | 空（ユーザー指定、必須）                    | いいえ      | sysテナント内のユーザーを設定する必要があります。このユーザーには内部テーブル（oceanbaseデータベース）の読み取り権限が付与されている必要があります。                                                           |
| cluster_password                     | 上記ユーザーのパスワード                           | 空（ユーザー指定、必須）                    | いいえ      |      なし                                                                                          |
| tenant_endpoint                     | テナントが存在するマシンのリスト                           | `|`（英字半角）                     | いいえ      | フォーマット：`host:sql_port`。複数のマシンを指定する場合は、`|`で区切ります。                                                                          |
| tenant_user                        | テナントのユーザー名                           | `|`（英字半角）                     | いいえ      | フォーマット：`user_name@tenant_name`または`user_name@tenant_name#cluster`。                                                                                 |
| tenant_password                     | 上記テナントのユーザー名に対応するパスワード                           | `|`（英字半角）                     | いいえ      |        なし                                                                                        |
| config_fpath                         | 設定ファイルのダンプパス                      | etc/liboblog.conf             | いいえ      | obcdcのすべての設定情報は、このファイルにダンプされます。                                                                    |
| timezone                             | obcdcが属するタイムゾーン                     | +8:00                         | いいえ      | データのフォーマット出力に影響を与えるタイムゾーンです。データソースの設定と一致させる必要があります。デフォルトは東部8区です。                                                             |
| timezone_info_path                             | タイムゾーンメタ情報ファイルのディレクトリを指定するために使用されます                     | etc/timezone_info.conf                         | はい      | `meta_data_refresh_mode`が`data_dict`に設定されている場合、RPMパッケージ内の`timezone_info.conf`ファイルをこのディレクトリに配置する必要があります。                                                             |
| skip_ob_version_COMPAT_check         | OceanBaseバージョンの互換性チェックをスキップするかどうか            | 0                             | いいえ      | obcdcバージョン3.xは、OceanBaseデータの同期を2.x/3.xバージョンでのみサポートしており、obcdcのバージョン番号はOceanBaseのバージョン番号よりも大きい必要があります（比較時はメジャーバージョンとマイナーバージョンを考慮します）。 |
| cluster_version_refresh_interval_sec | OceanBaseクラスタのOBServerノードの最小バージョン番号を照会する間隔 | 600                           | いいえ      | OceanBaseクラスタのバージョンをリフレッシュする間隔を指定します。単位は秒です。                                                                   |
| log_router_background_refresh_interval_sec | ログストリームメタ情報のリフレッシュ頻度 | 10   | はい     | ログストリームに対応するサーバー情報のバックグラウンドリフレッシュ時間間隔です。単位は秒です。       |

### 動作モード関連

|        パラメータ         |           意味           |           デフォルト値            | 動的変更は可能か |                                                              備考                                                               |
|--------------------|------------------------|--------------------------|--------|-------------------------------------------------------------------------------------------------------------------------------|
| working_mode       | 動作モード                   | storage（商用版） | いいえ      | 業務ではトランザクションのサイズをできるだけ制御してください。業務シナリオに大規模なトランザクションが存在する場合は、永続化モードの使用を推奨します。永続化モードでは、obcdcをSSDディスクにデプロイすることを推奨します。                                   |
| store_service_path | obcdc永続化モードでデータを保存するパス | ./storage                | いいえ      | 永続化モードでのみ有効です。 </br>相対パス：obcdcを呼び出すプロセスのパス配下の相対パスを指定します。 </br>絶対パス：store_service_path=/data/1 のように、指定した絶対パスに保存します。 |

### 同期粒度

obcdcは複数のデータを同期することができます。単一のobcdcインスタンスは、1つのクラスタ、1つまたは複数のテナント、複数のデータベース、複数のテーブルなどを同期することをサポートしています。

|   同期粒度   |       単一 obcdc       |   複数 obcdc   |
|----------|-------------------------|-----------------|
| クラスタ       | サポート（単一obcdcによる複数クラスタの同期はサポートされていません） | サポート              |
| テナント       | サポート                      | サポート              |
| データベース | サポートされていません                      | サポートされていません              |
| テーブル    | サポートされていません                      | サポートされていません（下記のマルチインスタンス構成パラメータを参照） |

テナントのトラフィックが非常に多く、単一インスタンスのobcdcではクラスタ全体をリアルタイムに同期することが困難な場合、複数のobcdcインスタンスを使用して同一クラスタを同期し、負荷を分散させることができます。各インスタンスは異なるテナントのデータを同期します。

#### アローリスト関連設定

|          パラメータ          |         意味         |   デフォルト値    | 動的変更は可能か |                           備考                           |
|-----------------------|--------------------|----------|--------|--------------------------------------------------------|
| enable_white_black_list         | obcdcのアローリストを有効にするかどうかを制御します           | 1 | いいえ      | デフォルトで有効であり、アローリストの設定が適用されます。無効にした後は、データを同期するテナント範囲がホワイトリストに設定されたテナントに限定されます。  |
| tb_white_list         | 同期するテーブルのホワイトリスト           | \*.\*.\* | いいえ      | 形式：テナント名.データベース名.テーブル名（例：mytenant1.db1.tb1\|mytenant2.db2.\*）。  |
| tb_black_list         | 同期するテーブルのブラックリスト           | `|`      | いいえ      | 上記と同じ形式です。                                                  |
| tablegroup_white_list | 同期するtablegroupのホワイトリスト | \*.\*    | いいえ      | 形式：テナント名.tablegroup名、例：(mytenant1.\*\|or_tenant\*.tg\*)。 |
| tablegroup_black_list | 同期するtablegroupのブラックリスト | `|`      | いいえ      | 上記と同じ形式です。                                                  |
| enable_filter_sys_tenant | sysテナントのデータ同期をフィルタリングするかどうか | 	0      | いいえ      | sysテナントが異常で通常のテナントが正常な場合に有効にします。                    |

  <main id="notice" type='explain'>
    <h4>説明</h4>
    <p>obcdcは、テーブルおよびtablegroupのアローリストに基づくデータフィルタリング機能を提供します：</p>
    <ul>
    <li>ホワイトリストに含まれ、かつブラックリストには含まれないテーブル / tablegroupのみが同期されます。ホワイトリストはデフォルトですべてのテナントのすべてのテーブル / tablegroupを対象とし、ブラックリストはデフォルトで空です。本番環境では、デフォルト値を使用せず（冗長なデータを取得するため）、ホワイトリストを使用して指定したテナントのデータを同期することを推奨します。</li>
    <li>設定方法はfnmatchによる正規表現マッチングです。デフォルトのマッチングルールは大文字小文字を区別しません。enable_oracle_mode_match_case_sensitive=1を設定した場合、ORACLEモードのテナントに対しては、名前のマッチングに大文字小文字を区別します。</li>
    </ul>
  </main>

#### マルチインスタンス関連設定

|                         パラメータ                         |                    意味                    | デフォルト値 | 動的変更は可能か |                                                  備考                                                  |
|-----------------------------------------------------|------------------------------------------|-----|--------|------------------------------------------------------------------------------------------------------|
| instance_num                                        | 流量を分散するobcdcの総数                        | 1   | いいえ      |    なし                                                                                                  |
| instance_index | 現在のobcdcが全体のインデックス（ハッシュバケットIDと理解できます） | 0 | いいえ | 値は \[0,instance_num) の範囲です。各obcdcは `hash(tenant_id, ls_id) % instance_num = instance_idx` のログストリームのデータのみを処理します。 |
| enable_global_unique_index_belong_to_multi_instance | マルチインスタンスシナリオでグローバル一意インデックステーブルを同期することを許可するかどうか                     | 0   | はい      |    値が0の場合、マルチインスタンスシナリオでグローバル一意インデックステーブルを同期することは許可されません。                                       |

## モニタリング関連の設定

### スロットリング

obcdcは、異常な状況によりメモリ内のデータが積み上がってOOMを引き起こすのを防ぐため、スロットリング機能を提供しています。obcdcの内部状態に基づいてスロットリングがトリガーされ、新しいデータの取得が一時停止されます。obcdcの状態が回復すると、データの取得が再開されます。スロットリング機能は以下のパラメータで制御され、必要に応じて対応するパラメータを調整して制限レベルを調整します。

|                     パラメータ                     |             意味             |  デフォルト値   | 動的変更は可能か |                              備考                               |
|---------------------------------------------|----------------------------|--------|--------|---------------------------------------------------------------|
| memory_limit                                | obcdcがスロットリングをトリガーした際に使用するメモリの上限      | `20G`  |  はい      | スロットリングの参考パラメータの一つです。obcdcのメモリ使用量を `memory_limit` の範囲内に抑えることを目標としています（obcdcは可能な限り制御します）。これは、obcdcが最大でも `memory_limit` 分のメモリしか使用しないことを意味するものではありません。最小構成は2Gです。 |
| memory_usage_warn_threshold | ログ取得のスロットリングをトリガーするメモリ使用率（`memory_limit` との比較） | 85 | はい | なし  |
| system_memory_avail_percentage_lower_bound | システム利用可能メモリの下限しきい値（割合）            | 10     |  はい      | 値の範囲：\[10, 80]。                                              |
| part_trans_task_active_count_upper_bound    | アクティブなパーティショントランザクション数の上限                | 200000 |  はい      | メモリ内のアクティブなパーティショントランザクション構造体の数（割り当てられており、回収されていないもの）。                                   |
| storager_task_count_upper_bound             | storagerモジュールで永続化待ちのタスク数のスロットリングしきい値 | 1000   |  はい      | 最小値は1です。                                                       |
| storager_mem_percentage | 永続化モジュールが占有する相対的なスロットリングメモリの倍数を制御する | 1   |  はい  |  値が0の場合は自動調整を意味します。  |
| part_trans_task_reusable_count_upper_bound  | 再利用待ちのパーティショントランザクション数の上限                 | 10240  |  はい      | ユーザーキュー待ち + ユーザー出力待ち + ユーザー返却待ち + GCされていないタスク数。                             |
| ready_to_seq_task_upper_bound               | シーケンス待ちタスク数の上限                 | 2000   |  はい      | シーケンス待ちタスク数の上限は、解析待ちタスク + フォーマット待ちタスク + 永続化待ちタスク数です。                             |
| pause_fetcher                               | ログ取得を強制的に一時停止                    | 0   |  はい      | 値が0の場合は、スロットリングの計算ロジックの結果を無視し、ログ取得を強制的に一時停止します。                                          |

スロットリングの参照条件：

1. 条件1：アクティブなパーティション数が上限しきい値（part_trans_task_active_count_upper_bound）を超える \|\| obcdcが保持するメモリが上限しきい値（memory_limit）を超える \|\| システム利用可能メモリが下限しきい値（system_memory_avail_percentage_lower_bound）を下回る。
2. 条件2：再利用待ちパーティショントランザクション構造体数が上限しきい値（part_trans_task_reusable_count_upper_bound)を超える \|\| シーケンス待ちタスク数が上限しきい値（ready_to_seq_task_upper_bound）を超える。
3. 条件3（3.x以降）：永続化待ちタスク数がしきい値（storager_task_count_upper_bound）を超える \&\& obcdcが保持するメモリがスロットリングトリガーメモリ（memory_limit）を超える \* 永続化モジュールが占有する相対的なスロットリングメモリの比率（storager_mem_percentage）。

スロットリングをトリガーする条件：((条件1 \&\& 条件2)\|\| 条件3) \|\| (pause_fetcher != 0)。

### モニタリング情報の出力

|                   パラメータ                   |                 意味                 |   デフォルト値   | 動的変更は可能か |          備考           |
|-----------------------------------------|------------------------------------|---------|--------|-----------------------|
| enable_dump_pending_trans_info          | obcdcが保持する分散トランザクションの状態を定期的に集計・出力するかどうかを制御する | 0       |  はい      | 値が0の場合は、obcdcが保持する分散トランザクションの状態を不定期に集計・出力します。`1` に設定した場合は10秒ごとに出力します。 |
| print_fetcher_slowest_ls_num          | ログ取得が最も遅いパーティションの数を出力する                      | 10      |  はい      |     なし                  |
| print_rpc_handle_info                   | RPC関連情報を出力するかどうかを制御する                      | 0       |  はい      |  値が0の場合はRPC関連情報を出力しません。                     |
| print_stream_dispatch_info              | ログ取得ストリーム情報を出力するかどうかを制御する                         | 0       |  はい      |   値が0の場合はログ取得ストリーム情報を出力しません。                    |
| print_ls_heartbeat_info          | パーティションハートビート情報を出力するかどうかを制御する                         | 0       |  はい      |   値が0の場合はパーティションハートビート情報を出力しません。                    |
| print_ls_serve_info              | パーティションサービス情報（パーティション状態、パーティショントランザクション数）を出力するかどうかを制御する            | 0       |  はい      |   値が0の場合はパーティションサービス情報（パーティション状態、パーティショントランザクション数）を出力しません。                    |
| print_participant_not_serve_info        | すべての分散トランザクションの非サービス（データを出力しない）パーティショントランザクション情報を出力するかどうかを制御する      | 0       |  はい      |   値が0の場合は、すべての分散トランザクションの非サービス（データを出力しない）パーティショントランザクション情報を出力しません。                   |
| print_ls_server_list_update_info | パーティションserverリストを更新する際にserverリストの詳細情報を出力するかどうかを制御する | 0       |  はい      |    値が0の場合は、パーティションserverリストを更新しない場合にserverリストの詳細情報を出力しません。                   |
| enableformatter_print_log              | formatterが一部のフォーマットを無視したデータを出力する際のログを許可するかどうかを制御する     | 0       |  はい      |   値が0の場合は、formatterが一部のフォーマットを無視したデータを出力する際のログを出力することを許可しません。                    |

## 機能制御関連のパラメータ

| パラメータ | 説明 | デフォルト値 | 動的変更は可能か | 備考 |
|------------|-----------|-----|--------|-------------------------|
| enable_output_virtual generado_column | 仮想生成列を出力するかどうかを制御します。 | 0 | いいえ | `enable_output_by_table_def`と併用してください。<main id="notice" type='explain'><h4>説明</h4><p>この構成パラメータは、V4.2.5バージョンから導入されました。つまり、OceanBaseデータベースがV4.2.5以降のV4.2.xシリーズバージョンの場合、CDCは仮想生成列の出力をサポートします。</p></main>  |

## メタデータ管理

obcdc 4.1バージョンでは、データディクショナリを通じてメタ情報（スキーマ/ログストリーム情報など）を取得できるため、OBServerノードにRPCを送信してログをプルする必要はありません。

| パラメータ | 説明 | デフォルト値 | 動的変更は可能か | 備考 |
|------------|-----------|-----|--------|-------------------------|
| meta_data_refresh_mode                  | スキーマ/ログストリームなどのメタ情報の取得方法を指定するために使用されます               | online   | いいえ      | <ul><li>onlineモード：SchemaServiceを介してserverにスキーマ情報をクエリします。OBServerノードへのアクセスが必要です。</li><li>data_dictモード：ログ内のデータディクショナリを解析してスキーマ情報を取得します。</li></ul><main id="notice" type='notice'><h4>注意</h4><p>obcdcがOceanBaseデータベースV4.2以降のバージョンのデータを同期する際、ユーザー設定を無視し、<code>data_dict</code>モードを強制的に使用します。<code>online</code>モードを使用する場合は、<code>skip_ob_version_COMPAT_check</code>パラメータを<code>1</code>に追加で変更する必要があります。</p></main> |

### テナント管理

|                   パラメータ                   |                説明                 | デフォルト値 | 動的変更は可能か |                                                                                 備考                                                                                 |
|-----------------------------------------|-----------------------------------|-----|--------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| skip_rename_tenant_ddl                  | テナント名の変更時のDDLを無視するかどうか               | 0   | はい      | テナント名が変更されたテナントがobcdcの同期ホワイトリストの範囲内にない場合は影響ありません。そうでない場合は、通常エラーが発生しプロセスが終了します（この場合、rename操作によりホワイトリストの制限が破られるためです）。この構成パラメータをtrueに設定すると、renameされたテナントのDDLをスキップしてデータの同期を続行します。 |
| enable_oracle_mode_match_case_sensitive | OceanBase-Oracleモードのテナントのホワイトリスト/ブラックリスト照合時の大文字小文字の区別 | 0   | いいえ      |                                          なし                                                                                                                          |

## Fetcherモジュール関連の設定

|               パラメータ                |         意味         |   デフォルト値   | 動的変更は可能か |       備考       |
|----------------------------------|--------------------|---------|--------|----------------|
| ls_count_upper_limit      | パーティション進捗数            | 2000000 | いいえ      | 最大で2億パーティションをサポートします。 |
| check_switch_server_interval_min | 定期的にパーティションのログ引き抜きマシン切り替えが必要かどうかをチェックする間隔 | 30      | はい      |   なし             |

### SQLクエリ関連の設定

|                パラメータ                |                  説明                  | デフォルト値 | 動的変更は可能か |         備考         |
|-----------------------------------|--------------------------------------|-----|--------|--------------------|
| access_systable_helper_thread_num | SQL実行用接続プールの接続数の上限                   | 64  | いいえ      | 値の範囲 \[48, 1024]。 |
| rs_sql_connect_timeout_sec         | root serviceのSQL接続タイムアウト時間             | 40  | はい      |  なし                  |
| rs_sql_query_timeout_sec           | root serviceのSQLリクエストタイムアウト時間                           | 30  | はい      |     なし               |
| tenant_sql_connect_timeout_sec           | テナントのSQL接続タイムアウト時間               | 40  | はい      |      なし              |
| tenant_sql_query_timeout_sec           | テナントのSQLリクエストタイムアウト時間                           | 30  | はい      |    なし                |
| sql_server_change_interval_sec    | SQLリクエストが実行マシンを切り替える周期                      | 60  | はい      |   なし                 |

### cluster_idに基づくデータのフィルタリング

obcdcは、cluster_idに基づくブラックリストによるデータフィルタリング機能を提供します。OceanBaseは有効なcluster_idの範囲を定義していますが、不要なデータへの同期を防ぐため、ユーザーはデータ書き込み時にcluster_idを指定できます。同期元側で書き込まれるデータのcluster_idを設定するには、[ob_org_cluster_id](../../../../800.configuration-items-and-system-variables/200.system-variable/400.session-system-variable/8200.ob_org_cluster_id-session.md) の説明を参照してください。

* OceanBaseとOMSが定義する有効なcluster_id範囲（有効範囲は \[1, 4294901759\] U \[4294901760, 4294967295\]）に該当しないデータは同期しません。
* cluster_idがcluster_id_black_listに含まれるデータは同期しません。
* cluster_id_black_list内の各cluster_idは、cluster_id_value_minとcluster_id_value_maxの間の値である必要があります。

|            パラメータ             |          意味          |    デフォルト値     | 動的変更は可能か |                                      備考                                      |
|----------------------------|----------------------|------------|--------|------------------------------------------------------------------------------|
| cluster_id_black_list      | 同期対象のcluster_idのブラックリスト  | `|`| いいえ      | 複数のcluster_idは半角文字`|`で区切り、各cluster_idはcluster_id_value_minとcluster_id_value_maxの間の値である必要があります。 |
| cluster_id_black_value_min | 同期対象のcluster_idブラックリストの下限 | 2147473648 | いいえ      |   なし                                                                           |
| cluster_id_black_value_max | 同期対象のcluster_idブラックリストの上限 | 2147483647 | いいえ      |    なし                                                                          |

### サーバーレベルのブラックリスト

obcdcはOBServerノードにSQLおよびRPCを送信します。SQL実行マシンとRPC実行マシン（すべてのリクエストに有効）の一部をブロックするためのパラメータを設定できます。

さらに、obcdcのログプルはパーティションレベルで行われるため、パーティションレベルのログプルブラックリストが組み込まれています。パーティションログの取得にタイムアウトやエラーが発生したマシンは、このパーティションのログプルマシンブラックリストに追加されます。マシンのブラックリストへの追加およびブラックリストからの除外は自動的に行われます。

|         パラメータ          |              意味               | デフォルト値 | 動的変更は可能か |                                                    備考                                                     |
|----------------------|-------------------------------|-----|--------|-----------------------------------------------------------------------------------------------------------|
| server_blacklist     | RPCリクエストのマシンブラックリストを設定します。obcdcはこのブラックリストに含まれるマシンにRPCリクエストを送信しません  | `|` | はい      | `svr_ip:rpc_port` の形式で、複数のOBServerは `|` で区切ります。このOBServerリストに含まれるマシンからはログを取得しません。OBServerノードが正常にログサービスを提供できない場合（ネットワーク分断や遅延など）、手動でこのリストに追加できます。 |
| sql_server_blacklist | SQLリクエストのマシンブラックリストを設定します。obcdcはこのブラックリストに含まれるマシンにSQLリクエストを送信しません | `|` | はい      | `svr_ip:rpc_port` の形式で、複数のOBServerは `|` で区切ります。                                           |

### パーティションレベルのログプルサーバーブラックリスト調整（通常は変更不要）

|                    パラメータ                     |           説明            | デフォルト値 | 動的変更は可能か |   備考    |
|--------------------------------------------|-------------------------|-----|--------|---------|
| blacklist_survival_time_sec                | マシンがブラックリストにいる待機時間            | 30  | いいえ      |   なし      |
| blacklist_survival_time_upper_limit_min    | マシンのブラックリスト滞在時間の上限            | 4   | はい      |  なし       |
| blacklist_survival_time Penalty_period_min | マシンが繰り返しブラックリストに追加された場合、ブラックリスト時間が倍増する間隔のしきい値 | 1   | はい      |    なし     |
| blacklist_history_overdue_time_min         | マシンのブラックリスト履歴がどの程度経過したら削除するか          | 30  | はい      | 最小値は10 |
| blacklist_history_clear_interval_min       | ブラックリスト内のマシン情報をクリーンアップする周期           | 20  | はい      | 最小値は10 |

### サーバーのキャッシュ情報関連設定

|                 パラメータ                  |      意味       | デフォルト値 | 動的変更は可能か |
|--------------------------------------|---------------|-----|--------|
| all_server_cache_update_interval_sec | マシンリスト情報キャッシュの更新間隔  | 5   | はい      |
| all_zone_cache_update_interval_sec   | Zone情報キャッシュの更新間隔 | 5   | はい      |

### ログ取得関連の設定

|                        パラメータ                        |                        説明                        | デフォルト値  | 動的変更は可能か |                                                     備考                                                      |
|---------------------------------------------------|--------------------------------------------------|------|--------|-------------------------------------------------------------------------------------------------------------|
| idle_pool_thread_num                              | 待発行パーティションのログ取得タスクを処理するスレッド数                                | 4    | いいえ      | 値の範囲 \[1,32\]。                                                                                              |
| dead_pool_thread_num                              | 回収パーティションのログ取得タスクを処理するスレッド数                                 | 1    | いいえ      | 値の範囲 \[1,32\]。                                                                                              |
| stream_worker_thread_num                          | パーティションのログ取得タスクを割り当てるスレッド数                                    | 8    | いいえ      | 値の範囲 \[1,64\]。                                                                                              |
| start_lsn_locator_thread_num                   | パーティションログの開始点を特定するスレッド数                                     | 4    | いいえ      | 値の範囲 \[1,32\]。                                                                                              |
| lob_data_merger_thread_num                   | lobデータ処理スレッド数                                     | 2    | いいえ      | 値の範囲 \[1, +∞)。                                                                                              |
| start_lsn_locator_locate_count                    | ログ開始点特定タスクを完了したリクエスト数                                | 1    | いいえ      | 最小値は1です。パーティションの開始ログIDを特定するには、指定された数のパーティションレプリカが同じ開始ログIDを返す必要があります。                                                                                            |
| skip_start_lsn_locator_result_consistent_check | 開始ログIDの一貫性チェックをスキップ                                  | いいえ    | いいえ      |    なし                                                                                                         |
| svr_stream_cached_count                           | マシンログストリームオブジェクトプールの初期サイズ                                    | 16   | いいえ      | 最小値は1です。                                                                                                     |
| fetch_stream_cached_count                         | パーティションログストリームオブジェクトプールの初期サイズ                                    | 16   | いいえ      | 最小値は1です。                                                                                                     |
| start_lsn_locator_rpc_timeout_sec              | パーティションの開始ログIDを特定するRPCのタイムアウト時間                           | 60   | はい      | 最小値は1です。                                                                                                     |
| start_lsn_locator_batch_count                  | パーティションの開始ログIDを特定するRPCのバッチサイズ                           | 2000 | はい      | 最小値は1です。                                                                                                     |
| heartbeat_interval_sec                            | パーティションハートビートRPCの周期                                      | 1    | はい      | 最小値は1です。                                                                                                     |
| fetch_log_rpc_timeout_sec                         | パーティションログ取得RPCのタイムアウト時間                                   | 15   | はい      | 最小値は1です。                                                                                                     |
| progress_limit_sec_for_dml                        | DMLデータログ取得進捗の上限                             | 300    | はい      | 最小値は1です。 各パーティションの進捗が概ね一致するように保証するため（一部のパーティションでログ取得が速すぎるのを防ぐため）、サーバーが提供できるログの上限をすべてのパーティションの進捗の最小値+progress_limit_sec_for_dmlに制限します。 |
| ls_fetch_progress_update_timeout_sec                             | パーティションログ取得RPCタイムアウトのしきい値                                | 3    | はい      | 最小値は1です。                                                                                                     |
| ls_fetch_progress_update_timeout_sec_for_lagged_replica          | 遅延レプリカのログ取得タイムアウトしきい値                                    | 15   | はい      | 最小値は1です。                                                                                                     |
| enable_continue_use_cache_server_list          | Cacheを使用するサーバー情報を使用するかどうか                                    | 0   | いいえ     | sysテナントが異常であるが通常のテナントが正常な場合に有効にします。                                                                                                     |
| fetching_log_mode          | ログ取得モードを指定するために使用します                                    | integrated   | いいえ     | <ul><li>integratedモード：デフォルトのログ取得モードで、OBServerノードにRPCを送信してログを取得します。ユーザーはOBServerノードのアドレス(cluter_urlまたはrootserver_list)およびシステムテナント内のユーザー名(cluster_user)とパスワード(cluster_password)を設定する必要があります。</li><li>directモード：アーカイブログを直接消費します。OBServerノードにRPCを送信してログを取得する必要はありません。ユーザーはアーカイブの宛先(nfsパス/oss情報など)を設定する必要があります。アーカイブの宛先については、[ログアーカイブの概要](../../../../../600.manage/600.backup-and-recovery/300.log-archive/100.overview-of-log-archive.md)を参照してください。obcdcが配置されているマシンは、アーカイブの宛先に直接アクセスできる必要があります。</li></ul> <main id="notice" type='notice'> <h4>注意</h4> <p>obcdcは、連続していないアーカイブログの取得をサポートしていません。</p></main>          |

#### ログプル優先順位関連

|  パラメータ   |         意味         |      デフォルト値       | 動的変更は可能か |                        備考                         |
|--------|--------------------|----------------|--------|---------------------------------------------------|
| region | obcdcがログを優先的にプルするRegionを設定します | default_region | はい | RegionはOceanBaseデータベースの内部テーブルから取得する必要があります。obcdcは、設定されたRegion内のOBServerノードから優先的にログをプルします。 |

#### RPCモジュール設定（変更は推奨されません）

|                    パラメータ                    |            意味             | デフォルト値 | 値の範囲 | 動的変更は可能か |
|-------------------------------------------|---------------------------|-----|--------|----------------------------------------|
| io_thread_num                             | RPC I/Oスレッド数               | 4  | [1, +∞)  | いいえ      |
| rpc_result_cached_count                   | ログプルRPC結果オブジェクトプールの初期サイズ         | 16  | [1, +∞) | いいえ      |
| rpc_process_handler_time_upper_limit_msec | RPC処理の最大時間                | 200 | [1, +∞) | はい      |
| rpc_result_count_per_rpc_upper_limit      | 単一ログプルストリームで未処理のログプルRPC結果数の上限。この上限に達するとログプルRPCが一時停止されます | 16 | [1, +∞)  | はい      |

#### セキュリティ通信関連

|            パラメータ            |    意味    | デフォルト値  | 動的変更は可能か |                                                        備考                                                        |
|---------------------------|----------|------|--------|------------------------------------------------------------------------------------------------------------------|
| ssl_client_authentication | セキュリティ通信を有効にするかどうか | 0    | いいえ      | デフォルトの0はセキュリティ通信を無効にすることを意味します。                                                                                                       |
| ssl_external_kms_info     | セキュリティ通信方式   | file | いいえ      | 外部使用ではファイルモードのみをサポートしています。キーファイルはobcdc起動ディレクトリ配下のwalletディレクトリに配置します（walletディレクトリはご自身で作成してください）。その下に以下のキーファイルを配置します：ca.pem/client-cert.pem/client-key.pem。 |

## Sequencerモジュール関連のパラメータ

|          パラメータ           |      意味      |  デフォルト値 | 値の範囲  | 動的変更は可能か |
|------------------------|--------------|--------|--------|---------|
| sequencer_thread_num   | 分散トランザクションシーケンサースレッドプールサイズ | 5    | [1, +∞)      | いいえ      |
| sequencer_queue_length | シーケンス待ちのパーティショントランザクションキュー   | 102400   | [1, +∞)  | いいえ      |

## REDO読み取り制御モジュール

|          パラメータ           |      意味      |  デフォルト値  | 値の範囲 | 動的変更は可能か |
|------------------------|--------------|--------|--------|---------|
| redo_dispatched_memory_limit_exceed_ratio   | enable_output_trans_order_by_sql_operation=1の場合、処理対象となるredoのサイズ制限を設定します（メモリ内で処理可能なredoのサイズは、redo_dispatched_memory_limit_exceed_ratio × redodispatcher_memory_limitとなります）。 | 2    |  [1, +∞)   | はい      |
| redodispatcher_memory_limit | 処理対象となるredoのサイズ制限を設定します   | 512M   |  [128M, +∞) | はい     |
| extra_redo_dispatch_memory_size | データ偏りシナリオにおいて、偏りがある参加者に追加で割り当てるREDO残高です。デッドロックによりobcdcがデータを出力しなくなるのを防ぎます   | 4M  | [0M, 512M] | はい      |

## 行データソートモジュール

|          パラメータ           |      意味      |  デフォルト値   | 値の範囲 | 動的変更は可能か |
|------------------------|--------------|--------|--------|---------|
| msg_sorter_task_count_upper_limit   | トランザクション内行データソートモジュールタスク数上限 | 200000    |  [1, +∞)    | 不可     |
| msg_sorter_thread_num | トランザクション内行データソートモジュールスレッド数   | 1  | [1, 32] | 不可      |

## 永続化モードで有効な設定

### Storagerモジュール関連設定

|          パラメータ          |     説明      |  デフォルト値   | 動的変更は可能か |
|-----------------------|-------------|--------|--------|
| storager_thread_num   | 永続化モジュールのスレッドプールサイズ | 10     | 不可      |
| storager_queue_length | 永続化待ちのタスク数   | 102400 | 不可      |
| rocksdb_flush_interval | 主動FLUSH ROCKSDBの間隔 | デフォルト10分 | はい |
| rocksdb_compact_interval | 主動COMPACT ROCKSDBの間隔 | デフォルト6時間  | はい |

### Data Processorモジュール関連設定

|             パラメータ             |       説明        |  デフォルト値   | 動的変更は可能か |
|-----------------------------|-----------------|--------|--------|
| reader_thread_num   | 永続化モジュールからデータを読むスレッドプールサイズ | 10     | 不可      |
| reader_queue_length | データ逆シリアライズ待ちのタスク数   | 102400 | 不可      |

### REDOバッチ永続化関連設定

|             パラメータ             |       説明        |  デフォルト値  | 値の範囲  | 動的変更は可能か |
|-----------------------------|-----------------|--------|--------|----|
| batch_buf_count   | redoバッチ永続化のメモリbuffer数 | 10  | [5, +∞)     | 不可      |
| batch_buf_size   | redoバッチ永続化の集約bufferサイズ | 20MB     | [2MB, +∞)   | 不可   |

## Parserモジュール関連の設定

|             パラメータ              |        意味         | デフォルト値 | 動的変更は可能か |                                備考                                |
|------------------------------|-------------------|-----|--------|------------------------------------------------------------------|
| dml_parser_thread_num        | DML文を解析するスレッドプールのサイズ  | 5   | いいえ      | 最小値は1です。                                                          |
| ddl_parser_thread_num        | DDL文を解析するスレッドプールのサイズ  | 1   | いいえ      | 最小値は1です。                                                          |
| skip_reversed_schema_version | スキーマバージョンの逆戻り異常を無視する | 0   | いいえ      | DDL処理は通常、スキーマが単調に上昇するプロセスです。スキーマバージョンが逆戻りした場合は異常が発生していることを示します。この設定を使用すると、その異常をスキップできます。 |

## Formatterモジュール関連の設定

|         パラメータ          |     意味     | デフォルト値 | 動的変更は可能か |                                                                                            備考                                                                                            |
|----------------------|------------|-----|--------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| formatter_thread_num | データフォーマットスレッドプールサイズ | 10  | いいえ      | 最小値は1です。                                                                                                                                                                                  |
| skip_dirty_data      | 異常データのスキップ     | 0   | はい      | 現在の異常状況：<ol><li> 非全列ログ（CLOGに変更された列の情報のみが記録される場合。パラメータ：binlog_row_image）。</li><li>異常な物理バックアップによるテナントのDDL復元。</li></ol>    |

## Binlogレコード出力関連の設定

|                    パラメータ                     |               説明               | デフォルト値 | 動的変更は可能か |                                  備考                                   |
|--------------------------------------------|--------------------------------|-----|--------|-----------------------------------------------------------------------|
| enable_output_trans_order_by_sql_operation | トランザクション内の行データをSQL実行順に出力するかどうか          | 0   | いいえ      | OceanBase内部でデータが変更される順序を指します。クライアントから送信されたSQLの順序と完全に一致しない場合があります。                       |
| sort_trans_participants                    | 分散トランザクションの参加者別にソートする                  | 1   | いいえ      | パーティションキーでソートします。enable_output_trans_order_by_sql_operationと組み合わせることで、安定したステートメント出力順序を得られます。 |
| enable_output_hidden_primary_key           | 主キーのないテーブルの隠れた主キーを出力するかどうか                  | 0   | いいえ      | 主キーのないテーブルで隠れた主キーを出力するには、enable_output_hidden_primary_key=1を設定します。           |
| enable_convert_timestamp_to_unix_timestamp | timestampタイムスタンプをUNIX整数時間に変換するかどうか | 0   | いいえ      |   なし                                                                    |
| enable_output_invisible_column             | 隠れた列を出力するかどうか                        | 0   | いいえ      | なし                                                                      |
| enable_output_by_table_def | ユーザー定義の列順序で列を出力するかどうかを制御する | 0 | いいえ | なし |
| output_heartbeat_interval_msec              | 安全ポイント情報を出力する間隔                    | 1000   | はい      |    なし                                                                   |

## リソース回収関連の設定（変更は推奨されません）

|                 パラメータ                  |            意味            | デフォルト値 | 動的変更は可能か |                 備考                  |
|--------------------------------------|--------------------------|-----|--------|-------------------------------------|
| resource_collector_thread_num        | リソース回収スレッドプールサイズ                | 10  | いいえ      | 最小値は1です。                             |
| resource_collector_thread_num_for_br | binlogレコード回収用スレッドプールサイズ | 7   | いいえ      | resource_collector_thread_numより大きくする必要があります。 |

  <main id="notice" type='notice'>
    <h4>注意</h4>
    <p>この構成パラメータの説明ドキュメントに記載されていない設定は、変更を推奨しません。</p>
  </main>

## 緊急時の設定

OceanBaseデータベースの異常状態下でもobcdcが正常に動作するようにするための緊急設定です。

|                 パラメータ                  |            説明            | デフォルト値  |  値の範囲 | 動的変更は可能か |                 備考                  |
|--------------------------------------|--------------------------|-----|--------|-------------------------------------|---|
| lsn_black_list | 特定のLSNを持つログを無視するかどうかを制御します | `|` | 対象外 | いいえ  | 緊急時に使用します。  |
| skip_delete_tablet_op | obcdc内でtablet削除操作を無視するかどうかを制御します | 0 | {0, 1} | はい | 緊急時に使用します。一部のダイレクトロードやオフラインDDL処理では、obcdcのパーサーモジュールが`ENTRY_NOT_EXIST`エラーを返す場合があります。この緊急設定を有効にすることで、そのエラーをスキップできます。この設定はV4.2.1 BP2以降で利用可能です。 |
| skip_empty_outrow_lob_update | obcdcが空のoutrow LOBに関する例外を無視するかどうかを制御します | 0 | {0, 1} | はい | 緊急時に使用します。 |