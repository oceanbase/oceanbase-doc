|description||
|---|---|
|machine-translation|ja-marker|

# oblogminerのインストール

本記事では、oblogminerをインストールする2つの方法を紹介します：YUMコマンドによるインストールとインストールパッケージによるインストールです。実際の状況に応じて、どちらかの方法を選択してインストールできます。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>本記事では、Community Editionのoblogminerのインストール方法を紹介しています。Enterprise Editionのoblogminerは、Enterprise Edition OceanBaseデータベースをデプロイする際に自動的にインストールされます（<code>oceanbase/bin</code> ディレクトリ内）。 </p>
</main>

## YUMコマンドによるインストール

YUMコマンドを使用してOceanBaseのツールパッケージ(OceanBase Utils)をインストールすることで、oblogminerのインストールを完了できます。YUMコマンドによるインストールでは、oblogminerは`/usr/bin/`ディレクトリにインストールされます。

1. リポジトリを追加します。

   ```shell
   [admin@test001 ~]$ sudo yum-config-manager --add-repo https://mirrors.aliyun.com/oceanbase/OceanBase.repo
   ```

2. OceanBase Utilsをインストールします。

   ```shell
   [admin@test001 ~]$ sudo yum install oceanbase-ce-utils
   ```

3. oblogminerのヘルプを確認します。

   インストールが成功したら、以下のコマンドを実行してoblogminerのヘルプを確認できます。

   ```shell
   [admin@test001 ~]$ oblogminer -h
   ```

## OceanBase Utilsインストールパッケージによるインストール

OceanBaseのツールパッケージ(OceanBase Utils)をインストールすることで、oblogminerのインストールを完了できます。

1. [OceanBaseソフトウェアダウンロードセンター](https://en.oceanbase.com/softwarecenter)にアクセスし、OceanBase Utilsを検索して、必要なバージョンをダウンロードします。

2. OceanBase Utilsのディレクトリで、以下のコマンドを実行してOceanBase Utilsをインストールします。

   ```shell
   [admin@test001 ~]$ sudo rpm -ivh oceanbase-ce-utils-<version>.rpm
   ```

   oblogminerのみ使用する場合は、以下のrpm2cpioコマンドを実行してoblogminerを取得できます。

   ```shell
   [admin@test001 ~]$ rpm2cpio oceanbase-ce-utils-<version> | cpio -idmv ./usr/bin/oblogminer
   ```

   rpm2cpioコマンドを使用する場合、デプロイが完了したら、以下のコマンドを実行して`oblogminer`を`/usr/local/bin`ディレクトリにコピーできます。

   ```shell
   [admin@test001 ~]$ sudo cp usr/bin/oblogminer /usr/local/bin
   ```

3. oblogminerのヘルプを確認します。

   インストールが成功したら、以下のコマンドを実行してoblogminerのヘルプを確認できます。

   ```shell
   [admin@test001 ~]$ oblogminer -h
   ```

## ソースコードからコンパイルする

OceanBaseデータベースのソースコードから独自にコンパイルすることもできます。

1. OceanBaseデータベースのオープンソースコードをローカルにクローンします。

   ```shell
   [admin@test001 ~]$ git clone https://github.com/oceanbase/oceanbase.git
   ```

2. コードディレクトリで、4.2.xブランチに切り替えます。

   ```shell
   [admin@test001 oceanbase]$ git checkout 4.2.x
   ```

3. 必要に応じて、コンパイルモードを選択してoblogminerをコンパイルします。

   * デバッグモード

     build.shスクリプトを実行し、デバッグモードをdebugモードに指定します。

     ```shell
     [admin@test001 oceanbase]$ bash build.sh debug --init
     ```

     コンパイルディレクトリに移動します。

     ```shell
     [admin@test001 oceanbase]$ cd build_debug
     ```

     oblogminerをコンパイルします。

     ```shell
     [admin@test001 build_debug]$ make oblogminer
     ```

     コンパイル結果を`/usr/local/bin`ディレクトリにコピーします。

     ```shell
     [admin@test001 build_debug]$ sudo cp src/logservice/logminer/oblogminer /usr/local/bin
     ```

     oblogminerのコンパイル結果は、デフォルトで`<DEBUG_BUILD_DIR>/src/logservice/logminer`ディレクトリに格納されます。ここで、`<DEBUG_BUILD_DIR>`はコンパイルディレクトリを表し、デバッグモードでは`build_debug`がデフォルトです。

   * リリースモード

     build.shスクリプトを実行し、デバッグモードをreleaseモードに指定します。

     ```shell
     [admin@test001 oceanbase]$ bash build.sh release --init
     ```

     コンパイルディレクトリに移動します。

     ```shell
     [admin@test001 oceanbase]$ cd build_release
     ```

     oblogminerをコンパイルします。

     ```shell
     [admin@test001 build_release]$ make oblogminer
     ```

     コンパイル結果を`/usr/local/bin`ディレクトリにコピーします。

     ```shell
     [admin@test001 build_release]$ sudo cp src/logservice/logminer/oblogminer /usr/local/bin
     ```

     oblogminerのコンパイル結果は、デフォルトで`<DEBUG_BUILD_DIR>/src/logservice/logminer`ディレクトリに格納されます。ここで、`<DEBUG_BUILD_DIR>`はコンパイルディレクトリを表し、リリースモードでは`build_release`がデフォルトです。

4. oblogminerのヘルプ情報を確認します。

   コンパイルが成功した後、以下のコマンドを実行してoblogminerのヘルプ情報を確認できます。

   ```shell
   [admin@test001 ~]$ oblogminer -h
   ```

## FAQ

oblogminerを使用する際、動的ライブラリ`libmariadb.so.3`が見つからない場合、以下のエラーが発生します：

```shell
oblogminer: error while loading shared libraries: libmariadb.so.3: cannot open shared object file: No such file or directory
```

OceanBaseデータベースには`libmariadb.so.3`が標準搭載されています。findコマンドを使用して`libmariadb.so.3`のパスを特定し、環境変数に動的ライブラリの検索パスを指定できます。具体的な操作手順は以下のとおりです：

1. findコマンドを実行して`libmariadb.so.3`のパスを特定します。

   ```shell
   [admin@test001 ~]$ find / -name libmariadb.so.3
   ```

   環境に動的ライブラリ`libmariadb.so.3`が存在する場合、そのパスが出力されます。例：

   ```shell
   /home/admin/observer/lib/libmariadb.so.3
   ```

2. 環境変数`LD_LIBRARY_PATH`に動的ライブラリの検索パスを指定します。

   ```shell
   [admin@test001 ~]$ export LD_LIBRARY_PATH=/home/admin/observer/lib
   ```

操作を完了すると、oblogminerのコマンドを使用できます。oblogminerのコマンドの詳細については、[コマンドライン引数の紹介](300.command-line-parameters.md)を参照してください。
