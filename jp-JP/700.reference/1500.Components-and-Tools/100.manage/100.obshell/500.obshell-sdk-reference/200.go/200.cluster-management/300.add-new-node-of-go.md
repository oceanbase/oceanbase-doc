|description||
|---|---|
|slug|add-new-node-go|

# クラスタ初期化前にノードを追加する

本記事では、obshell-sdk-goを使用して初期化されていないクラスタにノードを追加する方法について説明します。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>まず、<a href="../100.quickstart-of-go.md'>obshell-sdk-go クイックスタート</a> を参照し、obshell-sdk-goの使用方法を確認することをお勧めします。</p>
</main>

## 注意事項

初期化されていないクラスタにノードを追加する前に、以下の要件に注意してください：

* リクエスト元のクライアントに対応するノードが、初期化対象のクラスタ内に存在している必要があります（自己追加は除く）。

* 追加するノードが、これまでにいかなるクラスタにも参加したことがないことを確認する必要があります。

* 自身ノードを追加する場合、そのノードがこれまでにいかなるクラスタにも参加したことがなく、かつobshellが正常に実行されていることを確認する必要があります。

## サンプルコード

以下のコードは、`10.10.10.1` マシンに自身ノードを追加してMasterとし、さらに別のノード（`10.10.10.2`）を追加してFollowerにするために使用されます。

:::tab

tab タスクの同期的実行

```go
package main

import (
    "github.com/oceanbase/obshell-sdk-go/services"
)

func main() {
    // クライアントインスタンスを作成します。ノードのアドレスは '10.10.10.1'、ポートは2886です。
    client, err := services.NewClient("10.10.10.1", 2886)
    if err != nil {
        panic(err)
    }

    // 自身をjoinします。
    req := client.V1().NewJoinRequest("10.10.10.1", 2886, "zone1")
    if _, err = client.V1().JoinSyncWithRequest(req); err != nil {
        panic(err)
    }

    // '10.10.10.1' が存在するクラスタに、ノード '10.10.10.2:2886' を追加します。
    req = client.V1().NewJoinRequest("10.10.10.2", 2886, "zone1")
    if _, err = client.V1().JoinSyncWithRequest(req); err != nil {
        panic(err)
    }
}
```

tab タスクの非同期実行

```go
package main

import (
    "github.com/oceanbase/obshell-sdk-go/services"
)

func main() {
    // クライアントインスタンスを作成します。ノードのアドレスは '10.10.10.1'、ポートは2886です。
    client, err := services.NewClient("10.10.10.1", 2886)
    if err != nil {
        panic(err)
    }

    // 自身をjoinします。
    req := client.V1().NewJoinRequest("10.10.10.1", 2886, "zone1")
    dag, err := client.V1().JoinWithRequest(req)
    if err != nil {
        panic(err)
    }
    // タスクが成功するまで待機します。
    if _, err = client.V1().WaitDagSucceed(dag.GenericID); err != nil {
        panic(err)
    }

    // '10.10.10.1' が存在するクラスタに、ノード '10.10.10.2:2886' を追加します。
    req = client.V1().NewJoinRequest("10.10.10.2", 2886, "zone1")
    dag, err = client.V1().JoinWithRequest(req)
    if err != nil {
        panic(err)
    }
    // タスクが成功するまで待機します。
    if  _, err = client.V1().WaitDagSucceed(dag.GenericID); err != nil {
        panic(err)
    }
}
```

:::

## 関連ドキュメント

* クラスタ初期化前にノードを追加するAPIの説明については、[クラスタ初期化前にノードを追加する](../../../400.obshell-api-reference/200.cluster-management/300.add-new-node.md)を参照してください。

* obshell-sdk-pythonでAPIメソッドを呼び出す方法の紹介については、[クラスタ初期化前にノードを追加する](../../100.python/200.cluster-management/300.add-new-node-of-python.md)を参照してください。