|description||
|---|---|
|machine-translation|ja-marker|

# すべてのテナントの概要を取得する

本記事では、obshell-sdk-goを使用してクラスタ内のすべてのテナントの概要を取得する方法について説明します。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>まずは <a href="../100.quickstart-of-go.md'>obshell-sdk-goクイックスタート</a> を参照して、obshell-sdk-goの使用方法を確認することをお勧めします。</p>
</main>

## 注意事項

* obshellが正常に実行されていることを確認してください。

* リクエストのクライアントに対応するノードは、初期化済みのクラスタ内に存在する必要があります。

## サンプルコード

```go
package main

import (
  "fmt"

  "github.com/oceanbase/obshell-sdk-go/services"
)

func main() {
  // クライアントインスタンスを作成します。ノードアドレスは'10.10.10.1'、ポートは2886です。
  client, err := services.NewClientWithPassword("10.10.10.1", 2886, "****")
  if err != nil {
    fmt.Print(err)
    return
  }

  tenants, err := client.V1().GetAllTenantOverview()
  if err != nil {
    fmt.Print(err)
    return
  }
  fmt.Printf("tenants' overview: %v\n", tenants)
}
```

## 関連ドキュメント

* すべてのテナントの概要を取得するAPIインターフェースの説明については、[すべてのテナントの概要を取得する](../../../400.obshell-api-reference/500.tenant-management/1500.query-all-tenants.md)を参照してください。

* obshell-sdk-pythonを使用したAPIメソッドのリクエスト方法の紹介については、[すべてのテナントの概要を取得する](../../100.python/500.tenant-management/1500.query-all-tenants-of-python.md)を参照してください。