|description||
|---|---|
|machine-translation|ja-marker|
|slug|backup-configuration-for-cluster-of-go|

# クラスタレベルのバックアップ設定を設定する

本記事では、obshell-sdk-goを使用してクラスタレベルのバックアップ設定を設定する方法について説明します。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>まずは <a href="../100.quickstart-of-go.md">obshell-sdk-goクイックスタート</a> を参照して、obshell-sdk-goの使用方法を確認することをお勧めします。</p>
</main>

## 注意事項

* obshellが正常に実行されていることを確認してください。

* リクエストのクライアントに対応するノードは、初期化済みのクラスタ内に存在する必要があります。

## サンプルコード

```go
package main

import (
  "github.com/oceanbase/obshell-sdk-go/services"
)

func main() {
  // クライアントインスタンスを作成します。ノードアドレスは'10.10.10.1'、ポートは2886です。
  client, err := services.NewClientWithPassword("10.10.10.1", 2886, "****")
  if err != nil {
    panic(err)
  }

  request := client.V1().NewClusterBackupConfigPostRequest("file:///data/backup")
  request.SetLogArchiveConcurrency(100)
  _, err = client.V1().ClusterBackupConfigSyncWithRequest(request)
  if err != nil {
    panic(err)
  }
}
```

## 関連ドキュメント

* クラスタレベルのバックアップ設定を設定するAPIインターフェースの説明については、[クラスタレベルのバックアップ設定を設定する](../../../400.obshell-api-reference/600.backup-management/200.configure-backup-configuration-for-cluster-level-tenants.md)を参照してください。

* obshell-sdk-pythonを使用してAPIメソッドをリクエストする方法の紹介については、[クラスタレベルのバックアップ設定を設定する](../../100.python/600.backup-management/200.backup-configuration-for-cluster-of-python.md)を参照してください。