|description||
|---|---|
|machine-translation|ja-marker|

# 単一テナントのバックアップの開始

本記事では、obshell-sdk-goを使用して単一テナントのバックアップを実行する方法について説明します。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>まずは <a href="../100.quickstart-of-go.md">obshell-sdk-goクイックスタート</a> を参照して、obshell-sdk-goの使用方法を確認することを推奨します。</p>
</main>

## 注意事項

* obshellが正常に動作していることを確認してください。

* リクエストのクライアントに対応するノードは、初期化済みのクラスタ内に存在する必要があります。

## サンプルコード

```go
package main

import (
  "github.com/oceanbase/obshell-sdk-go/services"
)

func main() {
  // Create a client instance with the node address of '10.10.10.1' and port 2886.
  client, err := services.NewClientWithPassword("10.10.10.1", 2886, "****")
  if err != nil {
    panic(err)
  }

  request := client.V1().NewTenantBackupRequest("tenant2")
  request.SetBackupMode("full")
  _, err = client.V1().TenantBackupSyncWithRequest(request)
  if err != nil {
    panic(err)
  }
}
```

## 関連ドキュメント

* 単一テナントのバックアップを開始するAPIインターフェースの説明については、[単一テナントのバックアップの開始](../../../400.obshell-api-reference/600.backup-management/500.initiate-a-single-tenant-backup.md)を参照してください。

* obshell-sdk-pythonを使用したAPIメソッドのリクエスト方法の紹介については、[単一テナントのバックアップの開始](../../100.python/600.backup-management/500.initiate-a-single-tenant-backup-of-python.md)を参照してください。