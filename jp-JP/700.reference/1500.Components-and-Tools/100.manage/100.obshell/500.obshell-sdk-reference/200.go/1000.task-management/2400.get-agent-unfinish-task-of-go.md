|description||
|---|---|
|machine-translation|ja-marker|
|slug|query-unfinished-agent-tasks-go|

# Agentの未完了タスクの照会

本記事では、obshell-sdk-goを使用して現在のobshellノードの未完了タスクの詳細情報を取得する方法について説明します。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>まずは <a href="../100.quickstart-of-go.md">obshell-sdk-goクイックスタート</a> を参照して、obshell-sdk-goの使用方法を確認することを推奨します。</p>
</main>

## 注意事項

obshellが正常に実行されていることを確認してください。

## サンプルコード

```go
package main

import (
    "fmt"

    "github.com/oceanbase/obshell-sdk-go/services"
)

func main() {
    // clientインスタンスを作成します。ノードアドレスは'10.10.10.1'、ポートは2886です。
    // 所属クラスタのroot@sysパスワードは'****'です。
    client, err := services.NewClientWithPassword("10.10.10.1", 2886, "***")
    if err != nil {
        panic(err)
    }

    // 現在のobshellノードの未完了タスクの詳細情報を取得します。
    dags, err := client.V1().GetAgentUnfinishedDags()
    if err != nil {
        panic(err)
    }
    fmt.Print(dags)
}
```

## 関連ドキュメント

* Agentの未完了タスクを照会するAPIインターフェースの説明については、[Agentの未完了タスクの照会](../../../400.obshell-api-reference/1000.task-management/2400.get-agent-unfinish-task.md)を参照してください。

* obshell-sdk-pythonを使用したAPIメソッドのリクエスト方法の紹介については、[Agentの未完了タスクの照会](../../100.python/1000.task-management/2400.get-agent-unfinish-task-of-python.md)を参照してください。