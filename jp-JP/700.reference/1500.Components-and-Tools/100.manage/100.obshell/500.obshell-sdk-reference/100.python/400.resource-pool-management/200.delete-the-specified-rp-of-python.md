|description||
|---|---|
|machine-translation|ja-marker|

# リソースプールの削除

本記事では、obshell-sdk-pythonを使用して占有されていないリソースプールを削除する方法について説明します。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>まずは <a href="../100.quickstart-of-python.md">obshell-sdk-pythonクイックスタート</a> を参照し、obshell-sdk-pythonの使用方法を確認することを推奨します。</p>
</main>

## 注意事項

* obshellが正常に実行されていることを確認してください。

* リクエストのクライアントに対応するノードは、初期化済みのクラスタ内に存在する必要があります。

* 削除対象のリソースプールが占有されていないことを指定してください。

## サンプルコード

```python
from obshell import ClientSet
from obshell.auth import PasswordAuth

# クライアントインスタンスを作成します。ノードアドレスは '10.10.10.1'、ポートは2886です。
client = ClientSet("10.10.10.1", 2886, PasswordAuth("****"))

client.v1.drop_resource_pool("p1")
```

## 関連ドキュメント

* リソースプールを削除するAPIの詳細については、[リソースプールの削除](../../../400.obshell-api-reference/400.resource-pool-management/200.delete-the-specified-resource-pool.md)を参照してください。

* obshell-sdk-goを使用したAPIメソッドのリクエスト方法の紹介については、[リソースプールの削除](../../200.go/400.resource-pool-management/200.delete-the-specified-rp-of-go.md)を参照してください。