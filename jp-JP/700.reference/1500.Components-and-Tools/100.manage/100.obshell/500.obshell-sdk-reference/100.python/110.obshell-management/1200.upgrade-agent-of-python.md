|description||
|---|---|
|slug|upgrade-agent-python|

# Agentのアップグレード

本記事では、obshell-sdk-pythonを使用してクラスタ内のobshellノードをアップグレードする方法について説明します。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>まず、<a href='../100.quickstart-of-python.md'>obshell-sdk-python クイックスタート</a>を参照し、obshell-sdk-pythonの使用方法を確認することをお勧めします。</p>
</main>

## 注意事項

* obshellが正常に実行されていることを確認してください。

* リクエスト元のクライアントが対応するノードがアップグレード対象のクラスタ内にあることを確認してください。

## サンプルコード

:::tab

tab タスクの同期的実行

```python
from obshell import ClientSet
from obshell.auth import PasswordAuth

# クライアントインスタンスを作成します。ノードのアドレスは '10.10.10.1'、ポートは2886です。
# 所在クラスタのroot@sysパスワードは '****' です。
client = ClientSet("10.10.10.1", 2886, PasswordAuth("****"))

# Agentをアップグレードします。
client.v1.upgrade_agent_sync("4.2.2.0", "xxx.el7")
```

tab タスクの非同期実行

```python
from obshell import ClientSet
from obshell.auth import PasswordAuth

# クライアントインスタンスを作成します。ノードのアドレスは '10.10.10.1'、ポートは2886です。
# 所在クラスタのroot@sysパスワードは '****' です。
client = ClientSet("10.10.10.1", 2886, PasswordAuth("****"))

# Agentをアップグレードします。
client.v1.upgrade_agent("4.2.2.0", "xxx.el7")
# タスク完了を待ちます。
client.v1.wait_dag_succeed(dag_generic_id)
```

:::

## 関連ドキュメント

* Agentのアップグレードに関するAPIインターフェースの説明については、[Agentのアップグレード](../../../400.obshell-api-reference/100.obshell-management/1200.upgrade-agent.md)を参照してください。

* obshell-sdk-goを使用したAPIメソッドのリクエスト方法の紹介については、[Agentのアップグレード](../../200.go/110.obshell-management/1200.upgrade-agent-of-go.md)を参照してください。