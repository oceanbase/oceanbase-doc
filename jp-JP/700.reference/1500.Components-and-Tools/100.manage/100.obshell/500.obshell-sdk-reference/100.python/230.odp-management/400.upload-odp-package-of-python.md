|description||
|---|---|
|slug|upload-odp-package-python|

# OBProxyインストールパッケージのアップロード

本記事では、obshell-sdk-pythonを使用して、OBProxy（OceanBaseデータベースプロキシ、別名ODP）のアップグレードに必要なインストールパッケージをobshellメタデータベースにアップロードする方法について説明します。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>obshell V4.2.6以降、obshell-sdk-pythonを使用してOBProxyアップグレードに必要なインストールパッケージをアップロードできます。</p>
</main>

## 注意事項

* obshellが正常に実行されていることを確認してください。

* リクエスト元のクライアントに対応するノードにOBProxyがデプロイ済みであることを確認してください。

## サンプルコード

```python
from obshell import ClientSet
from obshell.auth import PasswordAuth

# クライアントインスタンスを作成します。ノードのアドレスは'10.10.10.1'、ポートは2886、ノードのagent passwordは'***'です。
client = ClientSet("10.10.10.1", 2886, PasswordAuth(agent_password='***'))

client.v1.upload_obproxy_pkg(
        "/home/download/obproxy-ce-4.3.3.0-5.el7.x86_64.rpm")
```

## 関連ドキュメント

OBProxyインストールパッケージのアップロードに関するAPIインターフェースの説明については、[OBProxyインストールパッケージのアップロード](../../../400.obshell-api-reference/230.odp-management/400.upload-odp-package.md)を参照してください。