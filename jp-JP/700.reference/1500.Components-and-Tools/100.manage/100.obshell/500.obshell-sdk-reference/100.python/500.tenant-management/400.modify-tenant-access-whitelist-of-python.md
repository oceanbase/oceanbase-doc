|description||
|---|---|
|machine-translation|ja-marker|

# テナントのアクセスアローリストの変更

本記事では、obshell-sdk-pythonを使用してテナントのアクセスアローリストを変更する方法について説明します。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>まずは <a href="../100.quickstart-of-python.md">obshell-sdk-pythonクイックスタート</a> を参照して、obshell-sdk-pythonの使用方法を確認することを推奨します。</p>
</main>

## 注意事項

* obshellが正常に実行されていることを確認してください。

* テナントが既に存在し、状態がNORMALであることを確認してください。

* リクエストのクライアントに対応するノードは、初期化済みのクラスタ内にある必要があります。

* デフォルトでは`127.0.0.1`が含まれます。

## サンプルコード

```python
from obshell import ClientSet
from obshell.auth import PasswordAuth

# クライアントインスタンスを作成します。ノードアドレスは'10.10.10.1'、ポートは2886です。
client = ClientSet("10.10.10.1", 2886, PasswordAuth("****"))

client.v1.set_tenant_whitelist("t1", "%")
```

## 関連ドキュメント

* テナントのアクセスアローリストを変更するAPIインターフェースの説明については、[テナントのアクセスアローリストの変更](../../../400.obshell-api-reference/500.tenant-management/400.modify-tenant-access-whitelist.md)を参照してください。

* obshell-sdk-goを使用したAPIメソッドのリクエストについては、[テナントのアクセスアローリストの変更](../../200.go/500.tenant-management/400.modify-tenant-access-whitelist-of-go.md)を参照してください。