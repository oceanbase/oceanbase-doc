|description||
|---|---|
|slug|query-all-tenants-python|

# すべてのテナントの概要を取得する

本記事では、obshell-sdk-pythonを使用してクラスタ内のすべてのテナントの概要を取得する方法について説明します。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>まず、<a href="../100.quickstart-of-python.md">obshell-sdk-python クイックスタート</a>を参照し、obshell-sdk-pythonの使用方法を確認することをお勧めします。</p>
</main>

## 注意事項

* obshellが正常に実行されていることを確認してください。

* リクエスト元のクライアントに対応するノードが初期化済みのクラスタ内にある必要があります。

## サンプルコード

```python
from obshell import ClientSet
from obshell.auth import PasswordAuth

# クライアントインスタンスを作成します。ノードのアドレスは '10.10.10.1'、ポートは2886です。
client = ClientSet("10.10.10.1", 2886, PasswordAuth("****"))

tenants = client.v1.get_all_tenants()
```

## 関連ドキュメント

* すべてのテナントの概要を取得するAPIの仕様については、[すべてのテナントの概要を取得する](../../../400.obshell-api-reference/500.tenant-management/1500.query-all-tenants.md)を参照してください。

* obshell-sdk-goでAPIメソッドをリクエストする方法については、[すべてのテナントの概要を取得する](../../200.go/500.tenant-management/1500.query-all-tenants-of-go.md)を参照してください。