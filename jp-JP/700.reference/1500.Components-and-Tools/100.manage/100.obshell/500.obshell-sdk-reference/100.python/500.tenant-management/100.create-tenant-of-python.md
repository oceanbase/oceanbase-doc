|description||
|---|---|
|slug|create-tenant-python|

# テナントの作成

本記事では、obshell-sdk-pythonを使用してテナントを作成する方法について説明します。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>まず、<a href="../100.quickstart-of-python.md'>obshell-sdk-python クイックスタート</a> を参照し、obshell-sdk-pythonの使い方を確認することをお勧めします。</p>
</main>

## 注意事項

* obshellが正常に実行されていることを確認してください。

* リクエスト元のクライアントに対応するノードが初期化済みのクラスタ内にある必要があります。

## サンプルコード

:::tab

tab タスクの同期的実行

```python
from obshell import ClientSet
from obshell.auth import PasswordAuth
from obshell.model import ZoneParam

# クライアントインスタンスを作成します。ノードのアドレスは '10.10.10.1'、ポートは2886です。
client = ClientSet("10.10.10.1", 2886, PasswordAuth("****"))

client.v1.create_tenant_sync("t1", [ZoneParam("zone1", 's1', 1),
                                    ZoneParam("zone2", 's1', 1),
                                    ZoneParam("zone3", 's1', 1)])
```

tab タスクの非同期実行

```python
from obshell import ClientSet
from obshell.auth import PasswordAuth
from obshell.model import ZoneParam

# クライアントインスタンスを作成します。ノードのアドレスは '10.10.10.1'、ポートは2886です。
client = ClientSet("10.10.10.1", 2886, PasswordAuth("****"))

dag = client.v1.create_tenant("t1", [ZoneParam("zone1", 's1', 1),
                                     ZoneParam("zone2", 's1', 1),
                                     ZoneParam("zone3", 's1', 1)))
# タスクの完了を待機します。
client.v1.wait_dag_succeed(dag_generic_id)
```

:::

## 関連ドキュメント

* テナント作成のAPIインターフェースについては、[テナントの作成](../../../400.obshell-api-reference/500.tenant-management/100.create-tenant-of-api.md)を参照してください。

* obshell-sdk-goでAPIメソッドを呼び出す方法については、[テナントの作成](../../200.go/500.tenant-management/100.create-tenant-of-go.md)を参照してください。