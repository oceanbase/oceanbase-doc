|description||
|---|---|
|slug|update-for-individual-tenants-python|

# 単一テナントのバックアップ設定を更新する

本記事では、obshell-sdk-pythonを使用して特定のテナントのバックアップ設定を更新する方法について説明します。

単一テナントのバックアップ設定を更新する場合の使用方法は、[単一テナントのバックアップ設定を設定する](100.backup-configuration-for-individual-tenants-of-python.md)と異なり、`data_base_uri`と`archive_base_uri`は設定しなくてもかまいません。

<main id="notice" type='notice'>
  <h4>注意</h4>
  <p><code>binding</code>と<code>piece_switch_interval</code>を設定する必要がある場合でも、<code>archive_base_uri</code>は設定する必要があります。これら3つは紐づけられており、増分設定はサポートされていないためです。</p>
</main>

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>まず、<a href="../100.quickstart-of-python.md'>obshell-sdk-pythonクイックスタート</a>を参照し、obshell-sdk-pythonの使用方法を確認することを推奨します。</p>
</main>

## 注意事項

* obshellが正常に実行されていることを確認してください。

* リクエスト元のクライアントに対応するノードが初期化済みのクラスタ内にあることを確認してください。

## サンプルコード

```python
from obshell import ClientSet
from obshell.auth import PasswordAuth

# クライアントインスタンスを作成します。ノードのアドレスは'10.10.10.1'、ポートは2886です。
client = ClientSet("10.10.10.1", 2886, PasswordAuth("****"))
client.v1.patch_tenant_backup_config_sync(
    "tenant1",
    log_archive_concurrency=100,
    ha_low_thread_score=100,
    delete_recovery_window="2d",
)
```

## 関連ドキュメント

* 単一テナントのバックアップ設定を更新するAPIの仕様については、[単一テナントのバックアップ設定を更新する](../../../400.obshell-api-reference/600.backup-management/300.update-backup-configuration-for-individual-tenants.md)を参照してください。

* obshell-sdk-goでAPIメソッドをリクエストする方法については、[単一テナントのバックアップ設定を更新する](../../200.go/600.backup-management/300.update-for-individual-tenants-of-go.md)を参照してください。