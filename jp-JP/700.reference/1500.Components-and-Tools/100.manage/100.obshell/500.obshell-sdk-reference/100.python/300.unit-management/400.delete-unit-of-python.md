|description||
|---|---|
|slug|delete-unit-python|

# リソース構成の削除

本記事では、obshell-sdk-pythonを使用して指定したリソース構成を削除する方法について説明します。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>まず、<a href="../100.quickstart-of-python.md">obshell-sdk-python クイックスタート</a> を参照し、obshell-sdk-pythonの使用方法を確認することをお勧めします。</p>
</main>

## 注意事項

* obshellが正常に実行されていることを確認してください。

* リクエスト元のクライアントに対応するノードが初期化済みのクラスタ内にある必要があります。

## サンプルコード

```python
from obshell import ClientSet
from obshell.auth import PasswordAuth

# クライアントインスタンスを作成します。ノードのアドレスは '10.10.10.1'、ポートは2886です。
client = ClientSet("10.10.10.1", 2886, PasswordAuth("****"))

client.v1.drop_resource_unit_config("s1")
```

## 関連ドキュメント

* リソース構成を削除するAPIの仕様については、[リソース構成の削除](../../../400.obshell-api-reference/300.unit-management/400.delete-unit.md)を参照してください。

* obshell-sdk-goでAPIメソッドをリクエストする方法については、[リソース構成の削除](../../200.go/300.unit-management/400.delete-unit-of-go.md)を参照してください。