|description||
|---|---|
|slug|analyze-events-waiting-in-query-using-sql-audit|

# SQL Auditを使用してクエリ内の待機イベントを分析する

SQL Auditは、待機イベントに関する以下の情報を記録します：

* 4つのカテゴリの待機イベントごとの所要時間（`APPLICATION_WAIT_TIME`、`CONCURRENCY_WAIT_TIME`、`USER_IO_WAIT_TIME`、`SCHEDULE_TIME`）を記録します。各カテゴリには、個々の待機時間が多数含まれます。

* 最も時間がかかった待機イベント名（`EVENT`）およびその待機イベントの所要時間（`WAIT_TIME_MICRO`）を記録しています。

* すべての待機イベントの発生回数（`TOTAL_WAITS`）およびすべての待機イベントの合計所要時間（`TOTAL_WAIT_TIME_MICRO`）を記録しています。

一般的に、待機イベントの合計所要時間が長い場合、最も時間がかかった待機イベント名（`EVENT`）を確認することで、遅延の主な原因を特定できます。

以下の例から、待機時間の大部分をI/O待機が占めていることが分かります。

```sql
obclient> SELECT SQL_ID, ELAPSED_TIME, QUEUE_TIME, GET_PLAN_TIME, EXECUTE_TIME,
     APPLICATION_WAIT_TIME, CONCURRENCY_WAIT_TIME, USER_IO_WAIT_TIME, SCHEDULE_TIME,
     EVENT, WAIT_CLASS,  WAIT_TIME_MICRO, TOTAL_WAIT_TIME_MICRO FROM V$OB_SQL_AUDIT
     WHERE TRACE_ID = 'YB42AC1E87E6-0005B8AB2D578471'\G;

************************** 1. row ***************************
               SQL_ID: YB42AC1E87E6-0005B8AB2D578471
         ELAPSED_TIME: 953
           QUEUE_TIME: 18
        GET_PLAN_TIME: 58
         EXECUTE_TIME: 867
APPLICATION_WAIT_TIME: 0
CONCURRENCY_WAIT_TIME: 0
    USER_IO_WAIT_TIME: 550
        SCHEDULE_TIME: 0
                EVENT: db file data index read
           WAIT_CLASS: USER_IO
      WAIT_TIME_MICRO: 352
TOTAL_WAIT_TIME_MICRO: 550
```
