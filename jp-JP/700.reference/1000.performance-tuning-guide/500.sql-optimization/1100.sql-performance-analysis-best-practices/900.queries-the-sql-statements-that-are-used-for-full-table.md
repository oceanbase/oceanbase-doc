|description||
|---|---|
|slug|sql-statements-used-for-full-table|

# 特定テナントでフルテーブルスキャンを実行したSQLの検索

以下のステートメントを実行すると、特定のテナントでフルテーブルスキャンを実行したSQLを照会できます。

```javascript
obclient> SELECT query_sql
      FROM oceanbase.GV$OB_SQL_AUDIT
      WHERE table_scan = 1 AND tenant_id = 1001
      GROUP BY sql_id;

+-------------------------------+
| query_sql                     |
+-------------------------------+
| SHOW VARIABLES LIKE 'version' |
+-------------------------------+
```