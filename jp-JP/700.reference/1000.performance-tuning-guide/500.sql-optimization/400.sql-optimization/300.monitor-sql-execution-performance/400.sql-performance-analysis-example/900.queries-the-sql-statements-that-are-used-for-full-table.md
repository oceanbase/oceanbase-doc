|description||
|---|---|
|machine-translation|ja-marker|
|slug|queries-the-sql-statements-that-are-used-for-full-table|

# 特定テナント内でフルスキャンを実行するSQLの検索

以下のステートメントを実行すると、特定のテナント内でフルスキャンを実行するSQLを照会できます。

```javascript
obclient> SELECT query_sql
      FROM oceanbase.GV$OB_SQL_AUDIT
      WHERE table_scan = 1 AND tenant_id = 1001
      GROUP BY sql_id;

+-------------------------------+
| query_sql                     |
+-------------------------------+
| SHOW VARIABLES LIKE 'version' |
+-------------------------------+
```