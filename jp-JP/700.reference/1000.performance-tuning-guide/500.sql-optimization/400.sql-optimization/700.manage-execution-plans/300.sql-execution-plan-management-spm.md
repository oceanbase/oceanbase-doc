|description||
|---|---|
|machine-translation|ja-marker|

# 実行計画管理

SQL Plan Management（SPM）とは、計画のロールバックを防ぐ仕組みであり、新しく生成された計画が検証された後にのみ使用されるようにすることで、計画のパフォーマンスを継続的に最適化および更新することを保証します。

<main id="notice" >
    <h4>適用対象</h4>
    <p>現在、OceanBaseデータベースCommunity EditionはSPM機能をサポートしていません。</p>
</main>

OceanBaseデータベースはオンラインSPM進化メカニズムをサポートしており、新しく生成された計画がベースラインに含まれていないことが検出されると、直ちに自動的に進化タスクが開始され、計画の進化が行われます。これにより、ユーザーが手動で介入することなく、計画の進化を自動的に完了させることが可能です。

OceanBaseデータベースのSPMでは、`DBA_SQL_PLAN_BASELINES` および `DBA_SQL_MANAGEMENT_CONFIG` ビュー、そして `DBMS_SPM` システムパッケージを使用して実行計画を管理します。

## SPMの実行メカニズム

SPMはSQL Plan Baselineに基づいて実装されており、SQL Plan Baselineとは実行計画のベースラインであり、検証済みの実行計画情報（アウトラインデータなど）を永続的に保存するために使用されます。各実行計画には1つのPlan Baselineが対応し、このPlan Baselineを通じて実行計画を再現することができます。

OceanBaseデータベースの計画の進化は常に計画生成によって引き起こされます。SPMの実行メカニズムは以下のように要約できます：

1. SQLが初めて生成した計画は、デフォルトでベースラインとして扱われ、`ACCEPT`されます。
2. 新しい計画が計画ベースラインに存在し、かつベースラインが`FIXED`である場合、または他の`FIXED`計画ベースラインが存在しない場合、現在の計画を直接使用します。
3. 再現可能な`FIXED`ベースラインが存在する場合、常に`FIXED`ベースライン計画を優先的に使用し、計画の進化は行いません。
4. 再現可能な`FIXED`ベースラインが存在しないが、再現可能な`ACCEPTED`ベースライン計画が存在する場合、新しい計画とベースライン計画との間で進化が行われます。
5. ベースライン計画を再現できるものがない場合、新しく生成された計画を直接使用します。

## SPMの使用制限

OceanBaseデータベースのSPMには、以下のような使用制限があります：

* バックアップ・リカバリ中のリカバリ状態にあるテナントおよびプライマリ/スタンバイクラスタのスタンバイ機では、計画進化を実行できません。
* システムテナント内のSQLおよびInner SQLでは、計画進化を行うことができません。
* SQL文に`INSERT INTO VALUES`が含まれている場合、計画進化は行われません。
* 進化の結果は一時的にOBServerノードのローカルキャッシュに保存され、定期的にインナーテーブルに同期されるため、任意のOBServerノードにおける進化結果は他のOBServerノードから即座に認識することはできません。

## SPM関連ビュー

### DBA_SQL_PLAN_BASELINES

`DBA_SQL_PLAN_BASELINES`ビューは、SPM内のSQLの計画ベースラインを記録します。このビューの各フィールドの意味は以下の表に示されています。

| **フィールド名** | **データ型** | **NULL許容** | **説明** |
| --- | --- | --- | --- |
| SIGNATURE | VARCHAR2(128) | NOT NULL | 正規化されたSQLによって生成されるSQLの一意識別子（このフィールドの型はOracleと互換性がありません）。 |
| SQL_HANDLE | VARCHAR2(128) | NOT NULL | 一意のテキスト形式のSQL表現。 |
| SQL_TEXT | CLOB | NOT NULL | 正規化されていないSQLテキスト。 |
| PLAN_NAME | VARCHAR2(128) | NOT NULL | 一意のテキスト形式の計画識別子。 |
| CREATOR | VARCHAR2(128) |  | この計画ベースラインを作成したユーザー名。 |
| ORIGIN | VARCHAR2(14) |  | 計画ベースラインの作成方法は、以下のとおりです。<ul><li> `MANUAL-LOAD`</li> <li> `AUTO-CAPTURE` </li>|
| PARSING_SCHEMA_NAME | VARCHAR2(128) |  | 常にNULLです。 |
| DESCRIPTION | VARCHAR2(500) |  | 計画ベースラインの説明。 |
| VERSION | VARCHAR2(64) |  | 計画ベースライン作成時のデータベースバージョン。 |
| CREATED | TIMESTAMP(6) | NOT NULL | 計画ベースラインの作成時間。 |
| LAST_MODIFIED | TIMESTAMP(6) |  | 計画ベースラインの前回の変更時間。 |
| LAST_EXECUTED | TIMESTAMP(6) |  | 計画ベースラインの前回の実行時間。 |
| LAST_VERIFIED | TIMESTAMP(6) |  | 計画ベースラインの前回の検証時間。 |
| ENABLED | VARCHAR2(3) |  | 計画ベースラインが有効（'YES'）か無効（'NO'）か。 |
| ACCEPTED | VARCHAR2(3) |  | 計画ベースラインが採択された（'YES'）か採択されなかった（'NO'）か。 |
| FIXED | VARCHAR2(3) |  | 計画ベースラインが固定されている（'YES'）か固定されていない（'NO'）か。 |
| REPRODUCED | VARCHAR2(3) |  | 計画ベースラインが再現可能（'YES'）か再現不可能（'NO'）か。 |
| AUTOPURGE | VARCHAR2(3) |  | 計画ベースラインが自動クリーンアップされる（'YES'）かされない（'NO'）か。 |
| ADAPTIVE | VARCHAR2(3) |  | SPMによって自動キャプチャされた計画ベースラインが適応済みであるかどうか。 |
| OPTIMIZER_COST | NUMBER |  | 計画ベースライン作成時のオプティマイザーによる計画コストの推定値。 |
| MODULE | VARCHAR2(64) |  | 常にNULLです。 |
| ACTION | VARCHAR2(64) |  | 常にNULLです。 |
| EXECUTIONS | NUMBER |  | 計画ベースライン作成時の計画の実行回数。 |
| ELAPSED_TIME | NUMBER |  | 計画ベースライン作成時の計画の総実行時間。 |
| CPU_TIME | NUMBER |  | 計画ベースライン作成時の計画の総CPU時間。 |
| BUFFER_GETS | NUMBER |  | 常にNULLです。 |
| DISK_READS | NUMBER |  | 常にNULLです。 |
| DIRECT_WRITES | NUMBER |  | 常にNULLです。 |
| ROWS_PROCESSED | NUMBER |  | 常にNULLです。 |
| FETCHES | NUMBER |  | 常にNULLです。 |
| END_OF_FETCH_COUNT | NUMBER |  | 常にNULLです。 |

### DBA_SQL_MANAGEMENT_CONFIG

`DBA_SQL_MANAGEMENT_CONFIG`ビューは、SPM内の構成パラメータを記録しています。このビューの各フィールドの意味は以下の表に示されています。

| **フィールド名** | **データ型** | **NULL許容** | **説明** |
| --- | --- | --- | --- |
| PARAMETER_NAME | VARCHAR2(128) | NOT NULL | 構成パラメータの名前。例：<ul><li>`SPACE_BUDGET_PERCENT`</li><li>`PLAN_RETENTION_WEEKS`</ul></li> |
| PARAMETER_VALUE | NUMBER | NOT NULL | 構成パラメータの値。 |
| LAST_MODIFIED | TIMESTAMP(6) |   | パラメータ値が最後に更新された時間。 |
| MODIFIED_BY | VARCHAR2(128) |   | パラメータ値が最後に誰によって更新されたか。 |

## DBMS_SPMシステムパッケージ

`DBMS_SPM` システムパッケージは、SQL計画を管理するために使用され、Plan Baseline情報の読み込み、変更、削除をサポートしています。

次の表は、OceanBaseデータベースの現行バージョンでサポートされているDBMS_SPMサブルーチンとその簡潔な説明を示しています。

| **サブルーチン** | **説明** |
| --- | --- |
| ACCEPT_SQL_PLAN_BASELINE | エボリューション計画の結果に基づいて、ある計画をベースラインとして受け入れます。 |
| ALTER_SQL_PLAN_BASELINE | ベースライン内の1つまたは複数の計画のプロパティを変更します。戻り値は変更されたベースラインの数を示します。 |
| CANCEL_EVOLVE_TASK | 現在進行中のエボリューションタスクをキャンセルします。 |
| CONFIGURE | SPMの一部のパラメータを設定します。 |
| DROP_EVOLVE_TASK | 1つのエボリューションタスクを破棄します。 |
| DROP_SQL_PLAN_BASELINE | 1つ以上の計画ベースラインを破棄します。 |
| LOAD_PLANS_FROM_CURSOR_CACHE | Cursor Cacheから1つまたは複数のSQL計画を読み取り、計画ベースラインとして設定します。 |
| BATCH_LOAD_PLANS_FROM_CURSOR_CACHE | Plan Cacheに存在するSQL計画を一括してSPMベースラインに追加します。 |
| BATCH_DROP_SQL_PLAN_BASELINE | 現在のテナントのすべての計画ベースラインを削除します。 |
| HANDLE_SPM_STATS_JOB_PROC | 毎日、期限切れのSPM統計情報をクリーンアップします。 |
| CREATE_STGTAB_BASELINE | ベースライン計画を保存するためのキャッシュテーブルを作成します。 |
| PACK_STGTAB_BASELINE | ベースライン計画をキャッシュテーブルにエクスポートします。 |
| UNPACK_STGTAB_BASELINE | キャッシュテーブル内のベースライン計画を内部テーブルにインポートします。 |
| PACK_CSV_BASELINE | ベースライン計画をCSVファイルにエクスポートします。 |
| UNPACK_CSV_BASELINE | CSVファイル内のベースライン計画を内部テーブルにインポートします。 |

### ACCEPT_SQL_PLAN_BASELINE

`ACCEPT_SQL_PLAN_BASELINE`プロシージャは、進化計画の結果に基づいてある計画をベースラインとして受け入れるために使用されます。

#### 構文

Oracleモードの構文は次のとおりです：

```sql
DBMS_SPM.ACCEPT_SQL_PLAN_BASELINE  (
    sql_handle        IN VARCHAR,
    plan_name         IN VARCHAR := NULL);
```

MySQLモードの構文は次のとおりです：

```sql
DBMS_SPM.ACCEPT_SQL_PLAN_BASELINE  (
     database_name     VARCHAR(65535),
     sql_handle        VARCHAR(65535),
     plan_name         VARCHAR(65535) DEFAULT NULL);
```

#### パラメータの説明

| **パラメータ名** | **説明** |
| --- | --- |
| database_name | データベース名。このパラメータはMySQLモードでのみ指定する必要があります。 |
| sql_handle | SQLの識別子、すなわち`SQL_ID`です。 |
| plan_name | プランの名前、すなわちPlan Hash Valueです。値が`NULL`場合、1つのSQLに対して進化中のすべてのプランを受け入れることを意味します。 |

#### 例

```sql
DECLARE
  v_accept_plans NUMBER;
BEGIN
  v_accept_plans := DBMS_SPM.ACCEPT_SQL_PLAN_BASELINE(
     sql_handle => '529F6E6454EF579C7CC265D1F6131D70',
     plan_name => '3388268709115914355'
     );
END;
/
```

### ALTER_SQL_PLAN_BASELINE

`ALTER_SQL_PLAN_BASELINE`関数は、ベースライン内の1つまたは複数の計画のプロパティを変更するために使用されます。戻り値は変更されたベースラインの数を示します。

#### 構文

Oracleモードの構文は次のとおりです：

```sql
DBMS_SPM.ALTER_SQL_PLAN_BASELINE (
  sql_handle        IN VARCHAR,
  plan_name         IN VARCHAR := NULL,
  attribute_name    IN VARCHAR,
  attribute_value   IN VARCHAR
)
RETURN PLS_INTEGER;
```

MySQLモードの構文は次のとおりです：

```sql
DBMS_SPM.ALTER_SQL_PLAN_BASELINE (
  database_name     VARCHAR(65535),
  sql_handle        VARCHAR(65535),
  plan_name         VARCHAR(65535) DEFAULT NULL,
  attribute_name    VARCHAR(65535),
  attribute_value   VARCHAR(65535)
)
RETURN DECIMAL;
```

#### パラメータの説明

| **パラメータ名** | **説明** |
| --- | --- |
| database_name | データベース名。このパラメータはMySQLモードでのみ指定する必要があります。 |
| sql_handle | SQLの識別子、すなわち`SQL_ID`です。 |
| plan_name | プランの名前、すなわちPlan Hash Valueです。値が`NULL`場合、1つのSQLに対して進化中のすべてのプランを受け入れることを意味します。 |
| attribute_name | プランの属性名。**属性の説明**を参照してください。 |
| attribute_value | プランの属性値。**属性の説明**を参照してください。 |

#### プロパティの説明

| **プロパティ名** | **推奨されるプロパティ値** | **説明** |
| --- | --- | --- |
| enabled | 'YES'または'NO' | 'YES'は、計画ベースラインが有効であることを示します。最終的に計画ベースラインが使用されるかどうかは、受け入れられるかどうかによって決まります。 |
| fixed | 'YES'または'NO' | 'YES'は、現在の計画ベースラインを優先的に使用し、**自動進化を行わない**ことを示します。 |
| autopurge | 'YES'または'NO' | 'YES'は、計画ベースラインが一定期間使用されない場合に自動的に廃棄されることを示します。'NO'は、計画ベースラインが決して廃棄されないことを示します。 |
| plan_name | 最長30文字の文字列。 | プランの名前。 |
| description | 最長500バイトの文字列。 | プランの説明。 |

#### 例

特定のPlan Baselineを固定化すると、そのSQLはその計画のみを使用します。

```sql
DECLARE
  v_alter_plans NUMBER;
BEGIN
  v_alter_plans := DBMS_SPM.ALTER_SQL_PLAN_BASELINE(
     sql_handle => '529F6E6454EF579C7CC265D1F6131D70',
     plan_name => '3388268709115914355',
     attribute_name => 'fixed',
     attribute_value => 'YES' );
END;
/
```

### CANCEL_EVOLVE_TASK

`CANCEL_EVOLVE_TASK` プロシージャは、進化中のタスクをキャンセルするために使用されます。

#### 構文

Oracleモードの構文：

```sql
DBMS_SPM.CANCEL_EVOLVE_TASK (
 task_name INVARCHAR2);
```

MySQLモードの構文：

```sql
DBMS_SPM.CANCEL_EVOLVE_TASK  (
  database_name     VARCHAR(65535),
  task_name         VARCHAR(65535));
```

#### パラメータの説明

| **パラメータ名** | **説明** |
| --- | --- |
| database_name | データベース名。このパラメータはMySQLモードでのみ指定する必要があります。 |
| task_name | タスク名、つまり現在のOceanBaseデータベースで`SQL_ID`によって表される進化タスクの名前です。 |

#### 例

```sql
BEGIN
  DBMS_SPM.CANCEL_EVOLVE_TASK ('SYS_AUTO_SPM_EVOLVE_TASK');
END;
```

### CONFIGURE

`CONFIGURE` プロシージャは、SPMの一部のパラメータを設定するために使用されます。

#### 構文

Oracleモードの構文は次のとおりです：

```sql
DBMS_SPM.CONFIGURE (
    parameter_name    IN VARCHAR2,
    parameter_value   IN NUMBER
  );
```

MySQLモードの構文は次のとおりです：

```sql
DBMS_SPM.CONFIGURE (
    database_name     VARCHAR(65535),
    parameter_name    VARCHAR(65535),
    parameter_value   DECIMAL
  );
```

#### パラメータの説明

| **パラメータ名** | **説明** |
| --- | --- |
| database_name | データベース名。このパラメータはMySQLモードでのみ指定する必要があります。 |
| parameter_name | 設定するパラメータ名。**SPMパラメータの説明**を参照してください。 |
| parameter_value | 設定するパラメータ値。**SPMパラメータの説明**を参照してください。 |

#### SPMパラメータの説明

| SPMパラメータ名 | **説明** | **推奨値** | **デフォルト値** |
| --- | --- | --- | --- |
| space_budget_percent | SPMのSYSAUX領域の最大割合を設定します。 | 1、2、…、50 | 10 |
| plan_retention_weeks | プランが何週間使用されない場合に自動的に破棄されるまでの期間を設定します。 | 5、6、…、523 | 53 |

#### 例

計画が20週間使用されない場合、自動的に廃棄されるように設定します。

```sql
BEGIN
  DBMS_SPM.CONFIGURE ('plan_retention_weeks', 20);
END;
```

### DROP_EVOLVE_TASK

`DROP_EVOLVE_TASK` プロシージャは、進化タスクを破棄するために使用されます。

#### 構文

Oracleモードの構文は次のとおりです：

```sql
DBMS_SPM.DROP_EVOLVE_TASK  (
  task_name        IN  VARCHAR2);
```

MySQLモードの構文は次のとおりです：

```sql
DBMS_SPM.DROP_EVOLVE_TASK  (
  database_name     VARCHAR(65535),
  task_name         VARCHAR(65535));
```

#### パラメータの説明

| **パラメータ名** | **説明** |
| --- | --- |
| database_name | データベース名。このパラメータはMySQLモードでのみ指定する必要があります。 |
| task_name | タスク名、つまり現在のOceanBaseデータベースで`SQL_ID`によって表される進化タスクの名前です。 |

#### 例

```sql
BEGIN
  DBMS_SPM.DROP_EVOLVE_TASK ('SYS_AUTO_SPM_EVOLVE_TASK');
END;
```

### DROP_SQL_PLAN_BASELINE

`DROP_SQL_PLAN_BASELINE`関数は、1つまたは複数の計画ベースラインを破棄するために使用されます。

#### 構文

Oracleモードの構文は次のとおりです：

```sql
DBMS_SPM.DROP_SQL_PLAN_BASELINE (
  sql_handle     IN VARCHAR2,
  plan_name      IN VARCHAR2 := NULL
)
RETURN PLS_INTEGER;
```

MySQLモードの構文は次のとおりです：

```sql
DBMS_SPM.DROP_SQL_PLAN_BASELINE (
  database_name     VARCHAR(65535),
  sql_handle        VARCHAR(65535),
  plan_name         VARCHAR(65535) DEFAULT NULL
)
RETURN DECIMAL;
```

#### パラメータの説明

| **パラメータ名** | **説明** |
| --- | --- |
| database_name | データベース名。このパラメータはMySQLモードでのみ指定する必要があります。 |
| sql_handle  | SQLの識別子、すなわち`SQL_ID`です。実行計画は、SQL文を識別するためのハンドル（Handle）として使用されます。特定のSQL文の実行計画を照会する際に、複数の類似したSQL文が存在する場合でも、sql_handleを使用してターゲット文を正確に指定できます。|
| plan_name   | 実行計画の名前、すなわちPlan Hash Valueです。値が`NULL`の場合、1つのSQLに対して進化中のすべての計画を許容することを意味します。|

#### 例

```sql
DECLARE
  v_drop_plans number;
BEGIN
  v_drop_plans := DBMS_SPM.DROP_SQL_PLAN_BASELINE(
     sql_handle => '529F6E6454EF579C7CC265D1F6131D70',
     plan_name => '3388268709115914355' );
END;
/
```

### LOAD_PLANS_FROM_CURSOR_CACHE

`LOAD_PLANS_FROM_CURSOR_CACHE`関数は、Plan Cacheから1つまたは複数のSQLの計画を読み取り、計画ベースラインとして使用します。

Oracleモードの構文は次のとおりです：

```sql
DBMS_SPM.LOAD_PLANS_FROM_CURSOR_CACHE (
  sql_id              IN VARCHAR,
  plan_hash_value     IN NUMBER   := NULL,
  fixed               IN VARCHAR  := 'NO',
  enabled             IN VARCHAR  := 'YES'
)
RETURN PLS_INTEGER;
```

MySQLモードの構文は次のとおりです：

```sql
DBMS_SPM.LOAD_PLANS_FROM_CURSOR_CACHE (
  database_name     VARCHAR(65535),
  sql_id            VARCHAR(65535),
  plan_hash_value   DECIMAL DEFAULT NULL,
  is_fixed          VARCHAR(65535) DEFAULT 'NO',
  enabled           VARCHAR(65535) DEFAULT 'YES'
)
RETURN DECIMAL;
```

#### パラメータの説明

| **パラメータ名** | **説明** |
| --- | --- |
| database_name | データベース名。このパラメータはMySQLモードでのみ指定する必要があります。 |
| sql_id | 読み込む`SQL_ID`。 |
| plan_hash_value | 読み込む計画のPlan Hash Value。値が`NULL`場合、Plan Cacheに指定されたSQLのすべての計画を計画ベースラインとして読み込みます。 |
| is_fixed | 読み込む計画ベースラインの`is_fixed`プロパティ。 |
| enabled | 読み込む計画ベースラインの`enabled`プロパティ。 |

#### 例

```sql
DECLARE
  v_load_plans number;
BEGIN
  v_load_plans := DBMS_SPM.LOAD_PLANS_FROM_CURSOR_CACHE(
     sql_id => '529F6E6454EF579C7CC265D1F6131D70',
     plan_hash_value => 13388268709115914355);
END;
/
```

### BATCH_LOAD_PLANS_FROM_CURSOR_CACHE

`BATCH_LOAD_PLANS_FROM_CURSOR_CACHE`は、Plan Cacheに存在するSQL計画を一括してSPMベースラインに追加するために使用されます。

Oracleモードの構文は次のとおりです：

```sql
PROCEDURE BATCH_LOAD_PLANS_FROM_CURSOR_CACHE (
    fixed                   IN VARCHAR  := 'NO',
  );
```

MySQLモードの構文は次のとおりです：

```sql
PROCEDURE BATCH_LOAD_PLANS_FROM_CURSOR_CACHE (
    is_fixed                VARCHAR(8) DEFAULT 'NO',
  );
```

#### パラメータの説明

| **パラメータ名** | **説明** |
| --- | --- |
| is_fixed | ベースラインの`is_fixed`プロパティを設定します。デフォルト値は`NO`であり、ベースラインを進化可能なベースラインに設定します。|

#### 例

```sql
call DBMS_SPM.BATCH_LOAD_PLANS_FROM_CURSOR_CACHE('no');
```

### BATCH_DROP_SQL_PLAN_BASELINE

`BATCH_DROP_SQL_PLAN_BASELINE`は、現在のテナントのすべての計画ベースラインを削除するために使用されます。

Oracleモードの構文は次のとおりです：

```sql
PROCEDURE BATCH_DROP_SQL_PLAN_BASELINE (
  degree         IN NUMBER   := NULL
);
```

MySQLモードの構文は次のとおりです：

```sql
PROCEDURE BATCH_DROP_SQL_PLAN_BASELINE(
  degree            DECIMAL(20,0)  DEFAULT NULL
);
```

#### パラメータの説明

| **パラメータ名** | **説明** |
| --- | --- |
|degree|一括削除プロセスにおいて、削除操作でパラレル処理を有効にするかどうかを指定します。|

#### 例

```sql
call BATCH_DROP_SQL_PLAN_BASELINE();
```

### HANDLE_SPM_STATS_JOB_PROC

`HANDLE_SPM_STATS_JOB_PROC`プロシージャは、期限切れのSPM統計情報を毎日クリーンアップするために使用されます。

#### 構文

Oracleモードの構文は次のとおりです：

```sql
PROCEDURE HANDLE_SPM_STATS_JOB_PROC;
```

MySQLモードの構文は次のとおりです：

```sql
PROCEDURE HANDLE_SPM_STATS_JOB_PROC();
```

#### 説明

このストアドプロシージャは、期限切れのSPM統計情報を毎日クリーンアップするために使用されます。内部テーブル内のデータは15日間保持され、2000件ずつバッチで削除されます。

### CREATE_STGTAB_BASELINE

`CREATE_STGTAB_BASELINE`プロシージャは、ベースライン計画を保存するキャッシュテーブルを作成するために使用されます。

#### 構文

Oracleモードの構文は次のとおりです：

```sql
PROCEDURE CREATE_STGTAB_BASELINE (
  table_name        IN VARCHAR2,
  table_owner       IN VARCHAR2 := NULL,
  tablespace_name   IN VARCHAR2 := NULL
);
```

MySQLモードの構文は次のとおりです：

```sql
PROCEDURE CREATE_STGTAB_BASELINE (
  table_name        VARCHAR(65535),
  table_owner       VARCHAR(65535) DEFAULT NULL,
  tablespace_name   VARCHAR(65535) DEFAULT NULL
);
```

#### パラメータの説明

| **パラメータ名** | **説明** |
| --- | --- |
| table_name | キャッシュテーブルのテーブル名を指定します。 |
| table_owner | キャッシュテーブルが属するユーザーを指定します。指定しない場合、またはNULLを指定した場合は現在のユーザーを表します。 |
| tablespace_name | キャッシュテーブルが属するtablespaceを指定します。実際には使用されず、互換性のためだけに使用されます。 |

### PACK_STGTAB_BASELINE

`PACK_STGTAB_BASELINE`プロシージャは、ベースライン計画をキャッシュテーブルにエクスポートするために使用されます。

#### 構文

Oracleモードの構文は次のとおりです：

```sql
PROCEDURE PACK_STGTAB_BASELINE (
  table_name       IN VARCHAR2,
  table_owner      IN VARCHAR2 := NULL,
  baseline_owner   IN VARCHAR2 := NULL,
  sql_id           IN VARCHAR2 := NULL,
  plan_hash        IN NUMBER   := NULL,
  sql_text         IN CLOB     := NULL,
  origin           IN VARCHAR2 := NULL,
  enabled          IN VARCHAR2 := NULL,
  accepted         IN VARCHAR2 := NULL,
  fixed            IN VARCHAR2 := NULL
);
```

MySQLモードの構文は次のとおりです：

```sql
PROCEDURE PACK_STGTAB_BASELINE (
  table_name        VARCHAR(65535),
  table_owner       VARCHAR(65535) DEFAULT NULL,
  baseline_owner    VARCHAR(65535) DEFAULT NULL,
  sql_id            VARCHAR(65535) DEFAULT NULL,
  plan_hash         INT UNSIGNED   DEFAULT NULL,
  sql_text          LONGTEXT       DEFAULT NULL,
  origin            VARCHAR(65535) DEFAULT NULL,
  enabled           VARCHAR(65535) DEFAULT NULL,
  accepted          VARCHAR(65535) DEFAULT NULL,
  fixed             VARCHAR(65535) DEFAULT NULL
);
```

#### パラメータの説明

| **パラメータ名** | **説明** |
| --- | --- |
| table_name | キャッシュテーブルのテーブル名を指定します。 |
| table_owner | キャッシュテーブルが属するユーザーを指定します。指定されていない場合、またはNULLを指定した場合は、現在のユーザーを表します。 |
| baseline_owner | どのユーザーのベースラインをエクスポートするかを指定します。指定されていない場合、またはNULLを指定した場合は、現在のユーザーを表します。 |
| sql_id | 特定のSQL IDのベースラインをエクスポートするように指定します。指定されていない場合、またはNULLを指定した場合は、すべてのSQL IDのベースラインをエクスポートします。 |
| plan_hash | 特定のプランハッシュのベースラインをエクスポートするように指定します。指定されていない場合、またはNULLを指定した場合は、すべてのプランハッシュのベースラインをエクスポートします。 |
| sql_text | 特定のSQLテキストのベースラインをエクスポートするように指定します。% を使用してあいまい一致を実行できます。指定されていない場合、またはNULLを指定した場合は、すべてのSQLテキストのベースラインをエクスポートします。 |
| origin | 特定のオリジンのベースラインをエクスポートするように指定します。`AUTO-CAPTURE`、`MANUAL-LOAD` のみを指定できます。指定されていない場合、またはNULLを指定した場合は、すべてのオリジンのベースラインをエクスポートします。 |
| enabled | enabledが特定の状態のベースラインをエクスポートするように指定します。`YES`、`NO` のみを指定できます。指定されていない場合、またはNULLを指定した場合は、すべてのenabled状態のベースラインをエクスポートします。 |
| accepted | acceptedが特定の状態のベースラインをエクスポートするように指定します。`YES`、`NO` のみを指定できます。指定されていない場合、またはNULLを指定した場合は、すべてのaccepted状態のベースラインをエクスポートします。 |
| fixed | fixedが特定の状態のベースラインをエクスポートするように指定します。`YES`、`NO` のみを指定できます。指定されていない場合、またはNULLを指定した場合は、すべてのfixed状態のベースラインをエクスポートします。 |

### UNPACK_STGTAB_BASELINE

`UNPACK_STGTAB_BASELINE`プロシージャは、キャッシュテーブル内のベースライン計画を内部テーブルにインポートするために使用されます。

#### 構文

Oracleモードの構文は次のとおりです：

```sql
PROCEDURE UNPACK_STGTAB_BASELINE (
  table_name       IN VARCHAR2,
  table_owner      IN VARCHAR2 := NULL,
  baseline_owner   IN VARCHAR2 := NULL,
  sql_id           IN VARCHAR2 := NULL,
  plan_hash        IN NUMBER   := NULL,
  sql_text         IN CLOB     := NULL,
  origin           IN VARCHAR2 := NULL,
  enabled          IN VARCHAR2 := NULL,
  accepted         IN VARCHAR2 := NULL,
  fixed            IN VARCHAR2 := NULL
);
```

MySQLモードの構文は次のとおりです：

```sql
PROCEDURE UNPACK_STGTAB_BASELINE (
  table_name        VARCHAR(65535),
  table_owner       VARCHAR(65535) DEFAULT NULL,
  baseline_owner    VARCHAR(65535) DEFAULT NULL,
  sql_id            VARCHAR(65535) DEFAULT NULL,
  plan_hash         INT UNSIGNED   DEFAULT NULL,
  sql_text          LONGTEXT       DEFAULT NULL,
  origin            VARCHAR(65535) DEFAULT NULL,
  enabled           VARCHAR(65535) DEFAULT NULL,
  accepted          VARCHAR(65535) DEFAULT NULL,
  fixed             VARCHAR(65535) DEFAULT NULL
);
```

#### パラメータの説明

| **パラメータ名** | **説明** |
| --- | --- |
| table_name | キャッシュテーブルのテーブル名を指定します。 |
| table_owner | キャッシュテーブルが属するユーザーを指定します。指定されていない場合、またはNULLを指定した場合は、現在のユーザーを表します。 |
| baseline_owner | インポートベースラインをどのユーザーに設定するかを指定します。指定されていない場合、またはNULLを指定した場合は、現在のユーザーを表します。 |
| sql_id | 特定のSQL IDのベースラインをインポートするように指定します。指定されていない場合、またはNULLを指定した場合は、すべてのSQL IDのベースラインをインポートすることを意味します。 |
| plan_hash | 特定のプランハッシュのベースラインをインポートするように指定します。指定されていない場合、またはNULLを指定した場合は、すべてのプランハッシュのベースラインをインポートすることを意味します。 |
| sql_text | 特定のSQLテキストのベースラインをインポートするように指定します。% を使用してあいまい一致を実行できます。指定されていない場合、またはNULLを指定した場合は、すべてのSQLテキストのベースラインをインポートすることを意味します。 |
| origin | 特定のオリジンのベースラインをインポートするように指定します。`AUTO-CAPTURE`、`MANUAL-LOAD` のみを指定できます。指定されていない場合、またはNULLを指定した場合は、すべてのオリジンのベースラインをインポートすることを意味します。 |
| enabled | enabledが特定の状態のベースラインをインポートするように指定します。`YES`、`NO` のみを指定できます。指定されていない場合、またはNULLを指定した場合は、すべてのenabled状態のベースラインをインポートすることを意味します。 |
| accepted | acceptedが特定の状態のベースラインをインポートするように指定します。`YES`、`NO` のみを指定できます。指定されていない場合、またはNULLを指定した場合は、すべてのaccepted状態のベースラインをインポートすることを意味します。 |
| fixed | fixedが特定の状態のベースラインをインポートするように指定します。`YES`、`NO` のみを指定できます。指定されていない場合、またはNULLを指定した場合は、すべてのfixed状態のベースラインをインポートすることを意味します。 |

### PACK_CSV_BASELINE

`PACK_CSV_BASELINE` プロシージャは、ベースライン計画をCSVファイルにエクスポートするために使用されます。

#### 構文

Oracleモードの構文は次のとおりです：

```sql
PROCEDURE PACK_CSV_BASELINE (
  detail_file_path  IN VARCHAR2,
  overall_file_path IN VARCHAR2,
  baseline_owner    IN VARCHAR2 := NULL
);
```

MySQLモードの構文は次のとおりです：

```sql
PROCEDURE PACK_CSV_BASELINE (
  detail_file_path  VARCHAR(65535),
  overall_file_path VARCHAR(65535),
  baseline_owner    VARCHAR(65535) DEFAULT NULL
);
```

#### パラメータの説明

| **パラメータ名** | **説明** |
| --- | --- |
| detail_file_path | `__all_plan_baseline_item` の内容をエクスポートするファイルの絶対パスを指定します。CSVファイルをエクスポートする際は既存のファイルを上書きできないため、存在しないファイルを指定する必要があります。 |
| overall_file_path | `__all_plan_baseline` の内容をエクスポートするファイルの絶対パスを指定します。CSVファイルをエクスポートする際は既存のファイルを上書きできないため、存在しないファイルを指定する必要があります。 |
| baseline_owner | エクスポートするベースラインのユーザーを指定します。指定されていない場合やNULLを指定した場合は、現在のユーザーを表します。 |

### UNPACK_CSV_BASELINE

`UNPACK_CSV_BASELINE` プロシージャは、CSVファイル内のベースラインを内部テーブルにインポートするために使用されます。

#### 構文

Oracleモードの構文は次のとおりです：

```sql
PROCEDURE UNPACK_CSV_BASELINE (
  detail_file_path  IN VARCHAR2,
  overall_file_path IN VARCHAR2,
  baseline_owner    IN VARCHAR2 := NULL
);
```

MySQLモードの構文は次のとおりです：

```sql
PROCEDURE UNPACK_CSV_BASELINE (
  detail_file_path  VARCHAR(65535),
  overall_file_path VARCHAR(65535),
  baseline_owner    VARCHAR(65535) DEFAULT NULL
);
```

#### パラメータの説明

| **パラメータ名** | **説明** |
| --- | --- |
| detail_file_path | `__all_plan_baseline_item` のデータをインポートするファイルを絶対パスで指定します。 |
| overall_file_path | `__all_plan_baseline` のデータをインポートするファイルを絶対パスで指定します。 |
| baseline_owner | ベースラインをどのユーザーにインポートするかを指定します。指定されていない場合、またはNULLが指定された場合は現在のユーザーを表します。 |