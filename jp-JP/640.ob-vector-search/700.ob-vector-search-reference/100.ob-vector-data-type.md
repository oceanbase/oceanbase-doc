| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |
|slug|vector-data-type|

# ベクトルデータ型の概要

OceanBaseは、AIベクトル検索関連のアプリケーションをサポートするためにベクトルデータ型を提供します。ベクトルデータ型を使用することで、例えば`[0.1, 0.3, -0.9, ...]`のような浮動小数点数の配列を格納およびクエリできます。配列はシングルクォートで囲みます。ベクトルデータを使用する前に、以下の点をご確認ください：

* 密ベクトルと疎ベクトルがサポートされており、どちらもデータ要素は単精度浮動小数点数です。
* ベクトルデータの要素値は、非数値(NaN)や無限大(Inf)を含むことはできません。含む場合はランタイムエラーが発生します。
* ベクトル列を作成する際には、ベクトルの次元を指定する必要があります。例：`VECTOR(3)`。
* 密ベクトルインデックスの作成がサポートされています。詳細は[密ベクトルインデックス](../200.ob-vector-index/300.ob-dense-vector-index/100.ob-dense-vector-index-overview.md)を参照してください。
* OceanBaseにおけるベクトルデータは配列形式で格納されます。
* 密ベクトルと疎ベクトルはどちらも[ハイブリッド検索](../350.ob-vector-hybrid-search/200.ob-vector-index-hybrid-search.md)をサポートします。

## 構文

密ベクトル値は、16000個以内の任意の数の浮動小数点数を含むことができます。構文は以下のとおりです：

```sql
-- 密ベクトル
'[<float>, <float>, ...]'
```

疎ベクトルはMAPデータ型に基づいており、順序のないキーと値のペアを含みます。構文は以下のとおりです：

```sql
-- 疎ベクトル
'{<uint:float>, <uint:float>...}'
```

ベクトル列とインデックスの作成例は以下のとおりです：

```sql
-- 密ベクトル列とインデックスの作成
CREATE TABLE t1(
    c1 INT,
    c2 VECTOR(3),
    PRIMARY KEY(c1),
    VECTOR INDEX idx1(c2) WITH (distance=L2, type=hnsw_sq)
    );
```

```sql
-- 疎ベクトル列の作成
CREATE TABLE t2 (
    c1 INT,
    c2 SPARSEVECTOR
    );
```

## 関連ドキュメント

* [ベクトルインデックス](../200.ob-vector-index/100.ob-vector-index-overview.md)
* [SQL関数の使用](../250.ob-vector-function.md)