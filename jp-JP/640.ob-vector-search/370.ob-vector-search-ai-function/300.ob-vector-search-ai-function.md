|keywords||
|dir-name||
|dir-name-en||
|tenant-type|MySQL Mode|
|slug|vector-search-ai-function|

# AI関数サービスの構文と使用例

このドキュメントは、AI関数サービスのリファレンスドキュメントであり、OceanBaseデータベースにおけるAI Functionの機能、各関数の構文とパラメータ、さまざまな使用例について説明します。

AI関数はSQL式を通じて、AIモデルの能力をデータベース内のデータ処理に直接統合します。これにより、AI大規模モデルを用いたデータの抽出、分析、要約、保存などの操作が大幅に簡素化され、現在のデータベースおよびデータウェアハウス分野における重要な新機能です。MySQLモードでは、OceanBaseデータベースは`DBMS_AI_SERVICE`パッケージを通じてAIモデルとエンドポイントの管理を提供し、いくつかの組み込みAI関数式を追加しています。また、ビューを通じてAIモデルの呼び出し状況を監視することもサポートしています。

## 前提条件

* AIモデルに関する権限を既に保有していること。詳細については、[AI関数サービスの権限](150.ob-vector-search-ai-function-permission.md)を参照してください。
* AIモデルとエンドポイントがまだ登録されていない場合は、[AIモデルの登録](200.ob-vector-search-ai-function-register-model.md)に従って、まず登録を完了してください。

## 注意事項

* `CREATE AI MODEL` および `DROP AI MODEL` 操作はプライマリテナントとスタンバイテナント間で同期されますが、`CREATE AI MODEL ENDPOINT`、`ALTER AI MODEL ENDPOINT`、`DROP AI MODEL ENDPOINT` 操作は同期されません。そのため、スタンバイテナントではAI関数サービスを使用するために、AIモデルのエンドポイントを手動で設定する必要があります。
* ハイブリッド検索（Hybrid Search）は、AI関数サービスのモデル管理および埋め込み（embedding）機能に依存しています。AIモデルを削除する際は、潜在的な問題を避けるために、それがハイブリッド検索で参照されていないか確認する必要があります。

## AI関数式

OceanBaseデータベースは以下のAI関数式をサポートしており、SQLステートメントを通じてOceanBaseデータベース内部で直接AIモデルを呼び出すことができます。これにより、呼び出しプロセスが簡素化されます：

| 名前 | 説明 |
|-----|------|
|`AI_COMPLETE`|指定されたテキスト生成大規模言語モデルを呼び出し、プロンプトとデータ情報を処理し、処理結果を解析します。|
|`AI_EMBED`|埋め込みモデル（Embedding Model）を呼び出して、テキストデータをベクトルデータに変換します。|
|`AI_RERANK`|再ランキングモデル（Rerank Model）を呼び出し、プロンプトに基づいてテキストの類似度順に並べ替えます。|

<main id="notice" type='notice'>
<h4>注意</h4>
<p>AI関数式を使用する際は、データベースにAIモデルおよびエンドポイント情報が登録されていることを確認する必要があります。登録手順については、<a href="200.ob-vector-search-ai-function-register-model.md">AIモデルの登録</a>を参照してください。以下の例で使用されているモデルキーは <code>ob_complete</code>、<code>ob_embed</code>、<code>ob_rerank</code> です。</p>
</main>

### AI_COMPLETE

`AI_COMPLETE` 関数は `model_key` を使用して、登録済みのテキスト生成大規模言語モデル（LLM）を指定し、ユーザーが提供したプロンプト（prompt）とデータを処理し、大規模言語モデルが生成したテキスト情報を返します。ユーザーは `prompt` パラメータで、プロンプトとデータベース内のデータ形式をカスタマイズできます。この方法は、テキストデータの多様な処理をサポートするだけでなく、データベース内部でのバッチ処理も可能にし、データベースと大規模言語モデル間でのデータのやり取りに伴うコピーオーバーヘッドを効果的に回避します。

#### 使用方法

構文は以下のとおりです：

```sql
AI_COMPLETE(model_key, prompt[, parameters])
```

パラメータ説明：

| パラメータ | 説明 | 型 | NULL許容 |
|------|------|------|----------|
| model_key | データベース内に登録されたモデル。 | VARCHAR(128) | No |
| prompt | ユーザーが入力したプロンプト情報。 | VARCHAR/TEXT(LONGTEXT) | No |
| parameters | APIが提供するオプションを設定するために使用されます。モデルのオプションフィールドは、生成されたメッセージボディに直接含まれます。ベンダーによって若干の違いがあります。一般的に、よく見られるオプションには以下のものがあります：`temperature`、`top_p`、`max_tokens`。通常はデフォルト設定で十分なので、指定する必要はありません。 | JSON | Yes |

`model_key` と `prompt` のどちらか一方を `NULL` に指定する必要があります。両方とも `NULL` にすることはできません。指定しない場合、関数はエラーを返します。

戻り値：

* text、プロンプトに基づいて大規模言語モデルが生成したテキスト。

#### 例

1. 感情分析の例

    ```sql
    SELECT AI_COMPLETE("ob_complete","あなたのタスクは、提供されたテキストに対して感情分析を行い、その感情の傾向がポジティブかネガティブかを判断することです。
    以下が分析対象のテキストです：
    <text>
    今日は本当にいい天気だね
    </text>
    判断基準は以下の通りです：
    テキストがポジティブな感情を表現している場合は1を出力し、ネガティブな感情を表現している場合は-1を出力します。他のものは出力しないでください。\n") AS ans;
    ```

    実行結果は次のとおりです：

    ```sql
    +-----+
    | ans |
    +-----+
    | 1   |
    +-----+
    ```

2. 翻訳の例

    ```sql
    CREATE TABLE comments (
        id INT AUTO_INCREMENT PRIMARY KEY,
        content TEXT
    );

    INSERT INTO comments (content) VALUES ('hello world!');

    -- concat式を使用して処理したデータをデータベースのテーブル列名に置き換えることで、データをデータベースから大規模言語モデルにコピーし、その結果を再びデータベースにコピーする必要なく、データベース内のデータを一括処理できます。
    SELECT AI_COMPLETE("ob_complete",
    concat("あなたは翻訳の達人です。以下の英語を中国語に翻訳してください。以下が翻訳対象のテキストです：<text>",
        content,
        "</text>")) AS ans FROM comments;
    ```
    実行結果は次のとおりです：

    ```sql
    +-------------+
    | ans         |
    +-------------+
    | 你好，世界！  |
    +-------------+
    ```

3. 分類の例

    ```sql
    SELECT AI_COMPLETE("ob_complete","あなたは分類の達人です。一連の質問テキストを受け取り、それらの質問が属するカテゴリを判断してください。カテゴリのリストは[\"ハードウェア部\",\"ソフトウェア部\",\"その他\"]です。以下が分析対象のテキストです：
    <text>
    この画面の品質は本当に悪い
    </text>") AS res;
    ```

    実行結果は次のとおりです：

    ```sql
    +--------+
    | res    |
    +--------+
    | ハードウェア部 |
    +--------+
    ```

### AI_EMBED

`AI_EMBED` 関数は、`model_key` を使用して登録済みの埋め込みモデル（Embedding Model）を指定し、ユーザーが提供するテキストデータをベクトルデータに変換します。モデルが複数の次元をサポートする場合、`dim` パラメータで出力する次元のベクトルを指定できます。

#### 使用方法

構文は以下のとおりです：

```sql
AI_EMBED(model_key, input, [dim])
```

パラメータ説明：

| パラメータ | 説明 | 型 | NULL許容 |
|------|------|------|----------|
| model_key | データベース内に登録されたモデル。 | VARCHAR(128) | No |
| input | 変換したいユーザー入力のテキストデータ。 | VARCHAR | No |
| dim | 出力する次元のベクトルを指定します。一部の大規模言語モデルベンダーが提供するAPIでは、複数の次元を設定できます。 | INT64 | Yes |

`model_key` と `input` のどちらか一方が `NULL` の場合、関数はエラーを返します。

戻り値：

* ベクトル形式の文字列。これは、埋め込みモデルがテキストに基づいて変換したベクトルです。

#### 例

1. 単一行データの埋め込み

    ```sql
    SELECT AI_EMBED("ob_embed","Hello world") AS embedding;
    ```

    実行結果は次のとおりです：

    ```sql
    +----------------+
    | embedding      |
    +----------------+
    | [0.1, 0.2, 0.3]|
    +----------------+
    ```

2. 表の列への埋め込み

    ```sql
    CREATE TABLE comments (
        id INT AUTO_INCREMENT PRIMARY KEY,
        content TEXT
    );

    INSERT INTO comments (content) VALUES ('hello world!');

    SELECT AI_EMBED("ob_embed",content) AS embedding FROM comments;
    ```

    実行結果は次のとおりです：

    ```sql
    +----------------+
    | embedding      |
    +----------------+
    | [0.1, 0.2, 0.3]|
    +----------------+
    ```

### AI_RERANK

`AI_RERANK` 関数は、`model_key` を使用して登録済みのリランキングモデル（Rerank Model）を指定し、ユーザーが提供するクエリワードとドキュメントリストをベンダーのルールに従って整理して指定されたモデルに送信します。モデルが返すソート結果を解析して返すもので、RAG の rerank シナリオに適しています。

#### 使用方法

構文は以下のとおりです：

```sql
AI_RERANK(model_key, query, documents[, document_key])
```

パラメータ説明：

| パラメータ | 説明 | 型 | NULL許容 |
|------|------|------|----------|
| model_key | データベース内に登録されたモデル。 | VARCHAR(128) | No |
| query | ユーザーが入力したテキスト。 | VARCHAR(1024) | No |
| documents | ユーザーが入力したドキュメントリスト。 | JSON ARRAY、例：`['apple', 'banana']` | No |

`model_key`、`query`、`documents` は必ず指定する必要があり、いずれか一方が `NULL` の場合、関数はエラーを返します。

戻り値：

* JSON配列。リランキングモデルが返したドキュメントとその関連性スコアを含み、関連性スコアの降順で並べ替えられています。

#### 例

```sql
SELECT AI_RERANK("ob_rerank","Apple",'["apple","banana","fruit","vegetable"]');
```

実行結果は次のとおりです：

```sql
+-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
| ai_rerank("ob_rerank","Apple",'["apple","banana","fruit","vegetable"]')                                                                                                                                                                                                                                                                                   |
+-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
| [{"index": 0, "document": {"text": "apple"}, "relevance_score": 0.9912109375}, {"index": 1, "document": {"text": "banana"}, "relevance_score": 0.0033512115478515625}, {"index": 2, "document": {"text": "fruit"}, "relevance_score": 0.0003669261932373047}, {"index": 3, "document": {"text": "vegetable"}, "relevance_score": 0.00001996755599975586}] |
+-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
```

## AI モデル情報の確認

OceanBaseデータベースでは、ビューを通じて登録済みのAIモデルおよびAIモデルエンドポイント情報を確認できます。詳細については、以下を参照してください：

* [CDB/DBA_OB_AI_MODELS](../../700.reference/700.system-views/400.system-view-of-mysql-mode/200.dictionary-view-of-mysql-mode/5720.o-dba_ob_ai_models-of-mysql-mode.md)：AIモデル情報を確認する。
* [CDB/DBA_OB.AI_MODEL_ENDPOINTS](../../700.reference/700.system-views/400.system-view-of-mysql-mode/200.dictionary-view-of-mysql-mode/5710.o-dba_ob_ai_model_endpoints-of-mysql-mode.md)：AIモデルエンドポイント情報を確認する。

## 関連ドキュメント

* [AI モデルの登録](200.ob-vector-search-ai-function-register-model.md)：モデルとエンドポイントを登録する完全なコマンド。
* [AI 関数サービスのクイックスタート](100.ob-vector-search-ai-function-quick-start.md)：初回使用ガイド。登録から最初のサンプルを実行するまでの最小手順。
* [ベクトル埋め込み技術](../150.ob-vector-search-generate-vector-embeddings.md)
* [MySQLモードの権限分類](../../600.manage/500.security-and-permissions/300.access-control/200.user-and-permission/200.permission-of-mysql-mode/100.permission-classification-of-mysql.md)