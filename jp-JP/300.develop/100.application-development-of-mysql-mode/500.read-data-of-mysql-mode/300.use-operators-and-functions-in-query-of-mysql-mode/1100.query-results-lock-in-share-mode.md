|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type|MySQL Mode|
|machine-translation|ja-marker|
|slug|use-select-lock-share-mode-to-lock-query-results-mysql-compatible-mode|

# クエリ結果のロック LOCK IN SHARE MODE

`SELECT ... LOCK IN SHARE MODE`は、データをクエリする際に共有ロックを取得し、他のトランザクションによるデータへの書き込み操作を防ぎますが、他のトランザクションによる読み取り操作は許可します。このステートメントを使用すると、読み取ったデータ行に共有モードのロックが設定され、他のセッションはこれらの行を読み取ることができますが、現在のトランザクションがコミットされるまで他のトランザクションはそれを変更できません。

<main id="notice" type='notice'>
<h4>注意</h4>
<p>OceanBaseデータベースは、<code>LOCK IN SHARE MODE</code>共有ロック構文の機能をシミュレートしており、書き込みロックを使用して実現しています。これにより、一部のソフトウェアの互換性要件を満たしつつ、構文の正確性も保証されます。ただし、書き込みロックを使用すると、<code>LOCK IN SHARE MODE</code>コマンドを追加した読み取り操作間で相互にブロックされるため、一般的にはこの構文の使用は推奨されません。特にパフォーマンスが重要なシナリオでは、使用を避けるべきです。</p>
</main>

本記事では、例を通じて`SELECT ... LOCK IN SHARE MODE`を使用してクエリ結果をロックする方法を紹介します。

## 例

1. **セッション1**で、以下のSQLステートメントを実行してトランザクションを開始します。

    ```sql
    START TRANSACTION;
    ```

2. **セッション1**で、`LOCK IN SHARE MODE`構文を使用して、テーブル`test_tbl1`内の`id`値が`1`に等しいレコードに対するクエリを実行し、共有ロックを取得します。

    ```sql
    SELECT * FROM test_tbl1 WHERE id = 1 LOCK IN SHARE MODE;
    ```

    実行結果は次のとおりです：

    ```shell
    +------+------+
    | id   | name |
    +------+------+
    |    1 | A1   |
    +------+------+
    1 row in set
    ```

3. **セッション2**で、以下のSQLステートメントを実行してトランザクションを開始します。

    ```sql
    START TRANSACTION;
    ```

4. **セッション2**で、以下のSQLステートメントを実行して、テーブル`test_tbl1`内の`id`値が`1`に等しいレコードを照会します。

    ```sql
    SELECT * FROM test_tbl1 WHERE id = 1;
    ```

    実行結果は次のとおりです：

    ```shell
    +------+------+
    | id   | name |
    +------+------+
    |    1 | A1   |
    +------+------+
    1 row in set
    ```

5. **セッション2**で、以下のSQLステートメントを実行して、テーブル`test_tbl1`内の`id`値が`1`に等しい行のnameを'A1A1A1'に変更します。このSQLステートメントは、**セッション1**のトランザクションがロールバックされるか、または`COMMIT`されるまで待機します。

    ```sql
    UPDATE test_tbl1 SET name = 'A1A1A1' WHERE id = 1;
    ```

    実行結果は次のとおりです：

    ```shell
    ERROR 1205 (HY000): Lock wait timeout exceeded; try restarting transaction
    ```

6. **セッション1**で、以下のSQLステートメントを実行してトランザクションをコミットします。

    ```sql
    COMMIT;
    ```

7. **セッション2**で、再度**ステップ4**の更新ステートメントを実行します。

    ```sql
    UPDATE test_tbl1 SET name = 'A1A1A1' WHERE id = 1;
    ```

    実行結果は次のとおりです：

    ```shell
    Query OK, 1 row affected
    Rows matched: 1  Changed: 1  Warnings: 0
    ```

## 関連ドキュメント

[SELECT](../../../../700.reference/500.sql-reference/100.sql-syntax/200.common-tenant-of-mysql-mode/600.sql-statement-of-mysql-mode/8100.select-of-mysql-mode/100.select-of-mysql-mode.md)