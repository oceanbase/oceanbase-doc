|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type|MySQL Mode|
|machine-translation|ja-marker|
|slug|update-data-mysql-compatible-mode|

# データの更新

テーブルにデータを挿入した後、`UPDATE` ステートメントやその他のステートメントを使用して、テーブルの行レコードを更新できます。本記事では、関連するステートメントの使用方法と例を紹介します。

## データ更新の準備

テーブルデータを更新する前に、以下の事項を確認してください：

* データベースのMySQLテナントに接続されていることを確認してください。データベースへの接続方法については、[接続方法の概要](../100.connect-to-oceanbase-database-of-mysql-mode/100.connection-methods-overview-of-mysql-mode.md)を参照してください。

  <main id="notice" type='explain'>
   <h4>説明</h4>
   <p>現在ログインしているテナントが属するテナントモードは、<code>sys</code>テナントで<code>oceanbase.DBA_OB_TENANTS</code>ビューを照会することで確認できます。</p>
  </main>

* 操作対象のテーブルに対する`UPDATE`権限を保有していることを確認してください。現在のユーザー権限に関する操作については、[ユーザー権限の確認](../../../600.manage/500.security-and-permissions/300.access-control/200.user-and-permission/200.permission-of-mysql-mode/400.view-user-permissions-of-mysql-mode.md)を参照してください。該当する権限がない場合は、管理者に連絡し権限付与を依頼してください。ユーザー権限に関する操作については、[直接権限付与](../../../600.manage/500.security-and-permissions/300.access-control/200.user-and-permission/200.permission-of-mysql-mode/200.authority-of-mysql-mode.md)を参照してください。

## UPDATEステートメントを使用したデータの更新

通常、テーブルデータを更新するには[UPDATE](../../../700.reference/500.sql-reference/100.sql-syntax/200.common-tenant-of-mysql-mode/600.sql-statement-of-mysql-mode/9000.update-of-mysql-mode.md)ステートメントを使用します。

`UPDATE` ステートメントの構文は次のとおりです：

```sql
UPDATE table_name
SET column_name = value [, column_name = value]...
[ WHERE condition ];
```

|                      パラメータ                       | 必須   |                   説明                    |
|-----------------------------------------------|------|-----------------------------------------|
| table_name                                  | はい    | 更新対象のテーブルを指定します。                              |
| column_name = value [, column_name = value] | はい    | 更新対象の列を指定します。等号（=）の後ろに続くvalueが更新後の目標値です。         |
| [ WHERE condition ]                         | いいえ    | 条件句は、更新する行レコードが満たす必要がある条件を指定します。省略した場合、更新対象のテーブルの該当列のすべてのレコードを更新することを意味します。 |

### すべてのレコードを更新する

`t_insert` テーブルの `value` 列のすべての値を更新し、すべての値に1を加算します。例：

```sql
obclient [test]> UPDATE t_insert SET value = value+1;
Query OK, 4 rows affected
Rows matched: 4  Changed: 4  Warnings: 0

obclient [test]> SELECT * FROM t_insert;
+----+------+-------+---------------------+
| id | name | value | gmt_create          |
+----+------+-------+---------------------+
|  1 | CN   | 10002 | 1970-01-01 17:18:06 |
|  2 | US   | 10003 | 1970-01-01 17:18:47 |
|  3 | EN   | 10004 | 1970-01-01 17:18:47 |
|  4 | JP   | 10005 | 1970-01-01 17:28:21 |
+----+------+-------+---------------------+
4 rows in set
```

`UPDATE` ステートメントを実行する際は、トランザクションが過大にならないように注意する必要があります。`LIMIT` キーワードを使用して数を制限するか、`WHERE` キーワードを使用して範囲を制限できます。これは、条件なしでデータを更新する場合、レコード数が数十万または数百万に達すると、大規模なトランザクションが発生し、実行失敗につながる可能性があるためです。

### 一部のレコードを更新する

テーブルデータを更新する際には、一部のレコードのみを更新することもできます。`t_insert` テーブルのレコード数が多い場合、以下の例のように `LIMIT` キーワードを追加して更新するレコード数を制御したり、`WHERE` キーワードを追加して更新範囲を制御したりします。

例：

* `LIMIT` を使用して、`t_insert` テーブルの最初の 100,000 行の `value` 列の値を `value+1` に更新します。

  ```sql
  obclient [test]> UPDATE t_insert SET value = value+1 LIMIT 100000;
  ```

* `where` を使用して、`t_insert` テーブルの `value` が `10005` のすべての列の値を `value+1` に更新します。

  ```sql
  obclient [test]>  UPDATE t_insert SET value = value+1 where value = 10005;
  ```

* `t_insert` テーブルの `id = 3` の行の `name` 列の値を `UK` に更新します。

  ```sql
  obclient [test]> SELECT * FROM t_insert;
  +----+------+-------+---------------------+
  | id | name | value | gmt_create          |
  +----+------+-------+---------------------+
  |  1 | CN   | 10002 | 1970-01-01 17:18:06 |
  |  2 | US   | 10003 | 1970-01-01 17:18:47 |
  |  3 | EN   | 10004 | 1970-01-01 17:18:47 |
  |  4 | JP   | 10005 | 1970-01-01 17:28:21 |
  +----+------+-------+---------------------+
  4 rows in set

  obclient [test]> UPDATE t_insert SET name = 'UK' WHERE id = 3;
  Query OK, 0 rows affected

  obclient [test]> SELECT * FROM t_insert;
  +----+------+-------+---------------------+
  | id | name | value | gmt_create          |
  +----+------+-------+---------------------+
  |  1 | CN   | 10002 | 1970-01-01 17:18:06 |
  |  2 | US   | 10003 | 1970-01-01 17:18:47 |
  |  3 | UK   | 10004 | 1970-01-01 17:18:47 |
  |  4 | JP   | 10005 | 1970-01-01 17:28:21 |
  +----+------+-------+---------------------+
  4 rows in set
  ```

### レコードの一意性制約による更新競合

テーブルに一意性制約が設定されている場合、同一のレコードに更新しようとするとシステムはエラーを報告し、データの更新は失敗します。エラーの例は以下のとおりです。

```sql
obclient [test]> SELECT * FROM t_insert;
+----+------+-------+---------------------+
| id | name | value | gmt_create          |
+----+------+-------+---------------------+
|  1 | CN   | 10002 | 1970-01-01 17:18:06 |
|  2 | US   | 10003 | 1970-01-01 17:18:47 |
|  3 | EN   | 10004 | 1970-01-01 17:18:47 |
|  4 | JP   | 10005 | 1970-01-01 17:28:21 |
+----+------+-------+---------------------+
4 rows in set

obclient [test]> CREATE UNIQUE INDEX uk_name ON t_insert(name);
Query OK, 0 rows affected

obclient [test]> UPDATE t_insert SET name = 'US' where id = 3;
ERROR 1062 (23000): Duplicate entry 'US' for key 'uk_name'
```

## 他のステートメントを使用したデータの更新

明示的な `UPDATE` ステートメントに加えて、他のステートメントでもデータを更新できます。例えば、`INSERT` ステートメントでデータを挿入する際、制約違反が発生した場合は、`ON DUPLICATE KEY UPDATE` 句を使用して挿入データを更新データのステートメントに変換し、関連フィールドを更新できます。

`ON DUPLICATE KEY UPDATE` 句を使用して挿入データを更新データに変換する例：

```sql
  obclient [test]> SELECT * FROM t_insert;
  +----+------+-------+---------------------+
  | id | name | value | gmt_create          |
  +----+------+-------+---------------------+
  |  1 | CN   | 10001 | 2022-10-12 15:17:17 |
  |  2 | US   | 10002 | 2022-10-12 16:29:16 |
  |  3 | EN   | 10003 | 2022-10-12 16:29:26 |
  |  4 | JP   | 10004 | 2022-10-12 17:02:52 |
  +----+------+-------+---------------------+
  4 rows in set

obclient [test]> INSERT INTO t_insert(id, name, value) VALUES (3,'UK', 10003),(5, 'CN', 10005) ON DUPLICATE KEY UPDATE name = VALUES(name);
Query OK, 1 row affected

obclient [test]> SELECT * FROM t_insert;
+----+------+-------+---------------------+
| id | name | value | gmt_create          |
+----+------+-------+---------------------+
|  1 | CN   | 10001 | 2022-10-12 16:29:16 |
|  2 | US   | 10002 | 2022-10-12 15:17:17 |
|  3 | UK   | 10003 | 2022-10-12 16:29:26 |
|  4 | JP   | 10004 | 2022-10-12 17:02:52 |
|  5 | CN   | 10005 | 2022-10-12 17:27:46 |
+----+------+-------+---------------------+
5 rows in set
```

例では、`ON DUPLICATE KEY UPDATE name = VALUES(name)` という部分は、挿入されるデータがテーブル内の主キー値と重複する場合、テーブル内の競合行の元のデータである `(3,'EN', 10003)` の `name` 列の値を、現在挿入しようとしている `name` 列の値に更新することを意味します。

## 関連ドキュメント

* [データの挿入](../400.write-data-of-mysql-mode/100.insert-data-of-mysql-mode-in-develop.md)

* [データの削除](../400.write-data-of-mysql-mode/300.delete-data-of-mysql-mode-in-develop.md)

* [データの置換](../400.write-data-of-mysql-mode/400.replace-data-of-mysql-mode-in-develop.md)