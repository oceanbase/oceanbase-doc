|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||
|machine-translation|ja-marker|
|slug|deploy-odp-using-cli-optional|

# （オプション）OBProxyのデプロイ

OceanBaseデータベースプロキシ(OceanBase Database Proxy、略称：ODP、またはOBProxy)は、OceanBaseが提供するデータベースプロキシミドルウェアであり、接続プール管理、ロードバランシング、フェイルオーバーなどの機能に使用されます。OBProxyはオプションのコンポーネントであり、そのデプロイはアプリケーションのニーズとシナリオによって決定されます。

このセクションでは、RPMパッケージを使用してOBProxyをインストールする方法について説明します。

  <main id="notice" type='notice'>
    <h4>注意</h4>
    <ul>
    <li>OBProxyは任意のマシンにデプロイされると、<code>ip:port</code> を外部に公開することでOceanBaseのプロキシサービスを提供します。ユーザーはこの <code>ip:port</code> を通じてMySQLデータベースにアクセスするのと同様に、OceanBaseデータベースにもアクセスできます。一般的にはOBServerノードにデプロイすることを推奨します。</li>
    <li>1台のマシンには1つのOBProxyサービスのみをデプロイし、規定された <code>2883</code> ポートを使用することを推奨します。同一マシン上に複数のOBProxyサービスをデプロイする場合は、異なるポートと設定ファイルのパスを指定して区別する必要があります。ポートは <code>3306</code> または <code>1521</code> 、あるいはその他のポートにカスタマイズ可能です。</li>
    </ul>
  </main>

## 手順

### ステップ１：OBProxy RPMパッケージのインストール

ここで、`$rpm_dir` はRPMパッケージを格納するディレクトリを表し、`$rpm_name` はRPMパッケージの名前を表します。

```bash
[root@xxx /]# cd $rpm_dir
[root@xxx $rpm_dir]# rpm -ivh $rpm_name
```

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p>OBProxyソフトウェアは、デフォルトでディレクトリ <code>/opt/taobao/install</code> にインストールされます。他のディレクトリを指定する必要がある場合は、<code>--prefix</code> パラメータを設定して指定できます。例えば、<code>/home/admin</code> ディレクトリにインストールするには、次のように実行します：<code>rpm -ivh --prefix=/home/admin  $rpm_name</code>。</p>
</main>

例：

```bash
[root@xxx admin]# rpm -ivh obproxy-4.0.0-20230109154442.el7.x86_64.rpm
Preparing...                          ################################# [100%]
Updating / installing...
   1:obproxy-4.0.0-20230109154442.el7 ################################# [100%]
```

### ステップ2：ディレクトリの設定

1. （オプション）obproxyへのシンボリックリンクを作成します。

   <main id="notice" type='explain'>
     <h4>説明</h4>
     <p>obproxyへのシンボリックリンクを作成することで、バージョン情報を隠し、後続の操作を容易にします。</p>
   </main>

   obproxyのインストールディレクトリは`/opt/taobao/install`下にあります。obproxyプロセスのホームディレクトリは`/opt/taobao/install/obproxy`であり、これはシンボリックリンクであり、実際には対応するobproxyバージョンのソフトウェアディレクトリを指しています。

   ```bash
   [root@xxx admin]# su - admin
   -bash-4.2$ cd /opt/taobao/install
   -bash-4.2$ ls
   ajdk-8.3.6-b129  obproxy-4.0.0
   # obproxyへのシンボリックリンクを作成
   -bash-4.2$ sudo ln -s obproxy-4.0.0 obproxy
   [sudo] password for admin:
   -bash-4.2$ ll
   total 8
   drwxr-xr-x 9 root  root  4096 Sep 26 15:24 ajdk-8.3.6-b129
   lrwxrwxrwx 1 root  root    13 Jan 29 16:51 obproxy -> obproxy-4.0.0
   drwxr-xr-x 5 admin admin 4096 Jan 29 16:42 obproxy-4.0.0

   -- obproxyの所有者とグループをadminに変更します。
   [root@xxx install]# chown -R admin:admin obproxy
   [root@xxx install]# ll
   total 8
   drwxr-xr-x 9 root  root  4096 Sep 26 15:24 ajdk-8.3.6-b129
   lrwxrwxrwx 1 admin admin   13 Jan 29 16:51 obproxy -> obproxy-4.0.0
   drwxr-xr-x 5 admin admin 4096 Jan 29 16:42 obproxy-4.0.0
   ```

2. obproxyプロセスの実行ログディレクトリを作成します。

   obproxyプロセスの実行ログディレクトリは、シンボリックリンクを介して`/home/admin/logs/obproxy/log`を指します。

   ```bash
   [root@xxx admin]# su - admin
   #ログディレクトリを作成
   -bash-4.2$ mkdir -p /home/admin/logs/obproxy/log
   # obproxyプロセスディレクトリの状況を確認
   -bash-4.2$ tree /opt/taobao/install/obproxy
   /opt/taobao/install/obproxy
   |-- bin
   |   |-- obp_xflush.py
   |   |-- obproxy
   |   |-- obproxyd.sh
   |   `-- unzip.py
   |-- lib
   |   |-- libgpr.so.7
   |   |-- libgrpc++.so.1
   |   |-- libgrpc++_cronet.so.1
   |   |-- libgrpc.so.7
   |   |-- libgrpc_cronet.so.7
   |   |-- libobproxy_so.so
   |   |-- libprotobuf.so.18
   |   `-- libstdc++.so.6
   |-- log -> /home/admin/logs/obproxy/log
   |-- minidump -> /home/admin/logs/obproxy/minidump
   |-- start_obproxy.sh
   `-- tools
      |-- dump_syms
      |-- log4cplus.conf
      |-- minidump.sh
      |-- minidump_stackwalk
      `-- obproxy.sym

   3 directories, 20 files
   ```

### ステップ3：OBProxyアカウントの初期化

OBProxyは、バックエンドのOBServerノードとの通信を維持する必要があります。そのため、事前にOceanBaseクラスタのsysテナント内でOBProxy用の接続ユーザー（proxyro）とパスワードを作成する必要があります。その後、OBProxy起動時にproxy起動パラメータを設定することで、proxyに指示を送ることができます。

  <main id="notice" type='explain'>
    <h4>説明</h4>
    <p>proxyroユーザーは、OBProxyがOceanBaseクラスタにアクセスするためのユーザーです。1つのOceanBaseクラスタに対して1つのproxyroアカウントが対応します。</p>
  </main>

例：

proxyroユーザーを作成し、`SELECT`権限を付与します。

```sql
$obclient -hxxx.xxx.xxx.1 -P2881 -uroot@sys -p******

obclient [(none)]> CREATE USER proxyro IDENTIFIED BY '******';
Query OK, 0 rows affected

obclient [(none)]> GRANT SELECT ON *.* TO proxyro;
Query OK, 0 rows affected

obclient [(none)]> SHOW GRANTS FOR proxyro;
+----------------------------------+
| Grants for proxyro@%             |
+----------------------------------+
| GRANT SELECT ON *.* TO 'proxyro' |
+----------------------------------+
1 row in set
```

### ステップ4：OBProxyを起動する

  <main id="notice" type='notice'>
    <h4>注意</h4>
    <p>OBProxyを起動する際は、adminユーザーであり、かつOBProxyソフトウェアのホームディレクトリ内で実行してください。他のユーザーまたは他のディレクトリから起動すると問題が発生する可能性があります。</p>
  </main>

1. OBProxyを起動するには、OceanBaseクラスタの場所を知っている必要があります。これは、rootservice_listパラメータで指定します。

   起動コードは以下のとおりです：

   ```shell
   cd /opt/taobao/install/obproxy && bin/obproxy -r "xxx.xxx.xxx.1:2881;xxx.xxx.xxx.2:2881;xxx.xxx.xxx.3:2881" -p 2883 -o "observer_sys_password=$sha1_value,enable_strict_kernel_release=false,enable_cluster_checkout=false,enable_metadb_used=false" -c cluster_name
   ```

   <main id="notice" type='explain'>
    <h4>説明</h4>
    <ul>
    <li><code>$sha1_value</code> は、実際に設定するパスワードに置き換える必要があります。proxy起動パラメータの設定方法では、設定するパスワードは元の値ではなく、sha1ハッシュ化された値となります。例えば、proxyroユーザーが設定したパスワードが <code>123456</code> の場合、<code>observer_sys_password</code> の値を <code>7c4a8d09ca3762af61e59520943dc26494f8941b</code> に設定する必要があります。</li>
    <li>サンプルIPアドレスはマスキング処理されていますが、これはインストール要件ではありません。起動時には、ご自身のマシンの実際のIPアドレスを入力してください。</li>
    </ul>
   </main>

   パラメータの説明：

   | **パラメータ** |         **説明**          |
   |--------|-------------------------|
   | `-r`   | OBServerノードのIPアドレスとポートを指定します。 |
   | `-p`   | サーバーのポート番号を指定します。通常は`2883`を指定します。   |
   | `-c`   | クラスタ名を指定します。                 |
   | `-o`   | ハードウェアまたはカーネルパラメータの設定を指定します。           |

   サンプルは以下のとおりです：

   ```shell
   [root@xxx admin]# su - admin
   -bash-4.2$ cd /opt/taobao/install/obproxy && bin/obproxy -r "xxx.xxx.xxx.1:2881" -p 2883 -o "observer_sys_password=7c4a8d09ca3762af61e59520943dc26494f8941b,enable_strict_kernel_release=false,enable_cluster_checkout=false,enable_metadb_used=false" -c test3241
   bin/obproxy -r xxx.xxx.xxx.1:2881 -p 2883 -o observer_sys_password=7c4a8d09ca3762af61e59520943dc26494f8941b,enable_strict_kernel_release=false,enable_cluster_checkout=false,enable_metadb_used=false -c test3241
   rs list: xxx.xxx.xxx.1:2881
   listen port: 2883
   optstr: observer_sys_password=7c4a8d09ca3762af61e59520943dc26494f8941b,enable_strict_kernel_release=false,enable_cluster_checkout=false,enable_metadb_used=false
   cluster_name: test3241
   ```

2. 起動後、プロセスが存在するかどうか確認できます。

   ```shell
   ps -ef|grep obproxy
   ```

   サンプルは以下のとおりです：

   ```shell
   [root@xxx admin]# ps -ef|grep obproxy
   admin     79111      0  6 17:32 ?        00:00:58 bin/obproxy -r xxx.xxx.xxx.1:2881 -p 2883 -o observer_sys_password=7c4a8d09ca3762af61e59520943dc26494f8941b,enable_strict_kernel_release=false,enable_cluster_checkout=false,enable_metadb_used=false -c test3241
   ```

## 次のステップ

### OceanBaseデータベースに接続する

OBProxy経由で接続する場合、ユーザー名の形式にはユーザー名、テナント名、クラスタ名が含まれる必要があります。形式は次のとおりです：**ユーザー名@テナント名#クラスタ名** または **クラスタ名:テナント名:ユーザー名**。

例：

* **ユーザー名@テナント名#クラスタ名** を使用してOceanBaseデータベースに接続します。

   ```shell
   [admin@xxx /home/admin]
   $obclient -hxxx.xxx.xxx.1 -P2883 -uroot@sys#test3241 -p******
   Welcome to the OceanBase.  Commands end with ; or \g.
   ...
   Type 'help;' or '\h' for help. Type '\c' to clear the current input statement.

   obclient [(none)]>
   ```

* **クラスタ名:テナント名:ユーザー名** を使用してOceanBaseデータベースに接続します。

   ```shell
   [admin@xxx /home/admin]
   $obclient -hxxx.xxx.xxx.1 -P2883 -utest3241:sys:root -p******
   Welcome to the OceanBase.  Commands end with ; or \g.
   ......
   Copyright (c) 2000, 2018, OceanBase and/or its affiliates. All rights reserved.

   Type 'help;' or '\h' for help. Type '\c' to clear the current input statement.

   obclient [(none)]>
   ```

### (オプション) OBProxyパラメータを調整する

サーバーの実際の状況に応じて、OBProxyパラメータの値を調整することで、実行ログ量やCPU消費などの情報を設定できます。

例：

以下は、実行ログ量を削減したりCPU消費を低減したりするために使用されるOBProxyの一部のパラメータ設定です。

```sql
obclient [(none)]> ALTER PROXYCONFIG SET slow_proxy_process_time_threshold='1000ms';
Query OK, 0 rows affected

obclient [(none)]> ALTER PROXYCONFIG SET xflush_log_level=ERROR;
Query OK, 0 rows affected

obclient [(none)]> ALTER PROXYCONFIG SET syslog_level=WARN;
Query OK, 0 rows affected

obclient [(none)]> ALTER PROXYCONFIG SET enable_compression_protocol=false;
Query OK, 0 rows affected

obclient [(none)]> SHOW PROXYCONFIG LIKE '%compress%'\G
*************************** 1. row ***************************
         name: enable_compression_protocol
        value: False
         info: if enabled, proxy will use compression protocol with server
  need_reboot: false
visible_level: USER
*************************** 2. row ***************************
         name: enable_syslog_file_compress
        value: False
         info: Whether to enable archive log compression
  need_reboot: false
visible_level: SYS
2 rows in set
```

## 関連ドキュメント

* OBProxyのデプロイに関する詳細は、[デプロイ方法](https://www.oceanbase.com/docs/enterprise-odp-enterprise-cn-10000000000982790)を参照してください。
* OBProxyパラメータの調整に関する詳細は、[構成パラメータの説明](https://www.oceanbase.com/docs/enterprise-odp-enterprise-cn-10000000000982784)を参照してください。
* OBProxyを使用してOceanBaseデータベースに接続する詳細については、[接続管理](https://www.oceanbase.com/docs/enterprise-odp-enterprise-cn-10000000000982772)を参照してください。