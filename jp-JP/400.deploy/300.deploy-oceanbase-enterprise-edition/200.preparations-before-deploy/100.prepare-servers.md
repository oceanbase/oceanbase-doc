|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||
|machine-translation|ja-marker|
|slug|prepare-servers|

# サーバーの準備

## OCPを使用したOceanBaseクラスタのデプロイ

サーバーが満たすべき最低構成要件は以下の表のとおりです：

| プロダクト | サーバー数 | 機能の最小構成 | パフォーマンスの最小構成 | ディスクタイプ |
|---------|-----------|-------------|-------------|------|
| OAT | 1台、OCPサーバーを再利用可能 | CPU：2コア</br>メモリ：4GB | N/A | N/A |
| OCPとMetaDB | 1台 | CPU：16コア</br>メモリ：32GB</br>ディスク：1.5TBストレージ（OATに必要なリソースを含む） <main id="notice" type='explain'><h4>説明</h4><p>16C32Gの構成ではOCPの安定性が保証されないため、検証用途に限ることを推奨します。</p></main>| CPU：32コア</br>メモリ：128GB</br>ディスク：1.5TB、10ギガビットNIC（OATに必要なリソースを含む） | SSDストレージ |
| OceanBaseクラスタ | 3台 <main id="notice" type='explain'><h4>説明</h4><p>スタンドアロン版OceanBaseデータベースをデプロイする場合は、サーバーを1台用意するだけで済みます。</p></main> | CPU：4コア</br>メモリ：16GB <main id="notice" type='explain'><h4>説明</h4><p><ul><li>ディスク内のログディスクはメモリの3倍以上、データディスクは目標データ量のストレージを満たす必要があります。</li><li>OBServerサーバーのメモリ設定は1TB以下を推奨します。</li><li>OBServerサーバーのCPU数は512コア以下を推奨します。</li></ul></p></main> | CPU：32コア</br>メモリ：256GB、10ギガビットNIC <main id="notice" type='explain'><h4>説明</h4><p><ul><li>ディスク内のログディスクはメモリの3倍以上、データディスクは目標データ量のストレージを満たす必要があります。</li><li>OBServerサーバーのメモリ設定は1TB以下を推奨します。</li><li>OBServerサーバーのCPU数は512コア以下を推奨します。</li></ul></p></main> | SSDストレージ |
|（オプション）ODP | 3台、OBServerノードサーバーを再利用可能 | CPU：4コア</br>メモリ：8GB</br>ディスク：200GB | N/A | N/A |

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p><ul><li>OceanBaseデータベースクラスタの高可用性および災害復旧では、少なくとも3つのノードで構成されており、各ノードには1つのobserverプロセスが対応しています。異なるノード上の複数のobserverプロセスが1つのクラスタを形成し、外部にサービスを提供します。</li><li>OCP管理サービスによる高可用性機能を必要とする場合は、複数の管理サーバーによる高可用性デプロイが必要です。詳細については、<a href="https://www.oceanbase.com/docs/common-ocp-1000000001406421">高可用性デプロイ</a>を参照してください。</li><li>OceanBaseクラスタのデプロイ後、ODPをデプロイするかどうかは、主にアプリケーションのシナリオとニーズによって決まります。<ul><li>アプリケーションがOceanBaseクラスタに対して高可用性、ロードバランシング、接続プール管理などの操作を行う必要がある場合は、ODPのデプロイを検討できます。ODPは、OceanBaseクラスタの接続数やロードバランシングなどの問題を効果的に解決し、アプリケーションの安定性と信頼性を向上させることができます。</li><li>アプリケーションが単純にOceanBaseクラスタに接続してクエリや操作を行うだけであり、高可用性、ロードバランシング、接続プール管理などの操作を必要としない場合は、ODPをデプロイせず、OceanBaseが提供するクライアントライブラリを使用してクラスタに直接接続することで、アプリケーションのニーズを満たすことができます。</li></ul>ODPの詳細な特性については、<a href="https://www.oceanbase.com/docs/enterprise-odp-enterprise-cn-10000000001715894">ODPの概要</a>を参照してください。</li></ul></p>
</main>

## コマンドラインを使用したOceanBaseクラスタのデプロイ

サーバーが満たすべき最低構成要件は以下の表のとおりです：

| プロダクト | サーバー数 | 機能の最小構成 | パフォーマンスの最小構成 | ディスクタイプ |
|---------|-----------|-------------|-------------|------|
| OAT | 1台 | CPU：2コア</br>メモリ：4GB | N/A | N/A |
| OceanBaseクラスタ | 3台 <main id="notice" type='explain'><h4>説明</h4><p>スタンドアロン版OceanBaseデータベースをデプロイする場合は、サーバーを1台用意するだけで済みます。</p></main> | CPU：4コア</br>メモリ：16GB <main id="notice" type='explain'><h4>説明</h4><p><ul><li>ディスク内のログディスクにはメモリの3倍以上の容量が必要であり、データディスクは対象となるデータ量を格納できる必要があります。</li><li>OBServerサーバーのメモリ設定は、1TBを超えないことを推奨します。</li><li>OBServerサーバーのCPUコア数は、512コアを超えないことを推奨します。</li></ul></p></main> | CPU：32コア</br>メモリ：256GB、10ギガビットNIC <main id="notice" type='explain'><h4>説明</h4><p><ul><li>ディスク内のログディスクにはメモリの3倍以上の容量が必要であり、データディスクは対象となるデータ量を格納できる必要があります。</li><li>OBServerサーバーのメモリ設定は、1TBを超えないことを推奨します。</li><li>OBServerサーバーのCPUコア数は、512コアを超えないことを推奨します。</li></ul></p></main> | SSDストレージ |
|（オプション）ODP | 3台、OBServerノードサーバーを再利用可能 | CPU：4コア</br>メモリ：8GB</br>ディスク：200GB | N/A | N/A |

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p><ul><li>OceanBaseデータベースクラスタの高可用性および災害復旧には、少なくとも3つのノードで構成されています。各ノードには1つのobserverプロセスが対応し、異なるノード上の複数のobserverプロセスが1つのクラスタを形成して外部にサービスを提供します。</li><li>OceanBaseクラスタのデプロイ後、ODPをデプロイするかどうかは、主にアプリケーションのシナリオとニーズによって決まります。<ul><li>アプリケーションがOceanBaseクラスタに対して高可用性、ロードバランシング、接続プール管理などの操作を必要とする場合、ODPのデプロイを検討できます。ODPは、OceanBaseクラスタの接続数やロードバランシングなどの問題を効果的に解決し、アプリケーションの安定性と信頼性を向上させることができます。</li><li>アプリケーションが単純にOceanBaseクラスタに接続してクエリや操作を行うだけであり、高可用性、ロードバランシング、接続プール管理などの操作が不要な場合、ODPをデプロイせずに、OceanBaseが提供するクライアントライブラリを使用してクラスタに直接接続することで、アプリケーションのニーズを満たすことができます。</li></ul>ODPの詳細な特性については、<a href="https://www.oceanbase.com/docs/enterprise-odp-enterprise-cn-10000000001715894">ODPの概要</a>を参照してください。</li></ul></p>
</main>

## アービトレーションサーバー

3台のサーバーを使用して、2レプリカのOceanBaseクラスタとアービトレーションサービスをデプロイする場合、アービトレーションサーバーは以下に示す最小マシン仕様を満たしている必要があります。

| バージョン | サポートされる最小構成 | 帯域幅 | 備考 |
| ------ | ------ | ------ | ------ |
| V4.1.0.0 | <ul><li>CPU：2コア </li><li>メモリ：8GB </li><li>ディスク：CLogディスクには少なくとも5GBのディスク容量が必要です。| 入出力データの転送速度はいずれも20Mbps以上であること。 | データストレージ用のディスク容量（`data`ディスク）の設定は不要です。ログファイルを格納する`log`ディレクトリには、別途容量を確保する必要があります。|
| V4.1.0 BP1以降のバージョン | <ul><li>CPU：2コア </li><li>メモリ：4GB </li><li>ディスク：CLogディスクには少なくとも5GBのディスク容量が必要です。 | 入出力データの転送速度はいずれも20Mbps以上であること。 | データストレージ用のディスク容量（`data`ディスク）の設定は不要です。ログファイルを格納する`log`ディレクトリには、別途容量を確保する必要があります。 |

### リソース計画

CPU型番の参考：Intel(R) Xeon(R) CPU E5-2682 v4 @ 2.50GHz

アービトレーションサーバーのリソース計画は、以下のルールを参考にしてください：

* 最小構成マシン（2C4G）では、デフォルトで32ユーザーテナントと`sys`テナントが同時にアービトレーションを有効にできます（2Fまたは4F、`primary_zone=RANDOM`、`unit_num=1`）。
* 単一ユニットテナント（2Fまたは4F、`primary_zone=RANDOM`、`unit_num=1`）が32個増えるごとに、アービトレーションサーバーのリソースは少なくとも以下を追加する必要があります：1C1G、ログディスク2GB、ネットワーク帯域幅20Mbps。
* `unit_num=N`（N>1）の場合は、単純にN個の単一ユニットテナントとして扱うことができます。

<main id="notice" type='explain'>
  <h4>説明</h4>
  <p><ul><li> 2F：2レプリカクラスタ。</li><li> 4F：4レプリカクラスタ。</li><li> <code>primary_zone=RANDOM</code>：<code>PRIMARY_ZONE</code>を指定する際、その値は<code>RANDOM</code>（大文字でなければなりません）に設定できます。これは、最も高い優先順位を持つゾーンをランダムに選択して<code>Primary Zone</code>とすることを意味します。</li><li> `unit_num=1`：リソースプールのターゲットゾーン内のユニット数は1です。</li><li>単一ユニットテナント：あるゾーン内のユニット数が1のテナント。</li></ul> <code>primary_zone</code>および<code>unit_num</code>の詳細については、<a href="../../../600.manage/200.tenant-management/600.common-tenant-operations/200.manage-create-tenant.md">テナントの作成</a>を参照してください。</p>
</main>

### ネットワーク遅延要件

アービトレーションサーバーとOceanBaseクラスタノード間の往復ネットワーク遅延は800ms以下である必要があります。