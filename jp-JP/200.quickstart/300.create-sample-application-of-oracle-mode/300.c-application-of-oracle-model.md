|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type|Oracle Mode|
|machine-translation|ja-marker|
|slug|build-c-application-oracle-mode|

# Cサンプルアプリケーションの作成

本記事では、CアプリケーションがOBCIを介してOceanBaseデータベースに接続し、使用する方法について説明します。

  <main id="notice" >
    <h4>適用対象</h4>
    <p>この内容はOceanBaseデータベースEnterprise Editionにのみ適用されます。OceanBaseデータベースCommunity EditionはMySQLモードのみを提供します。</p>
  </main>

## 前提条件

* データベース開発のための基本環境が整備されていることを確認してください。

* 以下のハードウェア環境を満たしていることを確認してください：

  * ハードウェアプラットフォーム：x86_64。

  * オペレーティングシステム：CentOS/Red Hat系Linuxディストリビューション7.2。

  * コンパイラ：GCC 4.8。

* 技術サポート担当者からOBCIおよびLibOBClientのRPMインストールパッケージを取得してください。

<main id="notice" type='notice'>
  <h4>注意</h4>
  <p>前提条件には、以下の点にもご注意ください：<ul><li>OBCI V2.0.4以降では、RPMパッケージの<code>include</code>フォルダに<code>oci.h</code>、<code>ociap.h</code>などのヘッダーファイルが含まれなくなりました。業務コンパイルには、まずOracle clntsh関連のRPMをインストールし、そのヘッダーファイルを直接使用する必要があります。</li><li>OceanBase：V2.2.76以降のバージョン（現在のバージョンの完全な機能を体験するには、OceanBaseデータベースV4.0以降のバージョンを推奨します）。</li><li>ODP（OBProxy）：V4.0以降のバージョンを推奨します。</li><li>libobclient：V2.1.1以降のバージョン。</li><li>Oracle関連ドライバーファイル<code>oracle-instantclient</code>をインストールしてください(OBCI V2.0.4以降ではOracleヘッダーファイルへの依存が必要です)。</li><li>OBCIがインストール済みであり、事前にGCCコンパイラがデプロイされていること。</li></ul></p>
</main>

### ステップ１：データベース接続文字列を取得する

OceanBaseデータベースのデプロイ担当者または管理者から、該当するデータベース接続文字列を取得します。例：

```sql
obclient  -h100.88.xx.xx -usys@oracle -p****** -P2881
```

データベース接続文字列には、データベースへのアクセスに必要なパラメータ情報が含まれています。アプリケーションを作成する前に、データベース接続文字列を使用してデータベースへのログインを検証し、接続文字列のパラメータ情報が正確であるか確認することができます。

パラメータの説明：

* **-h**：OceanBaseデータベースへの接続IPアドレス。ODPのアドレスである場合もあります。

* **-u**：テナントの接続ユーザー名。書式は**ユーザー@テナント#クラスタ名**です。Oracleモードの管理者ユーザー名はデフォルトで`sys`です。データベースに直接接続する場合はクラスタ名を入力しませんが、ODP経由で接続する場合は入力する必要があります。

* **-p**：ユーザーのパスワード。

* **-P**：OceanBaseデータベースの接続ポート番号であり、ODPのリスニングポートでもあります。

### ステップ2：C関連ドライバーのインストール

RPMパッケージを取得したら、コマンドラインツールでrootユーザー権限を使用して以下のコマンドを実行し、OBCIドライバーをインストールします：

```shell
rpm -ivh obci-<version>.x86_64.rpm
rpm -ivh libobclient-<version>.x86_64.rpm
```

デフォルトでは、ソフトウェアパッケージに含まれるプログラムとファイルは、次のパスにインストールされます：

* ヘッダーファイルは `/u01/obclient/include` パスにインストールされます。

* ライブラリファイルは `/u01/obclient/lib` パスにインストールされます。

### ステップ3：アプリケーションの作成

本記事では、具体的な例を通じて、OceanBaseデータベースのOracleモードにおいて、CアプリケーションがOBCIを使用してデータベースサーバーOBServerノードとやり取りする基本的な方法を紹介します。

1. OBCI環境とスレッドを初期化します。

   ```c
   /* OBCIプログラム環境の初期化 */
   OCIInitialize(OCI_DEFAULT, NULL, NULL, NULL, NULL)

   /* 環境ハンドルの初期化 */
   OCIEnvInit(&envhp, OCI_DEFAULT, 0, 0)
   ```

2. 必要なハンドルとデータ構造を割り当てます。

   ```c
   /* サービスコンテキストハンドル */
   OCIHandleAlloc(envhp, (dvoid **)&svchp, OCI_HTYPE_SVCCTX, 0, 0)

   /* サーバーハンドル */
   OCIHandleAlloc(envhp, (dvoid **)&srvhp, OCI_HTYPE_SERVER, 0, 0)

   /* セッションハンドル */
   OCIHandleAlloc(envhp, (dvoid **)&authp, OCI_HTYPE_SESSION, 0, 0)

   /* エラーハンドル */
   OCIHandleAlloc(envhp, (dvoid **)&errhp, OCI_HTYPE_ERROR, 0, 0)

   /* 説明ハンドル */
   OCIHandleAlloc(envhp, (dvoid **)&dschp, OCI_HTYPE_DESCRIBE, 0, 0)
   ```

3. データベースへの接続を確立し、ユーザーセッションを作成します。

   ```c
   /* ユーザー名とパスワードの設定 */
    OCIAttrSet(authp, OCI_HTYPE_SESSION, (text *)strUserName,
   (ub4)strlen(strUserName), OCI_ATTR_USERNAME, errhp)
    OCIAttrSet(authp, OCI_HTYPE_SESSION, (text *)strPassword,
   (ub4)strlen(strPassword), OCI_ATTR_PASSWORD, errhp)

   /* サーバー環境ハンドルのプロパティ設定 */
     OCIAttrSet((dvoid *)svchp, (ub4)OCI_HTYPE_SVCCTX,(dvoid *)srvhp, (ub4)0, OCI_ATTR_SERVER, errhp)
     OCIAttrSet(svchp, OCI_HTYPE_SVCCTX, (dvoid *)authp,0, OCI_ATTR_SESSION, errhp)

   /* ユーザーセッションの作成と開始 */
     OCISessionBegin(svchp, errhp, authp, OCI_CRED_RDBMS, OCI_DEFAULT)
     OCIHandleAlloc(envhp, (dvoid **)&stmthp, OCI_HTYPE_STMT, 0, 0)
   ```

4. SQLを使用してOceanBaseサーバーとデータを交換し、その後データ処理を行います。OBCIアプリケーションにおけるSQL文の実行手順は以下のとおりです：

   1. 関数 `OCIStmtPrepare()` または `OCIStmtPrepare2()` を呼び出して、SQL文を準備します。

      ```c
      OCIStmtPrepare(stmthp, errhp, (text *)sql, strlen(sql), OCI_NTV_SYNTAX,OCI_DEFAULT)
      ```

   2. 関数 `OCIBindByPos()` または `OCIBindByName()` など、1つまたは複数の関数を呼び出して、入力変数のアドレスをDML文のプレースホルダーにバインドします。

      ```c
      OCIBindByPos(stmthp, &bidhp[0], errhp, 1, &szpersonid,
                       sizeof(szpersonid), SQLT_INT, NULL, NULL, NULL, 0, NULL, 0)

      OCIBindByName(stmthp, &bidhp[0], errhp, (const OraText*)":personid", 9, &szpersonid,
                        sizeof(szpersonid), SQLT_INT, NULL, NULL, NULL, 0, NULL, 0)
      ```

   3. `OCIStmrExecute()` 関数を呼び出して、SQL文を実行します。

      ```c
      OCIStmtExecute(svchp, stmthp, errhp, (ub4)1, (ub4)0, (CONST OCISnapshot *)0,
      (OCISnapshot *)0, (ub4)OCI_DEFAULT)
      ```

   4. `OCIDefineByPos()` 関数を呼び出して、SQL文のデータ出力項目に対応する出力変数を定義します。

      ```c
      OCIDefineByPos(stmthp, &defhp[0], errhp, 1, &szpersonid,
      sizeof(szpersonid), SQLT_INT, &ind[0], 0, 0, OCI_DEFAULT)
      ```

   5. `OCIStmtFetch()` 関数を呼び出して、クエリの結果セットを取得します。

      ```c
      OCIStmtFetch(stmthp, errhp, 1, OCI_FETCH_NEXT, OCI_DEFAULT)
      ```

5. ユーザーセッションを終了し、データベースとの接続を切断します。

   ```c
   /* セッションの終了 */
     OCISessionEnd(svchp, errhp, authp, (ub4)0)

   /* データベースとの接続の切断 */
     OCIServerDetach(srvhp, errhp, OCI_DEFAULT)
   ```

6. プログラム内で割り当てられたハンドルを解放します。

   ```c
   OCIHandleFree((dvoid *)dschp, OCI_HTYPE_DESCRIBE)
   OCIHandleFree((dvoid *)stmthp, OCI_HTYPE_STMT)
   OCIHandleFree((dvoid *)errhp, OCI_HTYPE_ERROR)
   OCIHandleFree((dvoid *)authp, OCI_HTYPE_SESSION)
   OCIHandleFree((dvoid *)svchp, OCI_HTYPE_SVCCTX)
   OCIHandleFree((dvoid *)srvhp, OCI_HTYPE_SERVER)
   ```

#### サンプルコード

サンプルファイル `test.c` のコード内容は以下のとおりです：

```c
/**********************************************************
* Copyright(C) 2014 - 2020 Alibaba Inc. All Rights Reserved.
*
*  Filename: ob_oci_test.c
*  Description: ----
*  Create: 2020-07-07 10:14:59
*  Last Modified: 2020-07-07 10:14:59
***********************************************************/
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <malloc.h>
#include "oci.h"
/*ステートメントハンドルを宣言*/
OCIEnv *envhp;       /*環境ハンドル*/
OCISvcCtx *svchp;    /*サービス環境ハンドル*/
OCIServer *srvhp;    /*サーバーハンドル*/
OCISession *authp;   /*セッションハンドル*/
OCIStmt *stmthp;     /*ステートメントハンドル*/
OCIDescribe *dschp;  /*記述子ハンドル*/
OCIError *errhp;     /*エラーハンドル*/
OCIDefine *defhp[3]; /*定義ハンドル*/
OCIBind *bidhp[4];   /*バインドハンドル*/
sb2 ind[3];          /*インデクサ変数*/
/*SELECT結果セットをバインドするパラメータ*/
int szpersonid; /*personid列を格納*/
text szname[51];    /*name列を格納*/
text szemail[51];   /*mail列を格納*/
char sql[256];      /*実行されるSQLステートメントを格納*/

int main(int argc, char *argv[])
{
  char strServerName[50];
  char strUserName[50];
  char strPassword[50];
  /*サーバー、ユーザー名、パスワードを設定*/
  strcpy(strServerName, "172.30.xx.xx:2881");
  strcpy(strUserName, "s**@oracle");
  strcpy(strPassword, "******");
  /*OCIアプリケーション環境を初期化*/
  OCIInitialize(OCI_DEFAULT, NULL, NULL, NULL, NULL);
  /*環境ハンドルを初期化*/
  OCIEnvInit(&envhp, OCI_DEFAULT, 0, 0);
  /*ハンドルを割り当てる*/
  OCIHandleAlloc(envhp, (dvoid **)&svchp, OCI_HTYPE_SVCCTX, 0, 0);
  /*サーバー環境ハンドル*/
  OCIHandleAlloc(envhp, (dvoid **)&srvhp, OCI_HTYPE_SERVER, 0, 0);
  /*サーバーハンドル*/
  OCIHandleAlloc(envhp, (dvoid **)&authp, OCI_HTYPE_SESSION, 0, 0);
  /*セッションハンドル*/
  OCIHandleAlloc(envhp, (dvoid **)&errhp, OCI_HTYPE_ERROR, 0, 0);
  /*エラーハンドル*/
  OCIHandleAlloc(envhp, (dvoid **)&dschp, OCI_HTYPE_DESCRIBE, 0, 0);
  /*記述子ハンドル*/
  /*サーバーに接続*/
  OCIServerAttach(srvhp, errhp, (text *)strServerName, (sb4)strlen(strServerName), OCI_DEFAULT);
  /*ユーザー名とパスワードを設定*/
  OCIAttrSet(authp, OCI_HTYPE_SESSION, (text *)strUserName, (ub4)strlen(strUserName), OCI_ATTR_USERNAME, errhp);
  OCIAttrSet(authp, OCI_HTYPE_SESSION, (text *)strPassword, (ub4)strlen(strPassword), OCI_ATTR_PASSWORD, errhp);
  /*サーバー環境ハンドルのプロパティを設定*/
  OCIAttrSet((dvoid *)svchp, (ub4)OCI_HTYPE_SVCCTX, (dvoid *)srvhp, (ub4)0, OCI_ATTR_SERVER, errhp);
  OCIAttrSet(svchp, OCI_HTYPE_SVCCTX, (dvoid *)authp, 0, OCI_ATTR_SESSION, errhp);
  /*ユーザーセッションを作成して開始する*/
  OCISessionBegin(svchp, errhp, authp, OCI_CRED_RDBMS, OCI_DEFAULT);
  OCIHandleAlloc(envhp, (dvoid **)&stmthp, OCI_HTYPE_STMT, 0, 0);
  /*ステートメントハンドル*/

  /************************************************************************/
  /*personテーブルを作成*/
  /************************************************************************/
  static text* SQL_CREATE_TB = (text*)"create table person(personid number, name varchar(256), email  varchar(256))";
  /*SQLステートメントを準備*/
  OCIStmtPrepare(stmthp, errhp, SQL_CREATE_TB, strlen((char *)SQL_CREATE_TB),OCI_NTV_SYNTAX, OCI_DEFAULT);
  /*SQLステートメントを実行*/
  OCIStmtExecute(svchp, stmthp, errhp, 1, 0, 0, 0, OCI_DEFAULT);
  /*データベースにコミット*/
  OCITransCommit(svchp, errhp, OCI_DEFAULT);

  /************************************************************************/
  /*データを挿入*/
  /************************************************************************/
  memset(sql, 0, sizeof(sql));
  strcpy(sql, "insert into person values(:personid,:name,:email)");
  /*SQLステートメントを準備*/
  OCIStmtPrepare(stmthp, errhp, (text *)sql, strlen(sql),OCI_NTV_SYNTAX, OCI_DEFAULT);
  /*入力列をバインド*/
  OCIBindByName(stmthp, &bidhp[0], errhp, (const OraText*)":personid", 9, &szpersonid, sizeof(szpersonid), SQLT_INT, NULL, NULL, NULL, 0, NULL, 0);
  OCIBindByName(stmthp, &bidhp[2], errhp, (const OraText*)":name", 5, szname, sizeof(szname), SQLT_STR, NULL, NULL, NULL, 0, NULL, 0);
  OCIBindByName(stmthp, &bidhp[3], errhp, (const OraText*)":email", 6, szemail, sizeof(szemail), SQLT_STR, NULL, NULL, NULL, 0, NULL, 0);
  /*入力パラメータを設定*/
  szpersonid = 1;
  memset(szname, 0, sizeof(szname));
  strcpy((char*)szname, "obtest");
  memset(szemail, 0, sizeof(szemail));
  strcpy((char*)szemail, "t***@ob.com");
  /*SQLステートメントを実行*/
  OCIStmtExecute(svchp, stmthp, errhp, (ub4)1, (ub4)0, (CONST OCISnapshot *)0, (OCISnapshot *)0, (ub4)OCI_DEFAULT);
  /*データベースにコミット*/
  OCITransCommit(svchp, errhp, OCI_DEFAULT);

  /************************************************************************/
  /*personテーブルをクエリ*/
  /************************************************************************/
  strcpy(sql, "select personid ,name,email from person;");
  /*SQLステートメントを準備*/
  OCIStmtPrepare(stmthp, errhp, (text *)sql, strlen(sql), OCI_NTV_SYNTAX, OCI_DEFAULT);
  /*出力列をバインド*/
  OCIDefineByPos(stmthp, &defhp[0], errhp, 1, &szpersonid, sizeof(szpersonid), SQLT_STR, &ind[0], 0, 0, OCI_DEFAULT);
  OCIDefineByPos(stmthp, &defhp[1], errhp, 2, (ub1 *)szname, sizeof(szname), SQLT_STR, &ind[1], 0, 0, OCI_DEFAULT);
  OCIDefineByPos(stmthp, &defhp[2], errhp, 3, (ub1 *)szemail, sizeof(szemail), SQLT_STR, &ind[2], 0, 0, OCI_DEFAULT);
  /*SQLステートメントを実行*/
  OCIStmtExecute(svchp, stmthp, errhp, (ub4)0, 0, NULL, NULL,
                 OCI_DEFAULT);
  printf("%-10s%-10s%-10s\n", "PERSONID", "NAME", "email");
  while ((OCIStmtFetch(stmthp, errhp, 1, OCI_FETCH_NEXT, OCI_DEFAULT)) != OCI_NO_DATA)
  {
    printf("%-10d", szpersonid);
    printf("%-10s", szname);
    printf("%-10s\n", szemail);
    break;
  }
  /*データベースにコミット*/
  OCITransCommit(svchp, errhp, OCI_DEFAULT);

  /************************************************************************/
  /*personテーブルを削除*/
  /************************************************************************/
  static text* SQL_DROP_TB   = (text*)"drop table person";
  /*SQLステートメントを準備*/
  OCIStmtPrepare(stmthp, errhp, (text*)SQL_DROP_TB, strlen((char *)SQL_DROP_TB), OCI_NTV_SYNTAX, OCI_DEFAULT);
  /*SQLステートメントを実行*/
  OCIStmtExecute(svchp, stmthp, errhp, 1, 0, 0, 0, OCI_COMMIT_ON_SUCCESS);
  /*データベースにコミット*/
  OCITransCommit(svchp, errhp, OCI_DEFAULT);

  //セッションを終了
  OCISessionEnd(svchp, errhp, authp, (ub4)0);
  //データベースとの接続を切断
  OCIServerDetach(srvhp, errhp, OCI_DEFAULT);
  //OCIハンドルを解放
  OCIHandleFree((dvoid *)dschp, OCI_HTYPE_DESCRIBE);
  OCIHandleFree((dvoid *)stmthp, OCI_HTYPE_STMT);
  OCIHandleFree((dvoid *)errhp, OCI_HTYPE_ERROR);
  OCIHandleFree((dvoid *)authp, OCI_HTYPE_SESSION);
  OCIHandleFree((dvoid *)svchp, OCI_HTYPE_SVCCTX);
  OCIHandleFree((dvoid *)srvhp, OCI_HTYPE_SERVER);
  return 0;
}
```

コード内のデータベース接続パラメータを修正します。以下の項目を参照し、ステップ1で取得したデータベース接続文字列から対応する値を設定します。

```c
strcpy(strServerName, "172.30.xx.xx:2881");
strcpy(strUserName, "s**@oracle");
strcpy(strPassword, "******");
```

* **strServerName**：`-h` と `-P` パラメータから取得した `IP:port`。OceanBaseデータベースへの接続IPアドレスは通常ODPアドレスであり、アクセスに使用するポート番号です。

* **strUserName**：`-u` パラメータから取得したテナントの接続ユーザー名で、書式は **ユーザー@テナント#クラスタ名** となります。Oracleモードの管理者ユーザー名はデフォルトで `sys` です。データベースに直接接続する場合はクラスタ名を指定しませんが、ODP経由で接続する場合は指定する必要があります。

* **strPassword**：`-p` パラメータから取得したユーザーのパスワードです。

### ステップ4：アプリケーションの実行

コードの編集が完了したら、以下のコマンドでコンパイルを実行します。

```c
// コンパイル
gcc test.c -I/u01/obclient/include /u01/obclient/lib/libobci.a -L/usr/local/lib64 -lstdc++ -lpthread -ldl -lm -g -o test
```

コンパイルが完了し、次のような結果が得られた場合は、データベースへの接続に成功し、ステートメントの実行も正常であることを示します。

```bash
./test

PERSONID  NAME      email
1         obtest    t***@ob.com
```

## 詳細を見る

OBCIのインストールおよび使用方法の詳細については、公式ドキュメント[OceanBase C Language Call Interface](https://en.oceanbase.com/docs/obci-en)を参照してください。