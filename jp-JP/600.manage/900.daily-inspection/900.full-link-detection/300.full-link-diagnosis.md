|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||
|slug|end-to-end-tracing-methods|

# エンドツーエンド・トレース方法

運用保守担当者は、必要に応じてPL/SQLパッケージDBMS_MONITORの関連メソッドを使用して、アプリケーションが異なる識別情報の観点でエンドツーエンド・トレースを有効にするかどうか、およびそのトレースのイベント記録と関連情報をトレースログに出力するポリシーを制御できます。ログの出力はサンプリング頻度に基づいてサンプリングされ、メモリに記録された後、一定の確率でトレースログに出力されます。

## トレースの開始と停止操作

DBMS_MONITORパッケージ内のトレース関連メソッドを呼び出すことで、トレースのさまざまなレベルのオン/オフモードを実現できます。

ここでは、メソッドで使用される関連変数の定義を紹介します：

- `session_id`：セッションID。show processlist(MySQLモード)、GV$OB_PROCESSLISTなどのビューで照会できます。
- `client_id`：クライアントID。GV$OB_PROCESSLISTのclientフィールドで照会できます。
- `module_name`：モジュール名。GV$OB_PROCESSLISTのmoduleフィールドで照会できます。
- `action_name`：アクティビティ名。GV$OB_PROCESSLISTのactionフィールドで照会できます。
- `tenant_name`：テナント名。オプション：NULL（非NULLは現在のテナントを表す）および各テナント名。
- `level`：ログを出力する粒度。現在、3つの粒度レベルをサポートしています。Level1はモジュールレベルの粗粒度で、Level3が最も細かい粒度です。
- `sample_pct`：サンプリング頻度を制御します。値の範囲は[0,1]です。
- `record_policy`: ログ出力ポリシー、つまりトレース情報をログファイルに出力するポリシーです。以下の3種類のポリシーをサポートしています。

   - ALL：すべてのspanとtag情報をログファイルに出力し、各span終了時にもログファイルに出力します。
   - ONLY_SLOW_QUERY：現在のリクエストがslow queryの場合、その部分の情報のspanとtagをログファイルに出力します。
   - SAMPLE_AND_SLOW_QUERY：現在のリクエストがslow queryの場合、その部分の情報のspanとtagをログファイルに出力します。他のリクエスト情報のspanとtagは一定の確率でログファイルに出力されます。

詳細については、関連する[DBMS_MONITOR](../../../700.reference/600.pl-reference/300.pl-oracle/1400.pl-system-package-oracle/10000.dbms-monitor-oracle/100.dbms-monitor-overview-oracle.md)パッケージの定義を参照してください。

### セッションレベル

#### トレースの開始

```sql
DBMS_MONITOR.OB_SESSION_TRACE_ENABLE(
  session_id   IN  BINARY_INTEGER DEFAULT NULL,
  level        IN  INT,
  sample_pct   IN  NUMBER,
  record_policy IN VARCHAR2
);

```
例：現在のセッションに関連する時間などの情報を記録し、サンプリング頻度を50%に設定し、slow queryのみを記録します。

```sql
call dbms_monitor.ob_session_trace_enable(null, 1, 0.5, 'ONLY_SLOW_QUERY');
```

#### トレースの停止

```sql
DBMS_MONITOR.OB_SESSION_TRACE_DISABLE(session_id   IN  BINARY_INTEGER);
```

例：現在のセッションのトレースを停止します。

```sql
call dbms_monitor.ob_session_trace_disable(null);
```

### client_identifierレベル

#### トレースを開始

```sql
DBMS_MONITOR.OB_CLIENT_ID_TRACE_ENABLE(
  client_id    IN  VARCHAR2,
  level        IN  INT,
  sample_pct   IN  NUMBER,
  record_policy IN VARCHAR2
);
```

以下の例では、client_idがjdbcの関連する時間情報を記録し、サンプリング頻度は50%で、スロークエリのみを記録します。

```sql
call dbms_monitor.ob_client_id_trace_enable('jdbc', 1, 0.5, 'ONLY_SLOW_QUERY');
```

#### トレースを終了

```sql
DBMS_MONITOR.OB_CLIENT_ID_TRACE_DISABLE(client_id IN  VARCHAR2);
```

以下の例では、client_idがjdbcのトレースを終了します。

```sql
call dbms_monitor.ob_client_id_trace_disable('jdbc');
```

### Module/Actionレベル

#### トレースを開始

```sql
DBMS_MONITOR.OB_MOD_ACT_TRACE_ENABLE(
  module_name     IN VARCHAR2 DEFAULT ANY_MODULE,
  action_name     IN VARCHAR2 DEFAULT ANY_ACTION,
  level        IN  INT,
  sample_pct   IN  NUMBER,
  record_policy IN VARCHAR2
);
```

以下の例では、module_nameがbackup、action_nameがinsertの関連する時間情報を記録し、サンプリング頻度は50%で、スロークエリのみを記録します。ここでのmodule_nameとaction_nameは、dbms_application_infoパッケージのSET_MODULEメソッドで設定します。

```sql
call dbms_application_info.set_module('backup', 'insert');
call dbms_monitor.ob_mod_act_trace_enable('backup', 'insert', 1, 0.5, 'ONLY_SLOW_QUERY');
```

#### トレースを終了

```sql
DBMS_MONITOR.OB_MOD_ACT_TRACE_DISABLE(
  module_name     IN  VARCHAR2,
  action_name     IN  VARCHAR2 DEFAULT ALL_ACTIONS
);
```

以下の例では、module_nameがbackup、action_nameがinsertのトレースを終了します。

```sql
call dbms_monitor.ob_mod_act_trace_disable('backup', 'insert');
```

### テナントレベル

#### トレースを開始

```sql
DBMS_MONITOR.OB_TENANT_TRACE_ENABLE(
  level        IN  INT,
  sample_pct   IN  NUMBER,
  record_policy IN VARCHAR2
);
```

以下の例では、現在のテナント内の時間情報を記録し、すべて記録してすべて出力します。

```sql
call dbms_monitor.ob_tenant_trace_enable(1, 1, 'ALL');
```

#### トレースを終了

```sql
DBMS_MONITOR.OB_TENANT_TRACE_DISABLE(tenant_name  IN VARCHAR2 DEFAULT NULL);
```

以下の例では、テナント名ORACLEのトレースを無効にします。

```sql
call dbms_monitor.ob_tenant_trace_disable('ORACLE');
```

以下の例では、現在のテナントのトレースを無効にします。

```sql
call dbms_monitor.ob_tenant_trace_disable();
```

関連するログ情報はtrace.logに出力されます。具体的な内容例は以下のとおりです：

```sql
[20xx-0x-xx 00:07:46.027232] [1751][T1_TNT_L0][T1][YB42AC12050D-0005E42565DA44B2-0-0] {"trace_id":"0005e495-f920-5adc-7cd1-3730afa11dbd","name":"sql_execute","id":"0005e426-2c2b-0755-0000-000000000005","

start_ts":1658707664460666,"end_ts":1658707666027098,"parent_id":"0005e426-3355-05b6-0000-000000000002","is_follow":false}
[20xx-0x-xx 00:07:46.027234] [1751][T1_TNT_L0][T1][YB42AC12050D-0005E42565DA44B2-0-0] {"trace_id":"0005e495-f920-5adc-7cd1-3730afa11dbd","name":"pc_get_plan","id":"0005e426-5aac-d38c-0000-000000000004","start_ts":1658707664460552,"end_ts":1658707664460660,"parent_id":"0005e426-a22f-6cd4-0000-000000000003","is_follow":false}
```

エンドツーエンド・トレースの詳細については、[クエリのパス](https://en.oceanbase.com/docs/enterprise-oceanbase-ocp-en-10000000000838895)を参照してください。