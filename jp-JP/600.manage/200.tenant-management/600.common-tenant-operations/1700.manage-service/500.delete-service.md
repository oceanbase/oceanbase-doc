|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||
|slug|delete-service|

# サービスの削除

不要になったサービスは、`DELETE SERVICE` コマンドを使用して削除できます。

## 使用上の制限

サービス名で作成されたセッション内では、`DELETE SERVICE` コマンドを実行することはできません。

## 前提条件

`DELETE SERVICE` コマンドを実行するには、ユーザーに `ALTER SYSTEM` 権限が付与されている必要があります。

## 手順

1. テナント管理者がクラスタのユーザーテナントまたは sys テナントにログインします。

   接続例は以下のとおりです。データベースへの接続時は、実際の環境に準じてください。

   ```shell
   obclient -h10.xx.xx.xx -P2883 -uroot@sys#obdemo -p***** -A
   ```

2. 削除対象のサービスの状態が `STOPPED` であるか確認します。

   テナントのサービス状態を確認する詳細な操作については、[サービスの状態を確認する](200.view-service-status.md)を参照してください。

   クエリ結果に基づき、現在のサービスが `STOPPED` の場合は、直接サービスを削除できます。現在のサービスが `STOPPED` でない場合は、手動でサービスを一時停止する必要があります。サービスを一時停止する詳細な操作については、[サービスを一時停止する](400.stop-service.md)を参照してください。

3. サービスを削除します。

   ステートメントは以下のとおりです：

   ```sql
   ALTER SYSTEM DELETE SERVICE service_name [TENANT [=] tenant_name];
   ```

   ステートメント内の各項目の説明は以下のとおりです：

   * `service_name`：削除するサービス名を指定します。
   * `TENANT [=] tenant_name`：削除するサービスのテナント名を指定します。テナントを指定できるのは `sys` テナントのみであり、ユーザーテナントでは指定できません。

   例：

   * システムテナントがテナント `mysql_tenant` から名前が `s_hz` のサービスを削除します。

      ```shell
      obclient [oceanbase]> ALTER SYSTEM DELETE SERVICE s_hz TENANT = mysql_tenant;
      ```

   * ユーザーテナントが名前が `s_hz` のサービスを削除します。

      ```shell
      obclient > ALTER SYSTEM DELETE SERVICE s_hz;
      ```

## 関連ドキュメント

* [サービスの状態を確認する](200.view-service-status.md)
* [サービスの作成](100.create-service.md)