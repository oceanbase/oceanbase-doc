|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type|Oracle Mode|
|slug|delete-resource-group-oracle-mode|

# リソースグループの削除

使用されなくなったリソースグループを削除するには、`DBMS_RESOURCE_MANAGER` システムパッケージの `DELETE_CONSUMER_GROUP` サブプログラムを呼び出します。

## 前提条件

削除対象のリソースグループが存在することを確認してください。

## 注意事項

リソースグループを削除すると、そのリソースグループに関連付けられたリソース管理計画の内容も連鎖的に削除されます。

## 手順

1. テナント管理者としてクラスタのOracleテナントにログインします。

2. `DBMS_RESOURCE_MANAGER` システムパッケージの `DELETE_CONSUMER_GROUP` サブプログラムを呼び出してリソースグループを削除します。

   ```sql
   obclient [SYS]> delimiter //

   obclient [SYS]]> BEGIN DBMS_RESOURCE_MANAGER.DELETE_CONSUMER_GROUP(
   CONSUMER_GROUP => 'big_group'
   );
   END ;//

   obclient [SYS]> delimiter ;
   ```

   ここで、`CONSUMER_GROUP` は削除対象のリソースグループ名を指定するために使用されます。

3. 実行が成功した場合、`DBA_RSRC_CONSUMER_GROUPS` ビューで確認できます。

   ```sql
   obclient [SYS]> SELECT CONSUMER_GROUP_ID, CONSUMER_GROUP, COMMENTS FROM DBA_RSRC_CONSUMER_GROUPS;
   +-------------------+----------------+-----------------+
   | CONSUMER_GROUP_ID | CONSUMER_GROUP | COMMENTS        |
   +-------------------+----------------+-----------------+
   |                 4 | GROUP1         | トランザクション処理グループ       |
   |                 0 | OTHER_GROUPS   | NULL            |
   |                 3 | SMALL_GROUP    | AP              |
   |                 1 | SYS_GROUP      | NULL            |
   +-------------------+----------------+-----------------+
   4 rows in set
   ```

   `DBA_RSRC_CONSUMER_GROUPS` ビューの詳細については、[DBA_RSRC_CONSUMER_GROUPS](../../../../../700.reference/700.system-views/500.system-view-of-oracle-mode/200.dictionary-view-of-oracle-mode/18400.dba_rsrc_consumer_groups-of-oracle-mode.md)を参照してください。

## 関連ドキュメント

[DELETE_CONSUMER_GROUP](../../../../../700.reference/600.pl-reference/300.pl-oracle/1400.pl-system-package-oracle/13300.dbms-resource-manager-oracle/500.delete-consume-group-oracle.md)