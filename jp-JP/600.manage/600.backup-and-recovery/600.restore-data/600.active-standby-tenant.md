|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||
|slug|convert-standby-tenant-into-primary-tenant|

# スタンバイテナントをプライマリテナントに変換する

復元が成功した後、復元されたテナントはすべてスタンバイテナントとなります。`ACTIVATE STANDBY` コマンドを使用して、スタンバイテナントをプライマリテナントに変換できます。

## 背景

物理復元のプロセスは物理スタンバイデータベースと統一されており、物理復元後のテナントはスタンバイテナントとなります。その後、このテナントはスタンバイテナントとして関連サービスを提供することも、プライマリテナントに変換してサービスを提供することもできます。

## 前提条件

復元操作が成功したことを確認してください。復元結果の確認に関する操作については、[復元結果の確認](../600.restore-data/500.view-the-restore-history.md)を参照してください。

## 使用上の制限

クイック復元ではこの機能をサポートしていません。

## システムテナントによるスタンバイテナントからプライマリテナントへの変換

システムテナントは、システム内のスタンバイテナントをプライマリテナントに変換できます。

1. `root` ユーザーでクラスタの `sys` テナントにログインします。

2. 以下のステートメントを実行して、スタンバイからプライマリへの切り替えを行います。

   ステートメントは以下のとおりです：

   ```shell
   obclient [(none)]> ALTER SYSTEM ACTIVATE STANDBY TENANT tenant_name;
   ```

   <main id="notice" type='explain'>
   <h4>説明</h4>
   <p>このコマンドは一度に1つのテナントしか切り替えられません。複数のテナントを切り替える必要がある場合は、このコマンドを繰り返し実行する必要があります。</p>
   </main>

   例：

   ```shell
   obclient [(none)]> ALTER SYSTEM ACTIVATE STANDBY TENANT mysql;
   ```

   コマンド内の各パラメータの説明については、[物理復元関連パラメータ紹介](../600.restore-data/800.parameters-of-the-restore.md)を参照してください。

## ユーザーテナントによるスタンバイテナントからプライマリテナントへの変換

ユーザーテナントは、自身をスタンバイテナントからプライマリテナントに変換できます。

1. ユーザーテナントのテナント管理者がデータベースにログインします。

   <main id="notice" type='explain'>
   <h4>説明</h4>
   <p>MySQLモードの管理者ユーザーは<code>root</code>ユーザー、Oracleモードの管理者ユーザーは<code>SYS</code>ユーザーです。</p>
   </main>

2. 以下のステートメントを実行して、スタンバイからプライマリへの切り替えを行います。

   ステートメントは以下のとおりです：

   ```shell
   obclient [(none)]> ALTER SYSTEM ACTIVATE STANDBY;
   ```