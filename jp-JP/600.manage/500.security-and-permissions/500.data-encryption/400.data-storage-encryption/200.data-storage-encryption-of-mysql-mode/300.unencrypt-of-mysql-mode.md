|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type|MySQL Mode|
|slug|decrypt-table-mysql-compatible-mode|

# 暗号化の解除

<main id="notice">
      <h4>機能の適用範囲</h4>
      <p>OceanBase データベース Community Edition は、データの透過的暗号化をサポートしていません。</p>
  </main>

テーブルを暗号化されたテーブルスペースから移動することで、そのテーブルの暗号化を解除できます。

## 背景

テーブル `t1` の暗号化状態が以下のようになっており、テーブル `t1` が暗号化されたテーブルスペース `sectest_ts1` 内にあると仮定します。

```shell
obclient> SELECT * FROM oceanbase.V$OB_ENCRYPTED_TABLES;
+----------+------------+---------------+---------------+-----------+----------------------------------+-------------+------------------+------------------+--------+--------+
| TABLE_ID | TABLE_NAME | TABLESPACE_ID | ENCRYPTIONALG | ENCRYPTED | ENCRYPTEDKEY                     | MASTERKEYID | BLOCKS_ENCRYPTED | BLOCKS_DECRYPTED | STATUS | CON_ID |
+----------+------------+---------------+---------------+-----------+----------------------------------+-------------+------------------+------------------+--------+--------+
|   500010 | t1         |        500009 | aes-256       | YES       | xxxxxxxxxxxxxxxxxxxxxxxxxxxx7882 |      xxxx08 |                0 |                0 | NORMAL |      0 |
+----------+------------+---------------+---------------+-----------+----------------------------------+-------------+------------------+------------------+--------+--------+
1 row in set

obclient> SHOW CREATE TABLE t1;
+-------+--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
| Table | Create Table

                                               |
+-------+--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
| t1    | CREATE TABLE `t1` (
  `id1` int(11) DEFAULT NULL,
  `id2` int(11) DEFAULT NULL
) DEFAULT CHARSET = utf8mb4 ROW_FORMAT = DYNAMIC COMPRESSION = 'zstd_1.3.8' REPLICA_NUM = 1 BLOCK_SIZE = 16384 USE_BLOOM_FILTER = FALSE TABLET_SIZE = 134217728 PCTFREE = 0  TABLESPACE `sectest_ts1` |
+-------+--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
1 row in set
```

## テーブルを暗号化テーブルスペースから移動する

1. 管理者ユーザーがデータベースの MySQL テナントにログインします。

2. テーブルに対してプログレッシブマージを実行します。

   1. `progressive_merge_num` の値を `1` より大きい数に設定し、プログレッシブマージの準備として `OPTIMIZE` コマンドを実行します。

      例：

      ```shell
      obclient> ALTER TABLE t1 SET progressive_merge_num = 3;
      obclient> OPTIMIZE TABLE t1;
      ```

   2. 手動で複数回のプログレッシブマージを開始します。

      1 回のプログレッシブマージを開始するステートメントは以下のとおりです：

      ```shell
      obclient> ALTER SYSTEM MAJOR FREEZE;
      ```

      <main id="notice" type='explain'>
         <h4>説明</h4>
         <p>暗号化の解除は非同期プロセスです。解読処理では、すべてのデータが解読されるようにテーブルに対してプログレッシブマージを実行する必要があります。</p>
      </main>

3. 暗号化されていないテーブルスペース `ts2` を作成します。

   ```shell
   obclient> CREATE TABLESPACE ts2;
   ```

4. テーブルが存在するデータベースに接続します。

5. テーブル `t1` を暗号化されたテーブル領域 `sectest_ts1` から非暗号化テーブル領域 `ts2` に移動します。

   ```shell
   obclient> ALTER TABLE t1 TABLESPACE ts2;
   ```

   操作が成功した後、`SHOW CREATE TABLE` ステートメントを実行すると、テーブル `t1` がテーブル領域 `ts2` に移動されたことが確認できます。

   ```shell
   obclient> SHOW CREATE TABLE t1;
   +-------+------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
   | Table | Create Table                                                                                                                                                                                                                                                                 |
   +-------+------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
   | t2    | CREATE TABLE `t1` (
     `id1` int(11) DEFAULT NULL,
     `id2` int(11) DEFAULT NULL
   ) DEFAULT CHARSET = utf8mb4 ROW_FORMAT = DYNAMIC COMPRESSION = 'zstd_1.3.8' REPLICA_NUM = 1 BLOCK_SIZE = 16384 USE_BLOOM_FILTER = FALSE TABLET_SIZE = 134217728 PCTFREE = 0  TABLESPACE `ts2` |
   +-------+------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
   1 row in set
   ```

6. `oceanbase.V$OB_ENCRYPTED_TABLES` ビューをクエリして、テーブルの暗号化状態を確認します。

   ```shell
   obclient>  SELECT * FROM oceanbase.V$OB_ENCRYPTED_TABLES;
   +------------------+------------+------------------+---------------+-----------+--------------+-------------+------------------+------------------+------------+--------+
   | TABLE_ID         | TABLE_NAME | TABLESPACE_ID    | ENCRYPTIONALG | ENCRYPTED | ENCRYPTEDKEY | MASTERKEYID | BLOCKS_ENCRYPTED | BLOCKS_DECRYPTED | STATUS     | CON_ID |
   +------------------+------------+------------------+---------------+-----------+--------------+-------------+------------------+------------------+------------+--------+
   | 500010           | t1         | 500015           |               | NO        |              |           0 |                2 |                0 | NORMAL     |      0 |
   +------------------+------------+------------------+---------------+-----------+--------------+-------------+------------------+------------------+------------+--------+
   1 row in set
   ```

   テーブル `t1` の `ENCRYPTED` フィールドが `NO` であることが確認できます。これは、テーブル `t1` の暗号化解除操作が成功したことを示しています。

## 関連ドキュメント

* [新規作成テーブルへのストレージ暗号化の設定](../200.data-storage-encryption-of-mysql-mode/100.configure-internal-storage-encryption-of-mysql-mode.md)
* [既存テーブルへのストレージ暗号化の設定](../200.data-storage-encryption-of-mysql-mode/200.set-storage-encryption-for-existing-tables-of-mysql-mode.md)