| Description   |                 |
|---------------|-----------------|
| keywords      |  |
| dir-name      |  |
| dir-name-en   |  |
| tenant-type   | MySQL Mode |
|slug|column-encryption-privilege-management-mysql-compatible-mode|

# 列暗号化権限管理

列暗号化ルールを設定した後、適切な権限管理を行うことがデータセキュリティを確保するための重要なプロセスです。このドキュメントでは、OceanBaseの列暗号化機能の権限体系と管理構文について詳しく説明し、適切な権限制御メカニズムを構築して、機密データが許可されたユーザーにのみ表示されるようにする方法を解説します。

## 前提条件

機密ルールを使用する前に、TDE（透過的データ暗号化）機能を有効にし、マスターキーを作成する必要があります：

```sql
-- 透過的テーブル領域暗号化の方式をinternalまたはobcloudに設定します
ALTER SYSTEM SET tde_method = '<encryption_method>';
ALTER INSTANCE ROTATE INNODB MASTER KEY;        -- 初回実行時は、マスターキーが有効になるまで約20秒待機する必要があります
```

`tde_method`パラメータの詳細については、[tde_method](../../../../../700.reference/800.configuration-items-and-system-variables/100.system-configuration-items/400.tenant-level-configuration-items/7800.tde_method.md)を参照してください。

## 権限タイプ

列暗号化機能はルールベースの権限タイプを採用しており、データ保護ルールによってユーザーの機密列へのアクセス権限を制御します。OceanBaseはMySQLの権限体系に、列暗号化機能をサポートするための専用権限タイプを新たに追加しました。これにはユーザーレベル権限と機密ルールレベル権限が含まれます。

### ユーザーレベル権限

| 権限名 | 説明 | デフォルト所有者 | アップグレード時の取り扱い | 権限制限 | 機能範囲 | 包含関係 |
|---------|------|-----------|----------|----------|----------|----------|
| CREATE SENSITIVE RULE | ユーザーが機密ルールを作成および削除できるようにします。 | rootユーザー | システムアップグレード時に自動付与されます。 | rootユーザー権限は`REVOKE`できません。 | `CREATE/DROP SENSITIVE RULE`構文を使用できます。 | `ALL PRIVILEGES`に含まれます。 |
| PLAINACCESS | ユーザーがすべての平文データにアクセスできるようにし、データ保護ルールの影響を受けないようにします。 | rootユーザー | システムアップグレード時に自動付与されます。 | rootユーザー権限は`REVOKE`できません。 | すべての平文データにアクセスでき、いかなるデータ保護ルールの影響も受けません。 | `ALL PRIVILEGES`には**含まれません**。 |

### 機密ルールレベル権限

| 権限名 | 説明 | 権限範囲 | 自動付与 |
|---------|------|----------|----------|
| PLAINACCESS | ユーザーが特定のルールに関連付けられた列の平文データにアクセスすることを許可します。 | 特定のルールに対する `PLAINACCESS` 権限を持つユーザーは、そのルールに関連付けられた列の平文データにアクセスできます。 | ルールを作成したユーザーは、そのルールの `PLAINACCESS` 権限を自動的に取得しません。 |

## 権限管理

### 権限付与の構文と例

1. ユーザーレベル権限の付与

    構文は以下のとおりです：

    ```sql
    -- ユーザー/ロールに機密ルールを作成する権限を付与します。この権限はユーザーレベルのみです
    GRANT CREATE SENSITIVE RULE ON *.* TO <user_or_role_list>;

    -- ユーザー/ロールにユーザーレベルの平文アクセス権限を付与します
    GRANT PLAINACCESS ON *.* TO  <user_or_role_list>;
    ```

    構文とパラメータの説明は以下のとおりです：

    | 構文/パラメータ | 説明 | 例 |
    |------|------|------|
    | `user_or_role_list` | ユーザーまたはロールのリストパラメータで、複数のユーザーはカンマで区切ります。<br>例：`u1, u2, r1` |
    | `*.*` | ユーザーレベル（グローバル）権限を付与する際の固定構文で、グローバル範囲のすべてのデータベースとテーブルを表します。 |

    例：

    ```sql
    -- ユーザーu1に機密ルールを作成する権限を付与します
    GRANT CREATE SENSITIVE RULE ON *.* TO u1;

    -- 複数のユーザー/ロールに一括で権限を付与できます
    -- ユーザーu2とロールr1に機密ルールを作成する権限を付与します
    GRANT CREATE SENSITIVE RULE ON *.* TO u2, r1;
    ```

2. ルールレベル権限の付与

    構文は以下のとおりです：

    ```sql
    -- ルールレベルでの権限付与。SENSITIVE RULEキーワードの使用が必要です
    GRANT PLAINACCESS ON SENSITIVE RULE <rule_name> TO <user_or_role_list>;
    ```

    パラメータの説明は以下のとおりです：

    | パラメータ | 説明 | 例 |
    |------|------|------|
    | `rule_name` | 機密ルールの名前 | `'salary_encryption'`, `'credit_card_rule'`
    | `user_or_role_list` | ユーザーまたはロールのリスト。複数のユーザーはカンマで区切ります | `u1, u2, r1` |

    例：

    ```sql
    -- ユーザーu1にユーザーレベルの平文アクセス権限を付与します（同様に、複数のユーザー/ロールに一括で権限を付与できます）
    GRANT PLAINACCESS ON *.* TO u1;
    REVOKE PLAINACCESS ON *.* FROM u1;

    -- ユーザーu1にルールr1に対する平文アクセス権限を付与します
    -- 同様に、複数のユーザー/ロールに一括で権限を付与できます（ただし、一度に付与できるのは1つのルール上の権限のみです）
    GRANT PLAINACCESS ON SENSITIVE RULE r1 TO u1;
    ```

### 権限の取り消し構文と例

1. ユーザーレベル権限の取り消し

    構文は以下のとおりです：

    ```sql
    -- ユーザー/ロールが機密ルールを作成する権限を取り消します。この権限はユーザーレベルのみに付与されます
    REVOKE CREATE SENSITIVE RULE ON *.* FROM <user_or_role_list>;

    -- ユーザー/ロールのユーザーレベルの平文アクセス権限を取り消します
    REVOKE PLAINACCESS ON *.* FROM <user_or_role_list>;
    ```

    構文とパラメータの説明は以下のとおりです：

    | 構文/パラメータ | 説明 | 例 |
    |------|------|------|
    | `user_or_role_list` | ユーザーまたはロールのリストパラメータで、複数のユーザーはカンマで区切ります。<br>例：`u1, u2, r1` |
    | `*.*` | ユーザーレベル（グローバル）権限を取り消す際の固定構文で、グローバル範囲のすべてのデータベースとテーブルを表します。 |

    例：

    ```sql
    -- 権限の取り消し
    REVOKE CREATE SENSITIVE RULE ON *.* FROM u1;

    -- 複数のユーザー/ロールに対して一括で権限を取り消すことができます
    -- ユーザーu2とロールr1の機密ルール作成権限を取り消します
    REVOKE CREATE SENSITIVE RULE ON *.* FROM u2, r1;
    ```

2. ルールレベル権限の取り消し

    構文は以下のとおりです：

    ```sql
    -- ルールレベルの権限取り消し。SENSITIVE RULEキーワードを使用する必要があります
    REVOKE PLAINACCESS ON SENSITIVE RULE <rule_name> FROM <user_or_role_list>;
    ```

    パラメータの説明：

    | パラメータ | 説明 | 例 |
    |------|------|------|
    | `rule_name` | 機密ルールの名前 | `'salary_encryption'`, `'credit_card_rule'` |
    | `user_or_role_list` | ユーザーまたはロールのリスト。複数のユーザーはカンマで区切ります | `u1, u2, r1` |

    例：

    ```sql
    -- ユーザーu1のユーザーレベルの平文アクセス権限を取り消します（同様に、複数のユーザー/ロールに対して一括で権限を取り消すことも可能です）
    REVOKE PLAINACCESS ON *.* FROM u1;

    -- ユーザーu1のルールr1に対する平文アクセス権限を取り消します
    -- 同様に、複数のユーザー/ロールに対して一括で権限を取り消すことができます(ただし、一度に取り消せるのは一つのルール上の権限のみです)
    REVOKE PLAINACCESS ON SENSITIVE RULE r1 FROM u1;
    ```

### 権限の確認

1. 機密ルールの確認

    現在のテナント内のすべての機密データ保護ルール、または指定されたテーブルやデータベースに関連付けられたすべての機密ルールを確認できます。`LIKE`句を使用してルール名をフィルタリングできます。

    構文は以下のとおりです：

    ```sql
    SHOW SENSITIVE RULES opt_show_condition
    | SHOW SENSITIVE RULES from_or_in relation_factor opt_from_or_in_database_clause opt_show_condition
    | SHOW SENSITIVE RULES from_or_in DATABASE from_or_in database_factor opt_show_condition;
    ```

    パラメータ説明：

    | パラメータ | 説明 | 例 |
    |------|------|------|
    | `opt_show_condition` | オプションの表示条件。例：`LIKE`句 | `LIKE '%rule1%'` |
    | `relation_factor` | テーブル名 | `tb1`, `employees` |
    | `database_factor` | データベース名 | `test`, `hr_db` |

    例：`tb1` テーブルは `test` データベースにあります：

    ```sql
    -- 現在のテナント内のすべてのsensitive rulesを確認します
    SHOW SENSITIVE RULES;

    -- tb1に関連付けられたすべてのデータ保護ルールを確認します
    SHOW SENSITIVE RULES FROM tb1;

    -- test.tb1に関連付けられたすべてのデータ保護ルールを確認します
    SHOW SENSITIVE RULES FROM tb1 FROM test;

    -- test DBに関連付けられたすべてのデータ保護ルールを確認します
    SHOW SENSITIVE RULES FROM DATABASE test;

    -- LIKE句を使用してルール名をフィルタリングします
    SHOW SENSITIVE RULES LIKE '%rule1%';
    SHOW SENSITIVE RULES FROM tb1 LIKE '%rule1%';
    SHOW SENSITIVE RULES FROM tb1 FROM test LIKE '%rule1%';
    SHOW SENSITIVE RULES FROM DATABASE test LIKE '%rule1%';
    ```

    実行結果の例：

    | 列名 | rule_id | rule_name | POLICY | METHOD | ENABLED | PROTECT_COLS |
    | --- | --- | --- | --- | --- | --- | --- |
    | 例 | 1 | 'ENCRYPT_COL' | 'ENCRYPTION' | 'AES-256-ECB' | 'YES' | 'db1.tbl1(col_1, col_2), db2.tbl2(col_3), db3.tbl4(col_4, col_5)' |

    実行結果のフィールド説明：

    | フィールド名 | 説明 | サンプル値 |
    |--------|------|--------|
    | `rule_id` | ルールID。システムが自動生成します。 | `1`, `2` |
    | `rule_name` | ルール名 | `'ENCRYPT_COL'` |
    | `POLICY` | 戦略のタイプ | `'ENCRYPTION'` |
    | `METHOD` | 暗号化方式 | `'AES-256-ECB'` |
    | `ENABLED` | 有効かどうか | `'YES'`, `'NO'` |
    | `PROTECT_COLS` | 保護される列。形式は `database.table(columns)` | `'db1.tbl1(col_1, col_2)'`。複数のテーブルを指定する場合は、カンマで区切ります。 |

2. ユーザー権限の確認

    構文は以下のとおりです：

    ```sql

    -- 指定されたユーザーのすべての権限を確認します
    SHOW GRANTS FOR <user_or_role_list>;

    -- 現在のユーザーの権限を確認します
    SHOW GRANTS;
    ```

    例：

    ```sql
    -- ユーザーu1のすべての権限を確認します
    SHOW GRANTS FOR u1;
    ```

    実行結果の例：

    ```sql
    +-----------------------------------------------------------+
    | Grants for u1@%                                           |
    +-----------------------------------------------------------+
    | GRANT ALL PRIVILEGES ON *.* TO u1 WITH GRANT OPTION     |
    | GRANT CREATE SENSITIVE RULE ON *.* TO u1;									|
    | GRANT PLAINACCESS ON *.* TO u1;									          |
    | GRANT PLAINACCESS ON `enc_rule1` TO u1;									  |
    | GRANT PLAINACCESS ON `enc_rule2` TO u1;								    |
    +-----------------------------------------------------------+
    ```

## 関連ドキュメント

* [列暗号化機能の概要](../100.column-encryption-overview.md)
* [列暗号化ルールの作成](200.create-column-encryption-rule-of-mysql-mode.md)
* 機密レベルルールの定義とそのプロパティについては、[DBA/CDB_OB_SENSITIVE_RULES](../../../../../700.reference/700.system-views/400.system-view-of-mysql-mode/200.dictionary-view-of-mysql-mode/11070.o-dba_ob_sensitive_rules-of-mysql-mode.md)を参照してください。
