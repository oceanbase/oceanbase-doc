|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||
|slug|query-temporary-table-number|

# 一時テーブルの数をクエリする

ビュー `CDB_TABLES` には、すべてのテナントのすべてのテーブル（一時テーブルと非一時テーブルを含む）が記録されています。このビューを使用して一時テーブルの数を集計できます。

システムテナントでは、すべてのテナントの一時テーブルの数を集計するステートメントは以下のとおりです：

```shell
obclient> SELECT count(*), CON_ID FROM oceanbase.CDB_TABLES WHERE TEMPORARY = 'Y' GROUP BY CON_ID;
```

ここで、`CON_ID` はテナントの `tenant_id` を表します。

Oracleモードのユーザーテナントでは、そのテナント自身の一時テーブルの数のみをクエリする必要がある場合、以下のステートメントでビュー `DBA_TABLES`/`ALL_TABLES`/`USER_TABLES` をクエリできます。

```shell
obclient> SELECT count(*) FROM SYS.DBA_TABLES WHERE TEMPORARY = 'Y';
```

```shell
obclient> SELECT count(*) FROM SYS.ALL_TABLES WHERE TEMPORARY = 'Y';
```

```shell
obclient> SELECT count(*) FROM SYS.USER_TABLES WHERE TEMPORARY = 'Y';
```