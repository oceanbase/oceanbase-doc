|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||
|slug|application-exception-with-oceanbase-error-codes|

# アプリケーション例外 -- エラーメッセージにOceanBaseエラーコードが含まれる場合

データベースの運用中には、アプリケーションエラー、データベース接続エラー、データベース権限問題、データベースリソース問題、ネットワーク問題など、さまざまな異常状況が発生することがよくあります。これらすべての状況の中で、アプリケーション例外が発生するものの、そのエラーメッセージにOceanBaseエラーコードが含まれるケースがあります。

このようなシナリオで問題の根本原因を迅速に特定し、効率的に解決できるよう支援するために、ここではアプリケーション例外でエラーメッセージにOceanBaseエラーコードが含まれる場合の、明確で実用的なトラブルシューティングプロセスをまとめました。このプロセスは、明確な操作手順を提供し、問題処理の効率を向上させ、業務への影響を可能な限り軽減し、日常の運用保守作業を強力にサポートすることを目的としています。

アプリケーション実行時にエラーが発生し、そのエラーメッセージにOceanBaseエラーコードが含まれる場合の問題トラブルシューティングプロセスは、以下の図のとおりです。

<!-- ![アプリケーションの異常エラーにOBエラーコードが含まれる場合のトラブルシューティングプロセス](https://obbusiness-private.oss-cn-shanghai.aliyuncs.com/doc/img/observer-enterprise/V4.3.5/troublethooting/app-exception-with-error-code-V4x-new1.png) -->

## プロセスの概要

アプリケーション実行時に例外が発生し、エラーメッセージにOceanBaseエラーコードが含まれるシナリオに遭遇した場合、このプロセスに従って問題を調査することができます。

まず、OBClientを使用してデータベースに接続した後、手動で問題を再現できるかどうかを判断します：

* はいの場合、問題が再現された後に接続を維持したまま、さらに調査を進めることができます。

* いいえの場合、プログラムで設定されているデータソース情報を調査する必要があります。

### 問題の再現によるトラブルシューティング

1. OBClientを使用し、2881（SQLポート番号、デフォルトは2881です。カスタマイズされている場合は、実際のポート番号に置き換えてください）でOceanBaseクラスタに接続します。

2. エラーが発生したSQLを手動で実行し、問題シナリオを再現します。

3. 以下のステートメントを実行して、trace_idを取得します。

    <main id="notice" type='notice'>
    <h4>注意</h4>
    <p>エラーが発生したSQLの実行後、速やかに以下のステートメントを実行する必要があります。そうでない場合、クエリ結果はエラーが発生したSQLの<code>trace_id</code>ではありません。</p>
    </main>

    :::tab
    tab MySQLモード

    MySQLモードで`trace_id`を取得するステートメントは以下のとおりです：

    ```shell
    obclient> SELECT last_trace_id();
    ```

    tab Oracleモード

    Oracleモードで`trace_id`を取得するステートメントは以下のとおりです：

    ```shell
    obclient> SELECT last_trace_id() FROM DUAL;
    ```

    :::

4. 実際にSQLを実行したホスト情報を取得します。

    OceanBaseクラスタは通常、複数ノードでデプロイされています。以下のSQLを使用して、SQLが実際に実行されたノードを取得し、その後ログフィルタリングを行うことができます。

    :::tab
    tab MySQLモード

    MySQLモードで以下のステートメントを実行します：

    ```shell
    obclient> SELECT * FROM oceanbase.GV$OB_SQL_AUDIT WHERE trace_id=last_trace_id;
    ```

    ここで、`last_trace_id` は前の手順で取得した `trace_id` に置き換える必要があります。

    tab Oracleモード

    Oracleモードで以下のステートメントを実行します：

    ```shell
    obclient> SELECT * FROM SYS.GV$OB_SQL_AUDIT WHERE trace_id=last_trace_id;
    ```

    ここで、`last_trace_id` は前の手順で取得した `trace_id` に置き換える必要があります。

    :::

    `GV$OB_SQL_AUDIT` ビューのクエリ結果によると、`svr_ip` に対応するホストが、そのSQLを実際に実行したホストです。

5. 取得したホスト情報に基づき、`ssh` コマンドを使用して対応するホストにログインします。

6. ログが保存されているディレクトリに移動します。

    以下では、OceanBaseデータベースのインストールディレクトリを `/home/admin/oceanbase` と仮定します。ログの具体的な保存パスは、実際の環境に基づいてください。

    ```shell
    cd /home/admin/oceanbase/log
    ```

7. 以下のコマンドを実行して、ログ内の関連情報をフィルタリングします。

    ```shell
    grep "${trace_id}" observer.log
    ```

    ```shell
    grep "${trace_id}" observer.log.xxx
    ```

    ここで、`${trace_id}` は前の手順で取得した `trace_id` に置き換える必要があります。`observer.log.xxx` はタイムスタンプ付きのログファイルであり、`xxx` はSQLがエラーを返した時間に基づいて実際のタイムスタンプに置き換える必要があります。

8. ログから提供された情報と関連するエラーメッセージなどを組み合わせて問題を分析します。

    ログに関する詳細は、[ログの概要](../800.logging/100.logging-overview.md)を参照してください。ログの情報が不明確な場合は、テクニカルサポートにお問い合わせいただき、調査のご協力をお願いいたします。

### プログラムが設定するデータソース情報の確認

1. データソースに基づいてアクセスパスを明確にします。

2. アクセスパスには、以下の情報を含むアドレスの数が含まれていることを明確にします。

    * OBProxyの数とアドレス情報

    * OBServerホストの数とアドレス情報

3. OBProxyを経由しているかどうかを判断します。

    * はい、OBProxyを経由している場合は、以下の手順を参照してさらに調査します：

      1. `ssh` コマンドを使用して、対応するOBProxyノードにログインします。

      2. `cd` コマンドを使用して、OBProxyのログディレクトリに移動します。OBProxyのログは、インストールディレクトリの `/log` ディレクトリに保存されています。

      3. 以下のコマンドを実行して、エラーログをフィルタリングします。

         ```shell
         grep "エラーテキスト" obproxy_error.log obproxy_error.log.xxx | grep "エラー発生時刻"
         ```

      4. フィルタリングされたログエントリに基づいて、エラーが発生したSQLがルーティングされたOBServerノードを特定します。

      5. その後、`ssh` コマンドを使用して、対応するOBServerノードにログインし、調査を続けます。

    * いいえ、OBProxyを経由していない場合は、以下の手順を参照してさらに調査します：

      1. `ssh` コマンドを使用して、対応するOBServerノードにログインします。

      2. ログがあるディレクトリに移動します。

         以下では、OceanBaseデータベースのインストールディレクトリを `/home/admin/oceanbase` と仮定します。ログの実際の保存パスは、実際の環境に基づいてください。

         ```shell
         cd /home/admin/oceanbase/log
         ```

      3. 以下のコマンドを実行して、エラーログをフィルタリングします。

         ```shell
         grep "sending error" observer.log observer.log.xxx | grep "エラーコード'
         ```

      4. フィルタリングされたログからtrace_idを取得し、そのtrace_idを使用してログをさらにフィルタリングします。

         ```shell
         grep "trace id" observer.log observer.log.xxx
         ```

      5. ログが提供する情報と関連するエラーメッセージなどを組み合わせて、問題を分析します。

         詳細なログ関連情報については、[ログの概要](../800.logging/100.logging-overview.md)を参照してください。ログの情報が不明確な場合は、テクニカルサポートにお問い合わせいただき、調査のご協力をお願いします。

## 典型的なケース

* 普通業務テナントの普通ユーザーがOceanBaseデータベースの2881ポートに直接接続する際に、`ERROR 1040 (08004): Too many connections`エラーが発生した場合。具体的な調査手順については、[普通ユーザーが接続時にERROR 1040 (08004): Too many connectionsエラーが発生した場合](https://www.oceanbase.com/knowledge-base/oceanbase-database-1000000002356015)を参照してください。

## 関連ドキュメント

* [特定のSQLリクエストのログを検索する](../800.logging/600.view-logs/100.search-for-logs-of-a-sql-query.md)

* [Trace機能に基づいて前回のSQLリクエストログを検索する](../800.logging/600.view-logs/200.search-for-logs-of-a-sql-query-using-trace.md)

* [ログの概要](../800.logging/100.logging-overview.md)