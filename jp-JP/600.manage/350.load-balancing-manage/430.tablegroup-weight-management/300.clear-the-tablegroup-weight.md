|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||
|slug|clear-table-group-weights|

# テーブルグループの重みをクリアする

テーブルグループの重みを設定した後、業務上の必要に応じて、元の設定をクリアできます。

## 注意事項

テーブルグループの重みをクリアすると、テーブルグループの重みが削除されます。テーブルグループ内のテーブルにテーブルレベルまたはパーティションレベルの重みが設定されている場合、システムはテーブルレベルおよびパーティションレベルの重みを保持します。

## 重みのクリア

1. ユーザーがクラスタのMySQLテナントにログインします。

   接続例は以下のとおりです。データベースへの接続時は、実際の環境に準じてください。

   ```shell
   obclient -h10.xx.xx.xx -P2883 -uroot@mysql001#obdemo -p***** -A
   ```

2. テーブルグループの重み情報を取得します。

   ```shell
   obclient(root@mysql001)[test]> SELECT TABLEGROUP_NAME, WEIGHT, TABLEGROUP_ID, OBJECT_ID FROM oceanbase.DBA_OB_OBJECT_BALANCE_WEIGHT WHERE TABLEGROUP_NAME = 'TG_DB_test';
   ```

   クエリ結果は次のとおりです：

   ```shell
   +-----------------+--------+---------------+-----------+
   | TABLEGROUP_NAME | WEIGHT | TABLEGROUP_ID | OBJECT_ID |
   +-----------------+--------+---------------+-----------+
   | TG_DB_test      |     10 |        500007 |    500007 |
   +-----------------+--------+---------------+-----------+
   1 row in set
   ```

3. テーブルグループの重みをクリアします。

   テーブルグループ `TG_DB_test` の重みをクリアするステートメントは以下のとおりです。

   ```shell
   obclient(root@mysql001)[test]> CALL DBMS_BALANCE.CLEAR_TABLEGROUP_BALANCE_WEIGHT('TG_DB_test');
   ```

   `sys` テナントが指定ユーザーテナント `mysql001` のテーブルグループの重みをクリアすることもサポートしています。ステートメントは以下のとおりです：

   ```shell
   obclient(root@sys)[(none)]> CALL DBMS_BALANCE.CLEAR_TABLEGROUP_BALANCE_WEIGHT('TG_DB_test') TENANT = 'mysql001';
   ```

4. テーブルグループの重みをクリアした後、再度テーブルグループの重み情報を確認できます。

   ```shell
   obclient(root@mysql001)[test]> SELECT TABLEGROUP_NAME, WEIGHT, TABLEGROUP_ID, OBJECT_ID FROM oceanbase.DBA_OB_OBJECT_BALANCE_WEIGHT WHERE TABLEGROUP_NAME = 'TG_DB_test';
   ```

   クエリ結果が空の場合、テーブルグループの重みが正常にクリアされたことを意味します。

## 次の操作

テーブルグループの重みをクリアした後、テーブルグループ内のテーブルにおけるパーティションの分散が不均衡になる可能性があります。ユーザーができるだけ早くパーティションの均衡を実現したい場合は、`DBMS_BALANCE.TRIGGER_PARTITION_BALANCE` サブプログラムを呼び出して、手動でパーティションの均衡をトリガーすることができます。手動でパーティションの均衡をトリガーする詳細な操作については、[パーティションの均衡を手動でトリガーする](../600.partition-balance-management/200.manually-trigger-a-partition-balancing-task.md)を参照してください。

また、ユーザーは定期的なパーティション均衡タスクのトリガーを待機することもできます。定期的なパーティション均衡タスクの詳細については、[定期的なパーティション均衡タスクの設定](../600.partition-balance-management/100.configure-a-scheduled-partition-balancing-task.md)を参照してください。