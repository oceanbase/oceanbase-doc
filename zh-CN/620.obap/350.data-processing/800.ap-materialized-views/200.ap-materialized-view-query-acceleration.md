|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||

# 物化视图查询加速

物化视图（Materialized View, MV）是一种数据库对象，与普通视图不同的是，物化视图存储了视图的查询结果。通过保存某些耗时操作（例如聚合、连接）的结果，以便在查询时直接复用，从而避免重复执行这些耗时耗资源的操作，最终实现加速查询的目的。尤其是在数据仓库和决策支持系统中，能够大幅减少计算时间和提高查询效率。

## 为什么在 AP / 实时数仓中使用物化视图

* **降低重复计算**：多维分析、宽表关联、周期性报表往往重复相同的 JOIN / GROUP BY，物化后由刷新任务统一维护。
* **控制新鲜度与成本**：通过 **全量 / 增量 / 实时（按需）** 等策略，在延迟、资源占用之间做权衡（能力选型见 [物化视图经典场景实践](300.ap-materialized-view-scenario-practice.md)）。
* **组合其他 AP 能力**：例如 **列存形态** 的物化结果、**查询改写** 命中物化结果、与 **外表** 结合做湖仓协同（场景与分步 SQL 见 [物化视图经典场景实践](300.ap-materialized-view-scenario-practice.md)）。

## 适用场景

* **数据汇总**：按日/周/月汇总交易、行为明细等。
* **固定报表 / 指标层**：预计算指标表，供报表或 API 查询。
* **重查询优化**：将极耗资源的 SQL 结果物化，避免多次重复执行。
* **数据就近与副本策略**（按业务规划）：在合规前提下冗余聚合结果，配合路由与副本策略使用。
* **监控类预聚合**：按时间粒度预聚合监控明细。

## 核心能力

### 查询加速

* **单表聚合**：对于业务单表存量数据较多且需要经常进行分组聚合的查询场景，可以创建单表分组聚合的增量刷新 (refresh fast) 物化视图；若需查询时合并最新变更，在符合条件时可使用 **实时物化视图（ENABLE ON QUERY COMPUTATION）**。

* **多表连接**：对于业务多表存量数据较多，且需要经常进行连接查询的场景，可以创建多表连接的增量刷新 (refresh fast) 物化视图。多表 JOIN 类物化视图在维护好数据后，可配合 **查询改写**，使对基表的查询自动改走物化结果。

### 物化视图与实时数仓

在 OceanBase **AP / 实时数仓**场景中，物化视图常用于把 **明细或宽表上的重复计算** 沉淀为 **可刷新的物理结果集**，在 **时间（批次/准实时）、空间（维度/粒度）、主题（业务域）** 上组织 **多维汇总与派生指标**；与 **列存、查询改写、增量刷新** 等组合后，可降低在线分析对基表的扫描与计算压力。

#### 物化视图在实时数仓中的位置

典型实时或准实时数仓链路可概括为：**接入（CDC / 文件 / 外表）→ 明细层（DWD）→ 汇总与主题层（DWS / ADS）→ 查询与报表**。物化视图适合承担其中 **可重复的聚合、连接、宽表打平** 环节：

* **多维汇总**：按地域、类目、时间窗口等对事实表做 `GROUP BY`、连接维度。
* **数据分层**：通过 **全量 / 增量 / 实时物化视图** 与 **定时或手动刷新** 组合，在 **延迟、资源占用、结果一致性** 之间做权衡。
* **查询加速**：在保留对明细表 SQL 的前提下，通过 **查询改写** 自动改走物化结果，减少业务改造。

更多信息，参见 [物化视图概述（MySQL）](../../../700.reference/300.database-object-management/100.manage-object-of-mysql-mode/600.manage-views-of-mysql-mode/200.manage-materialized-views-of-mysql-mode/100.materialized-views-overview-of-mysql-mode.md)、[物化视图概述（Oracle）](../../../700.reference/300.database-object-management/200.manage-object-of-oracle-mode/500.manage-views-of-oracle-mode/200.manage-materialized-views-of-oracle-mode/100.materialized-views-overview-of-oracle-mode.md)。

#### 刷新能力：让物化结果与基表对齐

物化视图需要通过 **刷新策略** 与基表数据对齐。

| 能力要点 | 说明 | 实操入口 |
| -------- | ---- | -------- |
| **全量刷新** | 每次按定义重算并覆盖结果，适合定义复杂、可接受较长窗口或需要完全对齐的场景。 | [刷新物化视图（MySQL）](../../../700.reference/300.database-object-management/100.manage-object-of-mysql-mode/600.manage-views-of-mysql-mode/200.manage-materialized-views-of-mysql-mode/400.refresh-materialized-views-of-mysql-mode.md)、[刷新物化视图（Oracle）](../../../700.reference/300.database-object-management/200.manage-object-of-oracle-mode/500.manage-views-of-oracle-mode/200.manage-materialized-views-of-oracle-mode/400.refresh-materialized-views-of-oracle-mode.md) |
| **增量刷新（快速刷新）** | 基于 **Mlog** 处理变更，适合基表大、变更相对局部的场景；对 SQL 形态有限制。 | 同上；Mlog 见 [物化视图日志（MySQL 模式）](../../../700.reference/300.database-object-management/100.manage-object-of-mysql-mode/600.manage-views-of-mysql-mode/200.manage-materialized-views-of-mysql-mode/200.materialized-views-log-of-mysql-mode.md)、[物化视图日志（Oracle 模式）](../../../700.reference/300.database-object-management/200.manage-object-of-oracle-mode/500.manage-views-of-oracle-mode/200.manage-materialized-views-of-oracle-mode/200.materialized-views-log-of-oracle-mode.md) |
| **自动 / 手动刷新** | 创建时可配置调度；亦可按需手动刷新（如 `DBMS_MVIEW.REFRESH`）。 | [创建物化视图（MySQL）](../../../700.reference/300.database-object-management/100.manage-object-of-mysql-mode/600.manage-views-of-mysql-mode/200.manage-materialized-views-of-mysql-mode/300.create-materialized-views-of-mysql-mode.md)、[创建物化视图（Oracle）](../../../700.reference/300.database-object-management/200.manage-object-of-oracle-mode/500.manage-views-of-oracle-mode/200.manage-materialized-views-of-oracle-mode/300.create-materialized-views-of-oracle-mode.md) |
| **Mlog 自动管理** | 在支持的版本上减少手工维护 Mlog 的成本。 | [物化视图日志（MySQL 模式）](../../../700.reference/300.database-object-management/100.manage-object-of-mysql-mode/600.manage-views-of-mysql-mode/200.manage-materialized-views-of-mysql-mode/200.materialized-views-log-of-mysql-mode.md)、[物化视图日志（Oracle 模式）](../../../700.reference/300.database-object-management/200.manage-object-of-oracle-mode/500.manage-views-of-oracle-mode/200.manage-materialized-views-of-oracle-mode/200.materialized-views-log-of-oracle-mode.md) |
| **刷新资源隔离** | 大批量刷新与 Mlog 维护可与在线负载隔离。 | [物化视图资源隔离（MySQL 模式）](../../../700.reference/300.database-object-management/100.manage-object-of-mysql-mode/600.manage-views-of-mysql-mode/200.manage-materialized-views-of-mysql-mode/800.materialized-views-resource-isolation-of-mysql-mode.md)、[物化视图资源隔离（Oracle 模式）](../../../700.reference/300.database-object-management/200.manage-object-of-oracle-mode/500.manage-views-of-oracle-mode/200.manage-materialized-views-of-oracle-mode/800.materialized-views-resource-isolation-of-oracle-mode.md) |

并行度可以结合 [mview_refresh_dop](../../../700.reference/800.configuration-items-and-system-variables/200.system-variable/300.global-system-variable/4050.mview_refresh_dop-global.md) 等变量调优。

#### 查询改写能力：少改业务 SQL 也能命中物化结果

**查询改写** 指优化器将 **对基表（或等价语义）的查询** 匹配到已存在的物化视图上，用 **预计算结果** 回答查询。

* 创建物化视图时可启用 **`ENABLE QUERY REWRITE`**（具体语法与限制以手册为准）。
* 是否改写受会话/全局变量影响，例如 [query_rewrite_enabled](../../../700.reference/800.configuration-items-and-system-variables/200.system-variable/300.global-system-variable/11050.query_rewrite_enabled-global.md)、[query_rewrite_integrity](../../../700.reference/800.configuration-items-and-system-variables/200.system-variable/300.global-system-variable/11060.query_rewrite_integrity-global.md)。

详细规则与示例参见：[物化视图查询改写（MySQL）](../../../700.reference/300.database-object-management/100.manage-object-of-mysql-mode/600.manage-views-of-mysql-mode/200.manage-materialized-views-of-mysql-mode/500.materialized-views-rewrite-of-mysql-mode.md)、[物化视图查询改写（Oracle）](../../../700.reference/300.database-object-management/200.manage-object-of-oracle-mode/500.manage-views-of-oracle-mode/200.manage-materialized-views-of-oracle-mode/500.materialized-views-rewrite-of-oracle-mode.md)。

## 物化视图能力与选型

为避免与前文“实时数仓位置、刷新能力、查询改写能力”重复，本节仅保留决策层面的精简要点，示例与 SQL 见下文各章节。

### 刷新策略（全量 / 增量 / 自动 / 手动）

* **全量刷新（Complete）**：每次刷新时，物化视图都会重新执行查询语句，并将计算结果覆盖原有视图结果数据。适用于延时要求低、基表数据更新频率低、查询语句比较复杂或数据量较小的场景。
* **增量刷新（Fast）**：又称快速刷新（Fast Refresh），增量刷新需要依赖于物化视图日志（Materialized View Log，Mlog），对查询语句有一定的要求，目前支持单表聚合、多表连接以及多表连接聚合的查询语句，且对聚合函数和连接方式有一些要求适合大数据量与高频变更场景。
* **自动刷新**：在创建物化视图时，可以指定物化视图的刷新间隔，系统会根据配置的刷新时间规则自动调度物化视图的刷新任务。
* **手动刷新**：如果物化视图未配置自动刷新或自动刷新的间隔比较大时，可以手动执行物化视图的刷新命令，让物化视图的数据与基表数据保持同步状态。

详细语法与限制见 [刷新物化视图（MySQL）](../../../700.reference/300.database-object-management/100.manage-object-of-mysql-mode/600.manage-views-of-mysql-mode/200.manage-materialized-views-of-mysql-mode/400.refresh-materialized-views-of-mysql-mode.md)、[刷新物化视图（Oracle）](../../../700.reference/300.database-object-management/200.manage-object-of-oracle-mode/500.manage-views-of-oracle-mode/200.manage-materialized-views-of-oracle-mode/400.refresh-materialized-views-of-oracle-mode.md)。

### 实时物化视图（ON QUERY COMPUTATION）

实时物化视图（Real-Time Materialized Views）顾名思义是可以通过查询物化视图获取实时的数据，查询结果与直接查询基表相同，且能利用物化视图预计算的结果来加速查询。它通过使用 Mlog 的机制来捕获和处理底层基表的更改，确保物化视图中的数据能及时反映最新状态。由于依赖 Mlog，实时物化视图查询语句的要求同增量刷新的物化视图，即只有满足增量刷新物化视图要求，对应的物化视图才可以定义为实时物化视图。

更多关于创建实时物化视图的详细信息，参见 [MySQL 模式下创建物化视图](../../../700.reference/300.database-object-management/100.manage-object-of-mysql-mode/600.manage-views-of-mysql-mode/200.manage-materialized-views-of-mysql-mode/300.create-materialized-views-of-mysql-mode.md) 和 [Oracle 模式下创建物化视图](../../../700.reference/300.database-object-management/200.manage-object-of-oracle-mode/500.manage-views-of-oracle-mode/200.manage-materialized-views-of-oracle-mode/300.create-materialized-views-of-oracle-mode.md)。

### 查询改写（ENABLE QUERY REWRITE）

当想利用物化视图加速查询，但不想修改原始查询语句时，可以利用物化视图的查询改写能力，系统能够自动将查询语句与物化视图的定义进行匹配，如果发现匹配的物化视图，则自动将查询重写为使用物化视图的查询，这样在业务不做变更的情况下可以大大提高查询性能和效率。

更多关于物化视图查询改写的详细信息，参见 [MySQL 模式下物化视图查询改写](../../../700.reference/300.database-object-management/100.manage-object-of-mysql-mode/600.manage-views-of-mysql-mode/200.manage-materialized-views-of-mysql-mode/500.materialized-views-rewrite-of-mysql-mode.md) 和 [Oracle 模式下物化视图查询改写](../../../700.reference/300.database-object-management/200.manage-object-of-oracle-mode/500.manage-views-of-oracle-mode/200.manage-materialized-views-of-oracle-mode/500.materialized-views-rewrite-of-oracle-mode.md)。

### 查询加速优化方式

| 优化维度 | 推荐做法 | 适用场景 | 价值 |
| --- | --- | --- | --- |
| 按查询形态选择物化结果存储 | 宽表分析偏列存，行级访问偏行存。 | 需要在扫描效率与行访问效率之间平衡的场景。 | 减少不必要列读取，降低 I/O，提升查询效率。 |
| 物化视图数据的存储格式 | 根据实际应用场景选择行存或列存。行存适合“结果已聚合、以整行访问为主”；列存适合“宽表 + 聚合分析为主”。 | 报表聚合、宽表分析、固定指标查询。 | 更贴合访问模式，降低扫描开销。 |
| 使用主键物化视图 | 为物化视图定义主键，保证结果集唯一性。 | 需要稳定定位、更新或去重的物化结果。 | 提升查找与更新效率，减少重复数据风险。 |
| 基于物化视图创建索引 | 按过滤列、关联列、排序列创建索引。 | 高频过滤、关联、排序查询。 | 减少全表扫描，加速命中路径。 |
| 按 SLA 调刷新策略 | 在“延迟、新鲜度、资源”之间平衡，按需选择全量/增量、自动/手动刷新。 | 不同实时性等级的 AP/报表场景。 | 避免盲目追求实时，控制资源成本并保持可用性能。 |

物化视图是查询加速的重要手段。对于大规模数据场景，可通过“存储格式 + 索引 + 刷新策略”三者协同，持续优化查询性能。

## 创建物化视图示例

<main id="notice" type='notice'>
  <h4>注意</h4>
  <p>以下示例均是在 MySQL 模式下运行。</p>
</main>

### 创建源表

首先，我们需要创建源表，用于存储原始数据。在这个例子中，我们创建一个 `sales` 表和 `items` 表，其中包含了销售数据和商品信息。

1. 创建销售数据表 `sales`。

    ```sql
    CREATE TABLE sales (
    order_id   INT PRIMARY KEY,
    user_id    INT,
    item_id    INT,
    item_count INT,
    region     VARCHAR(100)
    );
    ```

2. 创建商品信息表 `items`。

    ```sql
    CREATE TABLE items (
    order_id       INT,
    product_id     INT,
    quantity       INT,
    price_per_item DECIMAL(10, 2) NOT NULL,
    pic_url        VARCHAR(1000),
    PRIMARY KEY (order_id, product_id)
    );
    ```

### 创建全量刷新物化视图

我们将创建一个基于 `sales` 表的物化视图。这个物化视图会按产品和地区汇总销售量，以便加速查询。

创建按产品和地区汇总销售量的物化视图 `mv_sales_summary`。

```sql
CREATE MATERIALIZED VIEW mv_sales_summary(PRIMARY KEY(item_id))
    REFRESH COMPLETE
    START WITH sysdate()
        NEXT sysdate() + interval 1 hour
    AS SELECT item_id, region, SUM(item_count) AS total_count
        FROM sales
        GROUP BY item_id, region;
```

在这个创建物化视图的示例中，我们指定了以下特性：

* PRIMARY KEY：为物化视图指定了主键。
* `REFRESH COMPLETE`：表示采用全量刷新方式。
* `START WITH sysdate() NEXT sysdate() + interval 1 hour`：定时刷新，每小时刷新一次。

### 创建增量刷新物化视图

对于一些数据变动较频繁的场景，可以使用增量刷新来提高刷新效率。在创建增量刷新的物化视图前，我们需要基于物化视图的基表创建物化视图日志（Mlog），然后才能成功创建增量刷新的物化视图。OceanBase 数据库默认开启物化视图日志自动管理功能，在创建增量刷新物化视图或实时物化视图时，OceanBase 数据库会自动创建 mlog 或自动更新 mlog 定义。

更多关于物化视图日志的详细信息，参见 [MySQL 模式下物化视图日志](../../../700.reference/300.database-object-management/100.manage-object-of-mysql-mode/600.manage-views-of-mysql-mode/200.manage-materialized-views-of-mysql-mode/200.materialized-views-log-of-mysql-mode.md) 和 [Oracle 模式下物化视图日志](../../../700.reference/300.database-object-management/200.manage-object-of-oracle-mode/500.manage-views-of-oracle-mode/200.manage-materialized-views-of-oracle-mode/200.materialized-views-log-of-oracle-mode.md)。

接下来将展示增量刷新物化视图支持的三种查询场景示例：

#### 单表聚合

创建单表聚合的增量刷新物化视图 `mv_sales_summary_fast`。

```sql
CREATE MATERIALIZED VIEW mv_sales_summary_fast
    REFRESH FAST
    START WITH sysdate() NEXT sysdate() + interval 1 hour
    AS SELECT
           item_id,
           region,
           SUM(item_count) AS total_count
       FROM sales
       GROUP BY item_id, region;
```

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>如果使用 V4.4.2 BP2 之前版本，物化视图定义中聚合函数在 <code>SELECT</code> 子句中必须包含依赖列，详细介绍参见 <a href="../../../700.reference/300.database-object-management/100.manage-object-of-mysql-mode/600.manage-views-of-mysql-mode/200.manage-materialized-views-of-mysql-mode/400.refresh-materialized-views-of-mysql-mode.md">刷新物化视图（MySQL 模式）</a> 和 <a href="../../../700.reference/300.database-object-management/200.manage-object-of-oracle-mode/500.manage-views-of-oracle-mode/200.manage-materialized-views-of-oracle-mode/400.refresh-materialized-views-of-oracle-mode.md">刷新物化视图（Oracle 模式）</a>。</p>
</main>

#### 多表连接

创建多表连接的增量刷新物化视图 `mv_sales_items_join`。

```sql
CREATE MATERIALIZED VIEW mv_sales_items_join
    PARTITION BY HASH(order_id)
        PARTITIONS 10
    REFRESH FAST
        START WITH sysdate()
            NEXT sysdate() + interval 1 hour
    AS SELECT s.order_id AS order_id,
            s.user_id AS customer_id,
            s.item_id AS item_id,
            s.item_count AS quantity,
            s.region AS region,
            i.order_id AS i_id,
            i.product_id AS i_item_id,
            i.price_per_item AS price_per_item,
            i.pic_url AS pic_url
        FROM sales s JOIN items i
            ON s.order_id = i.order_id;
```

#### 多表连接聚合

创建多表连接聚合的增量刷新物化视图 `mv_sales_item_join_group`。

```sql
CREATE MATERIALIZED VIEW mv_sales_item_join_group
    REFRESH FAST
    START WITH sysdate()
        NEXT sysdate() + interval 1 hour
    AS SELECT
            s.item_id AS item_id,
            s.region AS region,
            SUM(s.item_count * i.price_per_item) AS sum_price
        FROM sales s JOIN items i
           ON s.order_id = i.order_id
        GROUP BY item_id, region;
```

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>如果使用 V4.4.2 BP2 之前版本，物化视图定义中聚合函数在 <code>SELECT</code> 子句中必须包含依赖列，详细介绍参见 <a href="../../../700.reference/300.database-object-management/100.manage-object-of-mysql-mode/600.manage-views-of-mysql-mode/200.manage-materialized-views-of-mysql-mode/400.refresh-materialized-views-of-mysql-mode.md">刷新物化视图（MySQL 模式）</a> 和 <a href="../../../700.reference/300.database-object-management/200.manage-object-of-oracle-mode/500.manage-views-of-oracle-mode/200.manage-materialized-views-of-oracle-mode/400.refresh-materialized-views-of-oracle-mode.md">刷新物化视图（Oracle 模式）</a>。</p>
</main>

### 创建实时物化视图

实时物化视图能够在数据变动时保证物化视图的查询结果与基表同步。由于实时物化视图依赖物化视图日志，所以同增量刷新的物化视图，在创建物化视图前需要先创建物化视图日志。OceanBase 数据库默认开启物化视图日志自动管理功能，在创建增量刷新物化视图或实时物化视图时，OceanBase 数据库会自动创建 mlog 或自动更新 mlog 定义。

创建实时物化视图 `mv_sales_summary_com`。

```sql
CREATE MATERIALIZED VIEW mv_sales_summary_com
    REFRESH FORCE
    START WITH sysdate()
        NEXT sysdate() + interval 1 hour
    ENABLE ON QUERY COMPUTATION
    AS SELECT
            item_id,
            region,
            SUM(item_count) AS total_count
        FROM sales
        GROUP BY item_id, region;
```

在这个示例中，我们启用了 `ENABLE ON QUERY COMPUTATION`，使得物化视图在查询时实时更新，以确保获取到最新的数据，且物化视图的查询语句满足增量刷新物化视图的创建要求。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>如果使用 V4.4.2 BP2 之前版本，物化视图定义中聚合函数在 <code>SELECT</code> 子句中必须包含依赖列，详细介绍参见 <a href="../../../700.reference/300.database-object-management/100.manage-object-of-mysql-mode/600.manage-views-of-mysql-mode/200.manage-materialized-views-of-mysql-mode/400.refresh-materialized-views-of-mysql-mode.md">刷新物化视图（MySQL 模式）</a> 和 <a href="../../../700.reference/300.database-object-management/200.manage-object-of-oracle-mode/500.manage-views-of-oracle-mode/200.manage-materialized-views-of-oracle-mode/400.refresh-materialized-views-of-oracle-mode.md">刷新物化视图（Oracle 模式）</a>。</p>
</main>

### 创建列存物化视图

假设我们希望在大数据量分析场景中提高查询效率，可以创建一个列存物化视图。通过列存储的方式，查询时只读取需要的列，显著减少磁盘 I/O。

创建列存物化视图 `wide_sales_column`。

```sql
CREATE MATERIALIZED VIEW wide_sales_column
    WITH COLUMN GROUP(each column)
    REFRESH FAST
    START WITH sysdate()
        NEXT sysdate() + interval 1 hour
    AS SELECT
            s.order_id AS order_id,
            s.user_id AS customer_id,
            s.item_id AS item_id,
            s.item_count AS quantity,
            s.region AS region,
            i.order_id AS i_id,
            i.product_id AS i_item_id,
            i.price_per_item AS price_per_item,
            i.pic_url
        FROM sales s JOIN items i
            ON s.order_id = i.order_id;
```

在这个创建列存物化视图的示例中，我们指定了 `WITH COLUMN GROUP(each column)`，使得物化视图使用列存储格式。这对于 OLAP 场景非常有用，尤其是在大数据量和宽表查询中。

### 创建查询改写的物化视图

在这个示例中，指定 `ENABLE QUERY REWRITE` 创建了一个可以查询改写的全量刷新物化视图。

```sql
CREATE MATERIALIZED VIEW mv_sales_summary_select
    REFRESH COMPLETE
    START WITH sysdate()
        NEXT sysdate() + interval 1 hour
    ENABLE QUERY REWRITE
    AS SELECT item_id, region, SUM(item_count) AS total_count
        FROM sales
        GROUP BY item_id, region;
```

在这个示例中，指定 `ENABLE QUERY REWRITE` 以及 `ENABLE ON QUERY COMPUTATION` 创建了一个可以查询改写的实时物化视图。

创建可以查询改写的实时物化视图 `mv_sales_summary_com_select`。

```sql
CREATE MATERIALIZED VIEW mv_sales_summary_com_select
    REFRESH FAST
        START WITH sysdate() NEXT sysdate() + interval 1 hour
    ENABLE ON QUERY COMPUTATION
    ENABLE QUERY REWRITE
    AS SELECT
            item_id,
            region,
            SUM(item_count) AS total_sales
        FROM sales
        GROUP BY item_id, region;
```

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>如果使用 V4.4.2 BP2 之前版本，物化视图定义中聚合函数在 <code>SELECT</code> 子句中必须包含依赖列，详细介绍参见 <a href="../../../700.reference/300.database-object-management/100.manage-object-of-mysql-mode/600.manage-views-of-mysql-mode/200.manage-materialized-views-of-mysql-mode/400.refresh-materialized-views-of-mysql-mode.md">刷新物化视图（MySQL 模式）</a> 和 <a href="../../../700.reference/300.database-object-management/200.manage-object-of-oracle-mode/500.manage-views-of-oracle-mode/200.manage-materialized-views-of-oracle-mode/400.refresh-materialized-views-of-oracle-mode.md">刷新物化视图（Oracle 模式）</a>。</p>
</main>

### 创建物化视图索引

为了进一步优化查询性能，可以在物化视图上创建索引。物化视图索引有助于加速对物化视图的查询，尤其是在涉及大量数据的情况下。

1. （可选）创建按产品和地区汇总销售量的物化视图 `mv_sales_summary`。

    如果已根据 **创建全量刷新物化视图** 中示例创建物化视图 `mv_sales_summary`，请跳过此步骤。

    ```sql
    CREATE MATERIALIZED VIEW mv_sales_summary(PRIMARY KEY(item_id))
        REFRESH COMPLETE
        START WITH sysdate()
            NEXT sysdate() + interval 1 hour
        AS SELECT item_id, region, SUM(item_count) AS total_count
            FROM sales
            GROUP BY item_id, region;
    ```

2. 在物化视图 `mv_sales_summary` 的 `region` 列上创建索引 `idx_mv_sales_summary`。

    ```sql
    CREATE INDEX idx_mv_sales_summary ON mv_sales_summary (region);
    ```

    这个索引可以加速在 `mv_sales_summary` 物化视图上的查询，尤其是基于 `region` 列的查询。

## 字典与运维视图

排查物化视图、Mlog、刷新任务时，常用系统视图如下（以 **MySQL 模式**为例；完整列表与字段说明以当前版本参考文档为准）。

| DBA 视图 | ALL 视图 | USER 视图 | 功能 |
| -------- | -------- | --------- | ---- |
| `DBA_MVIEW_LOGS` | `ALL_MVIEW_LOGS` | `USER_MVIEW_LOGS` | 描述物化视图日志信息 |
| `DBA_MVIEWS` | `ALL_MVIEWS` | `USER_MVIEWS` | 描述物化视图信息 |
| `DBA_MVREF_STATS_SYS_DEFAULTS` | | `USER_MVREF_STATS_SYS_DEFAULTS` | 描述物化视图刷新历史统计属性的系统范围默认值 |
| `DBA_MVREF_STATS_PARAMS` | | `USER_MVREF_STATS_PARAMS` | 描述与每个物化视图关联的刷新统计信息属性 |
| `DBA_MVREF_RUN_STATS` | | `USER_MVREF_RUN_STATS` | 描述物化视图的每次刷新运行信息，每次运行均由 `REFRESH_ID` 标识 |
| `DBA_MVREF_STATS` | | `USER_MVREF_STATS` | 描述物化视图刷新的基本计时统计信息 |
| `DBA_MVREF_CHANGE_STATS` | | `USER_MVREF_CHANGE_STATS` | 描述所有物化视图的刷新运行关联的基表上的更改数据加载信息 |
| `DBA_MVREF_STMT_STATS` | | `USER_MVREF_STMT_STATS` | 描述刷新语句关联的信息 |

## 相关文档

* 有关物化视图的详细介绍和使用指导，参见 [物化视图概述（MySQL 模式）](../../../700.reference/300.database-object-management/100.manage-object-of-mysql-mode/600.manage-views-of-mysql-mode/200.manage-materialized-views-of-mysql-mode/100.materialized-views-overview-of-mysql-mode.md) 和 [物化视图概述（Oracle 模式）](../../../700.reference/300.database-object-management/200.manage-object-of-oracle-mode/500.manage-views-of-oracle-mode/200.manage-materialized-views-of-oracle-mode/100.materialized-views-overview-of-oracle-mode.md)。
* 有关刷新物化视图的详细介绍和使用指导，参见 [刷新物化视图（MySQL 模式）](../../../700.reference/300.database-object-management/100.manage-object-of-mysql-mode/600.manage-views-of-mysql-mode/200.manage-materialized-views-of-mysql-mode/400.refresh-materialized-views-of-mysql-mode.md) 和 [刷新物化视图（Oracle 模式）](../../../700.reference/300.database-object-management/200.manage-object-of-oracle-mode/500.manage-views-of-oracle-mode/200.manage-materialized-views-of-oracle-mode/400.refresh-materialized-views-of-oracle-mode.md)。
* 有关物化视图日志的详细介绍和使用指导，参见 [物化视图日志（MySQL 模式）](../../../700.reference/300.database-object-management/100.manage-object-of-mysql-mode/600.manage-views-of-mysql-mode/200.manage-materialized-views-of-mysql-mode/200.materialized-views-log-of-mysql-mode.md) 和 [物化视图日志（Oracle 模式）](../../../700.reference/300.database-object-management/200.manage-object-of-oracle-mode/500.manage-views-of-oracle-mode/200.manage-materialized-views-of-oracle-mode/200.materialized-views-log-of-oracle-mode.md)。
* 有关删除物化视图的详细介绍和使用指导，参见 [删除物化视图（MySQL 模式）](../../../700.reference/300.database-object-management/100.manage-object-of-mysql-mode/600.manage-views-of-mysql-mode/200.manage-materialized-views-of-mysql-mode/700.delete-materialized-views-of-mysql-mode.md) 和 [删除物化视图（Oracle 模式）](../../../700.reference/300.database-object-management/200.manage-object-of-oracle-mode/500.manage-views-of-oracle-mode/200.manage-materialized-views-of-oracle-mode/700.delete-materialized-views-of-oracle-mode.md)。
