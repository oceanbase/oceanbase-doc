|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type|MySQL Mode|

# 自定义热点缓存策略运维指南

本文将介绍通过 SQL 语句控制自定义热点缓存策略的后台任务执行，包括挂起、恢复和手动触发操作。

## 语法

自定义热点缓存策略生效后，可以使用 `ALTER SYSTEM` 控制热点缓存任务执行，语法格式如下：

```sql
ALTER SYSTEM storage_cache_policy_executor_option STORAGE_CACHE_POLICY_EXECUTOR;

storage_cache_policy_executor_option:
    SUSPEND
    | RESUME
    | TRIGGER
```

### 参数说明

* `SUSPEND`：用于将后台热点缓存任务挂起，通常用于热数据缓存任务影响到业务或系统稳定性的一个应急手段。
* `RESUME`：用于重新激活处于挂起状态的后台热点缓存任务。
* `TRIGGER`：用于手动立即触发一次热数据缓存任务的执行。

## 示例

* 执行如下命令将租户下所有热点缓存任务挂起。

    ```shell
    obclient> ALTER SYSTEM SUSPEND STORAGE_CACHE_POLICY_EXECUTOR;
    ```

* 执行如下命令，重新激活租户下所有处于挂起状态的热点缓存任务。

    ```shell
    obclient> ALTER SYSTEM RESUME STORAGE_CACHE_POLICY_EXECUTOR;
    ```

* 执行如下命令将立即触发一次热点缓存任务。

    ```shell
    obclient> ALTER SYSTEM TRIGGER STORAGE_CACHE_POLICY_EXECUTOR;
    ```

## 相关文档

* 查看热点缓存任务的实时状态和执行详情，参见 [V$OB_STORAGE_CACHE_TASKS](../../../700.system-views/300.system-view-of-sys-tenant/300.performance-view-of-sys-tenant/34550.v-ob_storage_cache_tasks-of-sys-tenant.md)。
* 有关自定义热点缓存策略介绍信息，参见 [自定义热点缓存策略概述](100.customize-hotspot-cache-overview-of-mysql-mode.md)。