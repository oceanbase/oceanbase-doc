|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type|Oracle Mode|
|slug|modify-random-partition-table-oracle-mode|

# 修改随机分布表

本文将向您介绍如何使用 SQL 语句修改随机分布表。

## 权限要求

修改修改随机分布表需要有 `ALTER` 权限。更多有关 OceanBase 数据库权限的详细介绍，参见 [Oracle 模式下的权限分类](../../../../../600.manage/500.security-and-permissions/300.access-control/200.user-and-permission/300.permission-of-oracle-mode/000.permission-classification-of-oracle-mode.md)。

## 语法

修改随机分布表的 SQL 语句格式如下：

```sql
ALTER TABLE table_name PARTITION BY RANDOM SIZE('size_value');
```

## 动态分区管理属性参数说明

|      **参数**      | **描述** |
|--------------------|----------|
| table_name         | 表名称。|
| SIZE('size_value') | 用于修改随机分布表每个分区的数据容量阈值，超出后触发自动新增分区。取值范围 [1MB, +∞)，支持 `MB`、`GB` 等单位。|

更多有关修改表语法的详细参数说明信息，参见 [ALTER TABLE](../../../../500.sql-reference/100.sql-syntax/300.common-tenant-of-oracle-mode/900.sql-statement-of-oracle-mode/100.ddl-of-oracle-mode/1000.alter-table-of-oracle-mode.md)。

## 示例

1. 创建随机分布表 `tbl1`，并指定分区大小为 2GB。

    ```shell
    obclient> CREATE TABLE tbl1(
        col1 INT,
        col2 VARCHAR2(1024)
        )
        PARTITION BY RANDOM SIZE('2GB');
    ```

2. 查看表 `tbl1` 的定义信息。

    ```shell
    obclient> SHOW CREATE TABLE tbl1;
    ```

    查询结果如下：

    ```shell
    +-------+-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
    | TABLE | CREATE TABLE                                                                                                                                                                                                                                                            |
    +-------+-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
    | TBL1  | CREATE TABLE "TBL1" (
    "COL1" NUMBER(*,0),
    "COL2" VARCHAR2(1024)
    ) COMPRESS FOR ARCHIVE REPLICA_NUM = 1 BLOCK_SIZE = 16384 USE_BLOOM_FILTER = FALSE ENABLE_MACRO_BLOCK_BLOOM_FILTER = FALSE TABLET_SIZE = 134217728 PCTFREE = 0
    partition by random size ('2048MB') |
    +-------+-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
    1 row in set
    ```

3. 修改随机分布表 `tbl1` 的分区大小为 8GB。

    ```sql
    obclient> ALTER TABLE tbl1 PARTITION BY RANDOM SIZE('8GB');
    ```

4. 再次查看表 `test_tbl1` 的定义信息。

    ```shell
    obclient> SHOW CREATE TABLE tbl1;
    ```

    查询结果如下：

    ```shell
    +-------+-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
    | TABLE | CREATE TABLE                                                                                                                                                                                                                                                            |
    +-------+-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
    | TBL1  | CREATE TABLE "TBL1" (
    "COL1" NUMBER(*,0),
    "COL2" VARCHAR2(1024)
    ) COMPRESS FOR ARCHIVE REPLICA_NUM = 1 BLOCK_SIZE = 16384 USE_BLOOM_FILTER = FALSE ENABLE_MACRO_BLOCK_BLOOM_FILTER = FALSE TABLET_SIZE = 134217728 PCTFREE = 0
    partition by random size ('8192MB') |
    +-------+-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
    1 row in set
    ```

## 相关文档

* [随机分布表概述](100.random-partition-table-overview-of-oracle-mode.md)
* [创建随机分布表](200.create-a-random-partition-table-of-oracle-mode.md)