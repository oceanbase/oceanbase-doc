|description||
|---|---|
| machine-translation ||
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||

# ob_java_connector_path

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该配置项从 V4.3.5 BP1 版本开始引入。</p>
</main>

## 功能描述

`ob_java_connector_path` 用于设置 JNI（Java Native Interface）为访问和执行外部表（如 HDFS 外表）所需的 Connector Jar 包的根路径。该路径将与 `ob_java_home` 配置项指定的路径拼接，共同构成 JVM 的类加载路径。

<main id="notice">
  <h4>功能适用性</h4>
  <p>该配置项仅适用于 OceanBase 数据库 MySQL 模式。</p>
</main>

## 权限要求

* 查询配置项
  `sys` 租户和所有用户租户均可以使用 `SHOW PARAMETERS` 语句或视图 `GV$OB_PARAMETERS` 查询该配置项。
* 修改配置项
  仅 `sys` 租户可以修改该配置项，用户租户不能修改该配置项。

## 属性说明

| **属性**  | **描述** |
|---|---|
| 参数类型 | String |
| 默认值 | `""` (空字符串) |
| 是否可修改 | 是，支持通过 `ALTER SYSTEM SET` 语句修改。 |
| 是否重启 OBServer 节点生效 | 否，设置后立即生效。 |

## 使用说明

* 该配置项是启用 OceanBase 数据库 Java 环境以支持 HDFS 外表等 JNI 功能的一部分。通常需要与 `ob_enable_java_env`、`ob_java_home` 和 `ob_java_opts` 配置项配合使用。
* **初始化耗时**：首次初始化 Java 环境可能需要较长时间，可能导致首次查询相关外部表时超时。建议适当调整查询超时时间，后续查询将不会再次初始化。
* **部署一致性**：在多机部署环境下，需要确保所有 OBServer 节点上配置的路径指向相同且存在的目录，以保证功能正常。
* **路径要求**：配置的路径应指向存放 Connector Jar 包的目录。实际交付的包目录下若包含 `current`、`hadoop`、版本目录（如 `vX.X.X`，实际版本号随 OceanBase 版本而定）等条目，建议将 `ob_java_connector_path` 设置为 `current` 目录的路径。
* **环境依赖**：使用此功能前，需要在服务器上正确安装 Java 环境（如 OpenJDK 8）并配置好必要的环境变量（如 `LD_LIBRARY_PATH` 指向 `libhdfs.so` 所在的目录）。

## 配置示例

设置 Java Connector 的路径为 `/home/admin/jni_packages/current`。

```shell
obclient> ALTER SYSTEM SET ob_java_connector_path = '/home/admin/jni_packages/current';
```

## 相关文档

* 启用 Java 环境配置项：[ob_enable_java_env](16630.ob_enable_java_env.md)
* 设置 Java 安装路径配置项：[ob_java_home](16635.ob_java_home.md)
* 设置 JVM 启动参数配置项：[ob_java_opts](16650.ob_java_opts.md)
