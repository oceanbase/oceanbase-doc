|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||

# zone_merge_order

<main id="notice" type='explain'>
<h4>说明</h4>
<p>由于 V4.x 版本不再支持按 Zone 控制合并，也不再支持轮转合并，该配置项从 V4.0.0 版本开始删除。</p>
</main>

## 功能描述

`zone_merge_order` 用于设置 Zone 的轮转合并顺序。

## 属性说明

|      **属性**                | **描述** |
|------------------------------|--------|
| 参数类型                      | String  |
| 默认值                        | 空值     |
| 取值范围                      | N/A   |
| 是否可修改                    | 是，支持通过 `ALTER SYSTEM SET` 语句修改。         |
| 是否重启 OBServer 节点生效    | 否，设置后立即生效。     |

## 使用说明

值为空时，由系统自动决定轮转合并顺序，一般不建议设置，保持默认值即可。


