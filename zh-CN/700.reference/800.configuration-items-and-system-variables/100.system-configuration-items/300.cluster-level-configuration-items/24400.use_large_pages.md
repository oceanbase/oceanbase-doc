|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||
|machine-translation||

# use_large_pages

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该配置项从 V2.2.50 版本开始引入。</p>
</main>

## 功能描述

`use_large_pages` 用于管理数据库使用的内存大页（Huge Pages）。

## 权限要求

* 查询配置项

  `sys` 租户和所有用户租户均可以使用 `SHOW PARAMETERS` 语句或视图 `GV$OB_PARAMETERS` 查询该配置项。

* 修改配置项

  仅 `sys` 租户可以修改该配置项，用户租户不能修改该配置项。

## 属性说明

| **属性** | **描述** |
| -------- | -------- |
| 参数类型             | String      |
| 默认值               | false       |
| 取值范围             | <ul><li>false：禁止使用大页。</li> <li>true：允许混合使用大页和普通页。</li> <li>only：强制系统完全使用大页。</li></ul>    |
| 是否可修改           | 是，支持通过 `ALTER SYSTEM SET` 语句修改。|
| 是否重启 OBSever 节点生效 | 是，需要重启节点生效。 |

## 配置示例

设置数据库禁止使用内存大页。

```shell
obclient> ALTER SYSTEM SET use_large_pages = 'false';
```

## 相关文档

* [内存相关参数](../../../../700.reference/100.oceanbase-database-concepts/1200.observer-node-architecture/500.memory-management/200.memory-related-parameters.md)