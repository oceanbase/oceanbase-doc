|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||
|machine-translation||

# lease_time

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该配置项从 V1.4 版本开始引入。</p>
</main>

## 功能描述

`lease_time` 用于设置心跳租约时长。

## 权限要求

* 查询配置项

  `sys` 租户和所有用户租户均可以使用 `SHOW PARAMETERS` 语句或视图 `GV$OB_PARAMETERS` 查询该配置项。

* 修改配置项

  仅 `sys` 租户可以修改该配置项，用户租户不能修改该配置项。

## 属性说明

| **属性** | **描述** |
| -------- | -------- |
| 参数类型    | Time         |
| 默认值      | 10s          |
| 取值范围    | [1s, 5min] |
| 是否可修改  | 是，支持通过 `ALTER SYSTEM SET` 语句修改。 <main id="notice" type='explain'><h4>说明</h4><p>一般不建议修改该配置项。</p></main>|
| 是否重启 OBServer 节点生效 | 否，设置后立即生效。 |

## 使用说明

Root Service 在心跳租约时长内未收到 OBServer 节点的心跳信息，则认为 OBServer 节点为脱机状态。

## 配置示例

设置心跳租约时长为 10s。

```shell
obclient> ALTER SYSTEM SET lease_time ='10s';
```

## 相关文档

有关 Root Service 的更多介绍，参见 [数据库层高可用](../../../../600.manage/400.high-availability/200.multi-replica-disaster-recovery/300.database-high-availability.md)。