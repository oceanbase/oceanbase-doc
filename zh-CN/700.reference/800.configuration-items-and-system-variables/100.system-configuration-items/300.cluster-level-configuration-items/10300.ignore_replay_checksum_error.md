|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||
|machine-translation||

# ignore_replay_checksum_error

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该配置项从 V1.4 版本开始引入。</p>
</main>

## 功能描述

`ignore_replay_checksum_error` 用于控制是否在事务日志回放时忽略校验和错误。

## 权限要求

* 查询配置项

  `sys` 租户和所有用户租户均可以使用 `SHOW PARAMETERS` 语句或视图 `GV$OB_PARAMETERS` 查询该配置项。

* 修改配置项

  仅 `sys` 租户可以修改该配置项，用户租户不能修改该配置项。

## 属性说明

|      **属性**    |   **描述**   |
|------------------|--------------|
| 参数类型             | Bool                |
| 默认值               | False                |
| 取值范围             | <ul><li>True：忽略</li> <li>False：不忽略</li></ul>     |
| 是否可修改           | 是，支持通过 `ALTER SYSTEM SET` 语句修改。|
| 是否重启 OBServer 节点生效 | 否，设置后立即生效。    |

## 配置示例

设置在事务日志回放时不忽略校验和错误。

```shell
obclient> ALTER SYSTEM SET ignore_replay_checksum_error = False;
```

## 相关文档

[事务日志说明](../../../../700.reference/1500.Components-and-Tools/100.manage/200.ob-admin/300.clog/500.transaction-information-of-clog.md)