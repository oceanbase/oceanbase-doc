|description||
|---|---|
| machine-translation ||
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||
|slug|enable_ttl|

# enable_ttl

<main id="notice" type='explain'>
<h4>说明</h4>
<p>该配置项从 V4.6.0 版本开始引入。</p>
</main>

## 功能描述

`enable_ttl` 用于控制 SQL 模式下是否开启 TTL 功能。开启 TTL 功能后，过期的数据将在 Compaction 任务执行时进行物理删除。

## 权限要求

* 查询配置项

  `sys` 租户和所有用户租户均可以使用 `SHOW PARAMETERS` 语句或视图 `GV$OB_PARAMETERS` 查询该配置项。

* 修改配置项

  `sys` 租户和用户租户均可修改该配置项。

## 属性说明

| **属性**  |    **描述**  |
|-----------|---------------|
| 参数类型    |    Bool              |
| 默认值      |  False  |
| 取值范围    |  <ul><li>`True`：开启 TTL 功能。开启后，系统会启动后台线程周期性处理 TTL 定义下过期的行数据。同时，用户也可以手动触发 TTL 任务。</li><li>`False`：关闭 TTL 功能。关闭后，将不允许系统周期性触发 TTL 任务，也不允许用户手动触发 TTL 任务。</li></ul>  |
| 是否可修改 | 是，支持通过 `ALTER SYSTEM SET` 语句修改。|
| 是否重启 OBServer 节点生效 | 否，设置后立即生效。 |

## 配置示例

* 系统租户为指定用户租户开启 TTL 功能。

  ```shell
  obclient(root@sys)[(none)]> ALTER SYSTEM SET enable_ttl = True TENANT = tenant_name;
  ```

* 用户租户为本租户开启 TTL 功能。

  ```shell
  obclient> ALTER SYSTEM SET enable_ttl = True;
  ```

## 相关文档

* 配置定期触发 TTL 任务的执行时间：[ttl_duty_time](8095.ttl_duty_time.md)

* [使用 TTL（Time To Live）功能](../../../200.system-management/500.manage-data-storage/400.copaction-ttl.md)