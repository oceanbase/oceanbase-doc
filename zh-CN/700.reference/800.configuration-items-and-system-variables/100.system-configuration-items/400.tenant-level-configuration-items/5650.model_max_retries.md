|description||
|---|---|
| machine-translation ||
|keywords||
|dir-name||
|dir-name-en||
|tenant-type|MySQL Mode|
|slug|model_max_retries|

# model_max_retries

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该配置项从 V4.5.0 版本开始引入。</p>
</main>

## 功能描述

`model_max_retries` 用于配置访问大模型的最大重试次数，作用于语义索引的搜索请求。

<main id="notice" >
  <h4>功能适用性</h4>
  <p>该配置项仅适用于 OceanBase 数据库 MySQL 模式。</p>
</main>

## 权限要求

* 查询配置项

  `sys` 租户和所有用户租户均可以使用 `SHOW PARAMETERS` 语句或视图 `GV$OB_PARAMETERS` 查询该配置项。

* 修改配置项

  `sys` 租户和用户租户均可修改该配置项。

## 属性说明

|      **属性**      |  **描述**   |
|------------------|-----------|
| 参数类型             | Int        |
| 默认值               | 2         |
| 取值范围             | [1, +∞) |
| 是否可修改 | 是，支持通过 `ALTER SYSTEM SET` 语句修改。|
| 是否重启 OBServer 节点生效 | 否，设置后立即生效。 |

## 配置示例

设置访问大模型的最大重试次数为 3 次。

```shell
obclient> ALTER SYSTEM SET model_max_retries = 3;
```

## 相关文档

[语义索引](../../../../640.ob-vector-search/200.ob-vector-index/300.ob-semantic-index.md)