|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||
|machine-translation| en |

# external_table_io_range_size

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该配置项从 V4.4.0 版本开始引入。</p>
</main>

## 功能描述

`external_table_io_range_size` 用于设置合并范围的最大大小。如果合并两个连续范围会产生大于此大小的范围，则不会合并它们。

## 权限要求

* 查询配置项

  `sys` 租户和所有用户租户均可以使用 `SHOW PARAMETERS` 语句或视图 `GV$OB_PARAMETERS` 查询该配置项。

* 修改配置项

  `sys` 租户和用户租户均可修改该配置项。

## 属性说明

| **属性** | **描述** |
|----------|----------|
| 参数类型 | Capacity |
| 默认值   | 8MB，以字节为单位 |
| 取值范围 | [0, +∞)|
| 是否可修改 | 是，支持通过 `ALTER SYSTEM SET` 语句修改。|
| 是否重启 OBServer 节点生效 | 否，设置后立即生效。|

## 配置示例

设置合并范围的最大大小为 16MB。

```shell
obclient> ALTER SYSTEM SET external_table_io_range_size = '16MB';
```

## 相关文档

有关设置两个连续 IO 范围之间的最大距离的介绍，参见 [external_table_io_hole_size](3230.external_table_io_hole_size.md)。