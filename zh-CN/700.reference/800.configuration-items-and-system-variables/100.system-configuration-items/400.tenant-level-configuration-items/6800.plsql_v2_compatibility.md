|description||
|---|---|
| machine-translation ||
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||
|slug|plsql_v2_compatibility|

# plsql_v2_compatibility

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该配置项从 V3.2.3 版本开始引入。</p>
</main>

## 功能描述

`plsql_v2_compatibility` 用于指定 Oracle 8 的兼容功能。

<main id="notice">
  <h4>功能适用性</h4>
  <p>该配置项仅适用于 OceanBase 数据库 Oracle 模式。</p>
</main>

## 权限要求

* 查询配置项

  `sys` 租户和所有用户租户均可以使用 `SHOW PARAMETERS` 语句或视图 `GV$OB_PARAMETERS` 查询该配置项。

* 修改配置项

  `sys` 租户和用户租户均可修改该配置项。

## 属性说明

|        属性      |  描述  |
|------------------|-------|
| 参数类型             | Bool                    |
| 默认值               | False                   |
| 取值范围             | <ul><li>True：兼容 </li><li>False：不兼容 </li></ul>   |
| 是否可修改           | 是，支持通过 `ALTER SYSTEM SET` 语句修改。|
| 是否重启 OBServer 节点生效 | 否，设置后立即生效。  |

## 配置示例

设置不兼容 Oracle 8 的功能。

```shell
obclient> ALTER SYSTEM SET plsql_v2_compatibility=false;
```