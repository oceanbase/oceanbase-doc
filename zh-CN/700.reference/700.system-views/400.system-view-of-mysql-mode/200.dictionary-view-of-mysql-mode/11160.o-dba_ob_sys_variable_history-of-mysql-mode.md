|description||
|---|---|
| machine-translation ||
|keywords||
|dir-name||
|dir-name-en||
|tenant-type| MySQL Mode |
|slug|o-dba_ob_sys_variable_history-mysql-mode|

# oceanbase.DBA_OB_SYS_VARIABLE_HISTORY

<main id="notice" type='explain'>
<h4>说明</h4>
<p>该视图从 V4.6.1 版本开始引入。</p>
</main>

## 功能描述

视图 `DBA_OB_SYS_VARIABLE_HISTORY` 用于展示当前租户的所有系统变量的修改历史。

## 字段说明

| 字段名称 | 类型 | 是否可以为 NULL | 描述 |
| ------- | ---- | -------------- | ---- |


## 查询示例

用户租户查询本租户的系统变量 `ob_query_timeout` 的修改历史。

```shell
obclient(root@mysql001)[oceanbase]> SELECT * FROM oceanbase.DBA_OB_SYS_VARIABLE_HISTORY WHERE NAME='ob_query_timeout';
```

查询结果如下：

```shell
```