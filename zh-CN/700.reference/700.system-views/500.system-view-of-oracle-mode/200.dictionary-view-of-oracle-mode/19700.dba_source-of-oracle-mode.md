|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type|Oracle Mode|
|machine-translation||

# DBA_SOURCE

<main id="notice" type='explain'>
<h4>说明</h4>
<p>该视图从 V2.2.30 版本开始引入。</p>
</main>

## 功能描述

展示当前租户下所有存储对象的文本源。其列与 `ALL_SOURCE` 中的列相同。

  <main id="notice" >
    <h4>功能适用性</h4>
    <p>该视图仅适用于 OceanBase 数据库 Oracle 模式。</p>
  </main>

## 字段说明

|   **字段名称**    |    **类型**    | **是否可以为 NULL** |       **描述**       |
|---------------|--------------|----------------|--------------------|
| OWNER         | VARCHAR2(30) | NO             | 对象的拥有者           |
| NAME          | VARCHAR2(30) | NO             | 对象名                |
| TYPE          | VARCHAR2(12) | NO             | 对象类型               |
| LINE          | NUMBER       | NO             | TEXT 字段对应的源代码中的行号  |
| TEXT          | CLOB         | NO             | 当前行的源代码            |
| ORIGIN_CON_ID | NUMBER(38)   | NO             | 对象所属的租户的租户 ID。如果是系统包，则值为 0|

## 查询示例

查看当前租户下所有存储对象的文本源，展示前 2 条记录。

```shell
obclient [SYS]> SELECT * FROM SYS.DBA_SOURCE WHERE ROWNUM<=2\G
```

查询结果如下：

```shell
*************************** 1. row ***************************
        OWNER: SYS
         NAME: JOB_UTILS
         TYPE: PACKAGE
         LINE: 1
         TEXT: PACKAGE JOB_UTILS IS
  PROCEDURE insert_date;
  PROCEDURE insert_date_program ( job IN VARCHAR2, job_name IN VARCHAR2);
END;
ORIGIN_CON_ID: 1004
*************************** 2. row ***************************
        OWNER: SYS
         NAME: JOB_UTILS
         TYPE: PACKAGE BODY
         LINE: 1
         TEXT: PACKAGE BODY JOB_UTILS IS
  PROCEDURE insert_date IS
  BEGIN
    INSERT INTO DBMS_SCHEDULER_T1 VALUES(TO_CHAR(sysdate));
    COMMIT;
  END;
  PROCEDURE insert_date_program ( job IN VARCHAR2, job_name IN VARCHAR2) IS
  BEGIN
    INSERT INTO DBMS_SCHEDULER_T2 VALUES(sysdate, job, job_name);
    COMMIT;
  END;
END;
ORIGIN_CON_ID: 1004
2 rows in set
```

## 相关视图或文档

* 查看当前用户可访问的所有存储对象的文本源：[ALL_SOURCE](4000.all_source-of-oracle-mode.md)

* 查看当前用户拥有的存储对象的文本源：[USER_SOURCE](29600.user_source-of-oracle-mode.md)