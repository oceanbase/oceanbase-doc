|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type|Oracle Mode|
|machine-translation||

# DBA_SUBPART_KEY_COLUMNS

<main id="notice" type='explain'>
<h4>说明</h4>
<p>该视图从 V2.2.77 版本开始引入。</p>
</main>

## 功能描述

展示本租户所有分区对象中子分区键包含的列。

## 字段说明

|      **字段名称**      |     **类型**     | **是否可以为 NULL** |                                                              **描述**                                                              |
|--------------------|----------------|----------------|----------------------------------------------------------------------------------------------------------------------------------|
| OWNER              | VARCHAR2(128)  | NO             | 分区表或分区索引的拥有者                                  |
| NAME               | VARCHAR2(128)  | NO             | 分区表或分区索引的名字                                   |
| OBJECT_TYPE        | CHAR(5)        | NO             | 分区对象的类型： <li> TABLE   <li> INDEX    |
| COLUMN_NAME        | VARCHAR2(4000) | NO             | 列的名字                                          |
| COLUMN_POSITION    | NUMBER         | NO             | 列在分区键中的位置                                     |
| COLLATED_COLUMN_ID | NUMBER         | NO             | 目前暂未支持该字段，当前该字段默认为 NULL                       |

## 查询示例

用户租户查看本租户下分区表 `TBL2_F_RL` 中子分区建包含的列。

```shell
obclient [SYS]> SELECT * FROM SYS.DBA_SUBPART_KEY_COLUMNS WHERE NAME='TBL2_F_RL';
```

查询结果如下：

```shell
+-------+-----------+-------------+-------------+-----------------+--------------------+
| OWNER | NAME      | OBJECT_TYPE | COLUMN_NAME | COLUMN_POSITION | COLLATED_COLUMN_ID |
+-------+-----------+-------------+-------------+-----------------+--------------------+
| SYS   | TBL2_F_RL | TABLE       | COL2        |               1 |               NULL |
+-------+-----------+-------------+-------------+-----------------+--------------------+
1 row in set
```

## 相关视图或文档

* 查看所有租户下所有分区对象中子分区键包含的列：[CDB_SUBPART_KEY_COLUMNS](../../300.system-view-of-sys-tenant/200.dictionary-view-of-sys-tenant/12500.o-cdb_subpart_key_columns-of-sys-tenant.md)

* 查看当前用户可访问的分区对象中子分区键包含的列：[ALL_SUBPART_KEY_COLUMNS](4300.all_subpart_key_columns-of-oracle-mode.md)

* 查看当前用户所拥有的分区对象中子分区键包含的列：[USER_SUBPART_KEY_COLUMNS](29900.user_subpart_key_columns-of-oracle-mode.md)