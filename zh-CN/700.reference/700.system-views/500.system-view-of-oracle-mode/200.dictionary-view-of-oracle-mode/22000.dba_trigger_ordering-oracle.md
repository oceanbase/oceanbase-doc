|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type|Oracle Mode|
|machine-translation||

# DBA_TRIGGER_ORDERING

<main id="notice" type='explain'>
<h4>说明</h4>
<p>该视图从 V4.1.0 版本开始引入。</p>
</main>

## 功能描述

展示当前租户下所有触发器的排序。

## 字段说明

| **字段** | **类型** | **是否可以为 NULL** | **描述** |
| --- | --- | --- | --- |
| TRIGGER_OWNER | VARCHAR2(128) | YES | 触发器拥有者 |
| TRIGGER_NAME | VARCHAR2(128) | NO | 触发器名字 |
| REFERENCED_TRIGGER_OWNER | VARCHAR2(128) | YES | 被引用的触发器拥有者 |
| REFERENCED_TRIGGER_NAME | VARCHAR2(128) | YES | 被引用的触发器名字 |
| ORDERING_TYPE | VARCHAR2(8) | NO | 排序的类型 |

## 查询示例

查看当前租户下所有触发器的排序。

```shell
obclient[SYS]> SELECT * FROM SYS.DBA_TRIGGER_ORDERING;
```

查询结果如下：

```shell
+---------------+------------------+--------------------------+-------------------------+---------------+
| TRIGGER_OWNER | TRIGGER_NAME     | REFERENCED_TRIGGER_OWNER | REFERENCED_TRIGGER_NAME | ORDERING_TYPE |
+---------------+------------------+--------------------------+-------------------------+---------------+
| SYS           | TRG_AUDIT_DETAIL | SYS                      | TRG_AUDIT_BASE          | FOLLOWS       |
+---------------+------------------+--------------------------+-------------------------+---------------+
1 row in set
```

## 相关视图或文档

* 查看当前用户可访问的触发器的排序：[ALL_TRIGGER_ORDERING](5800.all_trigger_ordering-oracle.md)

* 查看当前用户拥有的触发器的排序：[USER_TRIGGER_ORDERING](31600.user_trigger_ordering-oracle.md)

* 有关触发器的介绍和使用，参见 [创建和管理触发器](../../../300.database-object-management/200.manage-object-of-oracle-mode/800.manage-triggers-of-oracle-mode/100.trigger-overview-of-oracle-mode.md)。