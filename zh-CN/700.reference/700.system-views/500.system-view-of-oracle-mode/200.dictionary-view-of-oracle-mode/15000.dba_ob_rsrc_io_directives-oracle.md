|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type|Oracle Mode|
|machine-translation||

# DBA_OB_RSRC_IO_DIRECTIVES

<main id="notice" type='explain'>
<h4>说明</h4>
<ul><li>该视图从 V4.1.0 版本开始引入。</li>
<li>当前版本中，建议使用视图 <a href="15020.dba_ob_rsrc_directives-of-oracle-mode.md">DBA_OB_RSRC_DIRECTIVES</a> 来查看租户在 Resource Manager 中的各类资源配置参数信息。</li></ul>
</main>

## 功能描述

展示 Resource Manager 中的 IO 资源配置情况。

## 字段说明

| **字段** | **类型** | **是否可以为 NULL** | **描述** |
| --- | --- | --- | --- |
| PLAN | VARCHAR2(128) | NO | 指令所属资源计划名 |
| GROUP_OR_SUBPLAN | VARCHAR2(128) | NO | 指令控制的资源组名 |
| COMMENTS    | VARCHAR2(2000) | YES | 资源计划备注 |
| MIN_IOPS    |  NUMBER(38) | NO | 出现 IO 争用时，资源组预留的 IOPS 资源，所有资源组指定的 `MIN_IOPS` 的总和不超过 100，默认值为 `0`     |
| MAX_IOPS    |  NUMBER(38) | NO | 资源组最大可使用的 IOPS 资源，所有资源组指定的 `MAX_IOPS` 的总和可以超过 100，默认值为 `100`     |
| WEIGHT_IOPS |  NUMBER(38) | NO | 在发生 IO 资源争用的情况下 IOPS 的权重值，所有资源组指定的 `WEIGHT_IOPS` 的总和可以超过 100，在满足 `MIN_IOPS`、`MAX_IOPS` 设置的前提下，IO 资源会按照权重归一化值去分配，默认值为 `0`     |

## 查询示例

用户租户查询本租户 Resource Manager 中的 IO 资源配置情况。

```shell
obclient [SYS]> SELECT * FROM SYS.DBA_OB_RSRC_IO_DIRECTIVES;
```

查询结果如下：

```shell
+--------+------------------+-----------+----------+----------+-------------+
| PLAN   | GROUP_OR_SUBPLAN | COMMENTS  | MIN_IOPS | MAX_IOPS | WEIGHT_IOPS |
+--------+------------------+-----------+----------+----------+-------------+
| PLAN_A | BIG_GROUP        | TP 优先   |       20 |      100 |          20 |
+--------+------------------+-----------+----------+----------+-------------+
1 row in set
```

## 相关文档或视图

[DBA_OB_RSRC_DIRECTIVES](15020.dba_ob_rsrc_directives-of-oracle-mode.md)