|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type|Oracle Mode|
|machine-translation||

# ALL_OB_SOURCE

<main id="notice" type='explain'>
 <h4>说明</h4>
 <p>该视图从 V4.3.5 BP4 版本开始引入。</p>
</main>

## 功能描述

视图 `ALL_OB_SOURCE` 用于展示当前用户可访问的存储对象的源代码信息。

## 字段说明

| **字段名称** | **类型**  | **是否可以为 NULL** | **描述**                               |
|------------|-------------|---------------------|----------------------------------------|
| OWNER         | VARCHAR2(128)  | NO   | 对象所有者的用户名。 |
| TYPE          | VARCHAR2(12)   | NO   | 对象类型。 |
| LINE          | NUMBER(38)     | NO   | 源代码行号，标识该行在对象定义中的位置。 |
| TEXT          | VARCHAR2(4000) | NO   | 源代码文本，存储该行对应的实际代码内容。 |
| ORIGIN_CON_ID | NUMBER(38)     | NO   | 租户 ID，标识对象所属的租户。 |

## 查询示例

执行如下命令，查看当前用户可访问的存储对象的源代码信息，展示前 5 条记录。

```shell
obclient [oceanbase]>SELECT * FROM ALL_OB_SOURCE WHERE rownum <=5;
```

查询结果如下：

```shell
+-------+------------+---------+------+--------------------------------------------------------------------+---------------+
| OWNER | NAME       | TYPE    | LINE | TEXT                                                               | ORIGIN_CON_ID |
+-------+------------+---------+------+--------------------------------------------------------------------+---------------+
| SYS   | dbms_stats | PACKAGE |    1 | PACKAGE dbms_stats AUTHID CURRENT_USER                             |             1 |
| SYS   | dbms_stats | PACKAGE |    2 | DECLARE DEFAULT_METHOD_OPT     VARCHAR(1) DEFAULT 'Z';             |             1 |
| SYS   | dbms_stats | PACKAGE |    3 | DECLARE DEFAULT_GRANULARITY    VARCHAR(1) DEFAULT 'Z';             |             1 |
| SYS   | dbms_stats | PACKAGE |    4 | DECLARE AUTO_SAMPLE_SIZE       DECIMAL(20, 10) DEFAULT 0;          |             1 |
| SYS   | dbms_stats | PACKAGE |    5 | DECLARE DEFAULT_STAT_CATEGORY  VARCHAR(20) DEFAULT 'OBJECT_STATS'; |             1 |
+-------+------------+---------+------+--------------------------------------------------------------------+---------------+
```

## 相关文档

[DBA_OB_SOURCE](15065.dba_ob_source-of-oracle-mode.md)
[USER_OB_SOURCE](27850.user_ob_source-of-oracle-mode.md)