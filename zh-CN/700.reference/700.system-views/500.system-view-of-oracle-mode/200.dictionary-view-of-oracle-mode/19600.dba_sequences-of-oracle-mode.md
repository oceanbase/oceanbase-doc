|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type|Oracle Mode|
|machine-translation||

# DBA_SEQUENCES

<main id="notice" type='explain'>
<h4>说明</h4>
<p>该视图从 V2.2.77 版本开始引入。</p>
</main>

## 功能描述

展示当前租户下的所有序列。

## 字段说明

| 字段名称        | 类型          | 是否可以为 NULL | 描述              |
|----------------|---------------|----------------|-------------------|
| SEQUENCE_OWNER | VARCHAR2(128) | NO   | 序列所在的数据库的名称   |
| SEQUENCE_NAME  | VARCHAR2(128) | NO   | 序列名称     |
| MIN_VALUE    | NUMBER(28)    | NO             | SEQUENCE 可取的最小值（含最小值）   |
| MAX_VALUE    | NUMBER(28)    | NO             | SEQUENCE 可取的最大值（含最大值）   |
| INCREMENT_BY | NUMBER(28)    | NO             | 每次取值的步长，可为正整数或负整数，不可为 0 |
| CYCLE_FLAG   | CHAR(1)       | NO             | 是否可以循环取值                |
| ORDER_FLAG   | CHAR(1)       | NO             | 取值是否保证全局有序              |
| CACHE_SIZE   | NUMBER(28)    | NO             | 每个节点缓存值的数量              |
| LAST_NUMBER  | NUMBER(38)    | NO             | 记录下一个可分配给节点的值           |

## 查询示例

查看本租户下所有序列的详细信息。

```shell
obclient [SYS]> SELECT * FROM SYS.DBA_SEQUENCES;
```

查询结果如下：

```shell
+----------------+---------------+-----------+------------------------------+--------------+------------+------------+------------+-------------+
| SEQUENCE_OWNER | SEQUENCE_NAME | MIN_VALUE | MAX_VALUE                    | INCREMENT_BY | CYCLE_FLAG | ORDER_FLAG | CACHE_SIZE | LAST_NUMBER |
+----------------+---------------+-----------+------------------------------+--------------+------------+------------+------------+-------------+
| SYS            | SEQ_T1        |         1 | 9999999999999999999999999999 |            1 | N          | N          |         50 |       10050 |
+----------------+---------------+-----------+------------------------------+--------------+------------+------------+------------+-------------+
1 row in set
```

## 相关视图或文档

* 查看当前用户可访问的序列：[ALL_SEQUENCES](3900.all_sequences-of-oracle-mode.md)

* 查看当前用户拥有的序列：[USER_SEQUENCES](29500.user_sequences-of-oracle-mode.md)