|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type|Oracle Mode|
|machine-translation||

# USER_SYNONYMS

<main id="notice" type='explain'>
<h4>说明</h4>
<p>该视图从 V2.2.30 版本开始引入。</p>
</main>

## 功能描述

展示当前用户所拥有的同义词。

  <main id="notice" >
    <h4>功能适用性</h4>
    <p>该视图仅适用于 OceanBase 数据库 Oracle 模式。</p>
  </main>

## 字段说明

|   **字段名称**   |    **类型**     | **是否可以为 NULL** |                          **描述**                          |
|--------------|---------------|----------------|----------------------------------------------------------|
| SYNONYM_NAME | VARCHAR2(128) | NO             | 同义词名字                                                    |
| TABLE_OWNER  | VARCHAR2(128) | NO             | 同义词引用对象的名字，这里的TABLE_NAME 仅仅是列名而已，同义词也可以是对其他对象（例如视图等）的同义词 |
| TABLE_NAME   | VARCHAR2(128) | NO             | 同义词引用的对象名字                                               |
| DB_LINK      | VARCHAR2(128) | NO             | 如果引用的是 DB Link，则表示 DB Link 的名字                           |

## 查询示例

查看当前用户拥有的所有同义词。

```shell
obclient [SYS]> SELECT * FROM SYS.USER_SYNONYMS;
```

查询结果如下：

```shell
+--------------+-------------+------------+---------+---------------+
| SYNONYM_NAME | TABLE_OWNER | TABLE_NAME | DB_LINK | ORIGIN_CON_ID |
+--------------+-------------+------------+---------+---------------+
| S1           | SYS         | TEST       | NULL    |             0 |
+--------------+-------------+------------+---------+---------------+
1 row in set
```

## 相关视图或文档

* 查看当前租户下的所有同义词：[ALL_SYNONYMS](4500.all_synonyms-of-oracle-mode.md)

* 查看当前租户下的所有同义词：[DBA_SYNONYMS](20500.dba_synonyms-of-oracle-mode.md)

* 有关同义词的更多说明及使用，参见 [管理同义词](../../../300.database-object-management/200.manage-object-of-oracle-mode/700.manage-synonyms-of-oracle-mode.md)。