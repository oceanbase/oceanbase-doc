|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type||
|machine-translation|ja-marker|
|slug|try-out-oceanbase-hot-row-update-capability|

# 体验 OceanBase 数据库热点行更新能力

随着在线交易、电商行业的发展，业务系统的热点并发压力逐渐成为一种挑战。热点账户短时间内余额大量更新，或者热门商品在营销活动中限时抢购，都是这种场景的直接体现。热点更新的本质是短时间内对数据库中的同一行数据的某些字段值进行高并发的修改（余额，库存等），这其中的瓶颈主要在于关系型数据库为了保持事务一致性，对数据行的更新都需要经过 “加锁->更新->写日志提交->释放锁” 的过程，而这个过程实质上是串行的。所以，提高热点行更新能力，一方面要尽可能缩短持有锁的时间，另一方面也要降低大量事务并发提交时的磁盘 I/O 开销。

虽然学术界很早就提出了 “提前解行锁（Early Lock Release）” 的方案（即 ELR），但是因为 ELR 的异常处理场景非常复杂，业界很少有成熟的工业实现。OceanBase 数据库在这个问题上通过持续的探索，提出了一种基于分布式架构的实现方式，提升类似业务场景中单行并发更新的能力，作为 OceanBase 数据库 “可扩展的 OLTP” 中的关键能力之一。

除了从锁的维度优化，OceanBase 数据库还提供了另一种热点行优化方案——Group Commit（组提交）。在高并发热点行更新场景下，大量事务在短时间内各自提交 Redo Log，每次提交都至少对应一次磁盘写入，磁盘 I/O 很容易成为吞吐瓶颈。Group Commit 将多个并发事务的 Redo Log 合并为一次磁盘写入，把多次刷盘减少到一次，从而大幅提升热点行的写入吞吐。

本篇文章中，将通过构造多并发单行更新的场景，分别介绍 OceanBase 数据库 ELR 和 Group Commit 两种热点行优化方案的使用方法和效果对比。因为是在多并发压力场景下验证，建议至少使用和本例中相同的节点规格进行体验和验证，达到更好的效果。关于 ELR 和 Group Commit 的设计和原理实现，因其较为复杂，本文暂不做详细讨论。

## 热点行 ELR

本例中使用一台 16C-128GB 配置的节点，下面分步骤体验 OceanBase 数据库 ELR 特性。

### 步骤一：创建测试表，插入测试数据

首先在测试库中创建一张表，并插入测试数据。

```sql
CREATE TABLE `sbtest1` (
   `id` int(11) NOT NULL AUTO_INCREMENT,
   `k` int(11) NOT NULL DEFAULT '0',
   `c` char(120) NOT NULL DEFAULT '',
   `pad` char(60) NOT NULL DEFAULT '',
   PRIMARY KEY (`id`)
);

INSERT INTO sbtest1 VALUES(1,0,'aa','aa');
```

本例中通过类似 `UPDATE sbtest1  SET k=k+1 WHERE id=1` 的语句，以主键查询方式针对 `k` 列进行并发更新，您也可以插入更多数据进行测试，但因为是针对单行并发的压测，对整体结果基本没有影响。

### 步骤二：构造并发更新场景

本例中，使用 Python 多线程的方式来模拟并发更新，同时启动 50 个 Thread，每个 Thread 并发的对 id=1 的行数据将 k 字段值 +1。您在自己搭建的环境中也可以直接使用下面的脚本 `ob_elr.py` 进行测试，只需要将脚本中的数据库连接信息修改一下即可使用。

```python
#!/usr/bin/env python3

from concurrent.futures import ThreadPoolExecutor
import pymysql
import time
import threading

# database connection info
config = {
    'user': 'root@test',
    'password': '****',
    'host': 'xxx.xxx.xxx.xxx',
    'port': 2881,
    'database': 'test'
}

# parallel thread and updates in each thread
parallel = 50
batch_num = 2000

# update query
def update_elr():
    update_hot_row = ("update sbtest1 set k=k+1 where id=1")
    cnx = pymysql.connect(**config)
    cursor = cnx.cursor()

    for i in range(0,batch_num):
        cursor.execute(update_hot_row)
    cursor.close()
    cnx.close()

start=time.time()

with ThreadPoolExecutor(max_workers=parallel) as pool:
    for i in range(parallel):
        pool.submit(update_elr)

end = time.time()
elapse_time = round((end-start),2)

print('Parallel Degree:',parallel)
print('Total Updates:',parallel*batch_num)
print('Elapse Time:',elapse_time,'s')
print('TPS on Hot Row:' ,round(parallel*batch_num/elapse_time,2),'/s')
```

### 步骤三：默认配置下执行测试

作为对比参照，先在默认不开启 ELR 的情况下测试，在测试机器上直接执行 `ob_elr.py` 脚本。

本例中采用 50 并发数，总计更新 100000 次。

```shell
./ob_elr.py
```

执行完成，测试脚本输出执行时间和 TPS：

```shell
[root@obce00 ~]# ./ob_elr.py
Parallel Degree: 50
Total Updates: 100000
Elapse Time: 54.5 s
TPS on Hot Row: 1834.86 /s
```

测试结果如下：

在不开启 ELR 的默认配置下，本例测试环境中，单行并发更新 TPS 为1834.86/s。

### 步骤四：打开 OceanBase 数据库 ELR 配置

接下来开启 OceanBase 数据库的热点行功能，首先需要使用 `root` 用户登录集群的 `sys` 租户。

```shell
[root@obce00 ~]# obclient -h127.0.0.1 -P2881 -uroot@sys -Doceanbase -A -p -c
```

然后进行如下两个参数的设置。其中 `enable_early_lock_release` 参数的生效范围，既可以指定具体租户，也可以指定全部租户，即 `tenant=all`。

```sql
ALTER SYSTEM SET _max_elr_dependent_trx_count = 1000;
ALTER SYSTEM SET enable_early_lock_release = true tenant = test;
```

### 步骤五：开启 OceanBase 数据库 ELR 进行测试

开启热点行功能后，再次执行测试。在执行前先查看表 `sbtest1` 中 `id=1` 的记录，`k` 字段值为 100000，这是因为刚刚执行了一轮默认配置下的 100000 次的更新。

```shell
SELECT * FROM sbtest1 WHERE id=1;
+----+--------+----+-----+
| id | k      | c  | pad |
+----+--------+----+-----+
|  1 | 100000 | aa | aa  |
+----+--------+----+-----+
1 row in set
```

接下来在测试机器上再次执行 `ob_elr.py`。仍然采用 50 并发数，总计更新 100000 次。

```shell
./ob_elr.py
```

执行完成，测试脚本会输出执行时间和 TPS：

```shell
[root@obce00 ~]# ./ob_elr.py
Parallel Degree: 50
Total Updates: 100000
Elapse Time: 12.16 s
TPS on Hot Row: 8223.68 /s
```

测试结果如下：

OceanBase 数据库在开启 ELR 提前解行锁能力后，单行更新的 TPS 达到 8223.68/s，相比默认配置下提升 4.5 倍左右。

同时可以看到表 `sbtest1` 的 `k` 值为 200000，即本次也更新了 100000 次。

```sql
SELECT * FROM sbtest1 WHERE id=1;
+----+--------+----+-----+
| id | k      | c  | pad |
+----+--------+----+-----+
|  1 | 200000 | aa | aa  |
+----+--------+----+-----+
1 row in set
```

本例中仅介绍了单行并发更新的场景，OceanBase 数据库的 ELR 能力还支持多语句事务的并发更新，根据语句数量和场景差异，同样可以获得显著的性能提升。

此外 OceanBase 数据库的 ELR 还可以应用在多地部署高网络延迟的场景中，例如单个事务在默认场景下 30 ms，并发下开启 ELR 可以获得近百倍的 TPS 吞吐量提升。

由于 OceanBase 数据库的日志协议基于 Multi-Paxos 构建，并且优化了 2PC 提交过程，OceanBase 数据库在开启 ELR 后，如果发生节点宕机重启、Leader 切换，仍然可以保证事务的一致性。

## 热点行 Group Commit

<main id="notice" >
    <h4>功能适用性</h4>
    <p>该功能仅适用于 OceanBase 数据库企业版。</p>
</main>

在高并发热点行更新场景下，大量事务在提交时需要各自将 Redo Log 刷入磁盘，每次提交至少对应一次磁盘写入，磁盘 I/O 开销不可忽视。OceanBase 数据库提供了 Group Commit（组提交）方案来解决这一问题：将多个并发事务的 Redo Log 合并为一组，聚合成一次磁盘写入，把多次刷盘变成一次，从而大幅降低磁盘 I/O 压力，提升热点行的写入吞吐。

本例中使用一台 16C-128GB 配置的节点，下面分步骤体验 OceanBase 数据库热点行 Group Commit 特性。

### 步骤一：创建测试表，插入测试数据

首先在测试库中创建一张表，并插入测试数据。

```sql
CREATE TABLE `sbtest2` (
   `id` int(11) NOT NULL AUTO_INCREMENT,
   `k` int(11) NOT NULL DEFAULT '0',
   `c` char(120) NOT NULL DEFAULT '',
   `pad` char(60) NOT NULL DEFAULT '',
   PRIMARY KEY (`id`)
);

INSERT INTO sbtest2 VALUES(1,0,'aa','aa');
```

本例中通过类似 `UPDATE sbtest2 SET k=k+1 WHERE id=1` 的语句，以主键查询方式针对 `k` 列进行并发更新。

### 步骤二：构造并发更新场景

本例中，使用 Java 多线程的方式来模拟并发更新，同时启动 50 个 Thread，每个 Thread 在 autocommit 模式下通过 PreparedStatement 执行携带 `GROUP_COMMIT` Hint 的 `UPDATE` 语句 80000 次。您在自己搭建的环境中可以直接使用下面的代码 `GroupCommitTest.java` 进行测试，只需要将代码中的数据库连接信息修改一下即可使用。

```java
import java.sql.Connection;
import java.sql.DriverManager;
import java.sql.PreparedStatement;
import java.util.concurrent.CountDownLatch;
import java.util.concurrent.ExecutorService;
import java.util.concurrent.Executors;

public class GroupCommitTest {

    static final String URL = "jdbc:oceanbase://xxx.xxx.xxx.xxx:2881/test?useServerPrepStmts=true&cachePrepStmts=true";
    static final String USER = "root@test";
    static final String PASSWORD = "****";

    static final int PARALLEL = 50;
    static final int BATCH_NUM = 80000;

    public static void main(String[] args) throws Exception {
        ExecutorService pool = Executors.newFixedThreadPool(PARALLEL);
        CountDownLatch latch = new CountDownLatch(PARALLEL);

        long start = System.currentTimeMillis();

        for (int i = 0; i < PARALLEL; i++) {
            pool.submit(() -> {
                try (Connection conn = DriverManager.getConnection(URL, USER, PASSWORD)) {
                    conn.setAutoCommit(true);

                    PreparedStatement ps = conn.prepareStatement(
                        "UPDATE /*+ GROUP_COMMIT(TRUE) */ sbtest2 SET k = k + 1 WHERE id = ?"
                    );

                    for (int j = 0; j < BATCH_NUM; j++) {
                        ps.setInt(1, 1);
                        ps.executeUpdate();
                    }
                    ps.close();
                } catch (Exception e) {
                    e.printStackTrace();
                }
                latch.countDown();
            });
        }

        latch.await();
        pool.shutdown();

        long end = System.currentTimeMillis();
        double elapse = (end - start) / 1000.0;

        System.out.println("Parallel Degree: " + PARALLEL);
        System.out.println("Total Updates: " + PARALLEL * BATCH_NUM);
        System.out.println("Elapse Time: " + elapse + " s");
        System.out.println("TPS on Hot Row: " + Math.round(PARALLEL * BATCH_NUM / elapse) + " /s");
    }
}
```

编译并执行：

```shell
javac -cp oceanbase-client.jar GroupCommitTest.java
java -cp .:oceanbase-client.jar GroupCommitTest
```

### 步骤三：默认配置下执行测试

作为对比参照，先在默认不开启 Group Commit 的情况下测试，并通过以下语句确保 Group Commit 处于关闭状态：

```sql
ALTER SYSTEM SET _enable_hotspot_group_commit = false tenant = test;
```

在测试机器上执行 `GroupCommitTest`。本例中采用 50 并发数，总计更新 4000000 次。

```shell
java -cp .:oceanbase-client.jar GroupCommitTest
```

执行完成，测试程序输出执行时间和 TPS：

```shell
[root@obce00 ~]# java -cp .:oceanbase-client.jar GroupCommitTest
Parallel Degree: 50
Total Updates: 4000000
Elapse Time: 486.40 s
TPS on Hot Row: 8224 /s
```

在不开启 Group Commit 的默认配置下，本例测试环境中，单行并发更新 TPS 为 8224/s。

### 步骤四：打开 OceanBase 数据库 Group Commit 配置

接下来开启 OceanBase 数据库的热点行 Group Commit 功能，首先需要使用 `root` 用户登录集群的 `sys` 租户。

```shell
[root@obce00 ~]# obclient -h127.0.0.1 -P2881 -uroot@sys -Doceanbase -A -p -c
```

然后设置 Group Commit 参数。其中 `_enable_hotspot_group_commit` 参数既可以指定具体租户，也可以指定全部租户，即 `tenant=all`。

```sql
ALTER SYSTEM SET _enable_hotspot_group_commit = true tenant = test;
```

### 步骤五：开启 OceanBase 数据库 Group Commit 进行测试

开启 Group Commit 功能后，再次执行测试。

```shell
java -cp .:oceanbase-client.jar GroupCommitTest
```

执行完成，测试脚本输出执行时间和 TPS：

```shell
[root@obce00 ~]# java -cp .:oceanbase-client.jar GroupCommitTest
Parallel Degree: 50
Total Updates: 4000000
Elapse Time: 42.19 s
TPS on Hot Row: 94822 /s
```

测试结果如下：

OceanBase 数据库在开启热点行 Group Commit 能力后，单行更新的 TPS 达到 94822/s，相比不开启 Group Commit 时提升约 11.5 倍。

同时可以看到表 `sbtest2` 的 `k` 值为 8000000，即本次也更新了 4000000 次。

```sql
SELECT * FROM sbtest2 WHERE id=1;
+----+---------+----+-----+
| id | k       | c  | pad |
+----+---------+----+-----+
|  1 | 8000000 | aa | aa  |
+----+---------+----+-----+
1 row in set
```

本例中仅介绍了单行并发更新场景下 Group Commit 的用法。OceanBase 数据库的 Group Commit 能力同样适用于多语句事务的并发更新场景，根据语句数量和场景差异，可以获得不同程度的性能提升。

由于 OceanBase 数据库的日志协议基于 Multi-Paxos 构建，在开启 Group Commit 后，即使发生节点宕机重启或 Leader 切换，仍然可以保证事务的一致性。
