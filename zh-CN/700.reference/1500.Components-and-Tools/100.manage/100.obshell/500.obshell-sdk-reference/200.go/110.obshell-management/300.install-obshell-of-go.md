# 安装 obshell

本文介绍如何通过 obshell-sdk-go 在每个节点解压安装 obshell RPM 包。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>建议先查看 <a href='../100.quickstart-of-go.md'>obshell-sdk-go 快速上手</a> 内容，了解如何使用 obshell-sdk-go。</p>
</main>

## 注意事项

在执行安装 obshell 之前请先就将所需的 RPM 包下载到机器上。您可以手动下载，也可以使用 obshell-sdk-go 下载，使用 obshell-sdk-go 下载的介绍可参见 [下载 RPM 包](../1500.other-api/400.download-rpm-package-of-go.md)。

## 示例代码

```go
func main() {
    ips := []string{
        "10.10.10.1",
        "10.10.10.2",
        "10.10.10.3",
    }

    workDir := "/data/ob" // OceanBase 数据库的工作目录
    nodeConfigs := make([]util.NodeConfig, 0)
    for _, ip := range ips {
        nodeConfigs = append(nodeConfigs, util.NewNodeConfig(ip, workDir, 2886))
    }

    if err := util.InstallObshell("/tmp/obshell-4.2.4.2-12024102115.el7.x86_64.rpm", nodeConfigs...); err != nil {
        panic(err)
    }
}
```

## 相关文档

通过 obshell-sdk-python 请求 API 方法的介绍可参见 [安装 obshell](../../100.python/110.obshell-management/300.install-obshell-of-python.md)。
