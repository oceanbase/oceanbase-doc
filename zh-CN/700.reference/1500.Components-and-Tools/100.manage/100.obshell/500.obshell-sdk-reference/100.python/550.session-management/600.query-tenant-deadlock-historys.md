# 查询租户死锁历史

本文介绍如何通过 obshell-sdk-python 获取租户死锁历史。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>建议先查看 <a href='../100.quickstart-of-python.md'>obshell-sdk-python 快速上手</a> 内容，了解如何使用 obshell-sdk-python。</p>
</main>

## 注意事项

* 请确保 obshell 正常运行。

* 请求的 client 对应的节点需在已初始化的集群中。

* 租户名称区分大小写，请确保使用正确的租户名称。

## 示例代码

```python
from obshell import ClientSet
from obshell.auth import PasswordAuth

# 创建 client 实例，节点地址为 '10.10.10.1'，端口为 2886。
client = ClientSet("10.10.10.1", 2886, PasswordAuth("****"))

# 获取指定租户的死锁信息
response = client.v1.get_tenant_deadlocks(
    tenant_name="sys",
    page=1,
    size=10
)
print(f"Total deadlocks: {response.page.total_elements if response.page else len(response.contents)}")
if response.page:
    print(f"Page {response.page.number + 1} of {response.page.total_pages}")
for deadlock in response.contents:
    print(f"  Event ID: {deadlock.event_id}, Report Time: {deadlock.report_time}, Size: {deadlock.size}")
    for node in deadlock.nodes:
        print(f"    Node: {node.raw_data}")
```

## 相关文档

获取租户死锁历史 API 接口说明可参见 [查询租户死锁历史](../../../400.obshell-api-reference/550.session-management/600.query-tenant-deadlock-history.md)。