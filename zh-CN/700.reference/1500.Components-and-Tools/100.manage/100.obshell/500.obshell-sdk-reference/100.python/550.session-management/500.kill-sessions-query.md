# 终止查询

本文介绍如何通过 obshell-sdk-python 批量终止会话查询。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>建议先查看 <a href='../100.quickstart-of-python.md'>obshell-sdk-python 快速上手</a> 内容，了解如何使用 obshell-sdk-python。</p>
</main>

## 注意事项

* 请确保 obshell 正常运行。

* 请求的 client 对应的节点需在已初始化的集群中。

* 租户名称区分大小写，请确保使用正确的租户名称。

## 示例代码

```python
from obshell import ClientSet
from obshell.auth import PasswordAuth

# 创建 client 实例
client = ClientSet("10.10.10.1", 2886, PasswordAuth("****"))

# 终止会话查询
success = client.v1.kill_tenant_session_query(
    tenant_name="sys",
    session_ids=["1234567890", "1234567891"]
)
print(f"Kill session queries result: {success}")
```

## 相关文档

终止会话查询 API 接口说明可参见 [终止租户会话查询](../../../400.obshell-api-reference/550.session-management/500.terminate-the-tenant-session-query.md)。