# 获取单个会话详情

本文介绍如何通过 obshell-sdk-python 获取单个会话详情。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>建议先查看 <a href='../100.quickstart-of-python.md'>obshell-sdk-python 快速上手</a> 内容，了解如何使用 obshell-sdk-python。</p>
</main>

## 注意事项

* 请确保 obshell 正常运行。

* 请求的 client 对应的节点需在已初始化的集群中。

* 租户名称区分大小写，请确保使用正确的租户名称。

## 示例代码

```python
from obshell import ClientSet
from obshell.auth import PasswordAuth

# 创建 client 实例
client = ClientSet("10.10.10.1", 2886, PasswordAuth("****"))

# 获取指定会话的详细信息
session = client.v1.get_tenant_session(
    tenant_name="sys",
    session_id="1234567890"
)
print(f"Session ID: {session.id}")
print(f"User: {session.user}")
print(f"Database: {session.db}")
print(f"Client IP: {session.client_ip}")
print(f"Status: {session.status}")
```

## 相关文档

获取会话详情 API 接口说明可参见 [获取租户会话详情](../../../400.obshell-api-reference/550.session-management/200.get-details-of-the-tenant-session.md)。