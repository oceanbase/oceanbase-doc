# 列举凭据

本文介绍如何通过 obshell-sdk-python 列举凭据。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>建议先查看 <a href='../100.quickstart-of-python.md'>obshell-sdk-python 快速上手</a> 内容，了解如何使用 obshell-sdk-python。</p>
</main>

## 注意事项

* 请确保 obshell 正常运行。

* 请求的 client 对应的节点需在已初始化的集群中。

## 示例代码

```python
from obshell import ClientSet
from obshell.auth import PasswordAuth

# 创建 client 实例
client = ClientSet("10.10.10.1", 2886, PasswordAuth("****"))

# 列举凭据，支持分页和筛选
response = client.v1.list_credentials(
    target_type="HOST",
    page=1,
    size=10
)
print(f"Total credentials: {response.page.total_elements if response.page else len(response.contents)}")
for cred in response.contents:
    print(f"ID: {cred.credential_id}, Name: {cred.name}")
```

## 相关文档

列举凭证 API 接口说明可参见 [查询凭据列表](../../../400.obshell-api-reference/900.credential-management/600.query-list-of-credentials.md)。