|description||
|---|---|
|slug|query-inspection-history|

# 查询巡检历史

介绍如何通过 API 查询巡检历史记录，支持分页、过滤和排序功能。

## 调用说明

如果设置了密码，则需要通过认证。详情可参见 [API 混合加密](../20.api-hybrid-encryption.md)。

### 请求路径

GET /api/v1/obcluster/inspection/reports

### 请求参数

| 参数 | 类型 | 必选 | 示例值 | 描述 |
| --- | --- | --- | --- | --- |
| page | int | 否 | 1 | 页码，默认为 1。该参数通过 `query param` 指定。 |
| size | int | 否 | 10 | 每页大小，默认为 10。该参数通过 `query param` 指定。 |
| scenario | string | 是 | basic | 场景过滤，支持 "basic" 或 "performance"。该参数通过 `query param` 指定。 |
| sort | string | 否 | start_time,desc | 排序参数，格式为 "字段名,排序顺序"（如 "start_time,desc"），默认为 "start_time,desc"。该参数通过 `query param` 指定。 |

### 返回结果

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| successful | bool | 请求是否成功。 |
| timestamp | time.Time | 服务端完成请求的时间戳。 |
| duration | int | 服务端处理请求的时间（毫秒）。 |
| status | int | 符合 HTTP Status 规范的编码。 |
| traceId | string | 请求的 Trace ID。 |
| data | PaginatedInspectionHistoryResponse | 返回的分页巡检历史列表，包含如下信息：<ul><li>`contents`：巡检报告简要信息列表。详情参见下文 **InspectionReportBriefInfo 的数据结构**。</li><li>`page`：分页信息。详情参见下文 **CustomPage 的数据结构**。</li></ul> |
| error | ApiError | 请求产生的 Error，包含如下信息：<ul><li>`code`：错误码。</li><li>`message`：错误详细信息。</li><li>`subErrors`：子错误信息。</li></ul> |

InspectionReportBriefInfo 的数据结构：

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| id | int | 巡检报告 ID。 |
| scenario | string | 巡检场景，例如 "basic" 或 "performance"。 |
| critical_count | int | 严重问题数量。 |
| failed_count | int | 失败项数量。 |
| warning_count | int | 警告项数量。 |
| pass_count | int | 通过项数量。 |
| start_time | time.Time | 开始时间。 |
| finish_time | time.Time | 完成时间。 |
| local_task_id | string | 本地任务 ID。 |
| status | string | 状态。 |
| error_message | string | 错误信息。 |

CustomPage 的数据结构：

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| total_elements | uint64 | 总记录数。 |
| total_pages | uint64 | 总页数。 |
| size | uint64 | 每页大小。 |
| number | uint64 | 当前页码。 |

## 示例

### 请求示例

GET 10.10.10.1:2886/api/v1/obcluster/inspection/reports?page=1&size=10&scenario=basic&sort=start_time,desc

### 返回示例

```json
{
    "successful": true,
    "timestamp": "2025-10-14T15:15:01.277047628+08:00",
    "duration": 25,
    "status": 200,
    "traceId": "e6f64ba7f54e7a48",
    "data": {
        "contents": [
            {
                "id": 1,
                "scenario": "basic",
                "critical_count": 0,
                "failed_count": 0,
                "warning_count": 2,
                "pass_count": 15,
                "start_time": "2025-10-14T15:14:01.277047628+08:00",
                "finish_time": "2025-10-14T15:14:30.277047628+08:00",
                "local_task_id": "1001",
                "status": "SUCCEED",
            }
        ],
        "page": {
            "total_elements": 1,
            "total_pages": 1,
            "size": 10,
            "number": 1
        }
    }
}
```