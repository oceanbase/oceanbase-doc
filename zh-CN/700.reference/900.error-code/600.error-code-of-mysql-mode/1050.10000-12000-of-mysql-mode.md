|description||
|---|---|
|keywords||
|dir-name||
|dir-name-en||
|tenant-type|MySQL Mode|
|slug|10000-12000-mysql-mode|

# 10000 ~ 12000

此范围包含 OBProxy 相关错误码、OBSharding 错误码、OBKV 错误码以及客户端错误码等。

## ERROR 10500 (HY000) : incorrect route for obkv global index, client router should refresh.

* OceanBase 错误码：10500

* 错误原因：OBKV 全局索引的路由不正确，客户端路由需要刷新。
* 解决方法：请确认您的客户端连接和路由信息。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该错误码从 V4.3.0 版本开始引入。</p>
</main>

## ERROR 10501 (HY000) : TTL feature is not enabled

* OceanBase 错误码：10501

* 错误原因：TTL 功能未启用。

* 解决方法：请确保 TTL 功能被正确地启用。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该错误码从 V4.2.2 版本开始引入。</p>
</main>

## ERROR 10502 (HY000) : TTL column '%.*s' not exists

* OceanBase 错误码：10502

* 错误原因：TTL 列不存在。

* 解决方法：请确认指定的 TTL 列是否存在于数据库表中。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该错误码从 V4.2.2 版本开始引入。</p>
</main>

## ERROR 10503 (HY000) : Column type of '%.*s' is not supported for TTL definition

* OceanBase 错误码：10503

* 错误原因：指定的列类型不支持用于 TTL 定义。

* 解决方法：请确认指定的列类型是否被支持用于 TTL 定义。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该错误码从 V4.2.2 版本开始引入。</p>
</main>

## ERROR 10504 (HY000) : TTL command is not allowed, current TTL status is '%s'

* OceanBase 错误码：10504

* 错误原因：TTL 命令不被允许执行，因为当前的 TTL 状态为特定的值。

* 解决方法：检查当前状态是否符合预期，并使用合适的 TTL 命令进行状态切换。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该错误码从 V4.2.2 版本开始引入。</p>
</main>

## ERROR 10505 (HY000) : No TTL task is running, please try trigger a new TTL task

* OceanBase 错误码：10505

* 错误原因：没有正在运行的 TTL 任务，需要尝试触发一个新的 TTL 任务。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该错误码从 V4.2.2 版本开始引入。</p>
</main>

## ERROR 10506 (HY000) : Cannot execute TTL task during tenant is restore

* OceanBase 错误码：10506

* 错误原因：在租户恢复期间无法执行 TTL 任务。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该错误码从 V4.2.2 版本开始引入。</p>
</main>

## ERROR 10507 (HY000) : Time to live of hbase table must be greater than 0

* OceanBase 错误码：10507

* 错误原因：HBase 表的 TTL 必须大于 0。

* 解决方法：检查当前表的 `KV_ATTRIBUTES` 属性，确保定义的 TimeToLive 大于 0。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该错误码从 V4.2.2 版本开始引入。</p>
</main>

## ERROR 10508 (HY000) : MaxVersions of hbase table must be greater than 0

* OceanBase 错误码：10508

* 错误原因：HBase 表的 MaxVersions 必须大于 0。

* 解决方法：检查当前表的 `KV_ATTRIBUTES` 属性，确保定义的 MaxVersions 大于 0。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该错误码从 V4.2.2 版本开始引入。</p>
</main>

## ERROR 10509 (HY000) : Access denied, credential '%.*s' not match '%.*s'

* OceanBase 错误码：10509

* 错误原因：访问被拒绝，认证 '%.*s' 与 '%.*s' 不匹配。

* 解决方法：重新输入正确的凭据，确保用户名和密码正确，并且确保没有输入错误。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该错误码从 V4.3.0 版本开始引入。</p>
</main>

## ERROR 10510 (HY000) : Rowkey column count not match, schema rowkey count is '%ld', input rowkey count is '%ld'

* OceanBase 错误码：10510

* 错误原因：在执行操作时，输入的行键列数与数据库表的行键列数不匹配。

* 解决方法：确保在执行操作时，提供的行键列数与表的行键列数相匹配。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该错误码从 V4.3.0 版本开始引入。</p>
</main>

## ERROR 10511 (HY000) : Column type for '%.*s' not match, schema column type is '%.*s', input column type is '%.*s'

* OceanBase 错误码：10511

* 错误原因：输入的列类型与数据库表的列类型不匹配。这可能会导致数据转换错误或无法正确执行操作。

* 解决方法：确保在执行操作时，输入的列类型与表的列类型相匹配。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该错误码从 V4.3.0 版本开始引入。</p>
</main>

## ERROR 10512 (HY000) : Collation type for '%.*s' not match, schema collation type is '%.*s', input collation type is '%.*s'

* OceanBase 错误码：10512

* 错误原因：字符集类型不匹配。

* 解决方法：确保在执行操作时，输入的字符集类型与表的字符集类型相匹配。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该错误码从 V4.3.0 版本开始引入。</p>
</main>

## ERROR 10513 (HY000) : Scan range missing, input scan range cell count is '%ld', which should equal to rowkey count '%ld'

* OceanBase 错误码：10513

* 错误原因：执行扫描操作时，扫描范围列数和表主键列数不匹配。

* 解决方法：确保在执行扫描操作时，扫描范围列数和主键列数相等。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该错误码从 V4.3.0 版本开始引入。</p>
</main>

## ERROR 10515 (HY000) : Redis protocol parse error, the input redis string is: '%.*s'

* OceanBase 错误码：10515

* 错误原因：Redis 协议解析错误。

* 解决方法：客户端可能发送了自定义的、OceanBase 数据库不支持的 Redis 命令，或者命令格式有误。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该错误码从 V4.3.1 版本开始引入。</p>
</main>

## ERROR 10518 (HY000) : Check failed in %.*s"

* OceanBase 错误码：10518

* 错误原因：OBKV [checkAndInsertup 接口](https://www.oceanbase.com/docs/common-oceanbase-database-cn-1000000002025908)的 rollbackWhenCheckFailed 参数为设置 `true` 的时候，如果 check 失败，则会抛出该错误码。

* 解决方法：抛出该错误代表检查失败，根据业务实际情况进行忽略或者进行其他处理。

## ERROR 10519 (HY000) : '%.*s' is not disabled

* OceanBase 错误码：10519

* 错误原因：当前表的状态不是禁用（DISABLED）状态，无法使用 HBase Admin 的 `enableTable()` 或 `deleteTable()` 接口。

* 解决方法：调用 HBase Admin 的 `disableTable()` 接口将表的状态设置为禁用（DISABLED）状态，然后重试。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该错误码从 V4.4.0 版本开始引入。</p>
</main>

## ERROR 10520 (HY000) : '%.*s' is not enabled

* OceanBase 错误码：10520

* 错误原因：当前表的状态不是可用（ENABLED）状态，无法使用访问该表。

* 解决方法：调用 HBase Admin 的 `enableTable()` 接口将表的状态设置为可用（ENABLED）状态，然后重试。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该错误码从 V4.4.0 版本开始引入。</p>
</main>

## ERROR 10521 (HY000) : namespace '%.*s' is not found

* OceanBase 错误码：10521

* 错误原因：指定的 HBase 表空间不存在。

* 解决方法：在 OBKV-HBase 数据库创建相应 HBase 表空间，然后重试。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该错误码从 V4.4.0 版本开始引入。</p>
</main>

## ERROR 10522 (HY000) : table '%.*s' already exists

* OceanBase 错误码：10522

* 错误原因：指定的 HBase 表已经存在。

* 解决方法：确认已经创建的表是否符合要求，否则可以先删除再重建你需要的 HBase 表。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该错误码从 V4.4.0 版本开始引入。</p>
</main>

## ERROR 10523 (HY000) : table '%.*s' not found

* OceanBase 错误码：10523

* 错误原因：指定的 HBase 表不存在。

* 解决方法：创建对应的 HBase 表。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该错误码从 V4.4.0 版本开始引入。</p>
</main>

## ERROR 10650 (HY000) : ODP process timeout

* OceanBase 错误码：10650

* 错误原因：ODP 进程执行超时。

* 解决方法：检查操作中是否包含长时间的操作，调大操作的超时时间。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该错误码从 V4.3.0 版本开始引入。</p>
</main>

## ERROR 11032 (HY000) : HDFS path message %s

* OceanBase 错误码：11032

* 错误原因：出现 HDFS path message %s 报错，通常见于以下场景：

  1. 开启 Kerberos 认证的 HDFS 环境中，访问某 HDFS 外部表时，若权限不足导致无法访问，会出现 -11032 错误。

  2. 若 Kerberos 认证配置正确，但仍出现 HDFS: path not found，可能是创建外部表语句中指定的外部表路径错误，指向了不存在的路径。

  3. 外部表访问初期正常，但执行查询一段时间后出现 11032 报错，可能是 HDFS 对应外部表路径下的某个文件出现坏块，即 HDFS DataNode 报告该数据块出错、无法访问，进而触发报错。

* 解决方法：
  1. 确认 Kerberos 认证的 keytab、principal 有效且对应用户拥有 HDFS 集群的访问权限，重新配置外部表权限并设置 Kerberos 认证即可。

  2. 确认对应的 HDFS 路径正确且存在，重新建立外部表的 HDFS 路径映射。

  3. 反馈至客户运维团队，确认数据坏块情况后，可选择调整坏块或更换环境继续验证。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该错误码从 V4.5.0 版本开始引入。</p>
</main>

## ERROR 11056 (HY000) : jvm.so not found %s

* OceanBase 错误码：11056

* 错误原因：在 OceanBase 数据库中创建或访问 HDFS/ODPS 等外部表时，因 jvm 环境初始化失败而引发的错误。

* 解决方法：检查 JVM 运行环境的配置是否正确。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该错误码从 V4.5.0 版本开始引入。</p>
</main>

## ERROR 11058 (HY000) : cur java home path %s

* OceanBase 错误码：11058

* 错误原因：JAVA 路径设置错误。

* 解决方法：配置正确的JAVA 路径。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该错误码从 V4.5.0 版本开始引入。</p>
</main>

## ERROR 11065 (HY000) : fail to read orc file, message: %s

* OceanBase 错误码：11065

* 错误原因：无法读取 ORC 文件。

* 解决方法：检查中 ORC 文件消息：%s。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该错误码从 V4.4.0 版本开始引入。</p>
</main>

## ERROR 11093 (HY000) : OBLogService rpc execute failed

* OceanBase 错误码：11093

* 错误原因：日志服务 RPC 执行失败，目标节点资源不足或网络故障。
  
* 解决方法：优先通过 `trace_id` 定位目标节点日志，结合资源监控和参数调整解决。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该错误码从 V4.4.0 版本开始引入。</p>
</main>

## ERROR 11094 (HY000) : Sensitive rule '%.*s' exists

* OceanBase 错误码：11094

* 错误原因：有同名的已经存在的敏感规则。一般是在通过 DDL 语句新增敏感规则时，新增规则与既存规则重名。

* 解决方式：检查指定的敏感规则名是否已存在，重新指定一个不存在的敏感规则名。

## ERROR 11095 (HY000) : Sensitive rule '%.*s' does not exist

* OceanBase 错误码：11095

* 错误原因：不存在指定名称的敏感规则。一般是在通过 DDL 语句在删除敏感规则时，指定了不存在的规则名。

* 解决方式：检查输入的敏感规则名称是否正确，重新输入正确的敏感规则名称。

## ERROR 11096 (HY000) : Sensitive column %.*s(%.*s) exists

* OceanBase 错误码：11096

* 错误原因：指定名称的列已经是敏感列。一般是在通过 DDL 语句新增/修改敏感规则时，尝试对某个已经被其他敏感规则覆盖的列上施加敏感规则。

* 解决方式：检查指定的列是否为敏感列，选择合适的列添加敏感规则。

## ERROR 11097 (HY000) : Sensitive column %.*s(%.*s) does not exist

* OceanBase 错误码：11097

* 错误原因：不存在指定名称的敏感列。一般是在通过 DDL 语句修改敏感规则时，尝试移除某个并非敏感列的列。

* 解决方式：检查指定的敏感列是否存在，重新输入正确的敏感列名。

## ERROR 11098 (HY000) : %.*s command denied to user '%.*s'@'%.*s' for sensitive rule '%.*s'

* OceanBase 错误码：11098

* 错误原因：用户没有权限执行敏感规则命令 `'%.*s'`。通常发生在 DML 语句（例如 `INSERT`、`UPDATE`、`REPLACE`）访问了被敏感规则保护的列时。

* 解决方法：检查用户权限，通过 `GRANT PLAINACCESS ON SENSITIVE RULE '%.*s' TO '%.*s'@'%.*s'` 命令赋予该用户合适的明文访问权限。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该错误码从 V4.4.2 版本开始引入。</p>
</main>

## ERROR 11099 (HY000) : Invalid sensitive rule name '%.*s'

* OceanBase 错误码：11098

* 错误原因：非法的敏感规则名称。

* 解决方式：检查指定的敏感规则名称是否正确，重新输入正确的敏感规则名称。

## ERROR 11100 (HY000) : kerberos execution error

* OceanBase 错误码：11100

* 错误原因：kerberos 执行错误，可能由于配置错误或密钥文件无效。
  
* 解决方法：检查 Kerberos 配置文件（如 `krb5.conf`）和密钥文件路径，确保权限和内容正确。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该错误码从 V4.4.1 版本开始引入。</p>
</main>

## ERROR 11101 (HY000) : HMS execution error

* OceanBase 错误码：11101

* 错误原因：HMS 执行错误，可能由服务不可用或配置错误导致。
  
* 解决方法：验证 HMS 服务状态，检查连接参数（如地址、端口）是否正确，并查看 HMS 日志排查具体问题。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该错误码从 V4.4.1 版本开始引入。</p>
</main>

## ERROR 11102 (HY000) : HMS host: '%.*s' is invalid

* OceanBase 错误码：11102

* 错误原因：HMS 主机无效，可能主机地址格式不合法或无法解析。
  
* 解决方法：检查 HMS 的 IP 地址格式，确保配置正确。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该错误码从 V4.4.1 版本开始引入。</p>
</main>

## ERROR 11103 (HY000) : HMS port: '%.*s' is invalid

* OceanBase 错误码：11103

* 错误原因：HMS 端口无效。
  
* 解决方法：确认端口号符合规范，并检查防火墙规则是否允许访问该端口。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该错误码从 V4.4.1 版本开始引入。</p>
</main>

## ERROR 11104 (HY000) : HMS table location: '%.*s' is invalid

* OceanBase 错误码：11104

* 错误原因：HMS 表存储路径无效。
  
* 解决方法：验证表路径是否存在，确保 OceanBase 数据库有读取/写入权限。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该错误码从 V4.4.1 版本开始引入。</p>
</main>

## ERROR 11105 (HY000) : HMS service: '%.*s' is invalid

* OceanBase 错误码：11105

* 错误原因：HMS 服务无效。

* 解决方法：检查 HMS 服务名称是否与实际部署匹配，并确认服务已正常启动。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该错误码从 V4.4.1 版本开始引入。</p>
</main>

## ERROR 11106 (HY000) : HMS service FQDN: '%.*s' is invalid

* OceanBase 错误码：11106

* 错误原因：HMS 服务 FQDN 格式错误或无法解析。
  
* 解决方法：验证 FQDN 格式，并确保 DNS 解析正常。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该错误码从 V4.4.1 版本开始引入。</p>
</main>

## ERROR 11107 (HY000) : HMS transport: '%.*s' is invalid

* OceanBase 错误码：11107

* 错误原因：HMS 传输无效。
  
* 解决方法：确认传输协议与 HMS 服务兼容。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该错误码从 V4.4.1 版本开始引入。</p>
</main>

## ERROR 11108 (HY000) : HMS metastore: '%.*s' is invalid

* OceanBase 错误码：11108

* 错误原因：无效的 HMS 元存储。
  
* 解决方法：检查 HMS 元数据库的连接参数。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该错误码从 V4.4.1 版本开始引入。</p>
</main>

## ERROR 11109 (HY000) : HMS partition error

* OceanBase 错误码：11109

* 错误原因：HMS 分区操作失败，可能由分区字段类型不匹配或数据冲突导致。
  
* 解决方法：检查分区字段定义和数据内容，确保符合 HMS 元数据规范。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该错误码从 V4.4.1 版本开始引入。</p>
</main>

## ERROR 11110 (HY000) : HMS database: '%.*s' not found

* OceanBase 错误码：11110

* 错误原因：请求的 HMS 数据库不存在。
  
* 解决方法：确认数据库名称拼写正确，或联系管理员创建目标数据库。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该错误码从 V4.4.1 版本开始引入。</p>
</main>

## ERROR 11111 (HY000) : HMS table: '%.*s' not found

* OceanBase 错误码：11111

* 错误原因：请求的 HMS 表不存在。
  
* 解决方法：检查表名拼写和数据库上下文，确保表已正确创建。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该错误码从 V4.4.1 版本开始引入。</p>
</main>

## ERROR 11112 (HY000) : This ai model endpoint \'%.*s\' not found in the tenant

* OceanBase 错误码：11112

* 错误原因：用户输入的 AI 模型端点（Endpoint）名称不存在。
  
* 解决方法：查询 [DBA_OB_AI_MODEL_ENDPOINTS 视图](../../700.system-views/400.system-view-of-mysql-mode/200.dictionary-view-of-mysql-mode/5710.o-dba_ob_ai_model_endpoints-of-mysql-mode.md)，确认是否有对应的端点信息记录。如果没有，则需要使用 [DBMS_AI_SERVICE.CREATE_AI_MODEL_ENDPOINT](../../600.pl-reference/200.pl-mysql/1000.pl-system-package-mysql/1000.dbms-ai-service-mysql/400.create-ai-model-endpoint-mysql.md) 创建对应的端点记录。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该错误码从 V4.4.1 版本开始引入。</p>
</main>

## ERROR 11113 (HY000) : This ai model endpoint \'%.*s\' is already exists

* OceanBase 错误码：11113

* 错误原因：用户输入的 AI 模型端点（Endpoint）名称已经存在。
  
* 解决方法：使用其他的 AI 模型端点（Endpoint）名称创建。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该错误码从 V4.4.1 版本开始引入。</p>
</main>

## ERROR 11114 (HY000) : The param \'%.*s\' is empty or null

* OceanBase 错误码：11114

* 错误原因：用户指定的 `DBMS_AI_SERVICE` 子程序的参数不允许为空或者 NULL。
  
* 解决方法：检查用户指定的 `DBMS_AI_SERVICE` 子程序的参数是否为空或者 NULL，参考 [DBMS_AI_SERVICE 子程序概览](../../600.pl-reference/200.pl-mysql/1000.pl-system-package-mysql/1000.dbms-ai-service-mysql/100.dbms-ai-service-overview-mysql.md) 中的参数说明。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该错误码从 V4.4.1 版本开始引入。</p>
</main>

## ERROR 11115 (HY000) : The param \'%.*s\' is invalid

* OceanBase 错误码：11115

* 错误原因：用户指定的 `DBMS_AI_SERVICE` 子程序的参数名不符合要求。
  
* 解决方法：使用合法的参数名，参考 [DBMS_AI_SERVICE 子程序概览](../../600.pl-reference/200.pl-mysql/1000.pl-system-package-mysql/1000.dbms-ai-service-mysql/100.dbms-ai-service-overview-mysql.md) 中的参数说明。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该错误码从 V4.4.1 版本开始引入。</p>
</main>

## ERROR 11116 (HY000) : The value of param \'%.*s\' is invalid

* OceanBase 错误码：11116

* 错误原因：用户指定的 `DBMS_AI_SERVICE` 子程序的参数值不符合要求。
  
* 解决方法：使用合法的参数值，参考 [DBMS_AI_SERVICE 子程序概览](../../600.pl-reference/200.pl-mysql/1000.pl-system-package-mysql/1000.dbms-ai-service-mysql/100.dbms-ai-service-overview-mysql.md) 中的参数说明。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该错误码从 V4.4.1 版本开始引入。</p>
</main>

## ERROR 11117 (HY000) : This type of param \'%.*s\' is invalid, should be %.*s

* OceanBase 错误码：11117

* 错误原因：用户指定的 `DBMS_AI_SERVICE` 子程序的参数类型不符合要求。
  
* 解决方法：使用合法的参数类型，参考 [DBMS_AI_SERVICE 子程序概览](../../600.pl-reference/200.pl-mysql/1000.pl-system-package-mysql/1000.dbms-ai-service-mysql/100.dbms-ai-service-overview-mysql.md) 中的参数说明。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该错误码从 V4.4.1 版本开始引入。</p>
</main>

## ERROR 11118 (HY000) : This ai model \'%.*s\' not found in the tenant

* OceanBase 错误码：11118

* 错误原因：用户指定的 AI 模型不存在。
  
* 解决方法：查询 [DBA_OB_AI_MODELS 视图](../../700.system-views/400.system-view-of-mysql-mode/200.dictionary-view-of-mysql-mode/5720.o-dba_ob_ai_models-of-mysql-mode.md)，确认是否有对应的模型信息记录。如果没有，则需要使用 [DBMS_AI_SERVICE.CREATE_AI_MODEL](../../600.pl-reference/200.pl-mysql/1000.pl-system-package-mysql/1000.dbms-ai-service-mysql/200.create-ai-model-mysql.md) 创建对应的模型记录。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该错误码从 V4.4.1 版本开始引入。</p>
</main>

## ERROR 11119 (HY000) : This ai model \'%.*s\' is already exists

* OceanBase 错误码：11119

* 错误原因：用户指定的 AI 模型已经存在。
  
* 解决方法：使用其他的 AI 模型名称创建。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该错误码从 V4.4.1 版本开始引入。</p>
</main>

## ERROR 11120 (HY000) : Got error during COMMIT: %s

* OceanBase 错误码：11120

* 兼容 MySQL 错误码：1180

* 错误原因：事务在提交阶段发生了错误。
  
* 解决方法：解决方案的核心在于通过详细日志定位根本原因，建议在获得具体错误信息后，寻求官方技术支持以获得最准确的解决方案。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该错误码从 V4.4.1 版本开始引入。</p>
</main>

## ERROR 11121 (HY000) : Table snapshot '%.*s' does not exist

* OceanBase 错误码：11121
* 错误原因：表快照不存在。
  
* 解决方法：核实快照是否存在，如果不存在则重新创建所需的快照即可。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该错误码从 V4.5.0 版本开始引入。</p>
</main>

## ERROR 11122 (HY000) : Bad http request: %s

* OceanBase 错误码：11122
* 错误原因：HTTP 请求错误。
* 解决方法：检查 HTTP 请求的参数是否正确。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该错误码从 V4.5.0 版本开始引入。</p>
</main>

## ERROR 11123 (HY000) : Unauthorized http request: %s

* OceanBase 错误码：11123
* 错误原因：HTTP 请求权限拒绝。
* 解决方法：检查 HTTP 请求的鉴权信息是否正确设置。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该错误码从 V4.5.0 版本开始引入。</p>
</main>

## ERROR 11124 (HY000) : Http request not found: %s

* OceanBase 错误码：11124
* 错误原因：未找到 Http 请求。
* 解决方法：检查是否存在请求的资源。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该错误码从 V4.5.0 版本开始引入。</p>
</main>

## ERROR 11125 (HY000) : Http server error: %s

* OceanBase 错误码：11125
* 错误原因：Http 服务器错误。
* 解决方法：检查所访问的 HTTP 服务状态。
  
<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该错误码从 V4.5.0 版本开始引入。</p>
</main>

## ERROR 11126 (HY000) : unit \'%.*s\' not recognized

* OceanBase 错误码：11126
* 错误原因：调用日期截断函数（如 DATE_TRUNC 或 DATETRUNC）时，传入的时间单位参数不被识别。
  
* 解决方法：检查您的 SQL 语句中日期截断函数的第一个参数，确保其拼写正确且是 OceanBase 数据库支持的有效时间单位关键字。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该错误码从 V4.5.0 版本开始引入。</p>
</main>

## ERROR 11127 (HY000) : Privilege not allowed for sensitive rules

* OceanBase 错误码：11127

* 错误原因：执行了一项与敏感数据规则相关的操作，但其所拥有的权限不足以进行此操作。
  
* 解决方法：确认操作所需权限并授予对应的权限。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该错误码从 V4.5.0 版本开始引入。</p>
</main>
