| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |

# DBMS_DDL 概述

`DBMS_DDL` 用于将 PL 对象定义字符串加密。

## DBMS_EXTERNAL_TABLE 子程序概览

下表列出了 OceanBase 数据库当前版本所支持的 `DBMS_DDL` 子程序和简要描述。

| 子程序                   | 描述                  |
| ----------------------- | --------------------- |
|[WRAP]()|用于将 PL 对象定义字符串加密成 `CREATE WRAPPED` 语句字符串。|
|[CREATE_WRAPPED]()|将 PL 对象定义字符串加密成 `CREATE WRAPPED` 语句字符串之后，会立即执行这个语句创建 `WRAPPED`对象。|