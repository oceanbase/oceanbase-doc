| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
| machine-translation | en          |

# PUTF_NCHAR

`PUTF_NCHAR` 存储过程用于以 `NCHAR` 方式按格式写入到指定的文件中。

<main id="notice" >
    <h4>功能适用性</h4>
    <p>该内容仅适用于 OceanBase 数据库企业版。OceanBase 数据库社区版暂不支持。</p>
  </main>

## 语法

```sql
UTL_FILE.PUTF_NCHAR (
    file   IN FILE_TYPE,
    format IN VARCHAR2,
    arg1   IN VARCHAR2 DEFAULT NULL,
    arg2   IN VARCHAR2 DEFAULT NULL,
    arg3   IN VARCHAR2 DEFAULT NULL,
    arg4   IN VARCHAR2 DEFAULT NULL,
    arg5   IN VARCHAR2 DEFAULT NULL
);
```

## 参数解释

|    参数     |           解释            |
|-----------|-------------------------|
|file|由调用 `FOPEN` 返回的活动文件句柄。|
|format|格式字符串，其中 `%s` 为字符串占位符，`\n` 表示行终止符|
|arg1 至 arg5|最多 5 个可选参数，依次替换 `format` 中的 `%s`。若参数少于占位符，未被替换的 `%s` 将保持原样或忽略。|