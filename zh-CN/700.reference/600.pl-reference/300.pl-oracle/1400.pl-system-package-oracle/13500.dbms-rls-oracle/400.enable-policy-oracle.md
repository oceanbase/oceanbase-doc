| Description   |                 |
|---------------|-----------------|
| machine-translation ||
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
|slug|enable-policy-oracle-mode|

# ENABLE_POLICY

`ENABLE_POLICY` 过程用于启用或禁用指定表或视图上的安全策略。

  <main id="notice" >
    <h4>功能适用性</h4>
    <p>该内容仅适用于 OceanBase 数据库企业版。OceanBase 数据库社区版暂不支持。</p>
  </main>

## 语法

```sql
DBMS_RLS.ENABLE_POLICY(
    object_schema IN VARCHAR2 := NULL,
    object_name   IN VARCHAR2,
    policy_name   IN VARCHAR2,
    enable        IN BOOLEAN := TRUE);
```

## 参数说明

| 参数 | 说明 |
|------|------|
| object_schema | 表或视图所属的模式名称。默认为 `NULL`，表示当前模式。 |
| object_name   | 表或视图的名称。 |
| policy_name   | 策略名称。 |
| enable        | 设置为 `TRUE` 启用策略，`FALSE` 禁用策略。默认为 `TRUE`。 |

## 示例

```sql
-- 启用策略
BEGIN
  DBMS_RLS.ENABLE_POLICY(
    object_schema => 'hr',
    object_name   => 'employees',
    policy_name   => 'emp_policy',
    enable        => TRUE);
END;
/
```
