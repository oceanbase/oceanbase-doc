| Description   |                 |
|---------------|-----------------|
| machine-translation ||
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
|slug|alter-policy-oracle-mode|

# ALTER_POLICY

`ALTER_POLICY` 过程用于修改策略的命名空间和属性关联。

  <main id="notice" >
    <h4>功能适用性</h4>
    <p>该内容仅适用于 OceanBase 数据库企业版。OceanBase 数据库社区版暂不支持。</p>
  </main>

## 语法

```sql
DBMS_RLS.ALTER_POLICY(
    object_schema IN VARCHAR2 := NULL,
    object_name   IN VARCHAR2,
    policy_name   IN VARCHAR2,
    alter_option  IN BINARY_INTEGER := NULL,
    namespace     IN VARCHAR2,
    attribute     IN VARCHAR2);
```

## 参数说明

| 参数 | 说明 |
|------|------|
| object_schema | 表或视图所属的模式名称。默认为 `NULL`，表示当前模式。 |
| object_name   | 表或视图的名称。 |
| policy_name   | 策略名称。 |
| alter_option | 修改选项。支持以下常量：`ADD_ATTRIBUTE_ASSOCIATION=1`、`REMOVE_ATTRIBUTE_ASSOCIATION=2`。 |
| namespace     | 命名空间。 |
| attribute     | 属性。 |

## 示例

```sql
BEGIN
  DBMS_RLS.ALTER_POLICY(
    object_schema => 'hr',
    object_name   => 'employees',
    policy_name   => 'emp_policy',
    alter_option  => DBMS_RLS.ADD_ATTRIBUTE_ASSOCIATION,
    namespace     => 'emp_ns',
    attribute     => 'dept');
END;
/
```
