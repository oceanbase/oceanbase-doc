| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
| machine-translation | en          |
|slug|setcharset-oracle-compatible-mode|

# SETCHARSET

`SETCHARSET` 过程用于设置 DOM 文档序列化时使用的字符编码，可影响 XML 声明中的 encoding 信息及后续写出行为。

<main id="notice" >
  <h4>功能适用性</h4>
  <p>该内容仅适用于 OceanBase 数据库企业版。OceanBase 数据库社区版仅提供 MySQL 模式。</p>
</main>

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>对于 V4.4.2 版本，该子程序从 OceanBase 数据库 V4.4.2 BP3 版本开始引入。</p>
</main>

## 语法

```sql
DBMS_XMLDOM.setCharset (
   doc     IN DBMS_XMLDOM.DOMDOCUMENT,
   charset IN VARCHAR2);
```

## 参数解释

| **参数** | **解释** |
|----------|----------|
| doc | 目标 `DBMS_XMLDOM.DOMDOCUMENT` 句柄。 |
| charset | 字符集名称，例如 `UTF-8`。 |

## 返回值

无返回值。

## 使用说明

通常在调用 `WRITETOBUFFER` 或 `WRITETOCLOB` 序列化前设置。

## 示例

```sql
DECLARE
  doc DBMS_XMLDOM.DOMDOCUMENT;
BEGIN
  doc := DBMS_XMLDOM.newDOMDocument;
  DBMS_XMLDOM.setCharset(doc, 'UTF-8');
  DBMS_XMLDOM.freeDocument(doc);
END;
/
```
