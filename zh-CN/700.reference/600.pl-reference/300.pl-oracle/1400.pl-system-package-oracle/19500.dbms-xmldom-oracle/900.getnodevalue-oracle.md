| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
| machine-translation | en          |
|slug|getnodevalue-oracle-mode|

# GETNODEVALUE

`GETNODEVALUE` 函数用于获取指定节点的字符串值，可用于读取文本节点或元素节点的内容。

<main id="notice" >
  <h4>功能适用性</h4>
  <p>该内容仅适用于 OceanBase 数据库企业版。OceanBase 数据库社区版仅提供 MySQL 模式。</p>
</main>

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>对于 V4.4.2 版本，该子程序从 OceanBase 数据库 V4.4.2 BP2 版本开始引入。</p>
</main>

## 语法

```sql
DBMS_XMLDOM.getNodeValue (
   node IN DBMS_XMLDOM.DOMNODE);
```

## 参数解释

| **参数** | **解释** |
|----------|----------|
| node | 待读取的 `DBMS_XMLDOM.DOMNODE` 句柄。 |

## 返回值

节点的字符串值，类型为 `VARCHAR2`。

## 使用说明

对于文本节点或 CDATA 节点，直接返回其文本内容。对于元素节点，则根据 DOM 规范返回其第一个子节点（通常为 `#text`）的内容，适用于获取叶子元素的文本值。
