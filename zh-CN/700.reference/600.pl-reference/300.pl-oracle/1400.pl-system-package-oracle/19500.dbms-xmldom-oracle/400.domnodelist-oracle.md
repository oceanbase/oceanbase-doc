| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | Oracle Mode     |
| machine-translation | en          |
|slug|domnodelist-oracle-mode|

# DOMNODELIST

`DOMNODELIST` 是一个有序节点集合的句柄，由 `GETCHILDNODES`、`GETELEMENTSBYTAGNAME` 等方法返回。可使用 `GETLENGTH` 获取节点数量，再用 `ITEM` 按下标访问各节点。该句柄为不透明类型。

<main id="notice" >
  <h4>功能适用性</h4>
  <p>该内容仅适用于 OceanBase 数据库企业版。OceanBase 数据库社区版仅提供 MySQL 模式。</p>
</main>

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>对于 V4.4.2 版本，该类型从 OceanBase 数据库 V4.4.2 BP2 版本开始引入。</p>
</main>

## 语法

```sql
nodelist DBMS_XMLDOM.DOMNODELIST;
```

## 参数解释

| **参数** | **解释** |
|----------|----------|
| nodelist | 节点列表句柄。 |

## 返回值

节点列表句柄。

## 使用说明

`DOMNODELIST` 表示一组按序排列的 `DOMNODE` 引用。遍历时应先调用 [GETLENGTH](../19500.dbms-xmldom-oracle/800.getlength-xmldom-oracle.md) 获取长度，再通过 [ITEM](../19500.dbms-xmldom-oracle/1000.item-oracle.md) 按下标访问，以避免越界。

## 相关子程序

| **子程序** | **说明** |
|------------|----------|
| [GETCHILDNODES](../19500.dbms-xmldom-oracle/600.getchildnodes-oracle.md) | 返回子节点列表 |
| [GETELEMENTSBYTAGNAME](../19500.dbms-xmldom-oracle/700.getelementbytagname-oracle.md) | 返回匹配元素列表 |
| [GETLENGTH](../19500.dbms-xmldom-oracle/800.getlength-xmldom-oracle.md) | 返回列表长度 |
| [ITEM](../19500.dbms-xmldom-oracle/1000.item-oracle.md) | 按下标取节点 |
