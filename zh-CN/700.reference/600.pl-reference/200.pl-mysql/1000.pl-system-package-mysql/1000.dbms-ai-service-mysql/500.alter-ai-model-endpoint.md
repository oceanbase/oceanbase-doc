| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |

# ALTER_AI_MODEL_ENDPOINT

`ALTER_AI_MODEL_ENDPOINT` 过程用于修改 AI 模型对象的访问端点。

## 语法

```sql
PROCEDURE alter_ai_model_endpoint(
    IN name VARCHAR(128),
    IN params JSON);
```

## 参数说明

<table>
  <thead>
    <tr>
      <th>参数</th>
      <th>描述</th>
      <th>类型</th>
      <th>取值范围</th>
      <th>是否可空</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>name</td>
      <td>AI 模型对象的访问端点的名称。</td>
      <td>VARCHAR(128)</td>
      <td></td>
      <td>NO</td>
    </tr>
    <tr>
      <td rowspan="9">params</td>
      <td>AI 模型对象的访问端点的相关参数，必须为 JSON 对象。支持的 KEY 如下：KEY = <code>scope</code>，表示当前访问端点的作用范围</td>
      <td>JSON STRING</td>
      <td>取值只能是 <code>all</code> 或不指定（取默认值 <code>all</code>）</td>
      <td>NO</td>
    </tr>
    <tr>
      <td>KEY = <code>ai_model_name</code>，表示由 <code>CREATE_AI_MODEL</code> 过程创建的 AI 模型对象的名称</td>
      <td>JSON STRING</td>
      <td></td>
      <td>NO</td>
    </tr>
    <tr>
      <td>KEY = <code>url</code>，表示访问端点的地址</td>
      <td>JSON STRING</td>
      <td></td>
      <td>NO</td>
    </tr>
    <tr>
      <td>KEY = <code>access_key</code>，表示访问端点的访问密钥</td>
      <td>JSON STRING</td>
      <td></td>
      <td>NO</td>
    </tr>
    <tr>
      <td>KEY = <code>request_model_name</code>，表示用户自定义的模型名称，例如 <code>big-m3-custom</code>，放在请求消息体里面</td>
      <td>JSON STRING</td>
      <td></td>
      <td>YES，可以为空或不指定</td>
    </tr>
    <tr>
      <td>KEY = <code>provider</code>，表示模型供应商</td>
      <td>JSON STRING</td>
      <td><ul><li><code>aliyun-openAI</code>：阿里云 OpenAI</li><li><code>aliyun-dashscope</code>：阿里云 DashScope</li><li><code>deepseek</code>：DeepSeek</li><li><code>siliconflow</code>：SiliconFlow</li><li><code>hunyuan-openAI</code>：魂元 OpenAI</li><li><code>openAI</code>：OpenAI</li></ul></td>
      <td>JSON STRING</td>
      <td></td>
      <td>NO</td>
    </tr>
    <tr>
      <td>KEY = <code>parameters</code>，表示可选的模型参数</td>
      <td>JSON STRING</td>
      <td></td>
      <td>YES，可以为空或不指定</td>
    </tr>
    <tr>
      <td>KEY = <code>request_transform_fn</code>，用于构建请求数据</td>
      <td>JSON STRING</td>
      <td></td>
      <td>YES，必须为空或不指定</td>
    </tr>
    <tr>
      <td>KEY = <code>response_transform_fn</code>，用于解析模型返回数据</td>
      <td>JSON STRING</td>
      <td></td>
      <td>YES，必须为空或不指定</td>
    </tr>
  </tbody>
</table>

## 示例

修改 AI 模型对象的访问端点。

```sql
CALL DBMS_AI_SERVICE.ALTER_AI_MODEL_ENDPOINT (
  'my_model_endpoint1', '{
    "access_key" : "sk2-xxxxxxxxxxxx"
  }');
```

## 相关文档

* [DBMS_AI_SERVICE 子程序概览](100.dbms-ai-service-overview-mysql.md)
* [CREATE_AI_MODEL_ENDPOINT](400.create-ai-model-endpoint-mysql.md)
* [ALTER_AI_MODEL_ENDPOINT](500.alter-ai-model-endpoint-mysql.md)