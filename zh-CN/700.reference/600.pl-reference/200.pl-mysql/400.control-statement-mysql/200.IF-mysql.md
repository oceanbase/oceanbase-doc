| Description   |                 |
|---------------|-----------------|
| machine-translation ||
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |
|slug|if-mysql|

# IF

`IF` 语句用于构造基本的条件语句，根据条件执行不同的代码块。

## 语法

```sql
IF search_condition THEN statement_list
    [ELSEIF search_condition THEN statement_list] ...
    [ELSE statement_list]
END IF
```

## 参数解释

| 参数 | 说明 |
|------|------|
| search_condition | 条件表达式，计算结果为 True 或 False。 |
| statement_list | 要执行的 SQL/PL 语句列表。每个语句以分号（;）结尾。 |
| ELSEIF | 可选，用于指定额外的条件分支。可以包含多个 `ELSEIF` 子句。 |
| ELSE | 可选，当所有条件都不满足时执行的语句列表。 |

## 使用说明

`IF` 语句可以包含 `THEN`、`ELSE` 和 `ELSEIF` 子句，并以 `END IF` 终止。

如果指定的 `search_condition` 的计算结果为 True，则执行相应的 `THEN` 或 `ELSEIF` 子句语句列表。如果没有匹配的 `search_condition`，则执行 `ELSE` 子句中的 `statement_list`。

每个 `statement_list` 由一个或多个 SQL 语句组成；不允许使用空的 `statement_list`。

存储程序中的 `IF ... END IF` 块，必须使用分号（;）终止。与其他流控制构造一样，`IF ... END IF` 块可以嵌套在其他控制语句中，包括 `IF` 语句。每个 `IF` 必须以它自己的 `END IF;` 终止。您可以通过缩进代码使嵌套的流控制块更容易阅读。

## 示例

**示例 1：基本的 IF 语句。**

```sql
obclient> DELIMITER //

obclient> CREATE FUNCTION get_discount_level(order_amount DECIMAL(10,2))
    RETURNS VARCHAR(20)
    DETERMINISTIC
    BEGIN
        DECLARE level VARCHAR(20);
        IF order_amount >= 1000 THEN
            SET level = 'VIP';
        ELSEIF order_amount >= 500 THEN
            SET level = 'Gold';
        ELSE
            SET level = 'Standard';
        END IF;
        RETURN level;
    END //

obclient> DELIMITER ;

obclient> SELECT get_discount_level(1500);
```

查询结果如下：

```sql
+------------------------+
| get_discount_level(1500) |
+------------------------+
| VIP                    |
+------------------------+
1 row in set
```

**示例 2：嵌套的 IF 语句。**

```sql
obclient> DELIMITER //

obclient> CREATE FUNCTION compare_numbers(n INT, m INT)
    RETURNS VARCHAR(50)
    DETERMINISTIC
    BEGIN
        DECLARE result VARCHAR(50);
        IF n = m THEN
            SET result = 'is equal to';
        ELSE
            IF n > m THEN
                SET result = 'is greater than';
            ELSE
                SET result = 'is less than';
            END IF;
        END IF;
        SET result = CONCAT(n, ' ', result, ' ', m, '.');
        RETURN result;
    END //

obclient> DELIMITER ;

obclient> SELECT compare_numbers(5, 3);
```

查询结果如下：

```sql
+----------------------+
| compare_numbers(5, 3) |
+----------------------+
| 5 大于 3.            |
+----------------------+
1 row in set
```

**示例 3：在存储过程中使用 IF 语句。**

```sql
obclient> DELIMITER //

obclient> CREATE PROCEDURE update_employee_salary(IN emp_id_param INT, IN new_salary DECIMAL(10,2))
    BEGIN
        DECLARE current_salary DECIMAL(10,2);
        SELECT salary INTO current_salary FROM employee_info WHERE emp_id = emp_id_param;
        
        IF new_salary > current_salary * 1.2 THEN
            UPDATE employee_info SET salary = current_salary * 1.2 WHERE emp_id = emp_id_param;
        ELSEIF new_salary < current_salary * 0.8 THEN
            UPDATE employee_info SET salary = current_salary * 0.8 WHERE emp_id = emp_id_param;
        ELSE
            UPDATE employee_info SET salary = new_salary WHERE emp_id = emp_id_param;
        END IF;
    END //

obclient> DELIMITER ;
```

## 相关文档

- [CASE](./100.CASE-mysql.md)
- [存储过程](../200.storage-object-mysql/300.pl-stored-procedure-mysql.md)
- [存储函数](../200.storage-object-mysql/400.pl-storage-function-mysql.md)
