# 删除用户

本文介绍如何通过 obshell 删除租户中的用户。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>自 obshell V4.2.6 起支持该 API。</p>
</main>

## 调用说明

### 接口约束

obshell Server 会对该 API 进行安全校验，详细介绍可参见 [API 混合加密](../20.api-hybrid-encryption.md)。

### 请求路径

DELETE /api/v1/tenant/{tenant_name}/user/{user_name}

### 请求参数

| 参数 | 类型 | 必选 | 示例值 | 描述  |
|------|------|------|------|-------|
| root_password | string | 否 | ***** | 租户的 root 用户密码，当删除系统租户用户时，无需传入。  |

### 返回结果

| 参数 | 类型 | 说明  |
|-------|------|-------|
| successful | bool | 请求是否成功。  |
| timestamp | time.Time | 服务端完成请求的时间戳。  |
| duration | int | 服务端处理请求的时间（毫秒）。  |
| status | int | 符合 HTTP Status 规范的编码。  |
| traceId | string | 请求的 Trace ID。  |
| error | ApiError | 请求产生的 Error，包含如下信息：<ul><li>`code`：错误码。</li><li>`message`：错误详细信息。</li><li>`subErrors`：子错误信息。</li></ul> |

## 示例

### 请求示例

DELETE 10.10.10.1:2886/api/v1/tenant/sys/user/proxyro

### 返回示例

```json
{
    "successful": true,
    "timestamp": "2025-03-26T10:46:40.052855656+08:00",
    "duration": 54,
    "status": 200,
    "traceId": "53b9e01e070daf7c"
}
```

## 相关文档

除通过命令行调用 API 接口外，还可以通过 SDK 方法调用 API。通过 obshell-sdk-python 请求 API 方法的介绍可参见 [删除用户](../../500.obshell-sdk-reference/100.python/500.tenant-management/850.delete-user-of-tenant-of-python.md)。
