| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   |                 |

# SET DECRYPTION

## 描述

`SET DECRYPTION` 用于设置恢复的密码。只有在备份时添加了密码的场景下才需要设置恢复的密码。

## 权限要求

仅 `sys` 租户的 `root` 用户（`root@sys`）可以设置恢复的密码，其他租户不支持设置恢复的密码。

## 语法

```sql
SET DECRYPTION IDENTIFIED BY  ('password'[, 'password' ...]);
```

## 参数解释

|    参数    |                                             描述                                              |
|----------|---------------------------------------------------------------------------------------------|
| password | 在备份时添加的密码。 <main id="notice" type='explain'><h4>说明</h4><p> 当全量备份和增量备份设置的密码不一样，则需要输入多个密码，密码之间使用逗号分隔。</p></main> |

## 示例

* 为全量备份和增量备份设置相同的密码。

  ```sql
  obclient> SET DECRYPTION IDENTIFIED BY '***1***';
  ```

* 为全量备份和增量备份设置不同的密码，前面是全量备份密码，后面是增量备份密码。

  ```sql
  obclient> SET DECRYPTION IDENTIFIED BY '***1***','***2***';
  ```
