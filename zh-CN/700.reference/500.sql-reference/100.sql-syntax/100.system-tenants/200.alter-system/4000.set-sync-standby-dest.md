| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   |                 |
|machine-translation| en          |
|slug|sql-tenant-set-sync_standby_dest|

# SET SYNC_STANDBY_DEST

## 描述

`ALTER SYSTEM SET SYNC_STANDBY_DEST` 语句用于设置强同步的下游。

默认情况下，主租户的保护模式为最大性能模式。对于 V4.4.2 版本，OceanBase 数据库从 V4.4.2 BP1 版本开始支持最大可用模式和最大保护模式。如果需要将主租户的保护模式设置为最大可用模式或最大保护模式，需要先为主租户设置强同步的备租户。

## 使用限制及注意事项

* 对于 V4.4.2 版本，OceanBase 数据库从 V4.4.2 BP1 版本开始支持设置强同步的下游。

* 设置备租户进入强同步模式时，要求该备租户必须为基于网络的备库。

* 仅支持设置一个强同步的备租户，不支持设置多个强同步的备租户。

* 主备租户上均可以设置强同步的下游。需要注意的是，备租户设置强同步的下游通常情况下并无意义，但在 Switchover 场景下，如果希望主备租户仍然保持强同步关系，可以在备租户切成主租户前为备租户设置强同步下游为原主租户。

* 仅设置租户强同步的下游，不设置保护模式的情况下，并不会真的开启强同步模式。

## 权限要求

需要由 `sys` 租户的 `root` 用户（`root@sys`） 或各租户的管理员用户执行。其中：

* MySQL 模式的默认管理员用户为 `root` 用户。
* Oracle 模式的默认管理员用户为 `SYS` 用户。

## 语法

```sql
ALTER SYSTEM SET SYNC_STANDBY_DEST = 'SERVICE=ip_list NET_TIMEOUT=int_value HEALTH_CHECK_TIME=time USER=user_name@standby_tenant_name PASSWORD=password' [TENANT = tenant_name]；
```

## 参数说明

| 参数                    | 描述                                                                                                   |
|-------------------------|--------------------------------------------------------------------------------------------------------|
| ip_list                 | 下游备租户所在副本的 OBServer 节点的 IP 及 SQL 端口号（端口号默认为 2881），多个 IP 地址之间使用英文逗号（,）分隔。|
| NET_TIMEOUT             | 用于指定最大可用模式下主租户等待强同步的备租户日志完成同步的超时时间，如果超过指定时间仍然未完成，则主租户会自动降级为异步同步模式。其他保护模式下，该参数没有意义。该参数的单位为秒，默认值为 30，表示 30s，取值范围为 [10, 1200]。  |
| HEALTH_CHECK_TIME       | 用于指定最大可用模式下主租户检查备租户日志完成同步的检查时间，如果在指定时间内备租户均为同步状态，则主租户会自动升级为强同步模式。其他保护模式下，该参数没有意义。该参数的单位为秒，默认值为 60s，取值范围为 [0s, +∞)。  |
| user_name@standby_tenant_name、password | 填写下游备租户及其登录信息：<ul><li>`user_name`：填写 [创建访问视图的专用用户](../../../../../600.manage/400.high-availability/300.physical-standby-database-disaster-recovery/200.create-a-standby-tenant/200.create-an-empty-standby-tenant.md) 中创建的用户，例如：用户 `rep_user`。</li> <li>`standby_tenant_name`：填写下游备租户的租户名。</li> <li>`password`：填写 [创建访问视图的专用用户](../../../../../600.manage/400.high-availability/300.physical-standby-database-disaster-recovery/200.create-a-standby-tenant/200.create-an-empty-standby-tenant.md) 中创建的用户的密码。</li></ul> |
| tenant_name             | 系统租户指定待执行该操作的租户名。<main id="notice" type='notice'><h4>注意</h4><p>仅系统租户在执行此命令时需要通过 <code>TENANT tenant_name</code> 子句指定待操作的租户，用户租户只能对本租户执行此命令，故不需要指定该子句。</p></main> |

## 示例

* 系统租户为主租户 `mysql` 设置强同步的下游备租户 `standby_tenant`。

  ```shell
  obclient(root@sys)[(none)]> ALTER SYSTEM SET SYNC_STANDBY_DEST = 'SERVICE=6.xx.xxx.xxx:2881 NET_TIMEOUT=20 HEALTH_CHECK_TIME=40s USER=rep_user@standby_tenant PASSWORD=xxxxxx' TENANT = mysql;
  ```

* 主租户为本租户设置强同步的下游备租户 `standby_tenant`。

  ```shell
  obclient> ALTER SYSTEM SET SYNC_STANDBY_DEST = 'SERVICE=6.xx.xxx.xxx:2881 NET_TIMEOUT=20 HEALTH_CHECK_TIME=40s USER=rep_user@standby_tenant PASSWORD=xxxxxx';
  ```