| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   |                 |
|machine-translation||

# BOOTSTRAP

## 描述

该语句用于启动 OceanBase 集群。

该命令仅需要在 OceanBase 数据库初次启动时执行，每个集群仅能执行一次该命令。如果该命令执行失败，不能重新运行该命令，需要重新搭建 OceanBase 数据库集群。

在执行该命令前，请确保指定的各 OBServer 节点都已正常启动。

## 语法

```sql
ALTER SYSTEM BOOTSTRAP
    server_info_list [shared_storage_info]

server_info_list:
    server_info [, server_info]...

server_info:
    [REGION=region] ZONE=zone SERVER=ip:port

shared_storage_info:
    , SHARED_STORAGE_INFO = 'string_value'
```

## 参数解释

| 参数 | 描述 |
|---------|------|
| REGION | 指定数据库服务器所在的地理区域。可选参数。 |
| ZONE | 指定数据库服务器所在的可用区。必选参数。 |
| SERVER | 指定数据库服务器的 IP 地址和端口号。必选参数。格式为 'IP:PORT'。|
| shared_storage_info | 描述集群启动时每个 Zone 指定的初始的对象存储信息。字符型。字符串包括对象存储的桶名称，以及对象存储信息的若干子参数，二者以 `?` 间隔。子参数间以 `&` 分隔。可选参数。多个 server_info 之间用逗号分隔。|

## 示例

指定 Root Service 机器列表并启动集群。列表包含多台 OBServer 节点则需要以逗号（,）分隔。

```sql
obclient> ALTER SYSTEM BOOTSTRAP ZONE 'zone1' SERVER 'xxx.xxx.xx.xx1:xxxx', ZONE 'zone2' SERVER 'xxx.xxx.xx.xx2:xxxx', ZONE 'zone3' SERVER 'xxx.xxx.xx.xx3:xxxx';
```

```sql
obclient> ALTER SYSTEM BOOTSTRAP
    REGION='hangzhou' ZONE='zone1' SERVER='xxx.xxx.xxx.10:2882';
```

```sql
obclient> ALTER SYSTEM BOOTSTRAP
    ZONE 'z1' SERVER 'xxx.xxx.xxx.xxx:26603',
    ZONE 'z2' SERVER 'xxx.xxx.xxx.xxx:26605',
    ZONE 'z3' SERVER 'xxx.xxx.xxx.xxx:26607',
    SHARED_STORAGE_INFO "oss://cn-hangzhou-bucket-1?host=oss-cn-hangzhou-internal.aliyuncs.com&access_id=xxx&access_key=xxx&max_iops=10000&max_bandwidth=1GB";
```

以上示例展示了如何初始化单节点和多节点的 OceanBase 数据库集群。执行命令时，请将参数值替换为实际的配置信息。