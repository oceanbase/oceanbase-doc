| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   |                 |
|machine-translation||

# DROP TENANT

## 描述

该语句用于删除一个 OceanBase 数据库租户。

  <main id="notice" type='notice'>
    <h4>注意</h4>
    <p>只有用 root 用户连接到 sys 租户才能执行 <code>DROP TENANT</code> 去删除租户。</p>
  </main>

## 语法

```sql
drop_tenant_stmt:
DROP TENANT [IF EXISTS] tenant_name [PURGE|FORCE];
```

## 参数解释

|   **参数**    |                **描述**                |
|-------------|--------------------------------------|
| tenant_name | 指定要删除的租户名。                           |
| PURGE       | 表示删除租户，且删除后的租户一定不会进入回收站。当 `sys` 租户的回收站功能关闭时， `DROP TENANT` 语句与 `DROP TENANT... PURGE` 语句的效果一样。 |
| FORCE       | 表示强制删除租户，无法保证物理备份数据的完整性。删除后的租户不会进入回收站。 |

## 示例

删除租户 `test_tenant1`。

```sql
obclient> DROP TENANT test_tenant1;
```