| Description   |                 |
|---------------|-----------------|
| machine-translation ||
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |
|slug|formatdatetime-mysql-mode|

# FORMATDATETIME

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该函数从 V4.6.0 版本开始引入。</p>
</main>

## 声明

```sql
FORMATDATETIME(date, format)
```

## 说明

该函数用于将日期或日期时间表达式 `date` 按照指定的 `format` 格式字符串进行格式化，并返回格式化后的字符串。例如 `FORMATDATETIME(date, '%Y-%m-%d %H:%i:%s')`。

<main id="notice" type='notice'>
  <h4>注意</h4>
  <p>该函数为 ClickHouse 兼容函数，默认不开放，使用前需设置配置项 <code>sql_func_extension_mode</code> 来启用，详细信息参见 <a href="../../../../../../800.configuration-items-and-system-variables/100.system-configuration-items/400.tenant-level-configuration-items/7470.sql_func_extension_mode.md">sql_func_extension_mode</a>。</p>
</main>

## 示例

1. 启用 ClickHouse 扩展函数模式。

    ```shell
    obclient> ALTER SYSTEM SET sql_func_extension_mode = "ClickHouse";
    ```

2. 将当前日期时间格式化为 `YYYY 年 MM 月 DD 日 HH:MI:SS` 格式。

    ```shell
    obclient> SELECT FORMATDATETIME(NOW(), '%Y 年 %m 月 %d 日 %H:%i:%s') AS formatted_date;
    ```

    返回结果如下：

    ```shell
    +---------------------------------+
    | formatted_date                  |
    +---------------------------------+
    | 2026 年 03 月 30 日 10:44:53    |
    +---------------------------------+
    1 row in set
    ```