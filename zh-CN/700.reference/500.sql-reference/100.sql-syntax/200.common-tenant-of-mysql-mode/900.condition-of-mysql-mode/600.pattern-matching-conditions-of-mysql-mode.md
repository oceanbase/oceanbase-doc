| Description   |                 |
|---------------|-----------------|
| machine-translation ||
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |
|slug|pattern-matching-conditions-mysql-mode|

# 模式匹配条件

模式匹配条件（Pattern-matching Condition）用来比较字符数据。

模式匹配条件类型包括 `LIKE` 条件和 `REGEXP` 条件。

## LIKE 条件

`LIKE` 条件用于模式匹配。`LIKE` 条件通过在第一个值中搜索由第二个字符值指定的模式来将一个字符值的一部分与另一个字符值进行匹配。

### 语法

```sql
char1 [NOT] LIKE char2 [ ESCAPE esc_char ]
```

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>当 <code>LIKE</code> 条件中包含&quot;||&quot;字符时，将其作为字符连接运算符，由 <code>sql_mode</code> 中的 <code>PIPES_AS_CONCAT</code> 进行控制。</p>
</main>

### 参数解释

| 参数 | 说明 |
|------|------|
| char1 | 字符表达式，例如字符列，称为搜索值。 |
| char2 | 字符表达式，通常是字面量，称为模式。 |
| esc_char | 字符表达式，通常是字面量，`ESCAPE` 将 `esc_char` 标识为转义字符。 |

### 使用说明

模式可以包含如下特殊的模式匹配字符：

* 下划线 (`_`) 表示正好匹配值中的一个字符。
* 百分号 (`%`) 表示可以匹配值中的零个或多个字符。模式 `%` 不能匹配 NULL。

您可以使用 `ESCAPE` 子句在模式中包含实际字符 `%` 或 `_`。

### 示例

```sql
obclient> SELECT last_name FROM emp WHERE last_name LIKE '%A\_B%' ESCAPE '\' ORDER BY last_name;
```

## REGEXP 条件

`REGEXP` 用于正则表达式匹配。

### 语法

```sql
char1 [NOT] REGEXP pattern
```

### 示例

```sql
obclient> SELECT first_name, last_name FROM emp WHERE first_name REGEXP '^Ste(v|ph)en$'
    ORDER BY first_name, last_name;
+------------+-----------+
| first_name | last_name |
+------------+-----------+
| Stephen    | Stiles    |
| Steven     | King      |
| Steven     | Markle    |
+------------+-----------+
3 rows in set
```

## SOUNDS LIKE 条件

`SOUNDS LIKE` 条件用于比较两个字符串的发音是否相似。

### 语法

```sql
expr1 SOUNDS LIKE expr2
```

### 示例

```sql
obclient> SELECT * FROM emp WHERE last_name SOUNDS LIKE 'King' ORDER BY empno;
```
