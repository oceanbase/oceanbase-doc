| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |

# 可用于生成列的数据类型规则

在 OceanBase 数据库中，纯函数（Pure Function）可用于生成列，非纯函数（Non-pure Function）不可用于生成列。

纯函数，指的是在相同的输入下总是会产生相同输出，且没有任何可观察的副作用（例如改变全局变量或数据库中的其他列）。由于它们返回值的可预测性和一致性，纯函数适合用于创建列。

非纯函数依赖于外部状态或在每次调用时可能产生不同的输出，例如依赖系统时间，会话信息，随机数生成等。由于这些特性，非纯函数不应用于生成列，因为这将导致列值不是一致和可重复的。

## 不可用于生成列的非纯函数

在 MySQL 模式下，非纯函数列表如下：

- `CONNECTION_ID`
- `VERSION`
- `CURRENT_USER`
- `USER`
- `DATABASE`
- `SYSDATE`
- `CUR_DATE`
- `CUR_TIME`
- `CUR_TIMESTAMP`
- `UNIX_TIMESTAMP`
- `UTC_TIME`
- `UTC_TIMESTAMP`
- `UTC_DATE`
- `RAND`
- `RANDOM`
- `UUID`
- `UUID_SHORT`
- `SLEEP`
- `LAST_INSERT_ID`
- `ROW_COUNT`
- `FOUND_ROWS`
- `REGEXP_INSTR`
- `REGEXP_REPLACE`
- `REGEXP_SUBSTR`
- `CURRENT_USER_PRIV`

### 特殊的非纯函数

正则表达式相关的函数，如 `REGEXP_REPLACE`，`REGEXP_SUBSTR`， 和 `REGEXP_INSTR` 被分类为非纯函数（Non-pure Functions），原因是它们可能在不同的上下文中产生不同的结果。

以下是几个具体的例子，说明这些函数如何在不同上下文中产生不同的结果：

- **字符集和排序规则的变化**

假设字符集或排序规则（Collation）在不同的数据库实例或会话设置中不同，它们可能会影响字符比较的结果，进而影响正则表达式的匹配结果。例如：

- 在一个使用 UTF-8 字符集的数据库会话中，`REGEXP_INSTR` 可能匹配到一个特殊字符。
- 在另一个使用 Latin1 字符集的会话中，相同的 `REGEXP_INSTR` 调用可能就不匹配相同的特殊字符，因为字符编码有差异。

- **正则表达式模式的不确定性**

正则表达式的模式可能依赖于具体的输入字符串和模式定义。例如，如果模式包含可选择的部分（optional parts）或多种替代匹配项，同一个输入对于不同的模式可能返回不同的结果。例如：

- 使用 `REGEXP_SUBSTR` 寻找一个可选的前缀字符。当该字符存在时，匹配结果包含该字符；当字符缺失时，匹配其他部分。因此，即使是同一个模式，对于不同的输入字符串，也可能得到不同的截取结果。

- **执行计划和优化**

数据库查询优化器可能根据上下文选择不同的执行计划。对于包含正则表达式的复杂查询，不同的优化决策可能影响函数的调用次数和顺序，从而影响最终结果。例如：

- 一个包含 `REGEXP_INSTR` 函数的查询在一种情况下先执行了一个过滤条件，排除了大部分行，然后只在少数行上执行了正则匹配。
- 另一种情况下，执行计划不同，可能在更多的行上先进行了正则匹配，再应用了其他的过滤条件，这就可能在这两个步骤中间得到一个暂时的不同结果。

以上所有情况都说明了正则表达式相关函数容易受到外部因素的影响，因此不能保证在所有上下文和所有时候都返回一致的结果，这就是它们被视为非纯函数的原因。在使用这些函数进行数据库设计时，要特别留意这些潜在的变化，避免在计算列或其他需要一致性和可预测性的场景中使用。

## 特殊的时间函数

对于 `TO_DATE`, `TO_TIMESTAMP`, 和 `TO_TIMESTAMP_TZ` 这三个函数来说，它们是否为纯函数（pure function）取决于它们是否接收到了明确的日期/时间格式（format）字符串以及函数调用中是否包含了足够的日期/时间组件。当 `TO_DATE`, `TO_TIMESTAMP`, 和 `TO_TIMESTAMP_TZ` 这三个函数未指定格式字符串时，它将默认使用环境或会话级别的日期/时间格式。因为不同环境的默认设置可能不同，所以未指定格式的情况下函数不被视为纯函数。

`TO_DATE`, `TO_TIMESTAMP`, 和 `TO_TIMESTAMP_TZ` 函数成为非纯函数/纯函数需要满足下面几个条件：

1. 对于 `TO_DATE`，如果格式字符串中缺失月份和日期部分，例如仅提供了 'yyyy'，此时 `TO_DATE` 为**非纯函数**。它会返回当前月份的第一天，而当前月份随执行时刻的不同而变化。

2. 在 `TO_TIMESTAMP` 的情况下，如果格式字符串缺失了 'yyyy'，则这个函数不是纯函数。同样，即使是 'yy' 缺失也会被认为不是纯函数，因为会默认使用当前年份来填充。

   - 如果 `TO_TIMESTAMP` 格式字符串缺失月份信息 'mm'，函数也不被认为是纯函数，因为会默认填充当前月份，此时 `TO_TIMESTAMP` 是**非纯函数**。

   - 如果 `TO_TIMESTAMP` 格式字符串仅缺失日期 'dd'，则不影响函数纯净性，因为它会使用指定月份的第一天，此时 `TO_TIMESTAMP` 是**纯函数**。

   - 如果 `TO_TIMESTAMP` 格式字符串缺失时间的组件（如小时、分钟、秒或毫秒）并不影响这些函数的纯函数性质，此时 `TO_TIMESTAMP` 是**纯函数**。

8. 对于 `TO_TIMESTAMP_TZ` 来说，除了要通过与 `TO_TIMESTAMP` 相同的判断之外，还需要考虑时区（time zone）相关的格式指令。如果格式字符串包含了时区信息（如 'TZR' 或 'TZH:TZM'），函数的结果将依赖于时区设置，则被视为**非纯函数**。

对于 `TO_DATE`, `TO_TIMESTAMP`, 和 `TO_TIMESTAMP_TZ` 函数，保持纯函数性质的安全做法是提供一个完整和确定的格式字符串，并且这个字符串应该包含了所有需要的日期和时间组件。
