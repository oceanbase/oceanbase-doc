| Description   |                 |
|---------------|-----------------|
| description   | 介绍如何使用 ALTER EVENT 语句修改已存在事件的调度、状态、注释、执行体或重命名等属性。 |
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |
| machine-translation |             |

# ALTER EVENT

## 描述

该语句用于修改已存在事件的调度、启用状态、注释、执行体或重命名等属性。仅可修改指定的子句，未指定的子句保持原样。

## 权限要求

执行该语句的用户需要拥有 `EVENT` 权限。修改 DEFINER 需要超级权限。有关 OceanBase 数据库权限的详细介绍，参见 [MySQL 模式下的权限分类](../../../../../600.manage/500.security-and-permissions/300.access-control/200.user-and-permission/200.permission-of-mysql-mode/100.permission-classification-of-mysql.md)。

## 语法

```sql
ALTER
    [DEFINER = user]
    EVENT event_name
  [ON SCHEDULE schedule]
  [ON COMPLETION [NOT] PRESERVE]
  [RENAME TO new_event_name]
  [ENABLE | DISABLE | DISABLE ON SLAVE]
  [COMMENT 'string']
  [DO event_body]

schedule: {
    AT timestamp [+ INTERVAL interval] ...
  | EVERY interval
    [STARTS timestamp [+ INTERVAL interval] ...]
    [ENDS timestamp [+ INTERVAL interval] ...]
}

interval:
    quantity {YEAR | QUARTER | MONTH | DAY | HOUR | MINUTE |
              WEEK | SECOND | YEAR_MONTH | DAY_HOUR | DAY_MINUTE |
              DAY_SECOND | HOUR_MINUTE | HOUR_SECOND | MINUTE_SECOND}
```

## 使用说明与注意事项

* 事件必须在当前数据库中，请先使用 <code>USE db_name;</code> 切换到事件所在库后再执行 ALTER EVENT，不能跨库修改。
* 修改后的开始时间（STARTS）不能为过去时间，否则报错。
* 已开始执行的事件不允许修改调度计划，也不支持通过 <code>RENAME TO</code> 修改事件名（否则会报错）。仅当事件尚未开始执行时，才支持使用 `RENAME TO` 修改事件名；重命名不能与当前事件名相同，且不能改为其他库。
* 修改 DEFINER 需要当前用户具备超级权限。
* `DO event_body` 中若包含单引号字符串字面量，可能触发语法解析错误，建议通过 <code>CALL</code> 调用存储过程实现复杂逻辑。参见 [CREATE EVENT](2960.create-event-of-mysql-mode.md) 中 DO 的说明。

## 参数说明

| 参数                          | 描述     |
|-------------------------------|----------|
| DEFINER = user                | 指定事件定义者。修改 DEFINER 需当前用户具备超级权限。|
| event_name                    | 指定待修改事件的名称。须为当前库下已存在的事件。|
| ON SCHEDULE schedule          | 修改事件执行的时间和频率。规则与 CREATE EVENT 相同；修改后的开始时间不能为过去时间。|
| ON COMPLETION [NOT] PRESERVE  | 修改事件执行完成后是否保留。`PRESERVE` 表示保留，`NOT PRESERVE` 表示执行完毕后自动删除。|
| RENAME TO new_event_name      | 将事件重命名为新名称。仅当事件尚未开始执行时支持；新名称不能与当前名称相同。|
| ENABLE、DISABLE、DISABLE ON SLAVE | 修改事件状态。`ENABLE` 启用，`DISABLE` 禁用。|
| COMMENT 'string'              | 修改事件注释。|
| DO event_body                 | 修改事件要执行的 SQL 或存储过程调用。支持单条 SQL 或 `CALL proc_name;`。|

## 示例

以下示例基于「建库 → 建表 → 创建事件 → 修改事件」的完整场景，先做前置准备再执行各类 ALTER。

**前置准备**：建库、建表、创建事件及供 DO CALL 使用的存储过程。

```sql
obclient> CREATE DATABASE IF NOT EXISTS test_event_db;

obclient> USE test_event_db;

obclient> CREATE TABLE event_log (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    evt_name VARCHAR(128),
    action VARCHAR(32),
    ts DATETIME DEFAULT CURRENT_TIMESTAMP
);

obclient> CREATE EVENT evt_demo
    ON SCHEDULE EVERY 1 MINUTE
    STARTS CURRENT_TIMESTAMP
    ON COMPLETION NOT PRESERVE
    ENABLE
    COMMENT 'Test: write a log entry every minute'
    DO INSERT INTO event_log (evt_name, action) VALUES ('evt_demo', 'tick');
```

```sql
obclient> DELIMITER /

obclient> CREATE PROCEDURE sp_evt_log()
BEGIN
  INSERT INTO event_log (evt_name, action) VALUES ('evt_demo', 'altered');
END;
/

obclient> DELIMITER ;
```

**修改事件**：启用、改调度与注释、禁用、再启用、改执行体为调用存储过程。

```sql
obclient [test_event_db]> ALTER EVENT evt_demo ENABLE;
```

```sql
obclient [test_event_db]> ALTER EVENT evt_demo
    ON SCHEDULE EVERY 2 MINUTE
    STARTS CURRENT_TIMESTAMP + INTERVAL 1 MINUTE
    ON COMPLETION PRESERVE
    COMMENT 'Run every 2 minutes, preserve after completion';
```

```sql
obclient [test_event_db]> ALTER EVENT evt_demo DISABLE;
```

```sql
obclient [test_event_db]> ALTER EVENT evt_demo ENABLE;
```

```sql
obclient [test_event_db]> ALTER EVENT evt_demo DO CALL sp_evt_log();
```

**查看与清理**（可选）：

```sql
obclient [test_event_db]> SELECT EVENT_NAME, STATUS, EVENT_COMMENT FROM information_schema.events WHERE EVENT_SCHEMA = 'test_event_db';
```

查询结果如下：

```sql
+-------------+--------+-------------------------------------------+
| EVENT_NAME  | STATUS | EVENT_COMMENT                             |
+-------------+--------+-------------------------------------------+
| evt_demo    | ENABLED| Run every 2 minutes, preserve after completion |
+-------------+--------+-------------------------------------------+
1 row in set
```

若查询 `event_log` 为空，请先检查事件调度器状态；若为 OFF，则打开后再等待一个调度间隔后重新查询。

先检查事件调度器状态：

```sql
obclient> SHOW GLOBAL VARIABLES LIKE 'event_scheduler';
```

若结果为 OFF，则执行以下语句打开：

```sql
obclient> SET GLOBAL event_scheduler = ON;
```

然后等至少一个调度间隔（本示例为 1 分钟或 2 分钟）再执行以下查询：

```sql
obclient [test_event_db]> SELECT * FROM event_log ORDER BY id DESC LIMIT 5;
```

查询结果如下：

```sql
+------+----------+---------+---------------------+
| id   | evt_name | action  | ts                  |
+------+----------+---------+---------------------+
|    5 | evt_demo | altered | 2026-02-06 15:27:11 |
|    4 | evt_demo | altered | 2026-02-06 15:25:11 |
|    3 | evt_demo | altered | 2026-02-06 15:23:11 |
|    2 | evt_demo | altered | 2026-02-06 15:21:11 |
|    1 | evt_demo | altered | 2026-02-06 15:19:11 |
+------+----------+---------+---------------------+
```

```sql
obclient [test_event_db]> DROP EVENT IF EXISTS evt_demo;
```

## 相关文档

* [EVENT（MySQL 模式）](../../../../600.pl-reference/200.pl-mysql/200.storage-object-mysql/550.pl-event-mysql.md)：事件概述。
* [CREATE EVENT](2960.create-event-of-mysql-mode.md)：创建事件。
* [DROP EVENT](3450.drop-event-of-mysql-mode.md)：删除事件。
