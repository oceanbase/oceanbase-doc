| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |
|machine-translation||

# FLUSH TABLE

<main id="notice" type='notice'>
  <h4>注意</h4>
  <p>当前版本仅支持该关键字语法，功能并不会实际生效。</p>
</main>

## 描述

该语句用于清理或刷新特定的内部缓存、日志和表。

## 语法

```sql
FLUSH [NO_WRITE_TO_BINLOG | LOCAL] { tables_option }

tables_option: {
    table_synonym
  | table_synonym tbl_name [, tbl_name] ...
  | table_synonym WITH READ LOCK
  | table_synonym tbl_name [, tbl_name] ... WITH READ LOCK
  | table_synonym tbl_name [, tbl_name] ... FOR EXPORT
}

table_synonym: {
    TABLE
  | TABLES
}
```

## 使用说明

打开租户级的隐藏配置项 `_enable_mock_stmt_flush_table` 以后，`FLUSH TABLE` 语法不报错。关闭该配置项以后，语句会报 `NOT SUPPORT`。

您可以执行如下命令打开该配置项：

```sql
ALTER SYSTEM SET _enable_mock_stmt_flush_table = true;
```