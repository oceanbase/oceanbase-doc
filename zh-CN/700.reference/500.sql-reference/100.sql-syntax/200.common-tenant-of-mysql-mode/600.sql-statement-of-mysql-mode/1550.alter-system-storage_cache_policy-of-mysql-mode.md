| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |

# ALTER SYSTEM STORAGE_CACHE_POLICY_EXECUTOR

## 描述

该语句用于触发手动热点指定相应的运维操作。

## 语法

```sql
ALTER SYSTEM {suspend|resume|trigger} STORAGE_CACHE_POLICY_EXECUTOR
```

## 参数解释

|          **参数**           |      **描述**      |
|---------------------------|------------------|
|suspend|用于将后台缓存任务挂起，通常用于热数据缓存影响到业务或系统稳定性的一个应急手段。|
|resume|用于重新激活处于挂起状态的后台缓存任务。|
|trigger|用于立即触发一次热数据缓存后台任务，用于手动触发热数据缓存任务，是周期性触发后台任务的一个补充。|

## 示例

* 执行如下命令将热数据缓存后台过期删除任务挂起。

    ```sql
    obclient> ALTER SYSTEM suspend STORAGE_CACHE_POLICY_EXECUTOR；
    ```

* 执行如下命令，重新激活处于挂起状态的热数据缓存后台过期删除任务。

    ```sql
    obclient> ALTER SYSTEM resume STORAGE_CACHE_POLICY_EXECUTOR；
    ```

* 执行如下命令将立即触发热数据缓存后台任务。

    ```sql
    obclient> ALTER SYSTEM trigger STORAGE_CACHE_POLICY_EXECUTOR；
    ```
