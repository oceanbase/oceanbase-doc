| Description   |                 |
|---------------|-----------------|
| description   | PURGE 语句用于删除回收站中的数据库对象，支持删除租户、数据库、表与索引。 |
| machine-translation ||
| keywords      | PURGE,回收站,删除 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |
|slug|purge-database-mysql-mode|

# PURGE

## 描述

该语句用于删除回收站中的数据库对象，只支持删除租户、数据库、表与索引。

## 语法

```sql
 PURGE {TENANT | DATABASE | TABLE | INDEX} object_name

object_name:
    tenant_name
  | database_name
  | table_name
  | index_name
```

## 参数解释

|    **参数**     |                                                   **描述**                                                    |
|---------------|-------------------------------------------------------------------------------------------------------------|
| tenant_name   | 指定要删除的租户名称。可以使用删除前的原始租户名，也可以使用在回收站中的名字。回收站中的名字具有唯一性，可以指定确定的租户；原始租户名可能会重复，此时删除的是最早进入回收站的租户。 |
| database_name | 指定要删除的数据库在回收站中的名称，不支持直接指定数据库的原始名称。                                                                            |
| table_name    | 指定要删除的表名称。可以使用删除前的原始表名，也可以使用在回收站中的名字。如果不指定库名，默认从当前数据库下删除的表；指定库名时仅支持指定的库名为当前数据库。原始表名可能会重复，此时删除的是最早进入回收站的表。                                                                                |
| index_name    | 指定要删除的索引名称。可以使用删除前的原始索引名，也可以使用在回收站中的名字。如果不指定库名，默认从当前数据库下删除的索引；指定库名时仅支持指定的库名为当前数据库。原始索引名可能会重复，此时删除的是最早进入回收站的索引。                                                                              |

## 示例

* 删除回收站中的租户 `mysql`。

  查看回收站中的对象：

  ```sql
  obclient> SHOW RECYCLEBIN;
  ```

  查询结果如下：

  ```sql
  +--------------------------------+---------------+--------+----------------------------+
  | OBJECT_NAME                    | ORIGINAL_NAME | TYPE   | CREATETIME                 |
  +--------------------------------+---------------+--------+----------------------------+
  | __recycle_$_1_1599722585571328 | mysql         | TENANT | 2020-09-10 15:36:54.712101 |
  +--------------------------------+---------------+--------+----------------------------+
  1 row in set
  ```

  删除回收站中的租户：

  ```sql
  obclient> PURGE TENANT mysql;
  ```

  再次查看回收站：

  ```sql
  obclient> SHOW RECYCLEBIN;
  ```

  查询结果如下：

  ```sql
  Empty set
  ```

* 删除回收站中的数据库 `__recycle_$_1_1597384386029184`。

  创建并删除数据库：

  ```sql
  obclient> CREATE DATABASE db1;

  obclient> DROP DATABASE db1;
  ```

  查看回收站：

  ```sql
  obclient> SHOW RECYCLEBIN;
  ```

  查询结果如下：

  ```sql
  +--------------------------------+---------------+----------+----------------------------+
  | OBJECT_NAME                    | ORIGINAL_NAME | TYPE     | CREATETIME                 |
  +--------------------------------+---------------+----------+----------------------------+
  | __recycle_$_1_1597384386029184 | db1           | DATABASE | 2020-08-14 13:53:06.029367 |
  +--------------------------------+---------------+----------+----------------------------+
  1 row in set
  ```

  删除回收站中的数据库：

  ```sql
  obclient> PURGE DATABASE __recycle_$_1_1597384386029184;
  ```

  再次查看回收站：

  ```sql
  obclient> SHOW RECYCLEBIN;
  ```

  查询结果如下：

  ```sql
  Empty set
  ```

* 删除回收站中的表。

  可以使用原始表名或回收站中的对象名删除表。以下示例展示使用原始表名删除：

  创建并删除表：

  ```sql
  obclient> CREATE TABLE test(c1 INT);

  obclient> DROP TABLE test;
  ```

  查看回收站：

  ```sql
  obclient> SHOW RECYCLEBIN;
  ```

  查询结果如下：

  ```sql
  +-------------------------------------------+---------------+-------+----------------------------+
  | OBJECT_NAME                               | ORIGINAL_NAME | TYPE  | CREATETIME                 |
  +-------------------------------------------+---------------+-------+----------------------------+
  | __recycle_$_1_1099511628776_1099511677778 | test          | TABLE | 2017-10-20 17:40:22.304025 |
  +-------------------------------------------+---------------+-------+----------------------------+
  1 row in set
  ```

  使用原始表名删除回收站中的表：

  ```sql
  obclient> PURGE TABLE test;
  ```

  或者使用回收站中的对象名删除：

  ```sql
  obclient> PURGE TABLE __recycle_$_1_1099511628776_1099511677778;
  ```

  再次查看回收站：

  ```sql
  obclient> SHOW RECYCLEBIN;
  ```

  查询结果如下：

  ```sql
  Empty set
  ```

* 删除回收站中的索引。

  可以使用原始索引名或回收站中的对象名删除索引。以下示例展示使用原始索引名删除：

  创建表、索引并删除表：

  ```sql
  obclient> CREATE TABLE t1(c1 INT);

  obclient> CREATE INDEX idx ON t1(c1);

  obclient> DROP TABLE t1;
  ```

  查看回收站：

  ```sql
  obclient> SHOW RECYCLEBIN;
  ```

  查询结果如下：

  ```sql
  +--------------------------------+----------------------------+-------+----------------------------+
  | OBJECT_NAME                    | ORIGINAL_NAME              | TYPE  | CREATETIME                 |
  +--------------------------------+----------------------------+-------+----------------------------+
  | __recycle_$_1_1597387726700872 | __idx_1101710651081557_idx | INDEX | 2020-08-14 14:48:46.699145 |
  | __recycle_$_1_1597387726712976 | t1                         | TABLE | 2020-08-14 14:48:46.712643 |
  +--------------------------------+----------------------------+-------+----------------------------+
  2 rows in set
  ```

  使用原始索引名删除回收站中的索引：

  ```sql
  obclient> PURGE INDEX idx;
  ```

  或者使用回收站中的对象名删除：

  ```sql
  obclient> PURGE INDEX __recycle_$_1_1597387726700872;
  ```

  再次查看回收站：

  ```sql
  obclient> SHOW RECYCLEBIN;
  ```

  查询结果如下：

  ```sql
  +--------------------------------+----------------------------+-------+----------------------------+
  | OBJECT_NAME                    | ORIGINAL_NAME              | TYPE  | CREATETIME                 |
  +--------------------------------+----------------------------+-------+----------------------------+
  | __recycle_$_1_1597387726712976 | t1                         | TABLE | 2020-08-14 14:48:46.712643 |
  +--------------------------------+----------------------------+-------+----------------------------+
  1 rows in set
  ```
