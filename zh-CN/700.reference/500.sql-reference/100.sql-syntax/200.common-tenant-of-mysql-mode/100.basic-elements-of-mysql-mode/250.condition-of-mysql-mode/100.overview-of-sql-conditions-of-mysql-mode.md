| Description   |                 |
|---------------|-----------------|
| keywords      |                 |
| dir-name      |                 |
| dir-name-en   |                 |
| tenant-type   | MySQL Mode      |

# SQL 条件概述

条件用来判断数据的值，它由一个或多个表达式和逻辑（布尔）运算符等组合而成，并返回 `TRUE`、`FALSE` 或 `UNKNOWN`。

条件可以称为逻辑数据类型。例如，简单条件 `1 = 1` 的判断结果为 `TRUE`。又如下面示例中，将 `salary` 的值加上 `comm_pct` 的值（函数 `NVL()` 将 `salary` 中的 NULL 值替换为 0 ），并判断总和是否大于 2500。

```sql
NVL(salary, 0) + NVL(salary + (salary * comm_pct, 0) > 2500)
```

逻辑条件可以将多个条件组合成一个条件。例如，逻辑条件 `AND` 可以将多个条件组合成一个条件。

```sql
(1 = 1) AND (5 < 7)
```

**SQL 语句中的有效条件示例如下：**

* 返回列值等于某一值的记录。

  ```sql
  name = 'ALICE'
  ```

* 返回在两个表格之间列值相同的记录。

  ```sql
  emp.deptno = dept.deptno
  ```

* 返回列值大于某一值的记录。

  ```sql
  hire_date > 2000
  ```

* 返回包含几个值中任一列值的记录。

  ```sql
  job_id IN ('SA_MAN', 'SA_REP')
  ```

* 返回列值处于某个区间的记录。

  ```sql
  salary BETWEEN 5000 AND 8000
  ```

## 使用场景

您可以在如下 SQL 语句的 `WHERE` 子句中使用条件：

* `DELETE`

* `SELECT`

* `UPDATE`

您也可以在 `SELECT` 语句的如下子句中使用条件：

* `WHERE`

* `HAVING`

## 相关文档

OceanBase 数据库当前版本支持如下条件类型：

* [比较条件](300.comparison-conditions-of-mysql-mode.md)

* [逻辑条件](500.logical-conditions-of-mysql-mode.md)

* [模式匹配条件](600.pattern-matching-conditions-of-mysql-mode.md)

* [空条件](700.empty-condition-of-mysql-mode.md)

* [复合条件](800.composite-condition-of-mysql-mode.md)

* [BETWEEN 条件](900.between-condition-of-mysql-mode.md)

* [EXISTS 条件](1000.existence-condition-of-mysql-mode.md)

* [IN 条件](1100.in-conditions-of-mysql-mode.md)
